# Babel Bible — Agent Entry Point

Single front door for any agent (or human) working on this project. Read the
docs in the order below; everything else is reference or generated state.

## What this project is

A self-study curriculum taking a learner from high-school algebra to
graduate-level mastery across math, physics, chemistry, biology, philosophy,
language, and the social sciences. Every unit is a markdown file with YAML
frontmatter under `content/` (~2,400 units, 50 sections). Each unit exists at
three depths (Beginner / Intermediate / Master) in one source document. A
companion site (built with Neutron, deployed via Teploy) lives at babelbible.org.

## Read in this order

1. **`OVERVIEW.md`** — the load-bearing vision doc: what Babel Bible is, who it's
   for, the non-negotiable invariants, and production strategy. Read first, read
   when in doubt.
2. **`docs/plans/AGENT_PRODUCTION_PLAYBOOK.md`** — self-contained recipe for
   producing one unit to 27/27 first-pass. Read top-to-bottom before writing any
   content.
3. **Current state** — the "Current state" section below (the old session-journal
   `SESSION_NOTES.md` was consolidated here and retired on public release).

## Current state

- **Curriculum:** ~2,559 units across 50 sections (~360 chapters); every produced unit passes the 27-check validator (`validate_all` 68,987/68,987).
- **Site:** live at babelbible.org (Neutron + Vite, deployed via Teploy to an OVH VPS).
- **Production status:** the Tier A/B/C campaign (math spines 1–12 + Tier C non-math gap-fills) is **complete**. Remaining work is discretionary depth/curation — the live queue lives in `plans/expansion/FINALIZATION_PLAN.md` (status table §2) and `plans/expansion/DEPTH_QUALITY_AUDIT.md` §8.
- **Known quality gap:** ~98% of units cite via `TODO_REF` placeholders. Resolution path: acquire the ~13 highest-leverage sources in `docs/catalogs/NEED_TO_SOURCE.md` (Tyler-gated), then run a citation-resolution pass.
- **Framework note:** a Neutron build OOM was fixed via lazy markdown rendering (branch `fix/content-lazy-render-oom` in the Neutron repo, commits `0acf360` + `1206c7a`; not yet merged to Neutron `main` — merge before relying on it elsewhere). Heap fell from ~12.3 GB peak to ~1.3 GB.

## Public repo

This repository is **public** and mirrored to GitHub at https://github.com/im-tyler/babel-bible.
Treat all work as public-facing. Do not commit transient session/handoff/local
context (`SESSION_NOTES.md`, `*_NOTES.md`, `HANDOFF*.md`, `.claude/settings.local.json`,
secrets) — they are gitignored as a safety net, but don't create them in the first
place. Substantive design/planning docs (`OVERVIEW.md`, `docs/`, `plans/`) are
intentional public context — keep them.

## Reference layer (consult as needed, don't read cover-to-cover)

- **`docs/specs/`** — `UNIT_SPEC.md` (canonical content format), `QUALITY_RUBRIC.md`
  (the 27-check validator gate), orchestration/scaffold protocols.
- **`docs/briefs/`** — dispatch briefs handed to producing/auditing agents
  (`PRODUCER_BRIEF.md` is the main one; also audit, deepen-replace, exercise-pack).
- **`docs/catalogs/`** — `CONCEPT_CATALOG.md` (every named concept),
  `DEPENDENCY_MAP.md` (the prereq DAG), `FASTTRACK_BOOKLIST.md`, `MATHLIB_GAPS.md`,
  `NEED_TO_SOURCE.md`.
- **`_meta/NOTATION.md`**, **`style/editorial-voice.md`** — notation and voice.
- **`scripts/validate_unit.py`** — the validator that enforces the 27 checks.

## Live planning / expansion state

- **`plans/expansion/BACKLOG.md`** — historical record: the Tier A/B/C production
  campaign (spines 1–12 + Tier C gap-fills) is **complete**. The successor queue for
  all remaining work (structural normalization, re-homing, depth, new chapters, new
  subjects) lives in **`plans/expansion/FINALIZATION_PLAN.md`** — read that first.
- **`plans/expansion/COMPREHENSIVE_AUDIT.md`** — full section-by-section audit (2026-06-15;
  now stale on mechanical BLOCKERs — the June cleanup fixed dup chapter numbers, empty
  dirs, §25/§01/§11/§19 collisions. Still authoritative on content/organizational gaps).
- **`docs/plans/MATH_EXPANSION_ORCHESTRATION.md`** + **`LENS_SYSTEM_AND_MATH_EXPANSION.md`** —
  how the math corpus grows past the Fast Track slice, and the lens/filter system.
- **`docs/plans/AGENTIC_EXECUTION_PLAN.md`**, **`FASTTRACK_COVERAGE_ROADMAP.md`** —
  agentic execution budget and Fast Track coverage strategy.
- **`docs/plans/UNIT_QUALITY_SPEC.md`** — compact quality reference (points back to
  the briefs/specs above).
- **`docs/plans/PRODUCTION_PLAN.md`** — auto-generated burn-down (regenerated by
  tooling; do not hand-edit).

## Note on history

Earlier phase/wave/cycle and per-domain seed plans (the v0.x pilot, Waves 1–4,
Cycles 4–5, the per-subject BIOLOGY/CHEMISTRY/PHYSICS/PHILOSOPHY seed plans, the
K-12 and cross-domain expansion plans, and superseded production runbooks) were
removed once the work they described shipped. They remain in git history if a
retrospective is ever needed; the docs above are the live set.
