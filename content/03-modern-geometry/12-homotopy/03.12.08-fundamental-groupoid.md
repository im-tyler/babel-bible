---
id: 03.12.08
title: Fundamental groupoid
slug: fundamental-groupoid
section: modern-geometry
chapter: homotopy
concept_catalog_id: topology.fundamental-groupoid
prerequisites:
  - 03.12.01  # homotopy and homotopy group
  - 02.01.01  # topological space
  - 02.01.02  # continuous map
successors:
  - 03.12.02  # covering space
tier_anchors:
  beginner: "Brown — Topology and Groupoids §6 (informal); 3Blue1Brown loops-and-paths intuition"
  intermediate: "Brown — Topology and Groupoids §6; May A Concise Course Ch. 2"
  master: "Brown — Topology and Groupoids §6 and §10; Brown 1967 (originator paper)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Brown — Topology and Groupoids"
    locator: "§6 fundamental groupoid; §10 covering spaces"
  - source: TODO_REF
    path: "Brown 1967 — Groupoids and van Kampen's theorem"
    locator: "Proc. London Math. Soc. 17, originator paper for the groupoid van Kampen"
  - source: TODO_REF
    path: "May — A Concise Course in Algebraic Topology"
    locator: "Ch. 2, the fundamental groupoid and van Kampen"
lean_status: none
lean_mathlib_gap: |
  Mathlib has Category and Groupoid abstract structures plus a basic
  FundamentalGroupoid skeleton in Topology/Homotopy, but no full Path
  category with concatenation up to endpoint-fixing homotopy, no
  functoriality of the groupoid construction on continuous maps proven,
  no colimit / pushout description giving the groupoid Seifert-van
  Kampen theorem, and no equivalence between covering spaces and
  representations of the fundamental groupoid. A complete formal layer
  needs all five pieces, plus the Galois correspondence theorem at the
  level of groupoid actions.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 70m
status: shipped
produced_by: claude-opus-4-7 (gap-driven from Brown audit 2026-05-07)
reviewed_by: []
---

## Intuition [Beginner]

The fundamental group $\pi_1(X, x_0)$ records loops at a chosen base point $x_0$. Once you fix that base point, you cannot talk directly about loops starting elsewhere — they live in a different group. The **fundamental groupoid** drops the choice: instead of one group at one point, it keeps a whole web of homotopy classes of paths between *every* pair of points in $X$.

The picture is a directed graph whose vertices are points of $X$ and whose edges are homotopy classes of paths $\gamma: [0,1] \to X$. Concatenating paths gives a partial multiplication — partial because you can only compose two paths when the endpoint of the first equals the starting point of the second. That "compose only when endpoints match" structure is what a *groupoid* is: a category in which every arrow has an inverse.

Why bother? When the space splits into pieces meeting at several points, the fundamental group at any single point sees only one piece. The fundamental groupoid sees them all at once and lets you reason about the global gluing without artificially picking a base point.

## Visual [Beginner]

A space with several points marked, and a few paths drawn between distinct pairs. The diagram is the data of the fundamental groupoid: each vertex is a point, each arrow is a path-up-to-homotopy.

![A schematic placeholder diagram for the fundamental groupoid.](/img/fundamental-groupoid-placeholder.svg)

When you collapse to a single base point, you recover the fundamental group as the loops at that point.

## Worked example [Beginner]

Take the circle $S^1$ with two marked points: the north pole $N$ and the south pole $S$. The fundamental group $\pi_1(S^1, N)$ is the integers — the winding number. The fundamental groupoid keeps the same winding-number data plus the homotopy classes of arcs from $N$ to $S$: there are exactly two arcs (the east semicircle and the west semicircle), and they differ by a full loop, which is one unit of winding.

So in the fundamental groupoid you have:

- arrows from $N$ to $N$: one for each integer (winding number);
- arrows from $S$ to $S$: one for each integer (winding number);
- arrows from $N$ to $S$: the east arc, plus that arc composed with any number of full loops at $N$;
- arrows from $S$ to $N$: inverses of the above.

Picking the east arc as a reference gives a bijection between $N$-to-$S$ arrows and integers — and the same for $S$-to-$N$.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Why is the fundamental groupoid called a *groupoid* rather than a *group*?

A. The composition is only defined when the endpoint of one path matches the start of the next  
B. The set is groupoid-shaped  
C. There is no identity arrow  
D. Inverses do not exist

<details class="exercise__hint"><summary>Hint</summary>

Look at when path-concatenation is defined.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**A. The composition is only defined when the endpoint of one path matches the start of the next.**

In a groupoid every arrow still has an inverse and an identity at each object — but multiplication is only partial.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

If $X$ is path-connected, what is the relationship between the fundamental groupoid $\pi_1(X)$ and the fundamental group $\pi_1(X, x_0)$ at any point $x_0$?

A. They are unrelated  
B. The groupoid is equivalent to the group viewed as a one-object groupoid  
C. The groupoid has more arrows than the group  
D. The group has more arrows than the groupoid

<details class="exercise__hint"><summary>Hint</summary>

When the space is path-connected, every two points are joined by a path.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. The groupoid is equivalent to the group viewed as a one-object groupoid.**

Choosing a path from $x_0$ to each other point gives a categorical equivalence. The two contain exactly the same homotopy information.

</details>

</aside>

## Formal definition [Intermediate+]

Let $X$ be a topological space. A **path** in $X$ is a continuous map $\gamma: [0,1] \to X$. Two paths $\gamma, \delta: [0,1] \to X$ with $\gamma(0) = \delta(0)$ and $\gamma(1) = \delta(1)$ are **path-homotopic** (rel. endpoints) if there is a continuous $H: [0,1] \times [0,1] \to X$ with

$$
H(s, 0) = \gamma(s), \quad H(s, 1) = \delta(s), \quad H(0, t) = \gamma(0), \quad H(1, t) = \gamma(1).
$$

This is an equivalence relation [01.01.03 not relevant; the relation lives on paths, not on a vector space — see [03.12.01] for the analogous relation on free homotopies].

**Definition.** The **fundamental groupoid** $\pi_1(X)$ of $X$ is the small category whose objects are the points of $X$ and whose morphisms $\mathrm{Hom}_{\pi_1(X)}(x, y)$ are the path-homotopy classes of paths from $x$ to $y$. Composition is concatenation:

$$
[\delta] \circ [\gamma] = [\delta \cdot \gamma], \qquad
(\delta \cdot \gamma)(s) = \begin{cases} \gamma(2s) & 0 \leq s \leq 1/2 \\ \delta(2s - 1) & 1/2 \leq s \leq 1 \end{cases}
$$

The constant path $c_x(s) = x$ is the identity at $x$. The reverse path $\bar\gamma(s) = \gamma(1 - s)$ represents the inverse $[\gamma]^{-1}$.

When the choice of base points matters, fix a subset $A \subseteq X$ and define $\pi_1(X, A)$ to be the full sub-groupoid on the points of $A$.

A continuous map $f: X \to Y$ induces a functor $\pi_1(f): \pi_1(X) \to \pi_1(Y)$ by $x \mapsto f(x)$ on objects and $[\gamma] \mapsto [f \circ \gamma]$ on morphisms; this assignment is well-defined on homotopy classes and respects composition, so $\pi_1$ is a functor $\mathbf{Top} \to \mathbf{Groupoid}$.

## Key theorem with proof [Intermediate+]

**Theorem.** *Let $X$ be a topological space, $x \in X$ a point. Then the automorphism group $\mathrm{Aut}_{\pi_1(X)}(x) = \mathrm{Hom}_{\pi_1(X)}(x, x)$ recovers the fundamental group:*

$$
\mathrm{Aut}_{\pi_1(X)}(x) = \pi_1(X, x).
$$

*If $X$ is path-connected, the inclusion $\{x\} \hookrightarrow X$ induces an equivalence of categories from the one-object groupoid $\mathrm{B}\,\pi_1(X, x)$ to $\pi_1(X)$.*

**Proof.** The first claim is by inspection: an automorphism at $x$ in $\pi_1(X)$ is a path-homotopy class of paths from $x$ to $x$, which is exactly an element of $\pi_1(X, x)$ as defined classically.

For the second claim, suppose $X$ is path-connected. For each $y \in X$, choose a path $\eta_y: x \to y$ (with $\eta_x$ the constant path). Define a functor $F: \pi_1(X) \to \mathrm{B}\pi_1(X, x)$ by sending every object to the unique object of $\mathrm{B}\pi_1(X, x)$ and sending $[\gamma]: y \to z$ to $[\bar\eta_z \cdot \gamma \cdot \eta_y]$, a loop at $x$. This is well-defined on path-homotopy classes (concatenation respects homotopy) and respects composition (the inserted $\eta_y$ at the join cancels with $\bar\eta_y$). The choice of $\eta$'s gives an explicit quasi-inverse, so $F$ is an equivalence. $\square$

**Bridge.** The groupoid framing builds toward the groupoid Seifert-van Kampen theorem (Brown 1967), which states that for $X = U \cup V$ with $U, V$ open and $A$ meeting every path-component of $U$, $V$, and $U \cap V$, the groupoid $\pi_1(X, A)$ is exactly the pushout of $\pi_1(U, A \cap U) \leftarrow \pi_1(U \cap V, A \cap U \cap V) \rightarrow \pi_1(V, A \cap V)$. The same data appears again in [03.12.02] (covering space), where covering spaces of $X$ correspond to functors $\pi_1(X) \to \mathbf{Set}$ — the Galois correspondence in groupoid form. Putting these together, the foundational reason the groupoid framing is "the right one" is exactly that it removes the path-connectedness hypotheses required by the group form: in disconnected unions $U \cap V$, the group version simply does not apply, while the groupoid version still gives a clean pushout.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="short-answer" markdown="1">

**Exercise 1 (easy, short answer).**

Show that for a discrete space $X$ (every point is open), the fundamental groupoid $\pi_1(X)$ has only identity arrows.

<details class="exercise__hint"><summary>Hint</summary>

Continuous maps from a connected space to a discrete space are constant.

</details>

<details class="exercise__answer"><summary>Answer</summary>

A path $\gamma: [0,1] \to X$ is continuous and $[0,1]$ is connected, so $\gamma$ is constant. The only paths are constant paths, and their homotopy classes are themselves; hence the only arrows are identities. Rubric: full credit for invoking connectedness of $[0,1]$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 2 (medium, short-answer).**

Let $X$ be the disjoint union of two path-connected spaces $X_0 \sqcup X_1$. Describe $\pi_1(X)$ in terms of $\pi_1(X_0, x_0)$ and $\pi_1(X_1, x_1)$.

<details class="exercise__hint"><summary>Hint</summary>

There are no paths between the two components.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\pi_1(X)$ is the disjoint union of two equivalent-to-one-object groupoids, each carrying its own component's fundamental group. There are no arrows between objects in different components. Rubric: full credit for noting the disjoint union has no inter-component arrows.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 3 (medium, short-answer).**

A continuous map $f: X \to Y$ induces a functor $\pi_1(f): \pi_1(X) \to \pi_1(Y)$. Prove that if $f$ is a homotopy equivalence, then $\pi_1(f)$ is an equivalence of categories.

<details class="exercise__hint"><summary>Hint</summary>

Use a homotopy inverse $g$ and the homotopies $g \circ f \simeq \mathrm{id}_X$, $f \circ g \simeq \mathrm{id}_Y$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The induced functors $\pi_1(g \circ f) = \pi_1(g) \circ \pi_1(f)$ and $\pi_1(f \circ g) = \pi_1(f) \circ \pi_1(g)$. The homotopies provide natural isomorphisms $\pi_1(g \circ f) \cong \pi_1(\mathrm{id}_X) = \mathrm{id}_{\pi_1(X)}$ and similarly for the other side. Hence $\pi_1(f)$ has $\pi_1(g)$ as a quasi-inverse and is an equivalence. Rubric: full credit for naming the natural isomorphisms induced by the homotopies.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 4 (hard, short-answer).**

Compute $\pi_1(S^1)$ as a one-object groupoid using the fundamental-groupoid framework, choosing the base point and one reference path per non-base point, and state explicitly which step uses path-connectedness.

<details class="exercise__hint"><summary>Hint</summary>

Apply the equivalence-of-categories theorem above and the classical computation $\pi_1(S^1, x_0) = \mathbb{Z}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Pick $x_0 \in S^1$ as base. For each other $y \in S^1$, choose the shorter arc $\eta_y: x_0 \to y$ (with $\eta_{x_0}$ the constant path; for the antipode either arc works, fix east). Path-connectedness is used here — without it, no such $\eta_y$ exists for points in other components. The classical winding-number argument computes $\pi_1(S^1, x_0) = \mathbb{Z}$, and the equivalence theorem gives $\pi_1(S^1) \simeq \mathrm{B}\mathbb{Z}$ as groupoids. Rubric: full credit for explicit base-point choice, the $\eta_y$ system, and naming where path-connectedness is invoked.

</details>

</aside>

## Advanced results [Master]

The fundamental groupoid $\pi_1: \mathbf{Top} \to \mathbf{Groupoid}$ is the prototype of an algebraic-topology functor that takes finite limits to limits and behaves well under disjoint unions, but does *not* preserve arbitrary colimits. The Seifert-van Kampen theorem identifies the colimit it does preserve: open covers in which the cover and intersection groupoids meet a chosen subset $A$ in every path-component.

**Theorem (Brown 1967, groupoid van Kampen).** *Let $X = U \cup V$ with $U, V$ open in $X$. Let $A \subseteq X$ meet every path-component of $U$, $V$, and $U \cap V$. Then the diagram*

$$
\pi_1(U \cap V, A \cap U \cap V) \rightrightarrows \pi_1(U, A \cap U) \,\sqcup\, \pi_1(V, A \cap V) \to \pi_1(X, A)
$$

*is a pushout in the category of groupoids.*

The classical Seifert-van Kampen theorem on fundamental *groups* is the special case in which $A$ is a single point lying in $U \cap V$ and the cover and intersection are path-connected. In that case both groupoids degenerate to one-object groupoids, the groupoid pushout collapses to a group amalgamated free product $\pi_1(U, x_0) *_{\pi_1(U \cap V, x_0)} \pi_1(V, x_0)$, and the standard Hatcher-style theorem is recovered. When $U \cap V$ is *not* path-connected — as for the open cover of $S^1$ by two overlapping arcs — the group form does not apply but the groupoid form still gives a clean answer.

**Theorem (Galois correspondence for covering spaces).** *Let $X$ be a path-connected, locally path-connected, semi-locally simply connected space. The functor that sends a covering space $p: \tilde{X} \to X$ to the functor $\pi_1(X) \to \mathbf{Set}$ defined by $x \mapsto p^{-1}(x)$ on objects and lifting on morphisms is an equivalence of categories between $\mathbf{Cov}(X)$ and $\mathbf{Set}^{\pi_1(X)}$.*

This is the groupoid version of the classical correspondence between covering spaces and subgroups of $\pi_1(X, x_0)$. The base-point-free statement is more flexible: a covering space is a functor on the whole groupoid, and the subgroup-version is recovered by restricting to the automorphism group at one chosen point.

**Synthesis.** The fundamental groupoid generalises the fundamental group from "loops at one base point" to "paths between any pair of points," with the bilinear datum being concatenation up to endpoint-fixing homotopy [03.12.01]. The central insight is that the groupoid form is exactly the right construction for spaces that are *not* path-connected — every problem in classical algebraic topology where the fundamental group runs into trouble (disconnected $U \cap V$ in van Kampen, the orbit space of a free group action by deck transformations, the Galois correspondence in disconnected covers) admits a clean groupoid solution. Read in the opposite direction, the groupoid is dual to the universal cover: $\pi_1(X)$ acts on $\tilde{X}$ exactly as the morphisms of $\pi_1(X)$ act on the corresponding fibre, and the central identifications (loops with deck transformations, paths with lifts) become statements about the groupoid acting on its associated covering. The foundational reason this works is exactly the equivalence of categories between $\mathbf{Cov}(X)$ and $\mathbf{Set}^{\pi_1(X)}$: covering spaces and groupoid representations are the same thing.

## Full proof set [Master]

**Proposition (functoriality).** *The assignment $X \mapsto \pi_1(X)$, $f \mapsto \pi_1(f)$ is a functor $\mathbf{Top} \to \mathbf{Groupoid}$.*

For each continuous $f: X \to Y$ and path $\gamma$ in $X$, the composition $f \circ \gamma$ is a path in $Y$. If $H$ is an endpoint-fixing homotopy from $\gamma$ to $\delta$, then $f \circ H$ is one from $f \circ \gamma$ to $f \circ \delta$, so $\pi_1(f)$ is well-defined on homotopy classes. Identities go to identities (constant paths are preserved) and composition is preserved because $f \circ (\delta \cdot \gamma) = (f \circ \delta) \cdot (f \circ \gamma)$. $\square$

**Proposition (homotopy invariance).** *If $f \simeq g: X \to Y$ via a homotopy $H$, then $\pi_1(f) \simeq \pi_1(g)$ as functors via a natural transformation $\eta_H$ whose component at $x$ is the path $t \mapsto H(x, t)$ in $Y$.*

Naturality $\pi_1(g)([\gamma]) \circ \eta_{H, x_0} = \eta_{H, x_1} \circ \pi_1(f)([\gamma])$ is the statement that the diagonal of the unit square $H \circ (\gamma \times \mathrm{id}_{[0,1]})$ has the same homotopy class read horizontally and vertically — a direct consequence of continuity of $H$ on the closed unit square. $\square$

**Proposition (van Kampen pushout, sketch).** *Under the hypotheses above, $\pi_1(X, A)$ is the pushout.*

Given a groupoid $G$ and functors $F_U: \pi_1(U, A \cap U) \to G$ and $F_V$ agreeing on the intersection, define $F: \pi_1(X, A) \to G$ on a path $\gamma: a \to a'$ in $X$ by subdividing $\gamma$ via the Lebesgue number lemma into pieces lying entirely in $U$ or in $V$, then composing the images under $F_U$ and $F_V$. The choice of subdivision is unique up to refinements, and $F_U$, $F_V$ agreeing on $\pi_1(U \cap V)$ ensures the composite is independent of subdivision. Path-homotopy invariance follows by applying the same subdivision argument to a homotopy on $[0,1]^2$. $\square$

## Connections [Master]

- **Homotopy and homotopy group [03.12.01].** The fundamental group is the automorphism group of any object in the fundamental groupoid; $\pi_1(X, x_0) = \mathrm{Aut}_{\pi_1(X)}(x_0)$. Every result on $\pi_1$ at a single base point lifts to the groupoid by choosing path-systems.

- **Covering space [03.12.02].** The Galois correspondence becomes an equivalence of categories $\mathbf{Cov}(X) \simeq \mathbf{Set}^{\pi_1(X)}$; the classical subgroup-classification follows by restricting to the automorphism group at a chosen base point.

- **Topological space [02.01.01] and continuous map [02.01.02].** The construction is functorial in the underlying topological data: a continuous map of spaces gives a functor of groupoids, and a homotopy of maps gives a natural transformation. Homotopy equivalences go to equivalences of groupoids.

- **Higher homotopy.** The groupoid construction is the $n=1$ case of an entire tower of higher-dimensional groupoids — the *fundamental $\infty$-groupoid* — which captures all homotopy information of $X$. Brown-Higgins-Sivera (2011, *Nonabelian Algebraic Topology*) develops this systematically; the connection to higher categories and homotopy type theory is the modern home of this machinery.

## Historical & philosophical context [Master]

The fundamental group was introduced by Henri Poincaré in *Analysis Situs* (1895, *Journal de l'École Polytechnique*) [ref: TODO_REF Poincaré 1895] as the first homotopy invariant of a topological space. Poincaré's framing was already implicitly groupoid — he considered loops at *every* point — but the explicit categorical formulation had to wait nearly seventy years.

Ronald Brown's 1967 paper *Groupoids and van Kampen's theorem* (*Proc. London Math. Soc.* 17, 385–401) [ref: TODO_REF Brown 1967] is the originator paper for the groupoid version of the Seifert-van Kampen theorem. Brown observed that the classical theorem's path-connectedness hypothesis on $U \cap V$ was an artefact of the single-base-point setup, and that replacing the fundamental group by the fundamental groupoid removed the hypothesis cleanly. The textbook *Topology and Groupoids* (1968 first edition as *Elements of Modern Topology*, 1988 expansion, 2006 third edition) [ref: TODO_REF Brown 2006] develops the entire programme: identification topology, function spaces, fibrations, covering spaces, and higher-dimensional algebraic topology, all through the groupoid lens.

The further programme — replacing fundamental groups by fundamental ∞-groupoids — was developed by Brown, Higgins, Sivera, and others (most fully in *Nonabelian Algebraic Topology*, 2011) [ref: TODO_REF Brown-Higgins-Sivera 2011], and finds its modern home in the higher-categorical and homotopy-type-theoretic foundations of contemporary algebraic topology.

## Bibliography [Master]

```bibtex
@book{BrownTopologyAndGroupoids,
  author    = {Brown, Ronald},
  title     = {Topology and Groupoids},
  publisher = {BookSurge},
  year      = {2006},
  edition   = {3rd},
  note      = {Free PDF on author's site}
}

@article{Brown1967Groupoids,
  author  = {Brown, Ronald},
  title   = {Groupoids and van {K}ampen's theorem},
  journal = {Proc. London Math. Soc.},
  volume  = {17},
  year    = {1967},
  pages   = {385--401}
}

@book{BrownHigginsSiveraNonabelian,
  author    = {Brown, Ronald and Higgins, Philip J. and Sivera, Rafael},
  title     = {Nonabelian Algebraic Topology: Filtered Spaces, Crossed Complexes, Cubical Homotopy Groupoids},
  publisher = {European Mathematical Society},
  year      = {2011}
}

@book{MayConciseAlgebraicTopology,
  author    = {May, J. Peter},
  title     = {A Concise Course in Algebraic Topology},
  publisher = {University of Chicago Press},
  year      = {1999}
}

@article{Poincare1895AnalysisSitus,
  author  = {Poincar{\'e}, Henri},
  title   = {Analysis {S}itus},
  journal = {Journal de l'{\'E}cole Polytechnique},
  volume  = {1},
  year    = {1895},
  pages   = {1--121}
}
```
