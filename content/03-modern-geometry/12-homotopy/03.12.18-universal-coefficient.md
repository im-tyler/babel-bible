---
id: 03.12.18
title: Universal coefficient theorem
slug: universal-coefficient
section: modern-geometry
chapter: homotopy
concept_catalog_id: topology.universal-coefficient
prerequisites:
  - 03.12.11  # singular homology
  - 03.12.13  # cellular homology
successors:
  - 03.13.02  # Leray-Serre spectral sequence
tier_anchors:
  beginner: "Hatcher *Algebraic Topology* §3.A informal opening on changing coefficients"
  intermediate: "Hatcher §3.A and §3.1; Weibel *An Introduction to Homological Algebra* §3.6"
  master: "Hatcher §3.1 + §3.A (full proof); Cartan-Eilenberg *Homological Algebra* (1956); tom Dieck *Algebraic Topology* §17; Spanier *Algebraic Topology* §V"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Hatcher — Algebraic Topology"
    locator: "§3.1 Theorem 3.2 (UCT for cohomology), §3.A Theorem 3A.3 (UCT for homology), splitting and naturality discussion"
  - source: TODO_REF
    path: "Cartan-Eilenberg — Homological Algebra"
    locator: "Princeton 1956, Ch. V derived functors, Ch. VI Tor and Ext, applications to singular chain complexes"
  - source: TODO_REF
    path: "Weibel — An Introduction to Homological Algebra"
    locator: "§3.6 universal coefficient theorems, §3.4 Tor and §3.3 Ext as derived functors"
  - source: TODO_REF
    path: "tom Dieck — Algebraic Topology"
    locator: "§17 universal coefficients and Künneth"
  - source: TODO_REF
    path: "Spanier — Algebraic Topology"
    locator: "Ch. V universal coefficient theorems for homology and cohomology"
  - source: TODO_REF
    path: "Eilenberg-MacLane — Group extensions and homology"
    locator: "Ann. Math. 43 (1942) 757-831, originator paper for $\\mathrm{Ext}$ in the topological context"
  - source: TODO_REF
    path: "Hilton-Stammbach — A Course in Homological Algebra"
    locator: "Springer GTM 4, 1971; Ch. III, IV, modern repackaging of Cartan-Eilenberg"
lean_status: none
lean_mathlib_gap: |
  Mathlib has the algebraic infrastructure for derived functors in
  `CategoryTheory.Abelian.DerivedFunctor` and a notion of $\mathrm{Tor}$
  and $\mathrm{Ext}$ via projective resolutions in
  `Algebra.Homology.DerivedCategory.Ext`, but does not yet ship the
  universal coefficient theorem for singular (co)homology as a named
  theorem. The required pieces are: a free resolution of an arbitrary
  abelian group of length one (the structure theorem for finitely
  generated abelian groups generalises to arbitrary subgroups of free
  abelian groups, giving $\mathrm{Tor}_i = \mathrm{Ext}^i = 0$ for
  $i \geq 2$), the algebraic Künneth-style short exact sequence for a
  free chain complex tensored with an abelian group, the dual short
  exact sequence for $\mathrm{Hom}(C_\bullet, G)$, the splitting
  argument using a chain-level lift of cycles, and the topological
  application identifying the chain complex of singular chains as
  free. Each piece is formalisable from existing infrastructure; the
  consolidation as `singularHomology_uct` and `singularCohomology_uct`
  named theorems is the formalisation target.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 35m
  master: 70m
status: shipped
produced_by: claude-opus-4-7 (parallel agent batch 4 2026-05-07)
reviewed_by: []
---

## Intuition [Beginner]

The universal coefficient theorem answers a basic bookkeeping question. Once you know the homology of a space $X$ with integer coefficients, can you read off the homology with coefficients in some other group, like $\mathbb{Z}/2$ or $\mathbb{Q}$? The answer is almost yes. The integer homology determines the answer up to one extra correction, and the correction is computable from the integer homology alone.

The intuition is that integer homology already records everything the space has to offer, but in a form where torsion (cycles that vanish only after being multiplied by some integer) is mixed in with free generators. Switching coefficients is a kind of measurement: a field of characteristic zero (like $\mathbb{Q}$) cannot see torsion, while $\mathbb{Z}/2$ sees torsion of order two but is blind to other features. The theorem says exactly how each measurement reads the integer answer.

The reason this matters: many computations are easier with coefficients in a field, because then everything becomes a vector space and dimensions add up. The theorem lets you do the computation in the easier setting and reconstruct the integer answer from a small handful of cases.

## Visual [Beginner]

A schematic showing two columns: on the left, the integer homology groups of a space, written as a free part plus a torsion part; on the right, the homology with new coefficients, computed by tensoring the free part and applying a correction term to the torsion. An arrow between them is labelled "universal coefficient theorem" and a small caption indicates that the correction comes from a derived functor.

![A schematic placeholder showing integer homology on the left, the new-coefficient homology on the right, and the correction term coming from torsion.](/img/universal-coefficient-placeholder.svg)

The picture captures the essential message: the integer answer determines the new answer, but you have to track torsion and free pieces separately. Free pieces tensor straightforwardly; torsion pieces undergo a correction.

## Worked example [Beginner]

Compute the cohomology of the real projective plane $\mathbb{RP}^2$ with integer and with $\mathbb{Z}/2$ coefficients, starting from the known integer homology.

Step 1. The integer homology of $\mathbb{RP}^2$ is $H_0 = \mathbb{Z}$, $H_1 = \mathbb{Z}/2$, $H_2 = 0$. The free rank in dimension zero comes from path-connectedness; the torsion in dimension one comes from the orientation-reversing loop; the higher dimension is zero because the space is two-dimensional.

Step 2. To get integer cohomology, the universal coefficient theorem says $H^n$ equals the dual of the free part of $H_n$ plus a correction from the torsion of $H_{n-1}$. In dimension zero, $H^0 = \mathbb{Z}$ (the free part of $H_0$, no correction since $H_{-1} = 0$). In dimension one, $H^1 = 0$ because $H_1 = \mathbb{Z}/2$ has no free part and $H_0 = \mathbb{Z}$ has no torsion. In dimension two, $H^2 = \mathbb{Z}/2$ because $H_2 = 0$ has no free part but $H_1 = \mathbb{Z}/2$ contributes a correction of $\mathbb{Z}/2$.

Step 3. The integer cohomology is $H^* = \mathbb{Z}, 0, \mathbb{Z}/2$. The torsion has shifted up by one dimension compared to the homology — this is a general feature.

Step 4. With $\mathbb{Z}/2$ coefficients, every group becomes a $\mathbb{Z}/2$-vector space and the picture simplifies. A direct application gives $H^*(\mathbb{RP}^2; \mathbb{Z}/2) = \mathbb{Z}/2, \mathbb{Z}/2, \mathbb{Z}/2$ — the cohomology gains a class in every dimension. This reflects the fact that with $\mathbb{Z}/2$ coefficients, both the free part and the order-two torsion contribute.

What this tells us: the universal coefficient theorem turns one homology computation into many. Once the integer homology of $\mathbb{RP}^2$ is known, the answer with any coefficient group follows from a small dictionary.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

If a space $X$ has integer homology $H_0 = \mathbb{Z}$, $H_1 = \mathbb{Z}/2$, $H_2 = 0$ in low dimensions, what is $H_1(X; \mathbb{Q})$?

A. $\mathbb{Q}$  
B. $0$  
C. $\mathbb{Q}/2$  
D. $\mathbb{Z}/2$

<details class="exercise__hint"><summary>Hint</summary>

A field of characteristic zero cannot see torsion. The correction term involves tensoring torsion with $\mathbb{Q}$, and that vanishes.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. $0$.**

The free part of $H_1 = \mathbb{Z}/2$ is zero, so the tensor part of the universal coefficient theorem vanishes. The correction term involves $H_0 = \mathbb{Z}$, which has no torsion, so that vanishes too. Both pieces are zero.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: with rational coefficients, every torsion class in integer homology vanishes.

<details class="exercise__hint"><summary>Hint</summary>

Tensoring a finite abelian group with $\mathbb{Q}$ gives the zero group.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Tensoring a torsion abelian group with $\mathbb{Q}$ yields zero, and the correction term in the universal coefficient theorem involves only torsion. With $\mathbb{Q}$ coefficients, the homology becomes the free rank of the integer homology tensored with $\mathbb{Q}$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What does the universal coefficient theorem allow you to compute?

A. The fundamental group from the homology  
B. Homology with new coefficients from integer homology  
C. The Euler characteristic from the cellular structure  
D. The homotopy type from the cohomology ring

<details class="exercise__hint"><summary>Hint</summary>

The theorem is a coefficient-changing tool, not a homotopy or homotopical invariant.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. Homology with new coefficients from integer homology.**

The theorem expresses $H_n(X; G)$ in terms of $H_n(X; \mathbb{Z})$ and $H_{n-1}(X; \mathbb{Z})$ via tensor and correction terms. The other answers are unrelated to the theorem's content.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A$ and $G$ be abelian groups. The **tensor product** $A \otimes G$ is the abelian group generated by symbols $a \otimes g$ for $a \in A$, $g \in G$, modulo bilinearity relations. The **Hom group** $\mathrm{Hom}(A, G)$ is the abelian group of group homomorphisms $A \to G$, with addition pointwise.

For a fixed $G$, the assignments $A \mapsto A \otimes G$ and $A \mapsto \mathrm{Hom}(A, G)$ extend to functors on the category of abelian groups. The first is right-exact; the second is left-exact. They fail to be exact at one end, and the failure is measured by the derived functors $\mathrm{Tor}(-, G)$ and $\mathrm{Ext}(-, G)$.

**Definition (Tor and Ext).** Let $A$ be an abelian group. Choose a **free resolution** $0 \to F_1 \to F_0 \to A \to 0$, where $F_0, F_1$ are free abelian groups. (Every abelian group admits such a resolution of length at most one because subgroups of free abelian groups are free.) Define
$$
\mathrm{Tor}(A, G) = \ker(F_1 \otimes G \to F_0 \otimes G), \qquad \mathrm{Ext}(A, G) = \mathrm{coker}(\mathrm{Hom}(F_0, G) \to \mathrm{Hom}(F_1, G)).
$$
Both groups are independent of the chosen resolution up to canonical isomorphism.

For a topological space $X$ and a coefficient group $G$, the **homology with coefficients** $H_n(X; G)$ is defined as the homology of the chain complex $C_\bullet(X) \otimes G$, where $C_\bullet(X)$ is the singular chain complex with integer coefficients. The **cohomology with coefficients** $H^n(X; G)$ is the cohomology of the cochain complex $\mathrm{Hom}(C_\bullet(X), G)$.

A **short exact sequence** of abelian groups
$$
0 \to A \to B \to C \to 0
$$
is **split** if there exists a homomorphism $C \to B$ section of the projection. A split short exact sequence is isomorphic to the sum sequence $0 \to A \to A \oplus C \to C \to 0$.

### Counterexamples to common slips

- The splitting in the universal coefficient theorem is *not* natural in $X$. A continuous map $f: X \to Y$ induces compatible maps on the three terms of the short exact sequence, but the chosen splittings need not commute with $f_*$. Naturality fails on the splitting; the short exact sequence itself is natural.
- The correction term in the cohomology version uses $\mathrm{Ext}(H_{n-1}, G)$, not $\mathrm{Ext}(H_n, G)$. The dimension shift is opposite to the homology version: tensor pairs $H_n$ with $G$, $\mathrm{Tor}$ pairs $H_{n-1}$ with $G$; $\mathrm{Hom}$ pairs $H_n$ with $G$, $\mathrm{Ext}$ pairs $H_{n-1}$ with $G$.
- $\mathrm{Tor}(\mathbb{Z}/n, G)$ is *not* the $n$-torsion of $G$ in general; it equals the kernel of multiplication by $n$ on $G$. For $G$ free, this is zero, but for $G = \mathbb{Z}/m$ it is $\mathbb{Z}/\gcd(n, m)$.
- The free-resolution definition gives $\mathrm{Tor}_i = 0$ for $i \geq 2$ over $\mathbb{Z}$, because every abelian group has a free resolution of length one. Over more general rings (group rings, polynomial rings), higher $\mathrm{Tor}$ groups appear and the universal coefficient theorem becomes a spectral sequence.

## Key theorem with proof [Intermediate+]

**Theorem (Universal coefficient theorem for homology; Hatcher Theorem 3A.3).** *Let $X$ be a topological space and $G$ an abelian group. There is a short exact sequence of abelian groups*
$$
0 \to H_n(X) \otimes G \to H_n(X; G) \to \mathrm{Tor}(H_{n-1}(X), G) \to 0
$$
*natural in $X$ and in $G$, and the sequence splits (the splitting is not natural).*

**Proof.** The argument is purely algebraic once one observes that the singular chain complex $C_\bullet(X)$ consists of free abelian groups (free on the set of singular simplices in each degree). The theorem follows from the corresponding algebraic statement applied to this chain complex.

**Algebraic version.** Let $C_\bullet$ be a chain complex of free abelian groups with boundary $\partial$ and let $G$ be an abelian group. Write $Z_n = \ker(\partial_n)$ and $B_n = \mathrm{im}(\partial_{n+1})$ for cycles and boundaries. Both are subgroups of the free abelian group $C_n$, hence themselves free.

The short exact sequence
$$
0 \to Z_n \to C_n \xrightarrow{\partial} B_{n-1} \to 0
$$
splits because $B_{n-1}$ is free, so a splitting $s: B_{n-1} \to C_n$ exists. Tensoring with $G$ preserves split exactness:
$$
0 \to Z_n \otimes G \to C_n \otimes G \xrightarrow{\partial \otimes 1} B_{n-1} \otimes G \to 0.
$$
Splice these short exact sequences across $n$ to form the long exact sequence relating $H_n(C_\bullet \otimes G)$ to the homology of the complexes $Z_\bullet \otimes G$ and $B_\bullet \otimes G$ (each with zero differential). Since the differentials on $Z_\bullet$ and $B_\bullet$ vanish, their homologies coincide with the groups themselves, and the long exact sequence reduces to the short exact sequence
$$
0 \to H_n(C_\bullet) \otimes G \to H_n(C_\bullet \otimes G) \to \ker(B_{n-1} \otimes G \to Z_{n-1} \otimes G) \to 0.
$$
The kernel on the right is identified with $\mathrm{Tor}(H_{n-1}(C_\bullet), G)$ via the free resolution $0 \to B_{n-1} \to Z_{n-1} \to H_{n-1}(C_\bullet) \to 0$. This is the asserted short exact sequence.

**Splitting.** Choose a splitting $r_n: C_n \to Z_n$ of the inclusion $Z_n \hookrightarrow C_n$. (This exists because $Z_n$ is free and the quotient $C_n / Z_n \cong B_{n-1}$ is free.) The composite $C_n \otimes G \to Z_n \otimes G \to H_n(C_\bullet) \otimes G$ defines a left inverse on $H_n(C_\bullet \otimes G)$, providing the splitting. The choice of $r_n$ depends on $C_\bullet$, so the splitting is not natural.

**Topological case.** Apply the algebraic version to $C_\bullet = C_\bullet(X)$, the singular chain complex. Naturality in $X$ follows because the construction uses only the chain-map functoriality of $C_\bullet(-)$. Naturality in $G$ follows because $\otimes$ and $\mathrm{Tor}$ are functorial in their second argument. $\square$

**Theorem (Universal coefficient theorem for cohomology; Hatcher Theorem 3.2).** *Let $X$ be a topological space and $G$ an abelian group. There is a natural short exact sequence*
$$
0 \to \mathrm{Ext}(H_{n-1}(X), G) \to H^n(X; G) \to \mathrm{Hom}(H_n(X), G) \to 0
$$
*and the sequence splits (the splitting is not natural).*

The proof parallels the homology version, replacing $\otimes G$ by $\mathrm{Hom}(-, G)$ at each step, with care that $\mathrm{Hom}$ is contravariant in the first argument and so dimension indexing flips. Details are deferred to the Master section.

**Bridge.** The universal coefficient theorem builds toward the entire coefficient-changing infrastructure of algebraic topology. The foundational reason it holds is exactly that the singular chain complex consists of free abelian groups, so tensoring and Hom-ing preserve enough exactness to leave only the derived functors $\mathrm{Tor}$ and $\mathrm{Ext}$ as corrections. This is the same algebraic mechanism that appears again in [03.13.02] (Leray-Serre spectral sequence), where the $E_2$ page identifies as $H^p(B; H^q(F; G))$ with the inner cohomology already involving Tor and Ext through the universal coefficient theorem applied fibrewise. The central insight is that ordinary homology with arbitrary coefficients factors algebraically through integer homology — putting these together, every coefficient-changing question reduces to one tensor product and one correction term, and the correction is computable from the integer answer alone. The bridge is the recognition that Tor and Ext are not auxiliary curiosities but the only obstructions to coefficient change being a clean tensor or Hom, and this same pattern appears again in [03.13.02] (spectral sequences) when computing $E_2$ pages in the presence of torsion in fibre or base.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Compute the rank of $H_2(\mathbb{RP}^2; \mathbb{Q})$ using the universal coefficient theorem.

<details class="exercise__hint"><summary>Hint</summary>

The integer homology in dimensions one and two has $H_1 = \mathbb{Z}/2$, $H_2 = 0$. Tensoring or Tor-ing torsion with $\mathbb{Q}$ vanishes.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0$.** The tensor part is $H_2(\mathbb{RP}^2) \otimes \mathbb{Q} = 0 \otimes \mathbb{Q} = 0$. The correction is $\mathrm{Tor}(H_1(\mathbb{RP}^2), \mathbb{Q}) = \mathrm{Tor}(\mathbb{Z}/2, \mathbb{Q}) = 0$, since $\mathbb{Q}$ is torsion-free. Both pieces vanish; rank zero.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

Compute the dimension of $H^2(\mathbb{RP}^2; \mathbb{Z}/2)$ as a $\mathbb{Z}/2$-vector space.

<details class="exercise__hint"><summary>Hint</summary>

Use the cohomology universal coefficient theorem with $G = \mathbb{Z}/2$. Both $\mathrm{Hom}$ and $\mathrm{Ext}$ pieces contribute.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$1$.** The Hom part is $\mathrm{Hom}(H_2(\mathbb{RP}^2), \mathbb{Z}/2) = \mathrm{Hom}(0, \mathbb{Z}/2) = 0$. The Ext part is $\mathrm{Ext}(H_1(\mathbb{RP}^2), \mathbb{Z}/2) = \mathrm{Ext}(\mathbb{Z}/2, \mathbb{Z}/2) = \mathbb{Z}/2$. Total dimension one.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Compute $\mathrm{Tor}(\mathbb{Z}/m, \mathbb{Z}/n)$ for arbitrary positive integers $m, n$.

<details class="exercise__hint"><summary>Hint</summary>

Use the free resolution $0 \to \mathbb{Z} \xrightarrow{m} \mathbb{Z} \to \mathbb{Z}/m \to 0$ and tensor with $\mathbb{Z}/n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\mathbb{Z}/\gcd(m, n)$.** Tensoring the resolution with $\mathbb{Z}/n$ gives $\mathbb{Z}/n \xrightarrow{m} \mathbb{Z}/n \to \mathbb{Z}/m \otimes \mathbb{Z}/n \to 0$. The kernel of multiplication by $m$ on $\mathbb{Z}/n$ is $\{x \in \mathbb{Z}/n : mx = 0\} = \mathbb{Z}/\gcd(m, n)$. Rubric: full credit for the explicit gcd answer with the resolution argument.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 4 (medium, short-answer).**

Show that for $G$ a torsion-free abelian group, the universal coefficient theorem for homology collapses to $H_n(X; G) \cong H_n(X) \otimes G$.

<details class="exercise__hint"><summary>Hint</summary>

A torsion-free abelian group is flat: tensoring is exact. Equivalently, $\mathrm{Tor}(A, G) = 0$ for every $A$ when $G$ is torsion-free.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For $G$ torsion-free, the functor $- \otimes G$ is exact (torsion-free abelian groups are flat over $\mathbb{Z}$), so the derived functor $\mathrm{Tor}(-, G)$ vanishes identically. The universal coefficient short exact sequence becomes
$$
0 \to H_n(X) \otimes G \to H_n(X; G) \to 0 \to 0,
$$
forcing the first map to be an isomorphism. Examples: $G = \mathbb{Q}, \mathbb{R}, \mathbb{C}$, and any free abelian group. Rubric: full credit for the flat-resolution argument and one explicit example.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Compute $H^*(\mathbb{RP}^n; \mathbb{Z})$ for $n \geq 1$ using the integer homology
$$
H_k(\mathbb{RP}^n; \mathbb{Z}) = \begin{cases} \mathbb{Z} & k = 0 \\ \mathbb{Z}/2 & 0 < k < n, \, k \text{ odd} \\ \mathbb{Z} & k = n, \, n \text{ odd} \\ 0 & \text{otherwise} \end{cases}
$$
and the universal coefficient theorem.

<details class="exercise__hint"><summary>Hint</summary>

For each $k$ compute $\mathrm{Hom}(H_k, \mathbb{Z})$ and $\mathrm{Ext}(H_{k-1}, \mathbb{Z})$ separately; recall $\mathrm{Hom}(\mathbb{Z}/m, \mathbb{Z}) = 0$ and $\mathrm{Ext}(\mathbb{Z}/m, \mathbb{Z}) = \mathbb{Z}/m$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For $n$ even: $H^0 = \mathbb{Z}$; for $0 < k < n$, $H^k = 0$ if $k$ odd ($\mathrm{Hom}$ piece zero, $\mathrm{Ext}$ piece zero), and $H^k = \mathbb{Z}/2$ if $k$ even (Ext piece from $H_{k-1} = \mathbb{Z}/2$); $H^n = \mathbb{Z}/2$ from the same Ext correction. So $H^*(\mathbb{RP}^{2m}; \mathbb{Z}) = \mathbb{Z}, 0, \mathbb{Z}/2, 0, \mathbb{Z}/2, \ldots, \mathbb{Z}/2$ ending at degree $2m$.

For $n$ odd: same pattern except $H^n = \mathbb{Z}$ from the $\mathrm{Hom}(\mathbb{Z}, \mathbb{Z})$ piece, with no Ext correction since $H_{n-1} = 0$ for $n$ odd $\geq 3$. So $H^*(\mathbb{RP}^{2m+1}; \mathbb{Z}) = \mathbb{Z}, 0, \mathbb{Z}/2, 0, \mathbb{Z}/2, \ldots, \mathbb{Z}/2, \mathbb{Z}$. Rubric: full credit for both cases worked out by dimension.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 6 (medium, short-answer).**

Show that the splitting of the universal coefficient short exact sequence is not natural by exhibiting a continuous map $f: X \to Y$ for which no choice of splittings is compatible with $f_*$.

<details class="exercise__hint"><summary>Hint</summary>

Take $X = S^1$, $Y = \mathbb{RP}^2$, and let $f$ be the inclusion of the equator (a degree-two map onto the generator of $\pi_1$). Use $\mathbb{Z}/2$ coefficients.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $f: S^1 \to \mathbb{RP}^2$ be the standard inclusion classifying the generator of $\pi_1(\mathbb{RP}^2) = \mathbb{Z}/2$ (it factors through the universal cover then projects). With $\mathbb{Z}/2$ coefficients, $H_1(S^1; \mathbb{Z}/2) = \mathbb{Z}/2$ and $H_1(\mathbb{RP}^2; \mathbb{Z}/2) = \mathbb{Z}/2$. The universal coefficient short exact sequence for $S^1$ has $\mathrm{Tor}(H_0, \mathbb{Z}/2) = \mathrm{Tor}(\mathbb{Z}, \mathbb{Z}/2) = 0$, so the sequence splits as $H_1(S^1; \mathbb{Z}/2) \cong H_1(S^1) \otimes \mathbb{Z}/2 = \mathbb{Z} \otimes \mathbb{Z}/2 = \mathbb{Z}/2$. For $\mathbb{RP}^2$, the $\mathrm{Tor}$ term equals $\mathbb{Z}/2$ and the tensor term is zero, so the splitting selects the $\mathrm{Tor}$ summand. The map $f_*$ on $H_1$ with $\mathbb{Z}/2$ coefficients is forced by functoriality, and a quick chain-level check shows the canonical splittings on each side cannot be made simultaneously compatible: $f_*$ sends the tensor summand of $S^1$ to the $\mathrm{Tor}$ summand of $\mathbb{RP}^2$. Rubric: full credit for any specific example with the dimension-zero / dimension-one mixing argument.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Prove that for a finite CW complex $X$ and a field $\mathbb{F}$, the Euler characteristic $\chi(X) = \sum_n (-1)^n \dim_\mathbb{F} H_n(X; \mathbb{F})$ is independent of $\mathbb{F}$.

<details class="exercise__hint"><summary>Hint</summary>

Use the universal coefficient theorem and the structure theorem for finitely generated abelian groups to write $H_n(X; \mathbb{Z}) = \mathbb{Z}^{r_n} \oplus T_n$, then compute dimensions over each $\mathbb{F}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For a finite CW complex, $H_n(X; \mathbb{Z}) = \mathbb{Z}^{r_n} \oplus T_n$ with $T_n$ a finite torsion group. By the universal coefficient theorem,
$$
H_n(X; \mathbb{F}) = (H_n(X) \otimes \mathbb{F}) \oplus \mathrm{Tor}(H_{n-1}(X), \mathbb{F}).
$$
The free part contributes $\dim_\mathbb{F}(\mathbb{Z}^{r_n} \otimes \mathbb{F}) = r_n$. The torsion part contributes $\dim_\mathbb{F}(T_n \otimes \mathbb{F}) + \dim_\mathbb{F} \mathrm{Tor}(T_{n-1}, \mathbb{F})$. For any field $\mathbb{F}$ of characteristic $p$, $T \otimes \mathbb{F}$ and $\mathrm{Tor}(T, \mathbb{F})$ both equal the $p$-torsion subgroup of $T$ tensored with $\mathbb{F}$ as $\mathbb{F}$-vector spaces of equal dimension. The alternating sum
$$
\sum_n (-1)^n \dim_\mathbb{F} H_n(X; \mathbb{F}) = \sum_n (-1)^n r_n + \sum_n (-1)^n (t_n + t_{n-1}^{p}),
$$
where $t_n^p = \dim_\mathbb{F}(p\text{-torsion of } T_n) \otimes \mathbb{F}$. The torsion contributions telescope: $\sum_n (-1)^n (t_n^p + t_{n-1}^p) = 0$ by re-indexing. The Euler characteristic equals $\sum_n (-1)^n r_n$, the alternating sum of integer ranks, independent of $\mathbb{F}$. Rubric: full credit for the telescoping argument.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 8 (hard, short-answer).**

The Bockstein homomorphism associated to the short exact sequence $0 \to \mathbb{Z} \xrightarrow{n} \mathbb{Z} \to \mathbb{Z}/n \to 0$ is the connecting map $\beta: H_k(X; \mathbb{Z}/n) \to H_{k-1}(X; \mathbb{Z})$ in the induced long exact sequence in homology. Show that $\beta$ detects the torsion piece of integer homology in the sense that the composition $H_k(X; \mathbb{Z}/n) \xrightarrow{\beta} H_{k-1}(X; \mathbb{Z}) \to H_{k-1}(X; \mathbb{Z}/n)$ has image inside the kernel of multiplication by $n$, and identify this composition with the universal-coefficient $\mathrm{Tor}$ correction term.

<details class="exercise__hint"><summary>Hint</summary>

Apply the long exact sequence of homology to the coefficient sequence $0 \to \mathbb{Z} \to \mathbb{Z} \to \mathbb{Z}/n \to 0$. Compare with the universal coefficient short exact sequence in degrees $k$ and $k-1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The coefficient short exact sequence induces a long exact sequence
$$
\cdots \to H_k(X; \mathbb{Z}) \xrightarrow{n} H_k(X; \mathbb{Z}) \to H_k(X; \mathbb{Z}/n) \xrightarrow{\beta} H_{k-1}(X; \mathbb{Z}) \xrightarrow{n} H_{k-1}(X; \mathbb{Z}) \to \cdots
$$
The image of $\beta$ in $H_{k-1}(X; \mathbb{Z})$ lies in the kernel of multiplication by $n$, which is the $n$-torsion subgroup of $H_{k-1}(X; \mathbb{Z})$. Composing with the reduction mod $n$ map gives a homomorphism whose image is precisely the $n$-torsion of $H_{k-1}$ embedded in $H_{k-1}(X; \mathbb{Z}/n)$. By the universal coefficient theorem, $\mathrm{Tor}(H_{k-1}(X), \mathbb{Z}/n) = \{x \in H_{k-1}(X) : nx = 0\}$ — the $n$-torsion subgroup. The Bockstein composite is the same as the inclusion of $\mathrm{Tor}(H_{k-1}, \mathbb{Z}/n)$ into $H_{k-1}(X; \mathbb{Z}/n)$ via the universal coefficient sequence. The Bockstein is the homological detector of exactly the torsion piece that the universal coefficient theorem records. Rubric: full credit for the long-exact-sequence identification with the Tor term.

</details>

</aside>

## Lean formalization [Intermediate+]

Mathlib has the categorical infrastructure for derived functors and a definition of $\mathrm{Tor}$ and $\mathrm{Ext}$ via the abelian-category derived functor framework, but the universal coefficient theorem for singular (co)homology is not yet packaged as a named theorem. The intended formalisation would read schematically:

```lean
import Mathlib.Algebra.Homology.HomologicalComplex
import Mathlib.AlgebraicTopology.SingularSet
import Mathlib.CategoryTheory.Abelian.DerivedFunctor

/-- The universal coefficient theorem for the homology of a free chain complex. -/
theorem chain_complex_uct
    (C : ChainComplex AddCommGrp ℕ) (hC : ∀ n, Module.Free ℤ (C.X n))
    (G : AddCommGrp) (n : ℕ) :
    ∃ (e : ShortExact
        (TensorProduct ℤ (C.homology n) G)
        ((C.tensorRight G).homology n)
        (Tor 1 (C.homology (n-1)) G)),
      e.Splits :=
  sorry  -- algebraic UCT: split SES via free-resolution argument

/-- The universal coefficient theorem for singular homology. -/
theorem singular_uct (X : TopCat) (G : AddCommGrp) (n : ℕ) :
    ShortExact
      (TensorProduct ℤ (singularHomology X n) G)
      (singularHomologyWithCoeffs X G n)
      (Tor 1 (singularHomology X (n-1)) G) :=
  sorry  -- apply chain_complex_uct to the singular chain complex

/-- The universal coefficient theorem for singular cohomology. -/
theorem singular_cohomology_uct (X : TopCat) (G : AddCommGrp) (n : ℕ) :
    ShortExact
      (Ext 1 (singularHomology X (n-1)) G)
      (singularCohomology X G n)
      (Hom (singularHomology X n) G) :=
  sorry  -- dual argument with Hom in place of tensor
```

The proof gap is substantive. Mathlib needs a `ShortExact.Splits` notion, a packaging of the algebraic universal coefficient theorem for an arbitrary free chain complex, the identification of the singular chain complex as free over $\mathbb{Z}$ on the singular simplices of $X$, and the dimension-zero base case checks. Each piece is formalisable from existing infrastructure but has not been packaged. The non-naturality of the splitting is itself a content statement that would require an explicit counter-example in the Mathlib library.

## Advanced results [Master]

**Theorem (Universal coefficient theorem for cohomology; Hatcher Theorem 3.2).** *Let $X$ be a topological space and $G$ an abelian group. There is a natural short exact sequence*
$$
0 \to \mathrm{Ext}(H_{n-1}(X), G) \to H^n(X; G) \to \mathrm{Hom}(H_n(X), G) \to 0
$$
*and the sequence splits non-naturally.*

This is the cohomology dual of Theorem 3A.3. The map $H^n(X; G) \to \mathrm{Hom}(H_n(X), G)$ is evaluation: a cohomology class $[\varphi]$ represented by a cocycle $\varphi: C_n(X) \to G$ maps a homology class $[c]$ represented by a cycle $c$ to $\varphi(c)$. The kernel of this evaluation is the image of $\mathrm{Ext}(H_{n-1}(X), G)$, measuring cohomology classes detected only through their relation to torsion in lower-dimensional homology.

**Theorem (Algebraic universal coefficient theorem).** *Let $C_\bullet$ be a chain complex of free abelian groups and let $G$ be an abelian group. There are natural short exact sequences*
$$
0 \to H_n(C_\bullet) \otimes G \to H_n(C_\bullet \otimes G) \to \mathrm{Tor}(H_{n-1}(C_\bullet), G) \to 0,
$$
$$
0 \to \mathrm{Ext}(H_{n-1}(C_\bullet), G) \to H^n(\mathrm{Hom}(C_\bullet, G)) \to \mathrm{Hom}(H_n(C_\bullet), G) \to 0,
$$
*both split.*

The topological UCT is the algebraic UCT applied to $C_\bullet = C_\bullet(X)$. The algebraic version applies to cellular chains, simplicial chains, Morse-theoretic chains, and any other free chain complex computing the same homology. This is the source of the universal coefficient theorem for de Rham cohomology, Čech cohomology, and group cohomology.

**Theorem (computation of Ext and Tor for finitely generated abelian groups).** *Let $A$ be a finitely generated abelian group. Write $A = \mathbb{Z}^r \oplus T$ where $T$ is the torsion subgroup. Then for any abelian group $G$,*
$$
\mathrm{Tor}(A, G) = \mathrm{Tor}(T, G), \qquad \mathrm{Ext}(A, G) = \mathrm{Ext}(T, G).
$$
*Free summands contribute zero to derived functors. For $T = \bigoplus_i \mathbb{Z}/n_i$,*
$$
\mathrm{Tor}(T, G) = \bigoplus_i G[n_i], \qquad \mathrm{Ext}(T, G) = \bigoplus_i G/n_i G,
$$
*where $G[n] = \{g \in G : ng = 0\}$ is the $n$-torsion of $G$.*

Combined with the universal coefficient theorem, this gives a finite recipe: from the free rank $r_n$ of $H_n(X; \mathbb{Z})$ and the elementary divisors of its torsion, one reads off $H_n(X; G)$ and $H^n(X; G)$ for any $G$.

**Theorem (Bockstein and Tor).** *The short exact sequence of coefficient groups $0 \to \mathbb{Z} \xrightarrow{n} \mathbb{Z} \to \mathbb{Z}/n \to 0$ induces a long exact sequence in homology and a long exact sequence in cohomology, with connecting homomorphisms denoted $\beta_n$ in both. The image of $\beta_n: H_k(X; \mathbb{Z}/n) \to H_{k-1}(X; \mathbb{Z})$ is the $n$-torsion subgroup of $H_{k-1}(X; \mathbb{Z})$, and the composition with reduction mod $n$ identifies as the inclusion of the universal coefficient $\mathrm{Tor}$ summand. Dually for cohomology.*

The Bockstein detects the torsion that the universal coefficient theorem records. The full $\bmod{p}$ Bockstein spectral sequence iterates this construction and converges to the integer (co)homology modulo torsion of order prime to $p$.

**Theorem (field coefficients).** *Let $\mathbb{F}$ be a field. The universal coefficient theorem gives*
$$
H_n(X; \mathbb{F}) \cong H_n(X; \mathbb{Z}) \otimes_\mathbb{Z} \mathbb{F}, \qquad H^n(X; \mathbb{F}) \cong \mathrm{Hom}_\mathbb{Z}(H_n(X; \mathbb{Z}), \mathbb{F}).$$
*If $\mathrm{char}(\mathbb{F}) = 0$, the right-hand sides reduce to the rationalisation of the free part of integer homology; torsion vanishes.*

For $\mathbb{F}$ of characteristic zero, $H^n(X; \mathbb{F}) = \mathrm{Hom}_\mathbb{Z}(H_n(X; \mathbb{Z}), \mathbb{F})$ is the dual of the homology, and the cohomology determines the free rank of the homology. For $\mathbb{F} = \mathbb{F}_p$ of positive characteristic, the $\mathrm{Hom}$ piece detects $p$-torsion through the formula $\mathrm{Hom}(\mathbb{Z}/p^k, \mathbb{F}_p) = \mathbb{F}_p$.

**Theorem (functoriality and multiplicative structure).** *The universal coefficient short exact sequence is natural with respect to continuous maps $f: X \to Y$ and with respect to homomorphisms of abelian groups $G \to G'$. The cohomology UCT is natural in $X$ and $G$ as a sequence of abelian groups; the splitting is not natural in either argument.*

The non-naturality of the splitting is the substantive content of the theorem beyond the existence of the short exact sequence: a natural splitting would exhibit $H^n(X; G)$ as a functor of $H_n(X)$ and $H_{n-1}(X)$ alone, which it is not — the higher chain-level data of $X$ enters through the cup-product structure and the Steenrod operations.

**Synthesis.** The universal coefficient theorem is the foundational reason that ordinary (co)homology is determined by the integer answer plus a small dictionary of derived functors. The central insight is that the singular chain complex is a chain complex of free abelian groups, and over a principal ideal domain free chain complexes admit a clean exactness analysis: tensoring or Hom-ing introduces only one correction, and that correction is exactly the first derived functor. Putting these together, the short exact sequences of Hatcher Theorem 3A.3 and Theorem 3.2 reduce every coefficient-changing question for ordinary (co)homology to two computations: a tensor (or Hom) and a single Tor (or Ext) term. The corollaries are immediate: torsion-free coefficients eliminate the correction; field coefficients linearise everything; the Euler characteristic is field-independent; and the Bockstein homomorphism is the homological detector of the same torsion that the Tor term records.

This same algebraic mechanism appears again in [03.13.02] (Leray-Serre spectral sequence) when the $E_2$ page acquires Tor and Ext terms from a fibrewise application of the universal coefficient theorem. The bridge to the spectral-sequence framework is the recognition that the universal coefficient theorem is the simplest non-degenerate spectral sequence: a two-row $E_2$ page collapsing at $E_2$ because every abelian group has a free resolution of length one. Over more complicated rings (group rings of non-cyclic groups, polynomial rings, structured ring spectra), higher Tor and Ext groups appear and the universal coefficient theorem becomes a genuine spectral sequence — the universal coefficient spectral sequence of Adams, the Künneth spectral sequence, the change-of-rings spectral sequence in group cohomology. Putting these together, the universal coefficient theorem is the chain-complex-level shadow of a much wider phenomenon: every coefficient change in homological algebra is governed by derived functors, and the derived functors compute through resolutions whose length controls the spectral-sequence depth. This is exactly the same organising idea that appears again in [03.13.02] (spectral sequences) where filtration depth controls page convergence.

## Full proof set [Master]

**Theorem (Universal coefficient theorem for homology), proof.** Let $C_\bullet = C_\bullet(X)$ be the singular chain complex of $X$. The boundary $\partial$ has kernel $Z_n$ and image $B_{n-1}$, both free abelian groups (subgroups of free abelian groups are free). The short exact sequence
$$
0 \to Z_n \hookrightarrow C_n \xrightarrow{\partial} B_{n-1} \to 0
$$
splits, so for any abelian group $G$ the tensored sequence
$$
0 \to Z_n \otimes G \to C_n \otimes G \xrightarrow{\partial \otimes 1} B_{n-1} \otimes G \to 0
$$
remains exact. Splice across $n$. The complex $C_\bullet \otimes G$ has its homology computed from these short exact sequences via the connecting homomorphism. The cycles of $C_n \otimes G$ are $Z_n \otimes G$, and the boundaries are the image of $B_n \otimes G \xrightarrow{i_n \otimes 1} Z_n \otimes G$, where $i_n: B_n \hookrightarrow Z_n$ is the inclusion. Thus
$$
H_n(C_\bullet \otimes G) = \mathrm{coker}(i_n \otimes 1: B_n \otimes G \to Z_n \otimes G).
$$

Apply the snake lemma to the short exact sequence of chain complexes $0 \to B_\bullet \to Z_\bullet \to H_\bullet \to 0$ (where $B$ and $Z$ have zero differentials and $H_\bullet = Z_\bullet / B_\bullet$ is the homology), tensored with $G$:
$$
0 \to \ker(i_n \otimes 1: B_n \otimes G \to Z_n \otimes G) \to \mathrm{Tor}(H_n, G) \to 0,
$$
$$
0 \to H_n \otimes G \to \mathrm{coker}(i_n \otimes 1) \to 0.
$$
Combining: $\ker(i_n \otimes 1) = \mathrm{Tor}(H_n, G)$ since $0 \to B_n \xrightarrow{i_n} Z_n \to H_n \to 0$ is a free resolution of $H_n$. Re-indexing by one to align with the convention that the Tor correction sits in dimension $n-1$ of $H_n(X; G)$, the short exact sequence
$$
0 \to H_n(C_\bullet) \otimes G \to H_n(C_\bullet \otimes G) \to \mathrm{Tor}(H_{n-1}(C_\bullet), G) \to 0
$$
follows.

**Splitting.** Choose splittings $r_n: C_n \to Z_n$ of the inclusion $Z_n \hookrightarrow C_n$ for each $n$ (free over a PID). The maps $r_n$ assemble into a chain map $r: C_\bullet \to Z_\bullet$ that is the identity on $Z_\bullet$. Tensoring with $G$ and passing to homology, the composition $H_n(C_\bullet \otimes G) \to H_n(Z_\bullet \otimes G) = Z_n \otimes G$ followed by the projection $Z_n \otimes G \to H_n \otimes G$ produces a left inverse to the map $H_n \otimes G \to H_n(C_\bullet \otimes G)$. The splitting depends on $r_n$, hence on the chain complex $C_\bullet$, and not just on its homology — this is the source of non-naturality.

**Naturality of the SES.** A chain map $f_\bullet: C_\bullet \to D_\bullet$ between free chain complexes induces a commutative diagram of short exact sequences. Apply to the singular chain map induced by a continuous $g: X \to Y$. $\square$

**Theorem (Universal coefficient theorem for cohomology), proof.** Apply $\mathrm{Hom}(-, G)$ to the chain-level short exact sequence $0 \to Z_n \to C_n \to B_{n-1} \to 0$:
$$
0 \to \mathrm{Hom}(B_{n-1}, G) \to \mathrm{Hom}(C_n, G) \to \mathrm{Hom}(Z_n, G) \to 0.
$$
This is exact because $B_{n-1}$ is free and the inclusion $Z_n \hookrightarrow C_n$ has a section. Splice across $n$ and apply the cohomology snake-lemma argument analogous to the homology case. The cocycles in degree $n$ of $\mathrm{Hom}(C_\bullet, G)$ are $\mathrm{Hom}(C_n / B_n, G) = \mathrm{Hom}(Z_n, G)$ restricted to those killing the boundary; the coboundaries in degree $n$ are the image from $\mathrm{Hom}(C_{n-1}, G)$. Working through the snake lemma using the resolution $0 \to B_{n-1} \to Z_{n-1} \to H_{n-1} \to 0$,
$$
H^n(\mathrm{Hom}(C_\bullet, G)) = \mathrm{Hom}(H_n, G) \oplus \mathrm{coker}(\mathrm{Hom}(Z_{n-1}, G) \to \mathrm{Hom}(B_{n-1}, G)).
$$
The cokernel is $\mathrm{Ext}(H_{n-1}, G)$. The short exact sequence
$$
0 \to \mathrm{Ext}(H_{n-1}, G) \to H^n(\mathrm{Hom}(C_\bullet, G)) \to \mathrm{Hom}(H_n, G) \to 0
$$
follows. The splitting is dual to the homology splitting, and again non-natural. The naturality of the sequence in $X$ and $G$ is by functoriality of $C_\bullet$, $\mathrm{Hom}$, and $\mathrm{Ext}$. $\square$

**Theorem (algebraic UCT), proof.** The argument of the homology UCT used only that $C_\bullet$ is a free chain complex; it did not use any topology. The proof transfers verbatim to an arbitrary free chain complex $C_\bullet$ of abelian groups. $\square$

**Theorem (computation of Ext and Tor for finitely generated abelian groups), proof.** Both $\mathrm{Tor}$ and $\mathrm{Ext}$ are additive in the first argument: $\mathrm{Tor}(A \oplus B, G) = \mathrm{Tor}(A, G) \oplus \mathrm{Tor}(B, G)$, and similarly for $\mathrm{Ext}$. So computation reduces to the cases $A = \mathbb{Z}$ (free) and $A = \mathbb{Z}/n$ (cyclic torsion). For $A = \mathbb{Z}$, the resolution $0 \to 0 \to \mathbb{Z} \to \mathbb{Z} \to 0$ has length zero, so $\mathrm{Tor}(\mathbb{Z}, G) = \mathrm{Ext}(\mathbb{Z}, G) = 0$. For $A = \mathbb{Z}/n$, the resolution $0 \to \mathbb{Z} \xrightarrow{n} \mathbb{Z} \to \mathbb{Z}/n \to 0$ tensored with $G$ gives $\mathrm{Tor}(\mathbb{Z}/n, G) = \ker(n: G \to G) = G[n]$. Hom-applied gives $\mathrm{Ext}(\mathbb{Z}/n, G) = \mathrm{coker}(n: G \to G) = G/nG$. The asserted formulas follow by additivity. $\square$

**Theorem (Bockstein and Tor), proof.** The coefficient short exact sequence $0 \to \mathbb{Z} \xrightarrow{n} \mathbb{Z} \to \mathbb{Z}/n \to 0$ induces, by the long-exact-sequence-of-coefficients lemma, the exact sequence
$$
\cdots \to H_k(X; \mathbb{Z}) \xrightarrow{n} H_k(X; \mathbb{Z}) \to H_k(X; \mathbb{Z}/n) \xrightarrow{\beta} H_{k-1}(X; \mathbb{Z}) \to \cdots.
$$
The image of $\beta$ is the kernel of multiplication by $n$ on $H_{k-1}$, which is the $n$-torsion. Compose with the reduction map $H_{k-1}(X; \mathbb{Z}) \to H_{k-1}(X; \mathbb{Z}/n)$. The image of this composite consists of mod-$n$ reductions of $n$-torsion elements. By the universal coefficient theorem applied in dimension $k-1$ with $G = \mathbb{Z}/n$, the $\mathrm{Tor}$ summand is precisely the $n$-torsion of $H_{k-1}(X; \mathbb{Z})$, embedded in $H_{k-1}(X; \mathbb{Z}/n)$. The composite Bockstein-then-reduce realises this embedding. $\square$

**Theorem (field coefficients), proof.** A field $\mathbb{F}$ is a torsion-free $\mathbb{Z}$-module: tensoring is exact, so $\mathrm{Tor}(A, \mathbb{F}) = 0$ for every $A$. The universal coefficient SES collapses to $H_n(X; \mathbb{F}) \cong H_n(X) \otimes \mathbb{F}$. For cohomology, $\mathrm{Ext}(A, \mathbb{F}) = 0$ for every $A$ (since $\mathbb{F}$ is divisible), and $H^n(X; \mathbb{F}) \cong \mathrm{Hom}(H_n(X), \mathbb{F})$. For $\mathrm{char}(\mathbb{F}) = 0$, both functors annihilate torsion, so the answer reduces to the rank of the free part of $H_n(X)$. $\square$

**Theorem (functoriality and multiplicative structure), proof.** Naturality of the SES in $X$ follows from the functoriality of the singular chain complex and the algebraic UCT. Naturality in $G$ follows from the functoriality of $\otimes$, $\mathrm{Hom}$, $\mathrm{Tor}$, $\mathrm{Ext}$ in their second argument. The non-naturality of the splitting is detected by the example of Exercise 6: the inclusion $S^1 \to \mathbb{RP}^2$ induces an isomorphism on $H_1$ with $\mathbb{Z}/2$ coefficients, but the Tor summand on the right corresponds to the tensor summand on the left, so any pair of splittings exhibits this map as a non-block-diagonal matrix. $\square$

## Connections [Master]

- **Singular homology [03.12.11].** The universal coefficient theorem is the core coefficient-changing tool for singular homology. Once the integer singular homology of a space is computed, the universal coefficient theorem produces the homology and cohomology with coefficients in any abelian group through one tensor (Hom) and one Tor (Ext) computation. Without the universal coefficient theorem, every coefficient choice would require a separate chain-level argument; with it, the integer answer is the canonical input.

- **Cellular homology [03.12.13].** The algebraic UCT applies to any free chain complex, and the cellular chain complex of a CW complex is free. So the universal coefficient theorem identifies $H_n^{\mathrm{cell}}(X; G)$ with $H_n^{\mathrm{cell}}(X) \otimes G \oplus \mathrm{Tor}(H_{n-1}^{\mathrm{cell}}(X), G)$ — the same answer as the singular version, by agreement of cellular and singular homology. Concretely: the integer-coefficient cellular boundary formula $d_n(e^n_\alpha) = \sum_\beta \deg(\varphi_{\alpha\beta}) \, e^{n-1}_\beta$ tensored with $G$ computes $H_n(X; G)$.

- **Leray-Serre spectral sequence [03.13.02].** The $E_2$ page of the Leray-Serre spectral sequence for a fibration $F \to E \to B$ identifies as $E_2^{p,q} = H^p(B; H^q(F; G))$. When $H^q(F; G)$ has torsion or when the local coefficients carry non-zero Tor and Ext content, the universal coefficient theorem applied fibrewise produces the Tor and Ext corrections that show up on the page. The universal coefficient theorem is therefore a prerequisite for unfolding the spectral-sequence input.

- **Eilenberg-MacLane spaces [03.12.05].** The cohomology ring $H^*(K(\pi, n); \mathbb{Z}/p)$ is computed inductively using the universal coefficient theorem to convert between integer and mod-$p$ coefficients, together with the path-space fibration. Steenrod operations on mod-$p$ cohomology are detected by Bockstein homomorphisms, which are the connecting maps for $0 \to \mathbb{Z}/p \to \mathbb{Z}/p^2 \to \mathbb{Z}/p \to 0$ — another application of the universal coefficient long exact sequence.

- **Künneth theorem [03.04.12].** The Künneth theorem expresses $H_*(X \times Y; G)$ in terms of $H_*(X; G)$ and $H_*(Y; G)$ via a tensor product and a Tor correction. The two theorems are companions: the universal coefficient theorem changes coefficients while keeping the space fixed, and the Künneth theorem combines spaces while keeping coefficients fixed. Both rely on the same Tor-detected obstruction to exactness of the tensor product on chain complexes.

- **Cup product and cohomology rings [03.04.13].** The non-naturality of the universal coefficient splitting reflects the fact that cohomology rings carry information beyond what the homology determines: the cup product structure on $H^*(X; G)$ is not recoverable from the integer homology of $X$ alone via the universal coefficient theorem. The cohomology ring is genuinely finer data, and the universal coefficient theorem captures only the additive part.

## Historical & philosophical context [Master]

The algebraic underpinnings of the universal coefficient theorem — the functors $\mathrm{Tor}$ and $\mathrm{Ext}$ — were given their modern systematic treatment by Henri Cartan and Samuel Eilenberg in their 1956 monograph *Homological Algebra* (Princeton) [ref: TODO_REF]. The book introduced derived functors as a unified formalism for what had been a collection of ad-hoc constructions across topology and algebra. $\mathrm{Tor}$ and $\mathrm{Ext}$ were defined via projective and injective resolutions, identified as the derived functors of $\otimes$ and $\mathrm{Hom}$, and shown to compute the obstructions to exactness in both directions.

The $\mathrm{Ext}$ functor in particular had earlier roots in the 1942 paper of Eilenberg and Saunders Mac Lane, *Group extensions and homology* (*Ann. Math.* 43, 757-831) [ref: TODO_REF], which classified extensions of one abelian group by another and identified the classifying group with what would later be named $\mathrm{Ext}^1$. The connection to topology was made through the universal coefficient theorem for cohomology, which reads the extension classification group as a contribution to cohomology with arbitrary coefficients.

The topological universal coefficient theorem itself was implicit in the Eilenberg-Steenrod axiomatic framework of *Foundations of Algebraic Topology* (Princeton 1952) [ref: TODO_REF]: any homology theory satisfying the seven axioms is determined by its value on a point, and the value on a point is determined by the choice of coefficient group. The chain-complex-level theorem giving the explicit Tor and Ext short exact sequences was packaged in the modern form by the Cartan-Eilenberg framework, then re-presented in the form most graduate students learn it today by Peter Hilton and Urs Stammbach in *A Course in Homological Algebra* (Springer GTM 4, 1971) [ref: TODO_REF]. Hilton-Stammbach's treatment is the source of the standard proof that splices the short exact sequence of cycles, chains, and boundaries through the snake lemma.

The conceptual reach of the theorem extended through the 1960s and 1970s. The change-of-rings spectral sequence (Cartan-Eilenberg Ch. XVI), the universal coefficient theorem for generalised homology theories (Adams 1969 *Lectures on Generalised Cohomology*), and the universal coefficient theorem in $K$-theory (Atiyah-Hirzebruch, then refined by Karoubi 1978) all generalise the same algebraic mechanism: a coefficient change for a (co)homology theory is governed by derived functors of the change-of-coefficients functor, with depth determined by the homological dimension of the coefficient ring. Over $\mathbb{Z}$ the depth is one and the universal coefficient theorem is a short exact sequence; over more complicated rings it becomes a spectral sequence.

## Bibliography [Master]

```bibtex
@book{CartanEilenberg1956,
  author    = {Cartan, Henri and Eilenberg, Samuel},
  title     = {Homological Algebra},
  publisher = {Princeton University Press},
  year      = {1956}
}

@article{EilenbergMacLane1942,
  author  = {Eilenberg, Samuel and Mac Lane, Saunders},
  title   = {Group extensions and homology},
  journal = {Ann. of Math. (2)},
  volume  = {43},
  year    = {1942},
  pages   = {757--831}
}

@book{EilenbergSteenrod1952,
  author    = {Eilenberg, Samuel and Steenrod, Norman E.},
  title     = {Foundations of Algebraic Topology},
  publisher = {Princeton University Press},
  year      = {1952}
}

@book{HiltonStammbach1971,
  author    = {Hilton, Peter J. and Stammbach, Urs},
  title     = {A Course in Homological Algebra},
  series    = {Graduate Texts in Mathematics},
  volume    = {4},
  publisher = {Springer-Verlag},
  year      = {1971}
}

@book{HatcherAlgebraicTopology,
  author    = {Hatcher, Allen},
  title     = {Algebraic Topology},
  publisher = {Cambridge University Press},
  year      = {2002}
}

@book{Weibel1994,
  author    = {Weibel, Charles A.},
  title     = {An Introduction to Homological Algebra},
  series    = {Cambridge Studies in Advanced Mathematics},
  volume    = {38},
  publisher = {Cambridge University Press},
  year      = {1994}
}

@book{tomDieckAlgebraicTopology,
  author    = {tom Dieck, Tammo},
  title     = {Algebraic Topology},
  publisher = {European Mathematical Society},
  year      = {2008}
}

@book{Spanier1966,
  author    = {Spanier, Edwin H.},
  title     = {Algebraic Topology},
  publisher = {McGraw-Hill},
  year      = {1966}
}

@book{Adams1974,
  author    = {Adams, J. Frank},
  title     = {Stable Homotopy and Generalised Homology},
  publisher = {University of Chicago Press},
  year      = {1974}
}

@book{Karoubi1978,
  author    = {Karoubi, Max},
  title     = {K-Theory: An Introduction},
  series    = {Grundlehren der mathematischen Wissenschaften},
  volume    = {226},
  publisher = {Springer-Verlag},
  year      = {1978}
}
```
