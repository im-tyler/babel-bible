---
id: 08.13.07
title: The spherical model (Berlin-Kac)
slug: the-spherical-model-berlin-kac
section: stat-mech
chapter: exactly-solved-models
concept_catalog_id: stat-mech.spherical-model
prerequisites:
  - 08.01.01  # partition function
  - 08.03.02  # transfer matrix
  - 08.05.01  # critical exponents
successors: []
tier_anchors:
  beginner: "Baxter Ch. 5 §5.1; the spherical constraint as a relaxation of the Ising $\\pm 1$ constraint"
  intermediate: "Baxter Ch. 5 §5.3–5.5; saddle-point / Lagrange-multiplier solution of the spherical model in $d$ dimensions"
  master: "Baxter Ch. 5 §5.5–5.7; Berlin–Kac 1952 Phys. Rev. 86:821; dimensionality dependence of criticality and the Gaussian-model connection"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: jimmyqin
    path: "raw/pdfs/kardar-1-3-laws-entropy-potentials.pdf"
    locator: "Kardar statistical physics notes (saddle-point method, Gaussian integrals, critical exponents)"
  - source: quantum-well
    path: "md/Statistical Physics/Thermodynamics and Statistical Physics (Index).md"
    locator: "statistical physics index (mean-field models, critical dimension)"
  - source: TODO_REF
    path: "Baxter — Exactly Solved Models in Statistical Mechanics"
    locator: "Ch. 5 (the spherical model)"
  - source: TODO_REF
    path: "Berlin — Kac — The spherical model of a ferromagnet"
    locator: "Phys. Rev. 86:821 (1952), originator of the spherical model and its exact solution"
  - source: TODO_REF
    path: "Stanley — Spherical model as the limit of infinite spin dimensionality"
    locator: "Phys. Rev. 176:718 (1968), identification with the $n \\to \\infty$ vector model"
  - source: TODO_REF
    path: "Joyce — Critical properties of the spherical model"
    locator: "in Domb–Green eds., Phase Transitions and Critical Phenomena Vol. 2 (Academic Press, 1972), Ch. 10, spherical-model exponents in general $d$"
lean_status: none
lean_mathlib_gap: |
  Mathlib has Gaussian integration over finite-dimensional real vector spaces,
  the discrete Fourier transform on a finite abelian group, and Lebesgue
  integration with the dominated-convergence and Laplace/saddle-point
  asymptotics not yet packaged as a named theorem. The spherical-model
  solution requires a chain of objects absent as named results: the integral
  representation of the spherical delta-constraint $\delta(\sum_i s_i^2 - N)$
  as a contour integral in a Lagrange-multiplier variable, the Gaussian
  evaluation of the resulting $N$-fold integral over spin configurations, the
  thermodynamic-limit saddle-point selection of the multiplier $\mu$, and the
  Watson lattice-Green-function integral $g(\mu) = \int_{[-\pi,\pi]^d}
  \frac{d^d k}{(2\pi)^d}\,\frac{1}{\mu - \sum_j \cos k_j}$ whose convergence at
  the band edge controls the critical dimension. None of the saddle-point
  selection, the lattice Green function, or the resulting non-analytic free
  energy is available as a Mathlib theorem; this is a frontier statistical-
  mechanics formalisation target rather than a near-term one.
human_reviewer: "Tyler (pending external mathematical-physics reviewer)"
estimated_time:
  beginner: 16m
  intermediate: 38m
  master: 70m
status: shipped
produced_by: claude-opus-4-7 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

The Ising model asks each site of a lattice to hold a spin that is exactly $+1$ or exactly $-1$. That hard either-or choice is what makes the model so difficult: you cannot smoothly slide a spin from one value to the other, so the sum over configurations is a sum over corners of a high-dimensional cube. The spherical model keeps the spirit of the Ising model but trades the rigid per-site rule for one global rule. It lets every spin be any real number, and asks only that the whole collection sit on a sphere: the sum of the squares of all the spins must equal the number of sites.

Why bother? Because this one change turns an intractable sum into an integral that can be done by hand, in any number of dimensions. You give up the sharp per-site constraint, but you gain an exact answer everywhere, including in three dimensions, where the Ising model has never been solved in closed form. The spherical model becomes a clean laboratory for one of the deepest facts about phase transitions: how the dimension of space decides whether a magnet can order at all.

The single sphere keeps the spins honest on average. No spin can run off to infinity, because the others would have to shrink to compensate, and the total length is fixed. So the model still has the competition between energy, which wants neighbouring spins aligned, and entropy, which wants them spread out, that drives every phase transition.

## Visual [Beginner]

Picture two ways of choosing many spins. In the Ising picture, each spin is a switch that is either up or down, and the allowed states are the corners of a cube. In the spherical picture, the spins are coordinates of a single point that is free to move, as long as that point stays on the surface of a high-dimensional ball of a fixed radius.

![A schematic placeholder contrasting the Ising configuration space (corners of a cube, discrete) with the spherical-model configuration space (the surface of a single high-dimensional sphere, continuous), illustrating how the global spherical constraint replaces the per-site sign constraint.](/img/the-spherical-model-berlin-kac-placeholder.svg)

The picture captures the trade. The cube's corners are a finite, awkward set; the sphere's surface is smooth, and integrals over a sphere can be evaluated exactly. The radius of the sphere grows with the number of sites, so on average each spin still has size about one, matching the Ising scale, but no individual spin is pinned.

## Worked example [Beginner]

Take the smallest interesting case: two sites with a single bond between them, at zero magnetic field. The spherical rule says the two spins $s_1$ and $s_2$ must satisfy $s_1^2 + s_2^2 = 2$, so the pair sits on a circle of radius about $1.41$. The energy rewards alignment: it is lower when $s_1$ and $s_2$ have the same sign and similar size.

Step 1. Parametrise the circle by an angle, writing $s_1 = \sqrt{2}\cos\theta$ and $s_2 = \sqrt{2}\sin\theta$. Every allowed configuration is one value of $\theta$.

Step 2. The bond energy is proportional to $-s_1 s_2 = -2\cos\theta\sin\theta = -\sin(2\theta)$. This is most negative when $2\theta = 90$ degrees, that is $\theta = 45$ degrees, giving $s_1 = s_2 = 1$. The two spins want to be equal and both about one.

Step 3. At high temperature the system does not care about energy and spreads evenly over the circle, so the average of $s_1 s_2$ is near zero: no alignment. At low temperature it concentrates near $\theta = 45$ degrees (and the opposite point $\theta = 225$ degrees), where $s_1 s_2$ is largest: strong alignment.

What this tells us: even in this tiny case the spherical constraint keeps the spins finite while letting them vary smoothly, and the same competition between energy and spread that governs real magnets is already visible. The full model does exactly this calculation for a whole lattice at once, and the smooth circle becomes a smooth sphere that an integral can handle.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What does the spherical constraint require of the spins $s_1, \ldots, s_N$?

A. Each $s_i$ is exactly $+1$ or $-1$.  
B. Each $s_i$ lies between $-1$ and $+1$.  
C. The sum of the squares $s_1^2 + \cdots + s_N^2$ equals $N$.  
D. The sum $s_1 + \cdots + s_N$ equals zero.

<details class="exercise__hint"><summary>Hint</summary>

The constraint is global, not per site, and it fixes a total length rather than a sign.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** The spherical model replaces the per-site rule $s_i = \pm 1$ with a single global rule, that the sum of the squares of all the spins equals the number of sites $N$. This places the whole configuration on one sphere of radius $\sqrt{N}$, so on average each spin still has size about one.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: because the spherical constraint is smooth, the model's partition function can be computed exactly in any number of dimensions.

<details class="exercise__hint"><summary>Hint</summary>

The whole reason to introduce the spherical constraint is that it turns the configuration sum into a doable Gaussian integral.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Replacing the discrete corners of the Ising cube with a smooth sphere turns the sum over configurations into a Gaussian integral that can be evaluated in closed form for every dimension, including three, where the Ising model has no known closed solution.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

For a spherical model with $N = 100$ sites, what is the radius of the sphere on which the configuration must lie?

<details class="exercise__hint"><summary>Hint</summary>

The sum of squares equals $N$, so the radius is the square root of $N$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$10$.** The constraint $s_1^2 + \cdots + s_N^2 = N$ says the configuration lies on a sphere of radius $\sqrt{N} = \sqrt{100} = 10$. Each spin then has typical size about one, matching the Ising scale.

</details>

</aside>

## Formal definition [Intermediate+]

Let $\Lambda$ be a $d$-dimensional hypercubic lattice of $N$ sites with periodic boundary conditions. To each site $i$ assign a real spin $s_i \in \mathbb{R}$. The energy of a configuration $\mathbf{s} = (s_1, \ldots, s_N)$ in a uniform field $H$ is
$$
\mathcal{E}(\mathbf{s}) = -J \sum_{\langle i j \rangle} s_i s_j - H \sum_i s_i,
$$
the sum running over nearest-neighbour bonds. The **spherical model** of Berlin and Kac is the classical statistical-mechanical system with this energy, subject to the single **spherical constraint**
$$
\sum_{i=1}^{N} s_i^2 = N.
$$
Writing $K = \beta J$ for the dimensionless coupling and $h = \beta H$ for the scaled field (the Baxter convention $K = J/k_B T$), the partition function is the constrained integral
$$
Z_N = \int_{\mathbb{R}^N} \Big(\prod_i ds_i\Big)\, \delta\!\Big(\sum_i s_i^2 - N\Big)\, \exp\Big(K\sum_{\langle ij\rangle} s_i s_j + h \sum_i s_i\Big).
$$

The constraint is imposed by a Dirac delta, which is then written as a Laplace contour integral over a **Lagrange multiplier** $\mu$:
$$
\delta\!\Big(\sum_i s_i^2 - N\Big) = \frac{1}{2\pi i}\int_{a-i\infty}^{a+i\infty} d\mu\; \exp\Big(\mu\Big(\sum_i s_i^2 - N\Big)\Big),
$$
with the contour to the right of all singularities of the spin integral. The multiplier $\mu$ is the single auxiliary variable that carries the entire solution; the saddle-point value $\mu = \mu_s$ in the thermodynamic limit plays the role of an inverse susceptibility, vanishing in distance from the band edge as the critical point is approached.

The model is **isotropic in spin space**: there is no preferred direction, and the spherical constraint is the mean-field-exact stand-in for the Ising hard constraint. It is distinct from the **mean spherical model** of Lewis and Wannier, in which the strict constraint $\sum_i s_i^2 = N$ is replaced by the thermodynamically equivalent requirement $\langle \sum_i s_i^2 \rangle = N$ on the average; the two agree in the thermodynamic limit away from the critical point, and the strict version is the one solved exactly below.

### Counterexamples to common slips

- The spherical constraint is global, not local. Imposing $s_i^2 = 1$ at every site recovers the Ising model and loses exact solvability. Only the single sphere $\sum_i s_i^2 = N$ keeps the integral Gaussian.
- The Lagrange multiplier $\mu$ is not a free parameter; it is fixed by the constraint through the saddle-point equation. Treating $\mu$ as adjustable, rather than determined by the spherical condition, gives the unconstrained Gaussian model, which has no ordered phase and a different free energy.
- The model has a transition only for $d > 2$. Asserting a finite-temperature transition in one or two dimensions contradicts the convergence of the lattice Green function at the band edge, examined below.

## Key theorem with proof [Intermediate+]

**Theorem (exact free energy of the spherical model; Berlin–Kac 1952).** *On the $d$-dimensional hypercubic lattice, the spherical model at zero field has free energy per site, in the thermodynamic limit,*
$$
-\beta f = \frac{1}{2}\ln\frac{\pi}{K} + \frac{1}{2}\Big(2\mu_s - 1\Big) - \frac{1}{2}\,W_d(\mu_s),
$$
*where*
$$
W_d(\mu) = \int_{[-\pi,\pi]^d} \frac{d^d k}{(2\pi)^d}\, \ln\!\Big(\mu - \frac{1}{d}\sum_{j=1}^d \cos k_j\Big),
$$
*and the saddle-point value $\mu_s \ge 1$ is the unique solution of the constraint equation*
$$
\frac{1}{2K} = g_d(\mu_s), \qquad g_d(\mu) = \int_{[-\pi,\pi]^d}\frac{d^d k}{(2\pi)^d}\,\frac{1}{2\big(\mu - \tfrac{1}{d}\sum_j \cos k_j\big)},
$$
*as long as $1/(2K) \le g_d(1)$; for smaller $1/(2K)$ the saddle sticks at $\mu_s = 1$ and the system is in the ordered phase.*

**Proof.** Begin with the constrained partition function at $h = 0$ and replace the delta-constraint by its Laplace representation, so that
$$
Z_N = \frac{1}{2\pi i}\int_{a - i\infty}^{a+i\infty} d\mu\; e^{-N\mu} \int_{\mathbb{R}^N}\Big(\prod_i ds_i\Big)\exp\Big(\mu\sum_i s_i^2 + K\sum_{\langle ij\rangle} s_i s_j\Big).
$$
The exponent in the inner integral is a quadratic form $\mathbf{s}^{\mathsf T} \mathsf{A}\, \mathbf{s}$ with $\mathsf{A} = \mu\,\mathbb{1} - \tfrac{1}{2}K\,\mathsf{B}$, where $\mathsf{B}$ is the adjacency matrix of the lattice. Translation invariance diagonalises $\mathsf{B}$ by the discrete Fourier transform: its eigenvalues are $2\sum_{j} \cos k_j$ indexed by wavevectors $\mathbf{k}$ in the Brillouin zone. The Gaussian integral converges when $\mathsf{A}$ is positive definite, that is when $\mu$ exceeds the largest eigenvalue contribution, and evaluates to
$$
\int_{\mathbb{R}^N}\Big(\prod_i ds_i\Big)\,e^{-\mathbf{s}^{\mathsf T}\mathsf{A}\mathbf{s}} = \frac{\pi^{N/2}}{(\det \mathsf{A})^{1/2}}.
$$
Taking the logarithm and using $\ln\det \mathsf{A} = \sum_{\mathbf k} \ln \lambda_{\mathbf k}(\mathsf{A})$ converts the determinant into a sum over the Brillouin zone, which becomes the integral $W_d$ in the limit $N \to \infty$ after rescaling the coupling so that the band has unit half-width. Writing $\mathsf{A}$'s eigenvalues with the normalisation $\mu - \tfrac1d\sum_j\cos k_j$ absorbs $K$ into an overall prefactor.

The $\mu$-integral is then $Z_N = \frac{1}{2\pi i}\int d\mu \, e^{N \varphi(\mu)}$ with
$$
\varphi(\mu) = -K\mu + \tfrac{1}{2}\ln\frac{\pi}{K} - \tfrac{1}{2}\,W_d(\mu) + \text{const},
$$
the linear term $-K\mu$ coming from the Laplace factor and supplying the spherical constraint, while $W_d$ carries the Gaussian determinant.

For large $N$ the integral is dominated by the saddle where $\varphi'(\mu) = 0$. Differentiating $W_d$ under the integral sign,
$$
W_d'(\mu) = \int_{[-\pi,\pi]^d} \frac{d^d k}{(2\pi)^d}\,\frac{1}{\mu - \tfrac1d\sum_j\cos k_j} = 2\,g_d(\mu),
$$
so the saddle condition $\varphi'(\mu_s) = 0$ balances the constraint term against the determinant term and, with the multiplier normalised conjugate to $\sum_i s_i^2 = N$, takes the standard form
$$
\frac{1}{2K} = g_d(\mu_s),
$$
the constraint equation that fixes $\mu_s$. Equivalently, $g_d(\mu_s)$ is the per-site mean square spin from the Gaussian propagator, so the constraint $\langle s_i^2\rangle = 1$ selects the same saddle.
Because $g_d(\mu)$ is positive, decreasing, and finite at the band edge $\mu = 1$ precisely when $d > 2$, the equation has a solution $\mu_s > 1$ for $1/(2K) < g_d(1)$ and no solution below; in the latter case the saddle is pinned at the edge $\mu_s = 1$ and the surplus weight condenses into the zero-wavevector mode, the ordered phase. Substituting $\mu_s$ into $\varphi$ produces the stated free energy. $\square$

**Bridge.** This computation builds toward the dimension-resolved theory of critical behaviour, and it is exactly the place where the lattice Green function $g_d(\mu)$ decides everything. The foundational reason the spherical model is solvable is that its single global constraint keeps the configuration integral Gaussian, so the only hard step is the one-dimensional saddle-point integral over the multiplier $\mu$; this is exactly the mechanism that appears again in [08.13.02] (six-vertex / Bethe ansatz), where a single auxiliary spectral parameter likewise reduces an $N$-body sum to a one-variable consistency condition. The central insight is that the constraint equation $1/(2K) = g_d(\mu_s)$ identifies the susceptibility with the lattice Green function evaluated at the saddle, and the convergence or divergence of that integral at the band edge is what generalises the sharp Ising transition into a dimension-dependent statement. Putting these together, the spherical model becomes the cleanest exactly-solved bridge from the mean-field picture of [08.02.01] to the dimensional-crossover physics organised by the critical exponents of [08.05.01].

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

The upper critical dimension of the spherical model, above which the exponents take their mean-field (Gaussian) values, is the same as for the Ising model. What is it?

<details class="exercise__hint"><summary>Hint</summary>

It is the dimension at which the lattice Green function $g_d(\mu)$ becomes analytic at the band edge in a way that pins the exponents to their classical values.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$4$.** The upper critical dimension is $d_c = 4$. For $d > 4$ the spherical-model exponents equal the mean-field values; for $2 < d < 4$ they vary continuously with $d$. Rubric: full credit for $4$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

For which dimensions $d$ does the spherical model have no finite-temperature phase transition? Give the largest such integer dimension.

<details class="exercise__hint"><summary>Hint</summary>

The transition is absent when $g_d(1)$ diverges, which happens for $d \le 2$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2$.** The constraint integral $g_d(1)$ diverges for $d \le 2$, so the saddle never reaches the band edge and there is no ordered phase. The largest integer dimension with no transition is $d = 2$. Rubric: full credit for $2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Show that near the band edge, $\mu \to 1^+$, the singular part of the lattice Green function behaves as $g_d(\mu) - g_d(1) \sim -c_d\,(\mu - 1)^{(d-2)/2}$ for $2 < d < 4$, and identify the exponent of $(\mu - 1)$.

<details class="exercise__hint"><summary>Hint</summary>

Near $\mathbf k = 0$ the denominator behaves as $\mu - 1 + \tfrac{1}{2d}k^2$. Replace the sum by a small-$k$ radial integral $\int k^{d-1}\,dk / (\mu - 1 + k^2)$ and count powers.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Expand the denominator near the zone centre: $\mu - \tfrac1d\sum_j\cos k_j \approx (\mu - 1) + \tfrac{1}{2d}k^2$. The singular contribution is
$$
\int_0^{\Lambda} \frac{k^{d-1}\,dk}{(\mu - 1) + \tfrac{1}{2d}k^2}.
$$
Substituting $k = \sqrt{2d(\mu-1)}\,u$ extracts a factor $(\mu - 1)^{(d-2)/2}$ from the measure relative to the denominator, times a convergent $u$-integral for $2 < d < 4$. So $g_d(\mu) - g_d(1) \sim -c_d (\mu - 1)^{(d-2)/2}$, with exponent $(d-2)/2$. Rubric: full credit for the exponent $(d-2)/2$ and the power-counting argument.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 4 (medium, short-answer).**

Use the result of Exercise 3 to derive the spherical-model exponent $\gamma$ for the susceptibility, defined by $\chi \sim (T - T_c)^{-\gamma}$, in the range $2 < d < 4$.

<details class="exercise__hint"><summary>Hint</summary>

The saddle value $\mu_s - 1$ plays the role of inverse susceptibility, and $1/(2K) - 1/(2K_c) \propto T - T_c$ near $T_c$. Combine with the singular form of $g_d$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The constraint equation gives $1/(2K) - 1/(2K_c) = g_d(\mu_s) - g_d(1) \sim -c_d(\mu_s - 1)^{(d-2)/2}$. Since $1/(2K) - 1/(2K_c) \propto (T - T_c)$ near $T_c$, this yields $\mu_s - 1 \sim (T - T_c)^{2/(d-2)}$. The susceptibility is $\chi \sim (\mu_s - 1)^{-1} \sim (T - T_c)^{-2/(d-2)}$, so
$$
\gamma = \frac{2}{d-2}, \qquad 2 < d < 4.
$$
At $d = 3$ this gives $\gamma = 2$. Rubric: full credit for $\gamma = 2/(d-2)$ with the derivation.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

Evaluate the spherical-model susceptibility exponent $\gamma = 2/(d-2)$ at $d = 3$.

<details class="exercise__hint"><summary>Hint</summary>

Substitute $d = 3$ into the formula from Exercise 4.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2$.** With $d = 3$, $\gamma = 2/(3 - 2) = 2$. The three-dimensional spherical model has $\gamma = 2$, larger than the three-dimensional Ising value $\gamma \approx 1.24$ and larger than the mean-field value $\gamma = 1$. Rubric: full credit for $2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 6 (medium, short-answer).**

Explain why the mean spherical model (with the constraint imposed only on the average $\langle\sum_i s_i^2\rangle = N$) gives the same free energy as the strict spherical model in the thermodynamic limit.

<details class="exercise__hint"><summary>Hint</summary>

The strict constraint is imposed by a saddle point in $\mu$; the mean constraint introduces $\mu$ as a true thermodynamic parameter fixed by the same equation $1/(2K) = g_d(\mu)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

In the strict model the multiplier $\mu$ is integrated and evaluated at its saddle $\mu_s$, fluctuations about which are suppressed by $\sqrt N$. In the mean model $\mu$ is a Lagrange multiplier (an external field conjugate to $\sum_i s_i^2$) chosen so that $\langle\sum_i s_i^2\rangle = N$, which is precisely $-\partial(\ln Z)/\partial\mu = N$, the same equation $1/(2K) = g_d(\mu)$. Both procedures select the identical $\mu_s$ and identical free energy; they differ only in the $O(\ln N / N)$ correction terms and in the behaviour of constraint fluctuations, which do not affect the bulk free energy. Rubric: full credit for identifying the shared saddle equation and the vanishing of the difference in the bulk limit.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Derive the spherical-model correlation-length exponent $\nu$ and the specific-heat exponent $\alpha$ for $2 < d < 4$, and verify the hyperscaling relation $2 - \alpha = \nu d$.

<details class="exercise__hint"><summary>Hint</summary>

The singular free energy comes from the $\mu$-dependence near the saddle. Use $\mu_s - 1 \sim t^{2/(d-2)}$ with $t = T - T_c$ and integrate the singular part of $W_d$ twice in temperature.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The correlation length is set by the gap: $\xi^{-2} \sim \mu_s - 1 \sim t^{2/(d-2)}$, so $\xi \sim t^{-1/(d-2)}$ and $\nu = 1/(d-2)$. The singular free energy density scales as $f_{\rm sing} \sim (\mu_s - 1)^{d/2} \sim t^{d/(d-2)}$, read off from the band-edge integral $\int k^{d-1}\,dk\,\ln(\mu - 1 + k^2)$ after the substitution $k = \sqrt{\mu-1}\,u$. The specific heat is two temperature derivatives, $C \sim t^{d/(d-2) - 2} = t^{(4-d)/(d-2)}$, defining $\alpha$ through $C \sim t^{-\alpha}$:
$$
\alpha = \frac{d-4}{d-2}, \qquad 2 < d < 4.
$$
For $2 < d < 4$ this is negative, so the specific heat vanishes at $T_c$ as a cusp rather than diverging — the hallmark of the spherical model. Hyperscaling holds directly: $2 - \alpha = 2 - (d-4)/(d-2) = (2(d-2) - (d-4))/(d-2) = d/(d-2) = \nu d$. At $d = 3$: $\alpha = -1$, $\nu = 1$. Rubric: full credit for $\alpha = (d-4)/(d-2)$, $\nu = 1/(d-2)$, and the hyperscaling check.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 8 (hard, short-answer).**

Show that the spherical model coincides with the $n \to \infty$ limit of the $n$-vector model (Stanley 1968), and explain why this identifies the spherical-model exponents with those of the $O(n\to\infty)$ universality class.

<details class="exercise__hint"><summary>Hint</summary>

In the $n$-vector model each site carries an $n$-component unit vector $\mathbf S_i$ with $|\mathbf S_i|^2 = n$. Summing over the $n$ components and using the central-limit concentration of $\sum_a (S_i^a)^2$ recovers a single scalar with a global constraint.

</details>

<details class="exercise__answer"><summary>Answer</summary>

In the $n$-vector model the partition function integrates over $\mathbf S_i \in \mathbb{R}^n$ with $|\mathbf S_i|^2 = n$ at each site. As $n \to \infty$, each Cartesian component $S_i^a$ becomes a weakly-constrained scalar whose per-site constraint relaxes, by the law of large numbers over the $n$ components, into a single collective constraint on $\sum_{i,a}(S_i^a)^2$. The saddle-point analysis of the $O(n)$ model in the large-$n$ limit produces exactly the constraint equation $1/(2K) = g_d(\mu_s)$ of the spherical model, with $\mu_s$ the large-$n$ self-energy. Stanley (1968) made this identification precise. It places the spherical-model exponents $\gamma = 2/(d-2)$, $\nu = 1/(d-2)$, $\eta = 0$ in the $O(n\to\infty)$ universality class, the exactly-tractable endpoint of the family $n = 1$ (Ising), $n = 2$ (XY), $n = 3$ (Heisenberg). Rubric: full credit for the component-counting argument and the universality-class identification.

</details>

</aside>

## Advanced results [Master]

**Theorem (critical dimension via the lattice Green function; Berlin–Kac 1952).** *Let $g_d(\mu) = \int_{[-\pi,\pi]^d} \frac{d^d k}{(2\pi)^d}\,[2(\mu - \tfrac1d\sum_j\cos k_j)]^{-1}$ be the constraint integral. Then $g_d(1)$ is finite if and only if $d > 2$. Consequently the spherical model has a finite-temperature ordered phase precisely for $d > 2$, with critical coupling $K_c$ determined by $1/(2K_c) = g_d(1)$; for $d \le 2$ no $K$ drives the saddle to the band edge and the model is disordered at every positive temperature.*

The integrand near the band edge $\mathbf k = 0$ behaves as $1/k^2$, so the small-$k$ integral $\int k^{d-1}\,dk/k^2 = \int k^{d-3}\,dk$ converges at the origin exactly when $d - 3 > -1$, that is $d > 2$. The divergence at $d = 2$ is logarithmic, the same marginal behaviour that forbids long-range order in two dimensions for continuous symmetries, here arising from the spherical constraint rather than from Goldstone modes. The lower critical dimension is therefore $d_\ell = 2$, in agreement with the Mermin–Wagner threshold of the $O(n)$ models the spherical model represents.

**Theorem (continuously varying exponents in $2 < d < 4$).** *For $2 < d < 4$ the spherical-model critical exponents are*
$$
\gamma = \frac{2}{d-2}, \quad \nu = \frac{1}{d-2}, \quad \eta = 0, \quad \beta = \frac{1}{2}, \quad \alpha = \frac{d-4}{d-2}, \quad \delta = \frac{d+2}{d-2},
$$
*all rational functions of $d$, satisfying the scaling relations $\gamma = \nu(2 - \eta)$, $\alpha + 2\beta + \gamma = 2$, and the hyperscaling relation $2 - \alpha = \nu d$.*

The exponents are pinned by the single non-analytic feature of the saddle, the band-edge singularity $g_d(\mu) - g_d(1) \sim -(\mu-1)^{(d-2)/2}$. Because $\eta = 0$ exactly, the spherical model has no anomalous dimension: its two-point function decays with the Gaussian power $|x|^{-(d-2)}$ at criticality, the same as free-field theory, while the thermodynamic exponents still depend on $d$ through the constraint. This combination — Gaussian correlations with non-Gaussian thermodynamics — is the signature of the $O(n\to\infty)$ class and the reason the spherical model is the natural exactly-solved companion to the $\varepsilon$-expansion around $d = 4$ organised by the Wilson–Fisher fixed point of [08.04.02].

**Theorem (spherical-model free energy is non-analytic at $K_c$).** *The free energy per site $f(K)$ of the spherical model in $d > 2$ is analytic in $K$ except at $K = K_c$, where its $(\lceil d/2\rceil + 1)$-th derivative first diverges or develops a cusp, with the precise order set by the exponent $\alpha = (d-4)/(d-2)$. For $d > 4$ the singularity is the mean-field cusp ($\alpha = 0$, specific-heat jump); for $2 < d < 4$ it sharpens to a power-law divergence as $d \downarrow 2$.*

The non-analyticity is genuine and arises in the thermodynamic limit alone: at finite $N$ the saddle-point integral is a smooth function of $K$, and only as $N \to \infty$ does the saddle reach and stick at the band edge, producing the sharp transition. The spherical model is thus a fully explicit instance of the general principle that phase transitions are non-analyticities created by the infinite-volume limit, computable here without approximation in every dimension.

**Synthesis.** The spherical model is the cleanest exactly-solved demonstration that the dimension of space is the foundational reason a phase transition exists or fails to exist. The central insight is that the entire solution is controlled by one scalar saddle equation $1/(2K) = g_d(\mu_s)$, and the convergence of the lattice Green function $g_d$ at the band edge is exactly what generalises the sharp Ising transition into the statement that there is order for $d > 2$ and none below. Putting these together, the lower critical dimension $d_\ell = 2$, the upper critical dimension $d_c = 4$, and the rational exponents $\gamma = 2/(d-2)$, $\nu = 1/(d-2)$, $\eta = 0$ emerge from the single band-edge singularity, and this is exactly the dimensional-crossover structure that the Wilson–Fisher $\varepsilon$-expansion of [08.04.02] reproduces perturbatively near $d = 4$. The spherical model identifies the $n\to\infty$ vector model with a Gaussian theory carrying a global constraint, so the model is dual to a free field in its correlations while remaining non-mean-field in its thermodynamics; the bridge is the constraint multiplier $\mu$, whose deviation from the band edge is the inverse susceptibility and whose saddle value organises every critical exponent. It builds toward the modern large-$n$ field theory that underlies the $1/n$ expansion, where the same auxiliary-field constraint reappears as a Hubbard–Stratonovich variable.

## Full proof set [Master]

**Proposition (the constraint integral $g_d(1)$ converges iff $d > 2$).** *With $g_d(\mu) = \int_{[-\pi,\pi]^d}\frac{d^dk}{(2\pi)^d}\,[2(\mu - \tfrac1d\sum_j\cos k_j)]^{-1}$, the value $g_d(1) = \lim_{\mu\to 1^+} g_d(\mu)$ is finite for $d > 2$ and divergent for $d \le 2$.*

**Proof.** The only possible divergence of $g_d(1)$ is at the point where the denominator vanishes, namely $\mathbf k = 0$, since $\sum_j \cos k_j = d$ only there in the closed Brillouin zone. Split the integral into a small ball $B_\epsilon$ around the origin and its complement. On the complement the integrand is bounded and the region has finite measure, so that contribution is finite. On $B_\epsilon$ expand $\tfrac1d\sum_j\cos k_j = 1 - \tfrac{1}{2d}|\mathbf k|^2 + O(|\mathbf k|^4)$, giving the integrand $\sim [2 \cdot \tfrac{1}{2d}|\mathbf k|^2]^{-1} = d/|\mathbf k|^2$ up to bounded corrections. In $d$-dimensional spherical coordinates $d^dk = \Omega_{d-1}\,k^{d-1}\,dk$ with $k = |\mathbf k|$, so the singular contribution is proportional to
$$
\int_0^\epsilon \frac{k^{d-1}}{k^2}\,dk = \int_0^\epsilon k^{d-3}\,dk,
$$
which converges at $k = 0$ if and only if $d - 3 > -1$, that is $d > 2$. At $d = 2$ the integral is $\int_0^\epsilon k^{-1}\,dk$, logarithmically divergent; for $d = 1$ it is $\int_0^\epsilon k^{-2}\,dk$, power divergent. Hence $g_d(1)$ is finite precisely when $d > 2$. $\square$

**Proposition (existence and uniqueness of the saddle $\mu_s$).** *For $K$ with $1/(2K) \le g_d(1)$ and $d > 2$, the constraint equation $1/(2K) = g_d(\mu)$ has a unique solution $\mu_s \ge 1$, and $\mu_s = 1$ exactly at and below $K_c$ defined by $1/(2K_c) = g_d(1)$.*

**Proof.** On $\mu \in [1, \infty)$ the function $g_d$ is positive, continuous, and strictly decreasing, since $\partial_\mu g_d(\mu) = -\int \frac{d^dk}{(2\pi)^d}\,[2(\mu - \tfrac1d\sum_j\cos k_j)^2]^{-1} < 0$. As $\mu \to \infty$, $g_d(\mu) \to 0$; at $\mu = 1$ it attains its maximum $g_d(1)$, finite by the previous proposition. A strictly decreasing continuous function from $g_d(1)$ down to $0$ takes each value in $(0, g_d(1)]$ exactly once, so for every $1/(2K) \in (0, g_d(1)]$ there is a unique $\mu_s \ge 1$. When $1/(2K) > g_d(1)$ — equivalently $K < K_c$ — no $\mu > 1$ satisfies the equation and the saddle is pinned at the boundary $\mu_s = 1$; the unsatisfied portion of the constraint is absorbed by a macroscopic occupation of the zero mode, the spontaneous magnetisation of the ordered phase. $\square$

**Proposition (the exponent $\gamma = 2/(d-2)$).** *For $2 < d < 4$, the zero-field susceptibility diverges as $\chi \sim (T - T_c)^{-\gamma}$ with $\gamma = 2/(d-2)$.*

**Proof.** Near the band edge write $\mu = 1 + \phi$ with $\phi \to 0^+$. From the band-edge expansion the singular part of $g_d$ is $g_d(1) - g_d(\mu) \sim c_d\,\phi^{(d-2)/2}$ for $2 < d < 4$, obtained by the substitution $k = \sqrt{\phi}\,u$ in $\int_0^\epsilon k^{d-1}\,dk/(\phi + \tfrac{1}{2d}k^2)$, which factors out $\phi^{(d-2)/2}$ times a convergent integral. The constraint equation gives
$$
g_d(1) - \frac{1}{2K} = g_d(1) - g_d(\mu_s) \sim c_d\,\phi^{(d-2)/2}.
$$
Linearising $1/(2K)$ in temperature about $K_c$, $g_d(1) - 1/(2K) = 1/(2K_c) - 1/(2K) \propto (T - T_c)$, so $\phi = \mu_s - 1 \sim (T - T_c)^{2/(d-2)}$. The susceptibility is the inverse gap, $\chi = \langle (\sum_i s_i)^2\rangle/N \sim 1/(\mu_s - 1) = \phi^{-1}$, by the $\mathbf k = 0$ value of the Gaussian propagator $[2(\mu_s - \tfrac1d\sum_j\cos k_j)]^{-1}$ at $\mathbf k = 0$. Therefore $\chi \sim (T - T_c)^{-2/(d-2)}$ and $\gamma = 2/(d-2)$. $\square$

**Proposition ($\eta = 0$ exactly).** *The spherical-model critical correlation function decays as $\langle s_0 s_x\rangle_c \sim |x|^{-(d-2)}$, so the anomalous dimension vanishes.*

**Proof.** At criticality $\mu_s = 1$, the two-point function is the lattice propagator
$$
\langle s_0 s_x\rangle_c = \int_{[-\pi,\pi]^d}\frac{d^dk}{(2\pi)^d}\,\frac{e^{i\mathbf k\cdot \mathbf x}}{2(1 - \tfrac1d\sum_j\cos k_j)}.
$$
For large $|\mathbf x|$ the integral is dominated by small $\mathbf k$, where the denominator is $\sim \tfrac{1}{d}|\mathbf k|^2$, reducing the propagator to the continuum massless form $\int d^dk\,e^{i\mathbf k\cdot\mathbf x}/|\mathbf k|^2 \propto |\mathbf x|^{-(d-2)}$. The standard definition $\langle s_0 s_x\rangle_c \sim |x|^{-(d-2+\eta)}$ then forces $\eta = 0$. The decay exponent is the free-field one, so the spherical model has Gaussian correlations even though its thermodynamic exponents depend on $d$. $\square$

## Connections [Master]

- **Partition function [08.01.01].** The spherical model is solved entirely at the level of its partition function: the constrained configuration integral is reduced, by the Laplace representation of the delta-constraint and a Gaussian evaluation, to a one-dimensional saddle-point integral over the Lagrange multiplier. The whole content of the model is in how this partition function fails to be analytic as $N \to \infty$, making it the most explicit worked instance of the partition-function machinery developed there.

- **Transfer matrix [08.03.02].** Where the transfer-matrix method diagonalises a row-to-row operator to obtain the free energy of lattice models, the spherical model reaches the same free energy by a complementary route — Fourier-diagonalising the quadratic form and integrating out a single constraint variable. The Brillouin-zone eigenvalues $2\sum_j\cos k_j$ that appear in the spherical-model determinant are exactly the transfer-matrix band, so the two methods compute the same spectral data by different means.

- **Critical exponents [08.05.01].** The spherical model supplies a complete, exactly-computed set of critical exponents $\gamma, \nu, \eta, \alpha, \beta, \delta$ as rational functions of the dimension $d$, satisfying every scaling and hyperscaling relation. It is the canonical exactly-solved test of the exponent framework and the cleanest realisation of $d$-dependent (non-mean-field) exponents that still respect $\eta = 0$, sharpening the universality discussion that the critical-exponents unit frames.

- **Wilson–Fisher fixed point [08.04.02].** The spherical model is the $n\to\infty$ endpoint of the $O(n)$ family whose finite-$n$ critical behaviour the Wilson–Fisher $\varepsilon$-expansion computes perturbatively near $d = 4$. The spherical exponents are the exact large-$n$ limit against which the $\varepsilon$-expansion and the $1/n$ expansion are checked, and the constraint multiplier $\mu$ is the lattice precursor of the Hubbard–Stratonovich auxiliary field of the large-$n$ field theory.

## Historical & philosophical context [Master]

Theodore Berlin and Mark Kac introduced the spherical model in a 1952 paper in the *Physical Review*, "The spherical model of a ferromagnet" [ref: TODO_REF Berlin-Kac 1952]. Their stated motivation was to construct a model close enough to the Ising model to share its qualitative physics, yet tractable enough to solve exactly in three dimensions, where Onsager's transfer-matrix solution of the two-dimensional Ising model had no known extension. Kac had recognised that the obstruction to solving the Ising model was the per-site constraint $s_i = \pm 1$, and that replacing it with a single global constraint $\sum_i s_i^2 = N$ — geometrically, confining the configuration to one high-dimensional sphere — would render the partition function Gaussian. Berlin carried out the saddle-point analysis. The resulting solution gave, for the first time, an exact account of how critical behaviour depends on spatial dimension: a transition for $d > 2$, none for $d \le 2$, and continuously varying exponents between the lower critical dimension $d = 2$ and the upper critical dimension $d = 4$.

The model acquired a deeper meaning in 1968, when H. Eugene Stanley proved that it is the $n\to\infty$ limit of the $n$-vector model [ref: TODO_REF Stanley 1968], placing the spherical exponents in a genuine universality class rather than treating them as an artefact of an artificial constraint. This identification made the spherical model the exactly-solvable anchor of the large-$n$ expansion that Wilson, Fisher, and others were developing in parallel as part of the renormalisation-group programme. G. S. Joyce's 1972 review in the Domb–Green series collected the dimension-dependent exponents and the lattice-Green-function technology in the form used since [ref: TODO_REF Joyce 1972]. Baxter's Chapter 5 presents the model as the simplest exactly-solved system exhibiting genuine dimension-dependence of criticality, a foil to the two-dimensional vertex and Ising models that occupy the rest of his book and that cannot, by their fixed dimensionality, display the crossover the spherical model makes explicit.

## Bibliography [Master]

```bibtex
@article{BerlinKac1952,
  author  = {Berlin, T. H. and Kac, M.},
  title   = {The Spherical Model of a Ferromagnet},
  journal = {Physical Review},
  volume  = {86},
  number  = {6},
  year    = {1952},
  pages   = {821--835}
}

@article{Stanley1968,
  author  = {Stanley, H. E.},
  title   = {Spherical Model as the Limit of Infinite Spin Dimensionality},
  journal = {Physical Review},
  volume  = {176},
  number  = {2},
  year    = {1968},
  pages   = {718--722}
}

@incollection{Joyce1972,
  author    = {Joyce, G. S.},
  title     = {Critical Properties of the Spherical Model},
  booktitle = {Phase Transitions and Critical Phenomena, Vol. 2},
  editor    = {Domb, C. and Green, M. S.},
  publisher = {Academic Press},
  address   = {London},
  year      = {1972},
  pages     = {375--442}
}

@book{Baxter1982,
  author    = {Baxter, R. J.},
  title     = {Exactly Solved Models in Statistical Mechanics},
  publisher = {Academic Press},
  address   = {London},
  year      = {1982}
}

@article{LewisWannier1952,
  author  = {Lewis, H. W. and Wannier, G. H.},
  title   = {Spherical Model of a Ferromagnet},
  journal = {Physical Review},
  volume  = {88},
  number  = {3},
  year    = {1952},
  pages   = {682--683}
}

@article{Watson1939,
  author  = {Watson, G. N.},
  title   = {Three Triple Integrals},
  journal = {Quarterly Journal of Mathematics},
  volume  = {os-10},
  number  = {1},
  year    = {1939},
  pages   = {266--276}
}
```
