---
id: 39.01.02
title: "Commutative C*-Algebras and Gelfand Duality"
slug: commutative-c-star-gelfand-duality
section: operator-algebras
chapter: c-star-algebras-basics
concept_catalog_id: operator-algebras.commutative-c-star-gelfand-duality
prerequisites:
  - 39.01.01  # C*-algebras: axioms, spectrum, continuous functional calculus
  - 02.01.05  # metric space
  - 02.11.04  # Banach space fundamentals
successors: []
tier_anchors:
  beginner: "Strogatz-style 'a space of functions is the same data as the shape it sits on'; the dictionary between points and evaluation rules"
  intermediate: "Murphy *C*-Algebras and Operator Theory* Ch. 2 (Gelfand theory); Davidson *C*-Algebras by Example* Ch. I"
  master: "Davidson *C*-Algebras by Example* Ch. I; Murphy Ch. 2; Dixmier *C*-Algebras* §1; Connes *Noncommutative Geometry* Ch. I"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Davidson, K. R. — C*-Algebras by Example"
    locator: "Ch. I, Gelfand theory of commutative C*-algebras, character space, Gelfand transform"
  - source: TODO_REF
    path: "Murphy, G. J. — C*-Algebras and Operator Theory"
    locator: "Ch. 2, the Gelfand representation and the commutative Gelfand-Naimark theorem"
  - source: TODO_REF
    path: "Gelfand, I. and Naimark, M. — On the imbedding of normed rings into the ring of operators in Hilbert space"
    locator: "Mat. Sbornik 12 (1943), 197–213 — the commutative representation theorem"
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the character space of a commutative Banach algebra as
  `WeakDual.characterSpace`, the Gelfand transform `gelfandTransform`, and a
  development of `gelfandStarTransform` proving that for a commutative unital
  C*-algebra the transform is a *-isometric isomorphism onto
  `C(characterSpace ℂ A, ℂ)`. What is not packaged as the single named
  textbook duality with the footprint of this unit is the full contravariant
  equivalence of categories: the spectrum functor on morphisms, its quasi-inverse
  $X \mapsto C(X)$, the unit and counit (the evaluation homeomorphism
  $X \cong \widehat{C(X)}$ and the Gelfand isomorphism $A \cong C(\widehat A)$),
  the proper-map / non-unital $C_0(X)$ version via one-point compactification, and
  the resulting statement that taking spectra is an equivalence between commutative
  C*-algebras and locally compact Hausdorff spaces. These are formalisation targets
  that would assemble the scattered Mathlib pieces into a stated Gelfand-duality
  equivalence and its noncommutative-topology corollaries.
human_reviewer: "Tyler (pending external operator-algebras reviewer)"
estimated_time:
  beginner: 18m
  intermediate: 50m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [davidson-cstar]
source_curriculum: operator-algebras
---

## Intuition [Beginner]

A space of continuous functions remembers the shape it lives on. If you know every continuous function on a circle, and you know how to add and multiply them, then you can reconstruct the circle itself — the points of the circle are hidden inside the algebra of functions. Gelfand duality is the precise statement that nothing is lost: the shape and its function algebra carry exactly the same information.

How do you recover a point from the functions alone? A point is the same thing as a rule that takes a function and reports its value there. Picking the point $p$ on the circle is the same as picking the rule "evaluate at $p$". These evaluation rules respect addition and multiplication, and it turns out that every such rule comes from a point. So the points of the shape are exactly the multiplication-respecting evaluation rules on the algebra.

This is the conceptual seed of noncommutative geometry: if a commutative function algebra is secretly a space, then a noncommutative algebra can be treated as the functions on a "space" that has no ordinary points at all.

## Visual [Beginner]

A commutative C*-algebra and a compact shape are two views of one object. Going right, you read functions off the shape; going left, you recover the points as evaluation rules.

![On the left a compact shape X with a marked point p; an arrow labelled 'evaluate at p' pointing to the middle, where the algebra C(X) of continuous functions sits; on the right the same algebra A with its character space X-hat, where each point is a multiplication-respecting evaluation rule, and a dashed arrow back to the shape showing the two are the same.](/img/commutative-c-star-gelfand-duality-placeholder.svg)

The dictionary reads: a point becomes an evaluation rule; a continuous map between shapes becomes a backward substitution of functions; and the whole shape is rebuilt from the algebra as the set of all its evaluation rules with their natural notion of closeness.

## Worked example [Beginner]

Take the shape to be just two points, named $1$ and $2$. A continuous complex-valued function on two points is simply a choice of two complex numbers: its value at point $1$ and its value at point $2$. So the algebra of functions is all pairs $(z_1, z_2)$, added and multiplied slot by slot.

Now recover the points from the algebra of pairs. An evaluation rule must respect multiplication. The rule "report the first slot", sending $(z_1, z_2)$ to $z_1$, respects both addition and multiplication: the first slot of a product is the product of first slots. The rule "report the second slot" does the same. These are the only two such rules.

So the algebra of pairs has exactly two evaluation rules, and they correspond to the two points $1$ and $2$ we started with. Count them: two points on the shape, two evaluation rules in the algebra.

What this tells us: the number of points in the shape equals the number of multiplication-respecting evaluation rules in the function algebra, and each rule pins down one point. The shape was never lost; it was encoded in the algebra all along.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

In Gelfand duality, what plays the role of a single point of the shape?

A. A continuous function that is never zero
B. A multiplication-respecting rule that turns a function into a number
C. The largest function in the algebra
D. The constant function equal to one

<details class="exercise__hint"><summary>Hint</summary>

A point is the same as the recipe "evaluate a function here".

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. A multiplication-respecting evaluation rule.**

Feedback-correct: correct; a point of the shape is exactly an evaluation rule that respects addition and multiplication, called a character. Feedback-wrong: a point is not a single function; it is a way of sampling all functions at one place.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What does Gelfand duality say about a commutative C*-algebra?

A. It is always finite
B. It is the same data as continuous functions on some shape
C. It has no points
D. It can never come from a real space

<details class="exercise__hint"><summary>Hint</summary>

The shape and its function algebra carry the same information.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. It is the algebra of continuous functions on some shape.**

Feedback-correct: correct; every commutative C*-algebra equals the continuous functions on its space of evaluation rules. Feedback-wrong: such an algebra always has points — they are its evaluation rules — and it always comes from a genuine space.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A$ be a commutative C*-algebra [39.01.01]. A **character** of $A$ is a nonzero algebra homomorphism $\tau : A \to \mathbb{C}$, equivalently a nonzero linear functional with $\tau(ab) = \tau(a)\tau(b)$. The set of all characters is the **character space** (or **spectrum**, or **maximal ideal space**) $\widehat A$. In the unital case a character automatically satisfies $\tau(1) = 1$ and is a $*$-homomorphism ($\tau(a^*) = \overline{\tau(a)}$), and characters are automatically continuous with $\|\tau\| = 1$. The kernel of a character is a maximal ideal of codimension one, and in a commutative unital C*-algebra this correspondence between characters and maximal ideals is a bijection — hence the name maximal ideal space [ref: TODO_REF Murphy Ch. 2].

Give $\widehat A$ the **weak-$*$ topology** inherited from the dual space $A^*$, the topology of pointwise convergence: $\tau_i \to \tau$ iff $\tau_i(a) \to \tau(a)$ for every $a \in A$. With this topology $\widehat A$ is a **compact Hausdorff space** when $A$ is unital (it is a weak-$*$ closed subset of the unit ball of $A^*$, compact by Banach-Alaoglu [02.11.04]), and a **locally compact Hausdorff space** when $A$ is non-unital.

The **Gelfand transform** is the map
$$
\Gamma : A \longrightarrow C_0(\widehat A), \qquad \Gamma(a)(\tau) = \hat a(\tau) = \tau(a),
$$
sending an algebra element $a$ to the function $\hat a$ on $\widehat A$ that evaluates each character at $a$. The transform is an algebra homomorphism, and $\hat a(\widehat A) \cup \{0\} = \sigma(a) \cup \{0\}$, so the range of $\hat a$ is essentially the spectrum of $a$ [ref: TODO_REF Davidson Ch. I]. In the unital case the codomain is $C(\widehat A)$ (continuous functions on a compact space); in the non-unital case it is $C_0(\widehat A)$ (continuous functions vanishing at infinity).

### Counterexamples to common slips

- A character is not just any linear functional. On $A = \mathbb{C}^2$ the functional $(z_1, z_2) \mapsto z_1 + z_2$ is linear but is **not** a character: it fails $\tau(ab) = \tau(a)\tau(b)$, since $(1,0)\cdot(0,1) = (0,0)$ maps to $0$ while $\tau(1,0)\,\tau(0,1) = 1 \cdot 1 = 1$. Only the two coordinate projections are characters.
- The character space is not discrete in general. For $A = C[0,1]$ the character space is $[0,1]$ with its usual topology, not a discrete set of points; the weak-$*$ topology recovers the original topology of the shape, which is the whole content of duality.
- Non-unital algebras lose compactness, not the theory. For $A = C_0(\mathbb{R})$ the character space is $\mathbb{R}$, which is locally compact but not compact; adjoining a unit corresponds to one-point compactification $\mathbb{R} \cup \{\infty\}$, and characters of the unitisation that kill $A$ are the point at infinity.

## Key theorem with proof [Intermediate+]

**Theorem (commutative Gelfand-Naimark).** *Let $A$ be a commutative unital C*-algebra. The Gelfand transform*
$$
\Gamma : A \longrightarrow C(\widehat A), \qquad \Gamma(a) = \hat a, \quad \hat a(\tau) = \tau(a),
$$
*is an isometric $*$-isomorphism onto $C(\widehat A)$, where $\widehat A$ is the compact Hausdorff character space. In the non-unital case $\Gamma$ is an isometric $*$-isomorphism of $A$ onto $C_0(\widehat A)$ with $\widehat A$ locally compact Hausdorff.* [ref: TODO_REF Davidson Ch. I; Murphy Ch. 2]

**Proof.** Assume $A$ unital. Each character $\tau$ is a unital $*$-homomorphism, so $\Gamma$ is a unital $*$-homomorphism into $C(\widehat A)$: $\Gamma(a^*)(\tau) = \tau(a^*) = \overline{\tau(a)} = \overline{\hat a(\tau)}$, and products and the unit are preserved pointwise.

*Range and spectrum.* For $a \in A$ and $\lambda \in \mathbb{C}$, the value $\lambda$ lies in the range of $\hat a$ iff some character has $\tau(a) = \lambda$ iff $a - \lambda 1$ lies in a maximal ideal iff $a - \lambda 1$ is non-invertible (in a commutative unital algebra a non-invertible element generates a proper ideal, contained in a maximal ideal, which is the kernel of a character). Hence $\hat a(\widehat A) = \sigma(a)$, and in particular $\|\hat a\|_\infty = \sup_{\tau}|\tau(a)| = r(a)$, the spectral radius.

*Isometry.* For self-adjoint $a = a^*$, the C*-axiom forces $\|a^2\| = \|a^*a\| = \|a\|^2$, so by induction on powers of two $\|a^{2^n}\| = \|a\|^{2^n}$, and the spectral radius formula gives $r(a) = \lim_n \|a^{2^n}\|^{1/2^n} = \|a\|$. Thus $\|\hat a\|_\infty = r(a) = \|a\|$ on self-adjoint elements. For general $b$, applying this to the self-adjoint $b^*b$ and using that $\Gamma$ is a $*$-homomorphism,
$$
\|\hat b\|_\infty^2 = \|\overline{\hat b}\,\hat b\|_\infty = \|\widehat{b^*b}\|_\infty = \|b^*b\| = \|b\|^2,
$$
so $\|\hat b\|_\infty = \|b\|$. The transform is isometric, hence injective, and its range is a closed subalgebra of $C(\widehat A)$.

*Surjectivity.* The range $\Gamma(A)$ is a closed $*$-subalgebra of $C(\widehat A)$ that contains the constants (the unit maps to $1$) and **separates points**: if $\tau_1 \ne \tau_2$ are distinct characters, some $a$ has $\tau_1(a) \ne \tau_2(a)$, i.e. $\hat a(\tau_1) \ne \hat a(\tau_2)$. By the Stone-Weierstrass theorem a point-separating, conjugation-closed, unital subalgebra of $C(\widehat A)$ is dense; being closed, $\Gamma(A) = C(\widehat A)$. So $\Gamma$ is an isometric $*$-isomorphism onto $C(\widehat A)$.

For non-unital $A$, adjoin a unit to form $\tilde A = A \oplus \mathbb{C}1$, a commutative unital C*-algebra. Its character space $\widehat{\tilde A}$ is the one-point compactification of $\widehat A$, the extra character being the one that kills $A$. Restricting the unital isomorphism $\tilde A \cong C(\widehat{\tilde A})$ to the ideal $A$ identifies it with the functions vanishing at the point at infinity, namely $C_0(\widehat A)$. $\square$

**Bridge.** This theorem builds toward the entire dictionary of noncommutative topology, and it appears again in [39.01.01] from the other side: the continuous functional calculus is exactly the single-generator case of this duality, applied to the commutative subalgebra $C^*(a,1)$ generated by one normal element, whose character space is the spectrum $\sigma(a)$. The foundational reason the transform is isometric is the C*-axiom on self-adjoint elements, identical to the mechanism in [39.01.01]; this is exactly why $\|\hat a\|_\infty = \|a\|$ rather than merely $\le$. The duality generalises the finite case of [01.01.13], where a diagonalisable operator's commutative algebra has a finite character space equal to its eigenvalue set. Putting these together, the bridge is that "commutative C*-algebra" and "compact Hausdorff space" name one object viewed two ways, and the functional calculus is duality read for the smallest interesting algebra.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 1 (easy, multiple choice).**

What topology does the character space $\widehat A$ carry?

A. The norm topology of $A^*$
B. The weak-$*$ topology of pointwise evaluation
C. The discrete topology
D. The order topology

<details class="exercise__hint"><summary>Hint</summary>

Characters converge when they agree more and more on each fixed element $a$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** The weak-$*$ topology: $\tau_i \to \tau$ iff $\tau_i(a) \to \tau(a)$ for every $a$. Compactness in the unital case is Banach-Alaoglu.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

The algebra $A = \mathbb{C}^4$ of complex $4$-tuples, with slotwise operations, is a commutative unital C*-algebra. How many characters does it have?

<details class="exercise__hint"><summary>Hint</summary>

Each character reports one coordinate; count the coordinate projections.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$4$.** The characters of $\mathbb{C}^n$ are exactly the $n$ coordinate projections, so $\widehat A$ is a $4$-point space. Rubric: full credit for $4$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Identify the character space of $A = C(X)$ for a compact Hausdorff space $X$, and write the homeomorphism $X \to \widehat A$ explicitly.

<details class="exercise__hint"><summary>Hint</summary>

Each point of $X$ gives the evaluation functional; the duality says these are all the characters.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The map $\varepsilon : X \to \widehat{C(X)}$, $\varepsilon(x) = \mathrm{ev}_x$ with $\mathrm{ev}_x(f) = f(x)$, is a homeomorphism. Each $\mathrm{ev}_x$ is a character; injectivity is Urysohn separation of points by functions; surjectivity is that every maximal ideal of $C(X)$ is $\{f : f(x) = 0\}$ for some $x$; the weak-$*$ topology pulls back to the topology of $X$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

For $a$ in a commutative unital C*-algebra, express $\sigma(a)$ in terms of the Gelfand transform $\hat a$.

<details class="exercise__hint"><summary>Hint</summary>

The range of $\hat a$ is the spectrum.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$$
\sigma(a) = \hat a(\widehat A) = \{\tau(a) : \tau \in \widehat A\}.
$$
Equivalently $\lambda \in \sigma(a)$ iff $a - \lambda 1$ lies in some maximal ideal, i.e. some character sends $a$ to $\lambda$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 5 (medium, short-answer).**

Show that every character $\tau$ of a unital Banach algebra is continuous with $\|\tau\| \le 1$.

<details class="exercise__hint"><summary>Hint</summary>

If $|\tau(a)| > \|a\|$, then $a - \tau(a)1$ would be invertible while lying in the kernel of $\tau$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Suppose $|\tau(a)| > \|a\|$ for some $a$. Set $\lambda = \tau(a)$, so $|\lambda| > \|a\|$ and the Neumann series shows $a - \lambda 1$ is invertible (since $\|a/\lambda\| < 1$). But $\tau(a - \lambda 1) = \tau(a) - \lambda = 0$, so $a - \lambda 1$ lies in $\ker\tau$, a proper ideal; a proper ideal contains no invertible element, a contradiction. Hence $|\tau(a)| \le \|a\|$ for all $a$, giving $\|\tau\| \le 1$, so $\tau$ is bounded, hence continuous. (Equality $\|\tau\| = 1$ follows from $\tau(1) = 1$.)

Rubric: full credit for the invertibility contradiction and the bound $|\tau(a)| \le \|a\|$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 6 (hard, short-answer).**

Prove that for compact Hausdorff $X$ and $Y$, every unital $*$-homomorphism $\varphi : C(Y) \to C(X)$ is induced by a continuous map $f : X \to Y$ via $\varphi(g) = g \circ f$.

<details class="exercise__hint"><summary>Hint</summary>

Dualize: $\varphi$ induces a map on character spaces, and characters of $C(Y)$ are points of $Y$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The character spaces are $\widehat{C(X)} \cong X$ and $\widehat{C(Y)} \cong Y$ via evaluation. The homomorphism $\varphi$ induces the pullback $\varphi^* : \widehat{C(X)} \to \widehat{C(Y)}$, $\varphi^*(\tau) = \tau \circ \varphi$, continuous for the weak-$*$ topologies. Transporting through the evaluation homeomorphisms gives a continuous map $f : X \to Y$ with $\mathrm{ev}_x \circ \varphi = \mathrm{ev}_{f(x)}$, i.e. $\varphi(g)(x) = \mathrm{ev}_x(\varphi(g)) = (\mathrm{ev}_x \circ \varphi)(g) = g(f(x))$. Hence $\varphi(g) = g \circ f$. Continuity of $f$ is continuity of $\varphi^*$ read through the homeomorphisms.

Rubric: full credit for constructing $f$ as the dual of $\varphi$ on character spaces and verifying $\varphi(g) = g\circ f$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Let $A$ be a commutative unital C*-algebra and $a \in A$. Show that $a$ is invertible iff $\hat a$ vanishes nowhere on $\widehat A$.

<details class="exercise__hint"><summary>Hint</summary>

$\hat a$ vanishing somewhere means $0 \in \sigma(a)$; relate to invertibility through the Gelfand isomorphism.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By the commutative Gelfand-Naimark theorem $\Gamma : A \to C(\widehat A)$ is a $*$-isomorphism, so $a$ is invertible in $A$ iff $\hat a$ is invertible in $C(\widehat A)$. A continuous function on a compact space is invertible (has a continuous reciprocal) iff it is nowhere zero. Equivalently, $0 \notin \sigma(a) = \hat a(\widehat A)$ iff $\hat a$ never vanishes. Combining, $a$ is invertible iff $\hat a(\tau) \ne 0$ for all $\tau \in \widehat A$.

Rubric: full credit for transporting invertibility across the isomorphism and the nowhere-vanishing criterion in $C(\widehat A)$.

</details>

</aside>

## Lean formalization [Intermediate+]

`lean_status: none` — Mathlib has the character space `WeakDual.characterSpace`, the `gelfandTransform`, and `gelfandStarTransform` proving the unital commutative transform is a $*$-isometric isomorphism onto `C(characterSpace ℂ A, ℂ)`. What is not consolidated as one named theorem is the contravariant *equivalence of categories* — the spectrum functor on morphisms with its quasi-inverse $X \mapsto C(X)$, the unit/counit natural isomorphisms, and the $C_0$ non-unital version via one-point compactification.

The intended statement reads schematically:

```lean
import Mathlib.Analysis.CStarAlgebra.GelfandDuality

variable {A : Type*} [CommCStarAlgebra A]

/-- Commutative Gelfand-Naimark: the Gelfand transform is a *-isometric
isomorphism onto continuous functions on the character space. -/
theorem gelfand_naimark_commutative :
    ∃ Φ : A ≃⋆ₐ[ℂ] C(characterSpace ℂ A, ℂ),
      ∀ a, ‖Φ a‖ = ‖a‖ :=
  sorry  -- gelfandStarTransform packaged as a bundled *-isometric equiv
```

## Advanced results [Master]

The duality is not a single isomorphism but a contravariant equivalence of categories, and the structural payoff is organised by reading the dictionary in both directions.

**Functoriality of the spectrum.** Let $\mathsf{CommC^*}$ be the category of commutative C*-algebras with $*$-homomorphisms, and $\mathsf{CHaus}$ the category of compact Hausdorff spaces with continuous maps (for the unital case; locally compact Hausdorff spaces with proper continuous maps for the non-unital $C_0$ case). The character-space construction $A \mapsto \widehat A$ is a contravariant functor: a $*$-homomorphism $\varphi : A \to B$ induces $\widehat\varphi : \widehat B \to \widehat A$, $\widehat\varphi(\tau) = \tau \circ \varphi$, continuous for the weak-$*$ topologies. The continuous-function construction $X \mapsto C(X)$ is the contravariant functor in the other direction, $f \mapsto (g \mapsto g \circ f)$.

**Equivalence.** These two functors are mutually quasi-inverse. The counit is the Gelfand isomorphism $\Gamma_A : A \xrightarrow{\sim} C(\widehat A)$; the unit is the evaluation homeomorphism $\varepsilon_X : X \xrightarrow{\sim} \widehat{C(X)}$, $x \mapsto \mathrm{ev}_x$. Both are natural in their arguments. Thus $\mathsf{CommC^*}^{\mathrm{op}} \simeq \mathsf{CHaus}$: commutative C*-algebra theory and the topology of compact Hausdorff spaces are the same mathematics. Algebraic operations translate exactly — quotients of $A$ correspond to closed subspaces of $\widehat A$, tensor products to products of spaces, direct limits to inverse limits, and the ideals of $A$ to open subsets of $\widehat A$.

**Spectral synthesis of self-adjoint operators.** Specialising to the commutative C*-algebra generated by a single self-adjoint or normal operator recovers the spectral theorem: the character space is the spectrum $\sigma(a) \subseteq \mathbb{C}$, and $\Gamma$ is the continuous functional calculus $C(\sigma(a)) \cong C^*(a,1)$. The duality is therefore the conceptual container for the functional calculus [39.01.01]: one normal element is one commutative algebra is one compact subset of $\mathbb{C}$.

**Noncommutative topology.** Because every topological construction on $\widehat A$ has an algebraic shadow on $A$, one *defines* the analogues for noncommutative C*-algebras by their algebraic form. A noncommutative C*-algebra is read as the algebra of "functions" on a virtual noncommutative space with no points; connectedness, dimension, $K$-theory, and homology all migrate to the algebra. This is the founding move of Connes' noncommutative geometry [ref: TODO_REF Connes Ch. I], where a spectral triple equips such an algebra with the metric data a Riemannian manifold would supply.

**Synthesis.** The commutative Gelfand-Naimark theorem is the foundational reason the spectrum deserves to be called a space: it shows the character space, equipped with the weak-$*$ topology, is the compact Hausdorff shape whose functions reconstruct $A$, and this is exactly the equivalence $\mathsf{CommC^*}^{\mathrm{op}} \simeq \mathsf{CHaus}$. The construction is dual to the functional calculus of [39.01.01] — one passes from algebra to space, the other applies functions on the space back to the algebra — and putting these together gives the central insight that points, characters, and maximal ideals are three names for the same data. The duality generalises in two directions that appear again across operator algebras: downward to the single-generator case, where it is the continuous functional calculus and the character space is $\sigma(a)$, and upward to the measurable analogue, where von Neumann algebras [39.03.01] replace $C(X)$ by $L^\infty(X,\mu)$ and weak-$*$ closure replaces norm closure. The bridge to the rest of the subject is that "noncommutative C*-algebra" means "functions on a noncommutative space", and the entire program of noncommutative geometry is the systematic exploitation of that single reframing.

## Full proof set [Master]

**Proposition (characters are automatically $*$-preserving on a commutative unital C*-algebra).** Let $\tau$ be a character and $a = a^*$ self-adjoint. Write $\tau(a) = \alpha + i\beta$. For real $t$, the element $a + it1$ is normal, and $\tau(a + it1) = \alpha + i(\beta + t)$, so $|\alpha + i(\beta+t)|^2 \le \|a + it1\|^2 = \|(a - it1)(a + it1)\| = \|a^2 + t^2 1\| \le \|a\|^2 + t^2$. Expanding, $\alpha^2 + \beta^2 + 2\beta t + t^2 \le \|a\|^2 + t^2$, so $\alpha^2 + \beta^2 + 2\beta t \le \|a\|^2$ for all $t \in \mathbb{R}$, forcing $\beta = 0$. Hence $\tau$ maps self-adjoint elements to $\mathbb{R}$, and writing a general $b = b_1 + i b_2$ in self-adjoint parts gives $\tau(b^*) = \tau(b_1) - i\tau(b_2) = \overline{\tau(b)}$.

**Proposition (maximal ideals are kernels of characters).** Let $A$ be commutative unital and $M$ a maximal ideal. Then $M$ is closed (its closure is a proper ideal, since the invertibles form an open set disjoint from $M$, so $\overline M = M$ by maximality), and $A/M$ is a unital Banach algebra in which every nonzero element is invertible, hence a division algebra. By Gelfand-Mazur $A/M \cong \mathbb{C}$, and the quotient map $A \to A/M \cong \mathbb{C}$ is a character with kernel $M$. Conversely the kernel of any character is a maximal ideal of codimension one. This bijection identifies $\widehat A$ with the maximal ideal space.

**Proposition (the Gelfand transform has range the spectrum).** For $a \in A$ commutative unital, $\hat a(\widehat A) = \sigma(a)$. If $\lambda \in \sigma(a)$ then $a - \lambda 1$ is non-invertible, so it generates a proper ideal contained in some maximal ideal $M = \ker\tau$; then $\tau(a) = \lambda$, so $\lambda \in \hat a(\widehat A)$. Conversely if $\tau(a) = \lambda$ then $a - \lambda 1 \in \ker\tau$, a proper ideal, so $a - \lambda 1$ is non-invertible and $\lambda \in \sigma(a)$.

**Proposition (the unit and counit are natural isomorphisms).** Naturality of the counit: for $\varphi : A \to B$ a $*$-homomorphism, the square relating $\Gamma_A, \Gamma_B$ and the induced map $C(\widehat\varphi) : C(\widehat A) \to C(\widehat B)$ commutes because both routes send $a \in A$ to the function $\tau \mapsto \tau(\varphi^{-1}\dots)$ — concretely, $C(\widehat\varphi)(\Gamma_A a)(\tau) = \Gamma_A(a)(\widehat\varphi(\tau)) = (\tau\circ\varphi)(a) = \tau(\varphi(a)) = \Gamma_B(\varphi(a))(\tau)$. Naturality of the unit $\varepsilon_X : X \to \widehat{C(X)}$ for $f : X \to Y$ is the identity $\mathrm{ev}_{f(x)} = \mathrm{ev}_x \circ (f^*)$ from Exercise 6. Both $\Gamma_A$ (isometric $*$-isomorphism, by the commutative Gelfand-Naimark theorem) and $\varepsilon_X$ (homeomorphism, by Urysohn separation and the maximal-ideal description of $C(X)$) are isomorphisms, completing the equivalence $\mathsf{CommC^*}^{\mathrm{op}} \simeq \mathsf{CHaus}$.

**Proposition (non-unital duality via unitisation).** Let $A$ be a non-unital commutative C*-algebra and $\tilde A = A \oplus \mathbb{C}1$ its unitisation. Then $\widehat{\tilde A} = \widehat A \sqcup \{\tau_\infty\}$ where $\tau_\infty(a + \lambda 1) = \lambda$ is the character vanishing on $A$, and $\widehat{\tilde A}$ is the one-point compactification of the locally compact $\widehat A$. The unital isomorphism $\tilde A \cong C(\widehat{\tilde A})$ restricts on the ideal $A$ to an isomorphism onto $\{g \in C(\widehat{\tilde A}) : g(\tau_\infty) = 0\} = C_0(\widehat A)$. Hence $A \cong C_0(\widehat A)$ with $\widehat A$ locally compact Hausdorff, and proper continuous maps correspond to $*$-homomorphisms respecting the structure at infinity.

## Connections [Master]

- **C*-algebras: axioms, spectrum, and the continuous functional calculus [39.01.01]** — the continuous functional calculus is the single-generator case of this duality: the commutative subalgebra $C^*(a,1)$ generated by one normal element has character space $\sigma(a)$, so $C(\sigma(a)) \cong C^*(a,1)$ is Gelfand-Naimark for the smallest interesting commutative algebra.

- **Spectral theorem for normal operators [01.01.13]** — in finite dimensions the commutative algebra generated by a diagonalisable operator has a finite character space equal to its eigenvalue set, and the Gelfand transform is the diagonalisation $T = \sum_i \lambda_i P_i$; this unit is the coordinate-free, infinite-dimensional generalisation.

- **Von Neumann algebras and the double commutant [39.03.01]** — strengthening norm closure to weak-operator closure replaces the commutative model $C(X)$ by $L^\infty(X,\mu)$ and the character space by a measure space; the commutative von Neumann algebras are exactly the $L^\infty$ algebras, the measurable analogue of Gelfand duality.

- **Spectral triples and noncommutative geometry [39.06.01]** — the equivalence $\mathsf{CommC^*}^{\mathrm{op}} \simeq \mathsf{CHaus}$ is the entry point: a noncommutative C*-algebra is treated as functions on a pointless noncommutative space, and a Dirac operator restores the metric data that the topology alone omits.

## Historical & philosophical context [Master]

The transform and its commutative representation theorem originate with Israel Gelfand's theory of normed rings in the late 1930s, where he attached to a commutative Banach algebra its space of maximal ideals and represented elements as functions on that space [ref: TODO_REF Gelfand-Naimark 1943]. The decisive sharpening for C*-algebras came in the 1943 paper of Gelfand and Naimark, which proved that a commutative C*-algebra is isometrically $*$-isomorphic to the continuous functions on its maximal ideal space, and that an abstract C*-algebra embeds into the bounded operators on a Hilbert space. The isometry, as opposed to a mere contractive representation, rests on the C*-axiom through the spectral radius identity for self-adjoint elements, which is why the same algebra admits at most one C*-norm.

Marshall Stone's representation of Boolean algebras as clopen sets of a compact totally disconnected space (Stone duality, 1936) is the prototype of the same idea one categorical level down, and the appearance of Stone-Weierstrass in the surjectivity argument is not accidental. The reading of duality as a definition rather than a theorem belongs to Alain Connes, whose noncommutative geometry program from the 1980s onward takes the commutative dictionary as the specification for the noncommutative case, replacing the space by the algebra and equipping it with a spectral triple to recover differential and metric structure.

## Bibliography [Master]

- Davidson, K. R., *C*-Algebras by Example*, Fields Institute Monographs 6, American Mathematical Society, 1996. Ch. I.
- Murphy, G. J., *C*-Algebras and Operator Theory*, Academic Press, 1990. Ch. 2.
- Gelfand, I. and Naimark, M., "On the imbedding of normed rings into the ring of operators in Hilbert space", *Mat. Sbornik* 12 (1943), 197–213.
- Dixmier, J., *C*-Algebras*, North-Holland, 1977. §1.
- Connes, A., *Noncommutative Geometry*, Academic Press, 1994. Ch. I.
- Stone, M. H., "The theory of representations for Boolean algebras", *Transactions of the American Mathematical Society* 40 (1936), 37–111.

---

*Operator-algebras spine, commutative-theory companion to 39.01.01. Produced as the Gelfand-duality anchor: the contravariant equivalence between commutative C*-algebras and (locally) compact Hausdorff spaces, and the conceptual basis of noncommutative topology.*
