---
id: 39.01.03
title: "States, the GNS Construction, and the Gelfand-Naimark Representation Theorem"
slug: states-gns-gelfand-naimark
section: operator-algebras
chapter: c-star-algebras-basics
concept_catalog_id: operator-algebras.states-gns-gelfand-naimark
prerequisites:
  - 39.01.01  # C*-algebras: axioms, spectrum, continuous functional calculus
  - 39.01.02  # commutative C*-algebras and Gelfand duality
  - 02.11.08  # Hilbert space
successors: []
tier_anchors:
  beginner: "Strogatz-style 'a state is an averaging rule, and every averaging rule comes from a vector in a Hilbert space'; the expectation-value picture from quantum mechanics"
  intermediate: "Murphy *C*-Algebras and Operator Theory* Ch. 3 (states and the GNS construction); Davidson *C*-Algebras by Example* Ch. I"
  master: "Davidson *C*-Algebras by Example* Ch. I; Murphy Ch. 3; Dixmier *C*-Algebras* §2; Takesaki *Theory of Operator Algebras I* Ch. I §9"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Davidson, K. R. — C*-Algebras by Example"
    locator: "Ch. I, states, the GNS construction, the universal representation, Gelfand-Naimark"
  - source: TODO_REF
    path: "Murphy, G. J. — C*-Algebras and Operator Theory"
    locator: "Ch. 3, positive linear functionals, states, the GNS construction, irreducibility and pure states"
  - source: TODO_REF
    path: "Gelfand, I. and Naimark, M. — On the imbedding of normed rings into the ring of operators in Hilbert space"
    locator: "Mat. Sbornik 12 (1943), 197–213 — every C*-algebra embeds isometrically into B(H)"
  - source: TODO_REF
    path: "Segal, I. E. — Irreducible representations of operator algebras"
    locator: "Bull. Amer. Math. Soc. 53 (1947), 73–88 — the GNS construction and pure-state irreducibility"
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the C*-algebra layer (`CStarAlgebra`, `CStarRing`,
  positivity via `cfc`, the spectrum and continuous functional calculus) and
  the dual space machinery (`WeakDual`, Banach-Alaoglu), but it does not
  package the state space of a C*-algebra as a named weak-* compact convex
  set, nor does it carry the GNS construction as a stated theorem: the
  sesquilinear form $\langle a,b\rangle_\varphi = \varphi(b^*a)$ built from a
  state, the left kernel / null ideal $N_\varphi = \{a : \varphi(a^*a)=0\}$,
  the Hilbert-space completion $H_\varphi$ of $A/N_\varphi$, the left-regular
  representation $\pi_\varphi$ with cyclic vector $\xi_\varphi$ reproducing
  $\varphi(a) = \langle \pi_\varphi(a)\xi_\varphi, \xi_\varphi\rangle$, the
  universal representation as the direct sum over all states, and the
  resulting noncommutative Gelfand-Naimark theorem that every C*-algebra is
  isometrically *-isomorphic to a norm-closed *-subalgebra of $B(H)$, together
  with the bijection between pure states and (unitary classes of) irreducible
  representations. These are formalisation targets that would assemble the
  scattered Mathlib pieces into the named GNS / Gelfand-Naimark package.
human_reviewer: "Tyler (pending external operator-algebras reviewer)"
estimated_time:
  beginner: 18m
  intermediate: 50m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [davidson-cstar]
source_curriculum: operator-algebras
---

## Intuition [Beginner]

A state is a way of averaging. Given a collection of operators you can add, multiply, scale, and reflect through the star, a state is a rule that assigns to each operator a single number — its average value — in a way that respects the structure: the average of a sum is the sum of averages, and the average of an operator that is a square-of-a-star is never negative. In quantum mechanics this is exactly the recipe for the expected outcome of a measurement once the system has been prepared.

The surprising fact is that every such averaging rule comes from a vector. Pick any state on your algebra, and you can build a space of arrows and a single special arrow inside it, so that averaging an operator means sandwiching it between that arrow and itself. The abstract rule becomes a concrete geometric measurement: point the arrow, apply the operator, and read off the overlap.

Once every averaging rule is realised this way, you can collect all of them at once. Doing so turns any algebra of this kind — no matter how abstractly it was defined — into a genuine algebra of operators acting on arrows. That is the payoff: the abstract becomes concrete.

## Visual [Beginner]

A state turns into a Hilbert space with one marked vector; the operator acts on that vector and the average is the overlap with the marked vector.

![On the left an abstract algebra A with a state phi shown as an averaging dial; a thick arrow labelled GNS construction points to the right, where a Hilbert space H-phi is drawn with a single marked unit vector xi; an operator a is shown pushing xi to a new arrow pi(a)xi, and the average phi(a) is drawn as the shadow of that new arrow onto the original xi.](/img/states-gns-gelfand-naimark-placeholder.svg)

The dictionary reads: the averaging rule becomes a marked vector; an algebra element becomes an operator on the space; and the average of that element becomes the overlap of the operator-moved vector with the marked vector. The marked vector is cyclic, meaning the operators sweep it around enough to fill the whole space.

## Worked example [Beginner]

Take the algebra of two-by-two complex matrices. A simple averaging rule is "report the top-left entry": send a matrix to the number sitting in its first row and first column. Call this rule $\varphi$. Check it averages sensibly: the top-left entry of a sum is the sum of top-left entries, and the constant matrix that scales everything by one reports the number $1$.

Now check positivity on a star-times-itself. Take a matrix $a$ and form $a^* a$. Its top-left entry is the sum of the squared sizes of the first column of $a$, which is never negative. So $\varphi(a^* a) \ge 0$, as a state requires.

Build the geometric picture. Use the first standard basis vector $e_1$ in two-dimensional space as the marked arrow. For any matrix $a$, the overlap of $a\, e_1$ with $e_1$ is exactly the top-left entry of $a$. So $\varphi(a)$ equals the overlap of $a\,e_1$ with $e_1$, and the marked arrow is $e_1$ itself.

What this tells us: the abstract rule "report the top-left entry" is secretly the concrete measurement "apply the matrix to $e_1$ and read the overlap with $e_1$". The averaging rule and the marked vector carry the same information.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A state on an algebra of operators is best described as:

A. A rule that inverts every operator
B. A structure-respecting averaging rule sending each operator to a number, never negative on a star-times-itself
C. A choice of one favourite operator
D. A way to delete the star operation

<details class="exercise__hint"><summary>Hint</summary>

Think of the expected value of a measurement: linear, and never negative on a square.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. A structure-respecting averaging rule, never negative on a star-times-itself.**

Feedback-correct: correct; a state is a linear functional that is positive on elements of the form $a^*a$ and assigns the value $1$ to the identity. Feedback-wrong: a state does not invert operators or pick a favourite; it averages them.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

In the geometric picture built from a state, what does the average of an operator become?

A. The length of the operator
B. The overlap of the operator-moved marked vector with the marked vector
C. The largest entry of the operator
D. The number of basis vectors

<details class="exercise__hint"><summary>Hint</summary>

Apply the operator to the marked arrow, then compare with the original arrow.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. The overlap of the operator-moved marked vector with the marked vector.**

Feedback-correct: correct; the average $\varphi(a)$ is the inner product of $a$ applied to the marked vector with the marked vector itself. Feedback-wrong: it is not a length or an entry count; it is an overlap, which can be complex.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A$ be a C*-algebra [39.01.01]. A linear functional $\varphi : A \to \mathbb{C}$ is **positive** if $\varphi(a^*a) \ge 0$ for all $a \in A$; positivity forces the **Cauchy-Schwarz inequality** $|\varphi(b^*a)|^2 \le \varphi(a^*a)\,\varphi(b^*b)$ and the self-adjointness $\varphi(a^*) = \overline{\varphi(a)}$. A positive functional is automatically bounded, and on a unital algebra $\|\varphi\| = \varphi(1)$. A **state** is a positive linear functional of norm $1$; in the unital case this is the normalisation $\varphi(1) = 1$ [ref: TODO_REF Murphy Ch. 3]. The set of all states is the **state space** $S(A)$.

The state space is a **convex** subset of the dual $A^*$: a convex combination $t\varphi + (1-t)\psi$ of states is again a state. With the **weak-$*$ topology** (pointwise convergence, $\varphi_i \to \varphi$ iff $\varphi_i(a) \to \varphi(a)$ for every $a$), the state space of a unital C*-algebra is **weak-$*$ compact**, being a weak-$*$ closed subset of the unit ball of $A^*$, compact by Banach-Alaoglu [02.11.08]. A **pure state** is an extreme point of $S(A)$: a state that cannot be written as $t\varphi + (1-t)\psi$ with $0 < t < 1$ and $\varphi \ne \psi$ both states. By the Krein-Milman theorem the state space is the weak-$*$ closed convex hull of its pure states, so pure states exist in abundance.

A **representation** of $A$ is a $*$-homomorphism $\pi : A \to B(H)$ into the bounded operators on a Hilbert space $H$. A vector $\xi \in H$ is **cyclic** for $\pi$ if the orbit $\pi(A)\xi = \{\pi(a)\xi : a \in A\}$ is dense in $H$. A representation is **non-degenerate** if $\pi(A)H$ is dense, and **irreducible** if $H$ has no closed $\pi(A)$-invariant subspace other than $0$ and $H$. Two representations $(\pi_1, H_1)$ and $(\pi_2, H_2)$ are **unitarily equivalent** if there is a unitary $U : H_1 \to H_2$ with $U\pi_1(a) = \pi_2(a)U$ for all $a$.

### Counterexamples to common slips

- A positive functional need not respect multiplication. On $M_2(\mathbb{C})$ the normalised trace $\tau(a) = \tfrac12\operatorname{tr}(a)$ is a state, but $\tau(ab) \ne \tau(a)\tau(b)$ in general; states are not characters, and only in the commutative case do the pure states coincide with the characters.
- Positivity needs the star, not the spectrum alone. A functional satisfying $\varphi(a) \ge 0$ merely for self-adjoint $a$ with positive spectrum is not the definition; positivity is the condition $\varphi(a^*a) \ge 0$ for **every** $a$, which is what drives the Cauchy-Schwarz inequality.
- A cyclic vector need not be a state vector for a faithful representation. The GNS vector $\xi_\varphi$ is cyclic but the representation $\pi_\varphi$ can have a kernel; faithfulness is recovered only by summing over enough states, as in the universal representation.

## Key theorem with proof [Intermediate+]

**Theorem (GNS construction).** *Let $A$ be a C*-algebra and $\varphi$ a state on $A$. There exist a Hilbert space $H_\varphi$, a representation $\pi_\varphi : A \to B(H_\varphi)$, and a cyclic vector $\xi_\varphi \in H_\varphi$ such that*
$$
\varphi(a) = \langle \pi_\varphi(a)\xi_\varphi, \xi_\varphi \rangle \qquad (a \in A).
$$
*The triple $(\pi_\varphi, H_\varphi, \xi_\varphi)$ is unique up to unitary equivalence among cyclic triples implementing $\varphi$.* [ref: TODO_REF Murphy Ch. 3; Davidson Ch. I]

**Proof.** Define a sesquilinear form on $A$ by
$$
\langle a, b \rangle_\varphi = \varphi(b^* a).
$$
It is linear in the first slot, conjugate-linear in the second (using $\varphi(a^*) = \overline{\varphi(a)}$), and positive semidefinite since $\langle a, a\rangle_\varphi = \varphi(a^*a) \ge 0$ by positivity of $\varphi$. By Cauchy-Schwarz the set
$$
N_\varphi = \{ a \in A : \varphi(a^*a) = 0 \}
$$
is exactly $\{a : \langle a, b\rangle_\varphi = 0 \text{ for all } b\}$, the **null space** of the form. It is a closed **left ideal**: if $a \in N_\varphi$ and $c \in A$, then $\varphi((ca)^*(ca)) = \varphi(a^* c^* c a) \le \|c^*c\|\,\varphi(a^*a) = 0$, using that $b \mapsto \varphi(a^* b a)$ is a positive functional dominated by $\|b\|\varphi(a^*a)$ (the inequality $c^*c \le \|c\|^2 1$ in the unitisation transfers under any positive functional). Hence $ca \in N_\varphi$.

On the quotient vector space $A/N_\varphi$ the induced form
$$
\langle a + N_\varphi, b + N_\varphi \rangle = \varphi(b^* a)
$$
is a genuine inner product (positive definite, as the null directions were divided out). Let $H_\varphi$ be the **completion** of $A/N_\varphi$ to a Hilbert space, and write $[a] = a + N_\varphi$ for the image of $a$.

Define the representation by left multiplication: $\pi_\varphi(a)[b] = [ab]$. This is well-defined because $N_\varphi$ is a left ideal ($b \in N_\varphi \Rightarrow ab \in N_\varphi$), and it is bounded:
$$
\|\pi_\varphi(a)[b]\|^2 = \varphi(b^* a^* a b) \le \|a^*a\|\,\varphi(b^*b) = \|a\|^2\,\|[b]\|^2,
$$
again using $a^*a \le \|a\|^2 1$ applied inside the positive functional $b \mapsto \varphi(b^* \cdot b)$. So $\pi_\varphi(a)$ extends to a bounded operator on $H_\varphi$ with $\|\pi_\varphi(a)\| \le \|a\|$. The map $\pi_\varphi$ is linear, multiplicative ($\pi_\varphi(a)\pi_\varphi(a')[b] = [aa'b] = \pi_\varphi(aa')[b]$), and $*$-preserving:
$$
\langle \pi_\varphi(a)[b], [c]\rangle = \varphi(c^* a b) = \varphi((a^* c)^* b) = \langle [b], \pi_\varphi(a^*)[c]\rangle,
$$
so $\pi_\varphi(a)^* = \pi_\varphi(a^*)$. Thus $\pi_\varphi$ is a representation.

For the cyclic vector, assume first $A$ unital and set $\xi_\varphi = [1]$. Then
$$
\langle \pi_\varphi(a)\xi_\varphi, \xi_\varphi\rangle = \langle [a], [1]\rangle = \varphi(1^* a) = \varphi(a),
$$
and $\pi_\varphi(A)\xi_\varphi = \{[a] : a \in A\} = A/N_\varphi$ is dense in $H_\varphi$ by construction, so $\xi_\varphi$ is cyclic and is a unit vector since $\|\xi_\varphi\|^2 = \varphi(1) = 1$. (In the non-unital case one uses an approximate unit $(u_\lambda)$: the net $[u_\lambda]$ is Cauchy and its limit $\xi_\varphi$ serves as the cyclic vector, with $\varphi(a) = \lim_\lambda \varphi(u_\lambda a u_\lambda) = \langle \pi_\varphi(a)\xi_\varphi, \xi_\varphi\rangle$.)

Uniqueness: suppose $(\pi, H, \xi)$ is another cyclic triple with $\varphi(a) = \langle \pi(a)\xi, \xi\rangle$. Define $U[a] = \pi(a)\xi$. This is isometric,
$$
\|\pi(a)\xi\|^2 = \langle \pi(a^*a)\xi, \xi\rangle = \varphi(a^*a) = \|[a]\|^2,
$$
so it is well-defined on $A/N_\varphi$ and extends to an isometry $U : H_\varphi \to H$ with dense range $\pi(A)\xi = H$, hence a unitary. It intertwines, $U\pi_\varphi(a) = \pi(a)U$, and sends $\xi_\varphi$ to $\xi$. $\square$

**Bridge.** The GNS construction builds toward the entire representation theory of C*-algebras, and it appears again in [39.03.01] where the von Neumann algebra $\pi_\varphi(A)''$ generated by a representation is the next structural layer and the cyclic vector becomes a (separating, in the faithful case) trace or weight vector. The foundational reason it works is exactly the C*-positivity inequality $a^*a \le \|a\|^2 1$ pushed through the positive functional $\varphi$: it both bounds the operators $\pi_\varphi(a)$ and forces the null ideal $N_\varphi$ to be a left ideal, so left multiplication descends to the quotient. This is exactly the abstract face of the quantum-mechanical statement that a prepared state is a vector and an observable's expectation is an inner product; the construction generalises the commutative Gelfand picture [39.01.02], where a character is a one-dimensional GNS representation and the state space degenerates to the character space. Putting these together, every averaging rule on an abstract algebra is realised as a concrete vector measurement, and the bridge is that summing these realisations over all states embeds the whole algebra into operators on a Hilbert space.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 1 (easy, multiple choice).**

Which condition defines a state on a unital C*-algebra?

A. $\varphi$ is multiplicative and $\varphi(1) = 1$
B. $\varphi$ is linear, $\varphi(a^*a) \ge 0$, and $\varphi(1) = 1$
C. $\varphi$ is isometric onto $\mathbb{C}$
D. $\varphi(a) \ge 0$ for every self-adjoint $a$

<details class="exercise__hint"><summary>Hint</summary>

A state is a positive linear functional of norm one; on a unital algebra the norm equals the value at the identity.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** A state is linear, positive ($\varphi(a^*a) \ge 0$), and normalised by $\varphi(1) = 1$. Multiplicativity is the stronger condition defining a character.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

Let $\varphi$ be the state on $M_2(\mathbb{C})$ given by $\varphi(a) = a_{11}$ (the top-left entry). Compute $\varphi(a^*a)$ for $a = \begin{pmatrix} 0 & 0 \\ 3 & 0 \end{pmatrix}$.

<details class="exercise__hint"><summary>Hint</summary>

The top-left entry of $a^*a$ is the squared norm of the first column of $a$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$9$.** The first column of $a$ is $(0, 3)$, so the top-left entry of $a^*a$ is $0^2 + 3^2 = 9$. Rubric: full credit for $9$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 3 (medium, numeric).**

On $\mathbb{C}^3$ with slotwise operations, let $\varphi(z_1, z_2, z_3) = \tfrac{1}{2}z_1 + \tfrac{1}{3}z_2 + \tfrac{1}{6}z_3$. This is a state. What is the dimension of the GNS Hilbert space $H_\varphi$?

<details class="exercise__hint"><summary>Hint</summary>

The null ideal $N_\varphi$ consists of tuples with $\varphi(\,|z|^2\text{ slotwise}\,) = 0$; here all three weights are strictly positive.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$3$.** Since every weight is strictly positive, $\varphi(\bar z z) = \tfrac12|z_1|^2 + \tfrac13|z_2|^2 + \tfrac16|z_3|^2 = 0$ forces $z = 0$, so $N_\varphi = 0$ and $H_\varphi \cong \mathbb{C}^3$. Rubric: full credit for $3$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Write the GNS inner product on $A/N_\varphi$ induced by a state $\varphi$, and state the formula recovering $\varphi$ from the cyclic vector $\xi_\varphi = [1]$ (unital case).

<details class="exercise__hint"><summary>Hint</summary>

The inner product is $\varphi$ of the star of one element times the other.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$$
\langle [a], [b]\rangle = \varphi(b^* a), \qquad \varphi(a) = \langle \pi_\varphi(a)\xi_\varphi, \xi_\varphi\rangle = \varphi(1^* a 1).
$$
The cyclic vector is $\xi_\varphi = [1]$, and $\pi_\varphi(a)[b] = [ab]$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 5 (medium, short-answer).**

Show that the null space $N_\varphi = \{a : \varphi(a^*a) = 0\}$ of a state $\varphi$ is a left ideal of $A$.

<details class="exercise__hint"><summary>Hint</summary>

Use that $b \mapsto \varphi(a^* b a)$ is a positive functional dominated by $\|b\|\,\varphi(a^*a)$, together with $c^*c \le \|c\|^2 1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

First, $N_\varphi$ is a linear subspace: by Cauchy-Schwarz, $a \in N_\varphi$ iff $\varphi(b^*a) = 0$ for all $b$, and this set of linear conditions is a subspace. For the left-ideal property, let $a \in N_\varphi$ and $c \in A$. In the unitisation $c^*c \le \|c\|^2 1$, so applying the positive functional $x \mapsto \varphi(a^* x a)$ to both sides gives $\varphi(a^* c^* c\, a) \le \|c\|^2 \varphi(a^* a) = 0$. Hence $\varphi((ca)^*(ca)) = 0$, so $ca \in N_\varphi$. Thus $A N_\varphi \subseteq N_\varphi$.

Rubric: full credit for the domination $\varphi(a^*c^*ca) \le \|c\|^2\varphi(a^*a)$ and the conclusion $ca \in N_\varphi$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 6 (hard, short-answer).**

Prove that a state $\varphi$ is pure if and only if the GNS representation $\pi_\varphi$ is irreducible.

<details class="exercise__hint"><summary>Hint</summary>

Relate positive functionals dominated by $\varphi$ to positive operators in the commutant $\pi_\varphi(A)'$ via $T \mapsto \langle T\pi_\varphi(\cdot)\xi_\varphi, \xi_\varphi\rangle$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

There is an order isomorphism between positive functionals $\psi \le \varphi$ (meaning $\varphi - \psi$ is positive) and operators $T$ in the commutant $\pi_\varphi(A)'$ with $0 \le T \le 1$, given by $\psi(a) = \langle \pi_\varphi(a)T\xi_\varphi, \xi_\varphi\rangle$. Indeed any such $\psi$ satisfies a Cauchy-Schwarz bound through $\varphi$, so it factors through $H_\varphi$ as a bounded positive sesquilinear form, represented by a positive contraction $T$; the relation $\psi(ab) $ matching on both sides forces $T \in \pi_\varphi(A)'$. Now $\varphi$ is pure iff the only $\psi$ with $0 \le \psi \le \varphi$ are scalar multiples $t\varphi$, $0 \le t \le 1$; under the isomorphism this says the only $T \in \pi_\varphi(A)'$ with $0 \le T \le 1$ are scalars $t\cdot 1$, i.e. $\pi_\varphi(A)' = \mathbb{C}1$. By Schur's lemma $\pi_\varphi(A)' = \mathbb{C}1$ holds iff $\pi_\varphi$ is irreducible. Hence $\varphi$ pure $\iff$ $\pi_\varphi$ irreducible.

Rubric: full credit for the dominated-functional/commutant correspondence and the Schur's-lemma identification.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Show that for every nonzero $a$ in a C*-algebra there is a state $\varphi$ with $\varphi(a^*a) = \|a\|^2$, and deduce that the universal representation is isometric.

<details class="exercise__hint"><summary>Hint</summary>

Apply Hahn-Banach / the functional calculus to the positive element $a^*a$ inside the commutative subalgebra it generates, then extend to a state on all of $A$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The element $h = a^*a$ is positive with $\|h\| = \|a\|^2$, and $\|h\| \in \sigma(h)$ since $h \ge 0$. In the commutative C*-algebra $C^*(h,1) \cong C(\sigma(h))$ the evaluation at the point $\|h\| \in \sigma(h)$ is a character $\tau$ with $\tau(h) = \|h\|$; it is a state on $C^*(h,1)$. By the Hahn-Banach extension for positive functionals (extend preserving positivity and norm), $\tau$ extends to a state $\varphi$ on $A$ with $\varphi(h) = \|h\| = \|a\|^2$. Then in the universal representation $\pi_u = \bigoplus_{\psi \in S(A)} \pi_\psi$, the summand $\pi_\varphi$ gives $\|\pi_\varphi(a)\xi_\varphi\|^2 = \varphi(a^*a) = \|a\|^2 = \|a\|^2\|\xi_\varphi\|^2$, so $\|\pi_\varphi(a)\| = \|a\|$. Since each $\pi_\psi$ is norm-decreasing and one summand attains $\|a\|$, $\|\pi_u(a)\| = \|a\|$. Hence $\pi_u$ is isometric.

Rubric: full credit for the state attaining $\|a\|^2$ via functional calculus + Hahn-Banach and the isometry of $\pi_u$.

</details>

</aside>

## Lean formalization [Intermediate+]

`lean_status: none` — Mathlib carries the C*-algebra layer (`CStarAlgebra`, positivity through `cfc`, the spectrum) and the dual-space / Banach-Alaoglu machinery, but the state space as a named weak-* compact convex set and the GNS construction as a stated theorem are not packaged: the form $\varphi(b^*a)$, the null left ideal $N_\varphi$, the completion $H_\varphi$, the left-regular representation $\pi_\varphi$ with cyclic vector $\xi_\varphi$, and the noncommutative Gelfand-Naimark embedding are absent as a consolidated development.

The intended statement reads schematically:

```lean
import Mathlib.Analysis.CStarAlgebra.Basic
import Mathlib.Analysis.CStarAlgebra.ContinuousFunctionalCalculus.Basic

variable {A : Type*} [CStarAlgebra A]

/-- GNS: from a state φ build a cyclic representation reproducing φ as a
vector functional. -/
theorem gns_construction (φ : A →ₗ[ℂ] ℂ)
    (hpos : ∀ a, 0 ≤ (φ (star a * a)).re) (hstate : φ 1 = 1) :
    ∃ (H : Type*) (_ : NormedAddCommGroup H) (_ : InnerProductSpace ℂ H)
      (π : A →⋆ₐ[ℂ] (H →L[ℂ] H)) (ξ : H),
      (∀ a, φ a = inner (π a ξ) ξ) ∧ (Dense (Set.range fun a => π a ξ)) :=
  sorry  -- quotient by N_φ, completion, left-regular representation
```

## Advanced results [Master]

The representation theory assembles from the GNS construction by varying the state and collecting the pieces.

**The universal representation and noncommutative Gelfand-Naimark.** Form the **universal representation** $\pi_u = \bigoplus_{\varphi \in S(A)} \pi_\varphi$ on $H_u = \bigoplus_{\varphi} H_\varphi$. For each nonzero $a$, the functional calculus on $a^*a$ together with Hahn-Banach produces a state $\varphi$ with $\varphi(a^*a) = \|a\|^2$, so $\|\pi_\varphi(a)\| = \|a\|$, whence $\|\pi_u(a)\| = \|a\|$: the universal representation is **isometric**, in particular faithful. This is the **Gelfand-Naimark theorem**: every C*-algebra is isometrically $*$-isomorphic to a norm-closed $*$-subalgebra of $B(H_u)$. The abstract C*-axiom $\|a^*a\| = \|a\|^2$ is precisely the abstract characterisation — an involutive Banach algebra satisfying it is realisable as operators, and conversely any norm-closed $*$-subalgebra of $B(H)$ satisfies it.

**Pure states and irreducible representations.** A state is pure iff its GNS representation is irreducible. The correspondence is an order isomorphism between functionals $\psi$ dominated by $\varphi$ and positive contractions in the commutant $\pi_\varphi(A)'$; purity is extremality, which translates to $\pi_\varphi(A)' = \mathbb{C}1$, irreducibility by Schur's lemma. Two pure states give unitarily equivalent irreducible representations iff they are related by a unitary in the (multiplier) algebra; the pure state space modulo this equivalence is the **spectrum** $\widehat A$ of the C*-algebra, the noncommutative analogue of the character space, and for commutative $A$ it is exactly the Gelfand character space [39.01.02].

**Existence of states and the order structure.** States exist in profusion: the functional calculus and Hahn-Banach guarantee, for each self-adjoint $a$ and each $\lambda \in \sigma(a)$, a state $\varphi$ with $\varphi(a) = \lambda$. The state space recovers the order: $a \ge 0$ iff $\varphi(a) \ge 0$ for every state $\varphi$, and $\|a\| = \sup_{\varphi}|\varphi(a)|$ for self-adjoint $a$. Thus the positive cone, the norm, and the spectrum are all readable off the state space, which is why $S(A)$ is a complete invariant of the order-unit structure.

**Synthesis.** The GNS construction is the foundational reason the abstract C*-axiom suffices to characterise operator algebras: it converts each state into a concrete cyclic representation, and summing over all states gives the central insight that the universal representation is isometric, so an abstract C*-algebra is exactly a norm-closed $*$-subalgebra of some $B(H)$. This is dual to the commutative Gelfand-Naimark theorem [39.01.02] — there characters embed a commutative algebra into $C(X)$, here states embed an arbitrary algebra into $B(H)$, and putting these together the character space is the pure-state space of a commutative algebra. The construction generalises in two directions that appear again across operator algebras: downward to the single normal element, where the GNS space of a state on $C^*(a,1)$ is an $L^2$ space against a spectral measure and the calculus of [39.01.01] reappears, and upward to von Neumann algebras [39.03.01], where the bicommutant $\pi_\varphi(A)''$ of a GNS representation is the weak closure and the cyclic vector becomes the separating trace or weight vector of Tomita-Takesaki theory. The bridge to the rest of the subject is that pure states are exactly the irreducible representations, so the noncommutative spectrum $\widehat A$ — the orbit space of pure states — is the object that replaces the character space once commutativity is dropped, and the entire classification program reads off this space.

## Full proof set [Master]

**Proposition (positive functionals are bounded, with $\|\varphi\| = \varphi(1)$ in the unital case).** Let $\varphi$ be positive on a unital C*-algebra $A$. For self-adjoint $a$ with $\|a\| \le 1$, $1 - a \ge 0$ and $1 + a \ge 0$, so $\varphi(a) \le \varphi(1)$ and $-\varphi(a) \le \varphi(1)$, giving $|\varphi(a)| \le \varphi(1)$. For general $a$ with $\|a\| \le 1$, Cauchy-Schwarz gives $|\varphi(a)|^2 = |\varphi(1^* a)|^2 \le \varphi(1)\varphi(a^*a) \le \varphi(1)^2 \|a\|^2 \le \varphi(1)^2$, where $\varphi(a^*a) \le \varphi(1)\|a^*a\| \le \varphi(1)$ uses $a^*a \le \|a\|^2 1 \le 1$. Hence $\|\varphi\| \le \varphi(1)$, and the reverse is immediate from $\varphi(1) = |\varphi(1)| \le \|\varphi\|\|1\| = \|\varphi\|$. So $\|\varphi\| = \varphi(1)$.

**Proposition (Cauchy-Schwarz for positive functionals).** For a positive functional $\varphi$ and $a, b \in A$, the form $\langle a, b\rangle = \varphi(b^*a)$ is a positive semidefinite Hermitian form: $\overline{\varphi(b^*a)} = \varphi((b^*a)^*) = \varphi(a^*b)$ and $\varphi(a^*a) \ge 0$. The standard Cauchy-Schwarz argument for semidefinite forms — minimising $\varphi((a - tb)^*(a - tb)) \ge 0$ over $t \in \mathbb{C}$ — yields $|\varphi(b^*a)|^2 \le \varphi(a^*a)\varphi(b^*b)$. Taking $b = 1$ (unital case) gives $|\varphi(a)|^2 \le \varphi(1)\varphi(a^*a)$.

**Proposition (the GNS form is well-defined and $\pi_\varphi$ is bounded).** The null space $N_\varphi = \{a : \varphi(a^*a) = 0\}$ equals $\{a : \varphi(b^*a) = 0 \ \forall b\}$ by Cauchy-Schwarz, hence is a closed subspace; it is a left ideal by the domination $\varphi(a^*c^*ca) \le \|c\|^2\varphi(a^*a)$. The induced inner product on $A/N_\varphi$ is positive definite. Left multiplication is bounded by $\|\pi_\varphi(a)[b]\|^2 = \varphi(b^*a^*ab) \le \|a\|^2\varphi(b^*b)$, again from $a^*a \le \|a\|^2 1$ pushed through the positive functional $x \mapsto \varphi(b^*xb)$. So $\pi_\varphi(a)$ extends to $H_\varphi$ with $\|\pi_\varphi(a)\| \le \|a\|$, and $\pi_\varphi$ is a $*$-homomorphism.

**Proposition (Gelfand-Naimark: the universal representation is isometric).** For nonzero $a$, put $h = a^*a \ge 0$ with $\|h\| = \|a\|^2$ and $\|h\| \in \sigma(h)$. Evaluation at $\|h\|$ on $C^*(h,1) \cong C(\sigma(h))$ is a state $\tau_0$ with $\tau_0(h) = \|h\|$; extend it by Hahn-Banach (preserving positivity and norm) to a state $\varphi$ on $A$. In the GNS representation, $\|\pi_\varphi(a)\xi_\varphi\|^2 = \varphi(a^*a) = \|a\|^2$, so $\|\pi_\varphi(a)\| \ge \|a\|$; with $\|\pi_\varphi(a)\| \le \|a\|$ always, equality holds. Hence $\|\pi_u(a)\| = \sup_\varphi \|\pi_\varphi(a)\| = \|a\|$, and $\pi_u$ is an isometric $*$-isomorphism onto its (norm-closed) image in $B(H_u)$.

**Proposition (pure $\iff$ irreducible).** Fix a state $\varphi$. Positive functionals $\psi$ with $0 \le \psi \le \varphi$ correspond bijectively and order-isomorphically to operators $T \in \pi_\varphi(A)'$ with $0 \le T \le 1$ via $\psi(a) = \langle \pi_\varphi(a)T\xi_\varphi, \xi_\varphi\rangle$; the cyclicity of $\xi_\varphi$ makes the correspondence injective and the domination bound makes it surjective. Extreme points of the order interval $[0, \varphi]$ are the $t\varphi$ on one side and the $0,1$-spectral projections of $T$ on the other. So $\varphi$ is extreme in the state space iff the only $T \in \pi_\varphi(A)'$ in $[0,1]$ are scalars, iff $\pi_\varphi(A)' = \mathbb{C}1$, iff (Schur) $\pi_\varphi$ is irreducible.

**Proposition (existence of pure states separating points).** For self-adjoint $a \ne 0$ choose $\lambda \in \sigma(a)$ with $|\lambda| = \|a\|$; a state with $\varphi(a) = \lambda$ exists as above. The set of states $\psi$ with $\psi(a) = \|a\|$ is a weak-* compact convex face of $S(A)$, nonempty, so by Krein-Milman it has an extreme point, which is extreme in $S(A)$ as well (a face's extreme points are global extreme points), hence a pure state. Thus pure states separate self-adjoint elements, and by polarisation all elements: the pure states alone already give a faithful (atomic) representation.

## Connections [Master]

- **Commutative C*-algebras and Gelfand duality [39.01.02]** — the commutative Gelfand-Naimark theorem is the GNS theory specialised: characters are exactly the pure states of a commutative C*-algebra, each is a one-dimensional GNS representation, and the noncommutative spectrum $\widehat A$ collapses to the character space; this unit is the noncommutative completion of that picture.

- **C*-algebras: axioms, spectrum, and the continuous functional calculus [39.01.01]** — the existence-of-states argument runs through the continuous functional calculus on the positive element $a^*a$, and a state on the singly-generated subalgebra $C^*(a,1)$ has a GNS space that is an $L^2$ space against a spectral measure, recovering the calculus from the representation side.

- **Hilbert space [02.11.08]** — the GNS construction is a completion-to-a-Hilbert-space procedure: the state defines a semidefinite form, the null directions are quotiented, and the completion supplies the space on which the algebra acts; the cyclic vector is the geometric encoding of the state.

- **Von Neumann algebras and the bicommutant theorem [39.03.01]** — the weak-operator closure $\pi_\varphi(A)''$ of a GNS representation is the von Neumann algebra it generates, the bicommutant theorem identifies it with the bicommutant, and the cyclic vector becomes the separating/cyclic vector underlying Tomita-Takesaki modular theory.

- **Spectral triples and noncommutative geometry [39.06.01]** — the noncommutative spectrum $\widehat A$ of pure states is the pointless space on which noncommutative geometry operates; the GNS representation supplies the Hilbert space a spectral triple acts on, and irreducibility singles out the points of the noncommutative space.

## Historical & philosophical context [Master]

The construction is named for Israel Gelfand, Mark Naimark, and Irving Segal. Gelfand and Naimark proved in 1943 that an abstract C*-algebra (then a $B^*$-algebra, defined by $\|a^*a\| = \|a\|^2$) is isometrically $*$-isomorphic to a norm-closed $*$-subalgebra of the bounded operators on a Hilbert space [ref: TODO_REF Gelfand-Naimark 1943]. Their original argument carried a redundant extra axiom, later removed; the route through states and a single representation was given its definitive form by Segal in 1947, who introduced the systematic construction of a cyclic representation from a positive functional and connected pure states to irreducible representations [ref: TODO_REF Segal 1947].

The physical origin is quantum mechanics: a state in the operator-algebraic sense is the mathematical content of a quantum-mechanical state — a normalised positive expectation functional on the observables — and the GNS Hilbert space is the state's own representation space, the algebraic substitute for the wavefunction. The identification of pure states with irreducible representations is the algebraic form of the superposition principle, and the universal representation is the statement that the abstract algebra of observables is always realisable concretely. Von Neumann's earlier program of rings of operators supplied the ambient setting, and the GNS construction became the standard bridge from the abstract algebra of observables to a concrete Hilbert-space model used throughout algebraic quantum field theory and quantum statistical mechanics.

## Bibliography [Master]

- Davidson, K. R., *C*-Algebras by Example*, Fields Institute Monographs 6, American Mathematical Society, 1996. Ch. I.
- Murphy, G. J., *C*-Algebras and Operator Theory*, Academic Press, 1990. Ch. 3.
- Gelfand, I. and Naimark, M., "On the imbedding of normed rings into the ring of operators in Hilbert space", *Mat. Sbornik* 12 (1943), 197–213.
- Segal, I. E., "Irreducible representations of operator algebras", *Bulletin of the American Mathematical Society* 53 (1947), 73–88.
- Dixmier, J., *C*-Algebras*, North-Holland, 1977. §2.
- Takesaki, M., *Theory of Operator Algebras I*, Springer, 1979. Ch. I §9.

---

*Operator-algebras spine, representation-theoretic foundation of the C*-basics chapter. Produced as the GNS / Gelfand-Naimark anchor: states and the state space, the cyclic representation built from a state, the universal representation and the noncommutative embedding into B(H), and the pure-state / irreducible-representation correspondence.*
