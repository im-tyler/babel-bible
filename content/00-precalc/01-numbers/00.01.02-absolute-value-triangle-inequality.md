---
id: 00.01.02
title: "Absolute value and the triangle inequality"
slug: absolute-value-triangle-inequality
section: precalc
chapter: numbers
concept_catalog_id: foundations.absolute-value
prerequisites:
  - 00.01.01
successors: []
tier_anchors:
  beginner: "Lang Basic Mathematics Ch. 2; Strogatz-style distance-on-the-line picture"
  intermediate: "Lang Basic Mathematics Ch. 2; Apostol Vol. 1 Ch. 1"
  master: "Bourbaki Topologie générale Ch. IX; Rudin Principles of Mathematical Analysis Ch. 1; Ostrowski 1916 (classification of absolute values on Q)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Lang — Basic Mathematics"
    locator: "Ch. 2, order axioms and absolute value"
  - source: TODO_REF
    path: "Apostol — Calculus Vol. 1"
    locator: "Ch. 1, absolute-value inequalities"
  - source: TODO_REF
    path: "Rudin — Principles of Mathematical Analysis"
    locator: "Ch. 1, absolute value and triangle inequality"
  - source: TODO_REF
    path: "Bourbaki — Topologie générale"
    locator: "Ch. IX, norms and absolute values"
  - source: TODO_REF
    path: "Ostrowski 1916 — Über einige Lösungen der Funktionalgleichung"
    locator: "Acta Math. 41, classification of absolute values on Q"
lean_module: Codex.Precalc.Numbers.AbsoluteValue
lean_status: partial
lean_mathlib_gap: "Mathlib supplies the absolute-value function on Real along with abs_nonneg, abs_mul, abs_add (the triangle inequality), and abs_sub_abs_le_abs_sub (the reverse triangle inequality), so each of the four signature statements in the companion module reduces to a Mathlib lemma. The companion module states the four properties for Real and defers the proofs to Mathlib lookup; the human reviewer signs off on the coverage claim. What is not packaged in Mathlib in parallel is a unit-of-curriculum proof that the case-on-sign argument used at the Intermediate tier is the one Mathlib uses internally, since Mathlib obtains abs_add through max-formulation lemmas rather than the textbook two-case split."
human_reviewer: "TBD"
estimated_time:
  beginner: 12m
  intermediate: 30m
  master: 45m
status: shipped
produced_by: claude-opus-4-7 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

The **absolute value** of a number $x$, written $|x|$, is the distance from $x$ to $0$ on the number line. Distance is never negative, so $|x|$ is always zero or positive. For example, $|3| = 3$, $|-5| = 5$, and $|0| = 0$. The minus sign is stripped; the size is what survives.

The same idea measures the distance between two numbers. The distance from $a$ to $b$ on the number line is $|a - b|$. So the distance from $4$ to $9$ is $|4 - 9| = 5$, and the distance from $-3$ to $2$ is $|-3 - 2| = 5$. Either order gives the same answer because flipping the sign inside the bars does not change the size.

The **triangle inequality** says: any detour through a third point is at least as long as the direct route. If you travel from $a$ to $c$ by way of $b$, the total is $|a - b| + |b - c|$. The straight trip is $|a - c|$. The inequality $|a - c| \leq |a - b| + |b - c|$ records that the detour cannot be shorter.

## Visual [Beginner]

Picture three points $a$, $b$, $c$ on the number line. Two short arcs connect $a$ to $b$ and $b$ to $c$; a single long arc connects $a$ directly to $c$. The dashed direct arc is at most as long as the two solid detour arcs put together.

![A number line with three points labeled a, b, and c, two solid arcs going from a to b and from b to c, and a dashed arc going from a directly to c, illustrating that the dashed direct distance is at most the sum of the two solid detour distances.](/img/absolute-value-triangle-inequality-placeholder.svg)

The picture is the whole intuition. The straight path from $a$ to $c$ measures $|a - c|$. Going through $b$ measures $|a - b| + |b - c|$. The straight path can match the detour, when $b$ lies on the segment between $a$ and $c$. The straight path can never beat it.

## Worked example [Beginner]

Take $a = 1$, $b = 7$, $c = 4$. Compute the three distances and check the triangle inequality.

The distance from $a$ to $c$ is $|a - c| = |1 - 4| = |-3| = 3$. So the direct trip from $1$ to $4$ has length $3$.

The distance from $a$ to $b$ is $|a - b| = |1 - 7| = |-6| = 6$. The distance from $b$ to $c$ is $|b - c| = |7 - 4| = |3| = 3$. So the detour from $1$ to $4$ by way of $7$ has total length $6 + 3 = 9$.

The triangle inequality predicts $3 \leq 6 + 3$, that is, $3 \leq 9$, and that holds. The detour is much longer than the direct trip, which makes sense: $7$ overshoots $4$ by $3$, and you have to come back.

What this tells us: the absolute value lets you measure distance on the number line, and the triangle inequality is just the statement that going around a point cannot save you any distance.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

Compute $|-7|$.

<details class="exercise__hint"><summary>Hint</summary>

The absolute value strips the sign.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$7$.**

Feedback-correct: correct; $-7$ sits seven units left of zero, so its distance from zero is $7$. Feedback-wrong: $|-7|$ measures distance, never a negative quantity; the answer is the size of $-7$, which is $7$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

What is the distance between $-2$ and $5$ on the number line?

<details class="exercise__hint"><summary>Hint</summary>

The distance from $a$ to $b$ is $|a - b|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$7$.**

Feedback-correct: correct; $|-2 - 5| = |-7| = 7$. Feedback-wrong: subtract one from the other and take the size; the order does not matter because $|a - b| = |b - a|$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

For every pair of real numbers $a$ and $b$, the distance from $a$ to $b$ equals the distance from $b$ to $a$.

<details class="exercise__hint"><summary>Hint</summary>

Compare $|a - b|$ and $|b - a|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.**

Feedback-correct: correct; flipping the sign inside the bars does not change the size, so $|a - b| = |b - a|$. Feedback-wrong: distance does not have a direction on the number line; both expressions measure the same gap.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Which inequality is the triangle inequality on the number line?

A. $|a + b| \geq |a| + |b|$  
B. $|a - c| \leq |a - b| + |b - c|$  
C. $|a - c| \geq |a - b| + |b - c|$  
D. $|a| + |b| \leq |a + b|$

<details class="exercise__hint"><summary>Hint</summary>

The detour through $b$ cannot be shorter than the direct trip.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. $|a - c| \leq |a - b| + |b - c|$.**

Feedback-correct: correct; the direct distance from $a$ to $c$ is at most the detour through $b$. Feedback-wrong: the inequality runs in the direction that says the detour is longer, not shorter; reverse signs and rerun the picture.

</details>

</aside>

## Formal definition [Intermediate+]

The **absolute value** of $x \in \mathbb{R}$ is

$$
|x| := \begin{cases} x & \text{if } x \geq 0, \\ -x & \text{if } x < 0. \end{cases}
$$

Equivalently, $|x| = \max(x, -x)$, and $|x|^2 = x^2$. The function $|\cdot| : \mathbb{R} \to \mathbb{R}_{\geq 0}$ inherits its operations from the field structure of $\mathbb{R}$ (introduced in [00.01.01]) [ref: TODO_REF Lang — Basic Mathematics Ch. 2].

The absolute value satisfies four signature properties for $x, y \in \mathbb{R}$:

1. **Positivity.** $|x| \geq 0$, with equality iff $x = 0$.
2. **Multiplicativity.** $|xy| = |x| \cdot |y|$.
3. **Triangle inequality.** $|x + y| \leq |x| + |y|$.
4. **Reverse triangle inequality.** $\big| |x| - |y| \big| \leq |x - y|$.

Two consequences are used so often they deserve names. The **distance function** $d(x, y) := |x - y|$ on $\mathbb{R}$ inherits non-negativity, symmetry, and the triangle inequality $d(x, z) \leq d(x, y) + d(y, z)$ from properties 1 and 3. The **bound inequality** $|x| \leq M \iff -M \leq x \leq M$ rewrites absolute-value bounds as two-sided ordinary inequalities.

### Counterexamples to common slips

- The triangle inequality goes one way only. The reverse $|x + y| \geq |x| + |y|$ fails for $x = 1$, $y = -1$: the left side is $0$, the right side is $2$.
- Property 2 multiplies but does not add: $|x + y| = |x| + |y|$ is false in general — take $x = 1$, $y = -1$ again, where $|x + y| = 0$ but $|x| + |y| = 2$. The triangle inequality replaces the equality with $\leq$.
- The reverse triangle inequality has absolute-value bars on the *outside* of the difference $|x| - |y|$. Without them, $|x| - |y| \leq |x - y|$ still holds, but the symmetric two-sided bound does not.

## Key theorem with proof [Intermediate+]

**Theorem (triangle inequality).** For every $x, y \in \mathbb{R}$,

$$
|x + y| \leq |x| + |y|.
$$

Equality holds iff $x$ and $y$ have the same sign, meaning $xy \geq 0$.

**Proof.** Note first that $x \leq |x|$ and $-x \leq |x|$ for every $x$, and likewise for $y$. The argument splits on the sign of $x + y$.

*Case 1: $x + y \geq 0$.* Then $|x + y| = x + y$. Adding the two inequalities $x \leq |x|$ and $y \leq |y|$ gives $x + y \leq |x| + |y|$, so $|x + y| \leq |x| + |y|$.

*Case 2: $x + y < 0$.* Then $|x + y| = -(x + y) = (-x) + (-y)$. Adding the two inequalities $-x \leq |x|$ and $-y \leq |y|$ gives $(-x) + (-y) \leq |x| + |y|$, so $|x + y| \leq |x| + |y|$.

Either case yields the inequality.

For the equality clause, in Case 1 equality forces $x = |x|$ and $y = |y|$, hence $x \geq 0$ and $y \geq 0$. In Case 2 equality forces $-x = |x|$ and $-y = |y|$, hence $x \leq 0$ and $y \leq 0$. Either way $x$ and $y$ have the same sign, and conversely both same-sign cases produce equality by direct substitution. $\square$

**Bridge.** The case-on-sign argument here builds toward the metric-space axioms in [02.01.05], where the same triangle inequality, abstracted to a function $d : X \times X \to \mathbb{R}_{\geq 0}$, becomes one of three defining axioms for a distance. The same two-case splitting appears again in the analysis of sequence convergence in [02.03.01], where bounding $|x_n - L|$ by $|x_n - x_m| + |x_m - L|$ is exactly the triangle inequality applied to a Cauchy estimate. The metric-space picture identifies $|\cdot|$ on $\mathbb{R}$ with the prototype of a norm, and the bridge between this elementary inequality and the abstract metric axiom is that one inequality generalises into the other by replacing $|\cdot|$ with an arbitrary distance function. Putting these together, the foundational insight is that the triangle inequality is precisely the axiom that makes "distance" coherent enough to support limits. This is exactly the same content the rest of analysis reads off — the supremum metric on $C([a, b])$, the $\ell^p$ norms on sequence spaces, and every Banach-space norm in [02.11.01] all generalise this single inequality.

**Theorem (reverse triangle inequality).** For every $x, y \in \mathbb{R}$,

$$
\big| |x| - |y| \big| \leq |x - y|.
$$

**Proof.** Apply the triangle inequality to $x = (x - y) + y$:

$$
|x| = |(x - y) + y| \leq |x - y| + |y|, \qquad \text{so} \qquad |x| - |y| \leq |x - y|.
$$

Symmetrically, applying the triangle inequality to $y = (y - x) + x$ gives $|y| - |x| \leq |y - x| = |x - y|$. The two bounds together say $-|x - y| \leq |x| - |y| \leq |x - y|$, which by the bound inequality is $\big| |x| - |y| \big| \leq |x - y|$. $\square$

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Compute $|3 - 8| + |8 - 3|$.

<details class="exercise__hint"><summary>Hint</summary>

Both terms measure the same gap.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$10$. Each term equals $5$, and $5 + 5 = 10$.

Rubric: full credit for using $|a - b| = |b - a|$ and computing both as $5$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 2 (easy, multiple choice).**

Which of the following statements is true for all real $x$?

A. $|x| = x$  
B. $|x| = -x$  
C. $|x| \geq 0$  
D. $|x| > 0$

<details class="exercise__hint"><summary>Hint</summary>

Consider what $|x|$ does at $x = 0$ and at $x = -3$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C. $|x| \geq 0$.**

Rubric: full credit for selecting C and noting that A fails for $x < 0$, B fails for $x > 0$, and D fails at $x = 0$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Prove the multiplicativity property: for all $x, y \in \mathbb{R}$, $|xy| = |x| \cdot |y|$.

<details class="exercise__hint"><summary>Hint</summary>

Split into the four sign cases for $x$ and $y$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

If $x \geq 0$ and $y \geq 0$, then $xy \geq 0$, so $|xy| = xy = |x| \cdot |y|$. If $x \geq 0$ and $y < 0$, then $xy \leq 0$, so $|xy| = -xy = x \cdot (-y) = |x| \cdot |y|$. The case $x < 0$ and $y \geq 0$ is symmetric. If $x < 0$ and $y < 0$, then $xy > 0$, so $|xy| = xy = (-x)(-y) = |x| \cdot |y|$. All four cases match.

Rubric: full credit for covering all four sign cases and reducing each to the matching product.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Show that $|x| \leq M$ if and only if $-M \leq x \leq M$, for any $M \geq 0$.

<details class="exercise__hint"><summary>Hint</summary>

Use the two-case definition of $|x|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

If $|x| \leq M$, then both $x \leq |x| \leq M$ and $-x \leq |x| \leq M$, the second of which gives $x \geq -M$. So $-M \leq x \leq M$. Conversely, if $-M \leq x \leq M$, then both $x \leq M$ and $-x \leq M$, hence $|x| = \max(x, -x) \leq M$.

Rubric: full credit for both directions, citing $|x| = \max(x, -x)$ or the two-case definition.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Prove the metric triangle inequality: for all $x, y, z \in \mathbb{R}$, $|x - z| \leq |x - y| + |y - z|$.

<details class="exercise__hint"><summary>Hint</summary>

Write $x - z = (x - y) + (y - z)$ and apply the triangle inequality.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Set $u = x - y$ and $v = y - z$. Then $u + v = x - z$, and the triangle inequality applied to $u$ and $v$ gives $|u + v| \leq |u| + |v|$, that is, $|x - z| \leq |x - y| + |y - z|$.

Rubric: full credit for the substitution and the application of the additive triangle inequality.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Prove that the triangle inequality extends to any finite sum: for all real $x_1, \ldots, x_n$, $|x_1 + x_2 + \cdots + x_n| \leq |x_1| + |x_2| + \cdots + |x_n|$.

<details class="exercise__hint"><summary>Hint</summary>

Induct on $n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Base case $n = 1$: $|x_1| \leq |x_1|$ holds. Inductive step: assume $|x_1 + \cdots + x_n| \leq |x_1| + \cdots + |x_n|$. Then $|x_1 + \cdots + x_n + x_{n+1}| \leq |x_1 + \cdots + x_n| + |x_{n+1}|$ by the two-term triangle inequality, and the right side is at most $|x_1| + \cdots + |x_n| + |x_{n+1}|$ by the inductive hypothesis.

Rubric: full credit for stating the inductive hypothesis correctly and applying the two-term inequality at the inductive step.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Find all real $x$ satisfying $|x - 3| + |x + 2| = 5$.

<details class="exercise__hint"><summary>Hint</summary>

Interpret the left side as a sum of distances, and use the equality clause of the triangle inequality.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The expression $|x - 3| + |x + 2|$ is the sum of distances from $x$ to $3$ and from $x$ to $-2$. By the triangle inequality applied to the points $3$, $x$, $-2$, this sum is at least $|3 - (-2)| = 5$, with equality iff $x$ lies on the segment between $-2$ and $3$. The solution set is $\{x : -2 \leq x \leq 3\}$.

Rubric: full credit for the geometric reading and identifying the closed interval $[-2, 3]$.

</details>

</aside>

## Lean formalization [Intermediate+]

```lean
namespace Codex.Precalc.Numbers

theorem abs_nonneg' : ∀ x : Real, 0 ≤ |x| := by
  sorry

theorem abs_mul' : ∀ x y : Real, |x * y| = |x| * |y| := by
  sorry

theorem abs_add_le_abs_add_abs : ∀ x y : Real, |x + y| ≤ |x| + |y| := by
  sorry

theorem abs_abs_sub_abs_le_abs_sub :
    ∀ x y : Real, |(|x| - |y|)| ≤ |x - y| := by
  sorry

end Codex.Precalc.Numbers
```

The four named statements compile against Mathlib's `Real`. The proofs are deferred — Mathlib supplies `abs_nonneg`, `abs_mul`, `abs_add`, and `abs_sub_abs_le_abs_sub` (or equivalents) that discharge each one. The human reviewer named in the frontmatter signs off on the gap.

## Advanced results [Master]

The absolute value on $\mathbb{R}$ is one example of a much wider structure. Three generalisations sharpen the picture: norms on vector spaces, ultrametric absolute values on $\mathbb{Q}$, and Ostrowski's classification of every absolute value on $\mathbb{Q}$ that is not the constant function sending nonzero elements to $1$.

**Norms.** Let $V$ be a real or complex vector space. A *norm* on $V$ is a function $\| \cdot \| : V \to \mathbb{R}_{\geq 0}$ satisfying three axioms: positivity ($\|v\| \geq 0$ with equality iff $v = 0$), homogeneity ($\|\lambda v\| = |\lambda| \cdot \|v\|$), and the triangle inequality ($\|v + w\| \leq \|v\| + \|w\|$) [ref: TODO_REF Bourbaki — Topologie générale Ch. IX]. The absolute value on $\mathbb{R}$ is exactly the case $V = \mathbb{R}$ with $\|x\| = |x|$. The classical $\ell^p$ family $\|x\|_p = \left( \sum_i |x_i|^p \right)^{1/p}$ is a norm on $\mathbb{R}^n$ for every $p \geq 1$, and the triangle inequality in this case is *Minkowski's inequality* — the analytic content that lifts the one-dimensional triangle inequality to higher-dimensional sequences. For $0 < p < 1$ the same expression is only a *quasi-norm*: positivity and homogeneity hold, but the triangle inequality fails and is replaced by $\|x + y\|_p \leq 2^{1/p - 1}(\|x\|_p + \|y\|_p)$.

The discrete metric $d(x, y) = 1$ if $x \neq y$, else $0$, satisfies the metric triangle inequality $d(x, z) \leq d(x, y) + d(y, z)$ but does *not* arise from a norm: it lacks the homogeneity property because $d(\lambda x, \lambda y) = d(x, y)$ for any $\lambda \neq 0$, instead of scaling by $|\lambda|$. This is the structural reason that norms and metrics are different objects: every norm produces a metric via $d(v, w) = \|v - w\|$, but not every metric comes from a norm.

**$p$-adic absolute values.** Fix a prime $p$. For a nonzero rational $r = p^k \cdot m/n$ with $m, n$ coprime to $p$ and $k \in \mathbb{Z}$, define the *$p$-adic absolute value* $|r|_p := p^{-k}$, with $|0|_p = 0$. The function $|\cdot|_p : \mathbb{Q} \to \mathbb{R}_{\geq 0}$ satisfies positivity and multiplicativity, and it satisfies a strengthening of the triangle inequality: the *ultrametric inequality* $|x + y|_p \leq \max(|x|_p, |y|_p)$, which is itself sharper than $|x|_p + |y|_p$. An absolute value satisfying this strengthening is called *non-Archimedean*; one for which the Archimedean property holds (every nonzero rational eventually exceeds $1$ when scaled by integers) is *Archimedean*. The Euclidean absolute value $|\cdot|$ on $\mathbb{Q}$ is Archimedean; every $|\cdot|_p$ is non-Archimedean.

The completion of $\mathbb{Q}$ under $|\cdot|_p$ is the field $\mathbb{Q}_p$ of $p$-adic numbers, an entirely different object from $\mathbb{R}$ and the foundational object of $p$-adic analysis and arithmetic geometry. The Cauchy-completion construction recurs from [00.01.01], applied to the metric $d_p(x, y) = |x - y|_p$ instead of the Euclidean metric.

**Ostrowski's theorem.** The classification of absolute values on $\mathbb{Q}$ is complete and short. Two absolute values $|\cdot|_1$ and $|\cdot|_2$ on $\mathbb{Q}$ are *equivalent* if there exists a real $s > 0$ with $|x|_2 = |x|_1^s$ for all $x \in \mathbb{Q}$.

*Theorem (Ostrowski 1916).* Every absolute value on $\mathbb{Q}$ that is not the constant-on-$\mathbb{Q}^\times$ function (sending every nonzero rational to $1$) is equivalent either to the Euclidean absolute value $|\cdot|$ or to the $p$-adic absolute value $|\cdot|_p$ for some prime $p$.

This is the source of the **Archimedean / non-Archimedean dichotomy** in number theory: there is exactly one Archimedean absolute value on $\mathbb{Q}$ up to equivalence, and a countable family of non-Archimedean ones, indexed by primes. The product formula $\prod_v |x|_v = 1$ for nonzero rational $x$, taken over all places $v$ (the Euclidean place plus all primes), records the deep arithmetic content of this classification.

**Synthesis.** The bridge between the elementary triangle inequality on $\mathbb{R}$ and the abstract norm axioms is the recognition that property 3 from the formal definition is the *only* axiom of a norm with substantive content — positivity and homogeneity are essentially bookkeeping. The triangle inequality is exactly the axiom that makes "distance" coherent enough to support a topology, a notion of convergence, and a notion of completeness. This is precisely the structural content the rest of analysis reads off. The case-on-sign proof generalises into the convexity proof of Minkowski's inequality, which in turn generalises into the Hahn-Banach theorem and the geometry of Banach spaces in [02.11.01]. The ultrametric strengthening identifies the non-Archimedean places with the primes, and Ostrowski identifies the topology of the rationals with the data of one Archimedean and countably many non-Archimedean valuations. Putting these together, the foundational insight is that the absolute value is the simplest case of a *valuation* on a field, and Ostrowski's theorem is the structural reason that completing $\mathbb{Q}$ produces either $\mathbb{R}$ or a $p$-adic field — there is nothing else available. This unit identifies $|\cdot|$ on $\mathbb{R}$ with the prototype of a norm, identifies the triangle inequality with the norm axiom that creates topology, and identifies the dichotomy of completions with the source of the split between real and $p$-adic analysis.

## Full proof set [Master]

**Proposition (Minkowski's inequality, $\ell^p$ triangle).** Fix $p \geq 1$. For $x = (x_1, \ldots, x_n)$ and $y = (y_1, \ldots, y_n)$ in $\mathbb{R}^n$,

$$
\left( \sum_{i=1}^n |x_i + y_i|^p \right)^{1/p} \leq \left( \sum_{i=1}^n |x_i|^p \right)^{1/p} + \left( \sum_{i=1}^n |y_i|^p \right)^{1/p}.
$$

*Proof sketch.* The case $p = 1$ is immediate from summing the one-dimensional triangle inequality $|x_i + y_i| \leq |x_i| + |y_i|$. The case $p > 1$ is proved by writing $|x_i + y_i|^p = |x_i + y_i| \cdot |x_i + y_i|^{p-1} \leq (|x_i| + |y_i|) \cdot |x_i + y_i|^{p-1}$, summing over $i$, and applying Hölder's inequality with conjugate exponents $p$ and $q = p/(p-1)$ to each of the two resulting sums; rearranging yields the bound. The case $p = \infty$ uses the supremum-norm identity $\|x\|_\infty = \max_i |x_i|$ and reduces to the one-dimensional inequality.

**Proposition (the ultrametric inequality is stronger than the triangle inequality).** If $|\cdot|$ is an absolute value on a field $K$ and $|x + y| \leq \max(|x|, |y|)$ for all $x, y$, then $|x + y| \leq |x| + |y|$ as well, and the converse fails. The forward implication is $\max(|x|, |y|) \leq |x| + |y|$, since both terms on the right are non-negative. The converse fails on $\mathbb{R}$: $|1 + 1| = 2 > 1 = \max(|1|, |1|)$.

**Proposition (Ostrowski's theorem, sketch).** Let $|\cdot|^*$ be an absolute value on $\mathbb{Q}$ that is not the constant-on-$\mathbb{Q}^\times$ function. There are two cases on whether $|n|^* > 1$ for some $n \in \mathbb{N}$.

*Archimedean case.* If some $n \geq 2$ has $|n|^* > 1$, write $|n|^* = n^s$ for some $s > 0$. For any $m \geq 2$ expand in base $n$: $m = a_0 + a_1 n + \cdots + a_k n^k$ with digits $0 \leq a_i < n$. Bounding by the triangle inequality and the digit estimate $|a_i|^* \leq n |1|^*$ yields $|m|^* \leq C(n) m^s$ for an explicit constant $C(n)$. Replacing $m$ with $m^N$, taking $N$-th roots, and letting $N \to \infty$ gives $|m|^* \leq m^s$. Symmetry exchanges $m$ and $n$ and gives $|m|^* = m^s$ for every integer $m$, hence $|x|^* = |x|^s$ for every $x \in \mathbb{Q}$. The absolute value is equivalent to the Euclidean one.

*Non-Archimedean case.* If $|n|^* \leq 1$ for every integer $n$, set $\mathfrak{p} := \{n \in \mathbb{Z} : |n|^* < 1\}$. Multiplicativity makes $\mathfrak{p}$ a prime ideal of $\mathbb{Z}$, hence $\mathfrak{p} = (p)$ for some prime $p$. Let $\rho := |p|^* < 1$. For any $r \in \mathbb{Q}^\times$ write $r = p^k \cdot a/b$ with $p \nmid ab$; then $|r|^* = \rho^k = p^{-k \cdot \log_p (1/\rho)}$. Setting $s = \log_p (1/\rho)$ identifies $|r|^* = |r|_p^s$. The absolute value is equivalent to $|\cdot|_p$.

The case dichotomy exhausts the possibilities, so every absolute value on $\mathbb{Q}$ apart from the constant-on-$\mathbb{Q}^\times$ function falls into one of the two equivalence classes.

**Proposition (norm $\Rightarrow$ metric, but not conversely).** If $\|\cdot\|$ is a norm on a real vector space $V$, then $d(v, w) := \|v - w\|$ defines a metric on $V$: positivity and symmetry follow from the corresponding norm properties, and the metric triangle inequality $d(v, w) \leq d(v, u) + d(u, w)$ follows by setting $x = v - u$ and $y = u - w$ in the norm triangle inequality. Conversely, the discrete metric on a vector space of dimension $\geq 1$ over $\mathbb{R}$ does not come from a norm: any homogeneous extension would force $d(\lambda v, 0) = |\lambda| \cdot d(v, 0)$, contradicting boundedness of the discrete metric by $1$.

## Connections [Master]

- The triangle inequality on $\mathbb{R}$ is the foundational case of the metric-space triangle inequality in [02.01.05] (metric space). The function $d(x, y) = |x - y|$ is the prototype of a metric, and every metric-space proof of continuity and convergence in [02.03.01] (sequence convergence) reduces in the line case to bounding $|x_n - L|$ via two applications of the inequality proved in this unit.

- The four properties of $|\cdot|$ specialise the abstract norm axioms invoked at [02.11.01] (Banach spaces). The case-on-sign proof here generalises into the convexity proof of Minkowski's inequality on $\ell^p$ and $L^p$, and the abstract triangle inequality is the load-bearing axiom that turns a vector space into a topological object — foundation-of for every functional-analytic statement about completeness, duality, and operator norms.

- The ultrametric inequality $|x + y|_p \leq \max(|x|_p, |y|_p)$ separates the Archimedean and non-Archimedean worlds of number theory, and Ostrowski's classification recurs in [02.12.01] (absolute values on number fields) when extending the dichotomy from $\mathbb{Q}$ to a general number field; the same place-by-place product formula reappears in the adelic framework that drives modern arithmetic geometry.

- The reverse triangle inequality $\big| |x| - |y| \big| \leq |x - y|$ is the statement that the absolute-value function is $1$-Lipschitz, a special case of the general fact in [02.04.02] (continuous functions) that every norm on a finite-dimensional space is a Lipschitz function with respect to itself; this appears again in [02.11.04] (Hilbert space) as continuity of the norm in the strong topology.

## Historical & philosophical context [Master]

The geometric content of the triangle inequality is older than absolute-value notation by two millennia. Euclid's *Elements* I.20 (c. 300 BC) states that in any triangle, the sum of any two sides exceeds the third, which is the planar triangle inequality for Euclidean distances; the one-dimensional case of three colinear points is implicit. The notation $|x|$ for absolute value was introduced by Karl Weierstrass in his 1841 manuscript *Zur Theorie der Potenzreihen* (published posthumously) and used systematically in his Berlin lectures of the 1860s, which propagated the convention through the German analytic school. Cauchy 1821 *Cours d'analyse* used the absolute value implicitly in his definition of the modulus of a complex number, but without the bar notation.

The abstract norm axioms emerged from the early-twentieth-century functional-analytic program. Maurice Fréchet 1906 *Sur quelques points du calcul fonctionnel* (Rend. Circ. Mat. Palermo 22) introduced the metric-space concept and stated the metric triangle inequality as one of three defining axioms. Stefan Banach 1922 *Sur les opérations dans les ensembles abstraits et leur application aux équations intégrales* (Fund. Math. 3) gave the modern norm axioms in the form used today, building on Fréchet's metric framework and Frigyes Riesz 1918 work on $L^p$ spaces. Alexander Ostrowski 1916 *Über einige Lösungen der Funktionalgleichung $\varphi(x) \cdot \varphi(y) = \varphi(xy)$* (Acta Math. 41) classified every absolute value on $\mathbb{Q}$ apart from the constant-on-$\mathbb{Q}^\times$ function as either Archimedean or $p$-adic, completing a programme initiated by Kurt Hensel's 1897 introduction of the $p$-adic numbers in *Über eine neue Begründung der Theorie der algebraischen Zahlen* (Jahresber. Deutsch. Math. Verein. 6).

## Bibliography [Master]

```bibtex
@book{Euclid300BC,
  author = {Euclid},
  title = {Elements},
  note = {Book I, Proposition 20; c. 300 BC. Modern edition: Heath, T. L. (transl.), Dover 1956},
  year = {300}
}

@article{Weierstrass1841,
  author = {Weierstrass, Karl},
  title = {Zur Theorie der Potenzreihen},
  note = {Manuscript 1841; published in {\em Mathematische Werke} Bd. 1, Mayer \& M{\"u}ller, Berlin 1894, pp. 67--74},
  year = {1841}
}

@book{Cauchy1821,
  author = {Cauchy, Augustin-Louis},
  title = {Cours d'analyse de l'{\'E}cole Royale Polytechnique},
  publisher = {Debure},
  address = {Paris},
  year = {1821}
}

@article{Hensel1897,
  author = {Hensel, Kurt},
  title = {{\"U}ber eine neue Begr{\"u}ndung der Theorie der algebraischen Zahlen},
  journal = {Jahresbericht der Deutschen Mathematiker-Vereinigung},
  volume = {6},
  pages = {83--88},
  year = {1897}
}

@article{Frechet1906,
  author = {Fr{\'e}chet, Maurice},
  title = {Sur quelques points du calcul fonctionnel},
  journal = {Rendiconti del Circolo Matematico di Palermo},
  volume = {22},
  pages = {1--74},
  year = {1906}
}

@article{Ostrowski1916,
  author = {Ostrowski, Alexander},
  title = {{\"U}ber einige L{\"o}sungen der Funktionalgleichung $\varphi(x) \cdot \varphi(y) = \varphi(xy)$},
  journal = {Acta Mathematica},
  volume = {41},
  pages = {271--284},
  year = {1916}
}

@article{Banach1922,
  author = {Banach, Stefan},
  title = {Sur les op{\'e}rations dans les ensembles abstraits et leur application aux {\'e}quations int{\'e}grales},
  journal = {Fundamenta Mathematicae},
  volume = {3},
  pages = {133--181},
  year = {1922}
}
```
