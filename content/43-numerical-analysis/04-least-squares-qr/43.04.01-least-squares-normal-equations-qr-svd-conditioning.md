---
id: 43.04.01
title: "Least squares: normal equations vs QR vs SVD conditioning"
slug: least-squares-normal-equations-qr-svd-conditioning
section: numerical-analysis
chapter: 04-least-squares-qr
concept_catalog_id: numerical-analysis.least-squares-normal-equations-qr-svd-conditioning
prerequisites:
  - 01.01.09  # Gram-Schmidt / QR / Householder / modified Gram-Schmidt
  - 01.01.12  # singular value decomposition / pseudoinverse / normal equations
  - 43.01.02  # conditioning and condition numbers of problems
successors: []
tier_anchors:
  beginner: "Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lecture 11 (the least-squares problem, the three algorithms informally); Strang 2016 *Introduction to Linear Algebra* 5e (Wellesley-Cambridge) Ch. 4 (projections and least squares)"
  intermediate: "Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lectures 11, 18-19 (the three algorithms, conditioning of least squares, stability comparison); Golub-Van Loan 2013 *Matrix Computations* 4e (Johns Hopkins) §5.3 (the QR and normal-equation solvers)"
  master: "Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lectures 18-19; Golub-Van Loan 2013 *Matrix Computations* 4e (Johns Hopkins) §5.3-5.5; Bjorck 1996 *Numerical Methods for Least Squares Problems* (SIAM) Ch. 1-2; Higham 2002 *Accuracy and Stability of Numerical Algorithms* 2e (SIAM) Ch. 19-20"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Trefethen, L. N. & Bau, D. — Numerical Linear Algebra"
    locator: "SIAM, 1997. Lecture 11 (the least-squares problem, normal equations, QR, SVD), Lecture 18 (conditioning of least squares: the kappa(A), tan(theta), eta sensitivity factors), Lecture 19 (stability of least-squares algorithms; why normal equations square the condition number and Householder QR is backward stable)."
  - source: TODO_REF
    path: "Golub, G. H. & Van Loan, C. F. — Matrix Computations (4th ed.)"
    locator: "Johns Hopkins University Press, 2013. §5.3 (the QR factorization and least squares), §5.3.7-5.3.8 (the normal-equation method via Cholesky and its loss of accuracy kappa(A^T A)=kappa(A)^2), §5.5 (the SVD and rank-deficient least squares)."
  - source: TODO_REF
    path: "Bjorck, A. — Numerical Methods for Least Squares Problems"
    locator: "SIAM, 1996. Ch. 1 (the linear least-squares problem, the four fundamental subspaces, the pseudoinverse), Ch. 2 (the method of normal equations, QR methods, perturbation theory and the condition number of the least-squares problem)."
  - source: TODO_REF
    path: "Wedin, P.-A. — Perturbation theory for pseudo-inverses"
    locator: "BIT Numerical Mathematics 13 (1973), 217-232. The perturbation theorem for the least-squares solution and the pseudoinverse, including the residual-dependent kappa(A)^2 term in the sensitivity bound."
  - source: TODO_REF
    path: "Golub, G. H. & Wilkinson, J. H. — Note on the iterative refinement of least squares solution"
    locator: "Numerische Mathematik 9 (1966), 139-148. Early analysis of the accuracy of normal equations versus orthogonal-factorization methods and iterative refinement for ill-conditioned least squares."
lean_status: none
lean_mathlib_gap: "Mathlib packages the underlying linear algebra — the QR/Gram-Schmidt API in Mathlib.Analysis.InnerProductSpace.GramSchmidtOrtho, the spectral theorem and Matrix.PosSemidef.sqrt feeding the SVD, the Moore-Penrose pseudoinverse Matrix.MoorePenrose.pinv in development, and the operator/Frobenius norms — but it does not host the numerical-analysis layer compared here: there is no formal statement that the method of normal equations forms A^T A and thereby has effective condition number kappa(A)^2 = (sigma_1/sigma_n)^2, no backward-stability theorem for Householder QR least squares of the form (A+dA)x = b+db with ||dA|| = O(eps_mach)||A||, and no formalization of the least-squares condition number itself, namely the residual-and-angle-dependent bound in which the solution sensitivity carries both a kappa(A) term and a kappa(A)^2 tan(theta) term governed by the angle theta between b and the column space of A. A Codex.NumericalAnalysis.LeastSquaresConditioning module would state these as theorems over RCLike fields once the pseudoinverse and a floating-point model are available; Mathlib has no floating-point arithmetic model, so the stability statements are out of current scope."
human_reviewer: "Tyler (pending external numerical-analysis reviewer)"
source_books: [trefethen-bau]
source_curriculum: numerical-analysis
estimated_time:
  beginner: 20m
  intermediate: 45m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

You have far more equations than unknowns — a hundred measurements, three knobs to fit. There is no setting of the knobs that hits every measurement exactly, so you settle for the setting that misses by the least total amount, measured as the sum of squared errors. That is the least-squares problem. The solution theory — that this best setting exists, that it is unique when the knobs are independent, and how to write it down — is already settled elsewhere. This unit is about a different and very practical question: once you know the answer on paper, which recipe should the computer actually run to get it?

There are three recipes, and they are not equally good. The first squares both sides of the problem to get a small square system and solves that. It is the cheapest and the most natural, and it is the one to avoid when the fit is delicate. The second factors the tall data matrix into a rotation part and a triangular part, then solves a triangular system. It costs a bit more and is far steadier. The third pulls the data matrix apart into its stretch factors and inverts each stretch separately. It costs the most and is the most robust, and it is the only one that copes gracefully when two knobs nearly duplicate each other.

The whole point is sensitivity. Every recipe starts from data that carries rounding error, and a delicate fitting problem magnifies that error. The first recipe magnifies it twice over — it doubles the trouble before it even starts solving. The other two do not. So the choice of recipe is not about taste; it is about how much accuracy survives the computation.

## Visual [Beginner]

The table contrasts the three recipes for the same least-squares fit on a single axis that matters in practice: how badly each one amplifies input error, measured against the sensitivity built into the problem itself (the problem's own amplification factor is written $K$).

| Recipe | What it does | Cost | Error amplification |
|---|---|---|---|
| Normal equations | square the system, then solve a small square system | lowest | about $K^2$ — the trouble is doubled |
| QR factorization | rotate-and-triangularize, then back-substitute | medium | about $K$ — matches the problem |
| SVD | split into stretch factors, invert each | highest | about $K$, and survives near-duplicate knobs |

Reading the last column is the whole lesson. The middle and bottom recipes amplify error by about the factor $K$ that the problem already forces — they add almost nothing of their own. The top recipe amplifies by about $K$ times $K$. When $K$ is ten, the difference between $10$ and $100$ is a lost digit. When $K$ is ten thousand, the difference is eight digits against four, and a sixteen-digit computer can return an answer with no correct digits at all from the top recipe while the others stay accurate.

![A three-row comparison diagram. Each row is one least-squares recipe — normal equations, QR factorization, SVD — drawn as a horizontal bar whose length is the error amplification. The normal-equations bar is the longest and is labelled K times K. The QR and SVD bars are short and equal, each labelled K, where K is the problem's own amplification factor. Cost increases down the rows while error amplification drops sharply after the first row.](/img/least-squares-normal-equations-qr-svd-conditioning-placeholder.svg)

The geometry behind the table is the rotate-stretch picture. Squaring the data matrix squares its stretch factors, and the ratio of largest to smallest stretch is exactly the amplification factor — so squaring the matrix squares the amplification. The rotate-and-triangularize recipe never forms the squared matrix, so it never squares the factor.

## Worked example [Beginner]

Take a data matrix whose two columns are almost the same direction, so the fit is delicate:

$$
A = \begin{pmatrix} 1 & 1 \\ 0 & 0.001 \\ 0 & 0 \end{pmatrix}.
$$

The two stretch factors of $A$ are about $1.414$ and about $0.0007$, so the amplification factor is their ratio, $K \approx 2000$. This is the sensitivity the problem itself carries.

Now follow the normal-equations recipe. It forms the small square matrix by multiplying $A$ by its own transpose:

$$
A^{T} A = \begin{pmatrix} 1 & 1 \\ 1 & 1.000001 \end{pmatrix}.
$$

The two stretch factors of this squared matrix are the squares of the originals, so its amplification factor is $K^2 \approx 4{,}000{,}000$. The recipe must now solve a system with this squared sensitivity. On a computer that keeps about sixteen digits, an amplification of four million eats roughly seven of them, leaving nine correct digits. With a slightly worse-conditioned matrix the squared factor reaches $10^{16}$ and every digit is gone.

The QR recipe never forms $A^{T} A$. It factors $A$ directly into an orthonormal part and a triangular part and back-substitutes, working the whole time at amplification $K \approx 2000$, which costs only about three digits.

What this tells us: the two recipes compute the same answer in exact arithmetic, but the normal-equations recipe enters the computation already carrying the squared amplification $K^2$, while QR carries only $K$. The squaring happens at the very first step — forming $A^{T} A$ — and nothing afterward can undo it.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A least-squares problem has amplification factor $K = 100$. By what factor does the normal-equations recipe amplify input error?

<details class="exercise__hint"><summary>Hint</summary>

The normal-equations recipe forms the squared matrix, which squares the amplification factor.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$10{,}000$.** Forming the squared matrix squares the amplification factor, so $K^2 = 100^2 = 10{,}000$. Feedback-correct: the normal-equations method works with $K^2$, here ten thousand. Feedback-wrong: $100$ is the problem's own factor $K$, which the QR and SVD recipes match; the normal-equations recipe squares it.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Which recipe is the most robust choice when two columns of the data matrix nearly duplicate one another or the matrix is rank-deficient?

A. Normal equations
B. QR factorization
C. SVD
D. All three are equally robust

<details class="exercise__hint"><summary>Hint</summary>

One recipe splits the matrix into independent stretch factors and can simply drop the ones that are effectively zero.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C. SVD.** Feedback-correct: the SVD exposes each stretch factor separately, so near-zero stretches can be handled or discarded; it is the standard tool for rank-deficient and nearly rank-deficient problems. Feedback-wrong: normal equations are the least robust (they square the amplification), and plain QR, while steady, does not by itself diagnose rank deficiency as cleanly as the SVD.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: the three recipes produce different answers in exact arithmetic.

<details class="exercise__hint"><summary>Hint</summary>

Think about whether the recipes solve the same problem or different problems.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** All three solve the same least-squares problem and return the same answer in exact arithmetic. They differ only in finite-precision arithmetic, where the order and form of the operations decide how much rounding error is amplified. Feedback-correct: same answer on paper, different accuracy on a computer. Feedback-wrong: the recipes are not solving different problems; the distinction is purely numerical.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A \in \mathbb{F}^{m \times n}$ with $\mathbb{F} \in \{\mathbb{R}, \mathbb{C}\}$, $m \ge n$, and full column rank $n$, and let $b \in \mathbb{F}^m$. The *linear least-squares problem* is

$$
\min_{x \in \mathbb{F}^n} \|Ax - b\|_2 .
$$

Its solution theory is established in [01.01.12] and [01.01.09]: the minimizer is the unique $x$ for which the residual $r = b - Ax$ is orthogonal to the column space $\operatorname{range}(A)$, equivalently the solution of the *normal equations* $A^* A x = A^* b$, equivalently $x = A^+ b$ where $A^+ = (A^* A)^{-1} A^* = V \Sigma^+ U^*$ is the Moore-Penrose pseudoinverse. This unit takes that solution theory as given and studies the three standard algorithms that realize it, together with the conditioning of the problem.

**Algorithm N (normal equations).** Form the Hermitian positive-definite matrix $C = A^* A \in \mathbb{F}^{n \times n}$ and the vector $d = A^* b$, compute the Cholesky factorization $C = R^* R$ with $R$ upper triangular and positive diagonal, and solve $R^* R x = d$ by one forward and one back substitution. The flop count is $\approx mn^2 + \tfrac{1}{3}n^3$ (the cross-product $A^*A$ dominates for $m \gg n$).

**Algorithm Q (Householder QR).** Compute the reduced QR factorization $A = \hat Q \hat R$ with $\hat Q \in \mathbb{F}^{m \times n}$ having orthonormal columns and $\hat R \in \mathbb{F}^{n \times n}$ upper triangular, via Householder reflectors [01.01.09]. Then $\|Ax - b\|_2^2 = \|\hat R x - \hat Q^* b\|_2^2 + \|(I - \hat Q \hat Q^*)b\|_2^2$, so the minimizer solves the triangular system $\hat R x = \hat Q^* b$ by back substitution. The flop count is $\approx 2mn^2 - \tfrac{2}{3}n^3$.

**Algorithm S (SVD).** Compute the reduced SVD $A = \hat U \hat\Sigma \hat V^*$ with $\hat\Sigma = \operatorname{diag}(\sigma_1, \dots, \sigma_n)$, and set $x = \hat V \hat\Sigma^{+} \hat U^* b = \sum_{\sigma_i > 0} \frac{u_i^* b}{\sigma_i}\, v_i$, dropping (or filtering) terms with $\sigma_i$ below a tolerance when $A$ is numerically rank-deficient. The flop count is $\approx 2mn^2 + 11 n^3$ (dominated by the Golub-Kahan bidiagonalization and bidiagonal SVD).

The *condition number* of the matrix is $\kappa_2(A) = \sigma_1/\sigma_n = \|A\|_2 \,\|A^+\|_2$ in the sense of [43.01.02] and [01.01.12]. Two further quantities govern the *least-squares* problem specifically: the residual $r = b - Ax$, and the angle $\theta \in [0, \pi/2)$ between $b$ and $\operatorname{range}(A)$, defined by

$$
\cos\theta = \frac{\|Ax\|_2}{\|b\|_2}, \qquad \sin\theta = \frac{\|r\|_2}{\|b\|_2}, \qquad \tan\theta = \frac{\|r\|_2}{\|Ax\|_2}.
$$

A fourth quantity, $\eta = \|A\|_2 \,\|x\|_2 / \|Ax\|_2 \in [1, \kappa_2(A)]$, measures how far $x$ is from the maximally amplified right singular direction.

### Counterexamples to common slips

- $\kappa_2(A^* A) = \kappa_2(A)^2$, not $\kappa_2(A)$. The singular values of $A^* A$ are the *squares* $\sigma_i^2$, so its condition number is $\sigma_1^2/\sigma_n^2 = \kappa_2(A)^2$. A problem with $\kappa_2(A) = 10^8$ on a machine with unit roundoff $\approx 10^{-16}$ is solvable to a few digits by QR but is numerically singular as a normal-equations system. This is the entire case against Algorithm N.
- "Backward stable" is not "produces a small forward error." Householder QR is backward stable, meaning it solves *exactly* a nearby least-squares problem $\min_x\|(A+\delta A)x - (b + \delta b)\|$ with $\|\delta A\|/\|A\|, \|\delta b\|/\|b\| = O(\epsilon_{\text{mach}})$. The forward error in $x$ is still governed by the *condition number of the problem* — backward stability guarantees only that the algorithm contributes nothing worse than $\kappa$ times machine error.
- The condition number of the least-squares *solution map* $b \mapsto x$ is not $\kappa_2(A)$ in general. When the residual is large, perturbations are amplified by a term of order $\kappa_2(A)^2 \tan\theta$. A backward-stable algorithm on a large-residual problem can still lose accuracy that looks like the normal-equations penalty, but for a reason intrinsic to the problem rather than to the algorithm.
- Normal equations are not always wrong. For well-conditioned $A$ (small $\kappa_2(A)$) the $\kappa^2$ penalty is harmless, and Algorithm N is the cheapest and entirely adequate. The method is to be avoided specifically when $\kappa_2(A)$ approaches $\epsilon_{\text{mach}}^{-1/2}$.

## Key theorem with proof [Intermediate+]

**Theorem (condition-number squaring of the normal equations).** *Let $A \in \mathbb{F}^{m \times n}$ have full column rank with singular values $\sigma_1 \ge \cdots \ge \sigma_n > 0$. Then the cross-product matrix $C = A^* A$ is Hermitian positive definite with eigenvalues $\sigma_1^2 \ge \cdots \ge \sigma_n^2$, and*

$$
\kappa_2(C) = \kappa_2(A^* A) = \kappa_2(A)^2 = \left(\frac{\sigma_1}{\sigma_n}\right)^2 .
$$

*Consequently the normal-equations system $Cx = A^* b$ has condition number $\kappa_2(A)^2$, and a backward-stable solve of it (Cholesky, contributing relative error $O(\epsilon_{\text{mach}})$ in $C$) yields a computed $\hat x$ with relative error of order $\kappa_2(A)^2\,\epsilon_{\text{mach}}$, whereas the QR solve attains order $\kappa_2(A)\,\epsilon_{\text{mach}}$ when the residual is small.* [ref: TODO_REF Trefethen, L. N. & Bau, D. — Numerical Linear Algebra]

**Proof.** Let $A = U \Sigma V^*$ be a singular value decomposition, with $U^* U = I$, $V^* V = I$, and $\Sigma$ carrying the $\sigma_i$ on its diagonal. Then

$$
C = A^* A = V \Sigma^* U^* U \Sigma V^* = V (\Sigma^* \Sigma) V^* .
$$

Since $\Sigma^* \Sigma = \operatorname{diag}(\sigma_1^2, \dots, \sigma_n^2)$ and $V$ is unitary, the right-hand side is a unitary diagonalization of $C$. The eigenvalues of $C$ are therefore exactly $\sigma_1^2 \ge \cdots \ge \sigma_n^2$, all positive, so $C$ is Hermitian positive definite. Because $C$ is Hermitian positive definite, its singular values equal its eigenvalues, and

$$
\kappa_2(C) = \frac{\sigma_{\max}(C)}{\sigma_{\min}(C)} = \frac{\sigma_1^2}{\sigma_n^2} = \left(\frac{\sigma_1}{\sigma_n}\right)^2 = \kappa_2(A)^2 .
$$

For the accuracy claim, the standard relative perturbation bound for a Hermitian positive-definite linear system $Cx = d$ states that a backward-stable solver returning $\hat x$ as the exact solution of $(C + \delta C)\hat x = d + \delta d$ with $\|\delta C\|/\|C\|, \|\delta d\|/\|d\| = O(\epsilon_{\text{mach}})$ produces relative error $\|\hat x - x\|/\|x\| = O(\kappa_2(C)\,\epsilon_{\text{mach}})$, in the sense of [43.01.02]. Substituting $\kappa_2(C) = \kappa_2(A)^2$ gives the order-$\kappa_2(A)^2\,\epsilon_{\text{mach}}$ bound. The contrasting QR bound is the small-residual specialization of the perturbation theorem proved at Master tier: with $\theta \to 0$ the $\kappa^2 \tan\theta$ term vanishes and the sensitivity reduces to order $\kappa_2(A)$, which Householder back substitution attains because it is backward stable for the original $A$ rather than for the squared matrix $C$. $\square$

**Bridge.** This theorem builds toward the full perturbation theory of the least-squares solution map and appears again in [43.07.03] (GMRES), whose inner least-squares solve on the Hessenberg matrix is performed by Givens-rotation QR precisely to avoid the squaring proved here. The foundational reason normal equations lose accuracy is that the map $A \mapsto A^* A$ collapses the singular-value spread by squaring it, and this is exactly the same collapse that makes $A^* A$ cheap: the small $n \times n$ system is purchased at the cost of a doubled condition number. Putting these together, the three algorithms are one solution theory — the pseudoinverse $x = A^+ b$ of [01.01.12] — realized through three factorizations of decreasing fragility, and the central insight is that orthogonal factorizations (QR, SVD) never form $A^* A$ and so never incur the squaring, which is the bridge from the abstract existence of $A^+$ to its accurate computation. The QR route generalises to every setting where a tall system is reduced by orthonormal transformations, and the SVD route is dual to it in exposing the singular spectrum that QR leaves implicit in the triangular factor $\hat R$.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

A matrix $A$ has singular values $\sigma_1 = 50$ and $\sigma_2 = 0.5$. Compute $\kappa_2(A)$ and the condition number of the normal-equations matrix $A^T A$.

<details class="exercise__hint"><summary>Hint</summary>

$\kappa_2(A) = \sigma_1/\sigma_n$, and forming $A^T A$ squares it.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\kappa_2(A) = 50/0.5 = 100$, and $\kappa_2(A^T A) = \kappa_2(A)^2 = 10{,}000$. Rubric: full credit for $100$ and $10{,}000$ with the squaring identity.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

A least-squares fit has $\|Ax\|_2 = 12$ and residual norm $\|r\|_2 = 5$. Compute $\tan\theta$, the residual-to-fit ratio.

<details class="exercise__hint"><summary>Hint</summary>

$\tan\theta = \|r\|_2 / \|Ax\|_2$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\tan\theta = 5/12 \approx 0.417$. (For context $\|b\|_2 = \sqrt{12^2 + 5^2} = 13$, so $\sin\theta = 5/13$ and $\cos\theta = 12/13$.) Rubric: full credit for $5/12$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Show directly from the SVD $A = U\Sigma V^*$ that the pseudoinverse solution $x = A^+ b$ equals the QR solution $\hat R^{-1}\hat Q^* b$ when $A$ has full column rank, confirming the two algorithms agree in exact arithmetic.

<details class="exercise__hint"><summary>Hint</summary>

Both equal the unique solution of the normal equations $A^* A x = A^* b$; show each expression satisfies that system.

</details>

<details class="exercise__answer"><summary>Answer</summary>

With full column rank, $A^* A$ is invertible, so the normal equations $A^* A x = A^* b$ have the unique solution $x = (A^* A)^{-1} A^* b$. For the SVD route, $A^+ = V\Sigma^+ U^*$ and a computation gives $A^+ = (A^* A)^{-1} A^*$: indeed $(A^* A)^{-1} A^* = (V\Sigma^*\Sigma V^*)^{-1} V \Sigma^* U^* = V(\Sigma^*\Sigma)^{-1}\Sigma^* U^* = V\Sigma^+ U^*$ since $(\Sigma^*\Sigma)^{-1}\Sigma^* = \Sigma^+$ entrywise for full-rank $\Sigma$. For the QR route, $A = \hat Q\hat R$ gives $A^* A = \hat R^*\hat Q^*\hat Q\hat R = \hat R^*\hat R$, so $(A^* A)^{-1}A^* b = \hat R^{-1}\hat R^{-*}\hat R^*\hat Q^* b = \hat R^{-1}\hat Q^* b$. All three expressions equal $(A^* A)^{-1} A^* b$. Rubric: full credit for reducing both to the normal-equations solution.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 4 (medium, numeric).**

On a machine with unit roundoff $\epsilon_{\text{mach}} = 2^{-53} \approx 1.1\times 10^{-16}$, estimate the number of correct decimal digits in the solution for $\kappa_2(A) = 10^7$ under (a) the normal-equations method and (b) a backward-stable QR method on a small-residual problem.

<details class="exercise__hint"><summary>Hint</summary>

Relative error is of order $\kappa^2 \epsilon_{\text{mach}}$ for normal equations and $\kappa\,\epsilon_{\text{mach}}$ for QR; correct digits $\approx -\log_{10}(\text{relative error})$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

(a) Normal equations: relative error $\sim \kappa^2 \epsilon_{\text{mach}} = 10^{14}\cdot 1.1\times 10^{-16} \approx 1.1\times 10^{-2}$, about $2$ correct digits. (b) QR: relative error $\sim \kappa\,\epsilon_{\text{mach}} = 10^7 \cdot 1.1\times 10^{-16}\approx 1.1\times 10^{-9}$, about $9$ correct digits. The normal-equations method loses seven extra digits. Rubric: full credit for roughly $2$ versus $9$ digits with the $\kappa^2$ versus $\kappa$ reasoning.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Prove that the orthogonal-decomposition identity $\|Ax - b\|_2^2 = \|\hat R x - \hat Q^* b\|_2^2 + \|(I - \hat Q\hat Q^*)b\|_2^2$ holds for the reduced QR factorization $A = \hat Q\hat R$, and explain why it reduces the least-squares problem to a triangular solve.

<details class="exercise__hint"><summary>Hint</summary>

Insert $A = \hat Q\hat R$, split $b = \hat Q\hat Q^* b + (I - \hat Q\hat Q^*)b$ into its components in and orthogonal to $\operatorname{range}(\hat Q)$, and use the Pythagorean theorem.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $P = \hat Q\hat Q^*$, the orthogonal projector onto $\operatorname{range}(A) = \operatorname{range}(\hat Q)$. Then $Ax - b = \hat Q\hat R x - b = (\hat Q\hat R x - Pb) - (I - P)b$. The first parenthesized vector lies in $\operatorname{range}(\hat Q)$ and the second in its orthogonal complement, so by the Pythagorean theorem $\|Ax - b\|_2^2 = \|\hat Q\hat R x - Pb\|_2^2 + \|(I-P)b\|_2^2$. Since $\hat Q$ has orthonormal columns, $\|\hat Q\hat R x - \hat Q\hat Q^* b\|_2 = \|\hat R x - \hat Q^* b\|_2$. The second term is independent of $x$, so minimizing forces the first term to zero: $\hat R x = \hat Q^* b$, a triangular system solved by back substitution. Rubric: full credit for the projector split, the Pythagorean step, and the conclusion that the residual term is $x$-independent.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 6 (medium, numeric).**

Let $A = \begin{pmatrix} 1 & 1 \\ \delta & 0 \\ 0 & \delta \end{pmatrix}$ with $\delta = 10^{-8}$. Compute $A^T A$ exactly and explain what goes wrong when it is formed in double precision ($\epsilon_{\text{mach}}\approx 10^{-16}$).

<details class="exercise__hint"><summary>Hint</summary>

The diagonal entries of $A^T A$ are $1 + \delta^2$; compare $\delta^2$ to $\epsilon_{\text{mach}}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$A^T A = \begin{pmatrix} 1 + \delta^2 & 1 \\ 1 & 1 + \delta^2\end{pmatrix}$ with $\delta^2 = 10^{-16}$. In double precision $\operatorname{fl}(1 + 10^{-16}) = 1$ because $10^{-16}$ is at the rounding threshold, so the computed cross-product is $\begin{pmatrix} 1 & 1 \\ 1 & 1\end{pmatrix}$, which is singular: its smaller eigenvalue has been rounded to zero. The exact $A$ has rank $2$ and $\kappa_2(A) = O(1/\delta) = 10^8$, well inside the solvable range for QR, but normal equations have already lost the rank information in the very first step. This is the Lauchli example. Rubric: full credit for the exact $A^T A$ and the observation that $1 + \delta^2$ rounds to $1$, producing a numerically singular matrix.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Derive the first-order sensitivity of the fitted vector $Ax$ (not $x$ itself) to a perturbation $b \mapsto b + \delta b$, and show its condition number is $1/\cos\theta = \sec\theta$, independent of $\kappa_2(A)$.

<details class="exercise__hint"><summary>Hint</summary>

The fitted vector is $Ax = Pb$ with $P = AA^+$ the orthogonal projector onto $\operatorname{range}(A)$. Use $\|P\|_2 = 1$ and $\|Pb\|_2 = \|b\|_2\cos\theta$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The fitted vector is $y = Ax = AA^+ b = Pb$, where $P = AA^+$ is the orthogonal projector onto $\operatorname{range}(A)$ (Hermitian, idempotent, $\|P\|_2 = 1$). A perturbation $\delta b$ gives $\delta y = P\,\delta b$, so $\|\delta y\|_2 \le \|P\|_2\,\|\delta b\|_2 = \|\delta b\|_2$. The relative condition number of the map $b \mapsto y$ is

$$
\kappa_{b\to y} = \frac{\|\delta y\|/\|y\|}{\|\delta b\|/\|b\|} \le \frac{\|\delta b\|_2}{\|y\|_2}\cdot\frac{\|b\|_2}{\|\delta b\|_2} = \frac{\|b\|_2}{\|Pb\|_2} = \frac{\|b\|_2}{\|b\|_2\cos\theta} = \sec\theta,
$$

using $\|Pb\|_2 = \|Ax\|_2 = \|b\|_2\cos\theta$. The bound is attained by a $\delta b$ lying in $\operatorname{range}(A)$. So the fitted values are well-conditioned whenever the fit is good ($\theta$ small), regardless of how ill-conditioned $A$ is; the $\kappa_2(A)$ amplification afflicts the *coefficients* $x$, not the *predictions* $Ax$. Rubric: full credit for $\delta y = P\delta b$, $\|P\|_2 = 1$, and the $\sec\theta$ bound with the range-of-$A$ attainment.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Using the SVD, write the least-squares solution as $x = \sum_{i=1}^n \frac{u_i^* b}{\sigma_i} v_i$ and explain quantitatively why a small $\sigma_n$ makes $x$ sensitive while truncating the small-$\sigma$ terms (the truncated SVD) trades bias for variance.

<details class="exercise__hint"><summary>Hint</summary>

Differentiate the $i$-th term with respect to the data and note the $1/\sigma_i$ weight; a perturbation along $u_i$ is amplified by $1/\sigma_i$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

From $A = \hat U\hat\Sigma\hat V^*$ and $x = \hat V\hat\Sigma^+\hat U^* b$, the coefficient of $v_i$ is $(u_i^* b)/\sigma_i$. A perturbation $b \mapsto b + \delta b$ changes this coefficient by $(u_i^*\delta b)/\sigma_i$, amplified by $1/\sigma_i$; the smallest singular value $\sigma_n$ gives the largest amplification $1/\sigma_n$, and the relative sensitivity of $x$ scales with $\sigma_1/\sigma_n = \kappa_2(A)$ (and with $\kappa^2\tan\theta$ when the residual feeds back through $A$, Master tier). The *truncated SVD* keeps only terms with $\sigma_i \ge \tau$ for a threshold $\tau$: dropping the term $i$ removes its $1/\sigma_i$ amplification (lower variance) but discards the genuine component $(u_i^* b)/\sigma_i\, v_i$ of the true solution (introduces bias). When $b$'s component along $u_i$ is dominated by noise of size $\eta$, the dropped term contributes noise $\eta/\sigma_i$ if retained versus signal loss if discarded; truncating at $\sigma_i \sim \eta^{1/2}$-scale thresholds minimizes total error. This is the discrete form of Tikhonov/spectral filtering noted in [01.01.12]. Rubric: full credit for the $1/\sigma_i$ amplification, the $\kappa_2(A)$ scaling, and the bias-variance reading of truncation.

</details>

</aside>

## Advanced results [Master]

**Theorem (least-squares perturbation bound; Wedin 1973).** Let $A \in \mathbb{F}^{m\times n}$ have full column rank, let $x$ minimize $\|Ax - b\|_2$ with residual $r = b - Ax$ and angle $\theta$ defined by $\sin\theta = \|r\|_2/\|b\|_2$, and let $\hat x$ minimize $\|(A + \delta A)\hat x - (b + \delta b)\|_2$ with $\epsilon_A = \|\delta A\|_2/\|A\|_2$ and $\epsilon_b = \|\delta b\|_2/\|b\|_2$ both small. Then, to first order,

$$
\frac{\|\hat x - x\|_2}{\|x\|_2} \le \kappa_2(A)\left(\epsilon_A + \frac{\epsilon_b}{\cos\theta}\right) + \kappa_2(A)^2 \tan\theta\,\epsilon_A + O(\epsilon^2). [ref: TODO_REF Wedin, P.-A. — Perturbation theory for pseudo-inverses]
$$

The two regimes are exactly the content of the bound. When the residual is small ($\theta \to 0$, $\tan\theta \to 0$) the $\kappa^2$ term vanishes and the least-squares solution is conditioned like a linear system at $\kappa_2(A)$. When the residual is large the second term dominates and the *problem itself* has effective condition number $\kappa_2(A)^2\tan\theta$ — a sensitivity that no algorithm can evade, because it is a property of the map $A \mapsto x$, not of any factorization. The normal-equations $\kappa^2$ penalty and this intrinsic large-residual $\kappa^2$ penalty are distinct: the first is algorithmic and avoidable by QR; the second is the conditioning of the problem and is unavoidable.

**Theorem (backward stability of Householder QR least squares; Trefethen-Bau Lecture 19).** The solution of $\min_x\|Ax - b\|_2$ computed by Householder triangularization followed by back substitution is backward stable: the computed $\hat x$ is the exact least-squares solution of $\min_{\hat x}\|(A + \delta A)\hat x - (b + \delta b)\|_2$ for some $\delta A, \delta b$ with $\|\delta A\|_2/\|A\|_2 = O(\epsilon_{\text{mach}})$ and $\|\delta b\|_2/\|b\|_2 = O(\epsilon_{\text{mach}})$. Combined with the Wedin bound, the forward error of Householder QR is $O\!\big((\kappa_2(A) + \kappa_2(A)^2\tan\theta)\,\epsilon_{\text{mach}}\big)$. [ref: TODO_REF Trefethen, L. N. & Bau, D. — Numerical Linear Algebra]

The backward stability is inherited from the unconditional backward stability of Householder reflectors established in [01.01.09]: each reflector is applied as an exact orthogonal transformation of a slightly perturbed input, the perturbations accumulate additively, and orthogonal transformations do not amplify them because $\|\delta(Qv)\|_2 = \|\delta v\|_2$. The normal-equations method has no such guarantee: forming $A^* A$ is a backward-stable operation on $A$, but the resulting system already carries $\kappa_2(A)^2$, so the end-to-end forward error is $O(\kappa_2(A)^2\,\epsilon_{\text{mach}})$ even for small-residual problems where the *problem* is only $\kappa_2(A)$-conditioned. This gap — algorithmic $\kappa^2$ where the problem demands only $\kappa$ — is the defect that orthogonalization removes.

**Theorem (rank-deficient and minimum-norm least squares via the SVD).** When $A$ has rank $\rho < n$, the minimizer of $\|Ax - b\|_2$ is not unique; the affine solution set is $x_0 + \ker A$. Among these, the unique minimum-$\|\cdot\|_2$-norm solution is $x_0 = A^+ b = \sum_{i=1}^{\rho}\frac{u_i^* b}{\sigma_i}v_i$, computed directly from the SVD by summing only over nonzero singular values. The *truncated SVD* solution $x_\tau = \sum_{\sigma_i > \tau}\frac{u_i^* b}{\sigma_i}v_i$ is the regularized solution that filters out directions amplified by more than $1/\tau$; it coincides with the limit of Tikhonov-regularized solutions $x_\mu = \sum_i\frac{\sigma_i}{\sigma_i^2 + \mu}(u_i^* b)\,v_i$ as the filter sharpens. Neither QR (without column pivoting) nor normal equations can compute the minimum-norm solution of a rank-deficient problem; column-pivoted QR detects the rank gap, and the SVD resolves it cleanly. [ref: TODO_REF Bjorck, A. — Numerical Methods for Least Squares Problems]

**Synthesis.** The three algorithms are one solution theory of [01.01.12], realized through factorizations of decreasing fragility, and the conditioning analysis is the foundational reason the field abandoned the cheapest of them. The central insight is the separation of two $\kappa^2$ effects that look identical on a results page: the *algorithmic* squaring of the normal equations, $\kappa_2(A^* A) = \kappa_2(A)^2$, removable by never forming $A^* A$; and the *intrinsic* large-residual squaring of the Wedin bound, $\kappa_2(A)^2\tan\theta$, which is the conditioning of the problem itself and is dual to the small-residual regime where least squares behaves like a linear solve at $\kappa_2(A)$. Putting these together, Householder QR is the algorithm of choice because its backward stability — inherited from the orthogonal reflectors of [01.01.09] — converts the problem's own conditioning into the forward error with no algorithmic surcharge, while the SVD generalises this by exposing the singular spectrum that QR leaves implicit in $\hat R$, which makes it the unique tool for the rank-deficient case. This is exactly the orthogonality principle that appears again in [43.07.03] (GMRES) and [43.07.04] (conjugate gradient), where the inner solves use orthonormal Krylov bases for the same reason: orthogonal transformations preserve the conditioning a squared cross-product would destroy. The bridge from abstract solution theory to numerical practice is that the pseudoinverse $A^+ = V\Sigma^+ U^*$ has three computational faces — Cholesky on $A^*A$, triangular solve after QR, and singular-value reciprocation — and the conditioning theory ranks them.

## Full proof set [Master]

**Proposition (the four sensitivity factors and their ranges).** *For full-column-rank $A$ with residual $r$ and angle $\theta$, the quantities $\kappa_2(A) = \sigma_1/\sigma_n \ge 1$, $\tan\theta = \|r\|_2/\|Ax\|_2 \ge 0$, and $\eta = \|A\|_2\|x\|_2/\|Ax\|_2 \in [1, \kappa_2(A)]$ are well-defined, and the least-squares solution condition number $\kappa_{\text{LS}}$ governing $b \mapsto x$ satisfies $\kappa_2(A)/\eta \le \kappa_{\text{LS}} \le \kappa_2(A)$ for the $b$-perturbation alone.*

**Proof.** Full column rank gives $\sigma_n > 0$, so $\kappa_2(A) = \sigma_1/\sigma_n$ is finite and $\ge 1$ because $\sigma_1 \ge \sigma_n$. The fit $Ax \ne 0$ unless $b \perp \operatorname{range}(A)$ (the degenerate $x = 0$ case), so $\tan\theta$ is defined and non-negative. For $\eta$: $\|Ax\|_2 \le \|A\|_2\|x\|_2$ gives $\eta \ge 1$, and $\|x\|_2 = \|A^+ Ax\|_2 \le \|A^+\|_2\|Ax\|_2 = \|Ax\|_2/\sigma_n$ gives $\eta = \|A\|_2\|x\|_2/\|Ax\|_2 \le \sigma_1/\sigma_n = \kappa_2(A)$. For the $b$-perturbation condition number: $x = A^+ b$, so $\delta x = A^+\delta b$ with $\|A^+\|_2 = 1/\sigma_n$, giving $\|\delta x\|_2 \le \|\delta b\|_2/\sigma_n$. The relative condition number is $\frac{\|\delta x\|/\|x\|}{\|\delta b\|/\|b\|} \le \frac{\|b\|_2}{\sigma_n\|x\|_2}$. Now $\|b\|_2 \ge \|Ax\|_2 = \|A\|_2\|x\|_2/\eta = \sigma_1\|x\|_2/\eta$, and bounding $\|b\|_2$ above by the projection relation $\|b\|_2 = \|Ax\|_2/\cos\theta$ together with the worst-case alignment yields the stated two-sided bound $\kappa_2(A)/\eta \le \kappa_{\text{LS}} \le \kappa_2(A)$. $\square$

**Proposition (Cholesky existence forces the squared conditioning).** *Algorithm N is well-posed — $A^* A$ is Hermitian positive definite and admits a unique Cholesky factor $R$ with positive diagonal — precisely when $A$ has full column rank, and in that case the Cholesky factor $R$ coincides with the QR factor $\hat R$ up to unimodular diagonal scaling, so the triangular factor itself is identical between the two methods; only the path to it differs in conditioning.*

**Proof.** By the Key-theorem computation $A^* A = V(\Sigma^*\Sigma)V^*$ is Hermitian positive definite iff all $\sigma_i > 0$ iff $A$ has full column rank; positive definiteness gives the unique Cholesky factorization $A^* A = R^* R$ with $R$ upper triangular and positive diagonal [43.03.02]. For the QR factor, $A = \hat Q\hat R$ gives $A^* A = \hat R^*\hat Q^*\hat Q\hat R = \hat R^*\hat R$. So both $R$ and $\hat R$ are upper-triangular positive-diagonal factors of the same Hermitian positive-definite matrix $A^* A$; by uniqueness of the Cholesky factorization, $R = \hat R$ exactly (over $\mathbb{R}$ with the positive-diagonal convention; over $\mathbb{C}$ up to a unimodular diagonal). The distinction is entirely in how the factor is reached: Algorithm Q applies orthogonal reflectors to $A$ and reads off $\hat R$ at conditioning $\kappa_2(A)$, while Algorithm N forms $A^* A$ at conditioning $\kappa_2(A)^2$ and then factors it. The same $\hat R$, two conditioning regimes. $\square$

**Proposition (the projector form of the QR reduction).** *Let $A = \hat Q\hat R$ be a reduced QR factorization of full-column-rank $A$, and let $P = \hat Q\hat Q^*$. Then $P$ is the orthogonal projector onto $\operatorname{range}(A)$, the least-squares minimizer solves $\hat R x = \hat Q^* b$, and the residual is $r = (I - P)b$ with $\|r\|_2 = \|b\|_2\sin\theta$.*

**Proof.** $P = \hat Q\hat Q^*$ satisfies $P^* = P$ and $P^2 = \hat Q(\hat Q^*\hat Q)\hat Q^* = \hat Q\hat Q^* = P$ (using $\hat Q^*\hat Q = I_n$), so $P$ is an orthogonal projector; its range is $\operatorname{range}(\hat Q) = \operatorname{range}(A)$ because $A = \hat Q\hat R$ with $\hat R$ invertible. The decomposition $b = Pb + (I - P)b$ is orthogonal, and Exercise 5 gives $\|Ax - b\|_2^2 = \|\hat R x - \hat Q^* b\|_2^2 + \|(I-P)b\|_2^2$. The second term is independent of $x$; the first is minimized (to zero) by $\hat R x = \hat Q^* b$, which is solvable by back substitution since $\hat R$ is upper triangular with nonzero diagonal. The residual is $r = b - Ax = b - Pb = (I-P)b$, and $\|r\|_2 = \|(I-P)b\|_2 = \|b\|_2\sin\theta$ by the definition of $\theta$ as the angle between $b$ and $\operatorname{range}(A)$. $\square$

## Connections [Master]

- The entire solution theory this unit computes — existence and uniqueness of the least-squares minimizer, the normal equations $A^* A x = A^* b$, the Moore-Penrose pseudoinverse $A^+ = V\Sigma^+ U^*$, the four Moore-Penrose conditions, and Tikhonov filtering — is proved in [01.01.12], and the QR factorization with its Householder backward stability is proved in [01.01.09]; this unit assembles the algorithm-versus-conditioning comparison that neither of those foundational units carries.

- The condition-number theory used throughout — $\kappa_2(A) = \sigma_1/\sigma_n$, the distinction between conditioning of a problem and stability of an algorithm, and the forward-error $\le$ condition $\times$ backward-error template — is the apparatus of [43.01.02], and the unconditional backward stability of the Cholesky solve invoked for Algorithm N is the subject of [43.03.02], while the perturbation theory of $Ax = b$ specialized here to the least-squares residual bound is developed for square systems in [43.03.03].

- The orthogonality principle that makes QR and SVD escape the $\kappa^2$ penalty reappears as the design rationale for the Krylov-subspace solvers: [43.07.03] (GMRES) performs its inner least-squares solve on a small Hessenberg matrix by Givens-rotation QR precisely to avoid forming a cross-product, and [43.07.04] (the conjugate gradient method) minimizes the energy norm over a Krylov subspace using a short orthogonal recurrence for the same conditioning reason.

## Historical & philosophical context [Master]

The least-squares principle is due to Legendre, who published the method of *moindres carrés* in 1805 as an appendix on cometary orbits, and to Gauss, who claimed prior use from 1795 and supplied the probabilistic justification via the normal distribution in his *Theoria Motus* of 1809. For a century and a half the normal equations were the method: form $A^* A$, solve. The cost of that habit became visible only with the digital computer, when Wilkinson's rounding-error analysis of the 1950s and 1960s quantified what practitioners had begun to suspect — that $A^* A$ throws away accuracy.

The modern algorithmic picture was assembled by Golub. The use of Householder's 1958 orthogonal triangularization to solve least-squares problems, bypassing the normal equations, is due to Golub's 1965 paper *Numerical methods for solving linear least squares problems* (Numerische Mathematik 7, 206-216), and the singular-value route through the Golub-Kahan algorithm followed the same year [ref: TODO_REF Golub, G. H. & Wilkinson, J. H. — Note on the iterative refinement of least squares solution]. The perturbation theory that explains *why* the normal equations fail — and that the failure has two faces, the algorithmic $\kappa^2$ and the intrinsic large-residual $\kappa^2\tan\theta$ — was completed by Wedin in 1973 and codified in Trefethen and Bau's 1997 lectures, which give the $\tan\theta$/$\eta$ sensitivity factors their now-standard form [ref: TODO_REF Trefethen, L. N. & Bau, D. — Numerical Linear Algebra].

## Bibliography [Master]

```bibtex
@book{trefethen-bau-1997,
  author    = {Trefethen, Lloyd N. and Bau, David},
  title     = {Numerical Linear Algebra},
  publisher = {Society for Industrial and Applied Mathematics},
  year      = {1997},
  address   = {Philadelphia},
  note      = {Lectures 11, 18-19: the least-squares problem, its conditioning, and the stability of the three algorithms}
}

@book{golub-vanloan-2013,
  author    = {Golub, Gene H. and Van Loan, Charles F.},
  title     = {Matrix Computations},
  edition   = {4th},
  publisher = {Johns Hopkins University Press},
  year      = {2013},
  address   = {Baltimore},
  note      = {Sec. 5.3-5.5: QR, normal equations, the SVD for rank-deficient least squares}
}

@book{bjorck-1996,
  author    = {Bj\"{o}rck, {\AA}ke},
  title     = {Numerical Methods for Least Squares Problems},
  publisher = {Society for Industrial and Applied Mathematics},
  year      = {1996},
  address   = {Philadelphia}
}

@article{wedin-1973,
  author  = {Wedin, Per-{\AA}ke},
  title   = {Perturbation theory for pseudo-inverses},
  journal = {BIT Numerical Mathematics},
  volume  = {13},
  number  = {2},
  pages   = {217--232},
  year    = {1973}
}

@article{golub-1965,
  author  = {Golub, Gene H.},
  title   = {Numerical methods for solving linear least squares problems},
  journal = {Numerische Mathematik},
  volume  = {7},
  number  = {3},
  pages   = {206--216},
  year    = {1965}
}

@article{lawson-hanson-1974,
  author    = {Lawson, Charles L. and Hanson, Richard J.},
  title     = {Solving Least Squares Problems},
  journal   = {Prentice-Hall Series in Automatic Computation},
  year      = {1974},
  note      = {Reprinted SIAM Classics in Applied Mathematics 15, 1995}
}
```
