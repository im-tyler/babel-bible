---
id: 43.07.02
title: "Arnoldi and Lanczos iterations"
slug: arnoldi-lanczos-iterations
section: numerical-analysis
chapter: 07-iterative-krylov-methods
concept_catalog_id: numerical-analysis.arnoldi-lanczos-iterations
prerequisites:
  - 01.01.08  # eigenvalue / eigenvector — Ritz values
  - 01.01.09  # modified Gram-Schmidt / Householder / QR
  - 43.06.02  # Hessenberg/tridiagonal reduction — the dense analogue
successors: []
tier_anchors:
  beginner: "Building the best small matrix that mimics a giant one, by repeatedly multiplying a single starting vector and tidying the results — Strang 2016 *Introduction to Linear Algebra* 5e (Wellesley-Cambridge) Ch. 6 (eigenvalues and powers of a matrix); Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lecture 33 (overview of the Arnoldi idea)"
  intermediate: "Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lectures 33-34 (Arnoldi iteration and the Arnoldi/Lanczos approximation of eigenvalues — the modified-Gram-Schmidt construction and the Hessenberg relation $AQ_m = Q_{m+1}\\tilde H_m$); Golub-Van Loan 2013 *Matrix Computations* 4e (Johns Hopkins) §10.5 (the Lanczos and Arnoldi processes)"
  master: "Saad 2011 *Numerical Methods for Large Eigenvalue Problems* 2e (SIAM) Ch. 6 (the Arnoldi method, Ritz values, and convergence) and Ch. 4 (the Lanczos algorithm in exact and finite precision); Parlett 1998 *The Symmetric Eigenvalue Problem* (SIAM Classics) Ch. 13 (the Lanczos algorithm, Paige's loss-of-orthogonality theory, and reorthogonalisation); Trefethen-Bau 1997 *Numerical Linear Algebra* (SIAM) Lectures 33-36"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Trefethen, L. N. & Bau, D. — Numerical Linear Algebra (SIAM, 1997)"
    locator: "Lectures 33-34: the Krylov subspace K_m = span{b, Ab, ..., A^{m-1}b}, the Arnoldi iteration as modified Gram-Schmidt orthogonalisation of the Krylov sequence, the Hessenberg relation A Q_m = Q_{m+1} H-tilde_m and the projected H_m = Q_m^* A Q_m, and the Arnoldi/Rayleigh-Ritz approximation of eigenvalues (Ritz values as eigenvalues of H_m)."
  - source: TODO_REF
    path: "Saad, Y. — Numerical Methods for Large Eigenvalue Problems (2nd ed.)"
    locator: "SIAM, 2011. Ch. 6: the Arnoldi process, the orthogonal projection of A onto the Krylov subspace, Ritz values and Ritz vectors, the residual bound ||A y_i - theta_i y_i|| = |h_{m+1,m}| |e_m^* s_i| for a Ritz pair, and the convergence of extremal Ritz values; Ch. 4: the symmetric Lanczos algorithm, the three-term recurrence, the tridiagonal T_m, and its finite-precision behaviour."
  - source: TODO_REF
    path: "Parlett, B. N. — The Symmetric Eigenvalue Problem (SIAM Classics, 1998)"
    locator: "Ch. 13: the Lanczos algorithm, the three-term recurrence beta_{j+1} q_{j+1} = A q_j - alpha_j q_j - beta_j q_{j-1}, Paige's theorem that loss of orthogonality coincides with convergence of a Ritz value, the appearance of spurious 'ghost' eigenvalues, and the full / selective / partial reorthogonalisation remedies."
  - source: TODO_REF
    path: "Golub, G. H. & Van Loan, C. F. — Matrix Computations (4th ed.)"
    locator: "Johns Hopkins University Press, 2013. §10.1-10.5: Krylov subspace properties, the Arnoldi factorisation A Q_m = Q_m H_m + h_{m+1,m} q_{m+1} e_m^*, the symmetric Lanczos tridiagonalisation, the cost and storage of the two processes, and their role as the inner engine of GMRES and conjugate gradients."
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the static pieces this unit stands on — finite-dimensional
  inner-product spaces and orthonormality (`OrthonormalBasis`, the
  Gram-Schmidt process `gramSchmidt`), the spectrum and eigenvalues of a
  matrix or finite-dimensional operator, and the Hermitian/self-adjoint
  predicate — but it has no Krylov-subspace theory and no Arnoldi or Lanczos
  process. There is no definition of the Krylov subspace
  `K_m(A, b) = span {b, A•b, ..., A^(m-1)•b}` as a submodule, no theorem that
  for a nonsingular Krylov sequence its dimension grows by one at each step
  until the invariant-subspace breakdown, and no construction of the Arnoldi
  factorisation `A Q_m = Q_m H_m + h_{m+1,m} q_{m+1} eᵀ_m` producing an
  orthonormal basis `Q_m` and an unreduced upper-Hessenberg `H_m = Q_mᴴ A Q_m`.
  The Hermitian specialisation is equally absent: no lemma that the projected
  matrix becomes tridiagonal when `A` is self-adjoint, no formalisation of the
  three-term recurrence `β_{j+1} q_{j+1} = A q_j - α_j q_j - β_j q_{j-1}`, and
  no Ritz-value / Rayleigh-Ritz approximation theory with the residual identity
  `‖A y - θ y‖ = |h_{m+1,m}| |eᵀ_m s|`. The finite-precision phenomena — Paige's
  theorem tying loss of orthogonality to Ritz-value convergence, and the
  reorthogonalisation remedies — are outside Mathlib's analysis-of-algorithms
  reach entirely. A `Codex.NumericalAnalysis.Arnoldi` module would define the
  Krylov submodule, build the Arnoldi factorisation from `gramSchmidt`, prove
  the Hessenberg/tridiagonal projection and the exactness at breakdown, and
  state the Ritz residual bound; the named human reviewer is the correctness
  gate for the convergence and finite-precision claims, which rest on
  perturbation arguments Mathlib does not yet package for this setting.
human_reviewer: "Tyler (pending external numerical-linear-algebra reviewer)"
source_books: [trefethen-bau]
source_curriculum: numerical-analysis
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 88m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Suppose you have a matrix so enormous that you can never write it out — millions of rows — but you are allowed one cheap operation: hand it a vector, and it hands back the matrix times that vector. From this single power you want to learn the matrix's most important features: its largest eigenvalues, or the solution of a system it defines. The trick of this unit is to build, out of repeated uses of that one operation, a tiny matrix that behaves like the giant one on the part of space that matters.

Start with one vector. Multiply by the matrix to get a second; multiply again for a third; and so on. These vectors, the starting one and its successive products, span a growing space that captures more and more of how the matrix acts. That growing space is the Krylov space, named for the engineer who first used it.

There is a problem: the raw products quickly pile up almost on top of one another, all leaning toward the dominant direction, so they make a numerically useless basis. The fix is to tidy them as you go. Each time you form a new product, you strip off the parts already pointing along the earlier directions, leaving only the genuinely new direction, and you rescale it to unit length. This tidying is the Arnoldi iteration, and the leftover bookkeeping — how much of each old direction you stripped off — assembles into a small, almost-triangular matrix.

When the big matrix is symmetric, the tidying simplifies dramatically: almost every stripping coefficient turns out to be zero, so each new vector only needs to be cleaned against the previous two. That short three-term recipe is the Lanczos iteration, and it is one of the most efficient ideas in all of computation.

## Visual [Beginner]

The picture is a single starting vector being fed through the matrix again and again, with each fresh output tidied against everything built so far, so that a neat orthogonal frame grows one column at a time alongside a small bookkeeping matrix.

Read the table top to bottom. Each row adds one new direction. The left column is the raw product the matrix hands back; the middle column is that product after the old directions are stripped away and it is rescaled to length one; the right column notes the shape of the small matrix recording the strippings.

| step | raw product | tidied new direction | small matrix so far |
|---|---|---|---|
| 1 | start vector $b$ | $q_1$ (just rescale $b$) | $1 \times 1$ |
| 2 | $A q_1$ | $q_2$ (strip off $q_1$) | $2 \times 1$ Hessenberg |
| 3 | $A q_2$ | $q_3$ (strip off $q_1, q_2$) | $3 \times 2$ Hessenberg |
| 4 | $A q_3$ | $q_4$ (strip off $q_1, q_2, q_3$) | $4 \times 3$ Hessenberg |

![A two-panel diagram. The left panel shows a starting arrow labelled b, then a fan of arrows produced by repeatedly applying the matrix A; the raw arrows crowd together toward one dominant direction, illustrating why the raw Krylov vectors make a poor basis. A second set of arrows, drawn at right angles to one another, shows the tidied orthonormal vectors q1, q2, q3, q4 that the Arnoldi process extracts, each pointing in a genuinely new direction. The right panel shows two small grids: a staircase-shaped almost-triangular grid labelled Hessenberg H for the general case, and a thin three-stripe grid labelled tridiagonal T for the symmetric Lanczos case, with a caption noting that the symmetric case needs only the previous two vectors at each step.](/img/arnoldi-lanczos-iterations-placeholder.svg)

The takeaway: from one vector and the ability to multiply by the matrix, you grow an orthogonal frame and a small matrix. The eigenvalues of that small matrix are excellent estimates of the big matrix's extreme eigenvalues, and the symmetric case is cheaper because the frame only needs short-term memory.

## Worked example [Beginner]

Take the symmetric matrix and starting vector
$$
A = \begin{pmatrix} 2 & 1 & 0 \\ 1 & 2 & 1 \\ 0 & 1 & 2 \end{pmatrix}, \qquad b = \begin{pmatrix} 1 \\ 0 \\ 0 \end{pmatrix}.
$$
We run two steps of the symmetric (Lanczos) tidying by hand.

Step 1. The first direction is just $b$ rescaled. Since $b$ already has length one, $q_1 = (1, 0, 0)$.

Step 2. Multiply: $A q_1 = (2, 1, 0)$. The amount of this lying along $q_1$ is the dot product $q_1 \cdot A q_1 = 2$; call it $\alpha_1 = 2$. Strip it off: $(2, 1, 0) - 2(1, 0, 0) = (0, 1, 0)$. This leftover has length $1$, so $\beta_2 = 1$ and the new direction is $q_2 = (0, 1, 0)$.

Step 3. Multiply: $A q_2 = (1, 2, 1)$. The amount along $q_2$ is $\alpha_2 = q_2 \cdot A q_2 = 2$. The amount along the previous $q_1$ is exactly the $\beta_2 = 1$ we already recorded. Strip both off: $(1, 2, 1) - 2(0,1,0) - 1(1,0,0) = (0, 0, 1)$, of length $1$, so $q_3 = (0, 0, 1)$.

The small matrix assembled from these numbers is the tridiagonal
$$
T = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix},
$$
using $\alpha_1, \alpha_2$ on the diagonal and $\beta_2$ on the off-diagonals. Its eigenvalues are $2 \pm 1 = 1$ and $3$.

What this tells us: after only two steps the little matrix already gives two of the three exact eigenvalues of $A$ (the full spectrum is $2, 2 \pm \sqrt 2$, so $1$ and $3$ bracket the true extremes closely). Each step needed only one matrix-times-vector and a strip against the previous two directions — the short-memory pattern that makes the symmetric case so cheap.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

The Krylov space built from a starting vector $b$ and a matrix $A$ is spanned by which vectors?

A. $b$ and the columns of $A$.
B. $b, Ab, A^2b, \dots$ — the starting vector and its repeated products by $A$.
C. The eigenvectors of $A$.
D. The rows of $A$ times $b$.

<details class="exercise__hint"><summary>Hint</summary>

The only operation we are allowed is multiplying a vector by $A$, applied over and over starting from $b$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: correct; the Krylov space is the span of $b$ and its successive images $Ab, A^2b, \dots$, all reachable from $b$ by repeated multiplication. Feedback-wrong: we never need the columns or rows of $A$ on their own (A, D), and we do not know the eigenvectors in advance — finding their directions is part of what the method discovers (C).

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true / false).**

For a symmetric matrix, the Lanczos iteration needs to clean each new vector only against the previous two vectors, rather than against all earlier ones.

<details class="exercise__hint"><summary>Hint</summary>

Think about the worked example: when stripping the new product, how many earlier directions actually had a nonzero amount to remove?

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Feedback-correct: correct; for a symmetric matrix the stripping coefficients against all but the previous two directions vanish, giving the short three-term recurrence that defines Lanczos. Feedback-wrong: the general (non-symmetric) Arnoldi iteration must strip against all earlier vectors; it is precisely symmetry that collapses this to a two-vector memory.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

In the worked example the small tridiagonal matrix is $\begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$. What is its largest eigenvalue?

<details class="exercise__hint"><summary>Hint</summary>

For a $2 \times 2$ matrix with equal diagonal $d$ and off-diagonal $e$, the eigenvalues are $d + e$ and $d - e$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$3$.** Feedback-correct: correct; with diagonal $2$ and off-diagonal $1$ the eigenvalues are $2 + 1 = 3$ and $2 - 1 = 1$, so the largest is $3$. Feedback-wrong: if you got $2$ you used only the diagonal; the off-diagonal entry shifts the eigenvalues up and down by $1$.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A \in \mathbb{F}^{n \times n}$ with $\mathbb{F} \in \{\mathbb{R}, \mathbb{C}\}$, and let $b \in \mathbb{F}^n$ be nonzero. For $m \ge 1$ the *$m$-th Krylov subspace* of $A$ generated by $b$ is
$$
\mathcal{K}_m(A, b) = \operatorname{span}\{b, Ab, A^2 b, \dots, A^{m-1}b\} = \{\, p(A)\,b : p \text{ a polynomial of degree} < m \,\}.
$$
The two descriptions agree because $A^j b = (x^j)(A)\,b$. The dimensions are nondecreasing, $\dim\mathcal{K}_m \le \dim\mathcal{K}_{m+1}$, and they strictly increase by one at each step until the first index $m$ at which $\mathcal{K}_m = \mathcal{K}_{m+1}$; at that point $\mathcal{K}_m$ is an $A$-invariant subspace and the sequence stalls. The least such $m$ is the *grade* of $b$ with respect to $A$ [ref: TODO_REF Trefethen, L. N. & Bau, D. — Numerical Linear Algebra (SIAM, 1997)].

**The Arnoldi iteration.** The Arnoldi process orthonormalises the Krylov sequence by modified Gram-Schmidt, in the sense of [01.01.09]. Set $q_1 = b / \|b\|$. For $j = 1, 2, \dots$ form $w = A q_j$, orthogonalise it against the existing basis by recording
$$
h_{ij} = q_i^* w \quad (1 \le i \le j), \qquad w \leftarrow w - \sum_{i=1}^{j} h_{ij}\, q_i,
$$
then set $h_{j+1,j} = \|w\|$ and, if $h_{j+1,j} \ne 0$, $q_{j+1} = w / h_{j+1,j}$. Collecting the orthonormal vectors as the columns of $Q_m = [\,q_1\ \cdots\ q_m\,]$ and the coefficients into an $(m+1)\times m$ upper-Hessenberg matrix $\tilde H_m = (h_{ij})$, the construction satisfies the *Arnoldi relation*
$$
A Q_m = Q_{m+1}\tilde H_m = Q_m H_m + h_{m+1,m}\, q_{m+1} e_m^*,
$$
where $H_m = Q_m^* A Q_m$ is the leading $m \times m$ block of $\tilde H_m$, an upper-Hessenberg matrix as in [43.06.02]. The columns of $Q_m$ form an orthonormal basis of $\mathcal{K}_m(A,b)$, and $H_m$ is the matrix of the orthogonal projection of $A$ onto $\mathcal{K}_m$ expressed in that basis. *Breakdown* — $h_{m+1,m} = 0$ — occurs exactly when $\mathcal{K}_m$ is $A$-invariant, in which case the eigenvalues of $H_m$ are exact eigenvalues of $A$.

**The Lanczos iteration.** When $A = A^*$ is Hermitian, $H_m = Q_m^* A Q_m$ is both upper-Hessenberg and Hermitian, hence tridiagonal; write it $T_m$ with real diagonal $\alpha_j = q_j^* A q_j$ and positive off-diagonal $\beta_{j+1} = h_{j+1,j}$. The orthogonalisation then collapses to a *three-term recurrence*
$$
\beta_{j+1}\, q_{j+1} = A q_j - \alpha_j\, q_j - \beta_j\, q_{j-1}, \qquad \alpha_j = q_j^* A q_j, \quad \beta_{j+1} = \|A q_j - \alpha_j q_j - \beta_j q_{j-1}\|,
$$
with $q_0 = 0$, $\beta_1 = 0$. Only the two previous vectors $q_j, q_{j-1}$ enter, so the Lanczos process needs $O(n)$ storage and one matrix-vector product per step, against the $O(mn)$ storage and growing orthogonalisation cost of Arnoldi.

**Ritz values and Ritz vectors.** Let $(\theta_i, s_i)$ be an eigenpair of $H_m$ (or $T_m$): $H_m s_i = \theta_i s_i$. The scalar $\theta_i$ is a *Ritz value* and $y_i = Q_m s_i$ a *Ritz vector*; together they are the *Rayleigh-Ritz* approximation of an eigenpair of $A$ from the subspace $\mathcal{K}_m$. The Ritz residual measures their quality:
$$
\|A y_i - \theta_i y_i\| = |h_{m+1,m}|\,|e_m^* s_i|,
$$
so a Ritz pair is accurate precisely when the last entry of its eigenvector $s_i$ is small or the subdiagonal $h_{m+1,m}$ is small.

### Counterexamples to common slips

- *The Krylov dimension need not reach $n$.* If $b$ lies in a proper $A$-invariant subspace — for instance if $b$ is a single eigenvector — the sequence stalls early at the grade of $b$, and the Arnoldi process breaks down (a *lucky breakdown*) having captured an invariant subspace exactly. Breakdown is a feature, not a failure: it signals exact invariance.
- *Arnoldi's $H_m$ is Hessenberg, not triangular.* The projection $Q_m^* A Q_m$ has one nonzero subdiagonal because $A q_j$ can have a component along the genuinely new direction $q_{j+1}$ but none beyond it; expecting a triangular projection confuses the Krylov structure with a Schur form.
- *The three-term recurrence is exact only for Hermitian $A$.* For a non-Hermitian matrix the coefficients $h_{ij}$ with $i < j-1$ do not vanish, so a short recurrence does not hold; the two-sided (nonsymmetric) Lanczos process recovers a three-term recurrence only by sacrificing orthogonality, building bi-orthogonal bases instead.
- *Ritz values converge to the extremes first, not the interior.* The Rayleigh-Ritz approximation captures the outlying eigenvalues of $A$ — the largest and smallest — long before the clustered interior ones; reading interior eigenvalues off a small $m$ is unreliable without a spectral transformation.

## Key theorem with proof [Intermediate+]

The signature result is the Arnoldi relation itself: that modified Gram-Schmidt on the Krylov sequence produces an orthonormal basis together with a Hessenberg projection, and that this projection becomes tridiagonal exactly when $A$ is Hermitian.

**Theorem (the Arnoldi factorisation and its Lanczos specialisation).** *Let $A \in \mathbb{F}^{n\times n}$, $b \ne 0$, and run the Arnoldi process. As long as no breakdown has occurred ($h_{j+1,j} \ne 0$ for $j < m$), the vectors $q_1, \dots, q_m$ are orthonormal and span $\mathcal{K}_m(A,b)$, and*
$$
A Q_m = Q_m H_m + h_{m+1,m}\, q_{m+1}\, e_m^*, \qquad H_m = Q_m^* A Q_m,
$$
*with $H_m$ unreduced upper-Hessenberg. If $A = A^*$ then $H_m = T_m$ is tridiagonal, and the recurrence reduces to the three-term form $\beta_{j+1}q_{j+1} = Aq_j - \alpha_j q_j - \beta_j q_{j-1}$* [ref: TODO_REF Trefethen, L. N. & Bau, D. — Numerical Linear Algebra (SIAM, 1997)].

**Proof.** Orthonormality and the span claim go together by induction on $m$. For $m = 1$, $q_1 = b/\|b\|$ is a unit vector spanning $\mathcal{K}_1 = \operatorname{span}\{b\}$. Assume $q_1, \dots, q_m$ are orthonormal and span $\mathcal{K}_m$. The new vector is $w = A q_m - \sum_{i=1}^m h_{im}q_i$ with $h_{im} = q_i^* A q_m$, so by construction $q_i^* w = q_i^* A q_m - h_{im} = 0$ for each $i \le m$; thus $w \perp \operatorname{span}\{q_1,\dots,q_m\}$, and $q_{m+1} = w/\|w\|$ (when $\|w\| = h_{m+1,m} \ne 0$) is a unit vector orthogonal to all earlier ones. For the span, $A q_m \in A\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$, and since $\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$ the corrected $w$ lies in $\mathcal{K}_{m+1}$; conversely $A^m b = A(A^{m-1}b)$ and $A^{m-1}b \in \mathcal{K}_m = \operatorname{span}\{q_1,\dots,q_m\}$, so $\mathcal{K}_{m+1} = \mathcal{K}_m + \operatorname{span}\{q_{m+1}\}$. Hence $q_1, \dots, q_{m+1}$ are orthonormal and span $\mathcal{K}_{m+1}$.

The factorisation is the matrix form of the recurrence. The defining identity $A q_j = \sum_{i=1}^{j+1} h_{ij}q_i$ (rearranged from $w = Aq_j - \sum_{i\le j}h_{ij}q_i$ and $q_{j+1} = w/h_{j+1,j}$) says column $j$ of $AQ_m$ equals column $j$ of $Q_{m+1}\tilde H_m$. Stacking columns $j = 1, \dots, m$ gives $AQ_m = Q_{m+1}\tilde H_m$. Splitting off the last row of $\tilde H_m$, whose only nonzero entry is $h_{m+1,m}$ in column $m$, yields $A Q_m = Q_m H_m + h_{m+1,m}q_{m+1}e_m^*$. Left-multiplying by $Q_m^*$ and using $Q_m^* Q_m = I$, $Q_m^* q_{m+1} = 0$ gives $Q_m^* A Q_m = H_m$. The matrix $H_m$ is upper-Hessenberg because $h_{ij} = q_i^* A q_j = 0$ whenever $i > j+1$: indeed $A q_j \in \mathcal{K}_{j+1} = \operatorname{span}\{q_1,\dots,q_{j+1}\}$, so it is orthogonal to $q_i$ for $i > j+1$. It is unreduced before breakdown because each $h_{j+1,j} = \|w\| > 0$.

When $A = A^*$, $H_m = Q_m^* A Q_m$ satisfies $H_m^* = Q_m^* A^* Q_m = Q_m^* A Q_m = H_m$, so $H_m$ is Hermitian as well as upper-Hessenberg; a Hermitian upper-Hessenberg matrix has $h_{ij} = 0$ for $i > j+1$ and, by conjugate symmetry, $h_{ji} = \overline{h_{ij}} = 0$ for $j > i+1$, leaving only the diagonal and the two adjacent stripes — tridiagonal. Then $h_{ij} = q_i^* A q_j = 0$ for $i < j - 1$ too, so the orthogonalisation sum $\sum_{i\le j}h_{ij}q_i$ retains only the $i = j$ and $i = j-1$ terms, which is the three-term recurrence with $\alpha_j = h_{jj}$ and $\beta_j = h_{j,j-1}$. $\square$

**Bridge.** This factorisation is the foundational reason a single matrix-vector multiply suffices to probe a giant operator: it builds toward the Krylov solvers of the chapter by exposing, at step $m$, an orthonormal basis $Q_m$ and a small projected matrix $H_m$ whose eigenvalues are the Ritz approximations of the Advanced results. This is exactly the dense Hessenberg reduction of [43.06.02] read iteratively — there a finite sequence of Householder similarities produces $Q^*AQ$ Hessenberg on the whole space, here a growing sequence of Gram-Schmidt steps produces $Q_m^*AQ_m$ Hessenberg on a Krylov slice — and the tridiagonal Lanczos case generalises the symmetric tridiagonalisation to the Krylov setting with the same conjugate-symmetry collapse. The relation appears again in [43.07.03], where GMRES minimises the residual over $\mathcal{K}_m$ by solving a small least-squares problem on $\tilde H_m$, and in [43.07.04], where conjugate gradients is exactly Lanczos for symmetric positive-definite $A$ with the tridiagonal $T_m$ factored on the fly. Putting these together, the central insight is that the Arnoldi factorisation compresses the action of $A$ on a Krylov subspace into a Hessenberg matrix one tractable size smaller at each step, and the bridge is that every Krylov algorithm — for eigenvalues, for linear systems, symmetric or not — is a way of reading information off this one compression.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

For $A = \begin{pmatrix} 3 & 0 \\ 0 & 1 \end{pmatrix}$ and $b = \begin{pmatrix} 1 \\ 1 \end{pmatrix}$, what is the dimension of the Krylov subspace $\mathcal{K}_2(A,b)$?

<details class="exercise__hint"><summary>Hint</summary>

Compute $b$ and $Ab$ and check whether they are linearly independent.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2$.** Here $b = (1,1)$ and $Ab = (3,1)$. These two vectors are not scalar multiples of one another, so they are linearly independent and $\dim\mathcal{K}_2 = 2$. Since $A$ is $2\times 2$, the grade of $b$ is $2$ and the Krylov sequence already spans all of $\mathbb{F}^2$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

In the worked example the Lanczos tridiagonal was $T_2 = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$. Its smaller eigenvalue is a Ritz value approximating the smallest eigenvalue of $A$ (whose exact value is $2 - \sqrt 2 \approx 0.586$). Report the Ritz value and its absolute error to three decimals.

<details class="exercise__hint"><summary>Hint</summary>

The eigenvalues of $T_2$ are $2 \pm 1$. Subtract the true smallest eigenvalue $2 - \sqrt 2$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The smaller Ritz value is $2 - 1 = 1$. The true smallest eigenvalue is $2 - \sqrt 2 \approx 0.586$, so the absolute error is $1 - 0.586 = \boxed{0.414}$. The Ritz value brackets the true extreme from above at this early step; a third Lanczos step would land it exactly on $2 - \sqrt 2$, since the grade of $b$ here is $3$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Show that $H_m = Q_m^* A Q_m$ is upper-Hessenberg directly from the Krylov structure: prove $q_i^* A q_j = 0$ whenever $i > j + 1$.

<details class="exercise__hint"><summary>Hint</summary>

Use that $A q_j \in A\mathcal{K}_j \subseteq \mathcal{K}_{j+1} = \operatorname{span}\{q_1,\dots,q_{j+1}\}$, and that the $q_i$ are orthonormal.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Since $q_j \in \mathcal{K}_j = \operatorname{span}\{q_1,\dots,q_j\}$, we have $q_j = p(A)b$ for a polynomial $p$ of degree $< j$, so $A q_j = (xp)(A)b$ has degree $< j+1$ and lies in $\mathcal{K}_{j+1} = \operatorname{span}\{q_1,\dots,q_{j+1}\}$. Therefore $A q_j$ is a linear combination of $q_1, \dots, q_{j+1}$ only. Taking the inner product with $q_i$ for $i > j+1$ and using orthonormality $q_i^* q_k = \delta_{ik}$ gives $q_i^* A q_j = 0$. Hence the $(i,j)$ entry of $H_m$ vanishes whenever $i > j+1$, which is exactly the upper-Hessenberg condition.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Derive the Ritz residual identity $\|A y - \theta y\| = |h_{m+1,m}|\,|e_m^* s|$ for a Ritz pair $(\theta, y)$ with $y = Q_m s$, $H_m s = \theta s$, $\|s\| = 1$.

<details class="exercise__hint"><summary>Hint</summary>

Apply the Arnoldi relation $A Q_m = Q_m H_m + h_{m+1,m}q_{m+1}e_m^*$ to $s$, then use $H_m s = \theta s$ and the orthonormality of $q_{m+1}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Apply $A Q_m = Q_m H_m + h_{m+1,m}q_{m+1}e_m^*$ to $s$: $A y = A Q_m s = Q_m H_m s + h_{m+1,m}q_{m+1}(e_m^* s) = \theta Q_m s + h_{m+1,m}(e_m^* s)q_{m+1} = \theta y + h_{m+1,m}(e_m^* s)q_{m+1}$. Hence $A y - \theta y = h_{m+1,m}(e_m^* s)\,q_{m+1}$. Since $q_{m+1}$ is a unit vector, $\|A y - \theta y\| = |h_{m+1,m}|\,|e_m^* s|\,\|q_{m+1}\| = |h_{m+1,m}|\,|e_m^* s|$. The residual is small precisely when the subdiagonal $h_{m+1,m}$ is small (near breakdown) or the last component of the eigenvector $s$ is small (the Ritz vector barely uses the newest direction).

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

Run one Lanczos step on $A = \begin{pmatrix} 4 & 1 \\ 1 & 4 \end{pmatrix}$ with $q_1 = (1,0)$. Compute $\alpha_1$, the residual vector $A q_1 - \alpha_1 q_1$, and $\beta_2$.

<details class="exercise__hint"><summary>Hint</summary>

$\alpha_1 = q_1^* A q_1$. Then subtract $\alpha_1 q_1$ from $A q_1$ and take the length of what remains.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$A q_1 = (4, 1)$, so $\alpha_1 = q_1^* A q_1 = (1,0)\cdot(4,1) = 4$. The residual is $A q_1 - \alpha_1 q_1 = (4,1) - 4(1,0) = (0,1)$. Its length is $\beta_2 = \|(0,1)\| = 1$, and $q_2 = (0,1)$. The tridiagonal so far is $T_1 = (4)$, extending to $T_2 = \begin{pmatrix} 4 & 1 \\ 1 & 4 \end{pmatrix}$ at the next step, whose eigenvalues $3, 5$ are the exact spectrum of $A$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Show that Arnoldi breakdown at step $m$ (i.e. $h_{m+1,m} = 0$) implies $\mathcal{K}_m(A,b)$ is $A$-invariant, and conclude that every eigenvalue of $H_m$ is then an exact eigenvalue of $A$.

<details class="exercise__hint"><summary>Hint</summary>

Breakdown means $A q_m \in \operatorname{span}\{q_1,\dots,q_m\}$. Combine with the Arnoldi relation, which loses its last term.

</details>

<details class="exercise__answer"><summary>Answer</summary>

If $h_{m+1,m} = 0$ the Arnoldi relation becomes $A Q_m = Q_m H_m$ exactly, with no remainder term. For any $v \in \mathcal{K}_m$ write $v = Q_m c$; then $A v = A Q_m c = Q_m H_m c \in \operatorname{range}(Q_m) = \mathcal{K}_m$, so $\mathcal{K}_m$ is $A$-invariant. Now if $H_m s = \theta s$ with $s \ne 0$, set $y = Q_m s \ne 0$ (as $Q_m$ has orthonormal columns); then $A y = A Q_m s = Q_m H_m s = \theta Q_m s = \theta y$, so $(\theta, y)$ is an exact eigenpair of $A$. Thus at breakdown the Ritz values cease to be approximations and become exact eigenvalues — a *lucky breakdown*.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove that in exact arithmetic the Lanczos vectors $q_1, \dots, q_m$ are mutually orthogonal, by induction using only the three-term recurrence $\beta_{j+1}q_{j+1} = A q_j - \alpha_j q_j - \beta_j q_{j-1}$ and $A = A^*$.

<details class="exercise__hint"><summary>Hint</summary>

Assume $q_1,\dots,q_j$ are orthonormal. Take the inner product of the recurrence with $q_i$ for $i \le j$, splitting into the cases $i = j$, $i = j-1$, and $i < j-1$; use $\alpha_j = q_j^*Aq_j$, $\beta_j = q_{j-1}^* A q_j$, and self-adjointness $q_i^* A q_j = (A q_i)^* q_j$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Induct on $j$, the hypothesis being that $q_1,\dots,q_j$ are orthonormal. Take $q_i^*$ of $\beta_{j+1}q_{j+1} = Aq_j - \alpha_j q_j - \beta_j q_{j-1}$ for $i \le j$. For $i = j$: $\beta_{j+1}q_j^* q_{j+1} = q_j^*Aq_j - \alpha_j q_j^*q_j - \beta_j q_j^*q_{j-1} = \alpha_j - \alpha_j - 0 = 0$, using the definition $\alpha_j = q_j^*Aq_j$ and orthonormality. For $i = j-1$: $\beta_{j+1}q_{j-1}^*q_{j+1} = q_{j-1}^*Aq_j - 0 - \beta_j$; and $q_{j-1}^*Aq_j = (A q_{j-1})^* q_j$ by self-adjointness, while the recurrence at step $j-1$ gives $A q_{j-1} = \beta_j q_j + \alpha_{j-1}q_{j-1} + \beta_{j-1}q_{j-2}$, so $(Aq_{j-1})^*q_j = \beta_j$, giving $\beta_{j+1}q_{j-1}^*q_{j+1} = \beta_j - \beta_j = 0$. For $i < j-1$: $\beta_{j+1}q_i^*q_{j+1} = q_i^*Aq_j = (Aq_i)^*q_j$, and $A q_i = \beta_{i+1}q_{i+1} + \alpha_i q_i + \beta_i q_{i-1}$ lies in $\operatorname{span}\{q_{i-1},q_i,q_{i+1}\}$, all of whose indices are $\le i+1 < j$, so $(Aq_i)^*q_j = 0$ by the inductive orthonormality. As $\beta_{j+1} \ne 0$ before breakdown, $q_i^*q_{j+1} = 0$ for every $i \le j$, completing the induction. (In finite precision these identities fail to hold exactly — the subject of the loss-of-orthogonality theory below.)

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

For Hermitian $A$ with eigenvalues in $[\lambda_{\min}, \lambda_{\max}]$, show that the largest Ritz value $\theta_{\max}^{(m)}$ from an $m$-step Lanczos run satisfies $\theta_{\max}^{(m)} \le \lambda_{\max}$ and is nondecreasing in $m$. (This is the monotone convergence of the extreme Ritz value.)

<details class="exercise__hint"><summary>Hint</summary>

$\theta_{\max}^{(m)} = \max_{0 \ne x \in \mathcal{K}_m} \dfrac{x^* A x}{x^* x}$ is the Rayleigh quotient maximised over the Krylov subspace; compare it to the maximum over all of $\mathbb{F}^n$, and use $\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By the Rayleigh-Ritz characterisation, the largest eigenvalue $\theta_{\max}^{(m)}$ of $T_m = Q_m^* A Q_m$ equals $\max_{0\ne s}\frac{s^*T_m s}{s^* s} = \max_{0 \ne x \in \mathcal{K}_m}\frac{x^*Ax}{x^*x}$, the maximum of the Rayleigh quotient of $A$ over the subspace $\mathcal{K}_m$ (writing $x = Q_m s$ and using $Q_m^*Q_m = I$). Since $\mathcal{K}_m \subseteq \mathbb{F}^n$, this constrained maximum is at most the global maximum $\max_{x \ne 0}\frac{x^*Ax}{x^*x} = \lambda_{\max}$, giving $\theta_{\max}^{(m)} \le \lambda_{\max}$. Because $\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$, the maximum over the larger subspace is at least the maximum over the smaller, so $\theta_{\max}^{(m)} \le \theta_{\max}^{(m+1)}$: the largest Ritz value is nondecreasing in $m$ and bounded above by $\lambda_{\max}$, hence converges. The symmetric argument with the minimum gives $\theta_{\min}^{(m)} \ge \lambda_{\min}$ nonincreasing.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (exactness at the grade; finite termination).** *Let $\nu$ be the grade of $b$ with respect to $A$ — the least $m$ with $\dim\mathcal{K}_m = \dim\mathcal{K}_{m+1}$. The Arnoldi process breaks down at step $\nu$, the subspace $\mathcal{K}_\nu$ is $A$-invariant, and the $\nu$ eigenvalues of $H_\nu$ are exact eigenvalues of $A$.* The Krylov dimension increases strictly by one until the grade and then stalls, because $A^\nu b$ is the first power expressible in lower ones; at that step the orthogonal remainder $w$ vanishes, $h_{\nu+1,\nu} = 0$, and the Arnoldi relation closes to $A Q_\nu = Q_\nu H_\nu$. Any eigenpair $H_\nu s = \theta s$ lifts to $A(Q_\nu s) = \theta(Q_\nu s)$, so the Ritz pairs become exact. The grade satisfies $\nu \le \deg m_A$, the degree of the minimal polynomial of $A$, with equality for a *cyclic* (non-derogatory) $A$ and a generic $b$; this is the exact-arithmetic sense in which Krylov methods terminate in at most $n$ steps [ref: TODO_REF Saad, Y. — Numerical Methods for Large Eigenvalue Problems (2nd ed.)].

**Theorem 2 (Kaniel-Paige-Saad convergence of extremal Ritz values).** *Let $A = A^*$ have eigenvalues $\lambda_1 > \lambda_2 \ge \dots \ge \lambda_n$ with unit eigenvectors $u_1, \dots, u_n$, and run Lanczos from $q_1$ with $\cos\phi_1 = |u_1^* q_1| > 0$. The largest Ritz value $\theta_1^{(m)}$ after $m$ steps obeys*
$$
0 \le \lambda_1 - \theta_1^{(m)} \le (\lambda_1 - \lambda_n)\left(\frac{\tan\phi_1}{T_{m-1}(1 + 2\gamma)}\right)^2, \qquad \gamma = \frac{\lambda_1 - \lambda_2}{\lambda_2 - \lambda_n},
$$
*where $T_{m-1}$ is the degree-$(m-1)$ Chebyshev polynomial of the first kind.* Because $T_{m-1}$ grows like $\tfrac12(1 + 2\sqrt{\gamma(1+\gamma)})^{m-1}$ outside $[-1,1]$, the bound decays geometrically with a rate set by the *relative gap* $\gamma$ between the dominant eigenvalue and the rest of the spectrum: a well-separated extreme eigenvalue is captured in a handful of steps, while a clustered one converges slowly. The same Chebyshev mechanism — a polynomial small on the bulk of the spectrum and large at the target — drives the conjugate-gradient error bound, and the optimal Krylov polynomial is the structural object both share [ref: TODO_REF Saad, Y. — Numerical Methods for Large Eigenvalue Problems (2nd ed.)].

**Theorem 3 (Paige; loss of orthogonality equals convergence).** *In finite-precision Lanczos with unit roundoff $\epsilon_{\mathrm{mach}}$, the computed vectors $\hat q_j$ satisfy a perturbed recurrence $A\hat q_j = \beta_{j+1}\hat q_{j+1} + \alpha_j \hat q_j + \beta_j \hat q_{j-1} + f_j$ with $\|f_j\| = O(\epsilon_{\mathrm{mach}}\|A\|)$, and the loss of orthogonality of $\hat q_{j+1}$ against an earlier Ritz vector is inversely proportional to that Ritz value's residual: orthogonality is lost in the direction of a Ritz vector precisely as that Ritz value converges.* Paige's analysis shows the rounding errors do not corrupt the computed Ritz values arbitrarily; instead they cause the Lanczos vectors to redevelop components along already-converged Ritz directions, producing duplicate (*ghost*) copies of converged eigenvalues rather than wrong ones. The tridiagonal $T_m$ computed in floating point is the exact $T_m$ of a slightly larger problem, so the eigenvalue information is preserved even as the orthonormal-basis property degrades [ref: TODO_REF Parlett, B. N. — The Symmetric Eigenvalue Problem (SIAM Classics, 1998)].

**Theorem 4 (reorthogonalisation strategies).** *Full reorthogonalisation — orthogonalising each new Lanczos vector against all previous ones — restores orthogonality to $O(\epsilon_{\mathrm{mach}})$ at the cost $O(m^2 n)$ of Arnoldi, defeating the short recurrence. Selective reorthogonalisation orthogonalises only against converged Ritz vectors, of which there are few, restoring semi-orthogonality (inner products $O(\sqrt{\epsilon_{\mathrm{mach}}})$) at cost $O(mn)$ plus the occasional Ritz solve; partial reorthogonalisation monitors the recurrence for the $\omega$-estimates of lost orthogonality and reorthogonalises only when a threshold is crossed.* Semi-orthogonality is enough: the computed Ritz values remain accurate to $O(\epsilon_{\mathrm{mach}})$ as long as inner products stay below $O(\sqrt{\epsilon_{\mathrm{mach}}})$, which is the design target of the selective and partial schemes that keep Lanczos both cheap and reliable [ref: TODO_REF Parlett, B. N. — The Symmetric Eigenvalue Problem (SIAM Classics, 1998)].

**Synthesis.** The Arnoldi and Lanczos iterations are one construction — orthonormalise the Krylov sequence $b, Ab, A^2b, \dots$ and read off the projected matrix $H_m = Q_m^* A Q_m$ — viewed under one invariant, the compression of $A$ onto a growing subspace, and the foundational reason the scheme works is that this compression is a Hessenberg (tridiagonal, when Hermitian) matrix whose eigenvalues converge to the extremes of $A$'s spectrum. This is exactly the iterative counterpart of the dense reduction of [43.06.02]: there a finite sequence of Householder similarities bands the full matrix, here a growing sequence of Gram-Schmidt steps bands a Krylov slice, and the central insight is that both routes reach the same band structure because both express $A$ in an orthonormal basis adapted to its action. The convergence theory generalises the static variational characterisation of eigenvalues into a dynamical one: the extremal Ritz values are Rayleigh quotients maximised over the nested Krylov subspaces, the Chebyshev bound of Theorem 2 is dual to the conjugate-gradient error bound that reappears in [43.07.04], and the optimal polynomial in $A$ is the object every Krylov method optimises.

The finite-precision theory is where the bridge to practice is built: Paige's theorem (Theorem 3) shows that loss of orthogonality is not a corruption of the eigenvalue information but a side effect of convergence, and the reorthogonalisation hierarchy (Theorem 4) trades exactly the storage and cost saved by the short recurrence against the orthogonality it sacrifices. Putting these together, the Arnoldi factorisation is at once the eigenvalue engine of large-scale computation and the inner machinery of the linear solvers GMRES and conjugate gradients; the bridge is that the same projected $H_m$ which yields Ritz values yields, by a small least-squares or tridiagonal solve, the residual-minimising and energy-minimising iterates of the rest of the chapter.

## Full proof set [Master]

**Proposition 1 (Krylov dimension and the grade).** *The dimensions $\dim\mathcal{K}_m(A,b)$ are strictly increasing in $m$ until the grade $\nu$, after which $\mathcal{K}_m = \mathcal{K}_\nu$ for all $m \ge \nu$, and $\mathcal{K}_\nu$ is $A$-invariant.*

*Proof.* The inclusion $\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$ is immediate, so $\dim\mathcal{K}_m$ is nondecreasing and bounded by $n$, hence eventually constant. Let $\nu$ be the least $m$ with $\dim\mathcal{K}_{m+1} = \dim\mathcal{K}_m$, i.e. $A^\nu b \in \mathcal{K}_\nu = \operatorname{span}\{b, \dots, A^{\nu-1}b\}$, so $A^\nu b = \sum_{i=0}^{\nu-1}c_i A^i b$. Applying $A$ gives $A^{\nu+1}b = \sum_i c_i A^{i+1}b \in \operatorname{span}\{Ab,\dots,A^\nu b\} \subseteq \mathcal{K}_\nu$ (the last inclusion since $A^\nu b \in \mathcal{K}_\nu$), so $\mathcal{K}_{\nu+1} = \mathcal{K}_\nu$, and inductively $\mathcal{K}_m = \mathcal{K}_\nu$ for all $m \ge \nu$. For invariance, $A\mathcal{K}_\nu = \operatorname{span}\{Ab, \dots, A^\nu b\} \subseteq \mathcal{K}_\nu$ by the same membership $A^\nu b \in \mathcal{K}_\nu$. Strict increase before $\nu$ holds because $\nu$ was chosen least. $\square$

**Proposition 2 (Arnoldi factorisation).** *Before breakdown, $Q_m$ has orthonormal columns spanning $\mathcal{K}_m$, and $AQ_m = Q_mH_m + h_{m+1,m}q_{m+1}e_m^*$ with $H_m = Q_m^*AQ_m$ upper-Hessenberg and unreduced.*

*Proof.* The orthonormality and span are the induction of the Key theorem; the factorisation is the column-stacked recurrence $A q_j = \sum_{i=1}^{j+1}h_{ij}q_i$, valid for $j \le m$, with the last row of $\tilde H_m$ contributing only $h_{m+1,m}q_{m+1}e_m^*$. Left-multiplying $AQ_m = Q_{m+1}\tilde H_m$ by $Q_m^*$ and using $Q_m^*Q_{m+1} = [I_m\ 0]$ extracts $H_m$. Upper-Hessenberg structure is Exercise 3: $A q_j \in \mathcal{K}_{j+1}$ forces $q_i^*Aq_j = 0$ for $i > j+1$. Unreducedness is $h_{j+1,j} = \|w_j\| > 0$ for $j < m$, which is the no-breakdown hypothesis. $\square$

**Proposition 3 (tridiagonalisation and the three-term recurrence for Hermitian $A$).** *If $A = A^*$ then $H_m = T_m$ is real symmetric tridiagonal, and the Arnoldi recurrence reduces to $\beta_{j+1}q_{j+1} = Aq_j - \alpha_j q_j - \beta_j q_{j-1}$ with $\alpha_j = q_j^*Aq_j \in \mathbb{R}$, $\beta_{j+1} = h_{j+1,j} > 0$.*

*Proof.* $H_m^* = (Q_m^*AQ_m)^* = Q_m^*A^*Q_m = Q_m^*AQ_m = H_m$, so $H_m$ is Hermitian; combined with upper-Hessenberg ($h_{ij} = 0$ for $i > j+1$) and conjugate symmetry ($h_{ji} = \overline{h_{ij}}$, hence $= 0$ for $j > i+1$), only the diagonal and first off-diagonals survive, giving tridiagonal $T_m$. Its diagonal entries $\alpha_j = h_{jj} = q_j^*Aq_j$ are real (a Hermitian-form diagonal), and the sub/super-diagonals are conjugate, $h_{j,j-1} = \overline{h_{j-1,j}}$; a phase rescaling of the $q_j$ makes them real positive $\beta_j$. In the orthogonalisation $w = Aq_j - \sum_{i\le j}h_{ij}q_i$, the coefficients $h_{ij}$ with $i < j-1$ equal $q_i^*Aq_j$, which vanish by tridiagonality, leaving only $i = j$ (giving $\alpha_j q_j$) and $i = j-1$ (giving $\beta_j q_{j-1}$, since $h_{j-1,j} = \overline{h_{j,j-1}} = \beta_j$). Thus $w = Aq_j - \alpha_j q_j - \beta_j q_{j-1}$ and $\beta_{j+1}q_{j+1} = w$. $\square$

**Proposition 4 (Ritz residual identity and the interlacing bound).** *For a Ritz pair $(\theta, y = Q_m s)$ with $H_m s = \theta s$, $\|s\| = 1$, one has $\|Ay - \theta y\| = |h_{m+1,m}|\,|e_m^*s|$. For Hermitian $A$ the Ritz values interlace the spectrum: the eigenvalues of $T_m$ lie in $[\lambda_{\min}(A), \lambda_{\max}(A)]$, and the extreme Ritz values are monotone in $m$.*

*Proof.* The residual identity is Exercise 4: applying $AQ_m = Q_mH_m + h_{m+1,m}q_{m+1}e_m^*$ to $s$ gives $Ay = \theta y + h_{m+1,m}(e_m^*s)q_{m+1}$, and $\|q_{m+1}\| = 1$ yields the norm. For the spectral bound, the eigenvalues of $T_m = Q_m^*AQ_m$ are the stationary values of the Rayleigh quotient $\frac{s^*T_m s}{s^*s} = \frac{x^*Ax}{x^*x}$ with $x = Q_m s \in \mathcal{K}_m$; this quotient is confined to the numerical range of $A$, which for Hermitian $A$ is $[\lambda_{\min}, \lambda_{\max}]$. Hence every Ritz value lies in that interval. Monotonicity follows from $\mathcal{K}_m \subseteq \mathcal{K}_{m+1}$: the maximum (minimum) of the Rayleigh quotient over the larger subspace is at least (at most) that over the smaller, so $\theta_{\max}^{(m)} \le \theta_{\max}^{(m+1)} \le \lambda_{\max}$ and $\theta_{\min}^{(m)} \ge \theta_{\min}^{(m+1)} \ge \lambda_{\min}$. By the Cauchy interlacing theorem for the principal-submatrix-like compression, the full eigenvalues of $T_{m+1}$ interlace those of $T_m$. $\square$

**Proposition 5 (CG-Lanczos correspondence).** *For Hermitian positive-definite $A$, the conjugate-gradient iterate $x_m \in x_0 + \mathcal{K}_m(A, r_0)$ minimising the $A$-norm of the error is recovered from the Lanczos tridiagonal $T_m$ by solving $T_m z_m = \|r_0\|e_1$ and setting $x_m = x_0 + Q_m z_m$; the CG short recurrences are the $LDL^*$ factorisation of $T_m$ updated one column at a time.*

*Proof sketch.* The CG error-minimising iterate over $\mathcal{K}_m$ is the Galerkin solution: $r_m = b - Ax_m \perp \mathcal{K}_m$, i.e. $Q_m^*r_m = 0$. Writing $x_m = x_0 + Q_m z_m$ and $r_0 = \|r_0\|q_1$, the condition $Q_m^*(r_0 - AQ_m z_m) = 0$ becomes $\|r_0\|e_1 - Q_m^*AQ_m z_m = 0$, i.e. $T_m z_m = \|r_0\|e_1$. Since $A \succ 0$, $T_m = Q_m^*AQ_m$ is SPD and admits an $LDL^*$ factorisation; performing it incrementally — each new Lanczos column extends $T_m$ by one row and column, updating $L, D$ by one entry — turns the solve into the two coupled two-term recurrences for the CG search direction and iterate, with no need to store $Q_m$. The full statement and the Chebyshev convergence bound are the content of [43.07.04]; the correspondence here is that CG is Lanczos with the tridiagonal system solved on the fly. $\square$

## Connections [Master]

- **Eigenvalue, eigenvector, and the spectral radius [01.01.08]** supplies the spectral objects the Ritz machinery approximates: the Ritz values are the eigenvalues of the small projected matrix $H_m$, computed by the very theory of [01.01.08], and they converge to the extreme eigenvalues of the large $A$. The Rayleigh-quotient characterisation that makes the extremal Ritz values monotone is the variational face of the eigenvalue theory there, lifted from single vectors to the nested Krylov subspaces; the minimal-polynomial degree that caps the Krylov grade is the same invariant that governs diagonalisability in that unit.

- **Reduction to Hessenberg/tridiagonal form [43.06.02]** is the dense analogue this unit iterates: there a finite product of Householder similarities produces $Q^*AQ$ upper-Hessenberg (tridiagonal when Hermitian) on the whole of $\mathbb{F}^n$, here a growing Gram-Schmidt sequence produces $Q_m^*AQ_m$ with the identical band structure on a Krylov subspace. The two reach the same projected form by different routes — direct reflectors versus iterative orthogonalisation — and the band-preservation arguments are mirror images, so the present unit is the large-scale, matrix-free counterpart of that finite reduction; an Arnoldi run to completion ($m = n$) is a Hessenberg reduction.

- **GMRES [43.07.03]** and **the conjugate gradient method [43.07.04]** are the linear-solver engines this unit powers: GMRES minimises $\|b - Ax\|$ over $x_0 + \mathcal{K}_m$ by solving a small least-squares problem on the Arnoldi $\tilde H_m$ via Givens rotations, while conjugate gradients is exactly the Lanczos process for SPD $A$ with the tridiagonal $T_m$ factored incrementally to give a short three-term iterate recurrence. Both inherit the Krylov-subspace optimality and the Chebyshev convergence mechanism established here, and the stationary-iteration splitting $M$ of [43.07.01] returns in both as a preconditioner that reshapes the spectrum to accelerate the Ritz and residual convergence.

## Historical & philosophical context [Master]

The Krylov subspace is named for **Aleksei Nikolaevich Krylov**, the Russian naval engineer and applied mathematician who in 1931 used the sequence $b, Ab, A^2b, \dots$ to compute the characteristic polynomial of a matrix arising in ship-vibration analysis, reducing the eigenvalue problem to the powers of $A$ acting on a single vector. The orthogonalised form was introduced by **Walter Edwin Arnoldi** in 1951, who proposed reducing a matrix to Hessenberg form by a Gram-Schmidt process on the Krylov sequence as a means of approximating eigenvalues of large matrices without forming the full reduction [ref: TODO_REF Arnoldi 1951].

The symmetric specialisation is due to **Cornelius Lanczos**, whose 1950 paper at the U.S. National Bureau of Standards introduced the three-term recurrence — he called it the *method of minimized iterations* — for tridiagonalising a symmetric matrix and, in a companion development, for solving linear systems [ref: TODO_REF Lanczos 1950]. The method was initially distrusted because, in the finite-precision arithmetic of early computers, the Lanczos vectors rapidly lost orthogonality and produced spurious repeated eigenvalues; for two decades it was regarded as numerically unreliable. **Christopher Conway Paige**'s 1971 doctoral thesis at the University of London rehabilitated it by proving that the loss of orthogonality is not random corruption but coincides exactly with the convergence of a Ritz value, so the computed eigenvalues remain accurate [ref: TODO_REF Paige 1971]. **Beresford Parlett** and collaborators then developed the selective and partial reorthogonalisation schemes that made the method both efficient and trustworthy, consolidated in *The Symmetric Eigenvalue Problem* [ref: TODO_REF Parlett, B. N. — The Symmetric Eigenvalue Problem (SIAM Classics, 1998)]. **Yousef Saad** built the general (non-symmetric) Arnoldi theory of Ritz-value convergence and the modern large-eigenvalue and Krylov-solver framework that situates Arnoldi and Lanczos as the common engine of GMRES and conjugate gradients [ref: TODO_REF Saad, Y. — Numerical Methods for Large Eigenvalue Problems (2nd ed.)].

## Bibliography [Master]

```bibtex
@article{Krylov1931,
  author  = {Krylov, Aleksei N.},
  title   = {O chislennom reshenii uravneniya, kotorym v tekhnicheskikh voprosakh opredelyayutsya chastoty malykh kolebanii material'nykh sistem},
  journal = {Izvestiya Akademii Nauk SSSR, Otdelenie Matematicheskikh i Estestvennykh Nauk},
  volume  = {7},
  number  = {4},
  year    = {1931},
  pages   = {491--539}
}

@article{Arnoldi1951,
  author  = {Arnoldi, Walter E.},
  title   = {The Principle of Minimized Iterations in the Solution of the Matrix Eigenvalue Problem},
  journal = {Quarterly of Applied Mathematics},
  volume  = {9},
  number  = {1},
  year    = {1951},
  pages   = {17--29}
}

@article{Lanczos1950,
  author  = {Lanczos, Cornelius},
  title   = {An Iteration Method for the Solution of the Eigenvalue Problem of Linear Differential and Integral Operators},
  journal = {Journal of Research of the National Bureau of Standards},
  volume  = {45},
  number  = {4},
  year    = {1950},
  pages   = {255--282}
}

@phdthesis{Paige1971,
  author = {Paige, Christopher C.},
  title  = {The Computation of Eigenvalues and Eigenvectors of Very Large Sparse Matrices},
  school = {University of London},
  year   = {1971}
}

@book{Parlett1998,
  author    = {Parlett, Beresford N.},
  title     = {The Symmetric Eigenvalue Problem},
  publisher = {SIAM},
  series    = {Classics in Applied Mathematics},
  year      = {1998}
}

@book{Saad2011,
  author    = {Saad, Yousef},
  title     = {Numerical Methods for Large Eigenvalue Problems},
  edition   = {2},
  publisher = {Society for Industrial and Applied Mathematics},
  year      = {2011}
}

@book{TrefethenBau1997,
  author    = {Trefethen, Lloyd N. and Bau, David},
  title     = {Numerical Linear Algebra},
  publisher = {SIAM},
  address   = {Philadelphia},
  year      = {1997}
}

@book{GolubVanLoan2013,
  author    = {Golub, Gene H. and Van Loan, Charles F.},
  title     = {Matrix Computations},
  edition   = {4},
  publisher = {Johns Hopkins University Press},
  year      = {2013}
}
```
