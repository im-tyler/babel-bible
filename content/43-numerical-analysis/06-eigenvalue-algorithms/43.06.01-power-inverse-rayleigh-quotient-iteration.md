---
id: 43.06.01
title: "Power iteration, inverse iteration, and Rayleigh quotient iteration"
slug: power-inverse-rayleigh-quotient-iteration
section: numerical-analysis
chapter: 06-eigenvalue-algorithms
concept_catalog_id: numerical-analysis.power-inverse-rayleigh-quotient-iteration
prerequisites:
  - 01.01.08  # eigenvalue, eigenvector, characteristic polynomial
  - 01.01.14  # Rayleigh quotient and Courant-Fischer min-max
successors: []
tier_anchors:
  beginner: "Repeatedly multiplying by a matrix and watching the result line up with the dominant direction — Strang *Introduction to Linear Algebra* Ch. 6 (powers of a matrix); 3Blue1Brown *Essence of Linear Algebra* Ch. 14 (eigenvectors as the directions the matrix stretches most)"
  intermediate: "Trefethen-Bau *Numerical Linear Algebra* Lectures 27–28 (Rayleigh quotient, power iteration, inverse iteration, Rayleigh quotient iteration); Golub-Van Loan *Matrix Computations* §8.2 (the power method and its variants)"
  master: "Trefethen-Bau *Numerical Linear Algebra* Lectures 27–28; Golub-Van Loan *Matrix Computations* (4th ed.) §8.2; Parlett *The Symmetric Eigenvalue Problem* Ch. 4 (power methods) and §4.6–§4.7 (Rayleigh quotient iteration, cubic convergence); Wilkinson *The Algebraic Eigenvalue Problem* Ch. 9 (inverse iteration)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: fast-track
    path: "images/Shilov-Linear-Algebra__4cbdee00cc.jpg"
    locator: "Shilov *Linear Algebra* — Fast Track archive cover; Ch. 10 self-adjoint operators, the Rayleigh quotient and the extremal characterisation that the eigenvalue algorithms of this unit approximate"
  - source: TODO_REF
    path: "Trefethen, L. N. & Bau, D. — Numerical Linear Algebra (SIAM, 1997)"
    locator: "Lectures 27–28 — the Rayleigh quotient, power iteration with rate $|\\lambda_2/\\lambda_1|$, inverse iteration with a shift, and Rayleigh quotient iteration with cubic convergence for symmetric matrices"
  - source: TODO_REF
    path: "Golub, G. H. & Van Loan, C. F. — Matrix Computations (4th ed., Johns Hopkins, 2013)"
    locator: "§8.2 — the power method, inverse iteration, Rayleigh quotient iteration, and deflation"
  - source: TODO_REF
    path: "Parlett, B. N. — The Symmetric Eigenvalue Problem (SIAM Classics, 1998)"
    locator: "Ch. 4 and §4.6–§4.7 — the convergence theory of power methods and the cubic convergence of Rayleigh quotient iteration for symmetric matrices"
  - source: TODO_REF
    path: "Wilkinson, J. H. — The Algebraic Eigenvalue Problem (Oxford, 1965)"
    locator: "Ch. 9 — inverse iteration with a shift and the accuracy of the computed eigenvector despite the near-singular solve"
  - source: TODO_REF
    path: "von Mises, R. & Pollaczek-Geiringer, H. — Praktische Verfahren der Gleichungsauflösung"
    locator: "Zeitschrift für Angewandte Mathematik und Mechanik 9 (1929), 58–77 and 152–164 — the systematic introduction of the power method (Potenzmethode) for the numerical eigenvalue problem"
  - source: TODO_REF
    path: "Wielandt, H. — Beiträge zur mathematischen Behandlung komplexer Eigenwertprobleme"
    locator: "Bericht B 44/J/37, Aerodynamische Versuchsanstalt Göttingen (1944) — fractional (inverse) iteration with a shift for the targeted computation of eigenvalues near a chosen point"
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the static spectral infrastructure these algorithms
  approximate — eigenvalues and eigenvectors via `Module.End.HasEigenvalue`
  and `Module.End.eigenspace`, the Rayleigh quotient and its supremum/infimum
  eigenvalue characterisation in `Mathlib.Analysis.InnerProductSpace.Rayleigh`
  (`LinearMap.IsSymmetric.hasEigenvalue_iSup`), and the finite-dimensional
  spectral theorem — but it has no formalisation of the iterative eigenvalue
  algorithms or their convergence theorems. There is no named lemma stating
  that the normalised power-iteration sequence $x_{k+1} = A x_k / \|A x_k\|$
  converges to the dominant eigenvector at the linear rate $|\lambda_2/\lambda_1|$
  when the dominant eigenvalue is simple and the start vector has nonzero
  overlap with it, no statement that inverse iteration with a shift $\mu$
  converges to the eigenvalue nearest $\mu$, and no statement of the cubic
  local convergence $\sin\angle(x_{k+1},u) = O(\sin^3\angle(x_k,u))$ of
  Rayleigh quotient iteration for a self-adjoint operator. Each is a clean
  finite-dimensional analysis target reachable from the existing eigenbasis
  expansion and the `dist`/`angle` machinery, but the convergence-rate
  estimates (geometric decay of off-dominant coordinates, the second-order
  vanishing of the Rayleigh-quotient error in the symmetric case) are not
  packaged anywhere in Mathlib. The named human reviewer is the correctness
  gate for the convergence-rate constants and the cubic-convergence bound.
human_reviewer: "Tyler (pending external numerical-linear-algebra reviewer)"
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

A matrix has a set of special directions, the eigenvectors, and each one comes with a number, its eigenvalue, that says how much the matrix stretches that direction. The largest eigenvalue belongs to the direction the matrix stretches the most. A natural question is how to find that dominant direction and its stretch factor without solving for every eigenvalue at once.

Power iteration answers with a one-line idea: pick any starting vector, multiply it by the matrix, and repeat. Each multiplication amplifies the dominant direction more than any other, because that direction is stretched the most. After enough rounds the vector points almost exactly along the dominant eigenvector, and you rescale it each step so it does not grow without bound. The leftover directions fade away at a rate set by how much the largest stretch factor beats the second largest.

Inverse iteration flips the trick to target any eigenvalue you like. You pick a guess near the eigenvalue you want, called a shift, and apply the inverse of the matrix minus that guess. This new matrix stretches most along the eigenvector whose eigenvalue sits closest to your guess, so the same repeated-multiplication idea now homes in on that one. Rayleigh quotient iteration takes the next step: after each round it uses the current vector to form a sharper guess, and for symmetric matrices this feedback makes the accuracy improve at a breathtaking pace.

## Visual [Beginner]

The picture shows a starting vector being multiplied by a matrix again and again. At first the vector points in some arbitrary direction. After one multiplication it tilts toward the dominant eigendirection; after two it is closer still; after a handful of steps it sits almost exactly along that direction. The off-direction part shrinks by a constant factor every step, so the convergence looks like a vector swinging steadily into place.

![A diagram in two panels. The left panel shows a plane with two eigendirections drawn as dashed lines, one labelled the dominant direction with stretch factor four and the other labelled the weaker direction with stretch factor two. A sequence of arrows, the vectors x at steps zero, one, two, and three, starts pointing between the two dashed lines and rotates step by step until it nearly coincides with the dominant dashed line; each arrow is drawn the same length because of rescaling. The right panel plots the angle between the vector and the dominant direction against the step number, a curve dropping by a constant ratio of one half each step, the ratio two over four of the two stretch factors, illustrating the geometric convergence rate.](/img/power-inverse-rayleigh-quotient-iteration-placeholder.svg)

Two facts are visible. The vector turns toward the dominant direction and the angle to it shrinks by the same ratio every step, the ratio of the second stretch factor to the first. If those two factors were close together the curve would drop slowly, and if the first dwarfs the second the curve would plunge fast.

## Worked example [Beginner]

Take the symmetric matrix

$$
A = \begin{pmatrix} 3 & 1 \\ 1 & 3 \end{pmatrix},
$$

whose eigenvalues are $4$ (along the direction $(1, 1)$) and $2$ (along $(1, -1)$). Start power iteration from the vector $x_0 = (1, 0)$, which is not aligned with either eigendirection.

Step 1. Multiply: $A x_0 = (3, 1)$. Rescale by dividing by the larger entry $3$ to keep numbers tidy: $x_1 = (1, 0.333)$.

Step 2. Multiply: $A x_1 = (3 + 0.333,\ 1 + 1) = (3.333, 2)$. Rescale by $3.333$: $x_2 = (1, 0.6)$.

Step 3. Multiply: $A x_2 = (3 + 0.6,\ 1 + 1.8) = (3.6, 2.8)$. Rescale by $3.6$: $x_3 = (1, 0.778)$.

Step 4. Multiply: $A x_3 = (3 + 0.778,\ 1 + 2.333) = (3.778, 3.333)$. Rescale by $3.778$: $x_4 = (1, 0.882)$.

The second coordinate is climbing toward $1$, which is the dominant eigendirection $(1, 1)$. The gap to $1$ shrinks roughly by half each step, matching the ratio $2/4 = 0.5$ of the two eigenvalues.

Step 5. Estimate the eigenvalue. Take the Rayleigh quotient of $x_4 = (1, 0.882)$: compute $A x_4 = (3.882, 3.647)$, then the ratio $(A x_4) \cdot x_4 / (x_4 \cdot x_4) = (3.882 + 3.216) / (1 + 0.778) = 7.098 / 1.778 = 3.99$.

What this tells us: a few rounds of plain matrix multiplication drove an arbitrary vector almost onto the dominant eigenvector, and the Rayleigh quotient turned that vector into the estimate $3.99$ for the dominant eigenvalue $4$. No characteristic polynomial was solved; the matrix did the work by stretching.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Power iteration converges fastest when the largest and second-largest eigenvalues (in size) satisfy which relationship?

A. They are equal.
B. The largest is much bigger than the second.
C. The second is negative.
D. Both are zero.

<details class="exercise__hint"><summary>Hint</summary>

The off-direction part shrinks by the ratio of the second size to the first each step. A small ratio means fast shrinking.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. The largest is much bigger than the second.** The error shrinks by the ratio of the second-largest size to the largest each step, so a large gap makes that ratio small and convergence fast. Feedback-correct: a dominant eigenvalue that dwarfs the rest is the ideal case. Feedback-wrong: if the two are equal the ratio is $1$ and the method stalls; the sign of the second eigenvalue does not by itself set the speed; both being zero leaves nothing to find.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: inverse iteration with a shift near a chosen eigenvalue makes the method converge to the eigenvector belonging to the eigenvalue closest to that shift.

<details class="exercise__hint"><summary>Hint</summary>

Subtracting the shift and inverting makes the eigenvalue nearest the shift become the largest in size for the new matrix.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Subtracting the shift moves the targeted eigenvalue close to zero, and inverting turns a near-zero number into a very large one, so that eigenvector becomes the dominant direction of the inverted matrix. Feedback-correct: the shift lets you aim at any eigenvalue, not just the largest. Feedback-wrong: without the shift the plain inverse would only find the smallest eigenvalue of the original matrix.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A matrix has eigenvalues $6$ and $2$. In each step of power iteration the off-direction error is multiplied by the ratio of the smaller size to the larger. What is that ratio, as a decimal?

<details class="exercise__hint"><summary>Hint</summary>

Divide the smaller eigenvalue size by the larger: $2$ divided by $6$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0.333$.** The error shrinks by $2 / 6 = 0.333$ each step, so after each multiplication the leftover off-direction part is about a third of what it was. Rubric: full credit for $0.333$ (or $1/3$).

</details>

</aside>

## Formal definition [Intermediate+]

Let $A \in M_n(\mathbb{F})$ with $\mathbb{F} \in \{\mathbb{R}, \mathbb{C}\}$, and suppose $A$ is diagonalisable with eigenvalues $\lambda_1, \ldots, \lambda_n$ and a basis of eigenvectors $u_1, \ldots, u_n$, where $A u_i = \lambda_i u_i$ and $\|u_i\| = 1$ in the Euclidean norm of [01.01.08]. Order the eigenvalues by modulus, $|\lambda_1| \geq |\lambda_2| \geq \cdots \geq |\lambda_n|$. The eigenvalue $\lambda_1$ is **dominant** when $|\lambda_1| > |\lambda_2|$; in that strict case the dominant eigenspace is one-dimensional and $u_1$ is determined up to a scalar.

**Definition (power iteration).** Given a start vector $x_0$ with $\|x_0\| = 1$, power iteration generates the sequence

$$
y_k = A x_{k-1}, \qquad x_k = \frac{y_k}{\|y_k\|}, \qquad k = 1, 2, \ldots,
$$

together with the eigenvalue estimate by the **Rayleigh quotient** $\rho_k = R(x_k) = \langle A x_k, x_k \rangle / \langle x_k, x_k \rangle$, the optimal scalar estimate of the eigenvalue associated with the direction $x_k$, in the sense of [01.01.14]. Each step is one matrix-vector product followed by a normalisation.

**Definition (inverse iteration with a shift).** Fix a shift $\mu \in \mathbb{F}$ that is not an eigenvalue, so $A - \mu I$ is invertible. Inverse iteration generates

$$
(A - \mu I)\, y_k = x_{k-1}, \qquad x_k = \frac{y_k}{\|y_k\|},
$$

that is, power iteration applied to the matrix $(A - \mu I)^{-1}$. The vector $y_k$ is computed by solving a linear system with the fixed matrix $A - \mu I$ — a single factorisation reused across iterations — rather than by forming the inverse explicitly.

**Definition (Rayleigh quotient iteration).** Replace the fixed shift by the current Rayleigh quotient at every step. Starting from $x_0$ with $\|x_0\| = 1$ and $\mu_0 = R(x_0)$,

$$
(A - \mu_{k-1} I)\, y_k = x_{k-1}, \qquad x_k = \frac{y_k}{\|y_k\|}, \qquad \mu_k = R(x_k).
$$

The shift $\mu_{k-1}$ now varies, so each step solves a system with a different matrix, but the adaptively improving shift produces convergence far faster than a fixed shift.

Notation: $\angle(v, w)$ is the acute angle between the lines spanned by nonzero $v$ and $w$, with $\sin\angle(v, w)$ the standard measure of misalignment; $\sigma(A)$ is the spectrum; $R(v) = \langle A v, v \rangle / \langle v, v \rangle$ is the Rayleigh quotient. Throughout, "convergence of $x_k$ to $u$" means $\sin\angle(x_k, u) \to 0$, since an eigenvector is determined only up to a scalar.

### Counterexamples to common slips

- Power iteration needs a *strictly* dominant eigenvalue. For $A = \mathrm{diag}(1, -1)$ the two eigenvalues have equal modulus, and a generic start vector oscillates between two directions without settling; the ratio $|\lambda_2/\lambda_1| = 1$ gives no contraction.
- The method fails if the start vector has zero component along $u_1$. For $A = \mathrm{diag}(2, 1)$ and $x_0 = (0, 1)$, every iterate stays $(0, 1)$ and converges to the *subdominant* eigenvector. In exact arithmetic this is a measure-zero accident; in floating point, rounding usually reintroduces a $u_1$-component and the method recovers.
- A complex dominant pair $\lambda, \bar\lambda$ of a real matrix has equal modulus, so real power iteration does not converge to a single vector; the dominant *invariant subspace* is two-dimensional and requires a block method.

## Key theorem with proof [Intermediate+]

**Theorem (linear convergence of power iteration; Trefethen-Bau Lecture 27 [ref: TODO_REF]; Golub-Van Loan §8.2 [ref: TODO_REF]).** *Let $A \in M_n(\mathbb{F})$ be diagonalisable with eigenpairs $(\lambda_i, u_i)$, $\|u_i\| = 1$, and a strictly dominant eigenvalue, $|\lambda_1| > |\lambda_2| \geq \cdots \geq |\lambda_n|$. Write the start vector as $x_0 = \sum_{i=1}^n c_i u_i$ and assume $c_1 \neq 0$. Then the power-iteration sequence $x_k = A^k x_0 / \|A^k x_0\|$ satisfies*

$$
\sin\angle(x_k, u_1) = O\!\left( \left| \frac{\lambda_2}{\lambda_1} \right|^k \right) \quad \text{as } k \to \infty,
$$

*and the Rayleigh quotient $\rho_k = R(x_k)$ converges to $\lambda_1$. If in addition $A$ is self-adjoint, the eigenvalue error sharpens to $\rho_k - \lambda_1 = O\!\left(|\lambda_2/\lambda_1|^{2k}\right)$.*

**Proof.** Expand in the eigenbasis: $A^k x_0 = \sum_{i=1}^n c_i \lambda_i^k u_i$. Factor out the dominant term,

$$
A^k x_0 = c_1 \lambda_1^k \left( u_1 + \sum_{i=2}^n \frac{c_i}{c_1} \left(\frac{\lambda_i}{\lambda_1}\right)^k u_i \right) = c_1 \lambda_1^k \left( u_1 + r_k \right),
$$

where the remainder $r_k = \sum_{i \geq 2} (c_i/c_1)(\lambda_i/\lambda_1)^k u_i$ has norm bounded by

$$
\|r_k\| \leq \sum_{i \geq 2} \left| \frac{c_i}{c_1} \right| \left| \frac{\lambda_i}{\lambda_1} \right|^k \leq \left( \sum_{i \geq 2} \left| \frac{c_i}{c_1} \right| \right) \left| \frac{\lambda_2}{\lambda_1} \right|^k = C \left| \frac{\lambda_2}{\lambda_1} \right|^k,
$$

because $|\lambda_i/\lambda_1| \leq |\lambda_2/\lambda_1|$ for every $i \geq 2$. Normalising kills the scalar $c_1 \lambda_1^k$, so $x_k$ is a unit vector in the direction of $u_1 + r_k$. The sine of the angle to $u_1$ is the norm of the component of $x_k$ orthogonal to $u_1$, which is at most $\|r_k\| / \|u_1 + r_k\|$; since $\|u_1 + r_k\| \to 1$, this is $O(\|r_k\|) = O(|\lambda_2/\lambda_1|^k)$.

For the eigenvalue estimate, the Rayleigh quotient is continuous and $R(u_1) = \lambda_1$, so $\rho_k = R(x_k) \to \lambda_1$ as $x_k \to u_1$ in direction. When $A = A^*$, the eigenbasis is orthonormal, so $r_k \perp u_1$ to leading order and $R(x_k) - \lambda_1$ is a quadratic form in the small quantity $r_k$: writing $x_k \propto u_1 + r_k$ with $r_k \perp u_1$, one computes $R(x_k) - \lambda_1 = \langle (A - \lambda_1 I) r_k, r_k \rangle / (1 + \|r_k\|^2)$, which is $O(\|r_k\|^2) = O(|\lambda_2/\lambda_1|^{2k})$. This is the same second-order stationarity that makes the Rayleigh quotient the optimal eigenvalue estimate at an approximate eigenvector. $\square$

**Bridge.** This convergence analysis builds toward the inverse-iteration and Rayleigh-quotient-iteration theorems of the Advanced results, where the very same eigenbasis expansion is applied to $(A - \mu I)^{-1}$, whose eigenvalues $(\lambda_i - \mu)^{-1}$ are dominated by the one nearest the shift; the ratio $|\lambda_2/\lambda_1|$ that governs the rate here appears again in [43.06.03] as the convergence engine of the QR algorithm, which is simultaneous power iteration on an orthonormal basis. The foundational reason the method works is the spectral expansion $A^k x_0 = \sum_i c_i \lambda_i^k u_i$: raising $A$ to a power raises each eigenvalue to that power, and the largest in modulus eventually swamps the rest. This is exactly the dynamical content of the dominant eigenvalue, and the quadratic sharpening in the self-adjoint case generalises the stationarity of the Rayleigh quotient established in [01.01.14]. Putting these together, plain matrix multiplication is a convergence process whose speed is read off the spectral gap, and the bridge is the observation that a faster method must reshape the spectrum so that the targeted eigenvalue dominates more sharply — precisely what shifting and inverting accomplish.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

A diagonalisable matrix has eigenvalues $5, 3, 1$. What is the asymptotic per-step convergence factor $|\lambda_2/\lambda_1|$ for power iteration, as a decimal?

<details class="exercise__hint"><summary>Hint</summary>

Use the two largest moduli, $5$ and $3$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0.6$.** The rate is $|\lambda_2/\lambda_1| = 3/5 = 0.6$. Each step shrinks the angle to the dominant eigenvector by roughly this factor. Rubric: full credit for $0.6$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

For $A = \begin{pmatrix} 2 & 0 \\ 0 & 5 \end{pmatrix}$, run one step of power iteration from $x_0 = (1, 1)/\sqrt{2}$. After computing $A x_0$ and normalising, what is the ratio of the second coordinate to the first of the resulting $x_1$?

<details class="exercise__hint"><summary>Hint</summary>

$A x_0 = (2, 5)/\sqrt 2$. Normalising does not change the ratio of coordinates.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2.5$.** $A x_0 = (2, 5)/\sqrt 2$, and the coordinate ratio is $5/2 = 2.5$. The second (dominant) coordinate has grown relative to the first, pulling the vector toward the eigenvector $(0, 1)$ of the larger eigenvalue $5$. Rubric: full credit for $2.5$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 3 (medium, numeric).**

The matrix $A$ has eigenvalues $10, 7, 4$. Inverse iteration is run with shift $\mu = 6.5$. The method converges to the eigenvector of which eigenvalue, and what is the asymptotic per-step convergence factor?

<details class="exercise__hint"><summary>Hint</summary>

Inverse iteration with shift $\mu$ converges to the eigenvalue nearest $\mu$; the rate is the ratio $|\lambda_{\text{nearest}} - \mu| / |\lambda_{\text{second nearest}} - \mu|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Eigenvalue $7$, rate $0.167$.** The distances from $\mu = 6.5$ are $|10 - 6.5| = 3.5$, $|7 - 6.5| = 0.5$, $|4 - 6.5| = 2.5$. The nearest eigenvalue is $7$. For $(A - \mu I)^{-1}$ the relevant ratio is the nearest distance over the second-nearest distance, $0.5 / 2.5 = 0.2$ — using the eigenvalue $4$ at distance $2.5$ as the runner-up. Rubric: full credit for identifying $7$ and a rate of $0.2$ (accept $0.5/2.5$).

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Show that inverse iteration with shift $\mu$ is power iteration on $B = (A - \mu I)^{-1}$, and that the eigenvalues of $B$ are $(\lambda_i - \mu)^{-1}$ with the same eigenvectors as $A$. Deduce which eigenvector becomes dominant for $B$.

<details class="exercise__hint"><summary>Hint</summary>

If $A u_i = \lambda_i u_i$, apply $(A - \mu I)$ and then its inverse to $u_i$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

From $A u_i = \lambda_i u_i$, $(A - \mu I) u_i = (\lambda_i - \mu) u_i$, so applying $(A - \mu I)^{-1}$ gives $B u_i = (\lambda_i - \mu)^{-1} u_i$. Thus $B$ has the same eigenvectors $u_i$ with eigenvalues $(\lambda_i - \mu)^{-1}$. The step $(A - \mu I) y_k = x_{k-1}$ is exactly $y_k = B x_{k-1}$, so the iteration is power iteration on $B$. The dominant eigenvalue of $B$ is the largest of $|(\lambda_i - \mu)^{-1}|$, attained when $|\lambda_i - \mu|$ is smallest — the eigenvalue $\lambda_i$ nearest the shift $\mu$. So inverse iteration converges to the eigenvector of the eigenvalue closest to $\mu$. Rubric: full credit for the eigenvalue computation and the nearest-to-$\mu$ conclusion.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

Run one Rayleigh quotient iteration step on $A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$ from $x_0 = (1, 0)$. First compute $\mu_0 = R(x_0)$. Report $\mu_0$.

<details class="exercise__hint"><summary>Hint</summary>

$R(x_0) = \langle A x_0, x_0 \rangle / \langle x_0, x_0 \rangle$ with $x_0 = (1,0)$, so it is the top-left entry of $A$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2$.** With $x_0 = (1, 0)$, $A x_0 = (2, 1)$, so $\langle A x_0, x_0 \rangle = 2$ and $\langle x_0, x_0 \rangle = 1$, giving $\mu_0 = 2$. This shift sits exactly between the eigenvalues $1$ and $3$ of $A$, so the first solve uses $A - 2I = \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix}$, which is invertible, and the iteration proceeds. Rubric: full credit for $2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 6 (medium, numeric).**

After power iteration has nearly converged on a symmetric matrix, the eigenvalue estimate $\rho_k$ converges to $\lambda_1$ with error $O(|\lambda_2/\lambda_1|^{2k})$. If $|\lambda_2/\lambda_1| = 0.5$, by what factor does the eigenvalue error shrink per step?

<details class="exercise__hint"><summary>Hint</summary>

The eigenvalue error carries the exponent $2k$, so per step the factor is the square of $|\lambda_2/\lambda_1|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0.25$.** The eigenvector error shrinks by $0.5$ per step, but the symmetric Rayleigh-quotient eigenvalue error shrinks by $0.5^2 = 0.25$ per step, because the Rayleigh quotient is stationary at the eigenvector and its error is second order in the eigenvector error. Rubric: full credit for $0.25$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Prove that for a self-adjoint $A$ with simple eigenvalue $\lambda$ and unit eigenvector $u$, if $x = (\cos\theta) u + (\sin\theta) z$ with $z \perp u$, $\|z\| = 1$, then $R(x) - \lambda = O(\sin^2\theta)$.

<details class="exercise__hint"><summary>Hint</summary>

Expand $R(x) = \langle A x, x \rangle$ for the unit vector $x$, using $A u = \lambda u$ and self-adjointness to drop the cross terms.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Since $\|x\| = 1$, $R(x) = \langle A x, x \rangle$. Expand:
$$
\langle A x, x \rangle = \cos^2\theta\, \langle A u, u \rangle + 2\cos\theta\sin\theta\, \mathrm{Re}\langle A u, z\rangle + \sin^2\theta\, \langle A z, z\rangle.
$$
Now $\langle A u, z\rangle = \lambda \langle u, z\rangle = 0$ because $z \perp u$, so the cross term vanishes — this uses $A u = \lambda u$. Hence $R(x) = \lambda \cos^2\theta + \langle A z, z\rangle \sin^2\theta = \lambda + (\langle A z, z\rangle - \lambda)\sin^2\theta$, using $\cos^2\theta = 1 - \sin^2\theta$. The factor $\langle A z, z\rangle - \lambda$ is bounded by $\|A\| + |\lambda|$, so $R(x) - \lambda = O(\sin^2\theta)$. The vanishing of the cross term — a consequence of self-adjointness — is exactly why the Rayleigh-quotient error is second order. Rubric: full credit for the expansion, the vanishing cross term, and the $O(\sin^2\theta)$ conclusion.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Deflation: let $A$ be self-adjoint with dominant eigenpair $(\lambda_1, u_1)$, $\|u_1\| = 1$, already found. Show that $A' = A - \lambda_1 u_1 u_1^*$ has the same eigenpairs as $A$ except that the eigenvalue at $u_1$ is moved to $0$, so power iteration on $A'$ finds $\lambda_2$.

<details class="exercise__hint"><summary>Hint</summary>

Apply $A'$ to $u_1$ and to any eigenvector $u_j$ orthogonal to $u_1$, using the orthonormality of the eigenbasis.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The self-adjoint $A$ has an orthonormal eigenbasis $u_1, \ldots, u_n$ with $A u_i = \lambda_i u_i$ and $\langle u_i, u_j\rangle = \delta_{ij}$. Apply $A' = A - \lambda_1 u_1 u_1^*$. On $u_1$: $A' u_1 = A u_1 - \lambda_1 u_1 (u_1^* u_1) = \lambda_1 u_1 - \lambda_1 u_1 = 0$, so $u_1$ now has eigenvalue $0$. On $u_j$ with $j \geq 2$: since $u_1^* u_j = \langle u_j, u_1\rangle = 0$, $A' u_j = A u_j - \lambda_1 u_1 (u_1^* u_j) = \lambda_j u_j - 0 = \lambda_j u_j$, so every other eigenpair is unchanged. Thus the spectrum of $A'$ is $\{0, \lambda_2, \ldots, \lambda_n\}$ with the same eigenvectors. If $|\lambda_2| > |\lambda_3|$ and $|\lambda_2| > 0$, the dominant eigenvalue of $A'$ is $\lambda_2$, so power iteration on $A'$ converges to $u_2$ and recovers $\lambda_2$. Rubric: full credit for both cases of the action of $A'$ and the conclusion that $\lambda_2$ is now dominant.

</details>

</aside>

## Advanced results [Master]

**Theorem (inverse iteration converges to the eigenvalue nearest the shift; Wilkinson Ch. 9 [ref: TODO_REF]; Trefethen-Bau Lecture 27 [ref: TODO_REF]).** *Let $A \in M_n(\mathbb{F})$ be diagonalisable with eigenpairs $(\lambda_i, u_i)$, and let $\mu \notin \sigma(A)$ be a shift such that a unique eigenvalue $\lambda_J$ minimises $|\lambda_i - \mu|$, with the runner-up distance $\delta = \min_{i \neq J} |\lambda_i - \mu| > |\lambda_J - \mu|$. For any start vector with nonzero $u_J$-component, inverse iteration $x_k = (A - \mu I)^{-1} x_{k-1} / \|\cdots\|$ satisfies*

$$
\sin\angle(x_k, u_J) = O\!\left( \left( \frac{|\lambda_J - \mu|}{\delta} \right)^k \right).
$$

The mechanism is the eigenvalue transformation $\lambda_i \mapsto (\lambda_i - \mu)^{-1}$, which makes the eigenvalue nearest $\mu$ the dominant eigenvalue of $(A - \mu I)^{-1}$; the convergence theorem of the Intermediate tier then applies verbatim to $B = (A - \mu I)^{-1}$. The closer the shift is to $\lambda_J$, the smaller the ratio $|\lambda_J - \mu|/\delta$ and the faster the convergence — the practical reason inverse iteration is the standard tool for refining an eigenvector once an approximate eigenvalue is known. The system $(A - \mu I) y_k = x_{k-1}$ becomes severely ill-conditioned as $\mu \to \lambda_J$, yet Wilkinson's analysis shows the computed $y_k$ points accurately in the direction of $u_J$: the large error introduced by the near-singular solve lies almost entirely along $u_J$ itself, so after normalisation it does no harm.

**Theorem (cubic convergence of Rayleigh quotient iteration for self-adjoint $A$; Parlett §4.6 [ref: TODO_REF]).** *Let $A = A^*$ have a simple eigenvalue $\lambda$ with unit eigenvector $u$. There is a neighbourhood of $u$ such that, started from any $x_0$ in it with $\sin\angle(x_0, u)$ small, Rayleigh quotient iteration produces iterates with*

$$
\sin\angle(x_{k+1}, u) = O\!\left( \sin^3\angle(x_k, u) \right).
$$

*For a general (non-normal) diagonalisable $A$ the convergence is quadratic, $\sin\angle(x_{k+1}, u) = O(\sin^2\angle(x_k, u))$.*

Cubic convergence means the number of correct digits roughly triples per step; in practice Rayleigh quotient iteration on a symmetric matrix reaches machine precision in three or four iterations once it enters the basin of attraction. The cube — rather than the square — is bought by the second-order accuracy of the Rayleigh quotient: when $A = A^*$, the shift error $|\mu_k - \lambda| = O(\sin^2\angle(x_k, u))$ is one power smaller than for a generic shift, and inverse iteration with that shift contracts the eigenvector error by an extra factor of the shift error, compounding the second-order shift accuracy with the first-order inverse-iteration contraction to give the third power.

**Theorem (deflation and the full symmetric spectrum).** *Let $A = A^*$ with orthonormal eigenbasis $u_1, \ldots, u_n$ and eigenvalues ordered by modulus. Having computed $(\lambda_1, u_1)$, the deflated operator $A^{(1)} = A - \lambda_1 u_1 u_1^*$ has spectrum $\{0\} \cup \{\lambda_2, \ldots, \lambda_n\}$ with the same eigenvectors; iterating the deflation recovers the entire spectrum one eigenpair at a time.* The deflation is the Hermitian rank-one correction that annihilates the found eigenvalue while preserving orthogonal eigendirections, and it is the conceptual seed of the block and subspace methods that compute several eigenpairs at once. Numerically, explicit deflation accumulates rounding error across eigenpairs, which is why the production algorithm of the section is instead the QR algorithm of [43.06.03], computing the full spectrum simultaneously and stably.

**Theorem (shifted power iteration and spectral relocation).** *For any scalar $s$, the eigenvalues of $A - sI$ are $\lambda_i - s$ with unchanged eigenvectors. Choosing $s$ to maximise the gap $|\lambda_1 - s|/|\lambda_2 - s|$ accelerates power iteration without changing the target eigenvector; for a self-adjoint $A$ with spectrum in $[\alpha, \beta]$ and dominant $\lambda_1 = \beta$, the optimal real shift is $s = (\alpha + \lambda_2)/2$, centring the unwanted spectrum about zero.* This spectral-relocation principle is the bridge from the single-vector methods of this unit to polynomial acceleration: replacing $A$ by a polynomial $p(A)$ chosen to amplify the wanted eigenvalue and damp the rest is the idea behind Chebyshev acceleration and, ultimately, the Krylov methods of chapter $43.07$.

**Synthesis.** The four algorithms of this unit are one idea viewed through successively sharper lenses, and the spectral expansion $A^k x_0 = \sum_i c_i \lambda_i^k u_i$ is the foundational reason they work: power iteration amplifies the eigenvalue of largest modulus, inverse iteration reshapes the spectrum by $\lambda_i \mapsto (\lambda_i - \mu)^{-1}$ so that the eigenvalue nearest a chosen shift becomes dominant, and Rayleigh quotient iteration closes the loop by feeding the current Rayleigh-quotient estimate back as the shift. This is exactly where the variational theory of [01.01.14] pays off: the Rayleigh quotient is the optimal eigenvalue estimate at an approximate eigenvector, stationary there, so for a self-adjoint operator its error is second order in the eigenvector error, and that second-order shift accuracy compounds with the first-order contraction of inverse iteration to produce cubic convergence. The convergence rate $|\lambda_2/\lambda_1|$ that governs power iteration generalises to the gap ratio of any reshaped spectrum, and the same ratio appears again as the engine of the QR algorithm, which is simultaneous orthonormal power iteration.

Putting these together, the single-vector eigenvalue algorithms occupy the place between the static spectral theory — eigenvalues as roots, as critical values of the Rayleigh quotient, as min-max over subspaces — and the production eigensolvers: the central insight is that a good eigenvalue algorithm is a dynamical system whose fixed points are the eigenvectors and whose contraction rate is set by how sharply the targeted eigenvalue can be made to dominate the rest. Deflation and shifting are the two levers on that domination, and the bridge is the recognition that the QR algorithm and the Krylov methods are these same levers applied to a whole basis at once.

## Full proof set [Master]

**Proposition (power iteration, full statement and rate).** *Under the hypotheses of the Intermediate theorem — $A$ diagonalisable, $|\lambda_1| > |\lambda_2| \geq \cdots$, start vector $x_0 = \sum_i c_i u_i$ with $c_1 \neq 0$ — the normalised iterates satisfy $\sin\angle(x_k, u_1) \leq C |\lambda_2/\lambda_1|^k$ for a constant $C$ depending only on the $c_i$ and the eigenbasis conditioning.*

**Proof.** From $A^k x_0 = c_1 \lambda_1^k (u_1 + r_k)$ with $r_k = \sum_{i \geq 2} (c_i/c_1)(\lambda_i/\lambda_1)^k u_i$, decompose any unit iterate $x_k$ as its $u_1$-component plus an orthogonal remainder. Writing $P_1$ for the spectral projection onto $u_1$ along the other eigenvectors, $x_k$ is the unit vector in the direction of $u_1 + r_k$, so

$$
\sin\angle(x_k, u_1) = \frac{\|(I - P_1)(u_1 + r_k)\|}{\|u_1 + r_k\|} = \frac{\|(I - P_1) r_k\|}{\|u_1 + r_k\|},
$$

since $(I - P_1) u_1 = 0$. The numerator is at most $\|I - P_1\|\,\|r_k\|$, and $\|r_k\| \leq C_0 |\lambda_2/\lambda_1|^k$ from the Intermediate bound. For $k$ large, $\|u_1 + r_k\| \geq 1 - \|r_k\| \geq 1/2$. Hence $\sin\angle(x_k, u_1) \leq 2\|I - P_1\| C_0 |\lambda_2/\lambda_1|^k = C|\lambda_2/\lambda_1|^k$. The projection norm $\|I - P_1\|$ is the eigenbasis conditioning, equal to $1$ when $A$ is normal and larger for non-normal $A$. $\square$

**Proposition (inverse iteration rate).** *With $\mu \notin \sigma(A)$ and a unique nearest eigenvalue $\lambda_J$, inverse iteration converges to $u_J$ at rate $|\lambda_J - \mu|/\delta$, where $\delta = \min_{i \neq J}|\lambda_i - \mu|$.*

**Proof.** Set $B = (A - \mu I)^{-1}$. By the computation $B u_i = (\lambda_i - \mu)^{-1} u_i$, the eigenvalues of $B$ are $\beta_i = (\lambda_i - \mu)^{-1}$ with the eigenvectors of $A$. The largest in modulus is $\beta_J$ because $|\beta_i| = 1/|\lambda_i - \mu|$ is largest when $|\lambda_i - \mu|$ is smallest, which the hypothesis pins to $i = J$. The second-largest is $1/\delta$. Inverse iteration is power iteration on $B$, so the previous proposition gives $\sin\angle(x_k, u_J) \leq C |\beta_2(B)/\beta_1(B)|^k = C(|\lambda_J - \mu|/\delta)^k$, where $\beta_1(B) = \beta_J$ and $\beta_2(B)$ is the runner-up. The uniqueness of the nearest eigenvalue ensures $B$ has a strictly dominant eigenvalue, the condition the power-iteration proposition requires. $\square$

**Proposition (cubic convergence of Rayleigh quotient iteration, self-adjoint case).** *Let $A = A^*$ have simple eigenvalue $\lambda$ with unit eigenvector $u$ and spectral gap $\gamma = \mathrm{dist}(\lambda, \sigma(A) \setminus \{\lambda\}) > 0$. For $x_k$ with $\theta_k = \angle(x_k, u)$ small, the Rayleigh quotient iteration satisfies $\sin\theta_{k+1} = O(\sin^3\theta_k)$.*

**Proof.** Write $x_k = (\cos\theta_k) u + (\sin\theta_k) z_k$ with $z_k \perp u$, $\|z_k\| = 1$. By the self-adjoint Rayleigh-quotient expansion (Exercise 7), $\mu_k = R(x_k) = \lambda + (\langle A z_k, z_k\rangle - \lambda)\sin^2\theta_k$, so the shift error is

$$
|\mu_k - \lambda| \leq (\|A\| + |\lambda|)\sin^2\theta_k = O(\sin^2\theta_k).
$$

The next iterate is $x_{k+1} \propto (A - \mu_k I)^{-1} x_k$. Decompose along $u$ and $u^\perp$: applying $(A - \mu_k I)^{-1}$ scales the $u$-component $\cos\theta_k$ by $(\lambda - \mu_k)^{-1}$ and the $z_k$-component $\sin\theta_k$ by an operator of norm at most $(\gamma - |\mu_k - \lambda|)^{-1} = O(1)$, because $A$ restricted to the invariant subspace $u^\perp$ has spectrum at distance $\geq \gamma$ from $\lambda$ and hence $\geq \gamma - |\mu_k - \lambda|$ from $\mu_k$. Therefore

$$
\tan\theta_{k+1} = \frac{\|\text{component} \perp u\|}{\|\text{component along } u\|} \leq \frac{O(1)\,\sin\theta_k}{|\lambda - \mu_k|^{-1}\cos\theta_k} = O(|\mu_k - \lambda|)\,\tan\theta_k = O(\sin^2\theta_k)\,\tan\theta_k.
$$

Since $\tan\theta_k = O(\sin\theta_k)$ for small angles, $\tan\theta_{k+1} = O(\sin^3\theta_k)$, and $\sin\theta_{k+1} \leq \tan\theta_{k+1} = O(\sin^3\theta_k)$. Self-adjointness enters twice: to make $u^\perp$ an $A$-invariant orthogonal complement, and to make $\mu_k - \lambda$ second order in $\theta_k$. For non-normal $A$ the shift error is only first order, $|\mu_k - \lambda| = O(\sin\theta_k)$, and the same argument yields the quadratic rate $\sin\theta_{k+1} = O(\sin^2\theta_k)$. $\square$

**Proposition (deflation preserves the orthogonal spectrum, self-adjoint case).** *For $A = A^*$ with unit eigenvector $u_1$ and eigenvalue $\lambda_1$, the operator $A' = A - \lambda_1 u_1 u_1^*$ is self-adjoint with $A' u_1 = 0$ and $A' u_j = \lambda_j u_j$ for every eigenvector $u_j \perp u_1$.*

**Proof.** The correction $\lambda_1 u_1 u_1^*$ is Hermitian ($\lambda_1$ real, $(u_1 u_1^*)^* = u_1 u_1^*$), so $A'$ is self-adjoint. The eigenbasis of $A$ is orthonormal, so for $j \geq 2$, $u_1^* u_j = \langle u_j, u_1\rangle = 0$, giving $A' u_j = A u_j - \lambda_1 u_1 (u_1^* u_j) = \lambda_j u_j$. On $u_1$, $A' u_1 = \lambda_1 u_1 - \lambda_1 u_1 (u_1^* u_1) = \lambda_1 u_1 - \lambda_1 u_1 = 0$ since $\|u_1\| = 1$. The spectrum is therefore $\{0, \lambda_2, \ldots, \lambda_n\}$ with eigenvectors unchanged. $\square$

## Connections [Master]

The single-vector eigenvalue algorithms are the dynamical counterpart of the variational eigenvalue theory of [01.01.14]: where the Rayleigh quotient there is characterised statically as the optimal eigenvalue estimate and the critical values of $R$, here it is the adaptive shift that drives Rayleigh quotient iteration, and its second-order stationarity at an eigenvector is precisely what upgrades inverse iteration from linear to cubic convergence in the self-adjoint case.

Power iteration is the convergence engine of the QR algorithm of [43.06.03]: the unshifted QR iteration is simultaneous power iteration applied to a full orthonormal basis rather than a single vector, and the Wilkinson shift that gives the QR algorithm its cubic local convergence on symmetric tridiagonals is the Rayleigh-quotient shift of this unit transplanted into the orthogonal-similarity setting.

The inverse-iteration step $(A - \mu I) y_k = x_{k-1}$ is a linear solve with a fixed, reused matrix, so its efficiency rests on the direct factorisations of the chapter — Gaussian elimination and the LU factorisation are computed once and applied at every iteration; the preliminary reduction to Hessenberg or tridiagonal form of [43.06.02] is what makes each of those solves cheap, which is why the practical pipeline reduces first and iterates second.

The spectral-relocation idea — replacing $A$ by $A - sI$ or by a polynomial $p(A)$ to reshape which eigenvalue dominates — is the seed of the Krylov-subspace methods, where the iterate lies in $\mathrm{span}\{b, Ab, \ldots, A^{m-1}b\}$ and a polynomial in $A$ is chosen to amplify the wanted part of the spectrum; the conjugate gradient and GMRES convergence bounds rest on exactly the polynomial-acceleration principle introduced here in its single-vector form.

## Historical & philosophical context [Master]

The power method entered numerical practice through the 1929 paper of von Mises and Pollaczek-Geiringer, who set out the *Potenzmethode* as a systematic iterative procedure for the dominant eigenvalue of the matrices arising in elasticity and structural mechanics, where the characteristic polynomial was intractable to solve directly [ref: TODO_REF von Mises 1929]. The idea of repeatedly applying a linear operator to expose its dominant mode was older in the analytic theory of integral equations, but von Mises gave it the matrix-iteration form that the digital era inherited.

The shifted inverse variant is due to Helmut Wielandt, who in a 1944 Göttingen aerodynamics report introduced *gebrochene Iteration* — fractional iteration — applying $(A - \mu I)^{-1}$ to target an eigenvalue near a chosen point $\mu$, the technique now called inverse iteration or Wielandt iteration [ref: TODO_REF Wielandt 1944]. Wilkinson, in *The Algebraic Eigenvalue Problem* (1965), supplied the rounding-error analysis that resolved the apparent paradox of the method: the linear system $(A - \mu I)y = x$ becomes arbitrarily ill-conditioned as the shift approaches an eigenvalue, yet the computed solution is an excellent eigenvector, because the large error is aligned with the very eigenvector being sought [ref: TODO_REF Wilkinson 1965]. The Rayleigh quotient as a self-correcting shift, and the resulting cubic convergence for symmetric matrices, was analysed in detail by Parlett, whose *The Symmetric Eigenvalue Problem* established the cubic local rate and its dependence on the second-order accuracy of the Rayleigh quotient [ref: TODO_REF Parlett 1998]. These single-vector methods were the state of the art for the dominant eigenpair until the QR algorithm of Francis and Kublanovskaya (1961) made the full spectrum computable at comparable cost.

## Bibliography [Master]

```bibtex
@article{vonMises1929,
  author  = {von Mises, Richard and Pollaczek-Geiringer, Hilda},
  title   = {Praktische Verfahren der Gleichungsaufl{\"o}sung},
  journal = {Zeitschrift f{\"u}r Angewandte Mathematik und Mechanik},
  volume  = {9},
  year    = {1929},
  pages   = {58--77, 152--164}
}

@techreport{Wielandt1944,
  author      = {Wielandt, Helmut},
  title       = {Beitr{\"a}ge zur mathematischen Behandlung komplexer Eigenwertprobleme},
  institution = {Aerodynamische Versuchsanstalt G{\"o}ttingen},
  number      = {Bericht B 44/J/37},
  year        = {1944}
}

@book{Wilkinson1965,
  author    = {Wilkinson, James H.},
  title     = {The Algebraic Eigenvalue Problem},
  publisher = {Oxford University Press},
  address   = {Oxford},
  year      = {1965}
}

@book{Parlett1998,
  author    = {Parlett, Beresford N.},
  title     = {The Symmetric Eigenvalue Problem},
  publisher = {SIAM},
  series    = {Classics in Applied Mathematics},
  year      = {1998}
}

@book{TrefethenBau1997,
  author    = {Trefethen, Lloyd N. and Bau, David},
  title     = {Numerical Linear Algebra},
  publisher = {SIAM},
  address   = {Philadelphia},
  year      = {1997}
}

@book{GolubVanLoan2013,
  author    = {Golub, Gene H. and Van Loan, Charles F.},
  title     = {Matrix Computations},
  edition   = {4th},
  publisher = {Johns Hopkins University Press},
  address   = {Baltimore},
  year      = {2013}
}

@article{Francis1961,
  author  = {Francis, John G. F.},
  title   = {The QR Transformation, I},
  journal = {The Computer Journal},
  volume  = {4},
  number  = {3},
  year    = {1961},
  pages   = {265--271}
}
```
