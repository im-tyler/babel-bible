---
id: 44.03.04
title: "Quasi-Newton Methods: BFGS, DFP, and SR1"
slug: quasi-newton-bfgs-dfp-sr1
section: optimization-control
chapter: unconstrained-optimization
concept_catalog_id: optimization-control.quasi-newton-bfgs-dfp-sr1
prerequisites:
  - 44.03.03  # Newton's method (the exact-Hessian solve quasi-Newton approximates)
  - 44.03.01  # line search, Wolfe conditions (curvature condition gives sᵀy > 0)
  - 01.01.13  # spectral theorem / symmetric positive-definite operators
successors: []
tier_anchors:
  beginner: "Nocedal & Wright 2006 *Numerical Optimization* 2e (Springer) §2.2 and §6.1 (the quasi-Newton idea: build a curvature model from successive gradients instead of computing second derivatives, the secant picture in one dimension as the slope of the gradient between two points); Boyd & Vandenberghe 2004 *Convex Optimization* (Cambridge) §9.7 (the quasi-Newton method as a cheap Newton surrogate)"
  intermediate: "Nocedal & Wright 2006 *Numerical Optimization* 2e (Springer) §6.1 (the secant equation $B_{k+1}s_k=y_k$, the curvature condition $s_k^\\top y_k>0$, the DFP and BFGS rank-two updates and their inverse forms via Sherman-Morrison-Woodbury, hereditary positive-definiteness); Dennis & Schnabel 1996 *Numerical Methods for Unconstrained Optimization and Nonlinear Equations* (SIAM) §9.1-9.2 (the least-change secant-update derivation, the Broyden class, and the local superlinear-convergence theory)"
  master: "Nocedal & Wright 2006 *Numerical Optimization* 2e (Springer) §6.1-6.4 and §3.4 (the Broyden class, SR1 and its indefiniteness handling, the Dennis-Moré characterisation of superlinear convergence, the self-correcting property of BFGS, and the global-convergence theorem for BFGS with Wolfe line search); Dennis & Schnabel 1996 *Numerical Methods for Unconstrained Optimization and Nonlinear Equations* (SIAM) §9; Dennis & Moré 1977 *Quasi-Newton methods, motivation and theory* (SIAM Review)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Nocedal, J. & Wright, S. J. — Numerical Optimization (2nd ed.)"
    locator: "Springer Series in Operations Research and Financial Engineering (2006). Chapter 6 'Quasi-Newton Methods', §6.1-6.4: quasi-Newton methods replace the exact Hessian solve $\\nabla^2 f(x_k)p=-\\nabla f(x_k)$ of Newton's method by maintaining an approximation $B_k\\approx\\nabla^2 f(x_k)$ (or its inverse $H_k\\approx\\nabla^2 f(x_k)^{-1}$) updated from gradient differences. The secant equation $B_{k+1}s_k=y_k$ with $s_k=x_{k+1}-x_k$, $y_k=\\nabla f(x_{k+1})-\\nabla f(x_k)$, enforcing that $B_{k+1}$ reproduces the average curvature along the last step; the curvature condition $s_k^\\top y_k>0$ required for a positive-definite secant update, guaranteed by the Wolfe line search. The DFP update $B_{k+1}=(I-\\rho_k y_k s_k^\\top)B_k(I-\\rho_k s_k y_k^\\top)+\\rho_k y_k y_k^\\top$ with $\\rho_k=1/(y_k^\\top s_k)$ and the BFGS update $B_{k+1}=B_k-\\frac{B_k s_k s_k^\\top B_k}{s_k^\\top B_k s_k}+\\frac{y_k y_k^\\top}{y_k^\\top s_k}$, dual under the $B\\leftrightarrow H$, $s\\leftrightarrow y$ interchange; the inverse-Hessian forms $H_{k+1}^{\\mathrm{BFGS}}=(I-\\rho_k s_k y_k^\\top)H_k(I-\\rho_k y_k s_k^\\top)+\\rho_k s_k s_k^\\top$ obtained via the Sherman-Morrison-Woodbury formula so each step costs $O(n^2)$ with no linear solve. Hereditary positive-definiteness of BFGS: $B_k\\succ0$ and $s_k^\\top y_k>0\\Rightarrow B_{k+1}\\succ0$. The symmetric rank-one (SR1) update $B_{k+1}=B_k+\\frac{(y_k-B_k s_k)(y_k-B_k s_k)^\\top}{(y_k-B_k s_k)^\\top s_k}$ which can build indefinite approximations and so suits trust-region use but loses positive-definiteness; the safeguard skipping the update when $|(y_k-B_k s_k)^\\top s_k|<r\\|s_k\\|\\|y_k-B_k s_k\\|$. The Broyden class $B_{k+1}^\\phi=(1-\\phi)B_{k+1}^{\\mathrm{BFGS}}+\\phi B_{k+1}^{\\mathrm{DFP}}$. Local superlinear convergence and the Dennis-Moré condition $\\lim_k\\|(B_k-\\nabla^2 f(x^*))s_k\\|/\\|s_k\\|=0$ as the exact characterisation of superlinearity; the self-correcting property of BFGS; global convergence of BFGS with Wolfe line search on convex objectives. Motivation for limited-memory L-BFGS storing only the last $m$ pairs $(s_i,y_i)$."
  - source: TODO_REF
    path: "Dennis, J. E. & Schnabel, R. B. — Numerical Methods for Unconstrained Optimization and Nonlinear Equations"
    locator: "SIAM Classics in Applied Mathematics 16 (1996, reprint of the 1983 Prentice-Hall edition). Chapter 9 'Secant Methods for Unconstrained Minimization' develops the least-change secant-update viewpoint: among all symmetric matrices satisfying the secant equation $B_{k+1}s_k=y_k$, BFGS and DFP are the solutions of a weighted-Frobenius least-change problem $\\min\\|W^{1/2}(B_{k+1}-B_k)W^{1/2}\\|_F$ for an appropriate weight $W$ (the average Hessian for the affine-invariant derivation), and SR1 is the unique symmetric rank-one secant update. The book proves bounded deterioration of the approximation, local q-superlinear convergence of the BFGS and DFP iterations under a Lipschitz Hessian at a strong minimiser, and presents the Dennis-Moré theorem characterising superlinear convergence by the condition that the step asymptotically agrees with the Newton step. The Broyden class for nonlinear-equation rootfinding (the Jacobian-update analogue) is treated in Chapter 8 and specialised to the symmetric optimization setting in Chapter 9."
lean_status: none
lean_mathlib_gap: |
  Mathlib supplies the linear-algebraic and analytic substrate quasi-Newton
  theory rests on — the Fréchet derivative `fderiv` and gradient `gradient`,
  finite-dimensional inner-product spaces, the positive-definiteness predicates
  `Matrix.PosDef` and `InnerProductSpace` positivity, rank-one operators
  `LinearMap` via outer products, the spectral theorem
  `LinearMap.IsSymmetric.orthonormalBasis`, Taylor's theorem with remainder
  `taylor_mean_remainder`, and `LipschitzWith` for the Lipschitz-Hessian
  hypothesis — but it does not package the secant-update layer this unit owns.
  There is no definition of the secant equation `B_{k+1} • s = y`, no DFP or BFGS
  rank-two update operator, no Sherman-Morrison-Woodbury identity stated for the
  inverse-Hessian forms (`Matrix` lacks the rank-one inverse-update lemma in the
  form needed here), and no symmetric rank-one (SR1) update. The hereditary
  positive-definiteness theorem — `B ≻ 0` and `sᵀy > 0` imply `BFGS(B,s,y) ≻ 0` —
  has no Mathlib statement, nor does the curvature-condition consequence
  `sᵀy > 0` from the Wolfe curvature inequality (inherited from the line-search
  module). Most importantly Mathlib has no Dennis-Moré characterisation of
  superlinear convergence `lim ‖(B_k - ∇²f(x*)) s_k‖ / ‖s_k‖ = 0`, no
  bounded-deterioration lemma, no local superlinear-convergence theorem for the
  BFGS or DFP iterations, no self-correcting-property analysis, and no
  global-convergence theorem for BFGS with a Wolfe line search on convex
  objectives. A `Codex.Optimization.QuasiNewton` module would define the secant
  equation, the BFGS/DFP/SR1 updates and their inverse forms, prove hereditary
  positive-definiteness from the spectral theorem and the curvature condition,
  and establish the Dennis-Moré characterisation; the named human reviewer is the
  correctness gate for the superlinear-convergence theory, which Mathlib does not
  assemble.
human_reviewer: "Tyler (pending external numerical-optimization reviewer)"
source_books: [nocedal-wright]
source_curriculum: optimization-control
estimated_time:
  beginner: 20m
  intermediate: 50m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Newton's method is fast because it reads the curvature of the landscape — how the slope changes — and uses it to aim each step. The catch is that reading the curvature means computing a whole table of second derivatives, the Hessian, at every point. For a problem in thousands of variables that table is enormous and expensive, and sometimes you do not even have a formula for it. Quasi-Newton methods keep almost all of Newton's speed while never computing a single second derivative.

The trick is to *learn* the curvature as you go. Every time you take a step, you get to see two gradients: the one where you started and the one where you landed. The way the gradient changed across that step is a free measurement of the curvature along that direction. A quasi-Newton method stores a running guess of the curvature and, after each step, nudges that guess so it agrees with the new measurement. Over a handful of steps the guess sharpens into a good stand-in for the true Hessian, and the method starts taking near-Newton steps.

Think of it like mapping a dark room by walking through it. You cannot see the walls, but each time you move and feel how the floor tilts differently, you update your mental map. You never measure the whole room at once; you refine a picture from the changes you feel step by step. The requirement that the room "curve upward" along each step — so your map stays a sensible bowl rather than a saddle — is supplied by the same line-search rule from the previous unit, which guarantees the measured curvature is positive.

## Visual [Beginner]

Picture a one-dimensional slice again: height against position. Newton fits a parabola using the exact bend at your current point. A quasi-Newton method has no exact bend to use, so instead it draws a parabola whose slope matches the gradient you measured at *both* the old point and the new point. A parabola is pinned down by requiring its slope to pass through those two slope readings — that single requirement is the secant condition, and it forces the parabola's curvature to equal the average rate at which the slope changed between the two points.

![A one-dimensional height-versus-position plot showing two nearby points x_k and x_{k+1} on a curved valley. At each point a short tangent segment shows the measured slope (gradient). A dashed parabola is drawn whose slope agrees with both measured slopes; an annotation marks its curvature as 'rise in slope divided by run', the secant estimate of the second derivative. A side note contrasts this with the true Newton parabola, which would use the exact bend at one point; the two parabolas nearly coincide once the points are close, illustrating that the secant curvature approaches the true curvature.](/img/quasi-newton-bfgs-dfp-sr1-placeholder.svg)

| method | curvature it uses | cost per step | speed near a minimum |
|---|---|---|---|
| steepest descent | none (slope only) | cheapest | slow, zig-zag |
| Newton | exact Hessian | expensive (build + solve) | digits double |
| quasi-Newton | learned from gradient changes | cheap (no second derivatives) | digits almost double |

The takeaway: by matching the slope at two points instead of the exact bend at one, a quasi-Newton method estimates curvature for free from gradients it already had to compute, and refines that estimate every step.

## Worked example [Beginner]

Take the simple bowl $f(x) = \tfrac12\,c\,x^2$ in one variable, whose true curvature is the number $c$. Suppose $c = 4$ but pretend we do not know it; we will recover it from two gradient readings. The gradient (slope) is $f'(x) = 4x$.

Start at $x_0 = 3$. The slope there is $f'(3) = 12$. Take a step to $x_1 = 1$. The slope there is $f'(1) = 4$. Now form the two step quantities a quasi-Newton method records: how far we moved, $s = x_1 - x_0 = 1 - 3 = -2$, and how much the slope changed, $y = f'(x_1) - f'(x_0) = 4 - 12 = -8$.

The secant estimate of the curvature is the change in slope divided by the distance moved:
$$
B = \frac{y}{s} = \frac{-8}{-2} = 4.
$$
That is exactly the true curvature $c = 4$. On a perfect bowl, two gradient readings pin the curvature down precisely, so the learned model becomes the true Newton model after a single step.

Check the curvature is positive the way the line-search rule guarantees: $s \cdot y = (-2)(-8) = 16 > 0$. A positive product means the slope rose as we moved in the positive direction (or fell as we moved in the negative direction), the signature of a curve that bends upward — a sensible bowl to step into.

What this tells us: the pair $(s, y)$ of "distance moved" and "slope change" carries the curvature, and dividing one by the other recovers it. In many variables you cannot simply divide, so the update rules of this unit are the careful matrix version of this same one-number idea, and the positive product $s \cdot y > 0$ is what keeps the matrix a genuine bowl.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: a quasi-Newton method estimates the curvature of the function from the way the gradient changes between successive points, instead of computing second derivatives directly.

<details class="exercise__hint"><summary>Hint</summary>

Recall what two pieces of information each step hands you for free.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Each step gives you the gradient at the old point and at the new point; their difference, compared with how far you moved, is a free measurement of curvature. The method updates a running curvature model to match that measurement, so it never needs the exact second derivatives. Feedback-correct: gradient changes are the raw material of the curvature estimate. Feedback-wrong: if you said false, recall that the whole point is to avoid computing the Hessian directly.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple-choice).**

What is the main practical advantage of a quasi-Newton method over Newton's method?

<ol class="exercise__options" type="A">
<li>It always finds the global minimum.</li>
<li>It never needs a line search.</li>
<li>It avoids computing and solving with the exact Hessian, so each step is cheaper.</li>
<li>It converges in exactly one step on any function.</li>
</ol>

<details class="exercise__hint"><summary>Hint</summary>

Think about what Newton has to compute at every point that a quasi-Newton method learns instead.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** Newton must form the table of second derivatives and solve a linear system with it each step; quasi-Newton learns a curvature model from gradients already in hand, skipping both. Feedback-correct: cheaper steps with near-Newton speed is the trade. Feedback-wrong: A and D overclaim; B is false — quasi-Newton methods still use a line search to globalise.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

For $f(x) = \tfrac12\,c\,x^2$ you measure the slope $f'(5) = 10$ and, after stepping to $x = 2$, the slope $f'(2) = 4$. Using the secant estimate "slope change divided by distance moved", what curvature value $B = y/s$ do you recover?

<details class="exercise__hint"><summary>Hint</summary>

Compute $s = 2 - 5$ and $y = 4 - 10$, then divide $y$ by $s$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$2$.** The distance moved is $s = 2 - 5 = -3$ and the slope change is $y = 4 - 10 = -6$, so $B = y/s = (-6)/(-3) = 2$. (Indeed $f'(x) = 2x$ here, so $c = 2$.) Feedback-correct: dividing the slope change by the distance recovers the curvature. Feedback-wrong: if you got $-2$ you dropped a sign; if you got $6$ you divided distance by slope change.

</details>

</aside>

## Formal definition [Intermediate+]

Let $f : \mathbb{R}^n \to \mathbb{R}$ be twice continuously differentiable with gradient $g(x) = \nabla f(x)$. A **quasi-Newton method** is a line-search method [44.03.01] $x_{k+1} = x_k + \alpha_k p_k$ whose search direction $p_k = -H_k\, g_k$ uses a symmetric positive-definite matrix $H_k$ approximating the inverse Hessian $\nabla^2 f(x_k)^{-1}$, equivalently $p_k$ solves $B_k p_k = -g_k$ with $B_k = H_k^{-1} \approx \nabla^2 f(x_k)$. After each step define the **secant pair**
$$
s_k = x_{k+1} - x_k, \qquad y_k = g_{k+1} - g_k .
$$
The defining requirement is the **secant equation**
$$
B_{k+1}\, s_k = y_k, \qquad\text{equivalently}\qquad H_{k+1}\, y_k = s_k,
$$
which forces the updated model to reproduce the gradient change across the last step: it is the multivariable form of "average curvature equals slope-change over displacement", since by Taylor's theorem [02.05.05] $y_k = \big(\int_0^1 \nabla^2 f(x_k + t s_k)\,dt\big) s_k$, so $B_{k+1}$ matches the *average Hessian* along the segment. The secant equation admits a symmetric positive-definite solution only when the **curvature condition**
$$
s_k^\top y_k > 0
$$
holds; this is guaranteed at every iteration by the Wolfe curvature condition of the line search [44.03.01], since $s_k^\top y_k = \alpha_k\,p_k^\top(g_{k+1} - g_k) \ge \alpha_k(1 - c_2)(-p_k^\top g_k) > 0$.

The secant equation underdetermines $B_{k+1}$ (it is $n$ equations on a symmetric $n\times n$ matrix), so a quasi-Newton method selects the update by a least-change principle. Writing $\rho_k = 1/(y_k^\top s_k)$, the two classical rank-two updates are the **DFP** (Davidon-Fletcher-Powell) update of the Hessian approximation
$$
B_{k+1}^{\mathrm{DFP}} = (I - \rho_k\, y_k s_k^\top)\,B_k\,(I - \rho_k\, s_k y_k^\top) + \rho_k\, y_k y_k^\top,
$$
and the **BFGS** (Broyden-Fletcher-Goldfarb-Shanno) update
$$
B_{k+1}^{\mathrm{BFGS}} = B_k - \frac{B_k s_k s_k^\top B_k}{s_k^\top B_k s_k} + \frac{y_k y_k^\top}{y_k^\top s_k}.
$$
These are **dual**: interchanging $B \leftrightarrow H$ and $s \leftrightarrow y$ carries one into the other. Applying the **Sherman-Morrison-Woodbury** formula to the rank-two updates gives the inverse-Hessian forms used in practice (no linear solve, $O(n^2)$ per step): the BFGS inverse update is
$$
H_{k+1}^{\mathrm{BFGS}} = (I - \rho_k\, s_k y_k^\top)\,H_k\,(I - \rho_k\, y_k s_k^\top) + \rho_k\, s_k s_k^\top,
$$
and the DFP inverse update is $H_{k+1}^{\mathrm{DFP}} = H_k - \frac{H_k y_k y_k^\top H_k}{y_k^\top H_k y_k} + \frac{s_k s_k^\top}{y_k^\top s_k}$.

The **symmetric rank-one (SR1)** update solves the secant equation with a single symmetric rank-one correction,
$$
B_{k+1}^{\mathrm{SR1}} = B_k + \frac{(y_k - B_k s_k)(y_k - B_k s_k)^\top}{(y_k - B_k s_k)^\top s_k},
$$
which exists whenever the denominator is nonzero but is **not** required to be positive definite; SR1 can build an indefinite approximation that tracks the true Hessian's negative curvature, making it natural inside a trust region [44.03.02] rather than a line search. A standard safeguard skips the update when $|(y_k - B_k s_k)^\top s_k| < r\,\|s_k\|\,\|y_k - B_k s_k\|$ for a small $r$ (say $r = 10^{-8}$). The **Broyden class** interpolates the two rank-two updates,
$$
B_{k+1}^{\phi} = (1 - \phi)\,B_{k+1}^{\mathrm{BFGS}} + \phi\,B_{k+1}^{\mathrm{DFP}}, \qquad \phi \in \mathbb{R},
$$
with $\phi = 0$ giving BFGS, $\phi = 1$ giving DFP, and $\phi \in [0,1]$ the *convex* (restricted) Broyden class. The new symbols $s_k$, $y_k$ (secant pair), $B_k$, $H_k$ (Hessian and inverse-Hessian approximations), $\rho_k$, and the SR1/Broyden updates are recorded in `_meta/NOTATION.md`.

### Counterexamples to common slips [Intermediate+]

- *"Any symmetric matrix solving the secant equation will do."* The secant equation is satisfied by infinitely many symmetric matrices when $n > 1$; without a least-change selection the iteration can drift to an approximation unrelated to the Hessian. DFP and BFGS are the *minimum-change* secant updates in a weighted Frobenius norm, which is what makes them stable.

- *"BFGS needs $s_k^\top y_k > 0$ only for efficiency."* Positivity is necessary for *existence* of a positive-definite secant update: if $s_k^\top y_k \le 0$ no symmetric positive-definite $B_{k+1}$ can satisfy $B_{k+1}s_k = y_k$, because that would give $s_k^\top y_k = s_k^\top B_{k+1} s_k > 0$. The Wolfe line search is therefore not optional flavouring but the mechanism that makes the update well posed.

- *"SR1 is just a cheaper BFGS."* SR1 deliberately abandons positive-definiteness; its denominator can vanish or turn the correction indefinite. It is preferred precisely where indefiniteness is informative — inside a trust region that can exploit negative curvature — and is unsafe as a stand-alone line-search Hessian model without the skipping safeguard.

- *"Quasi-Newton converges quadratically like Newton."* It does not. Because $B_k$ only approximates the Hessian, the rate is *superlinear*, not quadratic; the Dennis-Moré condition characterises exactly when this superlinear rate is achieved, and it requires the approximation to act like the true Hessian along the step directions, not everywhere.

## Key theorem with proof [Intermediate+]

The signature structural result is that the BFGS update propagates positive-definiteness: if the current model is a genuine bowl and the measured curvature is positive, the next model is a genuine bowl too. This is what guarantees the quasi-Newton direction stays a descent direction at every iteration without any explicit safeguard, so the globalisation theory of the previous unit applies unchanged.

**Theorem (hereditary positive-definiteness of BFGS).** *Let $B_k \succ 0$ be symmetric positive definite and let the curvature condition $s_k^\top y_k > 0$ hold. Then the BFGS update*
$$
B_{k+1} = B_k - \frac{B_k s_k s_k^\top B_k}{s_k^\top B_k s_k} + \frac{y_k y_k^\top}{y_k^\top s_k}
$$
*is symmetric positive definite.*

**Proof.** Symmetry is plain since each term is symmetric. Take any $z \ne 0$ and evaluate the quadratic form. Write $a = B_k^{1/2} z$ and $b = B_k^{1/2} s_k$, both well defined because $B_k \succ 0$ has a symmetric positive-definite square root [01.01.13]. Then
$$
z^\top B_k z = a^\top a = \|a\|^2, \qquad z^\top B_k s_k = a^\top b, \qquad s_k^\top B_k s_k = b^\top b = \|b\|^2,
$$
so the middle term contributes
$$
z^\top\!\left(B_k - \frac{B_k s_k s_k^\top B_k}{s_k^\top B_k s_k}\right)\! z = \|a\|^2 - \frac{(a^\top b)^2}{\|b\|^2}.
$$
By the Cauchy-Schwarz inequality $(a^\top b)^2 \le \|a\|^2\|b\|^2$, so this quantity is $\ge 0$, and it equals $0$ only when $a$ and $b$ are parallel, i.e. $z = \lambda s_k$ for some scalar $\lambda \ne 0$. The last term contributes $\dfrac{(y_k^\top z)^2}{y_k^\top s_k} \ge 0$ because $y_k^\top s_k > 0$. Hence
$$
z^\top B_{k+1} z = \underbrace{\left(\|a\|^2 - \frac{(a^\top b)^2}{\|b\|^2}\right)}_{\ge 0} + \underbrace{\frac{(y_k^\top z)^2}{y_k^\top s_k}}_{\ge 0} \ge 0 .
$$
It remains to rule out equality. Suppose $z^\top B_{k+1} z = 0$. Both nonnegative summands must vanish. The first vanishes only if $z = \lambda s_k$, $\lambda \ne 0$; substituting into the second, $y_k^\top z = \lambda\, y_k^\top s_k \ne 0$ because $y_k^\top s_k > 0$ and $\lambda \ne 0$, so the second summand is strictly positive — a contradiction. Therefore $z^\top B_{k+1} z > 0$ for all $z \ne 0$, and $B_{k+1} \succ 0$. $\square$

**Bridge.** This is the foundational reason BFGS needs no descent-direction safeguard: $B_{k+1} \succ 0$ makes $p_{k+1} = -B_{k+1}^{-1} g_{k+1}$ a descent direction automatically, so the Wolfe line search of [44.03.01] always supplies a positive $s_{k+1}^\top y_{k+1}$, which keeps $B_{k+2} \succ 0$, and the invariant propagates for the whole run. The argument builds toward the global-convergence theorem of the Advanced results, where this hereditary positivity is exactly the hypothesis that lets the angle-bounded Zoutendijk machinery apply, and it appears again in the inverse form: the same computation on $H_{k+1}$ shows the inverse update preserves positive-definiteness, which is what makes the $O(n^2)$ direction computation $p_k = -H_k g_k$ legitimate. The central insight is that the curvature condition $s_k^\top y_k > 0$ — itself a gift of the Wolfe line search — is precisely the algebraic hypothesis under which a positive-definite secant update can exist at all; this is exactly the dual of the descent property of Newton's method [44.03.03], where positive-definiteness of the exact Hessian played the same role. Putting these together, the line search owns globalisation and the secant update owns the local model, and the bridge is the curvature condition that ties the two together so the quasi-Newton direction is a descent direction at every step.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Two consecutive iterates give $s = (1, 0)^\top$ and $y = (2, 1)^\top$. Compute the curvature product $s^\top y$ and state whether a positive-definite BFGS update is possible.

<details class="exercise__hint"><summary>Hint</summary>

$s^\top y = s_1 y_1 + s_2 y_2$; positivity of this number is the existence condition.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$s^\top y = 2 > 0$, so yes.** $s^\top y = 1\cdot 2 + 0\cdot 1 = 2$. Since $s^\top y > 0$, the curvature condition holds and a symmetric positive-definite $B_{k+1}$ satisfying $B_{k+1}s = y$ exists, so the BFGS update produces a valid bowl. Full credit for $2$ and "yes".

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

Starting from $B_0 = I$ (the $2\times 2$ identity), with $s = (1,0)^\top$ and $y = (2,0)^\top$, compute the BFGS-updated $B_1$.

<details class="exercise__hint"><summary>Hint</summary>

Use $B_1 = B_0 - \frac{B_0 s s^\top B_0}{s^\top B_0 s} + \frac{y y^\top}{y^\top s}$. Here $B_0 s = s$, $s^\top B_0 s = 1$, $y^\top s = 2$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$B_1 = \operatorname{diag}(2, 1)$.** With $B_0 = I$: the middle term is $\frac{s s^\top}{1} = \begin{bmatrix}1&0\\0&0\end{bmatrix}$; the last term is $\frac{y y^\top}{2} = \frac{1}{2}\begin{bmatrix}4&0\\0&0\end{bmatrix} = \begin{bmatrix}2&0\\0&0\end{bmatrix}$. So $B_1 = I - \begin{bmatrix}1&0\\0&0\end{bmatrix} + \begin{bmatrix}2&0\\0&0\end{bmatrix} = \begin{bmatrix}2&0\\0&1\end{bmatrix}$. Check the secant equation: $B_1 s = (2,0)^\top = y$. Full credit for $\operatorname{diag}(2,1)$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Verify directly that the SR1 update $B_{k+1} = B_k + \frac{(y - Bs)(y - Bs)^\top}{(y - Bs)^\top s}$ satisfies the secant equation $B_{k+1}s = y$ (write $B = B_k$, drop the index $k$).

<details class="exercise__hint"><summary>Hint</summary>

Apply $B_{k+1}$ to $s$ and simplify the scalar factor $\frac{(y-Bs)^\top s}{(y-Bs)^\top s}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $w = y - Bs$. Then
$$
B_{k+1}s = Bs + \frac{w\,(w^\top s)}{w^\top s} = Bs + w\,\frac{w^\top s}{w^\top s} = Bs + w = Bs + (y - Bs) = y,
$$
using that $w^\top s$ is a nonzero scalar so $\frac{w^\top s}{w^\top s} = 1$. Hence the SR1 update reproduces $y$ on $s$. The single rank-one correction is uniquely determined: any symmetric $B + \beta\, uu^\top$ satisfying the secant equation forces $u \propto y - Bs$ and the coefficient $\beta = 1/((y-Bs)^\top s)$. Full credit for the cancellation showing $B_{k+1}s = y$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 4 (medium, numeric).**

With $B_0 = I$ ($2\times 2$), $s = (1,0)^\top$, $y = (2,0)^\top$, compute the SR1-updated $B_1$ and compare with the BFGS result of Exercise 2.

<details class="exercise__hint"><summary>Hint</summary>

$w = y - B_0 s = (2,0)^\top - (1,0)^\top = (1,0)^\top$; then $w^\top s = 1$, $w w^\top = \begin{bmatrix}1&0\\0&0\end{bmatrix}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$B_1^{\mathrm{SR1}} = \operatorname{diag}(2, 1)$, identical to BFGS here.** $w = (1,0)^\top$, $w^\top s = 1$, so $B_1 = I + \frac{1}{1}\begin{bmatrix}1&0\\0&0\end{bmatrix} = \begin{bmatrix}2&0\\0&1\end{bmatrix}$. In this special case ($B_0 = I$ and $y$ a scalar multiple of $s$) SR1 and BFGS coincide, both giving $B_1 s = y$. They diverge in general because BFGS is a rank-two correction that also acts in the $s$-orthogonal directions while SR1 corrects only along $w$. Full credit for $\operatorname{diag}(2,1)$ and noting the coincidence.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Derive the inverse-Hessian BFGS update from the direct update using the Sherman-Morrison-Woodbury identity in the compact form: show that the BFGS update can be written $B_{k+1} = (I - \rho y s^\top)^{-\top}\!\big[\,\cdot\,\big]$ is unnecessary — instead verify that the stated $H_{k+1} = (I - \rho s y^\top)H(I - \rho y s^\top) + \rho s s^\top$ satisfies the secant equation $H_{k+1}y = s$, where $\rho = 1/(y^\top s)$.

<details class="exercise__hint"><summary>Hint</summary>

Apply $H_{k+1}$ to $y$. Note $(I - \rho y s^\top)y = y - \rho y (s^\top y) = y - y = 0$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Compute $(I - \rho y s^\top)y = y - \rho\,y\,(s^\top y) = y - y = 0$ since $\rho\,(s^\top y) = 1$. Therefore the first term $(I - \rho s y^\top)H(I - \rho y s^\top)y = 0$. The second term gives $\rho s s^\top y = s\,(\rho\, s^\top y) = s\cdot 1 = s$. Hence $H_{k+1} y = 0 + s = s$, the inverse secant equation. The Sherman-Morrison-Woodbury content is that this $H_{k+1}$ is exactly $(B_{k+1}^{\mathrm{BFGS}})^{-1}$: applying the rank-two inverse formula to $B_{k+1} = B_k - \frac{B_k s s^\top B_k}{s^\top B_k s} + \frac{y y^\top}{y^\top s}$ produces precisely the displayed congruence-plus-rank-one form, so each step costs $O(n^2)$ with no linear solve. Full credit for $H_{k+1}y = s$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Prove that on a strongly convex quadratic $f(x) = \tfrac12 x^\top A x - b^\top x$ ($A \succ 0$), every secant pair satisfies $B_{k+1}s_k = y_k = A s_k$, so any secant update with $B_0 = A$ gives $B_k = A$ for all $k$; hence the quasi-Newton step equals the Newton step from the first iteration.

<details class="exercise__hint"><summary>Hint</summary>

For a quadratic, $g(x) = Ax - b$, so $y_k = g_{k+1} - g_k = A(x_{k+1} - x_k) = A s_k$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For the quadratic, $g(x) = Ax - b$, so $y_k = g_{k+1} - g_k = A(x_{k+1} - x_k) = A s_k$, i.e. the exact secant pair satisfies $y_k = A s_k$ with the true Hessian $A$. If $B_k = A$ then $B_k s_k = A s_k = y_k$, so $B_k$ already satisfies the secant equation, and the least-change updates (BFGS, DFP, SR1) leave a matrix unchanged when it already satisfies the secant equation: each update's correction term involves $y_k - B_k s_k = 0$ for SR1 (zero correction), and for BFGS/DFP the rank-two terms reduce so that $B_{k+1} = B_k$ when $B_k s_k = y_k$ and the update reproduces $A$ on the relevant subspace. By induction from $B_0 = A$, $B_k = A$ for all $k$, and $p_k = -A^{-1}g_k$ is the Newton step. (More is true: even from $B_0 = I$, BFGS on an $n$-dimensional quadratic with exact line search builds $B_n = A$ and terminates in at most $n$ steps — the conjugate-direction property.) Full credit for $y_k = As_k$ and the fixed-point argument.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

State and motivate the Dennis-Moré condition: prove that if the quasi-Newton step asymptotically agrees with the Newton step, i.e. $\lim_{k\to\infty}\frac{\|(B_k - \nabla^2 f(x^*))s_k\|}{\|s_k\|} = 0$, then near a strong minimiser the unit step $\alpha_k = 1$ yields superlinear convergence, $\|x_{k+1} - x^*\|/\|x_k - x^*\| \to 0$.

<details class="exercise__hint"><summary>Hint</summary>

Write the error recursion using $g_k = \nabla^2 f(x^*)(x_k - x^*) + o(\|x_k - x^*\|)$ and $B_k s_k = -g_k$ (the unit-step quasi-Newton equation), then compare $s_k$ with the Newton step.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $H^* = \nabla^2 f(x^*) \succ 0$ and $e_k = x_k - x^*$. The unit quasi-Newton step solves $B_k s_k = -g_k$, so $s_k = x_{k+1} - x_k$ gives $e_{k+1} = e_k + s_k$. With $g_k = H^* e_k + o(\|e_k\|)$ (Taylor [02.05.05], $g(x^*) = 0$) and $g_{k+1} = H^* e_{k+1} + o(\|e_{k+1}\|)$,
$$
H^* e_{k+1} = g_{k+1} + o(\|e_{k+1}\|) = (g_{k+1} - g_k) + g_k + o = y_k - B_k s_k + o = (\nabla^2 f - B_k)s_k\big|_{\text{avg}} + o,
$$
using $g_k = -B_k s_k$ and $y_k = \overline{H}_k s_k$ for the average Hessian $\overline{H}_k \to H^*$. Thus $\|H^* e_{k+1}\| \le \|(\,\overline{H}_k - B_k)s_k\| + o(\|e_k\|)$, and the Dennis-Moré limit makes $\|(\overline{H}_k - B_k)s_k\|/\|s_k\| \to 0$. Since $\|s_k\|$ is comparable to $\|e_k\|$ near the solution (the steps and errors are of the same order once convergence sets in), dividing by $\|e_k\|$ gives $\|e_{k+1}\|/\|e_k\| \to 0$: superlinear convergence. The condition is the *exact* characterisation — superlinearity holds if and only if the approximation acts like the true Hessian along the step directions, not necessarily everywhere. Full credit for the error recursion and the conclusion that the Dennis-Moré limit forces $\|e_{k+1}\|/\|e_k\| \to 0$.

</details>

</aside>

## Advanced results [Master]

Quasi-Newton methods replace the exact Hessian solve $\nabla^2 f\, p = -g$ of Newton's method [44.03.03] by a secant-updated model, and the theory below organises around three facts: the least-change variational characterisation that singles out BFGS and DFP, the Dennis-Moré characterisation that pins superlinear convergence to step-directional Hessian fidelity, and the self-correcting and global-convergence properties that make BFGS the default unconstrained optimiser.

**Theorem 1 (least-change secant characterisation).** Among all symmetric matrices $B$ satisfying the secant equation $Bs_k = y_k$, the DFP update is the unique minimiser of the weighted Frobenius distance $\|W(B - B_k)W\|_F$ with any symmetric weight $W$ satisfying $W^2 y_k = s_k$ (e.g. $W = \overline{H}_k^{-1/2}$ for the average Hessian $\overline{H}_k = \int_0^1 \nabla^2 f(x_k + ts_k)\,dt$, which makes the derivation affine invariant). Dually, the BFGS update is the unique minimiser of the same weighted-Frobenius least-change problem on the *inverse* approximation $H$, subject to $Hy_k = s_k$, with weight satisfying $W^2 s_k = y_k$. This variational origin explains the stability of both updates: they change the current model as little as the new curvature measurement permits, so a single inaccurate secant pair cannot corrupt the accumulated information [ref: TODO_REF Dennis, J. E. & Schnabel, R. B. — Numerical Methods for Unconstrained Optimization and Nonlinear Equations].

**Theorem 2 (Dennis-Moré superlinear characterisation).** Let $x_k \to x^*$ with $\nabla f(x^*) = 0$, $\nabla^2 f(x^*) \succ 0$, and $\nabla^2 f$ Lipschitz near $x^*$. If the quasi-Newton iteration takes unit steps $x_{k+1} = x_k - B_k^{-1}g_k$ with $\{B_k\}$ nonsingular, then the convergence is q-superlinear *if and only if*
$$
\lim_{k\to\infty}\frac{\big\|\big(B_k - \nabla^2 f(x^*)\big)s_k\big\|}{\|s_k\|} = 0 .
$$
The condition demands only that $B_k$ act like the true Hessian *along the step directions* $s_k$, not that $B_k \to \nabla^2 f(x^*)$; indeed BFGS and DFP need not converge to the true Hessian and yet attain the superlinear rate. This is the precise sense in which quasi-Newton trades Newton's quadratic rate for a superlinear one while shedding second derivatives [ref: TODO_REF Dennis, J. E. & Schnabel, R. B. — Numerical Methods for Unconstrained Optimization and Nonlinear Equations].

**Theorem 3 (self-correcting property and global convergence of BFGS).** On a twice-differentiable convex $f$ whose Hessian is bounded and Lipschitz on the level set $\{f \le f(x_0)\}$, BFGS with a Wolfe line search [44.03.01] from any positive-definite $B_0$ converges to the minimiser, and the rate is superlinear. The mechanism is the **self-correcting property**: if at some iteration $B_k$ has badly distorted eigenvalues (too large or too small), the BFGS update tends to repair them over the next few iterations, because the trace and determinant identities
$$
\operatorname{tr}(B_{k+1}) = \operatorname{tr}(B_k) - \frac{\|B_k s_k\|^2}{s_k^\top B_k s_k} + \frac{\|y_k\|^2}{y_k^\top s_k}, \qquad \det(B_{k+1}) = \det(B_k)\,\frac{y_k^\top s_k}{s_k^\top B_k s_k},
$$
force a Lyapunov-type quantity (the Powell function $\psi(B) = \operatorname{tr}(B) - \ln\det(B)$) to stay controlled, bounding the iterations on which the search direction is poorly scaled. DFP lacks an equally strong self-correcting mechanism, which is the practical reason BFGS is preferred [ref: TODO_REF Nocedal, J. & Wright, S. J. — Numerical Optimization (2nd ed.)].

**Theorem 4 (SR1 and indefiniteness; trust-region convergence to the Hessian).** The SR1 update, when the safeguard $|(y_k - B_k s_k)^\top s_k| \ge r\,\|s_k\|\,\|y_k - B_k s_k\|$ is satisfied, generates approximations that converge to the true Hessian even on nonconvex problems: under uniform linear independence of the steps and a Lipschitz Hessian, $B_k \to \nabla^2 f(x^*)$ for SR1, a stronger conclusion than BFGS gives. Because SR1 does not enforce positive-definiteness, it captures negative curvature and is therefore the natural Hessian model inside a trust-region method [44.03.02], where indefiniteness is handled by the subproblem rather than forbidden. The price is the possibility of a vanishing or sign-changing denominator, which the skipping safeguard controls; skipped updates are rare and do not impede convergence [ref: TODO_REF Nocedal, J. & Wright, S. J. — Numerical Optimization (2nd ed.)].

**Theorem 5 (the Broyden class and quadratic termination).** For the convex Broyden class $B_{k+1}^\phi = (1-\phi)B_{k+1}^{\mathrm{BFGS}} + \phi B_{k+1}^{\mathrm{DFP}}$ with $\phi \in [0,1]$, every member preserves positive-definiteness under the curvature condition and satisfies the secant equation. On a strongly convex quadratic with exact line search, every Broyden-class method generates conjugate directions with respect to the Hessian and therefore terminates at the exact minimiser in at most $n$ steps, building $B_n = \nabla^2 f$ exactly. This quadratic-termination property links quasi-Newton methods to the conjugate-gradient method [44.03.06]: BFGS with exact line search and $B_0 = I$ generates the same iterates as linear conjugate gradient on a quadratic, which is the structural reason limited-memory L-BFGS [44.03.05] behaves like a preconditioned nonlinear CG on large problems [ref: TODO_REF Nocedal, J. & Wright, S. J. — Numerical Optimization (2nd ed.)].

**Synthesis.** Quasi-Newton optimization is one construction — approximate the Hessian by the least-change symmetric matrix satisfying the secant equation $B_{k+1}s_k = y_k$ — and its entire theory descends from a single algebraic gift: the curvature condition $s_k^\top y_k > 0$, supplied free by the Wolfe line search [44.03.01], is exactly the hypothesis under which a positive-definite secant update exists, and this is the foundational reason BFGS needs no descent safeguard while propagating positive-definiteness hereditarily. The central insight is the division of labour inherited from the previous units: the line search owns globalisation, the secant update owns the local curvature model, and the Dennis-Moré condition reveals that superlinear speed requires the model to match the true Hessian only along the step directions — this is exactly the relaxation of Newton's requirement that the *exact* Hessian be used, and it is what buys the dimensionless escape from second derivatives. The BFGS and DFP updates are dual under the $B \leftrightarrow H$, $s \leftrightarrow y$ interchange, and putting these together with the Sherman-Morrison-Woodbury inverse forms gives the $O(n^2)$ per-step cost that is the whole practical point.

The bridge forward is explicit. The same secant idea that here approximates a symmetric Hessian generalises to the nonsymmetric Jacobian of root-finding [43.02.03], from which Broyden's method descends; the conjugate-direction property of the Broyden class is dual to the conjugate-gradient method [44.03.06], so BFGS is a memory-rich CG and L-BFGS [44.03.05] is the memory-limited bridge between them; SR1's tolerance of indefiniteness makes it the Hessian model of choice inside trust-region methods [44.03.02], where the subproblem rather than the line search absorbs negative curvature. What Newton's method [44.03.03] achieves with the exact Hessian, quasi-Newton achieves with a learned one — slightly slower, vastly cheaper — and the learned model is the central object every large-scale solver carries forward.

## Full proof set [Master]

**Proposition 1 (hereditary positive-definiteness of BFGS).** *If $B_k \succ 0$ and $s_k^\top y_k > 0$, then $B_{k+1}^{\mathrm{BFGS}} \succ 0$.*

*Proof.* As in the Key theorem. For $z \ne 0$ set $a = B_k^{1/2}z$, $b = B_k^{1/2}s_k$ (square root exists by the spectral theorem [01.01.13]); then $z^\top B_{k+1} z = (\|a\|^2 - (a^\top b)^2/\|b\|^2) + (y_k^\top z)^2/(y_k^\top s_k)$. Cauchy-Schwarz makes the first summand $\ge 0$ with equality only if $z = \lambda s_k$, $\lambda \ne 0$; in that case $y_k^\top z = \lambda\,y_k^\top s_k \ne 0$ makes the second summand strictly positive. Hence $z^\top B_{k+1}z > 0$. $\square$

**Proposition 2 (curvature condition is necessary for a positive-definite secant update).** *If $B_{k+1} \succ 0$ satisfies $B_{k+1}s_k = y_k$, then $s_k^\top y_k > 0$.*

*Proof.* Left-multiply the secant equation by $s_k^\top$: $s_k^\top y_k = s_k^\top B_{k+1} s_k > 0$ because $B_{k+1} \succ 0$ and $s_k \ne 0$ (else $s_k^\top y_k = 0$ and no step was taken). $\square$

**Proposition 3 (SR1 satisfies the secant equation and is the unique symmetric rank-one secant update).** *With $w_k = y_k - B_k s_k$ and $w_k^\top s_k \ne 0$, the matrix $B_{k+1} = B_k + w_k w_k^\top/(w_k^\top s_k)$ is the unique symmetric matrix of the form $B_k + \beta\,uu^\top$ satisfying $B_{k+1}s_k = y_k$.*

*Proof.* Existence: $B_{k+1}s_k = B_k s_k + w_k (w_k^\top s_k)/(w_k^\top s_k) = B_k s_k + w_k = B_k s_k + (y_k - B_k s_k) = y_k$. Uniqueness: any symmetric rank-one secant update has the form $B_k + \beta uu^\top$ with $(B_k + \beta uu^\top)s_k = y_k$, i.e. $\beta(u^\top s_k)u = y_k - B_k s_k = w_k$. Thus $u$ is parallel to $w_k$, say $u = w_k$ (absorbing scale into $\beta$), and $\beta(w_k^\top s_k) = 1$ forces $\beta = 1/(w_k^\top s_k)$. $\square$

**Proposition 4 (DFP and BFGS are dual).** *The DFP update of $B$ has the same algebraic form as the BFGS update of $H = B^{-1}$ under the interchange $B \leftrightarrow H$, $s \leftrightarrow y$; consequently $(B_{k+1}^{\mathrm{BFGS}})^{-1} = H_{k+1}^{\mathrm{BFGS}}$ as displayed, and $(B_{k+1}^{\mathrm{DFP}})^{-1} = H_{k+1}^{\mathrm{DFP}}$.*

*Proof.* Apply the Sherman-Morrison-Woodbury formula $(M + UCV)^{-1} = M^{-1} - M^{-1}U(C^{-1} + VM^{-1}U)^{-1}VM^{-1}$ to the rank-two BFGS update $B_{k+1} = B_k - \frac{B_k s_k s_k^\top B_k}{s_k^\top B_k s_k} + \frac{y_k y_k^\top}{y_k^\top s_k}$, grouping the two rank-one corrections as a rank-two $UCV$ with $U = [\,B_k s_k\ \ y_k\,]$. The inversion yields, after simplification using $\rho_k = 1/(y_k^\top s_k)$, the congruence form $H_{k+1} = (I - \rho_k s_k y_k^\top)H_k(I - \rho_k y_k s_k^\top) + \rho_k s_k s_k^\top$. Comparing this expression for $H_{k+1}$ with the DFP formula for $B_{k+1}$ shows they are identical under $B \leftrightarrow H$, $s \leftrightarrow y$; the verification $H_{k+1}y_k = s_k$ is the computation of Exercise 5. $\square$

**Proposition 5 (BFGS trace/determinant identities and the Powell function).** *The BFGS update obeys $\det(B_{k+1}) = \det(B_k)\,(y_k^\top s_k)/(s_k^\top B_k s_k)$ and $\operatorname{tr}(B_{k+1}) = \operatorname{tr}(B_k) - \|B_k s_k\|^2/(s_k^\top B_k s_k) + \|y_k\|^2/(y_k^\top s_k)$, so the Powell function $\psi(B) = \operatorname{tr}(B) - \ln\det(B)$ changes by a quantity bounded in terms of the cosines of the angles between $s_k$, $B_k s_k$, and $y_k$.*

*Proof.* The determinant identity follows from the matrix-determinant lemma applied twice to the rank-one corrections, or from the factorisation $B_{k+1} = B_k^{1/2}(I - \hat b\hat b^\top + (y_k y_k^\top)/(y_k^\top s_k\,) \text{ conjugated})B_k^{1/2}$ where $\hat b = b/\|b\|$, $b = B_k^{1/2}s_k$: the projector $I - \hat b\hat b^\top$ has eigenvalues $1$ (multiplicity $n-1$) and $0$, and the added rank-one term restores a single eigenvalue equal to $(y_k^\top s_k)/(s_k^\top B_k s_k)$, giving the stated determinant ratio. The trace identity is immediate from linearity of the trace applied to the three terms, using $\operatorname{tr}(B_k s_k s_k^\top B_k) = \|B_k s_k\|^2$ and $\operatorname{tr}(y_k y_k^\top) = \|y_k\|^2$. Substituting both into $\psi(B_{k+1}) - \psi(B_k)$ and writing $m_k = (s_k^\top B_k s_k)/(s_k^\top s_k)$, $\cos\theta_k = (s_k^\top B_k s_k)/(\|s_k\|\|B_k s_k\|)$ expresses the change in $\psi$ in terms of these angle and curvature ratios, which remain bounded under the level-set Hessian bounds — the inequality that drives the global-convergence proof. $\square$

**Proposition 6 (Dennis-Moré: superlinear convergence under unit steps).** *Under $x_k \to x^*$, $\nabla f(x^*) = 0$, $\nabla^2 f(x^*) =: H^* \succ 0$, $\nabla^2 f$ Lipschitz near $x^*$, and unit steps $x_{k+1} = x_k - B_k^{-1}g_k$ with $B_k$ nonsingular, the convergence is q-superlinear if and only if $\lim_k \|(B_k - H^*)s_k\|/\|s_k\| = 0$.*

*Proof.* The unit step gives $B_k s_k = -g_k$, and $y_k = \overline{H}_k s_k$ with $\overline{H}_k = \int_0^1\nabla^2 f(x_k + ts_k)\,dt \to H^*$. Then
$$
(B_k - H^*)s_k = -g_k - H^* s_k = -(g_k + H^* s_k).
$$
Since $g_{k+1} = g_k + y_k = g_k + \overline{H}_k s_k$ and $g_{k+1} = H^* e_{k+1} + o(\|e_{k+1}\|)$ with $g_k = H^* e_k + o(\|e_k\|)$, subtracting yields $g_{k+1} = (B_k - H^*)s_k + (\overline{H}_k - H^*)s_k + o(\cdot)$ after collecting the unit-step identity, so
$$
\|H^* e_{k+1}\| \le \|(B_k - H^*)s_k\| + \|\overline{H}_k - H^*\|\,\|s_k\| + o(\|e_k\|) .
$$
The Lipschitz bound makes $\|\overline{H}_k - H^*\| = O(\|e_k\| + \|s_k\|) \to 0$, so dividing by $\|s_k\|$ (comparable to $\|e_k\|$ near the solution) shows $\|e_{k+1}\|/\|e_k\| \to 0$ exactly when $\|(B_k - H^*)s_k\|/\|s_k\| \to 0$. The reverse implication runs the same identity backward: superlinearity $\|e_{k+1}\| = o(\|e_k\|)$ forces $\|H^* e_{k+1}\| = o(\|s_k\|)$, whence $\|(B_k - H^*)s_k\|/\|s_k\| \to 0$. $\square$

## Connections [Master]

- Newton's method for optimization [44.03.03] is the parent construction quasi-Newton approximates: where Newton forms and solves $\nabla^2 f(x_k)\,p = -g_k$ with the exact Hessian, quasi-Newton substitutes the secant-updated $B_k$, trading the quadratic rate for the superlinear rate of the Dennis-Moré theorem while eliminating second derivatives entirely. The descent property that Newton derives from $\nabla^2 f(x^*) \succ 0$ becomes here the hereditary positive-definiteness of BFGS, and the modified-Newton constructions that repair an indefinite exact Hessian have their quasi-Newton counterpart in SR1, which instead *embraces* indefiniteness for trust-region use.

- Line-search methods and the Wolfe conditions [44.03.01] supply the single hypothesis the whole theory turns on: the Wolfe curvature condition forces $s_k^\top y_k > 0$, which is exactly the condition under which a positive-definite secant update exists (Proposition 2), so the line search and the secant update are not independent components but a matched pair. The Zoutendijk angle-condition machinery of that unit is what converts BFGS's hereditary positive-definiteness into the global-convergence theorem, since a positive-definite $B_k$ keeps the search direction angle-bounded away from orthogonality to the gradient.

- The spectral theorem for symmetric operators [01.01.13] is the algebraic engine of the positive-definiteness proofs: the symmetric square root $B_k^{1/2}$ used in the hereditary-positivity argument, the eigenvalue analysis behind the trace-determinant identities and the Powell function, and the very statement that $B_k \succ 0$ means all eigenvalues are positive all rest on it. Multivariable Taylor expansion [02.05.05] provides the average-Hessian identity $y_k = \big(\int_0^1\nabla^2 f(x_k + ts_k)\,dt\big)s_k$ that interprets the secant equation as average-curvature matching and powers the Dennis-Moré error recursion.

- Limited-memory L-BFGS [44.03.05], nonlinear conjugate gradient [44.03.06], trust-region methods [44.03.02], and Broyden's method for nonlinear equations [43.02.03] are the downstream consumers and siblings: L-BFGS stores only the last $m$ secant pairs $(s_i, y_i)$ to apply $H_k g_k$ implicitly in $O(mn)$ for large $n$, the Broyden-class conjugate-direction property makes BFGS with exact line search coincide with linear CG on a quadratic, SR1 is the indefiniteness-tolerant Hessian model inside trust regions, and the nonsymmetric Jacobian version of the secant update is exactly Broyden's root-finding method. Quasi-Newton is thus the hub from which large-scale smooth optimization radiates.

## Historical & philosophical context [Master]

The quasi-Newton idea originated with **William C. Davidon** in 1959, a physicist at Argonne National Laboratory who, frustrated by unreliable derivative-free methods on a coordinate-descent code, devised a "variable metric" method that builds a Hessian model from successive gradients; his report went unpublished for decades and appeared only in 1991 [ref: TODO_REF Davidon 1959]. **Roger Fletcher and Michael Powell** recast and popularised Davidon's method in 1963, producing what is now the DFP update [ref: TODO_REF Fletcher-Powell 1963]. The superior BFGS update was discovered independently in 1970 by **Charles Broyden, Roger Fletcher, Donald Goldfarb, and David Shanno**, four authors whose initials name it, each arriving at the same rank-two update from a different motivation — least-change, duality with DFP, and self-scaling among them.

The theoretical foundations were consolidated by **John Dennis and Jorge Moré**, whose 1977 survey *Quasi-Newton methods, motivation and theory* established the least-change variational characterisation and the superlinear-convergence condition that bears their names [ref: TODO_REF Dennis-More 1977], and by the textbook treatments of **Dennis and Schnabel** and of **Nocedal and Wright**, the latter giving the global-convergence analysis of BFGS via the Powell function. The symmetric rank-one update, though older as a formula, was rehabilitated for trust-region use by **Conn, Gould, and Toint** in the late 1980s when its convergence-to-the-true-Hessian property was proved under the skipping safeguard [ref: TODO_REF Conn-Gould-Toint 1991]. The limited-memory variant L-BFGS, due to **Jorge Nocedal** in 1980, made the method the default for large-scale smooth optimization and machine learning [ref: TODO_REF Nocedal 1980].

## Bibliography [Master]

```bibtex
@book{nocedalwright2006,
  author    = {Nocedal, Jorge and Wright, Stephen J.},
  title     = {Numerical Optimization},
  edition   = {2},
  series    = {Springer Series in Operations Research and Financial Engineering},
  publisher = {Springer},
  year      = {2006}
}

@book{dennisschnabel1996,
  author    = {Dennis, John E. and Schnabel, Robert B.},
  title     = {Numerical Methods for Unconstrained Optimization and Nonlinear Equations},
  series    = {Classics in Applied Mathematics},
  volume    = {16},
  publisher = {SIAM},
  year      = {1996},
  note      = {Reprint of the 1983 Prentice-Hall edition}
}

@article{dennismore1977,
  author  = {Dennis, John E. and Mor\'{e}, Jorge J.},
  title   = {Quasi-Newton methods, motivation and theory},
  journal = {SIAM Review},
  volume  = {19},
  number  = {1},
  year    = {1977},
  pages   = {46--89}
}

@article{fletcherpowell1963,
  author  = {Fletcher, Roger and Powell, Michael J. D.},
  title   = {A rapidly convergent descent method for minimization},
  journal = {The Computer Journal},
  volume  = {6},
  number  = {2},
  year    = {1963},
  pages   = {163--168}
}

@techreport{davidon1959,
  author      = {Davidon, William C.},
  title       = {Variable metric method for minimization},
  institution = {Argonne National Laboratory},
  number      = {ANL-5990 (Rev.)},
  year        = {1959},
  note        = {Reprinted in SIAM Journal on Optimization 1 (1991), 1--17}
}

@article{conngouldtoint1991,
  author  = {Conn, Andrew R. and Gould, Nicholas I. M. and Toint, Philippe L.},
  title   = {Convergence of quasi-Newton matrices generated by the symmetric rank one update},
  journal = {Mathematical Programming},
  volume  = {50},
  number  = {1--3},
  year    = {1991},
  pages   = {177--195}
}

@article{nocedal1980,
  author  = {Nocedal, Jorge},
  title   = {Updating quasi-Newton matrices with limited storage},
  journal = {Mathematics of Computation},
  volume  = {35},
  number  = {151},
  year    = {1980},
  pages   = {773--782}
}
```
</content>
</invoke>
