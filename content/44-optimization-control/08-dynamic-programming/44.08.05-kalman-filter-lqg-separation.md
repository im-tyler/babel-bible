---
id: 44.08.05
title: "The Kalman Filter, Linear-Quadratic-Gaussian Control, and the Separation Principle"
slug: kalman-filter-lqg-separation
section: optimization-control
chapter: 08-dynamic-programming
concept_catalog_id: optimization-control.kalman-filter-lqg-separation
prerequisites:
  - 44.07.02  # LQR and the algebraic Riccati equation — the control half of LQG and the Riccati dual of the filter covariance recursion
  - 26.03.01  # random variables, joint/conditional distributions, covariance, the multivariate normal — the Gaussian-conditioning substrate of the filter
successors: []
tier_anchors:
  beginner: "Blending a shaky prediction with a noisy measurement so the blend is steadier than either — Anderson & Moore 1979 *Optimal Filtering* (Prentice-Hall) Ch. 1 (the estimation problem in plain terms); Kalman 1960 'A New Approach to Linear Filtering and Prediction Problems' *Journal of Basic Engineering* 82 (the recursive estimator told as predict-then-correct)"
  intermediate: "Anderson & Moore 1979 *Optimal Filtering* (Prentice-Hall) Ch. 3-5 (the discrete-time Kalman filter, the conditional-mean derivation, the covariance recursion, and the steady-state filter); Bertsekas 2017 *Dynamic Programming and Optimal Control* Vol. 1 (4th ed., Athena Scientific) Ch. 5 (linear-quadratic-Gaussian problems, the Kalman filter via least-squares estimation, and certainty equivalence)"
  master: "Anderson & Moore 1979 *Optimal Filtering* (Prentice-Hall) Ch. 4-7 (innovations, the algebraic Riccati equation for the steady-state filter, the duality with the regulator, and the Kalman-Bucy filter); Kwakernaak & Sivan 1972 *Linear Optimal Control Systems* (Wiley-Interscience) Ch. 4-5 (the LQG problem and the separation theorem); Doyle 1978 'Guaranteed Margins for LQG Regulators' *IEEE Transactions on Automatic Control* 23 (the absence of robustness margins)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Anderson, B. D. O. & Moore, J. B. — Optimal Filtering (Prentice-Hall, 1979)"
    locator: "Ch. 3-7: the discrete-time linear-Gaussian state-space model x_{k+1}=A x_k + w_k, y_k = C x_k + v_k with w,v zero-mean white Gaussian of covariances Q,R; the Kalman filter as the recursive conditional-mean (minimum-mean-square-error) estimator, derived by Gaussian conditioning / orthogonal projection in the Hilbert space of square-integrable estimators; the predict step (time update) for the one-step-ahead mean and covariance, the update step (measurement update) using the innovation y_k - C x_hat_{k|k-1}, the Kalman gain L_k = P_{k|k-1} C^T (C P_{k|k-1} C^T + R)^{-1}, and the error-covariance Riccati recursion; the innovations sequence as a white-noise process; the steady-state filter and the filter algebraic Riccati equation; the estimation/control duality between the filter ARE and the regulator ARE; the continuous-time Kalman-Bucy filter dP/dt = A P + P A^T - P C^T R^{-1} C P + Q with gain K = P C^T R^{-1}."
  - source: TODO_REF
    path: "Bertsekas, D. P. — Dynamic Programming and Optimal Control, Vol. 1 (4th ed., Athena Scientific, 2017)"
    locator: "Ch. 5: linear-quadratic-Gaussian (LQG) problems. The optimal estimator for a linear-Gaussian system is the Kalman filter, obtained as recursive least-squares estimation; for the partially observed quadratic-cost stochastic control problem the optimal controller is the certainty-equivalent law that applies the deterministic LQR gain to the Kalman state estimate; the separation principle — the controller (regulator) and the estimator (filter) are designed independently, each solving its own Riccati equation, and the optimal output-feedback law is their cascade; the cost decomposes into the deterministic optimal cost plus an estimation-error penalty."
  - source: TODO_REF
    path: "Kwakernaak, H. & Sivan, R. — Linear Optimal Control Systems (Wiley-Interscience, 1972)"
    locator: "Ch. 4-5: the stochastic linear optimal regulator with incomplete and noisy observations; the optimal observer (Kalman-Bucy filter); the separation theorem stating that the optimal stochastic controller for the LQG problem is the deterministic optimal feedback gain composed with the optimal state estimate; the duality between the regulator and the filter Riccati equations and the resulting closed-loop pole structure (regulator poles plus filter poles)."
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the linear-algebraic and probabilistic substrate the Kalman
  filter rests on — positive-definite and positive-semidefinite matrices
  (`Matrix.PosDef`, `Matrix.PosSemidef`), the Schur-complement and block-inverse
  machinery, the spectral theorem, conditional expectation
  (`MeasureTheory.condExp`) as an L2 orthogonal projection, and the abstract
  Gaussian measure (`MeasureTheory.Measure.IsGaussian` / the characteristic
  functional layer) — but it has no Gaussian-conditioning formula and no
  estimation theory. There is no theorem that the conditional distribution of one
  block of a jointly Gaussian vector given another is Gaussian with mean
  $\mu_1 + \Sigma_{12}\Sigma_{22}^{-1}(x_2-\mu_2)$ and covariance
  $\Sigma_{11}-\Sigma_{12}\Sigma_{22}^{-1}\Sigma_{21}$ (the linear-Gaussian
  measurement update), no definition of the linear-Gaussian state-space model as a
  hidden Markov process, no Kalman predict/update recursion, no statement that the
  filtered mean is the minimum-mean-square-error estimator and the orthogonal
  projection onto the observation $\sigma$-algebra, and no error-covariance Riccati
  recursion or its steady-state filter algebraic Riccati equation. The
  continuous-time Kalman-Bucy filter as a matrix Riccati flow, the estimation /
  control duality between the filter ARE and the regulator ARE of `44.07.02`, the
  LQG separation theorem (certainty equivalence), and the Doyle non-robustness
  result are all absent. The named human reviewer is the correctness gate for the
  Gaussian-conditioning derivation, the MMSE-optimality and innovations arguments,
  the duality identification, and the separation-principle decomposition.
human_reviewer: "Tyler (Kalman filtering, stochastic optimal control, and the separation principle; pending external control-theory reviewer)"
estimated_time:
  beginner: 18m
  intermediate: 52m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [bertsekas]
source_curriculum: optimization-control
---

## Intuition [Beginner]

You are tracking something that moves — a plane on radar, a phone in your pocket guessing where you walked. Two sources of information disagree. One is a prediction: you know roughly how the thing moves, so from where it was you can guess where it is now. The other is a measurement: a sensor reading of where it is right now. Both are wrong. The prediction drifts because motion is never exactly known; the measurement jitters because sensors are noisy. The Kalman filter is the recipe for blending the two into a single best guess that is steadier than either alone.

The blend is a weighted average, but a smart one. If your prediction is trustworthy and the sensor is junk, lean on the prediction. If the sensor is sharp and your motion model is shaky, lean on the measurement. The filter keeps a running sense of how trustworthy its own guess is — a spread, a margin of error — and uses that to set the weights automatically at every step.

The loop is short and repeats forever. Predict where the thing went, then look at the new measurement and correct the prediction toward it by an amount set by the trust weights. The corrected guess becomes the starting point for the next prediction. Because the blend always lands between the two inputs and discards the noise pulling each way, the running guess is calmer and closer to the truth than the raw sensor ever is.

The same idea closes a bigger loop. If you are also steering the thing, you steer using your best guess of where it is, computed by the filter, and you compute the steering exactly as if the guess were the true position. Estimating and steering, designed apart, snap together.

## Visual [Beginner]

The picture shows the two-step cycle the filter runs and where the trust weight sits.

Read the table top to bottom as one cycle, repeated every time a new measurement arrives. The filter holds a current best guess and a margin of error. First it predicts: it pushes the guess forward by the known motion and widens the margin because prediction adds uncertainty. Then it corrects: it compares the prediction to the fresh measurement, and nudges the guess toward the measurement by the trust weight, shrinking the margin because a measurement adds information.

| stage | what happens | effect on the margin |
|---|---|---|
| predict | move the guess forward by the known motion | margin grows (uncertainty added) |
| measure | read the noisy sensor | a new clue arrives |
| weigh | set the trust weight from the two margins | bigger sensor noise means smaller weight |
| correct | nudge the guess toward the measurement | margin shrinks (information added) |

![A block diagram of the Kalman filter loop. On the left a box labelled "predict" takes the previous best guess and the motion model and outputs a predicted guess with a widened error margin. An arrow carries this into a box labelled "correct" which also receives an arrow from a sensor labelled "measurement". Between them a small triangle labelled "Kalman gain L" sets how far the guess moves toward the measurement. The output of "correct" is the new best guess with a shrunken margin, and a feedback arrow loops it back to "predict" for the next cycle. A caption reads: predict widens the margin, correct shrinks it, and the gain L balances trust in the prediction against trust in the measurement.](/img/kalman-filter-lqg-separation-placeholder.svg)

The takeaway: the filter is just predict-then-correct, with a single trust weight deciding how much of the new measurement to believe. When you also steer, you feed the best guess into the steering rule and treat it as if it were exact — that swap is the whole separation idea.

## Worked example [Beginner]

Track one number — say a temperature that holds roughly steady. The motion model is "it stays the same," so the prediction is just the old guess. We track a guess and its error spread (a variance, the square of the margin).

Start with guess $10$ and error spread $4$. The sensor has noise spread $4$ as well. A new measurement reads $14$.

Step 1. Predict. The model says it stays put, so the predicted guess is still $10$. Predicting adds a little uncertainty; take the added spread to be $0$ here for a steady value, so the predicted spread stays $4$.

Step 2. Set the trust weight. The weight is the prediction's spread divided by the total spread: $4 / (4 + 4) = 0.5$. Half-trust each, since both spreads are equal.

Step 3. Correct. Nudge the guess toward the measurement by the weight times the gap. The gap is $14 - 10 = 4$. New guess $= 10 + 0.5 \times 4 = 12$.

Step 4. Shrink the spread. A measurement always sharpens the guess: new spread $= (1 - 0.5) \times 4 = 2$.

What this tells us: the blended guess $12$ sits between the prediction $10$ and the measurement $14$, and the spread fell from $4$ to $2$ — the blend is more certain than either input. If the sensor had been noisier, say spread $12$, the weight would be $4/(4+12) = 0.25$, the guess would move only to $11$, and the filter would lean on its prediction. The trust weight does the balancing on its own.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple-choice).**

What does the Kalman filter do at each step?

<ol class="exercise__options" type="A">
<li>It throws away the prediction and keeps only the latest measurement.</li>
<li>It blends a prediction with a noisy measurement, weighting each by how trustworthy it is.</li>
<li>It averages all past measurements equally, forever.</li>
<li>It ignores measurements and trusts the motion model alone.</li>
</ol>

<details class="exercise__hint"><summary>Hint</summary>

The whole point is a smart weighted blend of two disagreeing sources.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** The filter combines a model-based prediction with the new measurement, using a trust weight set by the two error spreads. Feedback-correct: the weight leans toward whichever source is more reliable. Feedback-wrong: A and D each keep only one source; C weights all data equally, but the filter discounts old data through its running margin and the motion model.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: after the correct step, the filter's error spread is smaller than the predicted spread.

<details class="exercise__hint"><summary>Hint</summary>

A measurement adds information; information sharpens a guess.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Folding in a measurement can only sharpen the estimate, so the corrected spread is at most the predicted spread (and strictly smaller when the sensor carries real information). Feedback-correct: the predict step widens the margin, the correct step shrinks it. Feedback-wrong: if you expected the spread to grow, recall that prediction adds uncertainty while measurement removes it.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A steady value has predicted guess $20$ with predicted spread $3$, and the sensor noise spread is $1$. A measurement reads $24$. The trust weight is $3/(3+1)$. Report the corrected guess.

<details class="exercise__hint"><summary>Hint</summary>

Weight $= 3/(3+1) = 0.75$; new guess $= 20 + 0.75 \times (24 - 20)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$23$.** The weight is $0.75$, the gap is $24 - 20 = 4$, so the corrected guess is $20 + 0.75 \times 4 = 23$. The low sensor noise makes the filter trust the measurement heavily, moving most of the way toward $24$. Rubric: full credit for $23$.

</details>

</aside>

## Formal definition [Intermediate+]

Consider the discrete-time *linear-Gaussian state-space model*
$$
x_{k+1} = A x_k + B u_k + w_k, \qquad y_k = C x_k + v_k,
$$
with state $x_k \in \mathbb{R}^n$, control $u_k \in \mathbb{R}^m$, measurement $y_k \in \mathbb{R}^p$, and matrices $A, B, C$ of compatible size. The *process noise* $w_k$ and *measurement noise* $v_k$ are independent zero-mean Gaussian white-noise sequences with covariances $\operatorname{Cov}(w_k) = W \succeq 0$ and $\operatorname{Cov}(v_k) = V \succ 0$, mutually independent and independent of the initial state $x_0 \sim \mathcal{N}(\bar x_0, \Sigma_0)$ — the multivariate normal and covariance structure of [26.03.01]. Write $\mathcal{Y}_k = (y_0, \dots, y_k)$ for the observations through time $k$.

**Definition (filtered and predicted estimates).** The *filtered estimate* is the conditional mean $\hat x_{k\mid k} = \mathbb{E}[x_k \mid \mathcal{Y}_k]$ with error covariance $P_{k\mid k} = \operatorname{Cov}(x_k - \hat x_{k\mid k} \mid \mathcal{Y}_k)$. The *one-step predicted estimate* is $\hat x_{k\mid k-1} = \mathbb{E}[x_k \mid \mathcal{Y}_{k-1}]$ with predicted covariance $P_{k\mid k-1}$.

**Definition (Kalman filter recursion).** Given $\hat x_{0\mid -1} = \bar x_0$, $P_{0\mid -1} = \Sigma_0$, the *Kalman filter* alternates two steps. The *measurement update* (correct), using the *innovation* $\nu_k = y_k - C \hat x_{k\mid k-1}$, is
$$
L_k = P_{k\mid k-1} C^{\mathsf T}\big(C P_{k\mid k-1} C^{\mathsf T} + V\big)^{-1}, \quad
\hat x_{k\mid k} = \hat x_{k\mid k-1} + L_k \nu_k, \quad
P_{k\mid k} = (I - L_k C) P_{k\mid k-1},
$$
with $L_k$ the *Kalman gain*. The *time update* (predict) is
$$
\hat x_{k+1\mid k} = A \hat x_{k\mid k} + B u_k, \qquad
P_{k+1\mid k} = A P_{k\mid k} A^{\mathsf T} + W .
$$

**Definition (error-covariance Riccati recursion).** Eliminating $P_{k\mid k}$ gives the predicted-covariance recursion
$$
P_{k+1\mid k} = A P_{k\mid k-1} A^{\mathsf T} + W - A P_{k\mid k-1} C^{\mathsf T}\big(C P_{k\mid k-1} C^{\mathsf T} + V\big)^{-1} C P_{k\mid k-1} A^{\mathsf T},
$$
a *matrix Riccati recursion* that runs forward and depends on no measurements — the covariances are deterministic.

**Definition (steady-state filter and filter algebraic Riccati equation).** When $(A,C)$ is detectable and $(A, W^{1/2})$ stabilisable, $P_{k\mid k-1} \to P$ as $k \to \infty$, where $P \succeq 0$ is the stabilising solution of the *filter algebraic Riccati equation* (FARE)
$$
P = A P A^{\mathsf T} + W - A P C^{\mathsf T}\big(C P C^{\mathsf T} + V\big)^{-1} C P A^{\mathsf T},
$$
with steady gain $L = P C^{\mathsf T}(C P C^{\mathsf T} + V)^{-1}$ and a Schur (asymptotically stable) error dynamics $A - A L C$.

**Definition (continuous-time Kalman-Bucy filter).** For $\dot x = A x + B u + w$, $y = C x + v$ with white noises of spectral densities $W, V$, the estimate obeys $\dot{\hat x} = A \hat x + B u + K(y - C \hat x)$ with $K = P C^{\mathsf T} V^{-1}$ and $P$ solving the *filter Riccati differential equation* $\dot P = A P + P A^{\mathsf T} - P C^{\mathsf T} V^{-1} C P + W$.

**Definition (LQG problem).** Combine the model above with the quadratic cost $J = \mathbb{E}\big[\sum_{k} (x_k^{\mathsf T} Q x_k + u_k^{\mathsf T} R u_k)\big]$, $Q \succeq 0$, $R \succ 0$, where $u_k$ may depend only on $\mathcal{Y}_k$. The *linear-quadratic-Gaussian* problem is to minimise $J$ over such output-feedback policies.

Notation: $A, B, C$ the system matrices; $W, V$ the process and measurement noise covariances; $P_{k\mid k}, P_{k\mid k-1}$ the filtered and predicted error covariances; $L_k$ the Kalman gain; $\nu_k$ the innovation; $P$ the steady filter covariance; $K$ the continuous-time gain; $Q, R$ the LQG cost weights; $S$ (below) the regulator Riccati solution; $\mathcal{Y}_k$ the observation history; $\succeq, \succ$ the Loewner order. These are recorded in `_meta/NOTATION.md`.

### Counterexamples to common slips

- The Kalman gain is not the ratio of covariances written naively. The gain $L_k = P_{k\mid k-1} C^{\mathsf T}(C P_{k\mid k-1} C^{\mathsf T} + V)^{-1}$ maps measurement space to state space and inverts the *innovation* covariance $C P_{k\mid k-1} C^{\mathsf T} + V$, not the state covariance. Treating it as $P/(P+V)$ is correct only in the scalar single-output case.

- The filter covariances are not random. Because $P_{k\mid k-1}, P_{k\mid k}, L_k$ obey a recursion driven only by $A, C, W, V$, they can be precomputed offline before any data arrives. The measurements move the estimate $\hat x$; they do not move its covariance. This is what makes the steady-state filter a fixed linear system.

- LQG separation is not "estimation and control are unrelated." The two designs are computed independently — one Riccati for the regulator, one for the filter — but the closed loop couples them: its poles are the union of the regulator poles and the filter poles, and the achieved cost carries an irreducible estimation-error term. Independence of *design* does not mean independence of *performance*.

## Key theorem with proof [Intermediate+]

**Theorem (the Kalman filter is the recursive MMSE estimator; Anderson-Moore Ch. 3-5 [ref: TODO_REF]; Bertsekas Ch. 5 [ref: TODO_REF]).** *For the linear-Gaussian model, the conditional distribution of $x_k$ given $\mathcal{Y}_k$ is Gaussian, $x_k \mid \mathcal{Y}_k \sim \mathcal{N}(\hat x_{k\mid k}, P_{k\mid k})$, with mean and covariance produced by the Kalman recursion. The filtered mean $\hat x_{k\mid k}$ is the minimum-mean-square-error estimator of $x_k$ from $\mathcal{Y}_k$, that is $\hat x_{k\mid k} = \arg\min_{g} \mathbb{E}\|x_k - g(\mathcal{Y}_k)\|^2$ over all measurable $g$; in the non-Gaussian case with the same first two moments it remains the best linear unbiased estimator.*

**Proof.** The argument is Gaussian conditioning [26.03.01] applied recursively. Suppose inductively $x_k \mid \mathcal{Y}_{k-1} \sim \mathcal{N}(\hat x_{k\mid k-1}, P_{k\mid k-1})$ (the base case is $x_0 \sim \mathcal{N}(\bar x_0, \Sigma_0)$). Stack $x_k$ and the next measurement $y_k = C x_k + v_k$. Conditioned on $\mathcal{Y}_{k-1}$, the pair is jointly Gaussian with means $\hat x_{k\mid k-1}$ and $C \hat x_{k\mid k-1}$ and, using independence of $v_k$ from $x_k$,
$$
\operatorname{Cov}\begin{pmatrix} x_k \\ y_k \end{pmatrix} = \begin{pmatrix} P_{k\mid k-1} & P_{k\mid k-1} C^{\mathsf T} \\ C P_{k\mid k-1} & C P_{k\mid k-1} C^{\mathsf T} + V \end{pmatrix}.
$$
The Gaussian conditioning formula gives the law of $x_k$ given $y_k$ (hence given $\mathcal{Y}_k$): the conditional mean is
$$
\hat x_{k\mid k} = \hat x_{k\mid k-1} + P_{k\mid k-1} C^{\mathsf T}\big(C P_{k\mid k-1} C^{\mathsf T} + V\big)^{-1}\big(y_k - C \hat x_{k\mid k-1}\big) = \hat x_{k\mid k-1} + L_k \nu_k,
$$
and the conditional covariance is the Schur complement
$$
P_{k\mid k} = P_{k\mid k-1} - P_{k\mid k-1} C^{\mathsf T}\big(C P_{k\mid k-1} C^{\mathsf T} + V\big)^{-1} C P_{k\mid k-1} = (I - L_k C) P_{k\mid k-1},
$$
the measurement-update equations. For the time update, $x_{k+1} = A x_k + B u_k + w_k$ with $w_k$ independent of $x_k$ and of $\mathcal{Y}_k$; a linear image of a Gaussian plus an independent Gaussian is Gaussian, so $x_{k+1} \mid \mathcal{Y}_k \sim \mathcal{N}(A \hat x_{k\mid k} + B u_k,\; A P_{k\mid k} A^{\mathsf T} + W)$, which is the predict step and closes the induction.

MMSE optimality: for any joint distribution the conditional mean minimises mean-square error, since for any estimator $g(\mathcal{Y}_k)$,
$$
\mathbb{E}\|x_k - g\|^2 = \mathbb{E}\|x_k - \mathbb{E}[x_k\mid \mathcal{Y}_k]\|^2 + \mathbb{E}\|\mathbb{E}[x_k\mid \mathcal{Y}_k] - g\|^2 \ge \mathbb{E}\|x_k - \mathbb{E}[x_k\mid \mathcal{Y}_k]\|^2,
$$
the cross term vanishing because $x_k - \mathbb{E}[x_k\mid \mathcal{Y}_k]$ is orthogonal to every function of $\mathcal{Y}_k$. In the Gaussian case the conditional mean is the linear expression above, so the Kalman filter attains the global MMSE optimum; with only the first two moments specified the same linear expression is the orthogonal projection of $x_k$ onto the span of the observations, the best linear unbiased estimator. $\square$

**Bridge.** This Schur-complement covariance update builds toward the steady-state theory of the Advanced results, where the predicted covariance converges to the stabilising solution of the filter algebraic Riccati equation, and the same recursion appears again in the LQG separation theorem, where the filter supplies the state estimate that the regulator gain multiplies. The foundational reason the filter is recursive is that Gaussianity is preserved under linear maps and conditioning, so the entire posterior collapses to its mean and covariance — this is exactly the finite-dimensional sufficiency that turns an estimation over the whole observation history into a two-line update. The construction generalises the scalar trust-weight blend of the Beginner example to a matrix gain, and the error-covariance recursion is dual to the cost-to-go Riccati of the regulator [44.07.02]: transposing $A$, swapping $B \leftrightarrow C^{\mathsf T}$ and $Q,R \leftrightarrow W,V$ turns one Riccati equation into the other. The central insight is that the innovation $\nu_k$ is a white-noise sequence carrying exactly the new information in $y_k$, so the filter is the device that whitens the observations; putting these together, the bridge is the recognition that optimal estimation and optimal control are the same quadratic problem read forward and backward in time, the filter ARE and the regulator ARE being transposes of one another.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Scalar steady value: $A = 1$, $C = 1$, predicted covariance $P_{k\mid k-1} = 3$, measurement noise $V = 1$. Compute the Kalman gain $L_k = P_{k\mid k-1}(P_{k\mid k-1} + V)^{-1}$.

<details class="exercise__hint"><summary>Hint</summary>

$L = 3/(3+1)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0.75$.** The gain is $L_k = 3/(3+1) = 0.75$, so the filter moves three-quarters of the way toward the measurement. Rubric: full credit for $0.75$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

Continuing Exercise 1, compute the updated covariance $P_{k\mid k} = (1 - L_k C) P_{k\mid k-1}$ with $C = 1$, $L_k = 0.75$, $P_{k\mid k-1} = 3$.

<details class="exercise__hint"><summary>Hint</summary>

$(1 - 0.75)\times 3$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$0.75$.** The updated covariance is $(1 - 0.75)\times 3 = 0.25 \times 3 = 0.75$, smaller than the predicted $3$ as a measurement must sharpen the estimate. Rubric: full credit for $0.75$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 3 (medium, numeric).**

Scalar steady-value filter with $A = 1$, $C = 1$, process noise $W = 0$, measurement noise $V$. The FARE reduces to $P = P - P(P+V)^{-1}P$, whose only finite solution is $P = 0$. Instead take $W = 3$, $V = 1$: the FARE is $P = P + 3 - P^2/(P+1)$. Solve for the positive steady covariance $P$.

<details class="exercise__hint"><summary>Hint</summary>

$3 = P^2/(P+1)$ gives $P^2 - 3P - 3 = 0$; take the positive root.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\tfrac{3 + \sqrt{21}}{2} \approx 3.79$.** The FARE simplifies to $3 = P^2/(P+1)$, i.e. $P^2 - 3P - 3 = 0$, with positive root $P = (3 + \sqrt{21})/2 \approx 3.79$. The steady gain is $L = P/(P+1) \approx 0.79$. Rubric: full credit for the quadratic and the positive root.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Derive the measurement-update covariance $P_{k\mid k} = (I - L_k C)P_{k\mid k-1}$ from the Schur complement $P_{k\mid k} = P_{k\mid k-1} - P_{k\mid k-1}C^{\mathsf T}(C P_{k\mid k-1}C^{\mathsf T} + V)^{-1}C P_{k\mid k-1}$, given $L_k = P_{k\mid k-1}C^{\mathsf T}(C P_{k\mid k-1}C^{\mathsf T} + V)^{-1}$.

<details class="exercise__hint"><summary>Hint</summary>

Factor $P_{k\mid k-1}$ on the right out of both terms.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $S_k = C P_{k\mid k-1}C^{\mathsf T} + V$ (the innovation covariance). The Schur complement is $P_{k\mid k} = P_{k\mid k-1} - P_{k\mid k-1}C^{\mathsf T}S_k^{-1}C P_{k\mid k-1}$. The second term is $L_k C P_{k\mid k-1}$ since $L_k = P_{k\mid k-1}C^{\mathsf T}S_k^{-1}$. Hence $P_{k\mid k} = P_{k\mid k-1} - L_k C P_{k\mid k-1} = (I - L_k C)P_{k\mid k-1}$. Rubric: full credit for identifying $L_k$ inside the second term and factoring.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Show the innovation $\nu_k = y_k - C\hat x_{k\mid k-1}$ has mean zero and covariance $C P_{k\mid k-1}C^{\mathsf T} + V$ conditioned on $\mathcal{Y}_{k-1}$.

<details class="exercise__hint"><summary>Hint</summary>

Write $\nu_k = C(x_k - \hat x_{k\mid k-1}) + v_k$ and use independence of $v_k$ from the prediction error.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Since $y_k = C x_k + v_k$, the innovation is $\nu_k = C(x_k - \hat x_{k\mid k-1}) + v_k$. The prediction error $e = x_k - \hat x_{k\mid k-1}$ has conditional mean zero (as $\hat x_{k\mid k-1} = \mathbb{E}[x_k\mid\mathcal{Y}_{k-1}]$) and covariance $P_{k\mid k-1}$; $v_k$ has mean zero, covariance $V$, and is independent of $e$. So $\mathbb{E}[\nu_k\mid\mathcal{Y}_{k-1}] = 0$ and $\operatorname{Cov}(\nu_k\mid\mathcal{Y}_{k-1}) = C P_{k\mid k-1}C^{\mathsf T} + V$. Rubric: full credit for the decomposition, the zero mean, and the variance sum using independence.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

State the estimation/control duality: write the regulator algebraic Riccati equation (CARE-discrete) for $(A,B,Q,R)$ and the filter algebraic Riccati equation for $(A,C,W,V)$, and give the substitution turning one into the other.

<details class="exercise__hint"><summary>Hint</summary>

Transpose $A$ and swap $B \leftrightarrow C^{\mathsf T}$, $Q \leftrightarrow W$, $R \leftrightarrow V$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The discrete regulator DARE is $S = A^{\mathsf T}S A - A^{\mathsf T}S B(R + B^{\mathsf T}S B)^{-1}B^{\mathsf T}S A + Q$. The filter FARE is $P = A P A^{\mathsf T} + W - A P C^{\mathsf T}(C P C^{\mathsf T} + V)^{-1}C P A^{\mathsf T}$. Substituting $A \to A^{\mathsf T}$, $B \to C^{\mathsf T}$, $Q \to W$, $R \to V$ in the DARE produces the FARE (and vice versa). So the filter is the regulator of the transposed system; the Kalman gain $L = P C^{\mathsf T}(C P C^{\mathsf T}+V)^{-1}$ is the transpose-dual of the regulator gain. Rubric: full credit for both equations and the explicit substitution.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

For the LQG problem, suppose the cost decomposes as $J = \mathbb{E}[x_k^{\mathsf T}Q x_k + u_k^{\mathsf T}R u_k]$ with optimal control $u_k = -K\hat x_{k\mid k}$. Using $x_k = \hat x_{k\mid k} + e_k$ with estimation error $e_k$ orthogonal to $\hat x_{k\mid k}$, argue the achieved cost splits into a deterministic LQR term plus an estimation-error term.

<details class="exercise__hint"><summary>Hint</summary>

Expand $\mathbb{E}[x_k^{\mathsf T}Q x_k]$ with $x_k = \hat x + e$ and drop the cross term by orthogonality.

</details>

<details class="exercise__answer"><summary>Answer</summary>

With $x_k = \hat x_{k\mid k} + e_k$ and $\mathbb{E}[\hat x_{k\mid k}^{\mathsf T}M e_k] = 0$ for any $M$ (the estimation error is orthogonal to every function of the observations, hence to $\hat x_{k\mid k}$), we get $\mathbb{E}[x_k^{\mathsf T}Q x_k] = \mathbb{E}[\hat x_{k\mid k}^{\mathsf T}Q\hat x_{k\mid k}] + \mathbb{E}[e_k^{\mathsf T}Q e_k]$. The control is a function of $\hat x_{k\mid k}$ only, so the $u^{\mathsf T}R u$ term involves $\hat x$ alone. The first group of terms is exactly the LQR cost of the deterministic regulator applied to the estimate $\hat x_{k\mid k}$; the residual $\mathbb{E}[e_k^{\mathsf T}Q e_k] = \operatorname{tr}(Q P_{k\mid k})$ is the irreducible estimation-error penalty set by the filter covariance. Hence $J = J_{\mathrm{LQR}}(\hat x) + \sum_k \operatorname{tr}(Q P_{k\mid k})$. Rubric: full credit for the orthogonal split, the trace form of the error term, and naming the two pieces.

</details>

</aside>

## Advanced results [Master]

**Theorem (steady-state filter and the filter algebraic Riccati equation; Anderson-Moore Ch. 4-7 [ref: TODO_REF]).** *Let $(A,C)$ be detectable and $(A, W^{1/2})$ stabilisable, with $V \succ 0$, $W \succeq 0$. Then the predicted error covariance $P_{k\mid k-1}$ converges, from any initial $\Sigma_0 \succeq 0$, to the unique stabilising positive-semidefinite solution $P$ of the filter algebraic Riccati equation $P = A P A^{\mathsf T} + W - A P C^{\mathsf T}(C P C^{\mathsf T} + V)^{-1} C P A^{\mathsf T}$. The steady gain $L = P C^{\mathsf T}(C P C^{\mathsf T} + V)^{-1}$ makes the error dynamics $A_{\mathrm{f}} = A - A L C$ Schur (spectral radius below one), and the resulting time-invariant filter is the optimal stationary estimator; the innovations sequence is zero-mean white with covariance $C P C^{\mathsf T} + V$.*

Detectability of $(A,C)$ is what forces the error covariance to stay bounded — an undetected unstable mode would let the prediction error grow without correction — while stabilisability of $(A,W^{1/2})$ rules out a noise-free unstable mode that the recursion could not regularise, exactly the dual hypotheses to the regulator's stabilisability of $(A,B)$ and detectability of $(A,Q^{1/2})$.

**Theorem (estimation/control duality; Anderson-Moore Ch. 4 [ref: TODO_REF]; Kwakernaak-Sivan Ch. 4 [ref: TODO_REF]).** *The filter algebraic Riccati equation for $(A,C,W,V)$ is the regulator algebraic Riccati equation for the transposed data $(A^{\mathsf T}, C^{\mathsf T}, W, V)$ in place of $(A,B,Q,R)$. Consequently every theorem about the regulator Riccati equation of [44.07.02] — existence and uniqueness of the stabilising solution, the Hamiltonian / stable-invariant-subspace construction, Newton-Kleinman iteration as repeated Lyapunov solves — transfers verbatim to the Kalman filter by transposition. In particular the Kalman gain $L$ is the dual of the LQR gain, and the filter's error poles are the regulator poles of the dual system.*

The duality is not a formal coincidence but the statement that optimal estimation runs the optimal-control machinery backward: the regulator propagates a cost-to-go covariance backward in time toward the present, the filter propagates an error covariance forward in time from the past, and the two propagations solve the same quadratic matrix equation under transposition. This is the production route for filter design: a single algebraic-Riccati solver — Laub's ordered Schur reduction of the Hamiltonian, built on the real Schur form of [43.06] — serves both control and estimation.

**Theorem (LQG separation / certainty equivalence; Bertsekas Ch. 5 [ref: TODO_REF]; Kwakernaak-Sivan Ch. 5 [ref: TODO_REF]).** *For the LQG problem the optimal output-feedback control is $u_k = -K \hat x_{k\mid k}$, where $K$ is the deterministic LQR gain solving the regulator Riccati equation for $(A,B,Q,R)$ and $\hat x_{k\mid k}$ is the Kalman estimate solving the filter Riccati equation for $(A,C,W,V)$. The two designs are independent — the regulator gain does not depend on the noise covariances and the filter gain does not depend on the cost weights — and the optimal cost is the deterministic LQR cost plus an irreducible estimation-error term $\sum_k \operatorname{tr}(Q P_{k\mid k})$ (equivalently a term in the filter covariance and the regulator Riccati solution). The closed-loop spectrum is the union of the regulator eigenvalues $\sigma(A - BK)$ and the filter eigenvalues $\sigma(A - LC)$.*

Certainty equivalence is the assertion that the controller acts on the estimate exactly as it would on a known true state: the optimal stochastic policy is the optimal deterministic policy with $x_k$ replaced by $\hat x_{k\mid k}$, with no extra caution term despite the uncertainty. This holds because the LQG cost is quadratic and the estimation error is independent of the control's effect on the estimate — the cross terms vanish, decoupling the two optimisations.

**Theorem (LQG has no guaranteed stability margins; Doyle 1978 [ref: TODO_REF Doyle 1978]).** *There exist LQG designs whose loop transfer function has arbitrarily small gain and phase margins; the $60^\circ$ phase margin and infinite gain margin guaranteed for the full-state-feedback LQR are destroyed by the insertion of the Kalman filter in the loop.*

The separation principle guarantees nominal optimality and nominal stability, but the cascade of an optimal estimator and an optimal regulator carries none of the robustness of either piece alone: a small modelling error can destabilise the loop. Doyle's counterexample ended the presumption that LQG inherited LQR's margins and motivated loop-transfer recovery and, ultimately, the $H_\infty$ and $\mu$-synthesis robust-control theory that designs directly for stability margins rather than for nominal optimality.

**Synthesis.** The Kalman filter and the LQR are one quadratic problem viewed from two ends of time, and the Riccati equation is the central insight uniting them: the regulator's cost-to-go Hessian and the filter's error covariance solve algebraic Riccati equations that are transposes of one another, so existence, the Hamiltonian stable-invariant-subspace construction, and the Newton-Kleinman Lyapunov-solve route of [44.07.02] transfer by duality and a single solver serves both. This is exactly why Gaussian conditioning produces a recursion identical in form to dynamic programming: conditioning on a new measurement is the estimation analogue of one Bellman backup, and the innovation is the new information just as the running cost is the new expenditure. The foundational reason the LQG controller decomposes is certainty equivalence — the quadratic cost and the independence of estimation error from the control's action on the estimate force the cross terms to vanish, so the optimal output-feedback law is the LQR gain on the Kalman estimate, designed independently.

Putting these together, the closed-loop poles are the disjoint union of regulator poles and filter poles, the achieved cost is the deterministic optimum plus an irreducible $\operatorname{tr}(Q P)$ estimation penalty, and the bridge from full-state to output feedback is the recognition that the filter manufactures the state the regulator needs. That bridge is not free: the construction generalises the deterministic regulator but is dual to it only in nominal performance, and Doyle's result is the statement that the duality does not extend to robustness — the very independence that makes separation elegant is what voids the margin guarantees, the point where optimal control hands off to robust control.

## Full proof set [Master]

**Proposition 1 (Gaussian-conditioning measurement update).** *If $x \mid \mathcal{Y} \sim \mathcal{N}(\hat x, P)$ and $y = Cx + v$ with $v \sim \mathcal{N}(0,V)$ independent of $x$, then $x \mid (\mathcal{Y}, y) \sim \mathcal{N}(\hat x + L\nu,\ (I - LC)P)$ with $\nu = y - C\hat x$ and $L = P C^{\mathsf T}(C P C^{\mathsf T} + V)^{-1}$.*

**Proof.** Conditioned on $\mathcal{Y}$, the pair $(x,y)$ is jointly Gaussian with mean $(\hat x, C\hat x)$ and covariance blocks $\Sigma_{xx} = P$, $\Sigma_{xy} = P C^{\mathsf T}$, $\Sigma_{yy} = C P C^{\mathsf T} + V$ (using independence of $v$). The standard Gaussian conditional law [26.03.01] gives mean $\hat x + \Sigma_{xy}\Sigma_{yy}^{-1}(y - C\hat x) = \hat x + L\nu$ and covariance $\Sigma_{xx} - \Sigma_{xy}\Sigma_{yy}^{-1}\Sigma_{yx} = P - P C^{\mathsf T}(C P C^{\mathsf T}+V)^{-1}C P = (I - LC)P$. $\square$

**Proposition 2 (time update preserves Gaussianity).** *If $x_k \mid \mathcal{Y}_k \sim \mathcal{N}(\hat x_{k\mid k}, P_{k\mid k})$ and $x_{k+1} = A x_k + B u_k + w_k$ with $w_k \sim \mathcal{N}(0,W)$ independent of $x_k$ and $\mathcal{Y}_k$, then $x_{k+1}\mid\mathcal{Y}_k \sim \mathcal{N}(A\hat x_{k\mid k} + B u_k,\ A P_{k\mid k}A^{\mathsf T} + W)$.*

**Proof.** A linear image of a Gaussian is Gaussian, and the sum of two independent Gaussians is Gaussian. The conditional mean is $\mathbb{E}[A x_k + B u_k + w_k\mid\mathcal{Y}_k] = A\hat x_{k\mid k} + B u_k$. The conditional covariance is $\operatorname{Cov}(A x_k + w_k\mid\mathcal{Y}_k) = A\operatorname{Cov}(x_k\mid\mathcal{Y}_k)A^{\mathsf T} + \operatorname{Cov}(w_k) = A P_{k\mid k}A^{\mathsf T} + W$, the cross term vanishing by independence of $w_k$. $\square$

**Proposition 3 (MMSE optimality of the conditional mean).** *For any random vector $x$ and $\sigma$-algebra $\mathcal{G}$, $\mathbb{E}[x\mid\mathcal{G}]$ minimises $\mathbb{E}\|x - g\|^2$ over all $\mathcal{G}$-measurable square-integrable $g$.*

**Proof.** Let $\hat x = \mathbb{E}[x\mid\mathcal{G}]$ and $g$ be any $\mathcal{G}$-measurable estimator. Then $\mathbb{E}\|x - g\|^2 = \mathbb{E}\|(x - \hat x) + (\hat x - g)\|^2 = \mathbb{E}\|x-\hat x\|^2 + 2\mathbb{E}\langle x - \hat x, \hat x - g\rangle + \mathbb{E}\|\hat x - g\|^2$. The cross term vanishes: $\hat x - g$ is $\mathcal{G}$-measurable, and $\mathbb{E}[\langle x - \hat x, h\rangle] = \mathbb{E}[\langle \mathbb{E}[x - \hat x\mid\mathcal{G}], h\rangle] = 0$ for any $\mathcal{G}$-measurable $h$ since $\mathbb{E}[x-\hat x\mid\mathcal{G}] = 0$. Hence $\mathbb{E}\|x - g\|^2 = \mathbb{E}\|x-\hat x\|^2 + \mathbb{E}\|\hat x - g\|^2 \ge \mathbb{E}\|x - \hat x\|^2$, with equality iff $g = \hat x$ almost surely. $\square$

**Proposition 4 (innovations are white).** *Under the linear-Gaussian model the innovation sequence $\nu_k = y_k - C\hat x_{k\mid k-1}$ is zero-mean Gaussian with $\operatorname{Cov}(\nu_k) = C P_{k\mid k-1}C^{\mathsf T} + V$ and $\mathbb{E}[\nu_k \nu_j^{\mathsf T}] = 0$ for $j \neq k$.*

**Proof.** Write $\nu_k = C e_k + v_k$ with prediction error $e_k = x_k - \hat x_{k\mid k-1}$, which has mean zero and covariance $P_{k\mid k-1}$ and is independent of $v_k$; this gives the mean and covariance. For whiteness take $j < k$. The estimate $\hat x_{k\mid k-1}$ is a function of $\mathcal{Y}_{k-1} \supseteq \{\nu_0,\dots,\nu_{k-1}\}$, and $\mathbb{E}[\nu_k\mid\mathcal{Y}_{k-1}] = \mathbb{E}[y_k\mid\mathcal{Y}_{k-1}] - C\hat x_{k\mid k-1} = C\hat x_{k\mid k-1} - C\hat x_{k\mid k-1} = 0$. Since $\nu_j$ is $\mathcal{Y}_{k-1}$-measurable, $\mathbb{E}[\nu_k\nu_j^{\mathsf T}] = \mathbb{E}[\mathbb{E}[\nu_k\mid\mathcal{Y}_{k-1}]\nu_j^{\mathsf T}] = 0$. The innovations are thus an orthogonal (white) Gaussian sequence — the filter whitens the observations. $\square$

**Proposition 5 (filter/regulator duality of the algebraic Riccati equations).** *The map $A \mapsto A^{\mathsf T}$, $B \mapsto C^{\mathsf T}$, $Q \mapsto W$, $R \mapsto V$ carries the discrete regulator algebraic Riccati equation into the filter algebraic Riccati equation, and the stabilising solution of one maps to the stabilising solution of the other.*

**Proof.** The discrete regulator DARE is $S = A^{\mathsf T}S A - A^{\mathsf T}S B(R + B^{\mathsf T}S B)^{-1}B^{\mathsf T}S A + Q$. Apply the substitution: $A^{\mathsf T}\to A$, $B^{\mathsf T}\to C$ (so $B \to C^{\mathsf T}$), $Q \to W$, $R \to V$. The right-hand side becomes $A S A^{\mathsf T} + W - A S C^{\mathsf T}(V + C S C^{\mathsf T})^{-1}C S A^{\mathsf T}$, which is the FARE with $S$ renamed $P$. Stabilisability of $(A^{\mathsf T},C^{\mathsf T})$ — i.e. detectability of $(A,C)$ — and detectability of $(A^{\mathsf T},W^{1/2})$ — i.e. stabilisability of $(A,W^{1/2})$ — are the hypotheses that, under the regulator existence theorem of [44.07.02], yield a unique stabilising solution; transporting that theorem across the substitution gives the unique stabilising $P$ with $A - LC$ Schur. $\square$

**Proposition 6 (LQG separation / certainty equivalence).** *For the finite-horizon LQG problem the optimal control is $u_k = -K_k\hat x_{k\mid k}$ with $K_k$ the deterministic LQR gain, and the optimal cost is the deterministic LQR cost plus $\sum_k \operatorname{tr}(Q_k P_{k\mid k})$ (plus filter-covariance terms from the process noise).*

**Proof.** Solve by dynamic programming on the information state, which for the linear-Gaussian model is the pair $(\hat x_{k\mid k}, P_{k\mid k})$; the covariances are deterministic (Definitions above), so the only stochastic state is $\hat x_{k\mid k}$. Posit the value function $V_k(\hat x) = \hat x^{\mathsf T}S_k\hat x + c_k$. The Bellman step minimises $\mathbb{E}[x_k^{\mathsf T}Q x_k + u_k^{\mathsf T}R u_k + V_{k+1}(\hat x_{k+1\mid k+1})\mid\mathcal{Y}_k]$ over $u_k$. Decompose $x_k = \hat x_{k\mid k} + e_k$ with $e_k \perp \mathcal{Y}_k$ (Proposition 3), so $\mathbb{E}[x_k^{\mathsf T}Q x_k\mid\mathcal{Y}_k] = \hat x_{k\mid k}^{\mathsf T}Q\hat x_{k\mid k} + \operatorname{tr}(Q P_{k\mid k})$; the estimate evolves as $\hat x_{k+1\mid k+1} = A\hat x_{k\mid k} + B u_k + L_{k+1}\nu_{k+1}$ with the innovation $\nu_{k+1}$ zero-mean and independent of $\hat x_{k\mid k}$ and $u_k$ (Proposition 4). Substituting and taking the expectation, the innovation contributes only an additive constant $\operatorname{tr}(S_{k+1}L_{k+1}(\,\cdot\,)L_{k+1}^{\mathsf T})$ independent of $u_k$, while the $\hat x$-dependent terms reproduce exactly the deterministic LQR Bellman recursion. Minimising over $u_k$ therefore yields $u_k = -K_k\hat x_{k\mid k}$ with $K_k$ the LQR gain and $S_k$ the regulator Riccati solution, and accumulating the constants gives the cost $\hat x_0^{\mathsf T}S_0\hat x_0 + \sum_k[\operatorname{tr}(Q_k P_{k\mid k}) + \text{innovation terms}]$, the deterministic optimum plus the estimation-error penalty. $\square$

## Connections [Master]

The filter Riccati equation is the transpose-dual of the regulator Riccati equation of [44.07.02]: substituting $A \to A^{\mathsf T}$, $B \to C^{\mathsf T}$, $Q \to W$, $R \to V$ carries the algebraic Riccati equation for control into the one for estimation, so the existence-uniqueness theory, the Hamiltonian stable-invariant-subspace construction, and the Newton-Kleinman iteration of that unit transfer verbatim, and a single algebraic-Riccati solver designs both the optimal regulator and the optimal filter; the LQG controller is literally their cascade.

The Gaussian-conditioning derivation rests on the multivariate-normal and covariance theory of [26.03.01]: the measurement update is the conditional law of one Gaussian block given another, its mean the regression $\hat x + \Sigma_{xy}\Sigma_{yy}^{-1}(y - \hat y)$ and its covariance the Schur complement $\Sigma_{xx} - \Sigma_{xy}\Sigma_{yy}^{-1}\Sigma_{yx}$, so the entire filter is the recursive bookkeeping of conditioning a jointly Gaussian state-observation vector, with the finite-dimensional sufficiency of the Gaussian family the reason a two-line update suffices.

The information-form measurement update and the square-root filter forward-link to the factorisation-updating numerics of [43.04.08]: propagating a Cholesky factor of $P_{k\mid k-1}$ rather than $P$ itself guarantees a positive-definite covariance at every step, the measurement update is a QR row-append, and the recursive-least-squares information filter is the same update applied to the inverse covariance, the route that made the Kalman filter reliable in single-precision aerospace hardware.

The LQG framework is the linear-Gaussian, partially-observed instance of the stochastic dynamic programming of this chapter ([44.08.01] finite-horizon DP and [44.08.03] Markov decision processes): the separation proof is dynamic programming on the information state $(\hat x_{k\mid k}, P_{k\mid k})$, where the deterministic covariance reduces the sufficient statistic to the conditional mean and the Bellman recursion collapses to the deterministic LQR recursion plus an estimation-error constant, exhibiting certainty equivalence as the special structure of the linear-quadratic-Gaussian information state.

The continuous-time Kalman-Bucy filter connects to the stochastic-calculus and martingale theory of [37.06.01]: the innovations process is a Brownian motion in the filtered probability space, the filter is the projection of the signal onto the observation filtration, and the Riccati differential equation $\dot P = A P + P A^{\mathsf T} - P C^{\mathsf T}V^{-1}C P + W$ governs the conditional covariance of the diffusion, the linear-Gaussian special case of the general nonlinear filtering (Zakai/Kushner-Stratonovich) equations.

## Historical & philosophical context [Master]

The recursive linear filter was introduced by Rudolf Kalman in 1960, recasting the Wiener-Kolmogorov filtering theory of the 1940s — which solved the stationary, infinite-history estimation problem by spectral factorisation — into a finite-dimensional state-space recursion suited to digital computation [ref: TODO_REF Kalman 1960]. The continuous-time companion, the Kalman-Bucy filter, followed in 1961 with Richard Bucy, casting the conditional covariance as the solution of a matrix Riccati differential equation and exposing the duality with the linear-quadratic regulator that Kalman had formulated the same year [ref: TODO_REF Kalman Bucy 1961]. The filter's first decisive application was the Apollo program: the onboard navigation of the command and lunar modules used a Kalman filter, and the square-root reformulation by James Potter restored numerical reliability on the spacecraft's single-precision hardware.

The separation theorem — that the optimal stochastic controller for the linear-quadratic-Gaussian problem is the deterministic optimal feedback applied to the Kalman estimate — was established for the discrete and continuous cases by Joseph and Tou and by Wonham in the early-to-mid 1960s and consolidated in the monographs of Kwakernaak and Sivan and of Anderson and Moore [ref: TODO_REF Wonham 1968]. The presumption that the resulting LQG controllers were as robust as their full-state LQR counterparts was overturned by John Doyle in 1978, whose two-line counterexample exhibited LQG regulators with vanishing gain and phase margins [ref: TODO_REF Doyle 1978]; the result redirected control theory toward loop-transfer recovery and the robust-control program of $H_\infty$ and $\mu$-synthesis that designs for stability margins rather than nominal optimality.

## Bibliography [Master]

```bibtex
@article{Kalman1960Filter,
  author  = {Kalman, Rudolf E.},
  title   = {A New Approach to Linear Filtering and Prediction Problems},
  journal = {Journal of Basic Engineering},
  volume  = {82},
  number  = {1},
  year    = {1960},
  pages   = {35--45}
}

@article{KalmanBucy1961,
  author  = {Kalman, Rudolf E. and Bucy, Richard S.},
  title   = {New Results in Linear Filtering and Prediction Theory},
  journal = {Journal of Basic Engineering},
  volume  = {83},
  number  = {1},
  year    = {1961},
  pages   = {95--108}
}

@book{AndersonMoore1979,
  author    = {Anderson, Brian D. O. and Moore, John B.},
  title     = {Optimal Filtering},
  publisher = {Prentice-Hall},
  address   = {Englewood Cliffs, NJ},
  year      = {1979}
}

@book{KwakernaakSivan1972LQG,
  author    = {Kwakernaak, Huibert and Sivan, Raphael},
  title     = {Linear Optimal Control Systems},
  publisher = {Wiley-Interscience},
  address   = {New York},
  year      = {1972}
}

@book{Bertsekas2017DPv1,
  author    = {Bertsekas, Dimitri P.},
  title     = {Dynamic Programming and Optimal Control, Volume I},
  edition   = {4th},
  publisher = {Athena Scientific},
  address   = {Belmont, MA},
  year      = {2017}
}

@article{Doyle1978,
  author  = {Doyle, John C.},
  title   = {Guaranteed Margins for {LQG} Regulators},
  journal = {IEEE Transactions on Automatic Control},
  volume  = {23},
  number  = {4},
  year    = {1978},
  pages   = {756--757}
}

@article{Wonham1968,
  author  = {Wonham, W. Murray},
  title   = {On the Separation Theorem of Stochastic Control},
  journal = {SIAM Journal on Control},
  volume  = {6},
  number  = {2},
  year    = {1968},
  pages   = {312--326}
}

@article{Potter1963Kalman,
  author  = {Potter, James E. and Stern, Robert G.},
  title   = {Statistical Filtering of Space Navigation Measurements},
  journal = {Proceedings of the AIAA Guidance and Control Conference},
  year    = {1963}
}
```
