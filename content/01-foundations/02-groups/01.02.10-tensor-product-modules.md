---
id: 01.02.10
title: "Tensor product of modules (commutative case)"
slug: tensor-product-modules
section: foundations
chapter: groups
concept_catalog_id: foundations.tensor-product-modules
prerequisites:
  - 01.01.03
  - 01.01.15
successors: []
tier_anchors:
  beginner: "Atiyah-Macdonald Ch. 2 informal; polynomial multiplication analogy"
  intermediate: "Atiyah-Macdonald Ch. 2; Dummit-Foote §10.4"
  master: "Whitney 1938 Ann. Math. 39; Bourbaki Algebre Ch. II-III"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Whitney — Tensor products of abelian groups"
    locator: "Ann. Math. 39, 1938"
  - source: TODO_REF
    path: "Bourbaki — Algebre"
    locator: "Ch. II-III"
  - source: TODO_REF
    path: "Atiyah-Macdonald — Introduction to Commutative Algebra"
    locator: "Ch. 2"
  - source: TODO_REF
    path: "Dummit-Foote — Abstract Algebra"
    locator: "§10.4-10.5"
  - source: TODO_REF
    path: "Eisenbud — Commutative Algebra with a View Toward Algebraic Geometry"
    locator: "Ch. 2"
lean_status: none
lean_mathlib_gap: |
  Mathlib has a comprehensive tensor product API via
  Mathlib.LinearAlgebra.TensorProduct, including the universal property,
  bilinear maps, associativity, commutativity, and the tensor-hom adjunction
  for modules over commutative rings. What Mathlib lacks is a connected
  pedagogical formal narrative that bridges from the quotient construction
  through concrete torsion computations (Z/mZ tensor_Z Z/nZ) and
  extension-of-scalars examples to the flatness criterion, structured as
  a single pedagogical unit with explicit isomorphism witnesses for each
  canonical identification.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 80m
status: shipped
produced_by: glm-5.1 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

The tensor product takes two mathematical objects and combines them into one bigger object, the way multiplication takes two numbers and produces their product. The defining feature: if you double one input while keeping the other fixed, the output doubles. The same holds if you double the other input instead. The result is "separately linear" in each input — linear in the first slot when the second is held constant, and linear in the second slot when the first is held constant.

Think of multiplying polynomials. When you compute $(1 + 2x)(3 + 4x)$, you pair every term of the first with every term of the second: $1 \times 3$, $1 \times 4x$, $2x \times 3$, and $2x \times 4x$. Collecting terms gives $3 + 10x + 8x^2$. The tensor product performs exactly this kind of pairing: take every element of the first object, combine it with every element of the second, and then impose the linearity rules so that doubling one factor doubles the result.

Why does this concept exist? The tensor product converts operations that are linear in two inputs separately into ordinary single-input linear operations, which are far simpler to analyse.

## Visual [Beginner]

A $2 \times 3$ grid whose rows correspond to basis elements of a 2-dimensional space and whose columns correspond to basis elements of a 3-dimensional space. Each cell in the grid represents one basis element of the tensor product, which has $2 \times 3 = 6$ dimensions in total.

![Tensor product grid showing how basis elements of two spaces pair to form a 6-dimensional space](/img/tensor-product-modules-placeholder.svg)

The grid encodes the central idea: the tensor product of an $m$-dimensional space with an $n$-dimensional space has $m \times n$ dimensions, one for each pair.

## Worked example [Beginner]

Consider the 2-dimensional vector space $\mathbb{R}^2$ over the real numbers. The tensor product of $\mathbb{R}^2$ with itself produces a 4-dimensional space.

Take $v = (1, 2)$ and $w = (3, 4)$.

Step 1. Write each vector using the standard basis: $v = 1 \cdot e_1 + 2 \cdot e_2$ and $w = 3 \cdot e_1 + 4 \cdot e_2$.

Step 2. Pair every basis element from $v$ with every basis element from $w$, multiplying their coefficients: $1 \times 3 = 3$ paired with $(e_1, e_1)$, $1 \times 4 = 4$ paired with $(e_1, e_2)$, $2 \times 3 = 6$ paired with $(e_2, e_1)$, and $2 \times 4 = 8$ paired with $(e_2, e_2)$.

Step 3. The tensor product of $v$ and $w$ is the element with coefficient $3$ for the first basis pair, $4$ for the second, $6$ for the third, and $8$ for the fourth. The four basis pairs span a space of dimension $2 \times 2 = 4$.

What this tells us: the dimension of the tensor product of an $m$-dimensional space with an $n$-dimensional space is $m \times n$.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What is the dimension of the tensor product of $\mathbb{R}^3$ with $\mathbb{R}^5$?

A. 2
B. 8
C. 15
D. 125

<details class="exercise__hint"><summary>Hint</summary>

The dimension of the tensor product is the product of the dimensions.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** The dimension is $3 \times 5 = 15$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

In a tensor product, doubling the first input while keeping the second input fixed doubles the output.

<details class="exercise__hint"><summary>Hint</summary>

The tensor product is linear in each input separately.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Linearity in the first slot means that replacing the first input $m$ by $2m$ replaces the output by $2$ times the original output.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

The tensor product of a 4-dimensional space with another space has dimension 20. What is the dimension of the other space?

<details class="exercise__hint"><summary>Hint</summary>

If the dimensions are $a$ and $b$, the tensor product has dimension $a \times b$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**5.** If $4 \times b = 20$, then $b = 5$.

</details>

</aside>

## Formal definition [Intermediate+]

Throughout, $R$ is a commutative ring with identity. An $R$-module $M$ is an abelian group equipped with a scalar multiplication $R \times M \to M$ satisfying the same axioms as a vector space, except that the scalars come from $R$ rather than from a field [ref: TODO_REF Atiyah-Macdonald 1969]. Every vector space over a field is a module; every abelian group is a $\mathbb{Z}$-module.

**Definition (Bilinear map).** Let $M$, $N$, $P$ be $R$-modules. A function $f\colon M \times N \to P$ is *$R$-bilinear* if it is $R$-linear in each argument separately:
$$
f(rm + m',\, n) = rf(m,n) + f(m',n), \qquad f(m,\, rn + n') = rf(m,n) + f(m,n')
$$
for all $r \in R$, $m, m' \in M$, $n, n' \in N$.

**Definition (Tensor product).** The *tensor product* $M \otimes_R N$ is an $R$-module equipped with an $R$-bilinear map $\otimes\colon M \times N \to M \otimes_R N$, written $(m,n) \mapsto m \otimes n$, satisfying the following *universal property*: for every $R$-module $P$ and every $R$-bilinear map $f\colon M \times N \to P$, there exists a unique $R$-linear map $\bar{f}\colon M \otimes_R N \to P$ such that $f = \bar{f} \circ \, \otimes$ [ref: TODO_REF Dummit-Foote 2004].

Equivalently, $M \otimes_R N$ is the quotient of the free $R$-module on the set $M \times N$ by the submodule generated by all elements of the form
$$
(m+m',n)-(m,n)-(m',n), \quad (m,n+n')-(m,n)-(m,n'), \quad (rm,n)-r(m,n), \quad (m,rn)-r(m,n)
$$
for $m, m' \in M$, $n, n' \in N$, $r \in R$. The image of $(m,n)$ in this quotient is $m \otimes n$.

### Counterexamples to common slips [Intermediate+]

- *The tensor product of two nonzero modules can be zero.* For instance, $\mathbb{Z}/2\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/3\mathbb{Z} = 0$ because $1 \otimes 1 = 3(1 \otimes 1) - 2(1 \otimes 1) = 1 \otimes 3 - 2 \otimes 1 = 0 - 0 = 0$, using $r(m \otimes n) = (rm) \otimes n = m \otimes (rn)$ and the fact that $3 = 0$ in $\mathbb{Z}/3\mathbb{Z}$, $2 = 0$ in $\mathbb{Z}/2\mathbb{Z}$.

- *Not every element of $M \otimes_R N$ is a pure tensor $m \otimes n$.* General elements are finite sums $\sum_i m_i \otimes n_i$. For example, in $\mathbb{R}^2 \otimes_\mathbb{R} \mathbb{R}^2$, the element $e_1 \otimes e_1 + e_2 \otimes e_2$ cannot be written as a single pure tensor.

- *Injective maps need not remain injective after tensoring.* The map $\mathbb{Z} \xrightarrow{\times 2} \mathbb{Z}$ is injective, but after tensoring with $\mathbb{Z}/2\mathbb{Z}$ the induced map $\mathbb{Z}/2\mathbb{Z} \to \mathbb{Z}/2\mathbb{Z}$ sends $\bar{1} \mapsto \overline{2} = \bar{0}$, which is the zero map.

## Key theorem with proof [Intermediate+]

**Theorem (Universal property — existence and uniqueness).** Let $R$ be a commutative ring and let $M$, $N$ be $R$-modules. The tensor product $M \otimes_R N$ exists and is unique up to unique isomorphism.

(i) *Existence.* The quotient of the free $R$-module on $M \times N$ by the bilinearity relations is a tensor product of $M$ and $N$.

(ii) *Uniqueness.* If $T_1$ and $T_2$ are both tensor products of $M$ and $N$ (with bilinear maps $\otimes_1$, $\otimes_2$), there is a unique isomorphism $\varphi\colon T_1 \to T_2$ satisfying $\varphi(m \otimes_1 n) = m \otimes_2 n$ for all $m \in M$, $n \in N$.

*Proof.* **Existence.** Let $F$ be the free $R$-module on the set $M \times N$. Let $K$ be the submodule of $F$ generated by all elements
$$
(m+m',n)-(m,n)-(m',n), \quad (m,n+n')-(m,n)-(m,n'), \quad (rm,n)-r(m,n), \quad (m,rn)-r(m,n).
$$
Set $T = F/K$ and write $m \otimes n$ for the image of $(m,n)$ in $T$. By construction, the relations enforcing $R$-bilinearity hold in $T$.

Given an $R$-bilinear map $f\colon M \times N \to P$, define $\bar{f}\colon T \to P$ by $\bar{f}\bigl((m,n) + K\bigr) = f(m,n)$. This is well-defined because each generator of $K$ maps to zero: for instance,
$$
\bar{f}\bigl((m+m',n)-(m,n)-(m',n)\bigr) = f(m+m',n) - f(m,n) - f(m',n) = 0
$$
by bilinearity of $f$. The map $\bar{f}$ is $R$-linear on $F$ and descends to $T = F/K$. Uniqueness of $\bar{f}$ follows because the elements $m \otimes n$ generate $T$ as an $R$-module, so any $R$-linear map from $T$ is determined by its values on these generators.

**Uniqueness.** Suppose $T_1$ and $T_2$ are both tensor products with bilinear maps $\otimes_1$ and $\otimes_2$. By the universal property of $T_1$, there is a unique $R$-linear map $\varphi\colon T_1 \to T_2$ with $\varphi(m \otimes_1 n) = m \otimes_2 n$. By the universal property of $T_2$, there is a unique $R$-linear map $\psi\colon T_2 \to T_1$ with $\psi(m \otimes_2 n) = m \otimes_1 n$. Then $\psi \circ \varphi$ and $\varphi \circ \psi$ are $R$-linear endomorphisms that satisfy the same identity as the respective identity maps. By uniqueness in the universal property, $\psi \circ \varphi = \mathrm{id}_{T_1}$ and $\varphi \circ \psi = \mathrm{id}_{T_2}$. $\square$

**Bridge.** The universal property builds toward [01.01.15] (bilinear and quadratic forms), where every bilinear form on $V \times W$ corresponds to a linear functional on $V \otimes W$, and this is exactly the mechanism that identifies $\mathrm{Bil}(V,W;R)$ with $\operatorname{Hom}(V \otimes W, R)$. The foundational reason is that the tensor product is the universal receptacle for bilinear maps, and the central insight is that bilinearity on a product decomposes into linearity on the tensor product. The bridge is between the bilinear world of two-variable maps and the linear world of single-variable maps, and the pattern appears again in [01.01.05] (rank-nullity) through the tensor-hom adjunction.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Compute the rank of the $\mathbb{Z}$-module $\mathbb{Z}/2\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/5\mathbb{Z}$.

<details class="exercise__hint"><summary>Hint</summary>

Since $\gcd(2,5) = 1$, the tensor product $\mathbb{Z}/m\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/n\mathbb{Z} \cong \mathbb{Z}/\gcd(m,n)\mathbb{Z}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**0.** Since $\gcd(2,5) = 1$, we have $\mathbb{Z}/2\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/5\mathbb{Z} \cong \mathbb{Z}/1\mathbb{Z} = 0$. The module is the zero module, which has rank $0$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 2 (easy, symbolic).**

Write the element $(2e_1 + 3e_2) \otimes (e_1 - e_2)$ in $\mathbb{R}^2 \otimes_\mathbb{R} \mathbb{R}^2$ as a linear combination of the four basis tensors $e_i \otimes e_j$.

<details class="exercise__hint"><summary>Hint</summary>

Expand using bilinearity: $(au + bv) \otimes w = a(u \otimes w) + b(v \otimes w)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$$
(2e_1 + 3e_2) \otimes (e_1 - e_2) = 2(e_1 \otimes e_1) - 2(e_1 \otimes e_2) + 3(e_2 \otimes e_1) - 3(e_2 \otimes e_2).
$$

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Prove that the map $\varphi\colon M \to M \otimes_R R$ defined by $\varphi(m) = m \otimes 1$ is an isomorphism of $R$-modules, with inverse $\psi\colon M \otimes_R R \to M$ given by $\psi(m \otimes r) = rm$.

<details class="exercise__hint"><summary>Hint</summary>

Show $\psi$ is well-defined by verifying it descends from a bilinear map $M \times R \to M$, then show $\psi \circ \varphi = \mathrm{id}_M$ and $\varphi \circ \psi = \mathrm{id}_{M \otimes R}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The map $M \times R \to M$ sending $(m, r) \mapsto rm$ is $R$-bilinear, so it induces a well-defined $R$-linear map $\psi\colon M \otimes_R R \to M$ with $\psi(m \otimes r) = rm$.

For $m \in M$: $\psi(\varphi(m)) = \psi(m \otimes 1) = 1 \cdot m = m$, so $\psi \circ \varphi = \mathrm{id}_M$.

For $\sum_i m_i \otimes r_i \in M \otimes_R R$: each term satisfies $m_i \otimes r_i = r_i m_i \otimes 1$, so $\sum_i m_i \otimes r_i = \sum_i r_i m_i \otimes 1 = (\sum_i r_i m_i) \otimes 1$. Then $\varphi(\psi(\sum_i m_i \otimes r_i)) = \varphi(\sum_i r_i m_i) = (\sum_i r_i m_i) \otimes 1 = \sum_i m_i \otimes r_i$. Hence $\varphi \circ \psi = \mathrm{id}$. Therefore $\varphi$ is an isomorphism.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Prove that $M \otimes_R N \cong N \otimes_R M$ via the map $m \otimes n \mapsto n \otimes m$.

<details class="exercise__hint"><summary>Hint</summary>

Construct a bilinear map $M \times N \to N \otimes_R M$ and use the universal property.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The map $M \times N \to N \otimes_R M$ defined by $(m, n) \mapsto n \otimes m$ is $R$-bilinear: $(rm, n) \mapsto n \otimes rm = r(n \otimes m)$, and $(m_1 + m_2, n) \mapsto n \otimes (m_1 + m_2) = n \otimes m_1 + n \otimes m_2$. By the universal property, this induces a unique $R$-linear map $\varphi\colon M \otimes_R N \to N \otimes_R M$ with $\varphi(m \otimes n) = n \otimes m$.

Similarly, the map $(n, m) \mapsto m \otimes n$ induces $\psi\colon N \otimes_R M \to M \otimes_R N$ with $\psi(n \otimes m) = m \otimes n$. Since $\psi \circ \varphi$ and $\varphi \circ \psi$ are both the identity on generators, both composites are identity maps. Therefore $\varphi$ is an isomorphism.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

Compute the order of the element $\bar{1} \otimes \bar{1}$ in the abelian group $\mathbb{Z}/4\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/6\mathbb{Z}$.

<details class="exercise__hint"><summary>Hint</summary>

Use $\mathbb{Z}/m\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/n\mathbb{Z} \cong \mathbb{Z}/\gcd(m,n)\mathbb{Z}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**2.** We have $\mathbb{Z}/4\mathbb{Z} \otimes_\mathbb{Z} \mathbb{Z}/6\mathbb{Z} \cong \mathbb{Z}/\gcd(4,6)\mathbb{Z} = \mathbb{Z}/2\mathbb{Z}$. The element $\bar{1} \otimes \bar{1}$ generates this group and has order $2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Prove that $M \otimes_R (N_1 \oplus N_2) \cong (M \otimes_R N_1) \oplus (M \otimes_R N_2)$ as $R$-modules.

<details class="exercise__hint"><summary>Hint</summary>

Define maps in both directions using the universal property. Use the inclusion and projection maps for the direct sum.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define $\varphi\colon M \otimes_R (N_1 \oplus N_2) \to (M \otimes_R N_1) \oplus (M \otimes_R N_2)$ by $\varphi(m \otimes (n_1, n_2)) = (m \otimes n_1, m \otimes n_2)$, induced by the bilinear map $M \times (N_1 \oplus N_2) \to (M \otimes N_1) \oplus (M \otimes N_2)$ sending $(m, (n_1, n_2)) \mapsto (m \otimes n_1, m \otimes n_2)$.

Define $\psi$ in the reverse direction by $\psi\bigl((m \otimes n_1,\, m' \otimes n_2)\bigr) = m \otimes (n_1, 0) + m' \otimes (0, n_2)$. On summands: $\psi(m \otimes n_1, 0) = m \otimes (n_1, 0)$ and $\psi(0, m \otimes n_2) = m \otimes (0, n_2)$.

Check: $\psi \circ \varphi$ sends $m \otimes (n_1, n_2)$ to $m \otimes (n_1, 0) + m \otimes (0, n_2) = m \otimes (n_1, n_2)$. The reverse composite sends each summand back to itself. Both composites are identities on generators, so $\varphi$ is an isomorphism.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove the tensor-hom adjunction: for $R$-modules $M$, $N$, $P$, there is a canonical isomorphism of $R$-modules
$$
\operatorname{Hom}_R(M \otimes_R N,\, P) \cong \operatorname{Hom}_R(M,\, \operatorname{Hom}_R(N, P)).
$$

<details class="exercise__hint"><summary>Hint</summary>

Given $g\colon M \otimes_R N \to P$, define $\Phi(g)\colon M \to \operatorname{Hom}(N, P)$ by $\Phi(g)(m)(n) = g(m \otimes n)$. Verify that both $\Phi$ and its inverse are well-defined and $R$-linear.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define $\Phi\colon \operatorname{Hom}_R(M \otimes_R N, P) \to \operatorname{Hom}_R(M, \operatorname{Hom}_R(N, P))$ as follows. Given $g \in \operatorname{Hom}_R(M \otimes_R N, P)$, set $\Phi(g)(m)(n) = g(m \otimes n)$.

For fixed $m$, the map $n \mapsto g(m \otimes n)$ is $R$-linear because $g$ is $R$-linear and $n \mapsto m \otimes n$ is $R$-linear. For fixed $n$, the map $m \mapsto g(m \otimes n)$ is $R$-linear. So $\Phi(g) \in \operatorname{Hom}_R(M, \operatorname{Hom}_R(N, P))$.

Define the inverse $\Psi\colon \operatorname{Hom}_R(M, \operatorname{Hom}_R(N, P)) \to \operatorname{Hom}_R(M \otimes_R N, P)$ by: given $f\colon M \to \operatorname{Hom}_R(N, P)$, the map $M \times N \to P$ sending $(m, n) \mapsto f(m)(n)$ is $R$-bilinear, so by the universal property it induces $\Psi(f)\colon M \otimes_R N \to P$ with $\Psi(f)(m \otimes n) = f(m)(n)$.

Check: $\Psi(\Phi(g))(m \otimes n) = \Phi(g)(m)(n) = g(m \otimes n)$, so $\Psi \circ \Phi = \mathrm{id}$. Similarly, $\Phi(\Psi(f))(m)(n) = \Psi(f)(m \otimes n) = f(m)(n)$, so $\Phi \circ \Psi = \mathrm{id}$. Both maps are $R$-linear. Therefore $\Phi$ is an isomorphism.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Let $F$ be a free $R$-module with basis $\{f_i\}_{i \in I}$. Prove that the functor $F \otimes_R -$ is exact: if $0 \to A \xrightarrow{\alpha} B \xrightarrow{\beta} C \to 0$ is a short exact sequence of $R$-modules, then $0 \to F \otimes_R A \xrightarrow{\mathrm{id} \otimes \alpha} F \otimes_R B \xrightarrow{\mathrm{id} \otimes \beta} F \otimes_R C \to 0$ is also exact.

<details class="exercise__hint"><summary>Hint</summary>

Use the isomorphism $F \otimes_R M \cong \bigoplus_{i \in I} M$ for any $R$-module $M$, then verify exactness componentwise.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Since $F$ is free with basis $\{f_i\}$, the map $F \otimes_R M \to \bigoplus_{i \in I} M$ sending $f_i \otimes m \mapsto (0, \ldots, m, \ldots)$ (with $m$ in the $i$-th position) is an isomorphism, natural in $M$. Under this identification, the map $\mathrm{id} \otimes \alpha$ becomes $\bigoplus_{i \in I} \alpha\colon \bigoplus_I A \to \bigoplus_I B$, and $\mathrm{id} \otimes \beta$ becomes $\bigoplus_{i \in I} \beta\colon \bigoplus_I B \to \bigoplus_I C$.

Since direct sums preserve exactness, the sequence $0 \to \bigoplus_I A \xrightarrow{\bigoplus \alpha} \bigoplus_I B \xrightarrow{\bigoplus \beta} \bigoplus_I C \to 0$ is exact. The naturality of the isomorphism $F \otimes_R M \cong \bigoplus_I M$ gives a commutative diagram, so the original tensor-product sequence is exact as well.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Associativity).** For $R$-modules $M$, $N$, $P$, there is a canonical isomorphism $(M \otimes_R N) \otimes_R P \cong M \otimes_R (N \otimes_R P)$ sending $(m \otimes n) \otimes p \mapsto m \otimes (n \otimes p)$.

Both sides satisfy the universal property for $R$-trilinear maps $M \times N \times P \to Q$. The isomorphism identifies the two universal receptacles [ref: TODO_REF Eisenbud 1995].

**Theorem 2 (Commutativity).** For $R$-modules $M$, $N$, there is a canonical isomorphism $M \otimes_R N \cong N \otimes_R M$ sending $m \otimes n \mapsto n \otimes m$.

This is an isomorphism, not an equality: $m \otimes n$ and $n \otimes m$ live in different modules. Commutativity of $R$ is essential; the map $(m,n) \mapsto n \otimes m$ is bilinear because scalars commute in $R$.

**Theorem 3 (Distributivity over direct sums).** $M \otimes_R \bigl(\bigoplus_{i \in I} N_i\bigr) \cong \bigoplus_{i \in I} (M \otimes_R N_i)$.

Tensor products commute with arbitrary direct sums in the second argument. The isomorphism sends $m \otimes (n_i)_{i \in I}$ to $(m \otimes n_i)_{i \in I}$.

**Theorem 4 (Tensor product of algebras).** If $A$ and $B$ are commutative $R$-algebras, then $A \otimes_R B$ is a commutative $R$-algebra with multiplication $(a \otimes b)(a' \otimes b') = aa' \otimes bb'$.

This equips the tensor product of algebras with a ring structure. The polynomial ring $R[x] \otimes_R R[y] \cong R[x,y]$ is the canonical example: the variables from each factor become independent [ref: TODO_REF Atiyah-Macdonald 1969].

**Theorem 5 (Extension of scalars).** Let $f\colon R \to S$ be a homomorphism of commutative rings. For any $R$-module $M$, the tensor product $S \otimes_R M$ is an $S$-module with $s \cdot (s' \otimes m) = (ss') \otimes m$. The functor $S \otimes_R -$ is left adjoint to the restriction functor from $S$-modules to $R$-modules.

Extension of scalars converts an $R$-module into an $S$-module. The canonical example: if $V$ is an $\mathbb{R}$-vector space, then $\mathbb{C} \otimes_\mathbb{R} V$ is a complex vector space (complexification).

**Theorem 6 (Tensor-hom adjunction).** For $R$-modules $M$, $N$, $P$, there is a canonical isomorphism of $R$-modules:
$$
\operatorname{Hom}_R(M \otimes_R N,\, P) \cong \operatorname{Hom}_R(M,\, \operatorname{Hom}_R(N, P)).
$$
The isomorphism sends $g \mapsto [m \mapsto [n \mapsto g(m \otimes n)]]$.

This is the defining adjunction of the tensor product: the functor $- \otimes_R N$ is left adjoint to $\operatorname{Hom}_R(N, -)$. As with any adjunction, the tensor product preserves colimits (in particular, direct sums and cokernels) [ref: TODO_REF Eisenbud 1995].

**Theorem 7 (Flatness criterion).** An $R$-module $M$ is *flat* if the functor $M \otimes_R -$ is exact. Equivalently, $M$ is flat if for every injective $R$-linear map $N' \to N$, the induced map $M \otimes_R N' \to M \otimes_R N$ is injective.

Free modules are flat. Projective modules are flat. Finitely generated flat modules over a Noetherian local ring are free. The failure of flatness is measured by the Tor functor: $M$ is flat if and only if $\operatorname{Tor}^R_n(M, -) = 0$ for all $n \geq 1$.

**Synthesis.** The tensor-hom adjunction is the foundational reason that the tensor product governs the passage between multilinear and linear algebra across all of commutative algebra. The central insight is that the six structural properties — associativity, commutativity, distributivity, the unit $R$, the tensor-hom adjunction, and extension of scalars — equip the category of $R$-modules with the structure of a symmetric monoidal closed category. Putting these together with the flatness criterion, which detects when $M \otimes_R -$ preserves exact sequences, the bridge is between the homological algebra of Tor functors and the geometric problem of how modules change base ring.

This is exactly the structure that generalises from $R$-modules to quasi-coherent sheaves on a scheme, where the tensor product computes fibre products of affine schemes. The pattern recurs in [01.01.15] (bilinear and quadratic forms), where the tensor product encodes the universal bilinear pairing, and appears again in [01.01.03] (vector space), where extension of scalars transforms a real vector space into a complex one.

## Full proof set [Master]

**Proposition 1 (Tensor-hom adjunction).** For $R$-modules $M$, $N$, $P$, the map $\Phi\colon \operatorname{Hom}_R(M \otimes_R N, P) \to \operatorname{Hom}_R(M, \operatorname{Hom}_R(N, P))$ defined by $\Phi(g)(m)(n) = g(m \otimes n)$ is an isomorphism of $R$-modules.

*Proof.* For fixed $g \in \operatorname{Hom}_R(M \otimes_R N, P)$, the map $\Phi(g)\colon M \to \operatorname{Hom}_R(N, P)$ is $R$-linear: $\Phi(g)(rm + m')(n) = g((rm + m') \otimes n) = g(rm \otimes n + m' \otimes n) = rg(m \otimes n) + g(m' \otimes n) = r\Phi(g)(m)(n) + \Phi(g)(m')(n)$.

The inverse $\Psi$ sends $f \in \operatorname{Hom}_R(M, \operatorname{Hom}_R(N, P))$ to the map $\Psi(f)\colon M \otimes_R N \to P$ induced by the bilinear map $(m, n) \mapsto f(m)(n)$. Bilinearity of $(m, n) \mapsto f(m)(n)$ follows from $R$-linearity of $f(m)$ in $n$ and $R$-linearity of $f$ in $m$.

One verifies $\Psi(\Phi(g))(m \otimes n) = g(m \otimes n)$ and $\Phi(\Psi(f))(m)(n) = f(m)(n)$ on generators, so both composites are identities. $\square$

**Proposition 2 (Extension of scalars is left adjoint to restriction).** Let $f\colon R \to S$ be a ring homomorphism. The functor $S \otimes_R -$ from $R$-modules to $S$-modules is left adjoint to the restriction functor $U$ from $S$-modules to $R$-modules.

*Proof.* For an $R$-module $M$ and an $S$-module $N$, define
$$
\Phi\colon \operatorname{Hom}_S(S \otimes_R M, N) \to \operatorname{Hom}_R(M, U(N)), \quad \Phi(g)(m) = g(1 \otimes m).
$$
This is well-defined: $g(1 \otimes m) \in N$ and the map $m \mapsto g(1 \otimes m)$ is $R$-linear because $g$ is $S$-linear and $1 \otimes (rm) = f(r) \otimes m$, so $g(1 \otimes rm) = g(f(r) \otimes m) = f(r) g(1 \otimes m) = r \cdot g(1 \otimes m)$.

The inverse sends $h \in \operatorname{Hom}_R(M, U(N))$ to the unique $S$-linear map $\Psi(h)\colon S \otimes_R M \to N$ with $\Psi(h)(s \otimes m) = s \cdot h(m)$. This is induced by the $R$-bilinear map $S \times M \to N$ sending $(s, m) \mapsto s \cdot h(m)$.

Check: $\Psi(\Phi(g))(s \otimes m) = s \cdot \Phi(g)(m) = s \cdot g(1 \otimes m) = g(s \otimes m)$, and $\Phi(\Psi(h))(m) = \Psi(h)(1 \otimes m) = 1 \cdot h(m) = h(m)$. Both composites are identities. $\square$

**Proposition 3 (Associativity).** The map $\alpha\colon (M \otimes_R N) \otimes_R P \to M \otimes_R (N \otimes_R P)$ defined by $(m \otimes n) \otimes p \mapsto m \otimes (n \otimes p)$ is a canonical isomorphism.

*Proof.* For fixed $p \in P$, the map $M \times N \to M \otimes_R (N \otimes_R P)$ sending $(m, n) \mapsto m \otimes (n \otimes p)$ is $R$-bilinear, so it induces $\alpha_p\colon M \otimes_R N \to M \otimes_R (N \otimes_R P)$ with $\alpha_p(m \otimes n) = m \otimes (n \otimes p)$.

The map $(M \otimes_R N) \times P \to M \otimes_R (N \otimes_R P)$ sending $(t, p) \mapsto \alpha_p(t)$ is $R$-bilinear: for $r \in R$, $\alpha_{rp}(m \otimes n) = m \otimes (n \otimes rp) = m \otimes r(n \otimes p) = r(m \otimes (n \otimes p)) = r\alpha_p(m \otimes n)$. So it induces $\alpha\colon (M \otimes_R N) \otimes_R P \to M \otimes_R (N \otimes_R P)$.

The inverse $\beta\colon M \otimes_R (N \otimes_R P) \to (M \otimes_R N) \otimes_R P$ is constructed symmetrically. One verifies $\beta(\alpha((m \otimes n) \otimes p)) = (m \otimes n) \otimes p$ and $\alpha(\beta(m \otimes (n \otimes p))) = m \otimes (n \otimes p)$ on generators. $\square$

## Connections [Master]

- **Bilinear and quadratic forms [01.01.15].** The tensor product provides the universal receptacle for bilinear maps: every bilinear form on $V \times W$ factors through $V \otimes W$ as a linear functional. This is exactly the bridge between bilinear algebra and the linear-algebraic formalism of quadratic forms, and the identification $\mathrm{Bil}(V, W; R) \cong (V \otimes W)^*$ is the starting point for the classification of bilinear forms.

- **Linear transformation, rank-nullity [01.01.05].** The tensor-hom adjunction $\operatorname{Hom}(M \otimes N, P) \cong \operatorname{Hom}(M, \operatorname{Hom}(N, P))$ is the natural generalisation of the rank-nullity framework: the functor $- \otimes_R N$ is left adjoint to $\operatorname{Hom}_R(N, -)$, and the exactness properties of this adjunction control how rank behaves under extension of scalars.

- **Vector space [01.01.03].** Modules generalise vector spaces by replacing the field of scalars with a ring. The tensor product of vector spaces over a field is the simplest case: dimensions multiply, every element decomposes as a sum of pure tensors, and flatness is automatic because vector spaces are free. The foundational reason that the module-theoretic tensor product reduces to the vector-space tensor product when $R$ is a field is that vector spaces are free $R$-modules.

## Historical & philosophical context [Master]

Whitney 1938 introduced the tensor product of abelian groups in the context of cohomology [ref: TODO_REF Whitney 1938], defining $\otimes$ as a functor on the category of abelian groups that converts bilinear pairings into homomorphisms. The modern algebraic formulation crystallised with Bourbaki's *Algebre* in the 1940s, which systematised the universal property and the quotient construction for modules over a commutative ring [ref: TODO_REF Bourbaki 1940s].

The tensor-hom adjunction was recognised as the structural centrepiece by Cartan and Eilenberg 1956 in *Homological Algebra*, and extension of scalars became the primary tool in algebraic geometry through Grothendieck's 1960s reformulation of scheme theory in terms of quasi-coherent sheaves and their tensor products. Flatness, introduced by Serre 1956 in the context of algebraic geometry, measures when the tensor product preserves exact sequences, and its characterisation via the Tor functor closes the loop between the commutative-algebraic and homological-algebraic perspectives.

## Bibliography [Master]

```bibtex
@article{Whitney1938,
  author = {Whitney, Hassler},
  title = {Tensor products of abelian groups},
  journal = {Annals of Mathematics},
  volume = {39},
  year = {1938},
  pages = {657--665},
}

@book{Bourbaki1989,
  author = {Bourbaki, Nicolas},
  title = {Algebra I: Chapters 1--3},
  publisher = {Springer},
  year = {1989},
}

@book{AtiyahMacdonald1969,
  author = {Atiyah, Michael F. and Macdonald, Ian G.},
  title = {Introduction to Commutative Algebra},
  publisher = {Addison-Wesley},
  year = {1969},
}

@book{DummitFoote2004,
  author = {Dummit, David S. and Foote, Richard M.},
  title = {Abstract Algebra},
  edition = {3rd},
  publisher = {Wiley},
  year = {2004},
}

@book{Eisenbud1995,
  author = {Eisenbud, David},
  title = {Commutative Algebra with a View Toward Algebraic Geometry},
  publisher = {Springer},
  year = {1995},
  series = {Graduate Texts in Mathematics 150},
}
```
