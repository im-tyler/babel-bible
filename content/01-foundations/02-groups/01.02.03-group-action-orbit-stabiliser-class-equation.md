---
id: 01.02.03
title: "Group action, orbit-stabiliser, class equation"
slug: group-action-orbit-stabiliser-class-equation
section: foundations
chapter: groups
concept_catalog_id: foundations.group-action
prerequisites:
  - 01.02.01
  - 01.02.02
successors: []
tier_anchors:
  beginner: "D4 symmetries moving the corners of a square"
  intermediate: "Dummit-Foote §4.1; Artin Algebra Ch. 6"
  master: "Burnside 1897 Theory of Groups of Finite Order; Sylow 1872; Dummit-Foote §4.1-4.3"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Dummit-Foote — Abstract Algebra"
    locator: "§4.1-4.3, group actions"
  - source: TODO_REF
    path: "Burnside — Theory of Groups of Finite Order"
    locator: "Ch. 1-2, group actions and Burnside's lemma"
  - source: TODO_REF
    path: "Artin — Algebra"
    locator: "Ch. 6, symmetries and group actions"
  - source: TODO_REF
    path: "Rotman — An Introduction to the Theory of Groups"
    locator: "§3, orbits and stabilisers"
lean_status: none
lean_mathlib_gap: |
  Mathlib has complete infrastructure for group actions via the MulAction
  typeclass, including orbit, stabiliser, and the orbit-stabiliser theorem
  formalization. Burnside's lemma and the class equation also have Mathlib
  proofs. However the Codex curriculum needs a pedagogical bridge from
  beginner-tier language (symmetries moving corners of a square) through
  the orbit-stabiliser counting argument to the class equation and its
  consequences for Sylow theory, with notation matching the later Lie-group
  and representation-theory units. A future formalization should align
  local names like orbit, stabiliser, conjugacy_class, class_equation with
  Mathlib's MulAction.orbit, MulAction.stabilizer, and related APIs.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 75m
status: shipped
produced_by: glm-5.1 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

A group action is a way for the elements of a group to move objects around in a set. Think of the symmetries of a square: each symmetry rearranges the four corners. The group of symmetries acts on the set of corners by moving them to new positions.

An orbit is the set of places a single object can reach. Pick any corner of the square and apply all the symmetries: the rotations and reflections can send it to any of the other three corners. All four corners form one orbit because the symmetries connect every corner to every other corner.

A stabiliser is the set of symmetries that leave one specific object where it is. Pick the top-left corner. The only symmetries that keep it in place are the identity and the reflection across the diagonal through that corner: two elements. But the centre of the square is fixed by all eight symmetries, so its stabiliser is the whole group.

Why does this concept exist? Group actions connect abstract algebra to concrete objects you can see and count, turning questions about group structure into questions about rearrangements of a set.

## Visual [Beginner]

The dihedral group $D_4$ acting on the four corners of a square. Eight symmetries — four rotations and four reflections — permute the corners. The arrows show how a $90^\circ$ rotation sends each corner to the next one clockwise.

![D4 acting on the four corners of a square](/img/group-action-orbit-stabiliser-class-equation-placeholder.svg)

Each corner belongs to the same orbit: any corner can be moved to any other by some symmetry in $D_4$.

## Worked example [Beginner]

Consider the dihedral group $D_4$ acting on the four corners of a square. The group has $8$ elements: the identity, rotations by $90^\circ$, $180^\circ$, and $270^\circ$, and four reflections.

Step 1. Pick corner $1$ (top-left). Its orbit is the set of all positions reachable by symmetries: $\{1, 2, 3, 4\}$. Every corner can be reached, so the orbit has size $4$.

Step 2. The stabiliser of corner $1$ consists of the symmetries that fix corner $1$. Only the identity and the reflection across the diagonal through corner $1$ keep it in place. So the stabiliser has size $2$.

Step 3. The group has $8$ elements. The orbit size ($4$) times the stabiliser size ($2$) equals $8$. This is the orbit-stabiliser relationship: the size of the group equals the size of the orbit times the size of the stabiliser.

What this tells us: counting where things go and what keeps them fixed gives a clean arithmetic relationship with the size of the whole symmetry group.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

The dihedral group $D_4$ acts on the set of four corners of a square. What is the size of the orbit of corner $1$?

A. 1
B. 2
C. 4
D. 8

<details class="exercise__hint"><summary>Hint</summary>

Every corner can be reached from corner $1$ by some rotation or reflection.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** The orbit has size $4$: corner $1$ can be sent to any of the four corners by the symmetries of $D_4$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

If a group $G$ acts on a set and some element has a stabiliser equal to the whole group, then that element is fixed by every group element.

<details class="exercise__hint"><summary>Hint</summary>

The stabiliser of $x$ is the set of all $g$ in $G$ with $g \cdot x = x$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** If the stabiliser of $x$ equals all of $G$, then every element of $G$ fixes $x$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A group of order $12$ acts on a set. An element $x$ has an orbit of size $3$. How many elements are in the stabiliser of $x$?

<details class="exercise__hint"><summary>Hint</summary>

The orbit-stabiliser relationship says: order of group equals size of orbit times size of stabiliser.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**4.** The stabiliser has size $12 / 3 = 4$.

</details>

</aside>

## Formal definition [Intermediate+]

Let $G$ be a group and $X$ a set.

**Definition (Group action).** A *left action* of $G$ on $X$ is a map $G \times X \to X$, written $(g, x) \mapsto g \cdot x$, satisfying:
1. $e \cdot x = x$ for all $x \in X$ (identity fixes every point).
2. $(gh) \cdot x = g \cdot (h \cdot x)$ for all $g, h \in G$ and $x \in X$ (compatibility).

A set equipped with such an action is called a *$G$-set* [ref: TODO_REF Dummit-Foote §4.1].

**Definition (Orbit).** For $x \in X$, the *orbit* of $x$ is
$$
\operatorname{Orb}(x) = \{g \cdot x : g \in G\}.
$$

**Definition (Stabiliser).** For $x \in X$, the *stabiliser* of $x$ is
$$
\operatorname{Stab}(x) = \{g \in G : g \cdot x = x\}.
$$

The orbits partition $X$ into disjoint subsets. The stabiliser $\operatorname{Stab}(x)$ is a subgroup of $G$ for every $x \in X$.

**Definition (Fixed points).** For $g \in G$, the *fixed-point set* of $g$ is $X^g = \{x \in X : g \cdot x = x\}$.

**Definition (Centraliser and centre).** When $G$ acts on itself by conjugation $g \cdot h = ghg^{-1}$, the stabiliser of $h$ is the *centraliser* $C_G(h) = \{g \in G : gh = hg\}$. The *centre* of $G$ is $Z(G) = \{g \in G : gx = xg \text{ for all } x \in G\}$.

### Counterexamples to common slips [Intermediate+]

- *A group action is not just any map.* The constant map $(g, x) \mapsto x_0$ for a fixed $x_0$ fails the identity axiom unless $x_0 = x$ for every $x$. The map $(g, x) \mapsto x$ for all $g$ and $x$ satisfies both axioms but produces only singleton orbits.
- *The stabiliser depends on the point.* Different points have different stabilisers. In $D_4$ acting on the corners, the stabiliser of a corner has $2$ elements, but the stabiliser of the centre (if included in the set) has $8$ elements.
- *Orbits need not all have the same size.* The symmetric group $S_4$ acting on the set of ordered pairs $\{1,2,3,4\}^2$ has orbits of sizes $12$ (off-diagonal pairs) and $4$ (diagonal pairs).

## Key theorem with proof [Intermediate+]

**Theorem (Orbit-Stabiliser).** Let $G$ be a finite group acting on a set $X$. For any $x \in X$:
$$
|G| = |\operatorname{Orb}(x)| \cdot |\operatorname{Stab}(x)|.
$$

*Proof.* Define the map $\varphi: G \to \operatorname{Orb}(x)$ by $\varphi(g) = g \cdot x$. This map is surjective: every element of $\operatorname{Orb}(x)$ has the form $g \cdot x$ for some $g$.

Two elements $g, h \in G$ satisfy $\varphi(g) = \varphi(h)$ if and only if $g \cdot x = h \cdot x$, which gives $h^{-1}g \cdot x = x$, meaning $h^{-1}g \in \operatorname{Stab}(x)$. Equivalently, $g$ and $h$ lie in the same left coset of $\operatorname{Stab}(x)$ in $G$.

The fibres of $\varphi$ are precisely the left cosets of $\operatorname{Stab}(x)$. Each fibre has exactly $|\operatorname{Stab}(x)|$ elements, and there are $|\operatorname{Orb}(x)|$ distinct fibres (one for each element of the orbit). Therefore:
$$
|G| = |\operatorname{Stab}(x)| \cdot |\operatorname{Orb}(x)|.
$$
$\square$

**Bridge.** The orbit-stabiliser theorem builds toward [01.02.04] (Sylow theorems), where group actions on sets of subsets produce the existence, conjugacy, and counting results for Sylow $p$-subgroups. This result appears again in [01.02.02] (subgroups and cosets), since the coset partition underlying Lagrange's theorem is the same mechanism at work here: the fibres of $\varphi$ are cosets of the stabiliser. The foundational reason is that every group action produces a coset decomposition, and this is exactly the bridge between the algebraic structure of $G$ and the combinatorial structure of $X$. The central insight is that the stabiliser measures how much symmetry fixes a point, and the orbit measures how far that point can travel.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

The symmetric group $S_3$ acts on the set $\{1, 2, 3\}$ by permutation. Compute the size of the orbit of $1$ and the size of the stabiliser of $1$.

<details class="exercise__hint"><summary>Hint</summary>

$S_3$ has order $6$. Every element can be sent to every other by some permutation.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The orbit of $1$ is $\{1, 2, 3\}$, of size $3$. The stabiliser of $1$ consists of permutations fixing $1$: the identity and the transposition $(2\;3)$, giving size $2$. Check: $3 \times 2 = 6 = |S_3|$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 2 (easy, symbolic).**

Verify that the map $S_3 \times \{1, 2, 3\} \to \{1, 2, 3\}$ given by $(\sigma, x) \mapsto \sigma(x)$ satisfies both axioms of a group action.

<details class="exercise__hint"><summary>Hint</summary>

Check that the identity permutation fixes every element, and that composing permutations matches applying the group operation first.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Identity axiom: $\operatorname{id}(x) = x$ for all $x$. Compatibility axiom: $(\sigma\tau)(x) = \sigma(\tau(x))$ by definition of function composition. Both axioms hold.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Prove that $\operatorname{Stab}(x)$ is a subgroup of $G$ for any $x \in X$.

<details class="exercise__hint"><summary>Hint</summary>

Use the one-step subgroup test: show that $g, h \in \operatorname{Stab}(x)$ implies $gh^{-1} \in \operatorname{Stab}(x)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $g, h \in \operatorname{Stab}(x)$, so $g \cdot x = x$ and $h \cdot x = x$. Then:
$$
(gh^{-1}) \cdot x = g \cdot (h^{-1} \cdot x).
$$
From $h \cdot x = x$, apply $h^{-1}$ to both sides: $h^{-1} \cdot (h \cdot x) = h^{-1} \cdot x$, giving $x = h^{-1} \cdot x$. Therefore:
$$
(gh^{-1}) \cdot x = g \cdot x = x.
$$
So $gh^{-1} \in \operatorname{Stab}(x)$, and $\operatorname{Stab}(x) \leq G$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Prove that the orbits of a group action partition $X$: any two orbits are either identical or disjoint, and every element of $X$ lies in some orbit.

<details class="exercise__hint"><summary>Hint</summary>

Define a relation $x \sim y$ if $y = g \cdot x$ for some $g \in G$. Show this is an equivalence relation.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define $x \sim y$ if and only if $y = g \cdot x$ for some $g \in G$.

Reflexive: $x = e \cdot x$, so $x \sim x$.

Symmetric: if $y = g \cdot x$, then $x = g^{-1} \cdot y$, so $y \sim x$ implies $x \sim y$.

Transitive: if $y = g \cdot x$ and $z = h \cdot y$, then $z = h \cdot (g \cdot x) = (hg) \cdot x$, so $x \sim z$.

The equivalence classes of $\sim$ are the orbits, so they partition $X$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

The group $D_4$ acts on itself by conjugation: $g \cdot h = ghg^{-1}$. Compute the conjugacy classes and write down the class equation for $D_4$.

<details class="exercise__hint"><summary>Hint</summary>

$D_4 = \{e, r, r^2, r^3, s, rs, r^2 s, r^3 s\}$ where $r$ is rotation by $90^\circ$ and $s$ is a reflection. Compute $grg^{-1}$, $gsg^{-1}$, and so on for representative elements $g$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The conjugacy classes of $D_4$ are:
- $\{e\}$ (size $1$)
- $\{r^2\}$ (size $1$; the $180^\circ$ rotation is central)
- $\{r, r^3\}$ (size $2$)
- $\{s, r^2 s\}$ (size $2$)
- $\{rs, r^3 s\}$ (size $2$)

The centre is $Z(D_4) = \{e, r^2\}$. The class equation is $8 = 2 + 2 + 2 + 2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Let $G$ act on itself by conjugation. Prove that the stabiliser of an element $x$ under this action is the centraliser $C_G(x) = \{g \in G : gx = xg\}$.

<details class="exercise__hint"><summary>Hint</summary>

The conjugation action is $g \cdot x = gxg^{-1}$. What does $gxg^{-1} = x$ imply?

</details>

<details class="exercise__answer"><summary>Answer</summary>

The stabiliser of $x$ under conjugation is:
$$
\operatorname{Stab}(x) = \{g \in G : gxg^{-1} = x\}.
$$
Rearranging $gxg^{-1} = x$ gives $gx = xg$. Therefore $\operatorname{Stab}(x) = \{g \in G : gx = xg\} = C_G(x)$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Derive the class equation: for a finite group $G$,
$$
|G| = |Z(G)| + \sum_i [G : C_G(g_i)]
$$
where the sum runs over representatives $g_i$ of the non-central conjugacy classes.

<details class="exercise__hint"><summary>Hint</summary>

Apply the orbit-stabiliser theorem to the conjugation action. The orbits are conjugacy classes, and the stabilisers are centralisers. Elements in the centre have conjugacy class of size $1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $G$ act on itself by conjugation. The orbits are the conjugacy classes. By Exercise 6, the stabiliser of $x$ is $C_G(x)$.

By the orbit-stabiliser theorem, $|G| = |\operatorname{Orb}(x)| \cdot |C_G(x)|$, so $|\operatorname{Orb}(x)| = [G : C_G(x)]$.

The conjugacy classes partition $G$. The elements of $Z(G)$ are those with $|\operatorname{Orb}(x)| = 1$ (they commute with every element, so their conjugacy class is a singleton). Choosing one representative $g_i$ from each non-central conjugacy class:
$$
|G| = \sum_{\text{classes}} |\text{class}| = |Z(G)| + \sum_i [G : C_G(g_i)].
$$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Let $G$ act on a finite set $X$. Prove Burnside's lemma: the number of distinct orbits is
$$
\frac{1}{|G|}\sum_{g \in G} |X^g|
$$
where $X^g = \{x \in X : g \cdot x = x\}$ is the fixed-point set of $g$.

<details class="exercise__hint"><summary>Hint</summary>

Count the pairs $(g, x)$ with $g \cdot x = x$ in two ways: fix $g$ first (sum $|X^g|$), then fix $x$ first (sum $|\operatorname{Stab}(x)|$ and regroup by orbits using the orbit-stabiliser theorem).

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $S = \{(g, x) \in G \times X : g \cdot x = x\}$. Count $|S|$ in two ways.

Fixing $g$: $|S| = \sum_{g \in G} |X^g|$.

Fixing $x$: $|S| = \sum_{x \in X} |\operatorname{Stab}(x)|$.

Group the second sum by orbits. Let $x_1, \ldots, x_k$ be representatives of the $k$ distinct orbits. For any $x$ in the orbit of $x_i$:
$$
|\operatorname{Stab}(x)| = \frac{|G|}{|\operatorname{Orb}(x)|} = \frac{|G|}{|\operatorname{Orb}(x_i)|}.
$$
Each orbit $\operatorname{Orb}(x_i)$ has $|\operatorname{Orb}(x_i)|$ elements, each with stabiliser of size $|G|/|\operatorname{Orb}(x_i)|$. The total contribution of orbit $i$ is:
$$
|\operatorname{Orb}(x_i)| \cdot \frac{|G|}{|\operatorname{Orb}(x_i)|} = |G|.
$$
Therefore $\sum_{x \in X} |\operatorname{Stab}(x)| = k \cdot |G|$. Equating the two counts:
$$
\sum_{g \in G} |X^g| = k \cdot |G|,
$$
and dividing by $|G|$ gives $k = \frac{1}{|G|}\sum_{g \in G} |X^g|$.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Burnside's Lemma).** Let $G$ act on a finite set $X$. The number of distinct orbits is
$$
\frac{1}{|G|}\sum_{g \in G} |X^g|
$$
where $X^g = \{x \in X : g \cdot x = x\}$. Despite the name, this result is due to Cauchy (1845) and Frobenius (1887); Burnside included and popularised it in his 1897 textbook [ref: TODO_REF Burnside 1897].

**Theorem 2 (Class Equation).** For a finite group $G$:
$$
|G| = |Z(G)| + \sum_i [G : C_G(g_i)]
$$
where $g_i$ are representatives of the non-central conjugacy classes. Each index $[G : C_G(g_i)]$ exceeds $1$ and divides $|G|$.

**Theorem 3 (Cauchy via Group Actions).** If $G$ is a finite group and a prime $p$ divides $|G|$, then $G$ contains an element of order $p$. The proof considers the set $X = \{(g_1, \ldots, g_p) \in G^p : g_1 g_2 \cdots g_p = e\}$ with the cyclic rotation action, and applies the orbit decomposition to show that some fixed-point orbit corresponds to an element of order $p$ [ref: TODO_REF Dummit-Foote §4.2].

**Theorem 4 (Conjugacy Classes of $S_n$).** The conjugacy classes of the symmetric group $S_n$ are indexed by partitions of $n$. Two permutations are conjugate if and only if they have the same cycle type. The number of conjugacy classes equals the number of partitions of $n$ [ref: TODO_REF Dummit-Foote §4.3].

**Theorem 5 (Fixed-Point-Free Automorphisms).** If a finite group $G$ admits a fixed-point-free automorphism of prime order $p$ — meaning an automorphism $\varphi$ with $\varphi(g) = g$ only when $g = e$ — then $G$ is nilpotent. This result of Thompson (1959) relies on the structure of group actions and their fixed-point sets [ref: TODO_REF Rotman §3].

**Theorem 6 (Centre of a $p$-Group).** If $G$ is a group of order $p^n$ for a prime $p$, then $|Z(G)| \geq p$. The proof applies the class equation: each $[G : C_G(g_i)]$ is a power of $p$ exceeding $1$, so $p$ divides $\sum_i [G : C_G(g_i)]$. Since $p$ divides $|G|$, it follows that $p$ divides $|Z(G)|$, forcing $|Z(G)| \geq p$.

**Synthesis.** The orbit-stabiliser theorem is the foundational reason that counting arguments permeate finite group theory, and this is exactly the mechanism behind the class equation, Cauchy's theorem, and the Sylow theorems. The central insight is that conjugation makes the group act on itself, and putting these together with the orbit-stabiliser counting argument identifies the centre $Z(G)$ with the fixed points of this action. The bridge is between the combinatorial data of orbit sizes and the algebraic data of subgroup indices.

The pattern generalises from finite groups to compact Lie groups acting on manifolds, where orbits become homogeneous spaces and the class equation becomes the Weyl integration formula. Burnside's lemma appears again in [01.02.04] as the counting tool that underpins the Sylow congruence, and the pattern recurs in representation theory when counting irreducible characters via the class algebra constants.

## Full proof set [Master]

**Proposition 1 (Burnside's Lemma).** Let the finite group $G$ act on the finite set $X$. The number of orbits is $\frac{1}{|G|}\sum_{g \in G} |X^g|$.

*Proof.* Count the set $S = \{(g, x) \in G \times X : g \cdot x = x\}$ in two ways.

First, fixing $g \in G$: the contribution is $|X^g|$, the number of points fixed by $g$. So $|S| = \sum_{g \in G} |X^g|$.

Second, fixing $x \in X$: the contribution is $|\operatorname{Stab}(x)|$. So $|S| = \sum_{x \in X} |\operatorname{Stab}(x)|$.

Regroup the second sum by orbits. Let $\mathcal{O}_1, \ldots, \mathcal{O}_k$ be the distinct orbits with representatives $x_1, \ldots, x_k$. For any $x \in \mathcal{O}_i$, the orbit-stabiliser theorem gives $|\operatorname{Stab}(x)| = |G|/|\mathcal{O}_i|$. Since $\mathcal{O}_i$ contains $|\mathcal{O}_i|$ elements, its total contribution to $\sum_x |\operatorname{Stab}(x)|$ is:
$$
|\mathcal{O}_i| \cdot \frac{|G|}{|\mathcal{O}_i|} = |G|.
$$
Summing over all orbits: $\sum_{x \in X} |\operatorname{Stab}(x)| = k \cdot |G|$. Equating with the first count gives $k = \frac{1}{|G|}\sum_{g \in G} |X^g|$. $\square$

**Proposition 2 (Centre of a $p$-Group).** If $G$ is a group of order $p^n$ with $p$ prime, then $|Z(G)| \geq p$.

*Proof.* Apply the class equation:
$$
p^n = |G| = |Z(G)| + \sum_i [G : C_G(g_i)]
$$
where the sum runs over representatives of non-central conjugacy classes.

For each $g_i \notin Z(G)$, the centraliser $C_G(g_i)$ is a proper subgroup of $G$, so $[G : C_G(g_i)] > 1$. Since $|G| = p^n$ and $C_G(g_i)$ is a subgroup, $[G : C_G(g_i)] = p^n / |C_G(g_i)|$ is a power of $p$ exceeding $1$, hence divisible by $p$.

Therefore $p$ divides $\sum_i [G : C_G(g_i)]$, and since $p$ divides $|G| = p^n$, it follows that $p$ divides $|Z(G)|$. Since $e \in Z(G)$, we have $|Z(G)| \geq 1$, and divisibility by $p$ forces $|Z(G)| \geq p$. $\square$

**Proposition 3 (Cauchy's Theorem via Group Actions).** If $G$ is a finite group and $p$ divides $|G|$, then $G$ has an element of order $p$.

*Proof.* Consider the set $X = \{(g_1, \ldots, g_p) \in G^p : g_1 g_2 \cdots g_p = e\}$. For each choice of $g_1, \ldots, g_{p-1}$, the element $g_p = (g_1 \cdots g_{p-1})^{-1}$ is uniquely determined, so $|X| = |G|^{p-1}$.

The cyclic group $\langle \sigma \rangle \cong \mathbb{Z}/p\mathbb{Z}$ acts on $X$ by cyclic rotation: $\sigma \cdot (g_1, \ldots, g_p) = (g_2, \ldots, g_p, g_1)$. This is an action because rotating and then rotating again is the same as rotating twice, and the identity rotation fixes every tuple.

A tuple $(g_1, \ldots, g_p) \in X$ is fixed by $\sigma$ if and only if $(g_1, \ldots, g_p) = (g_2, \ldots, g_p, g_1)$, meaning $g_1 = g_2 = \cdots = g_p$. Since $g_1^p = e$, the fixed points correspond to elements of order dividing $p$.

Let $k$ be the number of orbits. The tuple $(e, e, \ldots, e)$ is a fixed point, so there is at least one orbit of size $1$. By Burnside's lemma:
$$
k = \frac{1}{p}\sum_{j=0}^{p-1} |X^{\sigma^j}|.
$$
For $j = 0$: $|X^{\sigma^0}| = |X| = |G|^{p-1}$. For $j \neq 0$: $|X^{\sigma^j}|$ counts tuples with $g_1 = g_2 = \cdots = g_p$ and $g_1^p = e$, so $|X^{\sigma^j}|$ is the number of elements $g \in G$ with $g^p = e$.

Since $p$ divides $|G|$, we have $p$ divides $|G|^{p-1}$, and since $k$ is an integer, $p$ must divide $|X^{\sigma^0}| + \sum_{j=1}^{p-1} |X^{\sigma^j}|$. The number of elements with $g^p = e$ is at least $1$ (the identity), and $k \geq 1$. For $p$ to divide $k \cdot p$ while $k$ is a positive integer, the fixed-point count under $\sigma^j$ ($j \neq 0$) must include at least one non-identity element. Any such element $g \neq e$ with $g^p = e$ has order $p$. $\square$

## Connections [Master]

- **Subgroup, coset, quotient group [01.02.02].** The coset partition underlying Lagrange's theorem is the same structure that the orbit-stabiliser theorem exploits. The fibres of the map $\varphi: G \to \operatorname{Orb}(x)$ are left cosets of $\operatorname{Stab}(x)$, and the bijection $G/\operatorname{Stab}(x) \to \operatorname{Orb}(x)$ is the concrete realisation of the orbit-stabiliser counting argument. The foundational reason that both Lagrange and orbit-stabiliser work is the same coset-decomposition mechanism.

- **Sylow theorems [01.02.04].** The Sylow existence theorem is proved by letting $G$ act on the set of all subsets of $G$ of size $p^k$, and the orbit-counting and stabiliser-size constraints force the existence of a subgroup of order $p^k$. The class equation controls the number of Sylow $p$-subgroups via the congruence $n_p \equiv 1 \pmod{p}$. Burnside's lemma provides the orbit-counting machinery that makes the Sylow proofs work.

- **Group [01.02.01].** The group axioms (identity and associativity) are precisely what make the action axioms well-behaved: the identity axiom for actions uses the group identity, and the compatibility axiom uses associativity. The action of a group on itself by left multiplication is the canonical free action, and the action by conjugation produces the class equation as its orbit decomposition.

## Historical & philosophical context [Master]

Cauchy 1845 established the orbit-counting formula now called Burnside's lemma, in the context of permutation groups acting on finite sets [ref: TODO_REF Burnside 1897]. Frobenius 1887 independently rediscovered the same counting result in *Journal fur die reine und angewandte Mathematik* 102. Burnside included the formula in the first edition of his *Theory of Groups of Finite Order* (1897, Cambridge University Press) and his textbook treatment made it widely known, which is the origin of the misattribution [ref: TODO_REF Burnside 1897].

Sylow 1872 proved the existence, conjugacy, and counting theorems for maximal $p$-subgroups of finite groups in *Mathematische Annalen* 5, using the action of a group on the collection of its subsets [ref: TODO_REF Dummit-Foote §4.5]. The orbit-stabiliser theorem in its modern abstract form developed with the axiomatisation of group theory in the early twentieth century, appearing in the treatments of Burnside (1911, second edition) and Speiser (1923, *Die Theorie der Gruppen von endlicher Ordnung*).

## Bibliography [Master]

```bibtex
@book{Burnside1897,
  author = {Burnside, William},
  title = {Theory of Groups of Finite Order},
  publisher = {Cambridge University Press},
  year = {1897},
  note = {2nd ed.\ 1911, reprinted by Dover 1955},
}

@book{DummitFoote2004,
  author = {Dummit, David S. and Foote, Richard M.},
  title = {Abstract Algebra},
  edition = {3rd},
  publisher = {Wiley},
  year = {2004},
}

@book{Artin2011,
  author = {Artin, Michael},
  title = {Algebra},
  edition = {2nd},
  publisher = {Pearson},
  year = {2011},
}

@book{Rotman1995,
  author = {Rotman, Joseph J.},
  title = {An Introduction to the Theory of Groups},
  edition = {4th},
  publisher = {Springer},
  year = {1995},
  series = {Graduate Texts in Mathematics 148},
}

@article{Sylow1872,
  author = {Sylow, Ludwig},
  title = {Th\'eor\`emes sur les groupes de substitutions},
  journal = {Mathematische Annalen},
  volume = {5},
  year = {1872},
  pages = {584--594},
}

@book{Speiser1923,
  author = {Speiser, Andreas},
  title = {Die Theorie der Gruppen von endlicher Ordnung},
  publisher = {Springer},
  year = {1923},
  note = {3rd ed.},
}
```
