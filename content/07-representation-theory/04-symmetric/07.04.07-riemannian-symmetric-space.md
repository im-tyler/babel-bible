---
id: 07.04.07
title: Riemannian symmetric space
slug: riemannian-symmetric-space
section: representation-theory
chapter: symmetric
concept_catalog_id: rep-theory.riemannian-symmetric-space
prerequisites:
  - 07.04.06
successors: []
tier_anchors:
  beginner: "Stillwell Naive Lie Theory Ch. 6; spaces where reflection through any point is a symmetry"
  intermediate: "Helgason Differential Geometry Lie Groups and Symmetric Spaces Ch. IV Section 1-3; Kobayashi-Nomizu Foundations Vol. II Ch. XI"
  master: "Cartan 1926 Bull. Soc. Math. France 54; Helgason 1978 Ch. IV-V; Besse 1987 Einstein Manifolds Ch. 7"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Cartan — Sur une classe remarquable d'espaces de Riemann"
    locator: "Bull. Soc. Math. France 54 (1926)"
  - source: TODO_REF
    path: "Helgason — Differential Geometry Lie Groups and Symmetric Spaces"
    locator: "Ch. IV Sections 1-3"
  - source: TODO_REF
    path: "Kobayashi-Nomizu — Foundations of Differential Geometry Vol. II"
    locator: "Ch. XI"
  - source: TODO_REF
    path: "Besse — Einstein Manifolds"
    locator: "Ch. 7"
lean_status: none
lean_mathlib_gap: |
  Mathlib lacks a formalisation of Riemannian symmetric spaces, the geodesic symmetry condition (an isometry at every point that fixes the point and reverses geodesics), the Lie-group-theoretic construction of symmetric spaces from orthogonal symmetric Lie algebras, and the classification into compact type, non-compact type, and Euclidean type. The necessary ingredients would include: geodesic symmetries on Riemannian manifolds, the correspondence between involutive isometries and orthogonal symmetric Lie algebras via the Lie algebra of the isometry group, transvection groups, and the curvature tensor characterisation $R(X,Y) = -\mathrm{ad}([X,Y])$ at the base point. These would extend Mathlib's Riemannian geometry and Lie group theory.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 80m
status: shipped
produced_by: glm-5.1
reviewed_by: []
---

## Intuition [Beginner]

A Riemannian symmetric space is a smooth manifold where every point has a "reflection" that is a symmetry of the space. This reflection (called the geodesic symmetry) fixes the chosen point and reverses every direction --- it sends each geodesic passing through the point back along itself. The sphere, the hyperbolic plane, and flat Euclidean space are all symmetric.

The key consequence is powerful: a symmetric space is completely determined by its geometry at a single point. The reflection symmetry forces the curvature to be the same everywhere along geodesics. This is why symmetric spaces are the best-understood class of Riemannian manifolds --- they have the maximum amount of symmetry compatible with their curvature.

Examples include the sphere $S^n$ (reflect through the antipodal point), the hyperbolic plane (reflect through any point), and the space of all $k$-planes in $\mathbb{R}^n$ (the Grassmannian). Flat $\mathbb{R}^n$ is also symmetric, with reflection $x \mapsto -x$ about the origin.

Why does this concept exist? Symmetric spaces are the natural geometric objects associated to Lie groups. Every symmetric space is a quotient $G/K$ of a Lie group by a compact subgroup, and the classification of symmetric spaces mirrors the classification of real forms of semisimple Lie algebras.

## Visual [Beginner]

A drawing of the 2-sphere $S^2$ with a point $p$ marked at the top (north pole) and arrows showing geodesics emanating from $p$. A geodesic symmetry $s_p$ is depicted as a reflection that sends each geodesic back through $p$: a point at distance $d$ along a geodesic is mapped to the point at distance $d$ in the opposite direction.

![The 2-sphere as a symmetric space, showing the geodesic reflection at the north pole. Geodesics (great circles) emanating from p are reversed by the symmetry s_p.](/img/riemannian-symmetric-space-placeholder.svg)

The reflection $s_p$ is an isometry: it preserves distances on the sphere. Every point of $S^2$ has such a reflection, making $S^2$ a symmetric space.

## Worked example [Beginner]

Consider the hyperbolic plane $\mathbb{H}^2$ (the upper half-plane $y > 0$ with metric $ds^2 = (dx^2 + dy^2)/y^2$). We verify that the reflection $s_{(0,1)}$ through the point $(0, 1)$ is an isometry.

Step 1. The geodesics through $(0, 1)$ are: vertical lines $x = 0$ (parameterised as $(0, e^t)$) and semicircles centred at the origin passing through $(0, 1)$ (parameterised as $(\sin \theta, \cos \theta)$ for $\theta \in (0, \pi)$).

Step 2. The geodesic symmetry at $(0, 1)$ sends a point at hyperbolic distance $d$ along a geodesic to the point at distance $d$ in the reverse direction. On the vertical geodesic: $(0, e^t) \mapsto (0, e^{-t})$ (reflection through $t = 0$, i.e., $y = 1$). On the semicircular geodesic: $(\sin \theta, \cos \theta) \mapsto (\sin(-\theta), \cos(-\theta)) = (-\sin\theta, \cos\theta)$ (reflect the angle).

Step 3. Check that $s_{(0,1)}$ preserves the metric. At any point $(x, y)$, the metric is $ds^2 = (dx^2 + dy^2)/y^2$. Under $y \mapsto 1/y$ (the vertical reflection composed with the scaling), the differential transforms $dy \mapsto -dy/y^2$ and $y \mapsto 1/y$, so $(dx^2 + dy^2/y^4)/(1/y^2) = (dx^2 y^2 + dy^2/y^2)/1 = (dx^2 + dy^2)/y^2$. Wait --- let me use the standard map instead.

The standard geodesic symmetry at $(0, 1)$ in the upper half-plane model is the inversion $z \mapsto -1/z$ (where $z = x + iy$). This is a Mobius transformation, and all Mobius transformations are isometries of $\mathbb{H}^2$.

What this tells us: the hyperbolic plane admits a geodesic symmetry at every point (by translating and rotating, which are isometries), confirming it is a Riemannian symmetric space.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A Riemannian symmetric space is a Riemannian manifold $M$ such that:

A. $M$ is flat (zero curvature everywhere)
B. Every point $p \in M$ is an isolated fixed point of an involutive isometry $s_p$
C. $M$ has constant positive curvature
D. $M$ is a Lie group

<details class="exercise__hint"><summary>Hint</summary>

The defining property is the existence of a geodesic symmetry (involutive isometry) at every point, with that point as an isolated fixed point.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: the definition requires an involutive isometry $s_p$ at every point $p$ with $s_p(p) = p$ and $ds_p = -\mathrm{id}$ on $T_pM$. Feedback-wrong: flat spaces are symmetric but not all symmetric spaces are flat; constant positive curvature gives only the sphere and its quotients; Lie groups are symmetric but not all symmetric spaces are groups.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

The 2-sphere $S^2$ with its standard round metric is a Riemannian symmetric space.

<details class="exercise__hint"><summary>Hint</summary>

Can you define a geodesic reflection at every point of $S^2$?

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** At any point $p \in S^2$, the geodesic symmetry sends each point at distance $d$ along a great circle through $p$ to the point at distance $d$ in the opposite direction. This is the antipodal map composed with the reflection through the equatorial plane, which is an isometry of $S^2$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

The symmetric space $SL(2, \mathbb{R})/SO(2)$ has real dimension $\dim SL(2, \mathbb{R}) - \dim SO(2)$. Compute this dimension. Recall that $SL(2, \mathbb{R})$ is the group of $2 \times 2$ real matrices with determinant 1, and $SO(2)$ is the group of $2 \times 2$ rotation matrices. State the answer as a single number.

<details class="exercise__hint"><summary>Hint</summary>

$\dim SL(2, \mathbb{R}) = 4 - 1 = 3$ (one constraint from the determinant). $\dim SO(2) = 1$ (one parameter for the rotation angle).

</details>

<details class="exercise__answer"><summary>Answer</summary>

**2.** $\dim SL(2, \mathbb{R}) = 3$ (traceless $2 \times 2$ real matrices) and $\dim SO(2) = 1$ (rotation angle). So $\dim SL(2, \mathbb{R})/SO(2) = 3 - 1 = 2$. This symmetric space is the hyperbolic plane.

</details>

</aside>

## Formal definition [Intermediate+]

Let $(M, g)$ be a connected Riemannian manifold.

**Definition (Geodesic symmetry).** A **geodesic symmetry** at a point $p \in M$ is a map $s_p \colon M \to M$ satisfying:
1. $s_p(p) = p$ (the point is fixed).
2. $s_p$ is an involutive isometry: $s_p^2 = \mathrm{id}$ and $s_p^* g = g$.
3. $s_p$ is an isolated fixed point: there is a neighbourhood $U$ of $p$ such that $s_p(q) = q$ implies $q = p$ for $q \in U$.
4. The differential $ds_p|_{T_pM} = -\mathrm{id}$ (reverses every tangent direction).

**Definition (Riemannian symmetric space).** A connected Riemannian manifold $M$ is a **Riemannian symmetric space** if for every point $p \in M$, there exists a geodesic symmetry $s_p$.

**Theorem (Symmetric implies homogeneous).** If $M$ is a Riemannian symmetric space, then $M$ is a homogeneous space: $M = G/K$ where $G$ is the identity component of the isometry group $\mathrm{Isom}(M)$ and $K$ is the isotropy subgroup at a chosen base point $o \in M$.

The Lie algebra $\mathfrak{g}$ of $G$ carries an orthogonal symmetric Lie algebra structure [07.04.06] via the involution $\theta = ds_o$ at the base point.

### Counterexamples to common slips

- *A locally symmetric space is not necessarily globally symmetric.* The condition $\nabla R = 0$ (parallel curvature tensor) characterises locally symmetric spaces. Global symmetry additionally requires completeness and the existence of global geodesic symmetries (not just local ones). A compact Riemann surface of genus $g \geq 2$ with constant negative curvature is locally symmetric but may not be globally symmetric (it is a quotient of $\mathbb{H}^2$ by a discrete group, and the symmetries do not descend).

- *A homogeneous space $G/K$ is not automatically symmetric.* The coset space $G/K$ is symmetric only when $K$ is an open subgroup of the fixed-point set of an involutive automorphism of $G$. Most homogeneous spaces lack this involution.

- *A symmetric space need not be simply-connected.* The real projective space $\mathbb{RP}^n = SO(n+1)/O(n)$ is symmetric but not simply-connected (its fundamental group is $\mathbb{Z}/2$).

## Key theorem with proof [Intermediate+]

**Theorem (Cartan: correspondence between symmetric spaces and orthogonal symmetric Lie algebras).** There is a bijective correspondence:

$$\left\{ \text{Simply-connected Riemannian symmetric spaces} \right\} \longleftrightarrow \left\{ \text{Effective orthogonal symmetric Lie algebras} \right\}$$

given by $M = G/K \mapsto (\mathfrak{g}, \theta)$ where $\mathfrak{g}$ is the Lie algebra of the transvection group and $\theta = ds_o$.

*Proof.*

**Direction $\Rightarrow$: Symmetric space to OSLA.** Let $M = G/K$ be a simply-connected symmetric space with $G = \mathrm{Isom}^0(M)$ and $K$ the isotropy at $o$. Define $\theta(g) = s_o \circ g \circ s_o$ (conjugation by the geodesic symmetry). At the Lie algebra level, $d\theta \colon \mathfrak{g} \to \mathfrak{g}$ is an involutive automorphism since $s_o^2 = \mathrm{id}$. The fixed-point set $\mathfrak{k} = \{X \in \mathfrak{g} : d\theta(X) = X\}$ is the Lie algebra of $K$.

The complement $\mathfrak{p} = \{X \in \mathfrak{g} : d\theta(X) = -X\}$ is identified with $T_oM$ via the map $X \mapsto X^*(o)$ where $X^*$ is the Killing vector field. The Killing form $B$ restricted to $\mathfrak{p}$ is positive definite (it gives the Riemannian metric at $o$). Compact embedding of $\mathfrak{k}$ follows from $K$ being a compact subgroup of $G$. Effectiveness holds because $K$ is the full isotropy group, which contains no normal subgroup of $G$ acting effectively on $M$.

**Direction $\Leftarrow$: OSLA to symmetric space.** Let $(\mathfrak{g}, \theta)$ be an effective orthogonal symmetric Lie algebra with decomposition $\mathfrak{g} = \mathfrak{k} \oplus \mathfrak{p}$. Let $G$ be the simply-connected Lie group with Lie algebra $\mathfrak{g}$ and $K$ the connected subgroup with Lie algebra $\mathfrak{k}$.

*Step 1.* $K$ is closed in $G$. Since $\mathfrak{k}$ is compactly embedded, the image $\mathrm{Ad}(K)$ is contained in a compact subgroup of $\mathrm{Aut}(\mathfrak{g})$. In a connected Lie group, a subgroup whose adjoint image has compact closure is closed.

*Step 2.* The coset space $M = G/K$ is a smooth manifold. The involution $\theta$ of $\mathfrak{g}$ integrates to an involutive automorphism $\sigma$ of $G$ (since $G$ is simply-connected). The map $\sigma$ satisfies $\sigma(k) = k$ for $k \in K$ and defines the geodesic symmetry at the base point $o = eK$.

*Step 3.* Define a Riemannian metric on $M$ as follows. At $T_oM \cong \mathfrak{p}$, set $g_o(X, Y) = B(X, Y)$ (the Killing form on $\mathfrak{p}$, which is positive definite by the orthogonal symmetric condition). Extend to all of $M$ by $G$-invariance: $g_{gK}(dL_g X, dL_g Y) = g_o(X, Y)$. This is well-defined because $K$ preserves $g_o$ (the bracket relation $[\mathfrak{k}, \mathfrak{p}] \subset \mathfrak{p}$ ensures $K$ acts on $\mathfrak{p}$ by isometries of $B$).

*Step 4.* The map $s_o = \sigma$ is an involutive isometry of $(M, g)$ with isolated fixed point at $o$ and $ds_o = -\mathrm{id}$ on $T_oM$. Geodesic symmetries at other points are obtained by translating: $s_{gK} = L_g \circ s_o \circ L_{g^{-1}}$. So $M$ is a Riemannian symmetric space.

**Bijectivity.** The two constructions are inverse. Starting from $M = G/K$, extracting $(\mathfrak{g}, \theta)$, and rebuilding gives back $M$ (uniqueness of the simply-connected Lie group). Starting from $(\mathfrak{g}, \theta)$, building $M = G/K$, and extracting the OSLA gives back $(\mathfrak{g}, \theta)$ by construction. $\square$

**Bridge.** This correspondence builds toward the complete classification of symmetric spaces in [07.04.08], where the restricted root system of each symmetric space refines its structure. The foundational reason is that the geodesic symmetry at the base point identifies the geometric notion of reflection with the algebraic notion of involution, and this is exactly the bridge between Riemannian geometry and Lie theory. Putting these together with the classification of orthogonal symmetric Lie algebras from [07.04.06], the symmetric space classification reduces to the classification of real forms of semisimple Lie algebras, and the bridge is Cartan's insight that the curvature tensor at the base point encodes the entire Lie bracket on $\mathfrak{p}$.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

The Grassmannian $\mathrm{Gr}(k, n)$ of $k$-planes in $\mathbb{R}^n$ is a symmetric space isomorphic to $SO(n)/(SO(k) \times SO(n-k))$. For $k = 2$ and $n = 4$, compute the dimension of $\mathrm{Gr}(2, 4)$. State the answer as a single number.

<details class="exercise__hint"><summary>Hint</summary>

$\dim SO(n) = n(n-1)/2$. The dimension of the symmetric space is $\dim SO(n) - \dim SO(k) - \dim SO(n-k)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**4.** $\dim SO(4) = 6$, $\dim SO(2) = 1$, $\dim SO(2) = 1$. Dimension $= 6 - 1 - 1 = 4$. Equivalently, $\mathrm{Gr}(2,4)$ has dimension $k(n-k) = 2 \times 2 = 4$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

The curvature tensor of a Riemannian symmetric space satisfies $\nabla R = 0$ (the curvature is parallel).

<details class="exercise__hint"><summary>Hint</summary>

The geodesic symmetry reverses geodesics. What does this imply about the derivative of the curvature tensor?

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** The geodesic symmetry $s_p$ at any point $p$ satisfies $s_p^* R = R$ (isometries preserve curvature) and $ds_p = -\mathrm{id}$ on $T_pM$. The covariant derivative $\nabla R$ at $p$ satisfies $s_p^*(\nabla R) = -\nabla R$ (one extra sign from the derivative). But also $s_p^*(\nabla R) = \nabla R$ (since $s_p^* R = R$ implies $s_p^* \nabla R = \nabla(s_p^* R) = \nabla R$). So $\nabla R = -\nabla R$, giving $\nabla R = 0$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="proof" markdown="1">

**Exercise 3 (medium, proof).**

Let $M = G/K$ be a symmetric space with geodesic symmetry $s_o$ at the base point. Show that the geodesic symmetry at $gK$ is $s_{gK} = L_g \circ s_o \circ L_{g^{-1}}$, where $L_g$ denotes left translation by $g$.

<details class="exercise__hint"><summary>Hint</summary>

Verify that $L_g \circ s_o \circ L_{g^{-1}}$ is an involutive isometry fixing $gK$ with the correct differential.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Set $s_{gK} = L_g \circ s_o \circ L_{g^{-1}}$.

Involutivity: $s_{gK}^2 = L_g s_o L_{g^{-1}} L_g s_o L_{g^{-1}} = L_g s_o^2 L_{g^{-1}} = L_g L_{g^{-1}} = \mathrm{id}$.

Fixed point: $s_{gK}(gK) = L_g s_o L_{g^{-1}}(gK) = L_g s_o(eK) = L_g(eK) = gK$.

Isometry: $L_g$ and $s_o$ are isometries, so their composition is an isometry.

Differential: $ds_{gK}|_{T_{gK}M} = dL_g \circ ds_o \circ dL_{g^{-1}} = dL_g \circ (-\mathrm{id}) \circ dL_{g^{-1}} = -\mathrm{id}$ on $T_{gK}M$, since $dL_g$ and $dL_{g^{-1}}$ are inverses.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="proof" markdown="1">

**Exercise 4 (medium, proof).**

Show that every Lie group $G$ with a bi-invariant metric is a Riemannian symmetric space. Describe the geodesic symmetry at the identity.

<details class="exercise__hint"><summary>Hint</summary>

The inversion map $\iota(g) = g^{-1}$ is an involutive isometry when the metric is bi-invariant.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $G$ be a Lie group with bi-invariant metric $\langle \cdot, \cdot \rangle$ (invariant under both left and right translations). Define $s_e = \iota$ (the inversion map $g \mapsto g^{-1}$).

Involutive: $\iota^2 = \mathrm{id}$. Fixed point: $\iota(e) = e$. Differential at $e$: $d\iota_e(X) = -X$ for $X \in T_eG = \mathfrak{g}$.

Isometry: $\langle d\iota_g(X), d\iota_g(Y) \rangle_{g^{-1}}$ for $X, Y \in T_gG$. Using the bi-invariance: left-translating by $g^{-1}$ gives $\langle dL_{g^{-1}} d\iota_g(X), dL_{g^{-1}} d\iota_g(Y) \rangle_e = \langle -dR_{g^{-1}}(X), -dR_{g^{-1}}(Y) \rangle_e = \langle dR_{g^{-1}}(X), dR_{g^{-1}}(Y) \rangle_e = \langle X, Y \rangle_g$ by right-invariance. So $\iota$ is an isometry.

At other points: $s_g = L_g \circ \iota \circ L_{g^{-1}}$, giving $s_g(h) = gh^{-1}g$. This is a symmetric space of Type II.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="proof" markdown="1">

**Exercise 5 (medium, proof).**

Prove that a simply-connected symmetric space of non-positive sectional curvature is diffeomorphic to $\mathbb{R}^n}$.

<details class="exercise__hint"><summary>Hint</summary>

Use the Cartan-Hadamard theorem: a complete, simply-connected manifold of non-positive curvature is diffeomorphic to $\mathbb{R}^n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

A Riemannian symmetric space is complete (homogeneous spaces are complete). If the sectional curvature is non-positive, the Cartan-Hadamard theorem applies: the exponential map $\exp_p \colon T_pM \to M$ at any point $p$ is a covering map. Since $M$ is simply-connected, $\exp_p$ is a diffeomorphism from $T_pM \cong \mathbb{R}^n$ to $M$.

This applies to all symmetric spaces of non-compact type (Type III), which have non-positive curvature.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="proof" markdown="1">

**Exercise 6 (hard, proof).**

Let $M = G/K$ be a symmetric space with orthogonal symmetric Lie algebra $(\mathfrak{g}, \theta)$ and decomposition $\mathfrak{g} = \mathfrak{k} \oplus \mathfrak{p}$. Prove that the rank of $M$ (the dimension of a maximal flat, totally geodesic submanifold) equals the dimension of a maximal abelian subalgebra of $\mathfrak{p}$.

<details class="exercise__hint"><summary>Hint</summary>

A flat totally geodesic submanifold through the base point corresponds to an abelian subspace of $\mathfrak{p}$ via the exponential map. The curvature formula $R(X, Y)Z = -[[X,Y],Z]$ vanishes on an abelian subspace.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\mathfrak{a} \subset \mathfrak{p}$ be a subspace. The image $\exp(\mathfrak{a}) \cdot o$ in $M$ is totally geodesic: the geodesics through $o$ tangent to $\mathfrak{a}$ stay in $\exp(\mathfrak{a}) \cdot o$ because $[\mathfrak{a}, \mathfrak{a}] \subset [\mathfrak{p}, \mathfrak{p}] \subset \mathfrak{k}$, and if $\mathfrak{a}$ is abelian ($[\mathfrak{a}, \mathfrak{a}] = 0$), the tangent spaces along $\exp(\mathfrak{a}) \cdot o$ are all identified with $\mathfrak{a}$ via left translation.

The sectional curvature of the 2-plane spanned by $X, Y \in \mathfrak{a}$ is $K(X, Y) = -\langle R(X,Y)Y, X \rangle / (\|X\|^2\|Y\|^2 - \langle X, Y\rangle^2)$. By the curvature formula, $R(X,Y)Y = -[[X,Y], Y] = 0$ when $[X, Y] = 0$. So $\mathfrak{a}$ abelian implies flat curvature on the corresponding submanifold.

Conversely, if $\exp(\mathfrak{a}) \cdot o$ is flat, then $R(X, Y)Z = 0$ for all $X, Y, Z \in \mathfrak{a}$, so $[[X, Y], Z] = 0$. In particular, $[[X, Y], X] = 0$ and $[[X, Y], Y] = 0$. By the Jacobi identity and the structure of the Lie algebra, this forces $[X, Y] = 0$ (since $\mathrm{ad}([X,Y])$ acting on $\mathfrak{p}$ is semisimple with eigenvectors being root vectors, and zero eigenvalues only when $[X,Y] = 0$ in the center of $\mathfrak{k}$, which is zero by effectiveness).

Maximal flats correspond to maximal abelian subalgebras of $\mathfrak{p}$, and their dimension is the rank. $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="numeric" markdown="1">

**Exercise 7 (hard, numeric).**

The symmetric space $SL(n, \mathbb{R})/SO(n)$ has rank equal to the dimension of the space of diagonal traceless $n \times n$ real matrices. Compute the rank for $n = 4$. State the answer as a single number.

<details class="exercise__hint"><summary>Hint</summary>

Diagonal traceless $n \times n$ matrices have $n$ diagonal entries summing to zero, so $n - 1$ free parameters.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**3.** The maximal abelian subalgebra of $\mathfrak{p}$ (symmetric traceless matrices) consists of diagonal traceless matrices: $\mathrm{diag}(a_1, a_2, a_3, a_4)$ with $a_1 + a_2 + a_3 + a_4 = 0$. This has dimension $4 - 1 = 3$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="proof" markdown="1">

**Exercise 8 (hard, proof).**

Prove that the sectional curvature of a symmetric space $G/K$ at the base point, for linearly independent $X, Y \in \mathfrak{p}$, is $K(X, Y) = -\langle [X, Y], [X, Y] \rangle / (\|X\|^2 \|Y\|^2 - \langle X, Y \rangle^2)$, where $\langle \cdot, \cdot \rangle$ is the inner product on $\mathfrak{p}$ from the Killing form.

<details class="exercise__hint"><summary>Hint</summary>

Use the curvature formula $R(X,Y)Z = -[[X,Y], Z]$ and the fact that $[X, Y] \in \mathfrak{k}$, so $\langle [X,Y], Z \rangle = 0$ for $Z \in \mathfrak{p}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The sectional curvature for the 2-plane $\sigma = \mathrm{span}(X, Y)$ is:

$$K(\sigma) = \frac{\langle R(X, Y)Y, X \rangle}{\|X\|^2 \|Y\|^2 - \langle X, Y \rangle^2}$$

Substituting $R(X, Y)Y = -[[X, Y], Y]$:

$$\langle R(X, Y)Y, X \rangle = -\langle [[X, Y], Y], X \rangle$$

Since $[X, Y] \in \mathfrak{k}$ (bracket relations), and $\mathfrak{k}$ acts on $\mathfrak{p}$ by derivations, we use the ad-invariance of the Killing form: $\langle [Z, Y], X \rangle = -\langle Y, [Z, X] \rangle$ for $Z = [X, Y] \in \mathfrak{k}$ and $X, Y \in \mathfrak{p}$ (the form is $\mathrm{ad}(\mathfrak{k})$-invariant). So:

$$\langle [[X,Y], Y], X \rangle = \langle [X,Y], [Y, X] \rangle = -\langle [X,Y], [X,Y] \rangle$$

Therefore $K(\sigma) = \frac{\langle [X,Y], [X,Y] \rangle}{\|X\|^2 \|Y\|^2 - \langle X, Y \rangle^2}$.

For compact type, $[X,Y] \in \mathfrak{k}$ with negative-definite Killing form, so $\langle [X,Y], [X,Y] \rangle < 0$, giving positive sectional curvature. For non-compact type, the sign reverses. $\square$

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Cartan's classification of irreducible symmetric spaces).** The irreducible, simply-connected Riemannian symmetric spaces are classified into:

- **Compact type:** symmetric spaces $G/K$ where $G$ is a compact simple Lie group and $K$ is the fixed-point set of an involutive automorphism. These have non-negative sectional curvature.
- **Non-compact type:** $G/K$ where $G$ is a non-compact simple Lie group and $K$ is a maximal compact subgroup. These have non-positive sectional curvature and are diffeomorphic to $\mathbb{R}^n$.
- **Euclidean type:** $\mathbb{R}^n$ with flat metric.

The compact and non-compact types come in dual pairs via the duality of [07.04.06].

**Theorem 2 (Rank and root system).** The rank of a symmetric space $M = G/K$ is the dimension of a maximal flat totally geodesic submanifold (maximal torus in the compact case, maximal flat in the non-compact case). The root system of $M$ is the set of restricted roots of the pair $(\mathfrak{g}, \mathfrak{a})$ where $\mathfrak{a}$ is a maximal abelian subalgebra of $\mathfrak{p}$.

**Theorem 3 (Decomposition theorem).** Every simply-connected symmetric space decomposes uniquely as $M = M_0 \times M_+ \times M_-$ where $M_0$ is Euclidean, $M_+$ is of compact type, and $M_-$ is of non-compact type. At the Lie algebra level, this corresponds to the decomposition of $(\mathfrak{g}, \theta)$ into a direct sum of an abelian algebra, compact simple algebras, and non-compact simple algebras.

**Theorem 4 (Strongly orthogonal roots and polynomials).** The radial part of every $G$-invariant differential operator on a symmetric space $G/K$ of non-compact type reduces to a polynomial differential operator on the maximal flat $\mathfrak{a} \cong \mathbb{R}^r$. The Harish-Chandra isomorphism identifies the algebra of $G$-invariant differential operators with the Weyl-group-invariant polynomials on $\mathfrak{a}^*$.

**Theorem 5 (Cartan's fixed-point theorem).** If $M = G/K$ is a symmetric space of non-compact type and $\Gamma$ is a compact group acting isometrically on $M$, then $\Gamma$ has a fixed point. In particular, every compact subgroup of $G$ is conjugate into $K$.

**Synthesis.** The Riemannian symmetric space is the foundational reason that the classification of highly symmetric geometries reduces to the algebraic classification of real forms of semisimple Lie algebras. The central insight is that the geodesic reflection identifies the local geometry with the global Lie group structure, and this is exactly the bridge between Riemannian geometry and representation theory. Putting these together with the orthogonal symmetric Lie algebra of [07.04.06], every symmetric space carries a root system determined by the Lie bracket, and the bridge is the restricted root system that appears again in [07.04.08].

The pattern generalises: the rank of the symmetric space equals the dimension of the maximal flat, which identifies the symmetric space with its root-theoretic skeleton, and this is exactly the structure that governs harmonic analysis on $G/K$. The duality between compact and non-compact types mirrors the duality of [07.04.06] at the geometric level: $S^n$ and $\mathbb{H}^n$ have the same Lie-theoretic skeleton but opposite curvature signs.

## Full proof set [Master]

**Proposition 1 (Symmetric implies complete and $\nabla R = 0$).** Every Riemannian symmetric space is complete and has parallel curvature tensor.

*Proof.* Completeness: Let $\gamma \colon [0, b) \to M$ be a maximal geodesic with $b < \infty$. Choose $t_0 \in (0, b)$ with $t_0$ close to $b$. The geodesic symmetry $s_{\gamma(t_0)}$ sends $\gamma$ to $\gamma'(t) = \gamma(2t_0 - t)$, extending $\gamma$ beyond $b$. Contradiction. So all geodesics are defined for all time, and $M$ is complete.

Parallel curvature: The geodesic symmetry $s_p$ at $p$ satisfies $s_p^* R = R$ (isometry). The differential $ds_p = -\mathrm{id}$ on $T_pM$, so $s_p^*(\nabla R)_p = (-1)^3 (\nabla R)_p = -(\nabla R)_p$ (three contravariant indices pick up a sign each). But $s_p^*(\nabla R) = \nabla(s_p^* R) = \nabla R$. So $(\nabla R)_p = -(\nabla R)_p = 0$ for every $p$. $\square$

**Proposition 2 (Transvections).** The transvection group of a symmetric space $M$ (the subgroup of $\mathrm{Isom}(M)$ generated by products $s_p \circ s_q$) acts transitively on frames, making $M$ a reductive homogeneous space.

*Proof.* For $X \in \mathfrak{p}$, define the transvection $T_X = s_{\gamma_X(1/2)} \circ s_o$ where $\gamma_X$ is the geodesic with $\gamma_X(0) = o$ and $\dot{\gamma}_X(0) = X$. The map $T_X$ translates along the geodesic $\gamma_X$ by distance 1 in the direction of $X$: $T_X(\gamma_X(t)) = \gamma_X(t + 1)$.

The differential of $T_X$ at $o$ sends $Y \in T_oM$ to $(dL_{\exp(X)})_o Y$ (left translation by the exponential). This is because the composition of two reflections is a translation along the geodesic.

The group $G' = \{T_X : X \in \mathfrak{p}\}$ generated by all transvections has Lie algebra $\mathfrak{g} = \mathfrak{k} \oplus \mathfrak{p}$ with $[\mathfrak{p}, \mathfrak{p}] \subset \mathfrak{k}$ and $[\mathfrak{k}, \mathfrak{p}] \subset \mathfrak{p}$, making $\mathfrak{p}$ a reductive complement. Since transvections can move any frame at $o$ to any other frame at $o$ (by choosing appropriate $X$), $G'$ acts transitively on frames. $\square$

## Connections [Master]

- **Orthogonal symmetric Lie algebra [07.04.06].** The orthogonal symmetric Lie algebra is the infinitesimal shadow of a Riemannian symmetric space. Every simply-connected symmetric space $G/K$ determines a unique OSLA $(\mathfrak{g}, \theta)$, and the classification of symmetric spaces reduces to the classification of OSLAs. The curvature tensor, rank, and holonomy all descend from the Lie algebra structure.

- **Restricted root system [07.04.08].** The restricted root system of a non-compact symmetric space $G/K$ governs its fine structure. The roots are the eigenvalues of $\mathrm{ad}(\mathfrak{a})$ on $\mathfrak{g}$ where $\mathfrak{a}$ is a maximal abelian subalgebra of $\mathfrak{p}$, and they form a (possibly non-reduced) root system in $\mathfrak{a}^*$.

- **Cartan involution [07.04.03].** For symmetric spaces of non-compact type, the geodesic symmetry at the base point integrates the Cartan involution to the group level. The Cartan involution $\theta$ of [07.04.03] is the differential $ds_o$ of the geodesic reflection, and the compact subgroup $K$ is the maximal compact subgroup determined by $\theta$.

## Historical & philosophical context [Master]

Elie Cartan introduced Riemannian symmetric spaces in his 1926 two-part memoir (Bull. Soc. Math. France 54) [ref: TODO_REF Cartan1926], identifying them as the Riemannian manifolds whose curvature tensor is parallel ($\nabla R = 0$) and classifying them completely using his earlier classification of real simple Lie algebras. Cartan's insight was that the condition $\nabla R = 0$ forces the manifold to be homogeneous and determines its geometry from a single algebraic datum.

The modern Lie-group-theoretic formulation, establishing the precise correspondence with orthogonal symmetric Lie algebras, is due to Helgason (1962 lectures, 1978 monograph) [ref: TODO_REF Helgason1978] and Kobayashi-Nomizu (Foundations Vol. II, 1969) [ref: TODO_REF KobayashiNomizu1969]. The decomposition into compact, non-compact, and Euclidean types was already implicit in Cartan's work but received its definitive form in Helgason's treatment. Besse's Einstein Manifolds (1987) [ref: TODO_REF Besse1987] developed the curvature and holonomy theory of symmetric spaces in the context of Einstein metrics.

## Bibliography [Master]

```bibtex
@article{Cartan1926,
  author = {Cartan, Elie},
  title = {Sur une classe remarquable d'espaces de {R}iemann},
  journal = {Bull. Soc. Math. France},
  volume = {54},
  year = {1926},
  pages = {214--264},
}

@book{Helgason1978,
  author = {Helgason, Sigurdur},
  title = {Differential Geometry, Lie Groups, and Symmetric Spaces},
  publisher = {Academic Press},
  year = {1978},
}

@book{KobayashiNomizu1969,
  author = {Kobayashi, Shoshichi and Nomizu, Katsumi},
  title = {Foundations of Differential Geometry, Vol. II},
  publisher = {Wiley-Interscience},
  year = {1969},
}

@book{Besse1987,
  author = {Besse, Arthur L.},
  title = {Einstein Manifolds},
  publisher = {Springer},
  year = {1987},
}
```
