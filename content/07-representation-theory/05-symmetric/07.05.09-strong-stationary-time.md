---
id: 07.05.09
title: Strong stationary time; coupling argument
slug: strong-stationary-time
section: representation-theory
chapter: symmetric
concept_catalog_id: rep-theory.strong-stationary-time
prerequisites:
  - 07.01.09
  - 07.05.01
successors: []
tier_anchors:
  beginner: "Aldous-Diaconis 1986 American Mathematical Monthly informal; stopping rule analogy"
  intermediate: "Aldous-Diaconis 1986, 1987; Diaconis Group Representations in Probability and Statistics Ch. 4"
  master: "Aldous 1983; Aldous-Diaconis 1986, 1987; Diaconis-Shahshahani 1981; Levin-Peres-Wilmer 2009 Ch. 6"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Aldous, D. and Diaconis, P. — Shuffling cards and stopping times"
    locator: "American Mathematical Monthly 93 (1986), 333-348"
  - source: TODO_REF
    path: "Aldous, D. and Diaconis, P. — Strong uniform times and finite random walks"
    locator: "Adv. Appl. Math. 8 (1987), 69--97"
  - source: TODO_REF
    path: "Aldous, D. — Random walks on finite groups and rapidly mixing Markov chains"
    locator: "Seminaire de Probabilites XVII, Lecture Notes in Math. 986 (1983)"
  - source: TODO_REF
    path: "Levin, D., Peres, Y. and Wilmer, E. — Markov Chains and Mixing Times"
    locator: "AMS 2009, Ch. 6 (coupling) and Ch. 9 (strong stationary times)"
lean_status: none
lean_mathlib_gap: |
  Mathlib lacks a formal treatment of strong stationary times and coupling
  arguments for Markov chains on finite groups. Missing components include:
  the definition of a strong stationary time as a randomised stopping rule
  T such that the chain at time T is exactly uniform and independent of T;
  the separation distance and its relation to total variation distance;
  the duality construction of strong stationary times via strong stationary
  duals; the coupling inequality bounding TV distance by the coupling
  probability; and the application to top-to-random shuffles and random
  transpositions. The probability infrastructure in Mathlib.Probability
  provides stopping times and martingales, but the coupling and
  strong-stationary-time layer has not been built.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 75m
status: shipped
produced_by: glm-5.1 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

A *strong stationary time* is a cleverly designed stopping rule that tells you the exact moment when a random walk has reached the uniform distribution. Instead of bounding how far the walk is from uniform at each step, you define a random time $T$ and prove: at time $T$, the position of the walk is exactly uniform, and $T$ itself reveals no information about which position was reached.

The idea comes from card shuffling. If you shuffle cards by repeatedly moving the top card to a random position, there is a natural moment when the original bottom card first reaches the top and is then inserted randomly. At that moment, the deck becomes exactly uniform — not approximately, but exactly. The random time $T$ when this happens is a strong stationary time.

The power of this approach is that the *distribution of $T$* gives an upper bound on the total variation distance. If $T$ is likely to be small, the walk mixes fast. If $T$ can be large, the walk mixes slowly.

## Visual [Beginner]

A diagram showing the top-to-random shuffle. At each step, the top card is removed and inserted at a uniformly random position in the deck. A marker shows when the original bottom card first reaches the top — that moment is a strong stationary time.

![A sequence of card configurations. At the left, a deck with cards labelled 1 through 5. At each step, the top card is highlighted and moved to a random position. A star marks the configuration where card 5 (the original bottom card) first appears at the top and is about to be inserted.](/img/strong-stationary-time-placeholder.svg)

The key insight: once the original bottom card reaches the top and is reinserted randomly, every ordering of the deck is equally likely.

## Worked example [Beginner]

Consider the *top-to-random shuffle* on a deck of 4 cards, initially in order $(1, 2, 3, 4)$ from top to bottom. At each step, take the top card and insert it at a uniformly random position in the deck (including back on top).

Step 1. After one step: the top card (1) is inserted at a random position among positions 1, 2, 3, 4. Each position has probability $1/4$. If card 1 goes to position 4 (the bottom), the deck is $(2, 3, 4, 1)$.

Step 2. Card 4 (the original bottom card) reaches the top when all cards originally above it have been moved. The probability that card 4 reaches the top by step $k$ involves a coupon-collector type argument.

Step 3. Once card 4 is on top and is then inserted at a random position (at some step $T$), the deck becomes exactly uniform. The time $T$ is a strong stationary time. For a deck of $n$ cards, the expected time for $T$ is $n \cdot H_n = n(1 + 1/2 + \cdots + 1/n)$, which is approximately $n \log n$.

What this tells us: for $n = 4$, the expected $T$ is $4 \cdot (1 + 1/2 + 1/3 + 1/4) = 4 \cdot 2.083 \approx 8.3$ steps. The top-to-random shuffle mixes in about $n \log n$ steps.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A strong stationary time $T$ for a random walk on a group $G$ satisfies:

A. The walk reaches $T$ in exactly $|G|$ steps
B. At time $T$, the walk is at the identity element
C. At time $T$, the position of the walk is exactly uniformly distributed, independent of the value of $T$
D. $T$ is a deterministic function of the starting position

<details class="exercise__hint"><summary>Hint</summary>

The defining property of a strong stationary time is that the position at time $T$ is uniform and independent of $T$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** Feedback-correct: a strong stationary time $T$ is a randomised stopping time such that the chain's position at time $T$ has the uniform distribution on $G$ and is independent of $T$ itself. Feedback-wrong: A confuses $T$ with the group order; B confuses the position with the identity; D would make $T$ non-random.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

If $T$ is a strong stationary time, then the total variation distance at step $k$ satisfies $\|Q^{*k} - U\|_{\mathrm{TV}} \leq \Pr(T > k)$.

<details class="exercise__hint"><summary>Hint</summary>

At time $T$, the distribution is exactly uniform. At any earlier time, it may not be.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** This is the fundamental coupling inequality for strong stationary times. If $T \leq k$, then the distribution at time $k$ is exactly uniform (since $T$ being a strong stationary time means uniformity at time $T$, and the chain stays close after that). The probability of not yet being uniform at step $k$ is at most $\Pr(T > k)$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

The expected strong stationary time for the top-to-random shuffle on $n$ cards is $n \cdot H_n$ where $H_n = 1 + 1/2 + \cdots + 1/n$. Compute this for $n = 3$ to one decimal place.

<details class="exercise__hint"><summary>Hint</summary>

$H_3 = 1 + 1/2 + 1/3 = 11/6 \approx 1.833$. Multiply by 3.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**5.5.** $3 \times 11/6 = 33/6 = 5.5$.

</details>

</aside>

## Formal definition [Intermediate+]

Let $(X_k)_{k \geq 0}$ be a Markov chain on a finite state space $\Omega$ with stationary distribution $\pi$. A *stopping time* is a random variable $T$ taking values in $\{0, 1, 2, \ldots\} \cup \{\infty\}$ such that the event $\{T = k\}$ depends only on $(X_0, X_1, \ldots, X_k)$.

**Definition (Strong stationary time).** A stopping time $T$ is a *strong stationary time* (SST) if:

1. $X_T$ has distribution $\pi$ (the position at time $T$ is exactly stationary), and
2. $X_T$ is independent of $T$ (the position reveals nothing about when $T$ occurred).

The second condition is what makes $T$ *strong*: it is not enough that $X_T \sim \pi$ on average; $X_T$ must be independent of the event $\{T = k\}$ for every $k$.

**Definition (Separation distance).** The *separation distance* at step $k$ is

$$
s(k) = \max_{x \in \Omega} \left(1 - \frac{Q^{*k}(x)}{\pi(x)}\right).
$$

Separation distance is an upper bound on total variation distance: $\|Q^{*k} - U\|_{\mathrm{TV}} \leq s(k)$. The separation distance equals $\Pr(T > k)$ for the *fastest* strong stationary time.

**Proposition (SST bound).** If $T$ is a strong stationary time, then

$$
\|Q^{*k} - \pi\|_{\mathrm{TV}} \leq s(k) = \Pr(T_* > k)
$$

where $T_*$ is the fastest (stochastically smallest) SST.

### Counterexamples to common slips

- *Stationary time is not strong stationary time.* A *stationary time* requires only $X_T \sim \pi$ (condition 1). A *strong* stationary time adds the independence condition (condition 2). Every SST is a stationary time, but not conversely.
- *Coupling time is not SST.* A *coupling* of two copies of the chain from different starting positions gives a coupling time when they first meet. The coupling time bounds TV distance via the coupling inequality, but it is not in general a strong stationary time because the meeting point need not be uniformly distributed.
- *SST gives separation distance, not TV.* The SST bound $\Pr(T > k)$ bounds the *separation distance* $s(k)$, which is at least as large as the TV distance. The SST bound can overestimate TV distance.

## Key theorem with proof [Intermediate+]

**Theorem (Aldous-Diaconis strong stationary time criterion 1987).** Let $T$ be a strong stationary time for the random walk on a finite group $G$ driven by $Q$. Then:

$$
\|Q^{*k} - U\|_{\mathrm{TV}} \leq s(k) = \Pr(T > k).
$$

Moreover, the separation distance $s(k)$ equals $\inf_T \Pr(T > k)$ where the infimum ranges over all strong stationary times $T$.

*Proof.* The proof has two parts: the upper bound and the characterisation of separation distance.

**Part (i): Upper bound.** Let $T$ be any SST. Define the "foreknowledge" distribution: at step $k$, if $T \leq k$ then the chain is already uniform (by the SST property), and if $T > k$ then we use the worst-case bound. Formally, for any $g \in G$:

$$
Q^{*k}(g) = \Pr(X_k = g) = \Pr(X_k = g \mid T \leq k)\Pr(T \leq k) + \Pr(X_k = g \mid T > k)\Pr(T > k).
$$

By the SST property, $\Pr(X_T = g \mid T \leq k) = U(g) = 1/|G|$. But $X_k$ for $k \geq T$ is obtained by further convolutions after $T$, so $X_k$ need not equal $X_T$. However, for a random walk on a group, convolution with any measure preserves the uniform distribution: if $X_T$ is uniform, then $X_k$ for $k \geq T$ is also uniform (since $U * Q^{*m} = U$ for any $m$). Therefore:

$$
Q^{*k}(g) \geq U(g) \cdot \Pr(T \leq k) = \frac{1}{|G|}(1 - \Pr(T > k)).
$$

This gives $Q^{*k}(g)/U(g) \geq 1 - \Pr(T > k)$, so $s(k) = \max_g(1 - Q^{*k}(g)/U(g)) \leq \Pr(T > k)$.

For the TV bound: $\|Q^{*k} - U\|_{\mathrm{TV}} = \frac{1}{2}\sum_g |Q^{*k}(g) - U(g)| \leq \max_g(1 - Q^{*k}(g)/U(g)) = s(k) \leq \Pr(T > k)$.

**Part (ii): Separation distance equals the infimum.** The separation distance $s(k)$ can be achieved by an optimal SST constructed via duality. The *strong stationary dual* chain $(\hat{X}_k)$ on a partially ordered state space $(\hat{\Omega}, \preceq)$ with maximum element $\hat{1}$ satisfies $\Pr(X_k = g) \geq U(g) \cdot \Pr(\hat{X}_k = \hat{1})$. Setting $T = \min\{k : \hat{X}_k = \hat{1}\}$ gives an SST with $\Pr(T > k) = 1 - \Pr(\hat{X}_k = \hat{1}) = s(k)$. Hence the infimum is achieved and equals $s(k)$. $\square$

**Bridge.** The Aldous-Diaconis SST criterion builds toward the duality theory for strong stationary times, which provides a systematic method for constructing optimal SSTs, and appears again in the analysis of specific shuffle models where the dual chain has a natural combinatorial interpretation. The foundational reason SSTs work is that the independence condition (position independent of stopping time) forces the distribution to be exactly uniform at time $T$, and the Markov property preserves this uniformity for all later times. This is exactly the coupling perspective: the SST couples the walk from its starting position to a walk started from the uniform distribution, and the coupling time is the SST. The bridge is between the probabilistic notion of a stopping time and the analytic notion of separation distance; putting these together identifies the optimal SST with the separation distance, generalising the coupling inequality.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 1 (easy, multiple choice).**

The separation distance $s(k)$ and the total variation distance $\|Q^{*k} - U\|_{\mathrm{TV}}$ satisfy:

A. $s(k) = \|Q^{*k} - U\|_{\mathrm{TV}}$
B. $\|Q^{*k} - U\|_{\mathrm{TV}} \leq s(k)$
C. $s(k) \leq \|Q^{*k} - U\|_{\mathrm{TV}}$
D. They are unrelated

<details class="exercise__hint"><summary>Hint</summary>

Separation distance measures the worst-case ratio $Q^{*k}(g)/U(g)$, while TV measures the average discrepancy.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: separation distance is always at least as large as total variation distance. The inequality can be strict: SSTs give separation bounds that may overestimate the true TV distance. Feedback-wrong: A is false in general (equality holds only for special chains); C has the inequality reversed; D is false.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

For the top-to-random shuffle on $n$ cards, the separation distance after $k$ steps is bounded by $s(k) \leq \Pr(T > k)$ where $T$ is the SST "original bottom card reaches the top and is reinserted." For $n = 3$ cards, what is the minimum number of steps needed so that the original bottom card has definitely reached the top? Give the worst-case number.

<details class="exercise__hint"><summary>Hint</summary>

The bottom card can only advance one position toward the top per step. It starts at position 3 (the bottom).

</details>

<details class="exercise__answer"><summary>Answer</summary>

**2.** The bottom card starts at position 3. Each time the top card is reinserted, the bottom card moves up at most one position (if the top card is inserted below the current bottom card). In the worst case, the bottom card advances one position per step: position 3 → position 2 after 1 step, position 1 (the top) after 2 steps. So the minimum number of steps for the bottom card to reach the top is 2.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 3 (medium, multiple choice).**

Which is the stronger condition: "T is a stationary time" or "T is a strong stationary time"?

A. They are equivalent
B. Strong stationary time is stronger: it adds the independence condition
C. Stationary time is stronger: it requires exact uniformity
D. They are incomparable

<details class="exercise__hint"><summary>Hint</summary>

A stationary time requires $X_T \sim \pi$. A strong stationary time requires $X_T \sim \pi$ AND $X_T$ independent of $T$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: every SST is a stationary time, but a stationary time need not be strong. The independence condition $X_T \perp T$ is an additional requirement. Feedback-wrong: A is false; C has the direction reversed; D is false because SST always implies stationary time.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

For the random transposition walk on $S_n$, the following is a strong stationary time: $T$ = the first time every card has been involved in at least one transposition. Prove that at time $T$, the permutation is exactly uniform.

<details class="exercise__hint"><summary>Hint</summary>

When a card is involved in its first transposition, it is swapped with a uniformly random other card. Once all cards have been touched, all orderings are equally likely.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Proceed by induction on the number of untouched cards. Initially all $n$ cards are "untouched." At each step, two positions are chosen uniformly at random and their cards are swapped. When a card at position $i$ is involved in its first transposition (swapped with card at position $j$), the card at position $j$ is chosen uniformly at random from the $n - 1$ other positions. This means the card at position $i$ is replaced by a uniformly random card from the other positions. By the time every position has been touched, each card at each position has been placed there by a uniformly random swap, independent of all previous swaps. The resulting permutation is therefore uniform. The independence of $X_T$ and $T$ follows because the conditional distribution of the permutation given $T = k$ is the same for all $k$: by the argument above, the permutation depends only on the sequence of swaps, and the constraint "every card touched by time $k$" does not bias the permutation.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Show that the expected time for the SST in Exercise 4 (every card touched) for the random transposition walk on $S_n$ is $\frac{1}{2}n \log n + O(n)$, matching the mixing time from the Upper Bound Lemma.

<details class="exercise__hint"><summary>Hint</summary>

This is a coupon collector problem. Each step involves 2 of the $n$ positions. The time until all $n$ positions have been touched is the coupon collector time with batch size 2.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Each transposition involves 2 positions chosen uniformly from $\{1, \ldots, n\}$. The probability that position $i$ is involved in a given step is $2/n$. The time until position $i$ is first touched is geometric with rate $2/n$. By the coupon collector analysis, the expected time until all $n$ positions have been touched is $\frac{n}{2} \cdot H_n = \frac{n}{2}(\log n + \gamma + O(1/n)) = \frac{1}{2}n\log n + O(n)$, where $\gamma$ is the Euler-Mascheroni constant. This matches the mixing time $t_n = \frac{1}{2}n\log n$ from the Diaconis-Shahshahani theorem.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 6 (medium, multiple choice).**

The duality construction for strong stationary times builds a "dual chain" on a partially ordered set with a maximum element $\hat{1}$. The SST is the hitting time of $\hat{1}$. What property does the dual chain satisfy?

A. It is the same Markov chain run backwards in time
B. Its transition probabilities bound the original chain's distance to uniformity from below
C. It is a Doob $h$-transform of the original chain
D. It has the same stationary distribution as the original chain

<details class="exercise__hint"><summary>Hint</summary>

The dual chain tracks how much information about the original chain's position has been "erased" by the randomising effect of the walk.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: the dual chain on $(\hat{\Omega}, \preceq)$ satisfies $\Pr(X_k \in A) \geq \pi(A) \cdot \Pr(\hat{X}_k \succeq \hat{a})$ for all $A$ and dual states $\hat{a}$, bounding the original chain's distance to uniformity from below. The maximum element $\hat{1}$ corresponds to complete uniformity. Feedback-wrong: A describes time-reversal; C is a specific construction that may arise but is not the defining property; D need not hold.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Construct a strong stationary dual for the top-to-random shuffle on $S_n$ and prove that the dual state space is $\{0, 1, \ldots, n\}$ with maximum element $n$, where the dual chain tracks the number of cards that have been "randomised" (touched by the shuffle).

<details class="exercise__hint"><summary>Hint</summary>

Define $\hat{X}_k$ = the number of cards in the lower portion of the deck that are "correctly randomised." At each step, the top card is inserted at a random position. If it lands among the $j$ already-randomised cards, the number of randomised cards stays at $j$; if it lands below them, the number increases by 1.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define the dual chain $\hat{X}_k$ on $\{0, 1, \ldots, n\}$ with $\hat{X}_0 = 0$. The state $j$ represents that the bottom $j$ cards of the deck are uniformly distributed among all possible orderings of those $j$ cards. At each step of the top-to-random shuffle: the top card is inserted at position $i$ chosen uniformly from $\{1, \ldots, n\}$. If $i \leq j + 1$ (the card lands in or just above the randomised portion), the randomised portion grows to $\min(j + 1, n)$. If $i > j + 1$, the randomised portion stays at $j$. This gives transition probabilities $\Pr(\hat{X}_{k+1} = j + 1 \mid \hat{X}_k = j) = (j + 1)/n$ and $\Pr(\hat{X}_{k+1} = j \mid \hat{X}_k = j) = (n - j - 1)/n$. The maximum element $n$ is reached when all cards are randomised. Setting $T = \min\{k : \hat{X}_k = n\}$ gives a strong stationary time. The distribution of $T$ is the coupon collector time, giving $\Pr(T > k)$ as the separation bound.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Prove that for any irreducible aperiodic random walk on a finite group $G$, the separation distance $s(k)$ is submultiplicative: $s(k + m) \leq s(k) \cdot s(m)$ for all $k, m \geq 0$.

<details class="exercise__hint"><summary>Hint</summary>

Use the SST characterisation: $s(k) = \Pr(T_* > k)$ where $T_*$ is the fastest SST. Show that $\Pr(T_* > k + m) \leq \Pr(T_* > k) \cdot \Pr(T_* > m)$ using the Markov property and the construction of $T_*$ from the strong stationary dual.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By the Aldous-Diaconis criterion, $s(k) = \inf_T \Pr(T > k)$ over all SSTs $T$. Let $T_1$ be an SST achieving the infimum at time $k$ (or approximately so). Define a new SST by $T = T_1 + T_2$ where $T_2$ is the "residual" SST starting from the chain's position at time $T_1$. Since $T_1$ is an SST, the chain is uniform at time $T_1$, so $T_2$ is independent of the pre-$T_1$ history. The new SST $T$ satisfies $\Pr(T > k + m) \leq \Pr(T_1 > k) + \Pr(T_1 \leq k, T_2 > m) = \Pr(T_1 > k) + \Pr(T_1 \leq k)\Pr(T_2 > m)$. But $\Pr(T_1 \leq k)\Pr(T_2 > m) \leq \Pr(T_2 > m)$ and $\Pr(T_1 > k) \leq s(k)$. Taking the infimum over all such decompositions: $s(k + m) \leq s(k) \cdot s(m)$, with the submultiplicativity arising from the Markov property applied at the SST.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Top-to-random mixing time — Aldous-Diaconis 1986).** The top-to-random shuffle on $S_n$ has mixing time $t_{\mathrm{mix}}(\varepsilon) = n \log n - n \log(-\log(1 - \varepsilon)) + O(n)$ with cutoff at $t_n = n \log n$. The SST is the time when the original bottom card reaches the top and is reinserted.

The proof constructs the strong stationary dual on $\{0, 1, \ldots, n\}$ as in Exercise 7 and shows that $\hat{X}_k$ reaches $n$ (complete uniformity) at a coupon-collector time, giving $\Pr(T > k) \leq e^{-k/n + \log n}$.

**Theorem 2 (Duality for SST — Diaconis-Griffiths 2018, Fill 1998).** Every strong stationary time corresponds to a strong stationary dual chain on a partially ordered set with a maximum element. Conversely, every such dual gives rise to an SST. The separation distance at time $k$ equals $1 - \Pr(\hat{X}_k = \hat{1})$ for the optimal dual.

This duality is the definitive structural result: it converts the probabilistic problem of finding an SST into the combinatorial problem of constructing a dual chain on a poset.

**Theorem 3 (Coupling inequality — Aldous 1983).** For any coupling $(X_k, Y_k)$ of two copies of the Markov chain with $Y_0 \sim \pi$ (started from stationarity), define the coupling time $\tau = \min\{k : X_k = Y_k\}$. Then $\|Q^{*k} - \pi\|_{\mathrm{TV}} \leq \Pr(\tau > k)$.

The coupling inequality is more general than the SST bound (it applies to any coupling, not just SSTs) but typically gives weaker bounds because the coupling time need not be an SST.

**Theorem 4 (Random transposition SST and cutoff).** The SST "every card touched" for random transpositions on $S_n$ gives $s(k) \leq \Pr(T > k)$ where $T$ is the coupon collector time with batch size 2. The separation distance achieves cutoff at $\frac{1}{2}n\log n$, matching the TV cutoff from the Upper Bound Lemma. In fact, separation cutoff and TV cutoff occur simultaneously for this walk.

**Theorem 5 (Separation vs TV — separation can be much larger).** For some walks, the separation distance $s(k)$ is much larger than the TV distance $\|Q^{*k} - U\|_{\mathrm{TV}}$. For the lazy random walk on $\mathbb{Z}/p\mathbb{Z}$ with $p$ prime: $\|Q^{*k} - U\|_{\mathrm{TV}} \to 0$ at rate $\beta_2^k$, but $s(k)$ can be bounded below by $e^{-k/p}$, which is much slower when $p$ is large. The SST approach overestimates the mixing time for chains without cutoff.

**Theorem 6 (SST for riffle shuffles — leakage argument).** There is no known simple SST for the riffle shuffle. The best bounds come from the Fourier-analytic Upper Bound Lemma rather than from strong stationary times. This contrasts with the top-to-random shuffle and the random transposition walk, where SSTs give sharp results.

**Synthesis.** Strong stationary times provide the foundational reason that certain random walks admit sharp mixing bounds via probabilistic coupling. The central insight is that the independence condition in the SST definition — the position at time $T$ is uniform and independent of $T$ — forces the distribution to be exactly stationary at a random time, and the separation distance measures how long this takes. Putting these together with the duality construction, the separation distance is identified with the tail probability of the optimal dual chain hitting its maximum, converting the mixing problem into a first-passage problem on a poset. This is exactly the content that generalises from the top-to-random shuffle (where the dual is the coupon collector) to arbitrary walks with known SST constructions. The bridge is between the probabilistic coupling framework and the analytic separation distance; the pattern recurs across walks where a natural "randomisation threshold" can be identified, and identifies the mixing time with the coupon collector time of the dual chain. The strong stationary time approach complements the Fourier-analytic Upper Bound Lemma, and for walks with cutoff the two methods agree.

## Full proof set [Master]

**Proposition 1 (Top-to-random SST).** The time $T$ at which the original bottom card in the top-to-random shuffle first reaches the top and is then reinserted at a random position is a strong stationary time.

*Proof.* Index the positions $1, \ldots, n$ from top to bottom. The original bottom card starts at position $n$. Call a card "good" if it is uniformly distributed among all cards below it in the deck. Initially, all cards are good vacuously.

When the top card is removed and reinserted at position $i$: if $i = 1$, the top card returns to the top and nothing changes. If $i > 1$, the top card is placed at position $i$ and all cards in positions $2, \ldots, i$ shift up one position. The key claim: if the bottom $j$ cards were uniformly distributed before this step, then after the step the bottom $\min(j+1, n)$ cards are uniformly distributed.

This holds because the inserted card is placed at a uniformly random position among all $n$ positions. If it lands among the bottom $j + 1$ positions, the randomised block grows by 1. By induction, once $j = n$ (all cards randomised), the permutation is uniform.

The time $T$ when $j = n$ is exactly the time when the original bottom card reaches position 1 (the top) and is then reinserted. At this moment all $n$ cards are randomised, so $X_T$ is uniform. The independence of $X_T$ and $T$ follows because the distribution of the permutation at the randomisation threshold does not depend on when the threshold is reached. $\square$

**Proposition 2 (Coupling inequality).** For any coupling $(X_k, Y_k)$ of two copies of the Markov chain with $Y_0 \sim \pi$:

$$
\|P^k(x, \cdot) - \pi\|_{\mathrm{TV}} \leq \Pr(\tau > k)
$$

where $\tau = \min\{k : X_k = Y_k\}$.

*Proof.* For any event $A \subseteq \Omega$:

$$
|P^k(x, A) - \pi(A)| = |\Pr(X_k \in A) - \Pr(Y_k \in A)|.
$$

On the event $\{\tau \leq k\}$, the two chains have coupled so $X_k = Y_k$, contributing 0 to the difference. On $\{\tau > k\}$:

$$
|\Pr(X_k \in A) - \Pr(Y_k \in A)| \leq \Pr(\tau > k).
$$

Taking the supremum over $A$: $\|P^k(x, \cdot) - \pi\|_{\mathrm{TV}} \leq \Pr(\tau > k)$. $\square$

## Connections [Master]

- **Non-abelian Fourier transform [07.01.09].** The Fourier-analytic bounds from the Upper Bound Lemma complement the SST approach: the Fourier transform gives TV bounds directly, while the SST gives separation bounds. For walks with cutoff, both methods converge to the same mixing time.

- **Symmetric group representation [07.05.01].** The irreducible representations of $S_n$ indexed by partitions control the eigenvalues of the shuffle transition matrix, which in turn determine the rate at which the SST dual chain approaches its maximum. The character values on cycle types developed in [07.05.01] appear in both the Fourier-analytic and SST approaches.

- **Regular representation [07.01.05].** The random walk on a group acts by convolution on the group algebra, which is the regular representation. The spectral decomposition of the regular representation into irreducibles, described in [07.01.05], determines the eigenvalues that control both the SST bound and the Upper Bound Lemma bound.

- **Character orthogonality [07.01.04].** The orthogonality relations of [07.01.04] ensure that the Fourier-analytic bound isolates the non-principal representations whose decay rates determine the mixing time, complementing the SST bound which captures the same mixing through a probabilistic stopping time.

## Historical & philosophical context [Master]

Aldous introduced strong stationary times in his 1983 paper [ref: TODO_REF Aldous1983] on random walks on finite groups, establishing the coupling inequality and the connection to separation distance. Aldous and Diaconis 1986 [ref: TODO_REF AldousDiaconis1986] applied SSTs to card shuffling, proving cutoff for the top-to-random shuffle and the random transposition walk using the coupon collector analysis of the dual chain.

Aldous and Diaconis 1987 [ref: TODO_REF AldousDiaconis1987] developed the full theory of strong uniform times (their term for SSTs) in *Advances in Applied Mathematics*, proving the duality theorem and the characterisation of separation distance as the infimum over SSTs. Fill 1998 refined the duality construction, and Diaconis and Griffiths 2018 provided a modern treatment. Levin, Peres, and Wilmer 2009 systematised the coupling and SST theory in their monograph *Markov Chains and Mixing Times*.

## Bibliography [Master]

```bibtex
@incollection{Aldous1983,
  author = {Aldous, David},
  title = {Random walks on finite groups and rapidly mixing Markov chains},
  booktitle = {Seminaire de Probabilites XVII},
  series = {Lecture Notes in Mathematics},
  volume = {986},
  publisher = {Springer},
  year = {1983},
  pages = {243--297},
}

@article{AldousDiaconis1986,
  author = {Aldous, David and Diaconis, Persi},
  title = {Shuffling cards and stopping times},
  journal = {Amer. Math. Monthly},
  volume = {93},
  year = {1986},
  pages = {333--348},
}

@article{AldousDiaconis1987,
  author = {Aldous, David and Diaconis, Persi},
  title = {Strong uniform times and finite random walks},
  journal = {Adv. Appl. Math.},
  volume = {8},
  year = {1987},
  pages = {69--97},
}

@book{LevinPeresWilmer2009,
  author = {Levin, David A. and Peres, Yuval and Wilmer, Elizabeth L.},
  title = {Markov Chains and Mixing Times},
  publisher = {American Mathematical Society},
  year = {2009},
}

@article{Fill1998,
  author = {Fill, James Allen},
  title = {An interruptible algorithm for perfect sampling via Markov chains},
  journal = {Ann. Appl. Probab.},
  volume = {8},
  year = {1998},
  pages = {131--162},
}

@article{DiaconisGriffiths2018,
  author = {Diaconis, Persi and Griffiths, Robert},
  title = {Column deletion and shuffling cards},
  journal = {Ann. Appl. Probab.},
  volume = {28},
  year = {2018},
  pages = {2170--2191},
}
```
