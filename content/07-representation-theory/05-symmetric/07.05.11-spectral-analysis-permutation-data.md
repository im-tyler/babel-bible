---
id: 07.05.11
title: Spectral analysis of permutation-valued data
slug: spectral-analysis-permutation-data
section: representation-theory
chapter: symmetric
concept_catalog_id: rep-theory.spectral-analysis-permutation-data
prerequisites:
  - 07.05.05
successors: []
tier_anchors:
  beginner: "Diaconis Group Representations in Probability and Statistics Ch. 3B informal; ranking-data intuition"
  intermediate: "Diaconis 1989 A Generalization of Spectral Analysis; Marden Analyzing and Modeling Rank Data Ch. 5"
  master: "Diaconis 1989 J. Amer. Statist. Assoc. 84; Marden 1995 Analyzing and Modeling Rank Data"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Diaconis, P. — A Generalization of Spectral Analysis"
    locator: "J. Amer. Statist. Assoc. 84 (1989), 694--701"
  - source: TODO_REF
    path: "Diaconis, P. — Group Representations in Probability and Statistics"
    locator: "IMS Lecture Notes Vol. 11 (1988), Ch. 3B, 7, 9"
  - source: TODO_REF
    path: "Marden, J. I. — Analyzing and Modeling Rank Data"
    locator: "Chapman & Hall (1995), Ch. 5--7"
lean_status: none
lean_mathlib_gap: |
  Mathlib lacks infrastructure for spectral analysis on the symmetric group
  applied to permutation-valued data. Missing pieces include: the Fourier
  transform of a real-valued function on S_n at irreducible representations
  indexed by partitions; the decomposition of a data distribution into
  irreducible components via the inverse Fourier transform on the group
  algebra; first-order and second-order effects for ranking data as projections
  onto isotypic components corresponding to the partitions (n), (n-1,1), and
  (n-2,2)+(n-2,1,1); the Mallows model and its Fourier spectrum; and
  the statistical testing framework using character sums as test statistics.
  The representation theory of S_n in Mathlib.RepresentationTheory provides
  foundations, but the statistical layer (ranking models, spectral decomposition
  of data, projection onto isotypic subspaces) has not been built.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 75m
status: shipped
produced_by: glm-5.1 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Imagine you collect survey responses where people rank five items from best to worst. Each person's response is a permutation — a reordering of the items. You have hundreds of such rankings and want to find the main "patterns" in the data.

Fourier analysis on the symmetric group does for permutation data what ordinary Fourier analysis does for time-series data: it decomposes a complicated signal into simple frequency components. Instead of sines and cosines, the "frequencies" are the irreducible representations of the symmetric group, indexed by partitions. Each component captures a different layer of structure in the ranking data.

Why does this concept exist? Because classical statistical tools assume numerical data, but rankings are permutations — and the representation theory of the symmetric group provides the natural harmonic analysis for functions defined on permutations.

## Visual [Beginner]

A diagram showing a bar chart of ranking data on three items (six possible rankings) being decomposed into three spectral components. The first component is the uniform average; the second captures "item 1 tends to be ranked first"; the third captures a pairwise swap effect.

![A six-bar histogram of rankings of three items, with arrows pointing to three spectral components: a flat uniform level, a linear tilt along one axis, and a checkerboard pairwise pattern.](/img/spectral-analysis-permutation-data-placeholder.svg)

The uniform component gives the overall popularity; the second-order component identifies which items are consistently preferred; higher components capture finer interactions.

## Worked example [Beginner]

Consider ranking data on $n = 3$ items. There are $3! = 6$ possible rankings. Imagine we observe the following counts from 60 respondents: the identity ranking (1,2,3) appears 20 times, the swap (2,1,3) appears 10 times, and each of the remaining four rankings appears 7 or 8 times.

Step 1. Convert counts to a probability distribution $f$ on the six permutations. For the identity $f(e) = 20/60 = 1/3$; for the swap $f((12)) = 10/60 = 1/6$; for the others roughly $1/8$.

Step 2. Compute the first spectral component: the "mean" level, which is the uniform distribution $U$ giving $1/6$ to each permutation. The deviation $f - U$ measures how far the data strays from uniform.

Step 3. The next spectral component projects onto the standard representation, which detects whether item 1 is systematically ranked too high or too low. Here, $f(e) - 1/6 = 1/6$ and $f((12)) - 1/6 = 0$, so the data is concentrated on rankings where item 1 is first.

What this tells us: the spectral decomposition separates the overall level (uniform), the first-order preference effects (item 1 is preferred), and the residual structure, giving a hierarchical summary of the ranking data.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Spectral analysis of permutation-valued data decomposes a distribution on permutations into components indexed by:

A. Prime numbers
B. Partitions of the number of items
C. Eigenvalues of the data matrix
D. Frequencies in hertz

<details class="exercise__hint"><summary>Hint</summary>

The irreducible representations of the symmetric group $S_n$ are indexed by partitions of $n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: the irreducible representations of $S_n$ are in bijection with partitions of $n$, and the spectral decomposition projects the data distribution onto the corresponding isotypic components. Feedback-wrong: A confuses representation theory with number theory; C confuses spectral analysis with principal component analysis; D confuses with classical Fourier analysis.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

The zeroth spectral component (corresponding to the identity representation) of any probability distribution on $S_n$ is the uniform distribution.

<details class="exercise__hint"><summary>Hint</summary>

The Fourier transform of a probability measure at the identity representation equals the sum of the probabilities, which is 1.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** The Fourier coefficient at the identity representation is 1 for any probability distribution (it just records that the values sum to 1). The zeroth *isotypic component* of the data itself, in the decomposition of $f$ into irreducibles, is the constant function $1/n!$, which is the uniform density. The distribution $f$ differs from uniform; the spectral decomposition measures that difference.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

For $S_3$, how many irreducible representations (and hence spectral components) does the spectral decomposition have? The partitions of 3 are $(3)$, $(2,1)$, and $(1,1,1)$.

<details class="exercise__hint"><summary>Hint</summary>

Count the partitions of 3.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**3.** The three partitions give three irreducible representations: the identity representation $(3)$, the standard representation $(2,1)$ of dimension 2, and the sign representation $(1,1,1)$ of dimension 1. The spectral decomposition of any function on $S_3$ has three levels.

</details>

</aside>

## Formal definition [Intermediate+]

Let $S_n$ be the symmetric group on $n$ letters with irreducible representations $V^\lambda$ indexed by partitions $\lambda \vdash n$, of dimension $d_\lambda$. Let $\rho^\lambda : S_n \to \mathrm{GL}(V^\lambda)$ denote the irreducible representation indexed by $\lambda$.

For a function $f : S_n \to \mathbb{C}$ (e.g., a probability distribution or a data histogram), the **Fourier transform** of $f$ at $\lambda$ is the $d_\lambda \times d_\lambda$ matrix

$$
\hat{f}(\lambda) = \sum_{\pi \in S_n} f(\pi) \, \rho^\lambda(\pi).
$$

The **spectral decomposition** of $f$ is the collection $\{\hat{f}(\lambda)\}_{\lambda \vdash n}$ of Fourier coefficients at all irreducible representations. The **inverse Fourier formula** reconstructs $f$ from its spectrum:

$$
f(\pi) = \frac{1}{n!} \sum_{\lambda \vdash n} d_\lambda \, \mathrm{tr}\!\left(\rho^\lambda(\pi^{-1}) \hat{f}(\lambda)\right).
$$

For ranking data, the key observation is that the isotypic components correspond to interpretable statistical effects. The component at $\lambda = (n)$ is the overall mean (uniform distribution). The component at $\lambda = (n-1,1)$ captures first-order effects (marginal preferences for individual items). The components at $\lambda = (n-2,2)$ and $(n-2,1,1)$ capture second-order effects (pairwise interactions).

### Counterexamples to common slips

- *Confusing Fourier coefficients with eigenvalues.* The Fourier coefficient $\hat{f}(\lambda)$ is a matrix, not a scalar. Its eigenvalues carry spectral information, but the coefficient itself is the basic object.
- *Omitting the dimension weighting.* The inverse formula weights by $d_\lambda$, not by 1. For $S_n$ the dimensions $d_\lambda$ vary widely (e.g., the standard representation has $d_{(n-1,1)} = n - 1$), so this weighting is essential.
- *Assuming abelian Fourier analysis suffices.* For abelian groups every irreducible is 1-dimensional and the Fourier transform produces scalars. For $S_n$ the irreducibles have dimensions up to roughly $\sqrt{n!}$, so the matrix-valued transform carries much more information.

## Key theorem with proof [Intermediate+]

**Theorem (Spectral decomposition for ranking data — Diaconis 1989).** Let $f : S_n \to [0,1]$ be a probability distribution on permutations. Define the **first-order projection** $f_1$ and the **second-order projection** $f_2$ by projecting $f$ onto the isotypic subspaces corresponding to the partitions $(n-1,1)$ and $\{(n-2,2), (n-2,1,1)\}$ respectively. Then:

$$
\|f - U\|_2^2 = \|\hat{f}((n{-}1,1))\|_{\mathrm{HS}}^2 + \sum_{\substack{\lambda \vdash n \\ \lambda \neq (n),\, (n-1,1)}} \frac{d_\lambda}{n!}\|\hat{f}(\lambda)\|_{\mathrm{HS}}^2,
$$

and the first-order term $\|\hat{f}((n-1,1))\|_{\mathrm{HS}}^2 / (n! \cdot d_{(n-1,1)})$ equals the sum of squared deviations of the marginal ranking probabilities from uniform:

$$
\frac{1}{n-1}\sum_{i=1}^{n}\sum_{j=1}^{n}\left(P(\pi(i) = j) - \frac{1}{n}\right)^2.
$$

*Proof.* The proof has three steps.

**Step 1 (Plancherel decomposition).** By the Plancherel formula for $S_n$:

$$
\sum_{\pi \in S_n} |f(\pi) - U(\pi)|^2 = \frac{1}{n!}\sum_{\lambda \vdash n} d_\lambda \|\widehat{f - U}(\lambda)\|_{\mathrm{HS}}^2.
$$

Since $\widehat{f-U}((n)) = \hat{f}((n)) - 1 = 0$ (as $f$ is a probability measure, its Fourier transform at the identity representation equals 1), the sum starts at $\lambda = (n-1,1)$.

**Step 2 (First-order identification).** The standard representation $V^{(n-1,1)}$ has a concrete realisation: it is the $(n-1)$-dimensional subspace of $\mathbb{C}^n$ consisting of vectors whose coordinates sum to zero, with $S_n$ acting by permutation of coordinates. The Fourier coefficient $\hat{f}((n-1,1))$ can be computed entry by entry:

$$
\hat{f}((n-1,1))_{a,b} = \sum_{\pi} f(\pi) \cdot [\rho^{(n-1,1)}(\pi)]_{a,b}.
$$

Using the basis $e_i - e_{i+1}$ for $i = 1, \ldots, n-1$, the matrix entries of $\rho^{(n-1,1)}(\pi)$ encode whether $\pi$ maps $i$ to $j$. The squared Hilbert-Schmidt norm then becomes a sum over the marginal probabilities $P(\pi(i) = j) = \sum_{\pi: \pi(i) = j} f(\pi)$:

$$
\frac{d_{(n-1,1)}}{n!}\|\hat{f}((n-1,1))\|_{\mathrm{HS}}^2 = (n-1)\sum_{i,j}\left(P(\pi(i) = j) - \frac{1}{n}\right)^2.
$$

The factor $(n-1)$ arises from the relationship between the trace inner product on the $(n-1)$-dimensional representation and the full $n \times n$ permutation matrix entries.

**Step 3 (Combine).** Separating the $(n-1,1)$ term from the rest of the Plancherel sum gives the stated decomposition. The first-order term captures all marginal (single-item) effects; the remaining terms capture all higher-order interactions. $\square$

**Bridge.** This spectral decomposition builds toward the analysis of partial rankings in [07.05.13] where the coset structure $S_n/S_{n-k}$ requires the representation theory of Gelfand pairs, and appears again in the metric analysis of [07.05.12] where the character-theoretic expressions for distances between permutations emerge from the same Fourier coefficients. The foundational reason the decomposition works is that the group algebra $\mathbb{C}[S_n]$ decomposes as a direct product of matrix algebras $\prod_\lambda M_{d_\lambda}(\mathbb{C})$, which is exactly the Artin-Wedderburn decomposition. The bridge is between statistical data analysis on permutations and the harmonic analysis on $S_n$ developed in [07.05.01]; putting these together, every test statistic on ranking data has a representation-theoretic interpretation as a spectral norm.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

For $S_4$, the partition $(3,1)$ indexes the standard representation. What is its dimension $d_{(3,1)}$? Use the hook-length formula: $d_\lambda = n!/\prod h(i,j)$ where $h(i,j)$ is the hook length of cell $(i,j)$ in the Young diagram.

<details class="exercise__hint"><summary>Hint</summary>

The Young diagram for $(3,1)$ has cells at positions $(1,1), (1,2), (1,3), (2,1)$. Hook lengths: $(1,1) \to 4$, $(1,2) \to 2$, $(1,3) \to 1$, $(2,1) \to 1$. So $d_{(3,1)} = 24/(4 \cdot 2 \cdot 1 \cdot 1) = 3$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**3.** $d_{(3,1)} = 4!/(4 \cdot 2 \cdot 1 \cdot 1) = 24/8 = 3$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 2 (easy, multiple choice).**

In the spectral decomposition of ranking data, the component corresponding to the partition $(n-1,1)$ captures which statistical effect?

A. The overall uniform distribution
B. First-order effects: how likely each item is to be ranked in each position
C. The sign of each permutation in the data
D. The total number of inversions

<details class="exercise__hint"><summary>Hint</summary>

The representation $(n-1,1)$ is the standard representation, which records how items are displaced from their "expected" positions.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: the first-order projection measures deviations of the marginal probabilities $P(\pi(i) = j)$ from $1/n$, which is exactly how likely item $i$ is to be in position $j$. Feedback-wrong: A is the identity representation $(n)$; C is the sign representation $(1^n)$; D is a scalar function, not tied to a single isotypic component.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Let $f$ be the uniform distribution on $S_n$. Compute $\hat{f}(\lambda)$ for every partition $\lambda \vdash n$.

<details class="exercise__hint"><summary>Hint</summary>

$U(\pi) = 1/n!$ for all $\pi$. Use the orthogonality of matrix coefficients: $\sum_{\pi \in S_n} \rho^\lambda_{ij}(\pi) \overline{\rho^\mu_{kl}(\pi)} = (n!/d_\lambda)\delta_{\lambda\mu}\delta_{ik}\delta_{jl}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\hat{U}(\lambda) = \frac{1}{n!}\sum_{\pi} \rho^\lambda(\pi) = \frac{1}{n!} \cdot \frac{n!}{d_\lambda} \cdot \mathrm{tr}(I_{d_\lambda})/d_\lambda \cdot I_{d_\lambda}$... actually more directly: for $\lambda = (n)$, $\hat{U}((n)) = \sum_\pi U(\pi) \cdot 1 = 1$. For $\lambda \neq (n)$: $\hat{U}(\lambda) = \frac{1}{n!}\sum_\pi \rho^\lambda(\pi)$. The sum $\sum_\pi \rho^\lambda(\pi)$ commutes with every $\rho^\lambda(\sigma)$, so by Schur's lemma it is a scalar matrix $cI_{d_\lambda}$. Taking traces: $c \cdot d_\lambda = \mathrm{tr}(\sum_\pi \rho^\lambda(\pi)) = \sum_\pi \chi_\lambda(\pi) = 0$ for $\lambda \neq (n)$ by column orthogonality. Hence $\hat{U}(\lambda) = 0$ for $\lambda \neq (n)$, and $\hat{U}((n)) = 1$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 4 (medium, numeric).**

For the Mallows model on $S_3$ with center $\pi_0 = e$ (identity) and dispersion parameter $\theta = 1$: $f(\pi) = e^{-d(\pi,e)}/Z$ where $d$ is Cayley distance and $Z$ is the normalising constant. Compute $\hat{f}((2,1))$, which is a $2 \times 2$ matrix. Give its trace.

<details class="exercise__hint"><summary>Hint</summary>

The Cayley distance $d(\pi, e)$ equals $n$ minus the number of cycles of $\pi$. For $S_3$: $d(e,e) = 0$, $d(\text{transp}) = 1$, $d(\text{3-cycle}) = 2$. So $f(e) = 1/Z$, $f(\text{transp}) = e^{-1}/Z$, $f(\text{3-cycle}) = e^{-2}/Z$. Compute $Z = 1 + 3e^{-1} + 2e^{-2}$. The character of $(2,1)$ is $\chi_{(2,1)}(e) = 2$, $\chi_{(2,1)}(\text{transp}) = 0$, $\chi_{(2,1)}(\text{3-cycle}) = -1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Since the Mallows model is conjugation-invariant (it depends only on the cycle type, i.e., the conjugacy class), the Fourier transform at every irreducible is a scalar matrix: $\hat{f}(\lambda) = \frac{\chi_\lambda(C)}{d_\lambda} \hat{Q}(\lambda)$... more directly, $\hat{f}(\lambda) = c_\lambda I_{d_\lambda}$ with $c_\lambda = \sum_{\pi} f(\pi)\chi_\lambda(\pi)/d_\lambda$.

For $\lambda = (2,1)$: $c_{(2,1)} = (f(e) \cdot 2 + f(\text{transp}) \cdot 0 + f(\text{3-cycle}) \cdot (-1))/2$.

$Z = 1 + 3e^{-1} + 2e^{-2} \approx 1 + 1.1036 + 0.2707 = 2.3743$.

$f(e) = 1/2.3743 \approx 0.4212$, $f(\text{3-cycle}) = e^{-2}/2.3743 \approx 0.1140$.

$c_{(2,1)} = (0.4212 \cdot 2 + 0.1140 \cdot (-1))/2 = (0.8424 - 0.1140)/2 = 0.7284/2 = 0.3642$.

Trace $= c_{(2,1)} \cdot d_{(2,1)} = 0.3642 \cdot 2 \approx$ **0.73**.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 5 (medium, multiple choice).**

Why is the spectral decomposition of ranking data called "spectral"?

A. Because it uses the spectrum of light
B. Because it decomposes the data into eigencomponents of the group algebra, analogous to the spectral theorem for matrices
C. Because it was developed by a person named Specter
D. Because it requires spectral sequences

<details class="exercise__hint"><summary>Hint</summary>

Think about the analogy with the spectral theorem in linear algebra, which decomposes a matrix into eigenvalues and eigenvectors.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: the group algebra $\mathbb{C}[S_n]$ decomposes as $\prod_\lambda M_{d_\lambda}(\mathbb{C})$ by Artin-Wedderburn, and projecting a data function onto the matrix blocks is the analogue of diagonalising a matrix — the "spectrum" is the collection of Fourier coefficients. Feedback-wrong: A is a physics analogy without mathematical content; C is a fabrication; D confuses representation theory with homological algebra.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Show that for a conjugation-invariant distribution $f$ on $S_n$ (i.e., $f(\sigma\pi\sigma^{-1}) = f(\pi)$ for all $\sigma, \pi$), every Fourier coefficient $\hat{f}(\lambda)$ is a scalar matrix, and express the scalar in terms of the character $\chi_\lambda$ and the dimension $d_\lambda$.

<details class="exercise__hint"><summary>Hint</summary>

Show $\hat{f}(\lambda)$ commutes with every $\rho^\lambda(\sigma)$ by using the conjugation-invariance of $f$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For any $\sigma \in S_n$: $\rho^\lambda(\sigma)\hat{f}(\lambda)\rho^\lambda(\sigma)^{-1} = \sum_\pi f(\pi)\rho^\lambda(\sigma\pi\sigma^{-1}) = \sum_\pi f(\sigma^{-1}\pi\sigma)\rho^\lambda(\pi) = \hat{f}(\lambda)$, where the last step uses the re-indexing $\pi \mapsto \sigma^{-1}\pi\sigma$ and conjugation-invariance $f(\sigma^{-1}\pi\sigma) = f(\pi)$. By Schur's lemma, $\hat{f}(\lambda) = c_\lambda I_{d_\lambda}$. Taking traces: $c_\lambda d_\lambda = \mathrm{tr}(\hat{f}(\lambda)) = \sum_\pi f(\pi)\chi_\lambda(\pi)$. So $c_\lambda = \frac{1}{d_\lambda}\sum_\pi f(\pi)\chi_\lambda(\pi)$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove the Plancherel formula for $S_n$: for any $f, g : S_n \to \mathbb{C}$,

$$
\sum_{\pi \in S_n} f(\pi)\overline{g(\pi)} = \frac{1}{n!}\sum_{\lambda \vdash n} d_\lambda \, \mathrm{tr}\!\left(\hat{f}(\lambda)\hat{g}(\lambda)^*\right).
$$

<details class="exercise__hint"><summary>Hint</summary>

Use the orthogonality of matrix coefficients: the functions $\sqrt{d_\lambda/n!}\,\rho^\lambda_{ij}$ form an orthonormal basis of $L^2(S_n)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The matrix coefficients $\{\rho^\lambda_{ij} : \lambda \vdash n, 1 \le i,j \le d_\lambda\}$ form an orthogonal basis of $L^2(S_n)$ with $\langle \rho^\lambda_{ij}, \rho^\mu_{kl}\rangle = (n!/d_\lambda)\delta_{\lambda\mu}\delta_{ik}\delta_{jl}$. Expanding $f$ in this basis: $f(\pi) = \frac{1}{n!}\sum_\lambda d_\lambda \sum_{i,j} \hat{f}(\lambda)_{ji}\,\rho^\lambda_{ij}(\pi)$. Then $\langle f, g\rangle = \sum_\pi f(\pi)\overline{g(\pi)}$. Substituting the expansions and using orthogonality: each $(i,j)$ cross-term gives $\frac{d_\lambda}{n!}\hat{f}(\lambda)_{ji}\overline{\hat{g}(\lambda)_{ji}}$. Summing over $i,j$ gives $\frac{d_\lambda}{n!}\mathrm{tr}(\hat{f}(\lambda)\hat{g}(\lambda)^*)$. Summing over $\lambda$ gives the Plancherel formula.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

For the Mallows model $f(\pi) = e^{-\theta \cdot d_K(\pi, \pi_0)}/Z$ where $d_K$ is the Kendall tau distance and $\pi_0$ is the identity, show that the first-order spectral coefficient $\hat{f}((n-1,1))$ determines the parameter $\theta$ uniquely (for $\theta > 0$).

<details class="exercise__hint"><summary>Hint</summary>

The Mallows model is right-invariant: $f(\pi\sigma) = f(\sigma)$ for $\sigma = \pi_0^{-1}\pi$, so it is conjugation-invariant when $\pi_0 = e$. Compute the character sum $\sum_\pi f(\pi)\chi_{(n-1,1)}(\pi)$ in terms of $\theta$ and show it is strictly monotone in $\theta$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For $\pi_0 = e$, the Mallows model depends only on the cycle type via Kendall tau distance. The character $\chi_{(n-1,1)}$ takes values $\chi_{(n-1,1)}(e) = n-1$, $\chi_{(n-1,1)}(\text{transp}) = n-3$, $\chi_{(n-1,1)}(\text{3-cycle}) = n-4$, etc. The scalar Fourier coefficient is $c(\theta) = \frac{1}{n-1}\sum_\pi f(\pi)\chi_{(n-1,1)}(\pi)$. Since $f$ is strictly decreasing in $d_K$ and $\chi_{(n-1,1)}$ is a class function that distinguishes conjugacy classes, $c(\theta)$ is a strictly monotone function of $\theta$ (as $\theta$ increases, more weight concentrates on $e$ where the character is largest). Hence $c(\theta)$ is injective for $\theta > 0$, and $\theta$ is determined by the first-order coefficient.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Diaconis 1989: spectral analysis generalises ANOVA).** The decomposition of $L^2(S_n)$ into isotypic components generalises the classical ANOVA decomposition for factorial designs. The identity component gives the grand mean; the $(n-1,1)$ component gives the main effects; the $(n-2,2)$ and $(n-2,1,1)$ components give the two-factor interactions; and so on through the partition lattice.

This was established in Diaconis 1989 *J. Amer. Statist. Assoc.* 84, where the isomorphism between isotypic components and ANOVA effects was made explicit through the relationship between irreducible characters and the inclusion-exclusion structure of marginal probabilities.

**Theorem 2 (Mallows model: explicit Fourier coefficients).** For the Mallows model $f(\pi) = e^{-\theta d_K(\pi, \pi_0)}/Z$ with Kendall tau distance $d_K$, the Fourier coefficients satisfy $\hat{f}(\lambda) = c_\lambda I_{d_\lambda}$ where $c_\lambda$ is expressible as a rational function of $q = e^{-\theta}$ involving the hook lengths of $\lambda$.

The closed-form expression was given by Diaconis 1989 and developed further by Marden 1995. The coefficient at $\lambda = (n-1,1)$ equals $(1 - q)/(1 - q^n)$, providing a direct link between the dispersion parameter and the first-order spectral component.

**Theorem 3 (Consistency of spectral estimates).** If $X_1, \ldots, X_N$ are i.i.d. draws from a distribution $f$ on $S_n$, then the empirical Fourier coefficients $\hat{f}_N(\lambda) = \frac{1}{N}\sum_{k=1}^N \rho^\lambda(X_k)$ satisfy $\hat{f}_N(\lambda) \to \hat{f}(\lambda)$ almost surely as $N \to \infty$, and $\sqrt{N}(\hat{f}_N(\lambda) - \hat{f}(\lambda))$ converges in distribution to a matrix-valued Gaussian.

This follows from the multivariate central limit theorem applied to the matrix entries of $\rho^\lambda(X_k)$, which are bounded random variables.

**Theorem 4 (Testing uniformity via spectral components).** To test $H_0: f = U$ against $H_1: f \neq U$, the test statistic $T = \sum_\pi (f_N(\pi) - 1/n!)^2$ has a representation-theoretic decomposition $T = \frac{1}{n!}\sum_{\lambda \neq (n)} d_\lambda \|\hat{f}_N(\lambda)\|_{\mathrm{HS}}^2$, and the first-order term $\|\hat{f}_N((n-1,1))\|_{\mathrm{HS}}^2$ provides the most powerful invariant test against alternatives with first-order structure.

This result appears in Diaconis 1989 and connects to the Neyman-Pearson lemma for group-invariant testing problems.

**Theorem 5 (Diaconis 1989: sufficient statistics from spectral components).** For the exponential family $f(\pi) = \exp(\sum_\lambda \langle\Theta_\lambda, \rho^\lambda(\pi)\rangle - A(\Theta))/Z$, where $\Theta_\lambda$ is a $d_\lambda \times d_\lambda$ matrix parameter for each partition $\lambda$, the collection of empirical Fourier coefficients $\{\hat{f}_N(\lambda)\}$ forms a sufficient statistic.

This is the exponential family on $S_n$ with natural parameters in each isotypic component; sufficiency follows from the factorisation theorem since the log-likelihood depends on the data only through the Fourier coefficients.

**Theorem 6 (Spectral analysis of paired comparisons).** For paired-comparison data (where subjects choose between pairs of items), the Bradley-Terry model has a spectral interpretation: the Bradley-Terry preference parameters are the eigenvalues of the first-order Fourier coefficient $\hat{f}((n-1,1))$.

This was observed by Marden 1995 and connects classical psychometric models to the spectral framework.

**Synthesis.** Spectral analysis of permutation-valued data is the foundational reason that representation theory enters statistics through the symmetric group. The central insight is that the Artin-Wedderburn decomposition of $\mathbb{C}[S_n]$ identifies the space of real-valued functions on permutations with the direct product of matrix algebras $\prod_\lambda M_{d_\lambda}(\mathbb{C})$, and putting these together with the Plancherel isomorphism, every statistical question about ranking data has a spectral reformulation. This is exactly the content that builds toward the metric analysis in [07.05.12] where distances between permutations become spectral norms, and appears again in [07.05.13] where partial rankings extend the framework to cosets. The bridge is between the combinatorics of partitions and the statistics of rankings; the pattern generalises from full rankings to partial rankings to paired comparisons, identifying the Fourier transform on $S_n$ as the universal tool for permutation-valued data analysis.

## Full proof set [Master]

**Proposition 1 (Inverse Fourier formula).** For $f : S_n \to \mathbb{C}$:

$$
f(\pi) = \frac{1}{n!}\sum_{\lambda \vdash n} d_\lambda \, \mathrm{tr}\!\left(\rho^\lambda(\pi^{-1})\hat{f}(\lambda)\right).
$$

*Proof.* Expand $\hat{f}(\lambda) = \sum_\sigma f(\sigma)\rho^\lambda(\sigma)$. Then:

$$
\frac{1}{n!}\sum_\lambda d_\lambda \, \mathrm{tr}\!\left(\rho^\lambda(\pi^{-1})\hat{f}(\lambda)\right) = \frac{1}{n!}\sum_\lambda d_\lambda \sum_\sigma f(\sigma)\mathrm{tr}\!\left(\rho^\lambda(\pi^{-1})\rho^\lambda(\sigma)\right).
$$

By the orthogonality of characters: $\sum_\lambda d_\lambda \,\mathrm{tr}(\rho^\lambda(\pi^{-1})\rho^\lambda(\sigma)) = \sum_\lambda d_\lambda \,\chi_\lambda(\pi^{-1}\sigma) = n! \cdot \delta_{\pi,\sigma}$ (column orthogonality of characters at the identity). Hence the right side collapses to $f(\pi)$. $\square$

**Proposition 2 (First-order coefficient and marginals).** For a probability distribution $f$ on $S_n$, the trace of $\hat{f}((n-1,1))$ equals $\sum_{i=1}^{n} P(\pi(i) = i) - 1$.

*Proof.* The character of the standard representation at $\pi$ is $\chi_{(n-1,1)}(\pi) = \text{fix}(\pi) - 1$ where $\text{fix}(\pi)$ is the number of fixed points of $\pi$. So:

$$
\mathrm{tr}(\hat{f}((n-1,1))) = \sum_\pi f(\pi)\chi_{(n-1,1)}(\pi) = \sum_\pi f(\pi)(\text{fix}(\pi) - 1) = \sum_{i=1}^n P(\pi(i)=i) - 1. \qquad \square
$$

## Connections [Master]

- **Random walk upper bound lemma [07.05.05].** The spectral decomposition of permutation data is the static counterpart to the dynamic analysis in [07.05.05]. The Upper Bound Lemma bounds the distance of a random walk distribution from uniform using the same character sums that appear here as spectral components; the random walk at time $k$ has Fourier coefficients $\hat{Q}(\lambda)^k$, and their decay rates control mixing, while the spectral analysis of data examines the same coefficients for a fixed empirical distribution.

- **Symmetric group representation [07.05.01].** The irreducible representations of $S_n$ indexed by partitions, their characters, and the hook-length formula for dimensions developed in [07.05.01] are the raw material for the spectral decomposition. Every Fourier coefficient $\hat{f}(\lambda)$ is computed using the matrix representations $\rho^\lambda$ and the characters $\chi_\lambda$ from that unit.

- **Metrics on the symmetric group [07.05.12].** The character-theoretic expressions for distances between permutations developed in the next unit are spectral norms of the difference of Fourier coefficients. The Cayley, Hamming, and Kendall tau distances all have representations as weighted character sums, making the spectral decomposition of this unit the common framework unifying distance computations on permutations.

- **Schur-Weyl duality [07.05.04].** The decomposition of tensor powers of $\mathbb{C}^n$ via Schur-Weyl duality produces the same indexing by partitions that governs the spectral analysis. The isotypic components of $L^2(S_n)$ are dual to the irreducible $\mathrm{GL}_n$-modules in the tensor algebra, linking the statistical framework to the representation theory of the general linear group.

## Historical & philosophical context [Master]

Diaconis introduced spectral analysis of permutation-valued data in his 1989 paper *A Generalization of Spectral Analysis* [ref: TODO_REF Diaconis1989] published in *J. Amer. Statist. Assoc.* 84. The key insight was that the ANOVA decomposition for factorial designs, which separates main effects from interactions, has a natural generalisation to permutations via the isotypic decomposition of the group algebra of $S_n$.

The statistical framework was systematised in Diaconis's 1988 monograph *Group Representations in Probability and Statistics* [ref: TODO_REF Diaconis1988], which placed the spectral analysis alongside the random walk and shuffling results as part of a unified programme applying representation theory to probability and statistics. Marden 1995 extended the framework to a wide variety of ranking models in *Analyzing and Modeling Rank Data* [ref: TODO_REF Marden1995], including the Mallows model, the Bradley-Terry model, and the Plackett-Luce model.

## Bibliography [Master]

```bibtex
@article{Diaconis1989,
  author = {Diaconis, Persi},
  title = {A Generalization of Spectral Analysis},
  journal = {J. Amer. Statist. Assoc.},
  volume = {84},
  year = {1989},
  pages = {694--701},
}

@book{Diaconis1988,
  author = {Diaconis, Persi},
  title = {Group Representations in Probability and Statistics},
  publisher = {Institute of Mathematical Statistics},
  year = {1988},
  series = {IMS Lecture Notes--Monograph Series},
  volume = {11},
}

@book{Marden1995,
  author = {Marden, John I.},
  title = {Analyzing and Modeling Rank Data},
  publisher = {Chapman \& Hall},
  year = {1995},
}
```
