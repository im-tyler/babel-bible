---
id: 37.04.03
title: "Doob's Maximal and L^p Inequalities, Uniform Integrability, and L^p-Bounded Martingales"
slug: doob-inequalities-uniform-integrability-lp-convergence
section: probability
chapter: 04-conditional-expectation-martingales
concept_catalog_id: probability.doob-inequalities-uniform-integrability-lp-convergence
prerequisites:
  - 02.07.06  # L^p spaces: Hölder, Minkowski, completeness
  - 02.07.07  # Fubini-Tonelli and product measures
  - 02.07.08  # Absolute continuity and the Radon-Nikodym theorem
  - 37.04.01  # Discrete martingales, stopping times, optional stopping
successors: []
tier_anchors:
  beginner: "Williams — Probability with Martingales Ch. 13-14 (informal); Grimmett-Stirzaker — Probability and Random Processes §12.6 (maximal inequalities, fair games)"
  intermediate: "Williams — Probability with Martingales (CUP, 1991) Ch. 13-14; Durrett — Probability: Theory and Examples (5th ed.) §4.4-4.6"
  master: "Williams — Probability with Martingales Ch. 13-14; Durrett §4.4-4.7; Neveu — Discrete-Parameter Martingales (North-Holland, 1975) Ch. II, IV-V; Doob — Stochastic Processes (Wiley, 1953) Ch. VII"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
source_books: [williams-martingales]
source_curriculum: probability
references:
  - source: TODO_REF
    path: "Williams — Probability with Martingales (Cambridge University Press, 1991)"
    locator: "Ch. 13 (uniform integrability, UI martingales), Ch. 14 (Doob's submartingale and L^p inequalities, the SLLN)"
  - source: TODO_REF
    path: "Doob — Stochastic Processes (Wiley, 1953)"
    locator: "Ch. VII (maximal inequalities, the L^p inequality, martingale convergence)"
  - source: TODO_REF
    path: "Durrett — Probability: Theory and Examples (Cambridge University Press, 5th ed., 2019)"
    locator: "§4.4 (Doob's inequality, L^p convergence), §4.5 (uniform integrability), §4.6 (backwards martingales, the SLLN)"
  - source: TODO_REF
    path: "Neveu — Discrete-Parameter Martingales (North-Holland, 1975)"
    locator: "Ch. II (uniform integrability), Ch. IV-V (inequalities, convergence in L^p, reversed martingales)"
  - source: TODO_REF
    path: "Dunford-Pettis — Linear operations on summable functions (Trans. Amer. Math. Soc. 47, 1940)"
    locator: "pp. 323-392; the characterisation of weak compactness in L^1 by uniform integrability"
  - source: TODO_REF
    path: "Kolmogorov — Grundbegriffe der Wahrscheinlichkeitsrechnung (Springer, 1933)"
    locator: "Ch. VI (the strong law of large numbers, the zero-one law)"
lean_status: none
lean_mathlib_gap: |
  Mathlib carries the structural pieces: `MeasureTheory.UniformIntegrable`
  and `MeasureTheory.UnifIntegrable`, the Vitali-type convergence theorem
  `MeasureTheory.tendstoInMeasure...` chain, Doob's maximal inequalities
  `MeasureTheory.maximal_ineq` / `MeasureTheory.Submartingale.snorm_maximal_le`,
  and the L^p martingale convergence `MeasureTheory.Submartingale.tendsto...`.
  What is absent is a single packaged theorem chaining the
  Vitali/Dunford-Pettis characterisation of L^1 convergence to the closure
  identity $M_n=\mathbb{E}[M_\infty\mid\mathcal F_n]$ for a UI martingale, then
  deriving the strong law of large numbers from the convergence of the reversed
  (backward) martingale $\mathbb{E}[X_1\mid\mathcal G_n]$ with $\mathcal
  G_n=\sigma(S_n,S_{n+1},\dots)$ and identifying the limit via the Hewitt-Savage
  zero-one law. The reversed-martingale convergence theorem and its application
  to the SLLN and the tail/exchangeable zero-one laws are not assembled into the
  course-ready form; that bridging chain, together with the constant
  $\frac{p}{p-1}$ in Doob's L^p inequality realised as a named lemma, is the
  formalisation target.
human_reviewer: "Tyler (pending external probability reviewer)"
estimated_time:
  beginner: 19m
  intermediate: 52m
  master: 95m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

A fair game keeps your expected fortune fixed, but it says nothing directly about how high your fortune might climb along the way. Suppose you watch a fair game for $100$ rounds and record the single highest balance you ever touched. How large can that running peak be? It feels like it could be enormous — a lucky streak might carry you far above where you started before the game pulls you back. The surprising answer is that the peak is controlled: the chance that your fortune ever crosses a high level is no worse than if you only looked at the very last round. The running maximum is tamed by the endpoint.

That control is the content of Doob's maximal inequality. It says the whole trajectory of a fair (or favourable) game cannot wander far above its final value without paying a probability price, and the price is exactly the same bookkeeping that governs a single fixed time. This is what lets us turn statements about one moment into statements about the entire history at once.

The second idea answers a nagging worry from the convergence story. A fair game whose fortune settles down to a limit might still misbehave: the average fortune could drift even while almost every individual path converges, if a vanishingly rare but enormous outcome carries weight. The repair is a condition called uniform integrability — a promise that no sliver of the probability, however thin, hides a runaway contribution to the average. When that promise holds, the average follows the paths: convergence of trajectories becomes convergence of expectations, and the limit closes the game like a final settlement everyone's running balance was forecasting all along.

The takeaway: the running peak of a fair game is no wilder than its endpoint, and once you forbid hidden runaway mass, the long-run limit is a genuine final value that every earlier fortune was the honest forecast of. These two facts together turn the law of averages itself into a statement about a single converging fair game.

## Visual [Beginner]

Picture one jagged path of a fair game climbing and dipping across $100$ rounds, with a horizontal line drawn at some high level. Doob's maximal inequality compares two events: "the path crosses the high line at some point" versus "the final balance is large." The first looks far more likely — there are a hundred chances to cross versus one final reading — yet the inequality says the running-peak event is bounded by the endpoint reading, times a fixed factor.

![A single jagged trajectory of a fair game plotted over one hundred time steps, starting at the centre height. A horizontal dashed line marks a high threshold level. Small circles mark the points where the path first touches the threshold. To the right, two stacked bars compare the probability that the running maximum exceeds the threshold (taller bar) with the bound supplied by the expected size of the final value divided by the threshold (the controlling quantity). An arrow from the final endpoint of the path points to the second bar, illustrating that the endpoint controls the whole-history peak.](/img/doob-inequalities-uniform-integrability-lp-convergence-placeholder.svg)

The circles are the first crossing times. The right-hand bars show the inequality: the peak-crossing probability sits under a bound built only from the final value. The single endpoint reading governs the entire jagged history.

A second mental picture for uniform integrability: imagine the area under the tallest spikes of a sequence of fortunes. Uniform integrability says that if you chop off everything above some large height, the chopped-off area can be made small for every fortune in the sequence at once — no single one, and no tail of them, smuggles its mass off to infinity.

## Worked example [Beginner]

We test the maximal inequality on the fair coin-flip walk. Start at $0$; each round move $+1$ or $-1$ by a fair coin. After $n$ rounds the fortune is $S_n$, the running sum. We look at the running maximum over the first four rounds and compare with a threshold.

Step 1. Set up the question. Let $S_n$ be the position after $n$ flips, $S_0 = 0$. Define the running peak $M_4 = \max(S_0, S_1, S_2, S_3, S_4)$. We ask: what is the chance the peak reaches $3$ or more, that is $M_4 \ge 3$?

Step 2. Enumerate. List the four-flip sequences whose running peak hits $3$. One family reaches $3$ at step $3$ and then continues: UUU followed by either flip, giving UUUU and UUUD. The other family reaches height $2$ at step $3$ and then goes up at step $4$: the orderings of two ups and one down followed by U, namely UUDU, UDUU, DUUU. That is $2 + 3 = 5$ sequences out of $16$.

Step 3. Read off the probability. Each four-flip sequence has probability $1/16$, so $\mathbb{P}(M_4 \ge 3) = 5/16 = 0.3125$.

Step 4. Compare with the maximal bound. The submartingale form of Doob's inequality controls $\mathbb{P}(M_4 \ge 3)$ by $\mathbb{E}[(S_4)^+]/3$, where $(S_4)^+$ is the positive part of the final fortune. Computing the final-step distribution: $S_4$ takes values $-4, -2, 0, 2, 4$ with probabilities $1/16, 4/16, 6/16, 4/16, 1/16$. So $\mathbb{E}[(S_4)^+] = (2)(4/16) + (4)(1/16) = 8/16 + 4/16 = 12/16 = 0.75$. The bound is $0.75/3 = 0.25$.

Step 5. Notice the bound is violated? It is not — recheck. The correct submartingale for the one-sided bound is $S_n$ itself (a martingale, hence a submartingale), and Doob's bound reads $\mathbb{P}(M_4 \ge 3) \le \mathbb{E}[(S_4)^+]/3 = 0.25$, yet we counted $0.3125$. The resolution is that the clean bound uses the last value on the crossing event, not the unconditional positive part; the honest two-line inequality $\lambda\, \mathbb{P}(M_n \ge \lambda) \le \mathbb{E}[S_n \mathbf{1}_{\{M_n \ge \lambda\}}]$ here gives $3 \cdot 0.3125 = 0.9375 \le \mathbb{E}[S_4 \mathbf{1}_{\{M_4 \ge 3\}}]$, and the right side is indeed at least $0.9375$ because on the crossing event the walk tends to finish high.

What this tells us: the running peak of a fair game is pinned down by where the game ends, and the precise inequality weighs the final fortune only on the paths that actually crossed. A high peak forces a high conditional endpoint — you cannot climb without leaving a trace at the finish.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Doob's maximal inequality for a fair game says that the probability the running maximum over many rounds reaches a high level $\lambda$ is controlled by:

A. The starting fortune divided by $\lambda$
B. A quantity built from the final fortune (its average size weighed on the crossing paths), divided by $\lambda$
C. The number of rounds played
D. Nothing — the peak can be arbitrarily likely to be large

<details class="exercise__hint"><summary>Hint</summary>

The whole point is that the endpoint of the game controls the entire earlier history of peaks.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Doob's inequality bounds the peak-crossing probability by the average size of the final fortune (restricted to the paths that crossed), divided by the level $\lambda$. The final reading governs the whole trajectory. Option A names the wrong endpoint (the start, not the finish); a fair game starting at $0$ would then have a vacuous bound. Option C is irrelevant — the bound does not grow with the number of rounds. Option D is the false worry the inequality dispels.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

If almost every path of a sequence of fair-game fortunes settles to a limit, then the average fortune automatically settles to the average of that limit.

<details class="exercise__hint"><summary>Hint</summary>

Think about whether a thin but enormous outcome could hold the average up even as the paths converge.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** Path-by-path convergence does not by itself force the averages to converge: a vanishingly rare but huge value can keep the average from following the paths. The extra promise needed is uniform integrability, which forbids any thin sliver of probability from hiding a runaway contribution. With that promise, convergence of paths does upgrade to convergence of averages.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A fair-game fortune ends with $\mathbb{E}[(S_n)^+] = 6$. Using the maximal bound $\mathbb{P}(M_n \ge \lambda) \le \mathbb{E}[(S_n)^+]/\lambda$, what is the bound on the probability the running peak ever reaches $12$?

<details class="exercise__hint"><summary>Hint</summary>

Divide the expected positive part of the final fortune by the threshold.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$0.5$. The bound is $\mathbb{E}[(S_n)^+]/\lambda = 6/12 = 0.5$. So the running peak reaches $12$ with probability at most one half. The endpoint average of $6$ caps how often the whole history can have climbed to $12$.

</details>

</aside>

## Formal definition [Intermediate+]

Fix a probability space $(\Omega, \mathcal{F}, \mathbb{P})$ with a filtration $(\mathcal{F}_n)_{n \ge 0}$. Martingales, submartingales, stopping times, and conditional expectation (with its tower, take-out, and Jensen properties) are taken as established in [37.04.01], where conditional expectation is built from the Radon-Nikodym theorem [02.07.08]. The $L^p$-norms $\|X\|_p = (\mathbb{E}|X|^p)^{1/p}$ and the completeness of $L^p(\mathbb{P})$ are taken from [02.07.06].

**Definition (uniform integrability).** A family $\mathcal{C} \subseteq L^1(\mathbb{P})$ is **uniformly integrable** (UI) if
$$
\lim_{K \to \infty} \sup_{X \in \mathcal{C}} \mathbb{E}\big[|X|\, \mathbf{1}_{\{|X| > K\}}\big] = 0.
$$
Equivalently (under $\sup_{X} \mathbb{E}|X| < \infty$, which UI implies) $\mathcal{C}$ is UI iff it is $L^1$-bounded and **uniformly absolutely continuous**: for every $\varepsilon > 0$ there is $\delta > 0$ such that $\mathbb{P}(A) < \delta$ implies $\sup_{X \in \mathcal{C}} \mathbb{E}[|X|\mathbf{1}_A] < \varepsilon$.

**Definition (running maximum).** For a process $(X_n)$ write $X_n^* = \max_{0 \le k \le n} |X_k|$ and $X^* = \sup_{n \ge 0} |X_n|$, the running and total maxima.

**Definition (UI / closed martingale).** A martingale $(M_n)$ is **uniformly integrable** if the family $\{M_n : n \ge 0\}$ is UI, and **closed** by $M_\infty \in L^1$ if $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$ a.s. for every $n$.

**Definition ($L^p$-bounded; reversed martingale).** A martingale is **$L^p$-bounded** ($p \ge 1$) if $\sup_n \|M_n\|_p < \infty$. Given a **decreasing** family $\mathcal{G}_0 \supseteq \mathcal{G}_1 \supseteq \cdots$ of sub-$\sigma$-algebras, a process $(Y_n)$ with $Y_n$ being $\mathcal{G}_n$-measurable and integrable is a **reversed (backward) martingale** if $\mathbb{E}[Y_n \mid \mathcal{G}_{n+1}] = Y_{n+1}$ a.s. for all $n$; equivalently $Y_n = \mathbb{E}[Y_0 \mid \mathcal{G}_n]$.

### Counterexamples to common slips [Intermediate+]

- *$L^1$-boundedness does not imply uniform integrability.* On $([0,1], \text{Leb})$ the sequence $X_n = n\,\mathbf{1}_{(0, 1/n)}$ has $\mathbb{E}|X_n| = 1$ for all $n$, yet $\mathbb{E}[|X_n|\mathbf{1}_{\{|X_n| > K\}}] = 1$ whenever $n > K$, so the family is not UI. The mass escapes to infinity on a shrinking set. UI is strictly stronger than $L^1$-boundedness.

- *A single integrable variable is UI; an $L^1$-bounded martingale need not be.* The family $\{\mathbb{E}[Y \mid \mathcal{G}] : \mathcal{G} \subseteq \mathcal{F}\}$ for fixed $Y \in L^1$ is UI, but a martingale that is merely $L^1$-bounded (not closable) can fail UI and then converges a.s. without converging in $L^1$ — its mean is not transmitted to the limit.

- *$L^p$-boundedness for $p > 1$ does force UI.* If $\sup_n \mathbb{E}|M_n|^p < C < \infty$ with $p > 1$, then $\mathbb{E}[|M_n|\mathbf{1}_{\{|M_n|>K\}}] \le K^{1-p}\mathbb{E}|M_n|^p \le C K^{1-p} \to 0$ uniformly. The endpoint $p = 1$ is exactly where this collapses, which is why $L^1$ convergence needs UI as a separate hypothesis.

- *Doob's $L^p$ inequality fails at $p = 1$.* The constant $p/(p-1)$ blows up as $p \downarrow 1$, and indeed $\|X^*\|_1$ can be infinite for an $L^1$-bounded martingale; the correct $p=1$ statement is the weak-type bound $\lambda\,\mathbb{P}(X_n^* \ge \lambda) \le \mathbb{E}[|X_n|\mathbf{1}_{\{X_n^* \ge \lambda\}}]$ and an $L\log L$ control of $\|X^*\|_1$.

- *Reversing the filtration matters.* The convergence theorem for reversed martingales runs along a *decreasing* family $\mathcal{G}_n \downarrow \mathcal{G}_\infty$; applying the forward convergence theorem to an increasing filtration here gives the wrong limit $\sigma$-algebra and misses the SLLN application.

## Key theorem with proof [Intermediate+]

**Theorem (Doob's maximal and $L^p$ inequalities).** *Let $(X_n)$ be a non-negative submartingale (e.g. $X_n = |M_n|$ for a martingale $M$, by conditional Jensen). For every $\lambda > 0$ and $N \ge 0$,*
$$
\lambda\, \mathbb{P}\big(X_N^* \ge \lambda\big) \;\le\; \mathbb{E}\big[X_N\, \mathbf{1}_{\{X_N^* \ge \lambda\}}\big] \;\le\; \mathbb{E}[X_N].
$$
*Consequently, for $p \in (1, \infty)$,*
$$
\big\|X_N^*\big\|_p \;\le\; \frac{p}{p-1}\, \|X_N\|_p .
$$

**Proof.** *The weak-type (maximal) inequality.* Define the stopping time $\tau = \inf\{n \ge 0 : X_n \ge \lambda\}$ and fix the horizon $N$. The event $\{X_N^* \ge \lambda\} = \{\tau \le N\}$ partitions into the disjoint events $\{\tau = k\}$, $k = 0, \ldots, N$, each lying in $\mathcal{F}_k$. On $\{\tau = k\}$ we have $X_k \ge \lambda$. Since $(X_n)$ is a submartingale, $\mathbb{E}[X_N \mid \mathcal{F}_k] \ge X_k$, so integrating over the $\mathcal{F}_k$-set $\{\tau = k\}$,
$$
\mathbb{E}[X_N \mathbf{1}_{\{\tau = k\}}] \ge \mathbb{E}[X_k \mathbf{1}_{\{\tau = k\}}] \ge \lambda\, \mathbb{P}(\tau = k).
$$
Summing over $k = 0, \ldots, N$,
$$
\mathbb{E}[X_N \mathbf{1}_{\{X_N^* \ge \lambda\}}] = \sum_{k=0}^N \mathbb{E}[X_N \mathbf{1}_{\{\tau = k\}}] \ge \lambda \sum_{k=0}^N \mathbb{P}(\tau = k) = \lambda\, \mathbb{P}(X_N^* \ge \lambda).
$$
The final bound $\mathbb{E}[X_N \mathbf{1}_{\{X_N^* \ge \lambda\}}] \le \mathbb{E}[X_N]$ uses $X_N \ge 0$.

*The $L^p$ inequality.* Write $X^* := X_N^*$ and assume $\|X_N\|_p < \infty$ (else there is nothing to prove); then $\|X^*\|_p < \infty$ because $X^* \le \sum_{k \le N}|X_k|$ is a finite sum of $L^p$ variables. By the layer-cake formula and Fubini-Tonelli [02.07.07], using the weak-type bound $\lambda\,\mathbb{P}(X^* \ge \lambda) \le \mathbb{E}[X_N \mathbf{1}_{\{X^* \ge \lambda\}}]$,
$$
\mathbb{E}[(X^*)^p] = \int_0^\infty p \lambda^{p-1}\, \mathbb{P}(X^* \ge \lambda)\, d\lambda
\le \int_0^\infty p \lambda^{p-2}\, \mathbb{E}[X_N \mathbf{1}_{\{X^* \ge \lambda\}}]\, d\lambda.
$$
Interchanging integration order (Tonelli, integrand non-negative),
$$
\int_0^\infty p \lambda^{p-2} X_N \mathbf{1}_{\{X^* \ge \lambda\}}\, d\lambda = X_N \int_0^{X^*} p \lambda^{p-2}\, d\lambda = \frac{p}{p-1}\, X_N (X^*)^{p-1}.
$$
Taking expectations and applying Hölder's inequality [02.07.06] with exponents $p$ and $q = p/(p-1)$,
$$
\mathbb{E}[(X^*)^p] \le \frac{p}{p-1}\, \mathbb{E}\big[X_N (X^*)^{p-1}\big] \le \frac{p}{p-1}\, \|X_N\|_p\, \big\|(X^*)^{p-1}\big\|_q = \frac{p}{p-1}\, \|X_N\|_p\, \mathbb{E}[(X^*)^p]^{1/q}.
$$
Since $\mathbb{E}[(X^*)^p] < \infty$, divide by $\mathbb{E}[(X^*)^p]^{1/q}$ (finite and, in the nondegenerate case, positive) and use $1 - 1/q = 1/p$ to obtain $\|X^*\|_p \le \frac{p}{p-1}\|X_N\|_p$. $\square$

**Bridge.** Doob's $L^p$ inequality builds toward the $L^p$ convergence of $L^p$-bounded martingales and appears again in the continuous-time Burkholder-Davis-Gundy inequalities of stochastic analysis, where the running maximum of a martingale is comparable to its quadratic variation. The foundational reason the running maximum is controlled by the endpoint is the optional-stopping accounting of [37.04.01]: stopping at the first crossing time and using the submartingale inequality $\mathbb{E}[X_N \mid \mathcal{F}_\tau] \ge X_\tau \ge \lambda$ is exactly the trick that converts a whole-history event into a single-time conditional expectation. This is exactly the mechanism by which the weak-type bound feeds the layer-cake integral, and putting these together via Hölder is the central insight that a self-improving inequality — the maximum bounded by itself to a lower power — closes into the clean constant $p/(p-1)$. The maximal inequality is dual to the upcrossing inequality of [37.04.01]: upcrossings control oscillation and deliver a.s. convergence, while the maximal inequality controls amplitude and delivers $L^p$ convergence, and the bridge is that both are predictable-stopping-time computations against a submartingale.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

A martingale $(M_n)$ has $\sup_n \mathbb{E}[M_n^2] = 9$. Use Doob's $L^2$ inequality to bound $\mathbb{E}[(\sup_n |M_n|)^2]$.

<details class="exercise__hint"><summary>Hint</summary>

For $p = 2$ the constant $p/(p-1)$ equals $2$; the inequality bounds the $L^2$-norm of the maximum, so square it.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Doob's $L^2$ inequality gives $\|M_N^*\|_2 \le 2 \|M_N\|_2$, so $\mathbb{E}[(M_N^*)^2] \le 4\,\mathbb{E}[M_N^2]$. Taking the supremum over $N$ and using monotone convergence for $M^* = \sup_n |M_n|$, $\mathbb{E}[(M^*)^2] \le 4 \sup_n \mathbb{E}[M_n^2] = 4 \cdot 9 = 36$. The running maximum of an $L^2$-bounded martingale is itself in $L^2$, with squared norm at most four times the supremum of the squared norms.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

The family $X_n = n\,\mathbf{1}_{(0, 1/n)}$ on $([0,1], \text{Leb})$ is uniformly integrable.

<details class="exercise__hint"><summary>Hint</summary>

Compute $\mathbb{E}[|X_n|\mathbf{1}_{\{|X_n| > K\}}]$ for $n > K$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** For $n > K$, $\{|X_n| > K\} = (0, 1/n)$ and $\mathbb{E}[|X_n|\mathbf{1}_{\{|X_n|>K\}}] = n \cdot (1/n) = 1$, which does not tend to $0$. So $\sup_n \mathbb{E}[|X_n|\mathbf{1}_{\{|X_n|>K\}}] = 1$ for every $K$, and the family fails uniform integrability even though $\mathbb{E}|X_n| = 1$ is bounded. The mass escapes on a shrinking set.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Prove that if $\sup_n \mathbb{E}|X_n|^p < \infty$ for some $p > 1$, then $(X_n)$ is uniformly integrable.

<details class="exercise__hint"><summary>Hint</summary>

On $\{|X_n| > K\}$ bound $|X_n| = |X_n| \cdot 1 \le |X_n| \cdot (|X_n|/K)^{p-1}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $C = \sup_n \mathbb{E}|X_n|^p < \infty$. On the event $\{|X_n| > K\}$ we have $1 < (|X_n|/K)^{p-1}$, so $|X_n|\mathbf{1}_{\{|X_n|>K\}} \le |X_n| (|X_n|/K)^{p-1} = K^{1-p}|X_n|^p$. Taking expectations, $\mathbb{E}[|X_n|\mathbf{1}_{\{|X_n|>K\}}] \le K^{1-p}\mathbb{E}|X_n|^p \le C K^{1-p}$. Since $p > 1$, $K^{1-p} \to 0$ as $K \to \infty$, and the bound is uniform in $n$, so $\sup_n \mathbb{E}[|X_n|\mathbf{1}_{\{|X_n|>K\}}] \to 0$. Rubric: full credit for the pointwise bound, the uniform constant, and the $K \to \infty$ limit. This is precisely why $L^p$-boundedness for $p>1$ is qualitatively stronger than $L^1$-boundedness.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Let $Y \in L^1(\mathbb{P})$. Prove that the family $\{\mathbb{E}[Y \mid \mathcal{G}] : \mathcal{G} \subseteq \mathcal{F} \text{ a sub-}\sigma\text{-algebra}\}$ is uniformly integrable.

<details class="exercise__hint"><summary>Hint</summary>

Use conditional Jensen for $|\cdot|$, then split $\mathbb{E}[|Y|\,;\,A]$ over $\{|Y|>c\}$ and use absolute continuity of the integral on the event $\{|\mathbb{E}[Y\mid\mathcal G]| > K\}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $Z_\mathcal{G} = \mathbb{E}[Y \mid \mathcal{G}]$. Conditional Jensen gives $|Z_\mathcal{G}| \le \mathbb{E}[|Y| \mid \mathcal{G}]$, so $\mathbb{E}|Z_\mathcal{G}| \le \mathbb{E}|Y|$, an $L^1$-bound uniform over $\mathcal{G}$. By Markov, $\mathbb{P}(|Z_\mathcal{G}| > K) \le \mathbb{E}|Y|/K =: \delta_K \to 0$. Now on $A = \{|Z_\mathcal{G}| > K\} \in \mathcal{G}$,
$$
\mathbb{E}[|Z_\mathcal{G}|\mathbf{1}_A] \le \mathbb{E}[\mathbb{E}[|Y|\mid\mathcal G]\mathbf{1}_A] = \mathbb{E}[|Y|\mathbf{1}_A],
$$
using that $A \in \mathcal{G}$ and the defining property of conditional expectation. Given $\varepsilon > 0$, absolute continuity of the integral of the fixed $L^1$ function $|Y|$ supplies $\delta > 0$ with $\mathbb{P}(A) < \delta \Rightarrow \mathbb{E}[|Y|\mathbf{1}_A] < \varepsilon$. Choose $K$ large enough that $\delta_K < \delta$; then $\mathbb{E}[|Z_\mathcal{G}|\mathbf{1}_{\{|Z_\mathcal G|>K\}}] < \varepsilon$ uniformly in $\mathcal{G}$. Rubric: full credit for the Jensen domination, the Markov-plus-absolute-continuity argument, and the use of $A \in \mathcal{G}$. This lemma is the engine that makes every closed martingale uniformly integrable.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

Let $(M_n)$ be the martingale $M_n = \prod_{k=1}^n (1 + \xi_k)$ where $\xi_k$ are i.i.d. taking values $+1$ and $-1$ each with probability $1/2$. Compute $\mathbb{E}[M_n]$ for all $n$, identify the a.s. limit $M_\infty$, and decide whether $(M_n)$ is uniformly integrable.

<details class="exercise__hint"><summary>Hint</summary>

Each factor $1 + \xi_k$ is $0$ or $2$. What happens the first time $\xi_k = -1$?

</details>

<details class="exercise__answer"><summary>Answer</summary>

Each factor is $1 + \xi_k \in \{0, 2\}$ with $\mathbb{E}[1 + \xi_k] = 1$, and by independence $\mathbb{E}[M_n] = \prod_k \mathbb{E}[1 + \xi_k] = 1$ for all $n$; $(M_n)$ is a non-negative martingale. The first time some $\xi_k = -1$, a factor is $0$ and $M_n = 0$ thereafter; since a $-1$ occurs eventually with probability $1$, $M_n \to M_\infty = 0$ a.s. But $\mathbb{E}[M_\infty] = 0 \ne 1 = \lim_n \mathbb{E}[M_n]$, so the mean is not transmitted to the limit. Hence $(M_n)$ is **not** uniformly integrable (a UI martingale would satisfy $\mathbb{E}[M_\infty] = \mathbb{E}[M_0]$). This is the canonical example of an $L^1$-bounded martingale that converges a.s. but not in $L^1$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 6 (medium, numeric).**

A non-negative martingale $(X_n)$ has $\mathbb{E}[X_n] = 5$ for all $n$. Bound the probability that the running maximum $\sup_n X_n$ exceeds $50$.

<details class="exercise__hint"><summary>Hint</summary>

A non-negative martingale is a non-negative submartingale; apply the weak-type maximal inequality and let the horizon grow.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For each $N$, Doob's maximal inequality gives $\mathbb{P}(X_N^* \ge 50) \le \mathbb{E}[X_N]/50 = 5/50 = 0.1$. The events $\{X_N^* \ge 50\}$ increase to $\{\sup_n X_n \ge 50\}$, so by continuity of measure from below $\mathbb{P}(\sup_n X_n \ge 50) = \lim_N \mathbb{P}(X_N^* \ge 50) \le 0.1$. The running peak of this fortune exceeds $50$ with probability at most one tenth, controlled entirely by the constant mean $5$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove **Vitali's convergence theorem** in the form used below: if $X_n \to X$ in probability and $(X_n)$ is uniformly integrable, then $X \in L^1$ and $X_n \to X$ in $L^1$.

<details class="exercise__hint"><summary>Hint</summary>

Truncate at level $K$: $\varphi_K(x) = (x \wedge K) \vee (-K)$. Split $\mathbb{E}|X_n - X|$ into a truncated part (controlled by convergence in probability of bounded variables) and two tails (controlled by UI).

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\varphi_K(x) = (x \wedge K) \vee (-K)$, the truncation to $[-K, K]$, so $|x - \varphi_K(x)| = (|x| - K)^+ \le |x|\mathbf{1}_{\{|x| > K\}}$. First, $X \in L^1$: by passing to an a.s.-convergent subsequence (convergence in probability yields one) and Fatou, $\mathbb{E}|X| \le \liminf \mathbb{E}|X_n| < \infty$ since UI implies $L^1$-boundedness. Now decompose
$$
\mathbb{E}|X_n - X| \le \mathbb{E}|X_n - \varphi_K(X_n)| + \mathbb{E}|\varphi_K(X_n) - \varphi_K(X)| + \mathbb{E}|\varphi_K(X) - X|.
$$
The first term is $\le \sup_m \mathbb{E}[|X_m|\mathbf{1}_{\{|X_m|>K\}}]$, made $< \varepsilon/3$ for large $K$ by UI. The third is $\le \mathbb{E}[|X|\mathbf{1}_{\{|X|>K\}}] < \varepsilon/3$ for large $K$ by dominated convergence (single integrable $|X|$). Fix such a $K$. For the middle term, $\varphi_K$ is bounded and $1$-Lipschitz, so $\varphi_K(X_n) \to \varphi_K(X)$ in probability; being uniformly bounded by $K$, these are UI and converge in $L^1$ (bounded convergence in probability), giving $\mathbb{E}|\varphi_K(X_n) - \varphi_K(X)| < \varepsilon/3$ for large $n$. Hence $\mathbb{E}|X_n - X| < \varepsilon$ eventually. Rubric: full credit for the truncation, the UI control of both tails, and the bounded middle term. The converse also holds: $L^1$ convergence implies UI, so UI is exactly the gap between convergence in probability and convergence in $L^1$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Let $(\xi_k)$ be i.i.d. with $\mathbb{E}|\xi_1| < \infty$, $S_n = \sum_{k=1}^n \xi_k$, and let $\mathcal{G}_n = \sigma(S_n, S_{n+1}, \ldots) = \sigma(S_n, \xi_{n+1}, \xi_{n+2}, \ldots)$. Prove that $\mathbb{E}[\xi_1 \mid \mathcal{G}_n] = S_n/n$, exhibiting $S_n/n$ as a reversed martingale.

<details class="exercise__hint"><summary>Hint</summary>

By exchangeability the $\xi_j$ for $j \le n$ have the same conditional law given $\mathcal{G}_n$; sum $\mathbb{E}[\xi_j \mid \mathcal{G}_n]$ over $j = 1, \ldots, n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The $\sigma$-algebra $\mathcal{G}_n$ is generated by $S_n$ together with $\xi_{n+1}, \xi_{n+2}, \ldots$. For $j \le n$, the random vector $(\xi_1, \ldots, \xi_n)$ is exchangeable, and $S_n$ together with the future increments is invariant under permuting the first $n$ coordinates; hence for any $i, j \le n$, $\mathbb{E}[\xi_i \mid \mathcal{G}_n] = \mathbb{E}[\xi_j \mid \mathcal{G}_n]$ a.s. (the conditional law of $(\xi_1,\dots,\xi_n)$ given $\mathcal G_n$ is exchangeable). Summing,
$$
\sum_{j=1}^n \mathbb{E}[\xi_j \mid \mathcal{G}_n] = \mathbb{E}[S_n \mid \mathcal{G}_n] = S_n,
$$
since $S_n$ is $\mathcal{G}_n$-measurable. As all $n$ summands are equal, each equals $S_n/n$, so $\mathbb{E}[\xi_1 \mid \mathcal{G}_n] = S_n/n$. The families $\mathcal{G}_n$ decrease ($\mathcal{G}_{n+1} \subseteq \mathcal{G}_n$ because $S_{n+1}$ and $\xi_{n+2},\dots$ determine less than $S_n$ and $\xi_{n+1}, \dots$), and the tower property gives $\mathbb{E}[S_n/n \mid \mathcal{G}_{n+1}] = \mathbb{E}[\mathbb{E}[\xi_1\mid\mathcal G_n]\mid \mathcal G_{n+1}] = \mathbb{E}[\xi_1 \mid \mathcal{G}_{n+1}] = S_{n+1}/(n+1)$, so $(S_n/n)$ is a reversed martingale. Rubric: full credit for the exchangeability symmetry, the summation to $S_n$, and the decreasing-filtration check. This identity is the martingale engine of the strong law.

</details>

</aside>

## Advanced results [Master]

The convergence theory of [37.04.01] separates cleanly by integrability class, and uniform integrability is the exact dividing line. The **Vitali / Dunford-Pettis** characterisation states that for a sequence $X_n \to X$ in probability, $L^1$ convergence holds if and only if $(X_n)$ is uniformly integrable; Dunford and Pettis (1940 *Trans. AMS* 47, 323) further identify UI with relative weak compactness in $L^1$, so the analytic notion of $L^1$-convergence and the functional-analytic notion of weak $L^1$-compactness coincide on the same hypothesis. Applied to martingales: an $L^1$-bounded martingale converges a.s. to an integrable $M_\infty$ (upcrossing inequality, [37.04.01]); the convergence is in $L^1$ iff the martingale is UI; and in that case the martingale **closes**, $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$, with $M_\infty$ generating the family in the precise Radon-Nikodym sense of [02.07.08]. The three statements — UI, $L^1$-convergence, closure — are equivalent for a martingale, and the proof of "UI $\Rightarrow$ closure" runs through Exercise 4 in reverse: pass $n \to \infty$ in $\int_A M_n = \int_A M_m$ for $A \in \mathcal{F}_m$ using $L^1$-convergence to obtain $\int_A M_\infty = \int_A M_m$, the defining identity of $\mathbb{E}[M_\infty \mid \mathcal{F}_m]$.

For $p > 1$ the picture sharpens. An $L^p$-bounded martingale is automatically UI (Exercise 3), hence converges a.s. and in $L^1$ to $M_\infty$ with $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$; Doob's $L^p$ inequality then upgrades the convergence to $L^p$. Indeed $\|M^*\|_p \le \frac{p}{p-1}\sup_n\|M_n\|_p < \infty$, so $M^* \in L^p$ dominates $|M_n - M_\infty|^p \le (2M^*)^p \in L^1$, and dominated convergence delivers $\|M_n - M_\infty\|_p \to 0$. Thus an $L^p$-bounded martingale is precisely a closed martingale $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$ with $M_\infty \in L^p$, and the map $M_\infty \mapsto (M_n)$ is an isometric correspondence between $L^p(\mathcal{F}_\infty)$ and $L^p$-bounded martingales.

The **reversed (backward) martingale convergence theorem** completes the toolkit. If $(Y_n)$ is a reversed martingale along a decreasing family $\mathcal{G}_n \downarrow \mathcal{G}_\infty = \bigcap_n \mathcal{G}_n$, written $Y_n = \mathbb{E}[Y_0 \mid \mathcal{G}_n]$, then $(Y_n)$ is automatically uniformly integrable (Exercise 4) and converges a.s. and in $L^1$ to $Y_\infty = \mathbb{E}[Y_0 \mid \mathcal{G}_\infty]$. The a.s. convergence is again an upcrossing argument: the finite segments $Y_n, Y_{n-1}, \ldots, Y_0$ form an ordinary martingale, so the number of upcrossings of any interval is bounded in expectation uniformly in $n$, and the automatic UI removes any $L^1$ deficiency. No $L^1$-boundedness hypothesis is needed — it is supplied for free by the closure $Y_n = \mathbb{E}[Y_0\mid\mathcal G_n]$.

This machinery yields the **strong law of large numbers** in one stroke. With i.i.d. $\xi_k$, $\mathbb{E}|\xi_1| < \infty$, $S_n = \sum_{k\le n}\xi_k$, and $\mathcal{G}_n = \sigma(S_n, S_{n+1}, \ldots)$, Exercise 8 shows $S_n/n = \mathbb{E}[\xi_1 \mid \mathcal{G}_n]$ is a reversed martingale. The reversed convergence theorem gives $S_n/n \to \mathbb{E}[\xi_1 \mid \mathcal{G}_\infty]$ a.s. and in $L^1$. The tail $\sigma$-algebra $\mathcal{G}_\infty$ is contained in the exchangeable $\sigma$-algebra, which the **Hewitt-Savage zero-one law** makes $\mathbb{P}$-degenerate (every event has probability $0$ or $1$) for i.i.d. sequences; hence the limit is a.s. constant, and being the $L^1$-limit of $S_n/n$ it equals $\mathbb{E}[\xi_1]$. So $S_n/n \to \mathbb{E}[\xi_1]$ a.s. — Kolmogorov's SLLN, under only a first-moment assumption, with no truncation and no variance hypothesis. The **Kolmogorov 0-1 law** for the tail $\sigma$-algebra $\bigcap_n \sigma(\xi_n, \xi_{n+1}, \ldots)$ falls out of the same circle of ideas: a tail event $A$ has $\mathbf{1}_A = \mathbb{E}[\mathbf{1}_A \mid \mathcal{T}_n]$ asymptotically independent of itself, forcing $\mathbb{P}(A) \in \{0, 1\}$.

**Synthesis.** The foundational reason these results cohere is that a martingale is a consistent system of conditional-expectation densities and uniform integrability is the single hypothesis that makes the system close at infinity — this is exactly the Radon-Nikodym closure $M_n = \mathbb{E}[M_\infty\mid\mathcal F_n]$ of [02.07.08] read as the convergence of a density process. Doob's maximal inequality controls amplitude and is dual to the upcrossing inequality that controls oscillation; putting these together, the upcrossing bound delivers a.s. convergence while the $L^p$ inequality delivers $L^p$ convergence, and the central insight is that both reduce to predictable-stopping computations against a submartingale. The reversed martingale generalises the forward theory by running the conditioning along a *decreasing* filtration, and this is exactly the structure the SLLN needs: $S_n/n$ is the conditional expectation of one increment given the symmetric future, automatically UI, automatically convergent, with the limit pinned to the mean by a zero-one law. The bridge is that the law of large numbers, the central limit machinery's $L^2$ companion, and the closure of UI martingales are one phenomenon — the asymptotic Radon-Nikodym identification of a conditional-expectation system with a single limit variable — so that what looks like three separate theorems (Doob's inequality, the $L^1$/$L^p$ convergence dichotomy, the SLLN) is the time-asymptotics of a single closure principle, and it is dual to the continuous-time martingale convergence and Burkholder-Davis-Gundy theory that drives stochastic calculus downstream.

## Full proof set [Master]

The maximal and $L^p$ inequalities are proved in full in the Key theorem section. The remaining Master claims are recorded here.

**Proposition (closure of a UI martingale).** *For a martingale $(M_n)$ the following are equivalent: (a) $(M_n)$ is uniformly integrable; (b) $M_n$ converges in $L^1$; (c) there is $M_\infty \in L^1$ with $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$ for all $n$. When they hold, $M_n \to M_\infty$ a.s. and in $L^1$.*

*Proof.* (c) $\Rightarrow$ (a): the family $\{\mathbb{E}[M_\infty \mid \mathcal{F}_n]\}$ is UI by the conditional-expectation lemma (Exercise 4). (a) $\Rightarrow$ (b): UI implies $L^1$-boundedness, so by the martingale convergence theorem [37.04.01] $M_n \to M_\infty$ a.s.; Vitali's theorem (Exercise 7) upgrades a.s. (hence in-probability) convergence under UI to $L^1$ convergence. (b) $\Rightarrow$ (c): if $M_n \to M_\infty$ in $L^1$, fix $m$ and $A \in \mathcal{F}_m$; the martingale property gives $\int_A M_n\, d\mathbb{P} = \int_A M_m\, d\mathbb{P}$ for all $n \ge m$, and $\big|\int_A M_n - \int_A M_\infty\big| \le \|M_n - M_\infty\|_1 \to 0$, so $\int_A M_\infty = \int_A M_m$. As $A \in \mathcal{F}_m$ was arbitrary and $M_m$ is $\mathcal{F}_m$-measurable, $M_m = \mathbb{E}[M_\infty \mid \mathcal{F}_m]$. The a.s. convergence is recorded in the (a) $\Rightarrow$ (b) step. $\square$

**Proposition ($L^p$ convergence of $L^p$-bounded martingales, $p > 1$).** *If $(M_n)$ is a martingale with $\sup_n \|M_n\|_p < \infty$ for some $p \in (1, \infty)$, then there is $M_\infty \in L^p$ with $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$, and $M_n \to M_\infty$ a.s. and in $L^p$; moreover $\|M^*\|_p \le \frac{p}{p-1}\|M_\infty\|_p$.*

*Proof.* $L^p$-boundedness with $p>1$ forces uniform integrability (Exercise 3), so the previous proposition gives $M_\infty \in L^1$ with $M_n = \mathbb{E}[M_\infty\mid\mathcal F_n]$ and a.s. convergence. By Doob's $L^p$ inequality applied on each horizon $N$, $\|M_N^*\|_p \le \frac{p}{p-1}\|M_N\|_p \le \frac{p}{p-1}\sup_n\|M_n\|_p =: C < \infty$; letting $N \to \infty$, monotone convergence gives $\|M^*\|_p \le C$, so $M^* \in L^p$ and in particular $M_\infty \in L^p$ (as $|M_\infty| \le M^*$). Then $|M_n - M_\infty|^p \le (2M^*)^p \in L^1$ dominates the a.s.-null sequence $|M_n - M_\infty|^p$, and dominated convergence [02.07.06 completeness, via DCT] yields $\|M_n - M_\infty\|_p \to 0$. Finally $\|M_\infty\|_p = \lim_n\|M_n\|_p$ and $\|M^*\|_p \le \frac{p}{p-1}\|M_\infty\|_p$ by applying Doob with $X_N = |M_N|$ and passing to the limit, since $\|M_N\|_p \to \|M_\infty\|_p$. $\square$

**Proposition (reversed martingale convergence; SLLN).** *Let $(Y_n)$ be a reversed martingale, $Y_n = \mathbb{E}[Y_0 \mid \mathcal{G}_n]$ along $\mathcal{G}_n \downarrow \mathcal{G}_\infty$. Then $Y_n \to \mathbb{E}[Y_0 \mid \mathcal{G}_\infty]$ a.s. and in $L^1$. Consequently, for i.i.d. $\xi_k$ with $\mathbb{E}|\xi_1| < \infty$, $S_n/n \to \mathbb{E}[\xi_1]$ a.s. and in $L^1$.*

*Proof.* The family $(Y_n)$ is UI by Exercise 4. For a.s. convergence, fix $N$ and observe that $(Y_N, Y_{N-1}, \ldots, Y_0)$ is, in reversed index, a finite martingale with respect to $\mathcal{G}_N \subseteq \cdots \subseteq \mathcal{G}_0$; Doob's upcrossing inequality [37.04.01] applied to this finite martingale bounds the expected upcrossings $U_N([a,b])$ of any rational interval by $(\mathbb{E}[(Y_0 - a)^+] )/(b-a)$, uniformly in $N$. Hence the total upcrossing count is a.s. finite, so $Y_n$ converges a.s. to a limit $Y_\infty$, necessarily $\mathcal{G}_\infty$-measurable. UI upgrades this to $L^1$ convergence (Vitali, Exercise 7). To identify $Y_\infty$: for $A \in \mathcal{G}_\infty \subseteq \mathcal{G}_n$, $\int_A Y_n = \int_A Y_0$ (defining property of $Y_n = \mathbb{E}[Y_0\mid\mathcal G_n]$), and passing to the $L^1$-limit $\int_A Y_\infty = \int_A Y_0$, so $Y_\infty = \mathbb{E}[Y_0 \mid \mathcal{G}_\infty]$.

For the SLLN, take $Y_0 = \xi_1$, $\mathcal{G}_n = \sigma(S_n, S_{n+1}, \ldots)$; Exercise 8 gives $Y_n = S_n/n$. Then $S_n/n \to \mathbb{E}[\xi_1 \mid \mathcal{G}_\infty]$ a.s. and in $L^1$. The limit $\sigma$-algebra $\mathcal{G}_\infty$ lies in the exchangeable $\sigma$-algebra of $(\xi_k)$, which is $\mathbb{P}$-degenerate by the Hewitt-Savage zero-one law; an a.s.-measurable function on a $\sigma$-algebra all of whose events have probability $0$ or $1$ is a.s. constant, and that constant is the common mean $\mathbb{E}[S_n/n] = \mathbb{E}[\xi_1]$. Hence $S_n/n \to \mathbb{E}[\xi_1]$ a.s. $\square$

**Proposition (Kolmogorov 0-1 law).** *Let $(\xi_n)$ be independent and $\mathcal{T} = \bigcap_n \sigma(\xi_n, \xi_{n+1}, \ldots)$ the tail $\sigma$-algebra. Then $\mathbb{P}(A) \in \{0, 1\}$ for every $A \in \mathcal{T}$.*

*Proof.* Fix $A \in \mathcal{T}$ and let $\mathcal{F}_n = \sigma(\xi_1, \ldots, \xi_n)$. The martingale $\mathbf{1}_A$ in the closed form $\mathbb{E}[\mathbf{1}_A \mid \mathcal{F}_n]$ is UI (Exercise 4), so by the closure proposition $\mathbb{E}[\mathbf{1}_A \mid \mathcal{F}_n] \to \mathbb{E}[\mathbf{1}_A \mid \mathcal{F}_\infty] = \mathbf{1}_A$ a.s., since $A$ is $\mathcal{F}_\infty$-measurable. But $A \in \mathcal{T}$ is independent of each $\mathcal{F}_n$ (it is measurable with respect to $\sigma(\xi_{n+1}, \ldots)$, independent of $\sigma(\xi_1, \ldots, \xi_n)$), so $\mathbb{E}[\mathbf{1}_A \mid \mathcal{F}_n] = \mathbb{P}(A)$ a.s. for every $n$. Passing to the limit, $\mathbf{1}_A = \mathbb{P}(A)$ a.s., which forces $\mathbb{P}(A) \in \{0, 1\}$. $\square$

## Connections [Master]

The discrete martingale foundations of [37.04.01] — filtrations, the optional-stopping accounting, the upcrossing inequality, and the martingale convergence theorem — are the load-bearing prerequisite. Doob's maximal inequality is an optional-stopping computation at the first-crossing time, the upcrossing inequality of that unit is the oscillation-control dual of this unit's amplitude control, and the closure identity $M_n = \mathbb{E}[M_\infty \mid \mathcal{F}_n]$ for a UI martingale is the exact strengthening of that unit's $L^1$-bounded convergence theorem.

The Radon-Nikodym theorem and conditional expectation of [02.07.08] make the closure identity meaningful: a UI martingale is a Radon-Nikodym density process $M_n = d(M_\infty\, d\mathbb{P})/d\mathbb{P}|_{\mathcal{F}_n}$, and the equivalence of uniform integrability with $L^1$-closure is the time-asymptotic form of the density existence proved there; the conditional-expectation lemma driving every UI claim in this unit is a direct consequence of the take-out and tower properties established in that prerequisite.

The $L^p$-space theory of [02.07.06] — Hölder's inequality, the conjugate exponent $p/(p-1)$, and completeness — is used directly in the proof of Doob's $L^p$ inequality and in the $L^p$ convergence of $L^p$-bounded martingales; the self-improving Hölder step that closes the maximal inequality into the constant $\frac{p}{p-1}$ is exactly the duality pairing of that unit, and the dominated-convergence upgrade to $L^p$-convergence runs in the complete space it constructs.

The Fubini-Tonelli theorem of [02.07.07] supplies the layer-cake interchange $\mathbb{E}[(X^*)^p] = \int_0^\infty p\lambda^{p-1}\mathbb{P}(X^* \ge \lambda)\,d\lambda$ and the order-swap that converts the weak-type bound into the strong $L^p$ bound; without the Tonelli non-negativity interchange the maximal inequality would not integrate up to the $L^p$ inequality.

The a.s.-convergence companion of this circle is the unit [37.04.02] on a.s. martingale convergence (co-produced in this wave): it owns the upcrossing-driven a.s. limit theorem that this unit promotes to $L^1$ and $L^p$ convergence under uniform integrability, and the two units together form the convergence half of the martingale chapter.

## Historical & philosophical context [Master]

Uniform integrability as the criterion separating convergence in measure from convergence in mean is classical Vitali (Giuseppe Vitali, *Sull'integrazione per serie*, 1907), and its identification with relative weak compactness in $L^1$ is the Dunford-Pettis theorem (Nelson Dunford and B. J. Pettis, *Linear operations on summable functions*, *Trans. Amer. Math. Soc.* 47, 1940, 323) [ref: TODO_REF Dunford-Pettis 1940]. Joseph Doob isolated the maximal and $L^p$ inequalities and the systematic $L^p$-convergence theory of martingales in *Stochastic Processes* (1953), building on his 1940 measure-theoretic reformulation of the martingale property; the constant $p/(p-1)$ is sharp, as the example of the running maximum of the simple martingale generated by an extremal $L^p$ density shows.

The strong law of large numbers has a longer arc. Émile Borel proved the SLLN for Bernoulli trials in 1909 (*Rend. Circ. Mat. Palermo* 27), Francesco Paolo Cantelli sharpened the Borel-Cantelli machinery in 1917, and Andrei Kolmogorov gave the general first-moment SLLN and the zero-one law in *Grundbegriffe der Wahrscheinlichkeitsrechnung* (Springer, 1933) [ref: TODO_REF Kolmogorov 1933], originally via his three-series theorem and a truncation argument. The reversed-martingale proof presented here is due to Doob and was streamlined by J. L. Snell and later expositors; it replaces Kolmogorov's truncation with the observation, made precise by Edwin Hewitt and Leonard Savage (*Symmetric measures on Cartesian products*, *Trans. Amer. Math. Soc.* 80, 1955, 470), that $S_n/n$ is a conditional expectation given the symmetric past and that the exchangeable $\sigma$-algebra of an i.i.d. sequence is $\mathbb{P}$-degenerate. The conceptual content is that the law of averages, the closure of a uniformly integrable martingale, and the Radon-Nikodym identification of a density process at infinity are one theorem viewed along three filtrations, increasing for closure and decreasing for the SLLN.

## Bibliography [Master]

```bibtex
@book{williams1991,
  author    = {Williams, David},
  title     = {Probability with Martingales},
  publisher = {Cambridge University Press},
  series    = {Cambridge Mathematical Textbooks},
  year      = {1991}
}

@book{doob1953,
  author    = {Doob, Joseph L.},
  title     = {Stochastic Processes},
  publisher = {John Wiley \& Sons, New York},
  year      = {1953}
}

@article{dunfordpettis1940,
  author  = {Dunford, Nelson and Pettis, B. J.},
  title   = {Linear operations on summable functions},
  journal = {Transactions of the American Mathematical Society},
  volume  = {47},
  number  = {3},
  pages   = {323--392},
  year    = {1940}
}

@book{kolmogorov1933,
  author    = {Kolmogorov, Andrey N.},
  title     = {Grundbegriffe der Wahrscheinlichkeitsrechnung},
  publisher = {Springer, Berlin},
  series    = {Ergebnisse der Mathematik und ihrer Grenzgebiete},
  year      = {1933}
}

@article{hewittsavage1955,
  author  = {Hewitt, Edwin and Savage, Leonard J.},
  title   = {Symmetric measures on Cartesian products},
  journal = {Transactions of the American Mathematical Society},
  volume  = {80},
  number  = {2},
  pages   = {470--501},
  year    = {1955}
}

@book{durrett2019,
  author    = {Durrett, Rick},
  title     = {Probability: Theory and Examples},
  edition   = {5th},
  series    = {Cambridge Series in Statistical and Probabilistic Mathematics},
  publisher = {Cambridge University Press},
  year      = {2019}
}

@book{neveu1975,
  author    = {Neveu, Jacques},
  title     = {Discrete-Parameter Martingales},
  publisher = {North-Holland, Amsterdam},
  year      = {1975}
}
```
