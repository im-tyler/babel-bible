---
id: 37.08.06
title: "The Largest Eigenvalue and the Operator-Norm Bound"
slug: largest-eigenvalue-operator-norm-bound
section: probability
chapter: 08-random-matrices
concept_catalog_id: probability.largest-eigenvalue-operator-norm-bound
prerequisites:
  - 37.08.01  # Wigner semicircle law and the moment method (ensemble, ESD, semicircle moments, trace-power expansion)
successors: []
tier_anchors:
  beginner: "Tao, Topics in Random Matrix Theory §2.3 (operator norm of a random matrix, the spectral edge); Vershynin, High-Dimensional Probability §4.4 (the epsilon-net heuristic for the norm); the physical picture of the eigenvalue histogram and its rightmost spike"
  intermediate: "Tao, Topics in Random Matrix Theory §2.3 (operator norm via high moments); Vershynin, High-Dimensional Probability (Cambridge, 2018) §4.4 (net argument, sub-gaussian norm bound); Bai-Yin, Annals of Probability 16 (1988) (a.s. convergence of the largest eigenvalue)"
  master: "Anderson-Guionnet-Zeitouni, An Introduction to Random Matrices (Cambridge, 2010) §2.1.6 (operator norm, high-trace method); Tao, Topics in Random Matrix Theory (AMS GSM 132, 2012) §2.3; Bai-Yin, Necessary and sufficient conditions for almost sure convergence of the largest eigenvalue, Annals of Probability 16 (1988), 1729-1741; Tracy-Widom, Commun. Math. Phys. 159 (1994), 151-174"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Bai, Yin — Necessary and sufficient conditions for almost sure convergence of the largest eigenvalue of a Wigner matrix"
    locator: "Annals of Probability 16 (1988), 1729-1741 (a.s. convergence of the operator norm under a fourth-moment condition)"
  - source: TODO_REF
    path: "Füredi, Komlós — The eigenvalues of random symmetric matrices"
    locator: "Combinatorica 1 (1981), 233-241 (high-trace / moment method for the operator-norm bound)"
  - source: TODO_REF
    path: "Anderson, Guionnet, Zeitouni — An Introduction to Random Matrices"
    locator: "Cambridge University Press, 2010, §2.1.6 (operator-norm upper bound via growing even traces)"
  - source: TODO_REF
    path: "Vershynin — High-Dimensional Probability: An Introduction with Applications in Data Science"
    locator: "Cambridge University Press, 2018, §4.4 (epsilon-net argument, non-asymptotic norm bounds for sub-gaussian matrices)"
  - source: TODO_REF
    path: "Tracy, Widom — Level-spacing distributions and the Airy kernel"
    locator: "Communications in Mathematical Physics 159 (1994), 151-174 (Tracy-Widom edge fluctuation law)"
  - source: TODO_REF
    path: "Tao — Topics in Random Matrix Theory"
    locator: "AMS Graduate Studies in Mathematics 132, 2012, §2.3 (operator norm, high moments, edge)"
lean_status: none
lean_mathlib_gap: |
  Mathlib has the spectral theorem for finite-dimensional Hermitian matrices, the
  operator norm of a continuous linear map, the matrix trace and trace of powers,
  and — through the probability and measure-theory libraries — independence,
  expectation, Markov's and Chebyshev's inequalities, and the Borel-Cantelli
  lemmas. What is absent is the random-matrix edge layer proper. There is no
  packaging of the operator norm of a Wigner matrix as the largest eigenvalue
  $\lambda_{\max}(M_n) = \|M_n\|_{\mathrm{op}}$ together with the high-trace
  bound $\mathbb{E}\,\mathrm{tr}(M_n)^{2k} \ge \mathbb{E}\,\lambda_{\max}^{2k}$
  used to push the moment method to the edge, no implementation of the choice of
  a slowly growing exponent $k = k_n \to \infty$ that turns the Catalan-number
  trace asymptotics into a sharp $2 + o(1)$ upper bound, and no formal
  epsilon-net / covering-number argument controlling $\sup_{\|x\|=1}\langle x, M_n x\rangle$
  by a maximum over a finite net with a union bound. The Bai-Yin theorem —
  almost-sure convergence $\|M_n\|_{\mathrm{op}} \to 2$ under exactly a finite
  fourth moment, with its truncation and the matching lower bound from a single
  large diagonal-block eigenvalue — and the Tracy-Widom edge fluctuation at
  scale $n^{2/3}$ are far beyond current Mathlib reach. Each is a concrete
  formalisation target on top of the existing spectral-theorem, operator-norm,
  and concentration files.
human_reviewer: "Tyler (pending external probability reviewer)"
estimated_time:
  beginner: 20m
  intermediate: 55m
  master: 95m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [agz-random-matrices]
source_curriculum: probability
---

## Intuition [Beginner]

The semicircle picture tells you where the bulk of a random matrix's eigenvalues live: spread smoothly across a fixed window from minus two to plus two once the entries are rescaled. But the histogram of the bulk leaves one question open. The single largest eigenvalue — the rightmost spike in the picture — could in principle poke out past the edge of the window, sitting alone far to the right of where the bulk ends. Does it? The answer is that it does not. As the matrix grows, the top eigenvalue presses right up against the edge of the semicircle and stops there, settling onto the value two.

This is a statement about the strongest stretching the matrix can do in any direction. The largest eigenvalue measures the most a symmetric matrix can amplify a vector. So saying it converges to two says the matrix never stretches anything by much more than two, no matter which direction you try, once you have rescaled it. There is no rogue direction of runaway amplification hiding outside the bulk.

Two different styles of argument pin this down. One counts very high powers of the matrix, because raising to a big power makes the largest eigenvalue dominate everything else, so its size leaks out of an averaged power. The other covers the sphere of all directions with a fine grid and checks them all at once. Both lead to the same wall at two.

## Visual [Beginner]

Picture the familiar semicircle histogram of eigenvalues, a smooth half-dome sitting over the window from minus two to plus two. Now mark the single rightmost eigenvalue with a tall thin pointer. For a small matrix the pointer wobbles: sometimes it lands a little inside the right edge, sometimes it pokes slightly past two. As you redraw the picture for bigger and bigger matrices, the pointer's wobble shrinks and it locks onto the right edge at two, hugging the spot where the half-dome meets the floor.

![A semicircle-shaped eigenvalue histogram spanning the horizontal window from minus two to plus two, with the bulk of eigenvalues filling the dome. A single tall vertical pointer marks the largest eigenvalue. Three stacked panels, top to bottom for increasing matrix size, show the pointer wobbling around the right edge for the small matrix, settling closer for the medium one, and locking onto the value two for the large one, where the dome density falls to zero. A dashed vertical line at two marks the limiting position of the largest eigenvalue.](/img/largest-eigenvalue-operator-norm-bound-placeholder.svg)

The dashed vertical line at two is the wall. The point of the picture is that the rightmost eigenvalue does not escape past this wall as the matrix grows: its random wandering is confined to a shrinking neighbourhood of the right edge of the semicircle, and the amount it pokes past two shrinks to nothing.

## Worked example [Beginner]

We watch how raising a matrix to a high power makes its largest eigenvalue stand out, using a tiny example with two known eigenvalues.

Step 1. Suppose a symmetric matrix has just two eigenvalues, $2$ and $1$. The largest is $2$. We want to recover the number $2$ from an averaged power without looking at the eigenvalues one at a time.

Step 2. The sum of the squares of the eigenvalues is $2^2 + 1^2 = 4 + 1 = 5$. Take the square root: about $2.24$. That overshoots the true largest value of $2$, because the smaller eigenvalue still contributes.

Step 3. Now use the sixth powers instead of the squares. The sum is $2^6 + 1^6 = 64 + 1 = 65$. Take the sixth root: about $2.01$. Much closer to $2$, because the bigger eigenvalue raised to the sixth power towers over the smaller one.

Step 4. Push to the twentieth powers. The sum is $2^{20} + 1^{20} = 1048576 + 1 = 1048577$. The twentieth root of that is about $2.0000007$. The smaller eigenvalue has been almost completely drowned out, and the averaged high power has handed us back the largest eigenvalue to high accuracy.

Step 5. What this tells us: a high enough power of a matrix is dominated by its largest eigenvalue, so an averaged high power gives a bound on that eigenvalue. For a random matrix you do not know the eigenvalues, but you can compute the average of a high power from the entries — and that is exactly the handle the moment method uses to fence the largest eigenvalue against the edge at two.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

As the rescaled random symmetric matrix grows large, its largest eigenvalue settles onto which value?

A. Zero  
B. One  
C. Two  
D. It grows without bound

<details class="exercise__hint"><summary>Hint</summary>

The largest eigenvalue presses up against the right edge of the semicircle window.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C. Two.** The largest eigenvalue converges to the right edge of the semicircle support, which sits at two. Feedback-correct: the top eigenvalue hugs the edge of the bulk and does not escape past it. Feedback-wrong: A and B sit inside the bulk, not at the edge; D would happen only without the rescaling that freezes the window.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

Raising a matrix to a higher power makes its largest eigenvalue dominate the averaged power more strongly.

<details class="exercise__hint"><summary>Hint</summary>

Recall the worked example: the twentieth power gave a far sharper estimate of two than the second power did.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** The bigger an eigenvalue, the faster it grows under powering, so high powers are dominated by the largest eigenvalue and the smaller ones fade from the average. This is why averaging a high power of the matrix and taking the matching root squeezes down toward the largest eigenvalue, the trick behind the operator-norm bound.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A symmetric matrix has eigenvalues $3$ and $1$. Compute the sum of their fourth powers, $3^4 + 1^4$.

<details class="exercise__hint"><summary>Hint</summary>

$3^4 = 81$ and $1^4 = 1$. Add them.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Eighty-two ($82$).** Since $3^4 = 81$ and $1^4 = 1$, the sum is $82$. Its fourth root is about $3.01$, already close to the largest eigenvalue $3$, showing how a modest power lets the biggest eigenvalue dominate an averaged power.

</details>

</aside>

## Formal definition [Intermediate+]

Let $M_n = n^{-1/2} A_n$ be a normalised Wigner matrix as in [37.08.01]: $A_n$ is $n\times n$ real symmetric (or Hermitian) with independent centred entries on and above the diagonal, off-diagonal variance $\mathbb{E}|a_{ij}|^2 = 1$, and finite higher moments where stated. Its eigenvalues $\lambda_1(M_n)\le\cdots\le\lambda_n(M_n)$ are real, and the **largest eigenvalue** is $\lambda_{\max}(M_n) = \lambda_n(M_n)$. Because $M_n$ is Hermitian, its **operator norm** (spectral norm) is the largest eigenvalue in absolute value,
$$
\|M_n\|_{\mathrm{op}} = \max_{\|x\|_2 = 1}\|M_n x\|_2 = \max_i |\lambda_i(M_n)| = \max\big(\lambda_{\max}(M_n),\, -\lambda_{\min}(M_n)\big).
$$
The symmetry of the entry distribution makes the spectrum symmetric in law, so $\lambda_{\max}$ and $-\lambda_{\min}$ have the same limit; the edge analysis below is stated for the right edge and applies verbatim to the left.

The **right edge** of the semicircle law $\mu_{\mathrm{sc}}$ is the right endpoint of its support $[-2,2]$. The semicircle is the limit of the empirical spectral distribution [37.08.01], a statement about the *bulk* histogram; it does not by itself control the single extreme eigenvalue, because moving one eigenvalue does not change the limiting empirical measure. The edge statement is therefore a genuinely sharper claim. Three forms appear, in increasing strength:

- **Operator-norm upper bound (in probability):** for every $\varepsilon > 0$, $\mathbb{P}\big(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon\big) \to 0$. No eigenvalue strays a fixed distance past the edge.
- **Bai-Yin convergence (almost sure):** if the entries have a finite fourth moment, $\lambda_{\max}(M_n) \to 2$ almost surely; the fourth-moment condition is necessary as well as sufficient.
- **Tracy-Widom fluctuation:** the centred and rescaled top eigenvalue $n^{2/3}\big(\lambda_{\max}(M_n) - 2\big)$ converges in distribution to the Tracy-Widom law $\mathrm{TW}_\beta$, where $\beta\in\{1,2\}$ is the symmetry class.

The first is the operator-norm bound proper; the second is its almost-sure sharpening under the optimal moment hypothesis; the third describes the fluctuations once the location is pinned. The **high-trace (moment) method** proves the upper bound by comparing $\lambda_{\max}^{2k}$ to the trace of the $2k$-th power, and the **$\varepsilon$-net** method proves a non-asymptotic version by discretising the unit sphere.

### Counterexamples to common slips [Intermediate+]

- *The semicircle law does not imply the edge.* Convergence of the empirical measure $\mu_n\Rightarrow\mu_{\mathrm{sc}}$ allows a vanishing fraction of eigenvalues — even a single one — to sit far outside $[-2,2]$ without affecting the limiting histogram. A separate argument is required to confine the extreme eigenvalue.
- *A fixed power does not reach the edge.* For each fixed $k$, $\mathbb{E}[\tfrac1n\mathrm{tr}\,M_n^{2k}]\to C_k$ and $(C_k)^{1/2k}\to 2$ only in the limit $k\to\infty$; one must let the exponent $k = k_n$ grow with $n$ to extract the value $2$ rather than a strict underestimate.
- *Finite fourth moment is exactly the threshold.* With infinitely many moments the upper bound is easy, but the sharp Bai-Yin result is that $\lambda_{\max}\to 2$ almost surely holds if and only if $\mathbb{E}[a_{ij}^4] < \infty$. If the fourth moment is infinite, the largest entry alone produces eigenvalues escaping to infinity, and $\limsup_n \lambda_{\max}/\sqrt{?} = \infty$.
- *Operator norm is not the bulk.* The norm reads off the extreme eigenvalue, not a typical one. A bound on $\tfrac1n\mathrm{tr}\,M_n^{2k}$ controls the *sum* of $2k$-th powers, hence the maximum, but the converse fails: knowing the bulk says nothing about the extreme.

## Key theorem with proof [Intermediate+]

**Theorem (operator-norm upper bound via the high-trace method).** *Let $(M_n)$ be normalised Wigner matrices with entries having finite moments of every order (the moment hypothesis of [37.08.01]). Then $\|M_n\|_{\mathrm{op}}$ converges to $2$ in probability from above: for every $\varepsilon > 0$,*
$$
\lim_{n\to\infty}\mathbb{P}\big(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon\big) = 0.
$$

*Proof.* The largest eigenvalue is bounded by any even trace power, because every eigenvalue contributes a non-negative term to an even trace:
$$
\lambda_{\max}(M_n)^{2k} \le \sum_{i=1}^n \lambda_i(M_n)^{2k} = \mathrm{tr}\,M_n^{2k},
$$
and the same bound holds for $|\lambda_{\min}|^{2k}$, hence $\|M_n\|_{\mathrm{op}}^{2k}\le\mathrm{tr}\,M_n^{2k}$. Taking expectations and applying Markov's inequality, for any threshold $t > 0$,
$$
\mathbb{P}\big(\|M_n\|_{\mathrm{op}} > t\big) = \mathbb{P}\big(\|M_n\|_{\mathrm{op}}^{2k} > t^{2k}\big) \le t^{-2k}\,\mathbb{E}\,\mathrm{tr}\,M_n^{2k} = t^{-2k}\, n\,\mathbb{E}\Big[\tfrac1n\mathrm{tr}\,M_n^{2k}\Big].
$$
The moment method of [37.08.01] computed the leading order of $\mathbb{E}[\tfrac1n\mathrm{tr}\,M_n^{2k}]$ for *fixed* $k$ as the Catalan number $C_k$. To reach the edge the exponent must be allowed to grow with $n$, and the combinatorics must be controlled uniformly in $k$. A refinement of the walk-counting argument gives the **non-asymptotic trace bound**: there is an absolute constant $C$ such that for all $n$ and all $k\le n^{1/2}$,
$$
\mathbb{E}\,\mathrm{tr}\,M_n^{2k} \le n\, C_k\, \big(1 + o(1)\big) \le n\, \frac{4^k}{k^{3/2}}\,C'
$$
for a constant $C'$, using the closed form $C_k = \tfrac{1}{k+1}\binom{2k}{k}\sim \tfrac{4^k}{\sqrt\pi\, k^{3/2}}$. The error terms collected from walks visiting fewer than $k+1$ vertices, or using an edge more than twice, are each suppressed by a factor $k^{O(1)}/n$ relative to the leading $C_k$, so as long as $k$ grows slower than a power of $n$ the leading Catalan term dominates.

Now choose the exponent to grow slowly: set $k = k_n = \lfloor (\log n)^2\rfloor$, so $k_n\to\infty$ while $k_n = o(n^{1/2})$. Fix $\varepsilon > 0$ and take $t = 2 + \varepsilon$. Then
$$
\mathbb{P}\big(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon\big) \le (2 + \varepsilon)^{-2k_n}\cdot n\,\frac{4^{k_n}}{k_n^{3/2}}\,C' = \frac{n\,C'}{k_n^{3/2}}\left(\frac{4}{(2+\varepsilon)^2}\right)^{k_n} = \frac{n\,C'}{k_n^{3/2}}\,\rho^{k_n},
$$
where $\rho = 4/(2+\varepsilon)^2 < 1$. With $k_n = (\log n)^2$, the factor $\rho^{k_n} = \exp\big(k_n\log\rho\big) = \exp\big(-c(\log n)^2\big)$ decays faster than any power of $n$, so $n\,\rho^{k_n}\to 0$. Therefore $\mathbb{P}(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon)\to 0$. The matching lower bound $\liminf_n\lambda_{\max}\ge 2 - \varepsilon$ follows from the bulk: since $\mu_n\Rightarrow\mu_{\mathrm{sc}}$ and $\mu_{\mathrm{sc}}$ puts positive mass on $(2 - \varepsilon, 2]$, with high probability at least one eigenvalue exceeds $2 - \varepsilon$, so $\lambda_{\max}\ge 2 - \varepsilon$. Combining, $\|M_n\|_{\mathrm{op}}\to 2$ in probability. $\square$

**Bridge.** This high-trace computation builds toward the Bai-Yin almost-sure theorem and the Tracy-Widom edge fluctuation, and it appears again in the $\varepsilon$-net route below, which reaches the same wall by discretising directions rather than by powering. The foundational reason the value is exactly $2$ is that the growth rate of the even traces is $C_k\sim 4^k k^{-3/2}$, whose exponential rate $4 = 2^2$ is the square of the edge: the moment method counts the same non-crossing tree walks as in the bulk, but letting the walk length $k_n\to\infty$ converts the $4^k$ growth constant into the location $2$ of the edge. This is exactly the slowly-growing-exponent device that turns a bulk moment computation into an edge bound, and it is dual to the resolvent analysis of [37.08.02], where the square-root branch point of $\sqrt{z^2 - 4}$ at $z = 2$ locates the same edge analytically. Putting these together, the bridge is that a single growth constant — the $4^k$ in the Catalan asymptotics — generalises into the edge location once the exponent is allowed to scale with the matrix size, and the central insight is that the extreme eigenvalue is controlled by trading the precision of a fixed moment for the reach of a growing one.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Use $C_k = \tfrac{1}{k+1}\binom{2k}{k}$ and the asymptotic $C_k\sim 4^k/(\sqrt\pi\, k^{3/2})$ to compute $\lim_{k\to\infty}(C_k)^{1/2k}$, and explain why this limit is the edge of the semicircle.

<details class="exercise__hint"><summary>Hint</summary>

$(C_k)^{1/2k} = (4^k)^{1/2k}\cdot(\text{subexponential})^{1/2k}$; the polynomial factor contributes $1$ in the limit.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$(C_k)^{1/2k} = \big(4^k k^{-3/2}\pi^{-1/2}(1 + o(1))\big)^{1/2k} = 4^{1/2}\cdot\big(k^{-3/2}\big)^{1/2k}\cdot(\cdots)^{1/2k} \to \sqrt 4 = 2$, since $\big(k^{-3/2}\big)^{1/2k} = \exp\big(-\tfrac{3}{4k}\log k\big)\to 1$. The $2k$-th moment of the semicircle grows like $4^k$, and the $2k$-th root of a $2k$-th moment converges to the essential supremum of $|x|$ on the support, namely $2$. This is the moment-method statement that the edge equals $\lim_k (\int x^{2k}d\mu)^{1/2k}$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

A symmetric matrix has eigenvalues $2, 1.5, 0.5$. Compute $(\mathrm{tr}\,M^{10})^{1/10} = (2^{10} + 1.5^{10} + 0.5^{10})^{1/10}$ and compare to $\lambda_{\max} = 2$.

<details class="exercise__hint"><summary>Hint</summary>

$2^{10} = 1024$, $1.5^{10}\approx 57.7$, $0.5^{10}\approx 0.001$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The sum is $1024 + 57.67 + 0.00098 \approx 1081.67$, and its tenth root is $\approx 2.0157$. This overshoots $\lambda_{\max} = 2$ by a little under one percent, because the second eigenvalue $1.5$ still contributes at the tenth power. A higher power would shrink the overshoot: this is the finite-$k$ overestimate that the limit $k\to\infty$ removes, and the reason the proof needs $k = k_n\to\infty$ rather than a fixed exponent.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Carry out the Markov-inequality step of the proof explicitly: starting from $\|M_n\|_{\mathrm{op}}^{2k}\le\mathrm{tr}\,M_n^{2k}$ and the trace bound $\mathbb{E}\,\mathrm{tr}\,M_n^{2k}\le n\,4^k$, find the largest $\varepsilon$-decay rate obtainable by optimising over $k$ for fixed $n$.

<details class="exercise__hint"><summary>Hint</summary>

Minimise $n\,(4/(2+\varepsilon)^2)^k$ over $k$; with $\rho < 1$ fixed it decreases in $k$, so the binding constraint is the largest admissible $k\approx n^{1/2}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

From $\mathbb{P}(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon)\le n\,\rho^k$ with $\rho = 4/(2+\varepsilon)^2 < 1$, the bound decreases monotonically in $k$, so one pushes $k$ as large as the trace estimate permits, $k\sim c\, n^{1/2}$. Then $n\,\rho^k = \exp\big(\log n - c\, n^{1/2}|\log\rho|\big)\to 0$ at a stretched-exponential rate $\exp(-c'\sqrt n)$. Choosing instead $k_n = (\log n)^2$ as in the proof gives the weaker but sufficient rate $\exp(-c(\log n)^2)$, which already beats every power of $n$; the gain from the larger $k$ is what feeds the Borel-Cantelli step in the almost-sure version, since $\exp(-c'\sqrt n)$ is summable. The optimisation shows the moment method delivers not just convergence in probability but a tail bound strong enough to upgrade to almost-sure convergence.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 4 (medium, short-answer).**

State the $\varepsilon$-net bound for the operator norm: if $\mathcal{N}$ is an $\varepsilon$-net of the unit sphere $S^{n-1}$ with $\varepsilon < 1$, then $\|M\|_{\mathrm{op}}\le(1 - 2\varepsilon)^{-1}\max_{x\in\mathcal N}|\langle x, M x\rangle|$ for Hermitian $M$. Prove it.

<details class="exercise__hint"><summary>Hint</summary>

For the maximiser $x^*$ of the quadratic form, pick a net point $y$ within $\varepsilon$ and expand $\langle x^*, Mx^*\rangle - \langle y, My\rangle$ using bilinearity and $\|M\| = |\langle x^*, Mx^*\rangle|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For Hermitian $M$, $\|M\|_{\mathrm{op}} = \max_{\|x\| = 1}|\langle x, Mx\rangle|$; let $x^*$ attain it. Choose $y\in\mathcal N$ with $\|x^* - y\|\le\varepsilon$. Writing $x^* = y + h$ with $\|h\|\le\varepsilon$,
$$
\langle x^*, Mx^*\rangle = \langle y, My\rangle + 2\operatorname{Re}\langle y, Mh\rangle + \langle h, Mh\rangle,
$$
and $|2\operatorname{Re}\langle y, Mh\rangle + \langle h, Mh\rangle|\le 2\|M\|\,\|y\|\,\|h\| + \|M\|\,\|h\|^2\le(2\varepsilon + \varepsilon^2)\|M\|\le 2\varepsilon\|M\|$ for the relevant regime. Hence $\|M\| = |\langle x^*, Mx^*\rangle|\le|\langle y, My\rangle| + 2\varepsilon\|M\|$, giving $\|M\|\le(1 - 2\varepsilon)^{-1}\max_{x\in\mathcal N}|\langle x, Mx\rangle|$. The use is that $S^{n-1}$ has an $\varepsilon$-net of size at most $(1 + 2/\varepsilon)^n$, so a union bound over $\mathcal N$ of a sub-gaussian tail for each fixed quadratic form $\langle x, Mx\rangle$ controls the norm at the cost of an $e^{Cn}$ factor that the tail must beat.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 5 (medium, short-answer).**

Use the $\varepsilon$-net bound to prove a non-asymptotic operator-norm estimate for an $n\times n$ symmetric matrix with independent sub-gaussian entries of variance $1/n$: with high probability $\|M_n\|_{\mathrm{op}}\le C$ for an absolute constant $C$.

<details class="exercise__hint"><summary>Hint</summary>

For fixed unit $x$, $\langle x, M_n x\rangle = \sum_{ij}(M_n)_{ij}x_i x_j$ is a sub-gaussian quadratic form with variance $O(1/n)\cdot\|x\|^4 = O(1/n)$ in the off-diagonal sum; use the Hanson-Wright tail and a union bound over a net of size $e^{Cn}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Fix a unit vector $x$. The quadratic form $\langle x, M_n x\rangle = \sum_{i\le j}c_{ij}(M_n)_{ij}$ (with $c_{ij}$ collecting the $x_ix_j$ weights) is a linear combination of independent sub-gaussian entries, hence sub-gaussian with parameter of order $\big(\sum_{ij}(x_ix_j)^2/n\big)^{1/2} = \|x\|^2/\sqrt n = 1/\sqrt n$; more carefully the Hanson-Wright inequality gives $\mathbb{P}(|\langle x, M_n x\rangle| > t)\le 2\exp(-cn\min(t^2, t))$ for $t = O(1)$. Take a $\tfrac14$-net $\mathcal N$ with $|\mathcal N|\le 9^n$. Union bound: $\mathbb{P}(\max_{x\in\mathcal N}|\langle x, M_nx\rangle| > t)\le 9^n\cdot 2e^{-cnt^2}$, which is $\le e^{-cn}$ once $t = C$ is large enough that $cC^2 > \log 9 + c$. On that event the net bound gives $\|M_n\|_{\mathrm{op}}\le 2\max_{x\in\mathcal N}|\langle x, M_nx\rangle|\le 2C$. This proves $\|M_n\|_{\mathrm{op}} = O(1)$ with probability $1 - e^{-cn}$ — a non-asymptotic bound, weaker in constant than the sharp edge value $2$ but valid for every $n$ and with an exponentially small failure probability the moment method does not directly provide.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 6 (hard, short-answer).**

Prove the matching **lower bound** $\liminf_n\lambda_{\max}(M_n)\ge 2$ almost surely cannot be improved, and show $\lambda_{\max}\ge 2 - \varepsilon$ eventually, using the bulk semicircle law. Then explain where the finite fourth moment enters the Bai-Yin upper bound.

<details class="exercise__hint"><summary>Hint</summary>

For the lower bound, count eigenvalues in $(2 - \varepsilon, 2]$ via $\mu_n\Rightarrow\mu_{\mathrm{sc}}$. For the fourth moment, consider the largest single entry $\max_{ij}|a_{ij}|/\sqrt n$ as a crude lower bound on the norm.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Lower bound.** Since $\mu_n\Rightarrow\mu_{\mathrm{sc}}$ almost surely [37.08.01] and $\mu_{\mathrm{sc}}\big((2 - \varepsilon, 2]\big) = \delta_\varepsilon > 0$, the portmanteau theorem gives $\liminf_n\mu_n\big((2 - \varepsilon, 2)\big)\ge\delta_\varepsilon > 0$, so for large $n$ the count $n\,\mu_n((2-\varepsilon, 2))\ge \tfrac12 n\delta_\varepsilon\ge 1$ eigenvalues lie above $2 - \varepsilon$; hence $\lambda_{\max}(M_n)\ge 2 - \varepsilon$ eventually, for every $\varepsilon$, giving $\liminf\lambda_{\max}\ge 2$ a.s. Combined with the high-trace upper bound $\limsup\lambda_{\max}\le 2$, the limit is exactly $2$. The value cannot be improved because the bulk genuinely reaches the edge: the semicircle has support up to $2$, not less. **Fourth moment.** The operator norm dominates the largest entry: $\|M_n\|_{\mathrm{op}}\ge\max_i|(M_n)_{ii}|$ and, via $\langle e_i, M_n e_j\rangle$ test vectors, also $\gtrsim\max_{i\ne j}|a_{ij}|/\sqrt n$. If $\mathbb{E}[a_{ij}^4] = \infty$, then $\max_{ij}|a_{ij}|$ over the $\sim n^2/2$ entries grows faster than $\sqrt n$ — by Borel-Cantelli the event $\{|a_{ij}| > \delta\sqrt n\}$ occurs infinitely often when $\sum_n n^2\,\mathbb{P}(|a| > \delta\sqrt n) = \infty$, which fails exactly when the fourth moment is infinite — so $\limsup\lambda_{\max} = \infty$ and convergence to $2$ breaks. Finiteness of the fourth moment is precisely the borderline that keeps $\max_{ij}|a_{ij}| = o(\sqrt n)$ a.s., which is what the Bai-Yin truncation argument needs to push the high-trace bound to almost-sure convergence.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

State the Tracy-Widom edge theorem precisely and explain, via the square-root vanishing of the semicircle density at the edge, why the fluctuation scale is $n^{2/3}$ rather than the bulk scale $n^{-1}$.

<details class="exercise__hint"><summary>Hint</summary>

The number of eigenvalues within distance $\delta$ of the edge is $n\int_{2-\delta}^2\rho_{\mathrm{sc}}\sim n\,\delta^{3/2}$; set this to order one.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Statement.** For a Wigner matrix in symmetry class $\beta\in\{1,2\}$ (real symmetric or complex Hermitian) with entries matching the Gaussian moments to fourth order, $n^{2/3}\big(\lambda_{\max}(M_n) - 2\big)\Rightarrow\mathrm{TW}_\beta$, where $\mathrm{TW}_\beta$ is the Tracy-Widom distribution, expressible for $\beta = 2$ through the Hastings-McLeod solution $q$ of Painlevé II ($q'' = sq + 2q^3$) via $F_2(s) = \exp\big(-\int_s^\infty(x - s)q(x)^2dx\big)$. **Scaling heuristic.** Near the edge $\rho_{\mathrm{sc}}(x)\sim\tfrac1\pi\sqrt{2 - x}$, so the expected number of eigenvalues in $[2 - \delta, 2]$ is $n\int_{2-\delta}^2\tfrac1\pi\sqrt{2 - x}\,dx = n\cdot\tfrac{2}{3\pi}\delta^{3/2}$. The top eigenvalue lives at the scale $\delta$ where this count is of order one, i.e. $n\,\delta^{3/2}\sim 1$, giving $\delta\sim n^{-2/3}$. The mean eigenvalue spacing at the edge is therefore $n^{-2/3}$, far coarser than the bulk spacing $n^{-1}$ where the density is order one; the square-root softening of the density spreads the top eigenvalues out, producing the $n^{2/3}$ window and the non-Gaussian Tracy-Widom law in place of the Gaussian fluctuations seen for sums of independent scalars. This is the same edge that the operator-norm bound locates at $2$ and the resolvent [37.08.02] locates through the branch point of $\sqrt{z^2 - 4}$.

</details>

</aside>

## Advanced results [Master]

The almost-sure operator-norm convergence under the optimal moment hypothesis is the **Bai-Yin theorem** [ref: TODO_REF Bai 1988]: for a Wigner matrix with i.i.d. centred off-diagonal entries of unit variance, $\lambda_{\max}(M_n)\to 2$ almost surely if and only if $\mathbb{E}[a_{ij}^4] < \infty$ (and the corresponding diagonal moment is finite). The proof truncates each entry at level $\delta_n\sqrt n$ with $\delta_n\downarrow 0$; the finite fourth moment makes the truncation error negligible in operator norm and ensures $\max_{ij}|a_{ij}| = o(\sqrt n)$ almost surely, after which the high-trace bound with $k = k_n\sim\log n$ produces a tail $\mathbb{P}(\lambda_{\max} > 2 + \varepsilon)\le n^{-A}$ summable in $n$, so Borel-Cantelli gives the almost-sure upper bound; the lower bound is the bulk argument. The necessity is sharp: if the fourth moment diverges, $\max_{ij}|a_{ij}|/\sqrt n$ does not vanish and the single largest entry forces $\limsup\lambda_{\max} = \infty$, so no finite limit exists.

The **non-asymptotic theory** gives bounds valid at every finite $n$ rather than only in the limit. For an $n\times n$ symmetric matrix with independent sub-gaussian entries of variance $1/n$, the $\varepsilon$-net argument [ref: TODO_REF Vershynin 2018] yields $\mathbb{P}\big(\|M_n\|_{\mathrm{op}} > C\big)\le 2e^{-cn}$ for absolute constants $C, c$: cover $S^{n-1}$ by a net of cardinality $e^{Cn}$, apply a sub-gaussian (Hanson-Wright) tail to each fixed quadratic form $\langle x, M_n x\rangle$, and union-bound. The constant $C$ this delivers is not sharp — it exceeds the true edge $2$ — but the bound is uniform in $n$ and carries an exponentially small failure probability, which the moment method does not provide directly. The two methods are complementary: the high-trace method gives the sharp constant $2$ but only a polynomial tail at fixed $k$, while the net gives the sharp $e^{-cn}$ tail but a loose constant. Sharpening the net constant to $2$ requires a chaining (generic-chaining / Dudley) refinement that replaces the single-scale net by a hierarchy of nets.

The **edge fluctuations** are governed by the Tracy-Widom law $\mathrm{TW}_\beta$ [ref: TODO_REF Tracy 1994], a distribution with no elementary closed form, defined through the Fredholm determinant of the Airy kernel $K_{\mathrm{Ai}}(x, y) = \frac{\mathrm{Ai}(x)\mathrm{Ai}'(y) - \mathrm{Ai}'(x)\mathrm{Ai}(y)}{x - y}$ on $[s,\infty)$, equivalently through Painlevé II. The left tail of $\mathrm{TW}_\beta$ decays like $\exp(-c|s|^3)$ and the right tail like $\exp(-c\,s^{3/2})$, the asymmetry reflecting that pushing the top eigenvalue inward against the bulk's repulsion is harder than pulling it outward into empty spectrum. The same distribution governs the largest eigenvalue of the Gaussian ensembles exactly, and **edge universality**, proved by the Erdős-Schlein-Yau program and by Tao-Vu, extends it to all Wigner matrices whose entries match the Gaussian moments through fourth order. The $n^{2/3}$ rescaling and the Airy kernel both descend from the $(2 - x)^{1/2}$ softening of the semicircle at the edge: a soft edge of a density vanishing like a square root is exactly the universality class of the Airy point process.

The **operator norm of non-Wigner ensembles** follows the same architecture with a shifted edge. For sample-covariance (Wishart) matrices $S_n = \tfrac1n XX^*$ with aspect ratio $\gamma$, the largest eigenvalue converges to the right edge $(1 + \sqrt\gamma)^2$ of the Marchenko-Pastur law and fluctuates, after $n^{2/3}$ rescaling, according to Tracy-Widom; the high-trace and net methods both transfer, with the trace growth constant set by the Marchenko-Pastur edge rather than by $4$. The principle is that the operator norm is the exponential growth rate of the even traces, and that growth rate is the square of the right edge of whatever limiting spectral distribution the ensemble has.

**Synthesis.** The foundational reason the operator norm converges to exactly $2$ is that $2$ is the exponential growth rate of the $2k$-th moments of the semicircle — the $4^k$ in the Catalan asymptotics is $(2^2)^k$ — and the high-trace method extracts that rate by letting the exponent $k = k_n\to\infty$ slowly, so the bulk moment computation of [37.08.01] becomes an edge statement with no new combinatorics. Putting these together, the moment route, the $\varepsilon$-net route, and the resolvent route of [37.08.02] are three roads to the same edge: the growth constant $4^k$ of the traces, the covering-number-versus-sub-gaussian-tail balance on the sphere, and the square-root branch point of $\sqrt{z^2 - 4}$ at $z = 2$ are the same edge seen three ways, and this is exactly the duality that the operator-norm bound, the net bound, and the analytic edge analysis share. The central insight is that the extreme eigenvalue is an exponential-growth-rate functional, so it is governed by the tail of the moment sequence and by the right endpoint of the support, not by the bulk; this generalises to every ensemble whose limiting law has a hard or soft edge, with the soft square-root edge carrying the universal $n^{2/3}$ Tracy-Widom fluctuation. The bridge to the frontier is that edge universality is the operator-norm bound made quantitative down to the fluctuation scale, the same selection principle that pinned the location now pinning the law, and this is dual to the bulk universality the local semicircle law makes quantitative.

## Full proof set [Master]

The high-trace upper bound, the slowly-growing-exponent device, and the matching lower bound are proved in full above. The remaining Master claims are recorded here.

**Proposition (operator norm equals the largest even-trace growth rate).** *For any Hermitian matrix $M$, $\|M\|_{\mathrm{op}} = \lim_{k\to\infty}\big(\mathrm{tr}\,M^{2k}\big)^{1/2k}$.*

*Proof.* Let $r = \|M\|_{\mathrm{op}} = \max_i|\lambda_i|$. Each term of $\mathrm{tr}\,M^{2k} = \sum_i\lambda_i^{2k}$ is non-negative, so $r^{2k}\le\mathrm{tr}\,M^{2k}\le n\, r^{2k}$, the lower bound from the single largest term and the upper from bounding all $n$ terms by the largest. Taking $2k$-th roots, $r\le\big(\mathrm{tr}\,M^{2k}\big)^{1/2k}\le n^{1/2k}\, r$, and $n^{1/2k}\to 1$ as $k\to\infty$, so the limit is $r$. $\square$

**Proposition (covering number of the sphere).** *For $\varepsilon\in(0,1)$ the unit sphere $S^{n-1}\subset\mathbb{R}^n$ admits an $\varepsilon$-net $\mathcal N$ with $|\mathcal N|\le(1 + 2/\varepsilon)^n$.*

*Proof.* Take a maximal $\varepsilon$-separated subset $\mathcal N\subseteq S^{n-1}$ (it exists by Zorn or by greedy selection since the sphere is compact). Maximality forces $\mathcal N$ to be an $\varepsilon$-net: any sphere point within $\varepsilon$ of no net point could be added, contradicting maximality. The open balls of radius $\varepsilon/2$ about the points of $\mathcal N$ are disjoint and contained in the shell $\{x : 1 - \varepsilon/2\le\|x\|\le 1 + \varepsilon/2\}$, which sits inside the ball of radius $1 + \varepsilon/2$. Comparing volumes, $|\mathcal N|\,(\varepsilon/2)^n\le(1 + \varepsilon/2)^n$, so $|\mathcal N|\le(1 + 2/\varepsilon)^n$. $\square$

**Proposition (net comparison for the operator norm).** *Let $M$ be Hermitian and $\mathcal N$ an $\varepsilon$-net of $S^{n-1}$ with $\varepsilon < 1/2$. Then $\max_{x\in\mathcal N}|\langle x, Mx\rangle|\le\|M\|_{\mathrm{op}}\le(1 - 2\varepsilon)^{-1}\max_{x\in\mathcal N}|\langle x, Mx\rangle|$.*

*Proof.* The left inequality is immediate since each $|\langle x, Mx\rangle|\le\|M\|_{\mathrm{op}}$. For the right, let $x^*$ with $\|x^*\| = 1$ attain $\|M\|_{\mathrm{op}} = |\langle x^*, Mx^*\rangle|$ (a maximiser exists by compactness and the variational characterisation of the spectral norm for Hermitian $M$). Pick $y\in\mathcal N$ with $\|x^* - y\| = \|h\|\le\varepsilon$. By sesquilinearity,
$$
\langle x^*, Mx^*\rangle - \langle y, My\rangle = \langle x^*, Mx^*\rangle - \langle x^* - h, M(x^* - h)\rangle = 2\operatorname{Re}\langle x^*, Mh\rangle - \langle h, Mh\rangle,
$$
so $|\langle x^*, Mx^*\rangle - \langle y, My\rangle|\le 2\|M\|\,\|x^*\|\,\|h\| + \|M\|\,\|h\|^2\le(2\varepsilon + \varepsilon^2)\|M\|\le 2\varepsilon\|M\|$ for $\varepsilon < 1$ after absorbing the $\varepsilon^2$ term. Hence $\|M\| = |\langle x^*, Mx^*\rangle|\le|\langle y, My\rangle| + 2\varepsilon\|M\|\le\max_{x\in\mathcal N}|\langle x, Mx\rangle| + 2\varepsilon\|M\|$, and rearranging gives the claim. $\square$

**Proposition (high-trace tail with growing exponent).** *Under the moment hypothesis, with the trace bound $\mathbb{E}\,\mathrm{tr}\,M_n^{2k}\le n\,4^k$ valid uniformly for $k\le c\sqrt n$, the choice $k_n = \lfloor(\log n)^2\rfloor$ gives $\mathbb{P}(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon)\le\exp(-c_\varepsilon(\log n)^2)$ for all large $n$, which is summable in $n$.*

*Proof.* By the Markov step in the Key theorem, $\mathbb{P}(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon)\le(2 + \varepsilon)^{-2k_n}n\,4^{k_n} = n\,\rho^{k_n}$ with $\rho = 4/(2 + \varepsilon)^2 < 1$. With $k_n = (\log n)^2$, $n\,\rho^{k_n} = \exp\big(\log n + (\log n)^2\log\rho\big) = \exp\big(-|\log\rho|(\log n)^2 + \log n\big)\le\exp\big(-\tfrac12|\log\rho|(\log n)^2\big)$ for $n$ large enough that $(\log n)^2|\log\rho|/2 > \log n$. Setting $c_\varepsilon = |\log\rho|/2$ gives the stated bound. Since $\sum_n\exp(-c_\varepsilon(\log n)^2) < \infty$ (the terms decay faster than $n^{-2}$ for large $n$), Borel-Cantelli yields $\|M_n\|_{\mathrm{op}}\le 2 + \varepsilon$ eventually almost surely; intersecting over a sequence $\varepsilon\downarrow 0$ gives $\limsup_n\|M_n\|_{\mathrm{op}}\le 2$ a.s. under the full moment hypothesis. $\square$

**Proposition (edge count and the $n^{2/3}$ scale).** *The expected number of eigenvalues of $M_n$ in $[2 - \delta, 2]$ is asymptotic to $\tfrac{2}{3\pi}n\,\delta^{3/2}$, so the natural edge fluctuation scale solving $n\,\delta^{3/2}\asymp 1$ is $\delta\asymp n^{-2/3}$.*

*Proof.* Using the semicircle density $\rho_{\mathrm{sc}}(x) = \tfrac{1}{2\pi}\sqrt{4 - x^2}$ and the edge expansion $4 - x^2 = (2 - x)(2 + x)\sim 4(2 - x)$ as $x\uparrow 2$, the expected proportion of eigenvalues in $[2 - \delta, 2]$ is $\int_{2-\delta}^2\rho_{\mathrm{sc}}(x)dx\sim\int_{2-\delta}^2\tfrac{1}{2\pi}\cdot 2\sqrt{2 - x}\,dx = \tfrac1\pi\int_0^\delta\sqrt u\, du = \tfrac{2}{3\pi}\delta^{3/2}$. Multiplying by $n$ gives the count $\tfrac{2}{3\pi}n\,\delta^{3/2}$. The top eigenvalue is resolved at the scale $\delta$ where this count is of order one, namely $\delta\asymp n^{-2/3}$, which is the width of the Tracy-Widom window. $\square$

## Connections [Master]

The Wigner semicircle law and the moment method [37.08.01] supplies both the trace-power machinery and the matching lower bound. The high-trace upper bound reuses the closed-walk expansion of $\mathbb{E}\,\mathrm{tr}\,M_n^{2k}$ developed there, now with the exponent $k = k_n\to\infty$ instead of fixed, and the lower bound $\lambda_{\max}\ge 2 - \varepsilon$ is read directly off the convergence $\mu_n\Rightarrow\mu_{\mathrm{sc}}$ proved in that unit; the operator-norm statement is the edge refinement of which the semicircle law is the bulk statement.

The Stieltjes transform and the semicircle law via the resolvent [37.08.02] is the analytic dual of the edge analysis here. The branch point of $s_{\mathrm{sc}}(z) = \tfrac12(-z + \sqrt{z^2 - 4})$ at $z = 2$ is the same spectral edge the operator-norm bound locates combinatorially, and the resolvent method, refined to imaginary parts of order $n^{-2/3}$, is what makes the Tracy-Widom edge fluctuation quantitative where the moment method only pins the location.

The strong law of large numbers and Borel-Cantelli [37.02.02] is the engine of the almost-sure Bai-Yin upgrade. The summable tail $\mathbb{P}(\|M_n\|_{\mathrm{op}} > 2 + \varepsilon)\le\exp(-c(\log n)^2)$ produced by the growing-exponent high-trace bound is exactly the input Borel-Cantelli needs to convert convergence in probability into almost-sure convergence of the largest eigenvalue.

The spectral concentration via log-Sobolev and the Herbst argument [37.08.07] underlies the $\varepsilon$-net route and its sharpening. The Hanson-Wright quadratic-form tail bounding each $\langle x, M_n x\rangle$ and the union bound over an exponentially large net are concentration statements, and the functional-inequality machinery of that unit gives the dimension-free Gaussian concentration of the operator norm around its mean; the net method trades the sharp constant of the moment method for the exponentially small failure probability that concentration provides.

## Historical & philosophical context [Master]

The high-trace method for the operator norm was introduced by Zoltán Füredi and János Komlós in 1981 [ref: TODO_REF Füredi 1981], who refined Wigner's moment computation by letting the trace exponent grow with the matrix dimension and tracking the walk combinatorics uniformly, obtaining $\|M_n\|_{\mathrm{op}} = 2 + o(1)$ with quantitative error terms for matrices with bounded entries. The sharp almost-sure result under the minimal hypothesis is due to Zhidong Bai and Yong-Qua Yin in 1988 [ref: TODO_REF Bai 1988], who proved that a finite fourth moment of the entries is both necessary and sufficient for $\lambda_{\max}(M_n)\to 2$ almost surely, identifying the exact integrability threshold and closing the question of when the largest eigenvalue is asymptotically governed by the bulk edge rather than by a single anomalous entry.

The edge fluctuation law was found by Craig Tracy and Harold Widom in 1994 [ref: TODO_REF Tracy 1994] for the Gaussian ensembles, who expressed the limiting distribution of $n^{2/3}(\lambda_{\max} - 2)$ through the Airy kernel and a Painlevé II transcendent; the same distribution was subsequently identified governing the longest increasing subsequence of a random permutation by Baik, Deift and Johansson, and a wide family of models in the Kardar-Parisi-Zhang universality class. The $\varepsilon$-net approach to non-asymptotic matrix norms, by contrast, grew out of geometric functional analysis and the local theory of Banach spaces, and was systematised for high-dimensional probability and data science by Roman Vershynin [ref: TODO_REF Vershynin 2018]; edge universality, extending the Tracy-Widom law from the Gaussian ensembles to general Wigner matrices, was established by the Erdős-Schlein-Yau local-law program and by Tao and Vu in the late 2000s.

## Bibliography [Master]

```bibtex
@article{furedikomlos1981,
  author  = {F\"uredi, Zolt\'an and Koml\'os, J\'anos},
  title   = {The eigenvalues of random symmetric matrices},
  journal = {Combinatorica},
  volume  = {1},
  number  = {3},
  pages   = {233--241},
  year    = {1981}
}

@article{baiyin1988,
  author  = {Bai, Zhidong and Yin, Yong-Qua},
  title   = {Necessary and sufficient conditions for almost sure convergence of the largest eigenvalue of a {Wigner} matrix},
  journal = {Annals of Probability},
  volume  = {16},
  number  = {4},
  pages   = {1729--1741},
  year    = {1988}
}

@book{agz2010,
  author    = {Anderson, Greg W. and Guionnet, Alice and Zeitouni, Ofer},
  title     = {An Introduction to Random Matrices},
  series    = {Cambridge Studies in Advanced Mathematics},
  volume    = {118},
  publisher = {Cambridge University Press},
  year      = {2010}
}

@book{vershynin2018,
  author    = {Vershynin, Roman},
  title     = {High-Dimensional Probability: An Introduction with Applications in Data Science},
  series    = {Cambridge Series in Statistical and Probabilistic Mathematics},
  publisher = {Cambridge University Press},
  year      = {2018}
}

@article{tracywidom1994,
  author  = {Tracy, Craig A. and Widom, Harold},
  title   = {Level-spacing distributions and the Airy kernel},
  journal = {Communications in Mathematical Physics},
  volume  = {159},
  number  = {1},
  pages   = {151--174},
  year    = {1994}
}

@book{tao2012,
  author    = {Tao, Terence},
  title     = {Topics in Random Matrix Theory},
  series    = {Graduate Studies in Mathematics},
  volume    = {132},
  publisher = {American Mathematical Society},
  year      = {2012}
}
```
