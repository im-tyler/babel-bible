---
id: 09.02.03
title: "Constrained Lagrangian Systems: Lagrange Multipliers and D'Alembert's Principle"
slug: constrained-lagrangian-lagrange-multipliers-dalembert
section: classical-mech
chapter: lagrangian
concept_catalog_id: classical-mech.constrained-lagrangian-lagrange-multipliers-dalembert
prerequisites:
  - 09.02.01  # action principle
  - 09.02.02  # Euler-Lagrange equations
pending_prereqs: true
successors:
  - 09.02.04  # small oscillations
  - 09.04.01  # Legendre transform
tier_anchors:
  beginner: "Taylor, *Classical Mechanics* (2005), Ch. 7.1-7.3"
  intermediate: "Taylor, *Classical Mechanics* (2005), Ch. 7; Goldstein, Poole & Safko, *Classical Mechanics* 3e, Ch. 2.4"
  master: "Goldstein, Poole & Safko, *Classical Mechanics* 3e, Ch. 2; Arnold, *Mathematical Methods of Classical Mechanics*, 2nd ed. (1989), Section 3"
tiers_present: [beginner, intermediate, master]
references:
  - source: TODO_REF
    path: "Taylor — Classical Mechanics (University Science Books, 2005)"
    locator: "Ch. 7 Lagrange's Equations. Constraint forces and Lagrange multipliers. The method of Lagrange multipliers for holonomic constraints: append lambda_alpha partial f_alpha/partial q^i to the Euler-Lagrange equations and determine the lambda_alpha simultaneously with the motion. Worked examples: particle on a constraint surface, bead on a wire hoop."
  - source: TODO_REF
    path: "Goldstein, Poole & Safko — Classical Mechanics, 3rd ed. (Pearson, 2002)"
    locator: "Ch. 2 Variational Principles and Lagrange's Equations. Section 2.4 Forces of constraint and Lagrange multipliers. The extended Euler-Lagrange equations. Physical interpretation of the multipliers as constraint forces. D'Alembert's principle and the principle of least action."
  - source: TODO_REF
    path: "Landau & Lifshitz — Mechanics, 3rd ed. (Course of Theoretical Physics Vol. 1, Pergamon, 1976)"
    locator: "Section 1 Generalised coordinates; Section 25 Holonomic and non-holonomic constraints in the Lagrangian framework. Lagrange multipliers for holonomic constraints."
  - source: TODO_REF
    path: "Arnold — Mathematical Methods of Classical Mechanics, 2nd ed. (Springer GTM 60, 1989)"
    locator: "Section 3 Equations of motion. Constraint manifolds as submanifolds of the configuration space. Lagrange multipliers as the normal components of the force. Geometric interpretation on the tangent bundle."
lean_status: none
lean_mathlib_gap: "Mathlib has the infrastructure for smooth manifolds, tangent bundles, submanifolds, and the Euler-Lagrange equations for unconstrained systems. It does not yet contain: the Lagrange multiplier method for constrained Lagrangian systems as a formal theorem; the identification of multipliers with constraint forces; the extended Euler-Lagrange equations incorporating constraint terms; the principle of virtual work in a variational framework on manifolds; or the Lagrange-d'Alembert equations for non-holonomic constraints. Formalisation would require assembling the submanifold geometry of constraint surfaces with the variational calculus on the tangent bundle, extending Mathlib's existing Euler-Lagrange machinery to the constrained setting."
human_reviewer: "Tyler (pending external classical-mechanics reviewer per PHYSICS_PLAN)"
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 65m
status: shipped
produced_by: claude-glm-5.1-runbook-Wave-2
reviewed_by: []
source_books: [taylor-2005, goldstein-2002]
source_curriculum: classical-mech
hooks_out:
  - target: 09.04.01
    kind: proposed
    why: "The Legendre transform from Lagrangian to Hamiltonian mechanics must handle constrained systems; the Lagrange multiplier method developed here produces constraint forces that carry over to the Hamiltonian side as secondary constraints in the Dirac-Bergmann classification, which the Hamiltonian unit on constrained systems references directly."
  - target: 09.02.06
    kind: proposed
    why: "Field Lagrangians inherit the Lagrange multiplier method for imposing constraints on the field configuration; this unit's treatment of finite-dimensional holonomic constraints is the direct finite-dimensional prototype for the constraint equations in continuum elasticity and gauge field theories."
  - target: 09.03.01
    kind: proposed
    why: "Noether's theorem applies to constrained systems with the modification that only symmetries preserving the constraint surface contribute conserved quantities; the constraint-compatible symmetry analysis requires the Lagrange multiplier formalism developed here to identify which symmetries of the full Lagrangian descend to the constrained dynamics."
see_also:
  - classical-mech.09.02.01
  - classical-mech.09.02.02
  - classical-mech.09.04.01
  - classical-mech.09.01.06
---

## Intuition [Beginner]

Imagine a bead threaded on a stiff wire bent into a hoop. The bead can slide freely along the wire but cannot leave it. The wire is a **constraint** -- it restricts the bead's possible positions to a one-dimensional curve embedded in two- or three-dimensional space.

An unconstrained particle in three dimensions has three independent coordinates $(x, y, z)$ and three degrees of freedom (DOF). A bead on a wire has only one degree of freedom -- the distance along the wire, or equivalently an angle $\theta$ that parameterises position. The constraint has removed two degrees of freedom.

A simple pendulum is another constrained system. The bob can swing in a plane but cannot move farther from the pivot than the rod length $\ell$. Without the rod, the bob would have two degrees of freedom $(x, y)$ in the plane. The rigid rod imposes the constraint $x^2 + y^2 = \ell^2$, reducing the system to one degree of freedom -- the angle $\theta$.

In the Lagrangian framework [09.02.02], the standard approach is to choose coordinates that automatically satisfy the constraints. For the pendulum, you write the Lagrangian in terms of $\theta$ alone: $L = \tfrac{1}{2}m\ell^2\dot\theta^2 + mg\ell\cos\theta$. The constraint is built into the coordinate choice and never appears explicitly.

But sometimes you need the **constraint force** -- the tension in the pendulum rod, or the normal force the wire exerts on the bead. Sometimes the constraint is complicated enough that eliminating it by a clever coordinate choice is impractical. This is where **Lagrange multipliers** enter.

The idea: keep all the original coordinates, add a new unknown variable (the multiplier $\lambda$) for each constraint, and solve the extended equations of motion. The multiplier appears exactly where the constraint force would appear in Newton's second law. Solving gives you both the trajectory and the constraint force simultaneously.

The multiplier has a direct physical meaning. For a constraint $f(q) = 0$, the constraint force points perpendicular to the constraint surface, in the direction of steepest change of $f$. The multiplier $\lambda$ scales this gradient to produce the correct magnitude. It adjusts dynamically so the trajectory never leaves the constraint surface.

## Visual [Beginner]

*Figure: A bead of mass $m$ on a circular wire of radius $R$ in a vertical plane. The bead's position is parameterised by the angle $\theta$ measured from the bottom. Gravity $mg$ acts downward. The constraint force $\mathbf{N}$ acts radially (perpendicular to the wire, toward the centre). At the bottom, $\mathbf{N}$ points upward, opposing gravity and providing centripetal acceleration. At the top, $\mathbf{N}$ points downward, and the bead falls off if gravity exceeds the required centripetal force.*

![Bead on a circular wire hoop in a vertical plane. The bead is parameterised by angle theta from the bottom. Forces shown: gravity mg downward, constraint force N directed radially toward the centre. The Lagrange multiplier lambda scales the constraint force to the correct magnitude.](/img/09.02.03-bead-on-hoop-placeholder.svg)

The constraint force is always perpendicular to the wire (normal to the constraint surface). The tangential component of gravity alone determines the acceleration along the wire.

## Worked example [Beginner]

### The bead on a hoop

A bead of mass $m$ slides on a vertical circular wire of radius $R$. Find the equation of motion and the constraint force.

**Using the built-in coordinate.** Parameterise by the angle $\theta$ from the bottom. Position: $(R\sin\theta,\, -R\cos\theta)$. The Lagrangian is

$$
L = \tfrac{1}{2}mR^2\dot\theta^2 + mgR\cos\theta.
$$

The Euler-Lagrange equation gives $mR^2\ddot\theta = -mgR\sin\theta$, or

$$
\ddot\theta = -\frac{g}{R}\sin\theta.
$$

This is a simple pendulum equation. But it tells you nothing about the normal force.

**Using Lagrange multipliers to find the constraint force.** Work in Cartesian coordinates with the constraint $f(x,y) = x^2 + y^2 - R^2 = 0$. The Lagrangian is $L = \tfrac{1}{2}m(\dot x^2 + \dot y^2) - mgy$. The extended equations of motion are

$$
m\ddot x = 2\lambda x, \qquad m\ddot y = -mg + 2\lambda y,
$$

together with $x^2 + y^2 = R^2$.

At the bottom of the hoop ($\theta = 0$, so $x = 0$, $y = -R$): the constraint force is purely radial (vertical). Using the centripetal condition $m\ddot y = mv^2/R$ (with $v$ the speed along the hoop), the $y$-equation gives $mv^2/R = -mg + 2\lambda(-R)$, so $2\lambda R = -mg - mv^2/R$. The magnitude of the normal force is $|N| = |2\lambda R| = mg + mv^2/R$. At rest ($v = 0$), the wire supports the full weight: $N = mg$.

At the top ($\theta = \pi$, $x = 0$, $y = R$): $mv^2/R = mg + 2\lambda R$ gives $N = mv^2/R - mg$. The bead maintains contact only if $v^2 \geq gR$ -- the centripetal requirement exceeds gravity.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A particle in three dimensions is constrained to move on the surface of a sphere of radius $R$. How many degrees of freedom does it have?

A. 1

B. 2

C. 3

D. 4

<details class="exercise__answer"><summary>Answer</summary>

**2** -- option B.

The sphere imposes one constraint ($x^2 + y^2 + z^2 = R^2$) on three coordinates, leaving two degrees of freedom. These can be taken as the spherical angles $(\theta, \phi)$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true/false).**

When using Lagrange multipliers for a constrained system, the number of equations equals the original number of coordinates minus the number of constraints.

<details class="exercise__answer"><summary>Answer</summary>

**False.** You get one Euler-Lagrange equation per coordinate plus one constraint equation per multiplier. For a bead in 2D on a circle: 2 Euler-Lagrange equations plus 1 constraint = 3 equations for 3 unknowns ($x$, $y$, $\lambda$).

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

The Lagrange multiplier $\lambda$ in a constrained mechanical system is proportional to:

A. The velocity of the particle

B. The constraint force

C. The kinetic energy

D. The potential energy

<details class="exercise__answer"><summary>Answer</summary>

**The constraint force** -- option B.

The multiplier scales the gradient of the constraint function. The constraint force is proportional to the multiplier $\lambda$ and points perpendicular to the constraint surface, and $\lambda$ determines its magnitude.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true/false).**

A simple pendulum (mass $m$, length $\ell$) has two degrees of freedom before the constraint is imposed.

<details class="exercise__answer"><summary>Answer</summary>

**True.** The bob in a vertical plane has two Cartesian coordinates $(x, y)$. The rod imposes $x^2 + y^2 = \ell^2$, reducing the system to one degree of freedom (the angle $\theta$).

</details>

</aside>

## Formal definition [Intermediate+]

### Holonomic constraints

A **holonomic constraint** on a mechanical system with configuration coordinates $q^1, \ldots, q^n$ is an equation of the form

$$
f_\alpha(q^1, \ldots, q^n, t) = 0, \qquad \alpha = 1, \ldots, k.
$$

The adjective "holonomic" means the constraint involves only positions and possibly time, not velocities -- or if velocities appear, they can be integrated to a position-only constraint. Each holonomic constraint reduces the number of degrees of freedom by one: $n$ coordinates with $k$ constraints leave $n - k$ independent degrees of freedom.

By contrast, a **non-holonomic constraint** is one that involves velocities in a way that cannot be integrated to a position constraint. A wheel rolling without slipping is the standard example: the no-slip condition relates linear and angular velocities but cannot be reduced to a constraint on positions alone. Non-holonomic constraints do not reduce the number of degrees of freedom in configuration space (the system can still reach any configuration); they restrict the *paths* the system can take between configurations.

### The extended Euler-Lagrange equations

For a system with Lagrangian $L(q, \dot q, t)$ and holonomic constraints $f_\alpha(q, t) = 0$ ($\alpha = 1, \ldots, k$), the equations of motion are

$$
\boxed{\;\frac{d}{dt}\frac{\partial L}{\partial \dot q^i} - \frac{\partial L}{\partial q^i} \;=\; \sum_{\alpha=1}^k \lambda_\alpha\,\frac{\partial f_\alpha}{\partial q^i}, \qquad i = 1, \ldots, n,\;}
$$

together with the constraint equations

$$
f_\alpha(q, t) = 0, \qquad \alpha = 1, \ldots, k.
$$

This is a system of $n + k$ equations for $n + k$ unknowns $(q^1, \ldots, q^n, \lambda_1, \ldots, \lambda_k)$. The right-hand side is the **generalised constraint force**:

$$
Q_i^{(c)} = \sum_\alpha \lambda_\alpha\,\frac{\partial f_\alpha}{\partial q^i}.
$$

### Variational derivation

The extended equations arise from extremising the augmented action

$$
S = \int_0^T \left(L(q, \dot q, t) - \sum_{\alpha=1}^k \lambda_\alpha(t)\,f_\alpha(q, t)\right) dt,
$$

where the $\lambda_\alpha(t)$ are functions of time (one multiplier per constraint, varying freely in time). Varying $q^i$ gives the extended Euler-Lagrange equations. Varying $\lambda_\alpha$ gives the constraints $f_\alpha = 0$. The endpoint conditions $\delta q^i(0) = \delta q^i(T) = 0$ are used in the integration by parts for the $q^i$ variation.

### D'Alembert's principle

D'Alembert's principle states that the total virtual work of the applied forces minus the inertial terms vanishes for all virtual displacements compatible with the constraints:

$$
\sum_i \left(Q_i - \left[\frac{d}{dt}\frac{\partial L}{\partial \dot q^i} - \frac{\partial L}{\partial q^i}\right]\right)\delta q^i = 0
$$

for all $\delta q^i$ satisfying $\sum_i (\partial f_\alpha/\partial q^i)\,\delta q^i = 0$. The multipliers enforce this orthogonality: the "unbalanced force" (the left side of the extended Euler-Lagrange equation) must be a linear combination of the constraint gradients, which is exactly the statement that the constraint force compensates the component of the applied force normal to the constraint surface.

### Constraint force identification

For a single particle of mass $m$ in a potential $V(\mathbf{r})$ with constraint $f(\mathbf{r}) = 0$, the constraint force is $\mathbf{F}^{(c)} = \lambda\,\nabla f$. The multiplier satisfies

$$
\lambda = \frac{m(\dot{\mathbf{r}} \cdot \nabla)^2 f - \nabla V \cdot \nabla f}{|\nabla f|^2},
$$

evaluated on the constraint surface. The numerator has two contributions: a centripetal term from the curvature of the constraint surface and a term from the projection of the applied force onto the surface normal.

### Worked example: the double pendulum

Two masses $m_1$ and $m_2$ are connected by rigid rods of lengths $\ell_1$ (pivot to $m_1$) and $\ell_2$ ($m_1$ to $m_2$), swinging in a vertical plane. This system has two constraints: $x_1^2 + y_1^2 = \ell_1^2$ and $(x_2 - x_1)^2 + (y_2 - y_1)^2 = \ell_2^2$. Four Cartesian coordinates minus two constraints gives two degrees of freedom, which can be taken as the angles $\theta_1$ and $\theta_2$.

Expressing the positions in terms of angles:

$$
x_1 = \ell_1\sin\theta_1, \quad y_1 = -\ell_1\cos\theta_1,
$$
$$
x_2 = \ell_1\sin\theta_1 + \ell_2\sin\theta_2, \quad y_2 = -\ell_1\cos\theta_1 - \ell_2\cos\theta_2.
$$

The Lagrangian is

$$
L = \tfrac{1}{2}(m_1 + m_2)\ell_1^2\dot\theta_1^2 + \tfrac{1}{2}m_2\ell_2^2\dot\theta_2^2 + m_2\ell_1\ell_2\dot\theta_1\dot\theta_2\cos(\theta_1 - \theta_2) + (m_1 + m_2)g\ell_1\cos\theta_1 + m_2 g\ell_2\cos\theta_2.
$$

The Euler-Lagrange equations for $\theta_1$ and $\theta_2$ give a coupled nonlinear system. In the small-angle limit, this reduces to two coupled linear oscillators, which can be diagonalised into normal modes. The full nonlinear system exhibits chaotic behaviour for large amplitudes.

To find the rod tensions (the constraint forces), one would instead work in Cartesian coordinates with two Lagrange multipliers $\lambda_1$ and $\lambda_2$. The extended equations give both the motion and the tensions as functions of time.

### Worked example: the Atwood machine

Two masses $m_1$ and $m_2$ ($m_1 > m_2$) are connected by a massless inextensible string over a frictionless pulley. The constraint is $y_1 + y_2 = \ell$ (constant string length), giving $\dot y_1 = -\dot y_2$ and $\ddot y_1 = -\ddot y_2$.

The Lagrangian with both coordinates is $L = \tfrac{1}{2}m_1\dot y_1^2 + \tfrac{1}{2}m_2\dot y_2^2 + m_1 g y_1 + m_2 g y_2$. The extended equations are

$$
m_1\ddot y_1 = m_1 g + \lambda, \qquad m_2\ddot y_2 = m_2 g + \lambda,
$$

with constraint $y_1 + y_2 = \ell$. Using $\ddot y_2 = -\ddot y_1$:

$$
m_1\ddot y_1 = m_1 g + \lambda, \qquad -m_2\ddot y_1 = m_2 g + \lambda.
$$

Subtracting: $(m_1 + m_2)\ddot y_1 = (m_1 - m_2)g$, giving the acceleration $\ddot y_1 = (m_1 - m_2)g/(m_1 + m_2)$. Back-substituting for the multiplier: $\lambda = m_1\ddot y_1 - m_1 g = -2m_1 m_2 g/(m_1 + m_2)$. The constraint force on $m_1$ is $T = \lambda = -2m_1 m_2 g/(m_1 + m_2)$ (the string tension, directed upward).

## Key theorem with proof [Intermediate+]

**Theorem (Constraint forces do no virtual work).** *For a system with scleronomic (time-independent) holonomic constraints $f_\alpha(q) = 0$, the constraint forces do zero work along any virtual displacement compatible with the constraints:*

$$
\sum_i Q_i^{(c)}\,\delta q^i = \sum_i \sum_\alpha \lambda_\alpha\,\frac{\partial f_\alpha}{\partial q^i}\,\delta q^i = 0
$$

*for all $\delta q^i$ satisfying $\sum_i (\partial f_\alpha/\partial q^i)\,\delta q^i = 0$ for each $\alpha$.*

**Proof.** A virtual displacement $\delta q^i$ compatible with the constraints satisfies $\sum_i (\partial f_\alpha/\partial q^i)\,\delta q^i = 0$ for each $\alpha$ (this is the linearisation of the constraint: to first order, $\delta f_\alpha = 0$). The virtual work of the constraint forces is

$$
\sum_i Q_i^{(c)}\,\delta q^i = \sum_\alpha \lambda_\alpha \underbrace{\sum_i \frac{\partial f_\alpha}{\partial q^i}\,\delta q^i}_{=\,0} = 0.
$$

Each term in the outer sum vanishes because the inner sum is the variation $\delta f_\alpha$, which is zero by the compatibility condition. $\square$

This is the formal expression of D'Alembert's principle: the constraint forces are orthogonal to the constraint surface, and virtual displacements lie within the constraint surface, so their dot product vanishes. The physical content is that rigid wires, rods, and surfaces do no work on the system -- they redirect motion without adding or removing energy.

**Corollary.** The work done by constraint forces on an actual trajectory also vanishes for scleronomic constraints. The power is $P = \sum_i Q_i^{(c)}\dot q^i = \sum_\alpha \lambda_\alpha \dot f_\alpha$. Since $f_\alpha(q) = 0$ implies $\dot f_\alpha = \sum_i (\partial f_\alpha/\partial q^i)\dot q^i = 0$, the constraint forces do zero work on the actual trajectory. This is consistent with energy conservation: if the Lagrangian has the standard form $L = T - V$ with no explicit time dependence, the total energy $T + V$ is conserved, and the constraint forces contribute nothing to the energy budget.

### Worked example: bead on a parabola

A bead of mass $m$ moves in a vertical plane, constrained to the parabola $y = x^2$. The constraint is $f(x,y) = y - x^2 = 0$.

The Lagrangian in Cartesian coordinates is $L = \tfrac{1}{2}m(\dot x^2 + \dot y^2) - mgy$. The extended equations:

$$
m\ddot x = -2\lambda x, \qquad m\ddot y = -mg + \lambda.
$$

The constraint implies $\dot y = 2x\dot x$ and $\ddot y = 2\dot x^2 + 2x\ddot x$. Substituting the $\ddot x$ equation: $\ddot y = 2\dot x^2 - 4\lambda x^2/m$. Setting equal to the $\ddot y$ from the second extended equation: $2\dot x^2 - 4\lambda x^2/m = -g + \lambda/m$, giving

$$
\lambda = \frac{m(g + 2\dot x^2)}{1 + 4x^2}.
$$

The constraint force is $\mathbf{F}^{(c)} = \lambda(-2x, 1)$, perpendicular to the parabola. At equilibrium ($\dot x = 0$, $\ddot x = 0$): $\lambda = mg/(1 + 4x^2)$. The only equilibrium is at the vertex $x = 0$, where $\lambda = mg$ and the normal force balances gravity.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 1 (easy, symbolic).**

A particle moves in a plane under no applied forces, constrained to the unit circle $x^2 + y^2 = 1$. Write the extended Euler-Lagrange equations and show that the particle moves with constant angular velocity.

<details class="exercise__hint"><summary>Hint</summary>

$L = \tfrac{1}{2}m(\dot x^2 + \dot y^2)$ with constraint $f = x^2 + y^2 - 1 = 0$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Extended equations: $m\ddot x = 2\lambda x$ and $m\ddot y = 2\lambda y$. Differentiating the constraint: $x\dot x + y\dot y = 0$ and $\dot x^2 + x\ddot x + \dot y^2 + y\ddot y = 0$. Substituting: $2\lambda(x^2 + y^2) + m(\dot x^2 + \dot y^2) = 0$, giving $\lambda = -m(\dot x^2 + \dot y^2)/2$. Since the constraint force does no work, $|\dot{\mathbf{r}}|^2 = \dot x^2 + \dot y^2$ is constant, so $\lambda$ is constant. In polar coordinates $\dot\theta = v/R = \text{const}$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 2 (easy, symbolic).**

A particle of mass $m$ is constrained to move on the surface of a cone $z = r$ (in cylindrical coordinates) in a uniform gravitational field $V = mgz$. Use the constraint to eliminate $z$ and write the Lagrangian in terms of $r$ and $\phi$.

<details class="exercise__hint"><summary>Hint</summary>

Substitute $z = r$ and $\dot z = \dot r$ into $L = T - V$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$L = \tfrac{1}{2}m(\dot r^2 + r^2\dot\phi^2 + \dot r^2) - mgr = \tfrac{1}{2}m(2\dot r^2 + r^2\dot\phi^2) - mgr$. The coordinate $\phi$ is cyclic, so $p_\phi = mr^2\dot\phi = \ell$ is conserved. The effective one-dimensional Lagrangian for $r$ is $L_{\rm eff} = m\dot r^2 - \ell^2/(2mr^2) - mgr$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

A particle of mass $m$ slides on the inside of a smooth sphere of radius $R$ in a uniform gravitational field. Write the Lagrangian in spherical coordinates with the constraint built in, and find the condition for circular orbits at polar angle $\theta_0$.

<details class="exercise__hint"><summary>Hint</summary>

With $r = R$ fixed, the Lagrangian is $L = \tfrac{1}{2}mR^2(\dot\theta^2 + \sin^2\theta\,\dot\phi^2) - mgR\cos\theta$. The coordinate $\phi$ is cyclic.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$p_\phi = mR^2\sin^2\theta\,\dot\phi = \ell$ (conserved). The effective potential for $\theta$ is $V_{\rm eff}(\theta) = \ell^2/(2mR^2\sin^2\theta) + mgR\cos\theta$. A circular orbit at $\theta_0$ satisfies $\partial V_{\rm eff}/\partial\theta = 0$: $-\ell^2\cos\theta_0/(mR^2\sin^3\theta_0) - mgR\sin\theta_0 = 0$, giving $\ell^2\cos\theta_0 = -m^2gR^3\sin^4\theta_0$. Since $\ell^2 > 0$, this requires $\cos\theta_0 < 0$, i.e., $\theta_0 > \pi/2$ (upper hemisphere).

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Two masses $m_1$ and $m_2$ are connected by a rigid rod of length $\ell$. They move in a plane under no external forces. Write the constraint and show that the centre of mass moves with constant velocity while the system rotates at constant angular velocity.

<details class="exercise__hint"><summary>Hint</summary>

Constraint: $(x_1 - x_2)^2 + (y_1 - y_2)^2 = \ell^2$. Use centre-of-mass and relative coordinates.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\mathbf{R} = (m_1\mathbf{r}_1 + m_2\mathbf{r}_2)/(m_1 + m_2)$ and $\mathbf{r} = \mathbf{r}_1 - \mathbf{r}_2$. The constraint is $|\mathbf{r}|^2 = \ell^2$. The Lagrangian separates: $L = \tfrac{1}{2}(m_1 + m_2)|\dot{\mathbf{R}}|^2 + \tfrac{1}{2}\mu|\dot{\mathbf{r}}|^2$ where $\mu = m_1 m_2/(m_1 + m_2)$. No force acts on $\mathbf{R}$, so $\ddot{\mathbf{R}} = 0$. For $\mathbf{r}$: in polar coordinates with $r = \ell$ fixed, $\mu\ell^2\ddot\theta = 0$, giving $\dot\theta = \text{const}$. The system translates uniformly and rotates uniformly.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Show that the work done by the constraint force $\mathbf{F}^{(c)} = \lambda\,\nabla f$ on a trajectory satisfying $f = 0$ is zero when $f$ is time-independent.

<details class="exercise__hint"><summary>Hint</summary>

Compute $\mathbf{F}^{(c)} \cdot \dot{\mathbf{r}} = \lambda\,\nabla f \cdot \dot{\mathbf{r}}$ and use the time derivative of the constraint.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The constraint $f = 0$ implies $\dot f = \nabla f \cdot \dot{\mathbf{r}} = 0$ for scleronomic constraints. The power is $P = \mathbf{F}^{(c)} \cdot \dot{\mathbf{r}} = \lambda(\nabla f \cdot \dot{\mathbf{r}}) = \lambda\dot f = 0$. The constraint force does zero work, consistent with D'Alembert's principle.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

A simple pendulum (mass $m$, length $\ell$) has a pivot that oscillates vertically as $y_0(t) = A\cos\omega t$. Write the constraint and the Lagrangian in terms of the pendulum angle $\theta$, and find the equation of motion.

<details class="exercise__hint"><summary>Hint</summary>

The bob's position is $x = \ell\sin\theta$, $y = y_0(t) - \ell\cos\theta$. This is a rheonomic (time-dependent) constraint.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Velocity: $\dot x = \ell\dot\theta\cos\theta$, $\dot y = -A\omega\sin\omega t + \ell\dot\theta\sin\theta$. Kinetic energy: $T = \tfrac{1}{2}m(\ell^2\dot\theta^2 + A^2\omega^2\sin^2\omega t - 2A\omega\ell\dot\theta\sin\theta\sin\omega t)$. Potential: $V = mg(A\cos\omega t - \ell\cos\theta)$. The Euler-Lagrange equation gives $m\ell^2\ddot\theta - m A\omega^2\ell\cos\omega t\sin\theta = -mg\ell\sin\theta$, or

$$
\ddot\theta + \frac{g}{\ell}\sin\theta = \frac{A\omega^2}{\ell}\cos\omega t\,\sin\theta.
$$

In the small-angle limit this becomes the Mathieu equation -- a parametrically driven oscillator.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

A particle of mass $m$ slides on the inside of a smooth paraboloid of revolution $z = (x^2 + y^2)/(2a)$ in a uniform gravitational field. Find the condition for circular orbits at height $z_0$ and determine their stability.

<details class="exercise__hint"><summary>Hint</summary>

Use cylindrical coordinates $(r, \phi, z)$ with constraint $z = r^2/(2a)$. Angular momentum $p_\phi$ is conserved.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Eliminating $z$: $L = \tfrac{1}{2}m[(1 + r^2/a^2)\dot r^2 + r^2\dot\phi^2] - mgr^2/(2a)$. Conserved: $\ell = mr^2\dot\phi$. The effective radial equation is $m(1 + r^2/a^2)\ddot r + mr\dot r^2/a^2 - \ell^2/(mr^3) + mgr/a = 0$. For circular orbit ($\dot r = 0$, $\ddot r = 0$): $\ell^2/(mr_0^3) = mgr_0/a$, giving $\ell^2 = m^2 g r_0^4/a$.

Linearising around $r_0$: $m(1 + r_0^2/a^2)\ddot\epsilon + [-3\ell^2/(mr_0^4) + mg/a]\epsilon = 0$. Substituting $\ell^2$: the bracket is $-3mg/a + mg/a = -2mg/a < 0$ (restoring). Frequency $\omega^2 = 2g/[a(1 + r_0^2/a^2)]$. Stable for all $r_0$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Derive the extended Euler-Lagrange equations from the modified action $S = \int_0^T [L - \sum_\alpha \lambda_\alpha f_\alpha]\,dt$ by varying independently in $q^i$ and $\lambda_\alpha$. State where the assumption $\delta q^i(0) = \delta q^i(T) = 0$ is used.

<details class="exercise__hint"><summary>Hint</summary>

Define $L' = L - \sum_\alpha \lambda_\alpha f_\alpha$. Vary $q^i$ and integrate by parts. The boundary terms vanish because $\delta q = 0$ at the endpoints.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define $L' = L - \sum_\alpha \lambda_\alpha f_\alpha$. Varying $q^i \to q^i + \delta q^i$:

$$
\delta S = \int_0^T \left[\frac{\partial L'}{\partial q^i} - \frac{d}{dt}\frac{\partial L'}{\partial \dot q^i}\right]\delta q^i\,dt + \left[\frac{\partial L'}{\partial \dot q^i}\,\delta q^i\right]_0^T.
$$

The boundary term vanishes because $\delta q^i(0) = \delta q^i(T) = 0$. Since $\delta q^i$ is arbitrary in the interior:

$$
\frac{d}{dt}\frac{\partial L}{\partial \dot q^i} - \frac{\partial L}{\partial q^i} = -\sum_\alpha \lambda_\alpha\,\frac{\partial f_\alpha}{\partial q^i}.
$$

Varying $\lambda_\alpha$: $\delta S = -\int_0^T f_\alpha\,\delta\lambda_\alpha\,dt = 0$ for arbitrary $\delta\lambda_\alpha$, giving $f_\alpha = 0$. The endpoint assumption is used only in the integration by parts for the $q^i$ variation.

</details>

</aside>

## Lean formalization [Intermediate+]

`lean_status: none`. The Lagrange multiplier method extends Mathlib's unconstrained Euler-Lagrange machinery to systems with holonomic constraints. The main formalisation gap is the construction of the constrained action functional $S' = \int(L - \sum_\alpha \lambda_\alpha f_\alpha)\,dt$ and its variation, together with the identification of $\lambda_\alpha\,\nabla f_\alpha$ as the constraint force. This would require combining Mathlib's calculus of variations on manifolds with the submanifold geometry of constraint surfaces -- assembling the tangent-bundle geometry of constraint manifolds with the variational framework, and extending the existing Euler-Lagrange machinery to the constrained setting. The Lagrange-d'Alembert equations for non-holonomic constraints are a further gap.

## Advanced results [Master]

### Constraint manifolds and the geometry of multipliers

When $k$ holonomic constraints $f_1, \ldots, f_k$ are imposed on an $n$-dimensional configuration space $Q$, the constraint surface $S = \{q \in Q : f_\alpha(q) = 0,\, \alpha = 1, \ldots, k\}$ is a submanifold of codimension $k$ provided the gradients $\nabla f_\alpha$ are linearly independent at every point of $S$. The tangent space $T_q S$ at $q \in S$ consists of all vectors $\delta q$ satisfying $\sum_i (\partial f_\alpha/\partial q^i)\,\delta q^i = 0$ for each $\alpha$. The constraint forces span the **normal bundle** of $S$: at each $q \in S$, the constraint force lies in the span of $\{\nabla f_1, \ldots, \nabla f_k\}$, which is the orthogonal complement of $T_q S$ in $T_q Q$.

The multipliers $\lambda_\alpha$ are determined by the requirement that the trajectory remain on $S$. Differentiating the constraint $f_\alpha = 0$ twice with respect to time gives $\ddot f_\alpha = 0$, which produces $k$ algebraic equations for the $k$ multipliers. This is the content of the constraint-force formula derived in the Formal definition section: the multipliers adjust instantaneously (they are not dynamical variables with their own equations of motion) to enforce the constraints at every instant.

### Non-holonomic constraints and the Lagrange-d'Alembert equations

Non-holonomic constraints $g_\alpha(q, \dot q, t) = 0$ that depend on velocities in a non-integrable way cannot be handled by the augmented-action method. The correct equations are the **Lagrange-d'Alembert equations**:

$$
\frac{d}{dt}\frac{\partial L}{\partial \dot q^i} - \frac{\partial L}{\partial q^i} = \sum_\alpha \mu_\alpha\,\frac{\partial g_\alpha}{\partial \dot q^i}.
$$

The crucial structural difference from the holonomic case: the multipliers multiply $\partial g_\alpha/\partial \dot q^i$, not $\partial g_\alpha/\partial q^i$. This is because non-holonomic constraints restrict the *velocity* direction, not the *position*, so the constraint force acts in velocity space.

The Lagrange-d'Alembert equations cannot be derived from an augmented variational principle $\delta \int L'\,dt = 0$. They are instead derived from D'Alembert's principle directly: the virtual work of the constraint forces vanishes for all virtual displacements satisfying $\sum_i (\partial g_\alpha/\partial \dot q^i)\,\delta q^i = 0$. This is a weaker foundation than the variational principle for holonomic constraints and is the source of ongoing discussion in the mechanics literature.

**Example: the rolling disk.** A thin disk of radius $r$, mass $m$, rolling without slipping on a horizontal plane. The configuration is $(x, y, \theta, \phi)$ where $(x, y)$ is the contact point, $\theta$ the heading angle, $\phi$ the rotation angle. The rolling constraints are $g_1 = \dot x - r\dot\phi\cos\theta = 0$ and $g_2 = \dot y - r\dot\phi\sin\theta = 0$. These are linear in velocities but cannot be integrated to position constraints (the path depends on the history of $\theta$). The Lagrange-d'Alembert equations give the friction forces at the contact point as the multipliers $\mu_1$ and $\mu_2$.

### Semi-holonomic constraints

A constraint is **semi-holonomic** if it can be written in the form $g_\alpha(q, \dot q, t) = 0$ and admits an integrating factor -- a function $\mu(q, \dot q, t)$ such that $\mu\,g_\alpha$ is the total time derivative of some function of $q$ and $t$. Semi-holonomic constraints are effectively holonomic after this integration and can be treated by the augmented-action method. The boundary between semi-holonomic and genuinely non-holonomic constraints is important: Frobenius's theorem provides the integrability condition (the distribution defined by the constraint must be involutive).

### Dirac constraints in Hamiltonian mechanics -- preview

The Lagrange multiplier method in the Lagrangian framework has a direct Hamiltonian counterpart. When the Legendre transform [09.04.01] is applied to a constrained system, the constraints may become relations among the phase-space variables $(q, p)$. Dirac's theory of constrained Hamiltonian systems classifies these as:

- **Primary constraints**: relations among $(q, p)$ that follow directly from the definition $p_i = \partial L/\partial \dot q^i$. If the constraint $f(q) = 0$ is holonomic, the corresponding primary constraint in phase space involves both $q$ and $p$ in a way that depends on the specific Lagrangian.
- **Secondary constraints**: consistency conditions requiring that primary constraints be preserved under time evolution.
- **First-class constraints**: those whose Poisson brackets with all other constraints vanish on the constraint surface. These generate gauge transformations.
- **Second-class constraints**: those that have non-vanishing Poisson brackets with at least one other constraint. These are eliminated by the **Dirac bracket**, a modified Poisson bracket that respects the constraint structure.

The Lagrange multipliers of the Lagrangian formulation correspond to the undetermined velocities associated with primary constraints in the Dirac theory. The passage from Lagrange multipliers to Dirac brackets is the Hamiltonian counterpart of the material in this unit.

### Gauge theory analogy

In gauge field theories, the gauge field $A_\mu$ has a Lagrangian (the Yang-Mills Lagrangian $L = -\tfrac{1}{4}F_{\mu\nu}F^{\mu\nu}$) that is singular: the Hessian with respect to $\dot A_\mu$ is degenerate because $\dot A_0$ does not appear. This is a primary constraint in Dirac's sense. The Lagrange multiplier associated with this constraint is the undetermined time component of the gauge field, and the freedom to choose it is the **gauge freedom**.

The parallel is exact: the Lagrange multiplier method for finite-dimensional systems with holonomic constraints is the finite-dimensional prototype for the gauge-fixing procedures of field theory. The constraint forces (normal to the constraint surface) are the finite-dimensional analogues of the ghost fields and gauge-fixing terms in the Faddeev-Popov formalism. Understanding the finite-dimensional case -- this unit -- is prerequisite for the field-theoretic generalisation [09.02.06].

### Rheonomic constraints

Constraints that depend explicitly on time, $f_\alpha(q, t) = 0$, are called **rheonomic**. The extended Euler-Lagrange equations are formally identical to the scleronomic case, but the constraint force now does work: the power is $P = \sum_\alpha \lambda_\alpha\,\dot f_\alpha = \sum_\alpha \lambda_\alpha\,\partial f_\alpha/\partial t$, which is generally non-zero. Energy is not conserved for rheonomic systems; the constraint force injects or extracts energy through the time-dependent boundary.

The parametrically driven pendulum (Exercise 6) is the canonical example. The oscillating pivot is a rheonomic constraint, and the work done by the pivot's motor is what drives the parametric resonance.

**Synthesis.** The Lagrange multiplier method is the foundational device that lets constrained dynamics be formulated without eliminating coordinates. The central insight is that the multiplier is dual to the constraint, encoding the force needed to maintain it. This duality -- kinematic restriction as equation, dynamic enforcement as force, linked by the multiplier -- is the algebraic structure that Dirac exploited in his theory of constrained Hamiltonian systems. Putting these together, the multiplier formulation provides the computational bridge from the Lagrangian coordinate-elimination strategy to the Hamiltonian constraint-classification strategy. The generalisation to field theories with gauge constraints -- where the multipliers become gauge fields -- completes the passage from classical mechanics to modern theoretical physics.

## Connections [Master]

- **Action principle [09.02.01]** -- the augmented action $S = \int(L - \sum\lambda_\alpha f_\alpha)\,dt$ is a constrained variational principle. The multipliers enforce the constraints as Euler-Lagrange equations of the extended Lagrangian.

- **Euler-Lagrange equations [09.02.02]** -- the extended Euler-Lagrange equations are the standard EL equations with the constraint force added to the right-hand side. The unconstrained EL equations are the special case $k = 0$.

- **Legendre transform [09.04.01]** -- the passage to Hamiltonian mechanics carries the Lagrange multipliers into Dirac's primary constraints. The multiplier $\lambda$ in the Lagrangian setting becomes the undetermined velocity of a primary constraint in the Hamiltonian setting. The Dirac-Bergmann algorithm is the Hamiltonian counterpart of the multiplier method.

- **Field Lagrangians [09.02.06]** -- gauge field theories use the Lagrange multiplier method at the field-theoretic level. The electromagnetic Gauss-law constraint $\nabla \cdot \mathbf{E} = \rho$ arises as a secondary constraint in the Dirac analysis of the Maxwell Lagrangian, and the multiplier is the time component $A_0$ of the gauge field.

- **Noether's theorem [09.03.01]** -- symmetries of a constrained system must preserve the constraint surface to generate conserved quantities. The multiplier formalism makes this condition explicit: only symmetries for which $\delta f_\alpha = 0$ on the constraint surface contribute conserved Noether charges.

## Historical & philosophical context [Master]

Jean-Baptiste le Rond d'Alembert introduced the principle of virtual work in his *Traite de dynamique* (1743). The key insight was that the forces of constraint do no work under virtual displacements compatible with the constraints -- a principle that converts the problem of constrained motion from a force-balance problem (Newton) into a geometric problem (restricting motion to a submanifold of configuration space). D'Alembert's principle subsumes the statics principle of virtual work (Bernoulli, 1717) into dynamics by including the "inertial force" $-m\mathbf{a}$ alongside the applied forces.

Joseph-Louis Lagrange, in *Mechanique analytique* (1788), built on d'Alembert's principle to develop the generalised coordinate formulation of mechanics. Lagrange introduced the multiplier method as a systematic algebraic procedure for handling constraints without identifying and eliminating them by coordinate choices. His key recognition was that the multipliers have a direct mechanical interpretation as constraint forces -- they are not merely computational devices but carry physical information.

The distinction between holonomic and non-holonomic constraints emerged gradually in the 19th century. Hertz, in *Die Prinzipien der Mechanik* (1894), gave the first systematic treatment of non-holonomic constraints and emphasised their fundamentally different character: holonomic constraints reduce the dimension of configuration space, while non-holonomic constraints restrict the possible paths without changing the accessible configurations.

Paul Dirac, in his 1950 paper "Generalized Hamiltonian Dynamics" and his *Lectures on Quantum Mechanics* (1964), developed the theory of constrained Hamiltonian systems. Dirac's classification of constraints into first-class and second-class, and the construction of the Dirac bracket, transformed the Lagrange multiplier from a computational tool into a structural element with deep physical meaning. First-class constraints generate gauge transformations -- their multipliers represent genuine physical freedom (gauge choice), while second-class constraints are eliminable.

A philosophical observation. The multiplier method reveals that constraints have a dual nature. As equations ($f = 0$), they restrict the configuration space. As forces ($\lambda\,\nabla f$), they act on the system. The multiplier $\lambda$ is the link between these two aspects -- it is determined by the constraint equation but enters the dynamics as a force. This duality between kinematic restriction and dynamic enforcement is a structural feature of all constrained systems, from classical pendulums to gauge field theories. In modern language, it is an instance of duality between a constraint and its Lagrange dual variable, the same mathematical structure that appears in optimisation theory and convex analysis.

## Bibliography [Master]

- D'Alembert, J. le R., *Traite de dynamique* (1743).
- Lagrange, J.-L., *Mechanique analytique* (1788).
- Hertz, H., *Die Prinzipien der Mechanik* (1894).
- Dirac, P. A. M., "Generalized Hamiltonian Dynamics," *Canadian Journal of Mathematics* **2** (1950), 129-148.
- Dirac, P. A. M., *Lectures on Quantum Mechanics* (Yeshiva University, 1964), Ch. 1.
- Arnold, V. I., *Mathematical Methods of Classical Mechanics*, 2nd ed. (Springer GTM 60, 1989), Section 3.
- Taylor, J. R., *Classical Mechanics* (University Science Books, 2005), Ch. 7.
- Goldstein, H., Poole, C. P. & Safko, J., *Classical Mechanics*, 3rd ed. (Pearson, 2002), Ch. 2.
- Landau, L. D. & Lifshitz, E. M., *Mechanics*, 3rd ed. (Course of Theoretical Physics Vol. 1, Pergamon, 1976), Sections 1, 25.
- Marsden, J. E. & Ratiu, T. S., *Introduction to Mechanics and Symmetry*, 2nd ed. (Springer TAM 17, 1999), Ch. 1.3-1.4.
- Bloch, A. M., *Nonholonomic Mechanics and Control* (Springer, 2003), Ch. 1-3.
- Henneaux, M. & Teitelboim, C., *Quantization of Gauge Systems* (Princeton University Press, 1992), Ch. 1-2.
