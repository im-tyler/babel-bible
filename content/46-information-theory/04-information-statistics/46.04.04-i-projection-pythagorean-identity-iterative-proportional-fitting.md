---
id: 46.04.04
title: "The I-Projection, Pythagorean Identity, and Iterative Proportional Fitting (Blahut-Arimoto)"
slug: i-projection-pythagorean-identity-iterative-proportional-fitting
section: information-theory
chapter: 04-information-statistics
concept_catalog_id: information-theory.i-projection-pythagorean-identity-iterative-proportional-fitting
prerequisites:
  - 46.04.01
  - 46.01.02
  - 44.06.01
pending_prereqs: false
successors: []
tier_anchors:
  beginner: "Cover & Thomas 2006 *Elements of Information Theory* 2e (Wiley) §10.1 and §13.7 (the idea of projecting one distribution onto another, iterative algorithms for finding capacity)"
  intermediate: "Cover & Thomas 2006 *Elements of Information Theory* 2e (Wiley) §10.8, §13.7 (I-projection definition, Pythagorean theorem, Blahut-Arimoto convergence)"
  master: "Csiszar & Körner 2011 *Information Theory* §3.1-3.4; Csiszar 1975 *I-Divergence Geometry of Probability Distributions*; Blahut 1972 *Computation of Channel Capacity and Rate-Distortion Functions*"
tiers_present: [beginner, intermediate, master]
references:
  - source: TODO_REF
    path: "Cover, T. M. & Thomas, J. A. — Elements of Information Theory (2nd ed.)"
    locator: 'Wiley (2006). Section 13.7: the Blahut-Arimoto algorithm alternates between computing the optimal input distribution $p^*(x) = \sum_y p(y|x) q^*(y) e^{I(x;y)/C}$ and the capacity update $C_{k+1} = \sum_x p_k(x) D(p(y|x) \| q_k(y))$, converging monotonically to the channel capacity. Section 10.8: the I-projection of $q$ onto a convex set $\mathcal{P}$ is $p^* = \arg\min_{p \in \mathcal{P}} D(p \| q)$; the Pythagorean identity $D(p\|q) = D(p\|p^*) + D(p^*\|q)$ holds for all $p \in \mathcal{P}$.'
  - source: TODO_REF
    path: "Csiszar, I. — I-Divergence Geometry of Probability Distributions and Identification Problems"
    locator: 'Periodica Mathematica Hungarica 6(2-3):149-161 (1975). Introduces the I-projection as the unique minimiser of $D(p\|q)$ over a convex set of distributions; proves the Pythagorean identity and establishes the geometric structure of the space of probability distributions under the KL divergence as a generalised Riemannian manifold.'
  - source: TODO_REF
    path: "Blahut, R. E. — Computation of Channel Capacity and Rate-Distortion Functions"
    locator: 'IEEE Transactions on Information Theory 18(4):460-473 (1972). The Blahut-Arimoto algorithm: initialise an arbitrary input distribution $p_0(x)$, then iterate $q_k(y|x) \propto p_k(x) p(y|x) / q_k(y)$ and $p_{k+1}(x) \propto p_k(x) \exp(D(p(y|x)\|q_k(y)))$ until convergence; proves global convergence to the capacity $C = \max_p I(X;Y)$.'
  - source: TODO_REF
    path: "Arimoto, S. — An Algorithm for Computing the Capacity of Arbitrary Discrete Memoryless Channels"
    locator: 'IEEE Transactions on Information Theory 18(1):14-20 (1972). Independent discovery of the same algorithm: iterates between updating the output distribution and the input distribution, proves monotonic convergence to the capacity via the inequality $C_{k+1} \geq C_k$.'
lean_status: none
lean_mathlib_gap: |
  Mathlib has no formalisation of I-projection, the Pythagorean identity for KL divergence,
  or the Blahut-Arimoto algorithm. The KL divergence D(p||q) is not defined on PMF or
  general measure spaces in Mathlib. The optimisation over conditional distributions required
  for the Blahut-Arimoto algorithm is beyond Mathlib's current probability theory library.
  The convexity of D(p||q) in p and the first-order optimality conditions are unformalised.
  The iterative convergence proof requires monotonicity arguments and fixed-point theorems
  that are not available in the required form. This unit ships without a lean_module and
  is reviewer-attested.
human_reviewer: "Tyler (pending external information-theory reviewer)"
source_books: [cover-thomas-2006]
source_curriculum: information-theory
estimated_time:
  beginner: 15m
  intermediate: 45m
  master: 90m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
hooks_out:
  - target: 46.03.01
    kind: proposed
    why: >
      The Blahut-Arimoto algorithm computes channel capacity for discrete memoryless channels;
      the capacity formula derived in 46.03.01 is the target of the algorithm, and the I-projection
      structure guarantees that the iterates converge monotonically to the capacity-achieving
      input distribution.
  - target: 46.02.05
    kind: proposed
    why: >
      The rate-distortion function R(D) is computed by a dual version of Blahut-Arimoto;
      the same I-projection geometry governs both the capacity computation and the rate-distortion
      computation, with the Pythagorean identity providing the convergence guarantee in both cases.
  - target: 46.04.01
    kind: proposed
    why: >
      Stein's lemma establishes that the optimal error exponent in hypothesis testing is the
      KL divergence D(P||Q); the I-projection of Q onto the set of distributions consistent
      with the test is the geometric structure underlying this optimality, and the Pythagorean
      identity quantifies the information loss from the projection.
---

## Intuition [Beginner]

You have two probability distributions. Call one the "target" $q$ and the other a "candidate" $p$. The KL divergence $D(p \| q)$ measures how different $p$ is from $q$. Now suppose you want to find the distribution $p$ that is closest to $q$ (in the KL sense) but belongs to a constrained set. For example, $p$ might need to match certain marginal distributions.

This is the I-projection: project $q$ onto a set of distributions by finding the member of the set that minimises $D(p \| q)$. Think of it as finding the closest point to $q$ inside a fenced region, where "closest" means least KL divergence.

A beautiful property of I-projection is the Pythagorean identity. If $p^*$ is the I-projection of $q$ onto a convex set $\mathcal{P}$, then for any $p \in \mathcal{P}$:

$$D(p \| q) = D(p \| p^*) + D(p^* \| q).$$

This looks like the Pythagorean theorem: the squared distance from $p$ to $q$ decomposes into the distance from $p$ to the projection $p^*$ plus the distance from $p^*$ to $q$. The I-projection splits the divergence into orthogonal components.

The Blahut-Arimoto algorithm uses this geometry to compute channel capacity. It alternates between two steps: (1) find the best output distribution given the current input distribution, and (2) find the best input distribution given the current output distribution. Each step is an I-projection, and the Pythagorean identity guarantees that the mutual information increases monotonically. The algorithm converges to the capacity-achieving input distribution.

## Visual [Beginner]

| Concept | Analogy | Mathematical object |
|---|---|---|
| KL divergence $D(p \| q)$ | Squared Euclidean distance | Measure of dissimilarity |
| I-projection | Orthogonal projection onto a set | $\arg\min_{p \in \mathcal{P}} D(p \| q)$ |
| Pythagorean identity | $a^2 + b^2 = c^2$ | $D(p\|q) = D(p\|p^*) + D(p^*\|q)$ |
| Blahut-Arimoto | Alternating projection (von Neumann) | Iterative I-projections converge to capacity |

*Figure: a Venn-diagram-style picture. A point $q$ sits outside a convex set $\mathcal{P}$. The I-projection $p^*$ is the closest point in $\mathcal{P}$ to $q$ (under KL divergence). For any other point $p \in \mathcal{P}$, the three points form a right triangle: $D(p\|q)$ is the hypotenuse, $D(p\|p^*)$ is one leg (inside $\mathcal{P}$), and $D(p^*\|q)$ is the other leg (the projection direction).*

## Worked example [Beginner]

A binary symmetric channel flips each bit with probability 0.1. What input distribution maximises the mutual information?

By symmetry, the uniform input $p(0) = p(1) = 0.5$ should be optimal. The Blahut-Arimoto algorithm confirms this. Starting from any input distribution, it alternates:

**Step A.** Given input $p(x)$, compute the output $q(y)$ by mixing over all inputs with their channel transition probabilities.

**Step B.** Update the input: $p_{\text{new}}(x) \propto p(x) \cdot \exp(I(x; Y))$ where $I(x; Y)$ measures the information that sending symbol $x$ provides about the output.

Starting from $p(0) = 0.3$, $p(1) = 0.7$:

The output distribution is $q(0) = 0.3 \times 0.9 + 0.7 \times 0.1 = 0.34$, $q(1) = 0.66$. After one iteration, the algorithm pushes $p$ toward uniform. After a few iterations, $p(0) \approx p(1) \approx 0.5$, and the mutual information converges to $C = 1 - H_b(0.1) = 1 - 0.469 = 0.531$ bits/use.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What does the I-projection of $q$ onto a set $\mathcal{P}$ find?

A. The distribution in $\mathcal{P}$ that maximises $D(p \| q)$.
B. The distribution in $\mathcal{P}$ that minimises $D(p \| q)$.
C. The distribution $q$ itself.
D. The uniform distribution.

<details class="exercise__answer"><summary>Answer</summary>

**B.** The I-projection finds the distribution in $\mathcal{P}$ that is closest to $q$ in KL divergence.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true/false).**

The Blahut-Arimoto algorithm converges even if started from a bad initial guess.

<details class="exercise__answer"><summary>Answer</summary>

**True.** The algorithm converges globally from any initial distribution with full support. Each iteration increases the mutual information (or decreases the rate-distortion Lagrangian), and the objective is bounded.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise (medium, numeric).**

Let $q = (0.5, 0.5)$ be a uniform distribution on two outcomes. The constrained set $\mathcal{P}$ contains only the distribution $p = (0.8, 0.2)$. Compute $D(p \| q)$.

<details class="exercise__answer"><summary>Answer</summary>

$D(p \| q) = 0.8 \log_2(0.8/0.5) + 0.2 \log_2(0.2/0.5) = 0.8 \times 0.678 + 0.2 \times (-1.322) = 0.543 - 0.264 = 0.278$ bits.

</details>

</aside>

## Formal definition [Intermediate+]

Let $\mathcal{P}$ be a convex set of probability distributions on a finite alphabet $\mathcal{X}$, and let $q$ be a distribution on $\mathcal{X}$ with full support.

**Definition (I-projection).** The I-projection of $q$ onto $\mathcal{P}$ is:

$$p^* = \arg\min_{p \in \mathcal{P}} D(p \| q) = \arg\min_{p \in \mathcal{P}} \sum_x p(x) \log \frac{p(x)}{q(x)}.$$

Since $D(p \| q)$ is strictly convex in $p$ (for fixed $q$ with full support) and $\mathcal{P}$ is convex, $p^*$ exists and is unique.

**Theorem (Pythagorean identity for I-projection).** If $\mathcal{P}$ is a linear family (a set of distributions satisfying linear constraints of the form $\sum_x p(x) a_i(x) = b_i$), then for any $p \in \mathcal{P}$:

$$D(p \| q) = D(p \| p^*) + D(p^* \| q).$$

*Proof.* Since $\mathcal{P}$ is defined by linear constraints, the optimal $p^*$ satisfies the KKT conditions. The Lagrangian is:

$$\mathcal{L}(p, \lambda) = D(p \| q) + \sum_i \lambda_i \left(b_i - \sum_x p(x) a_i(x)\right).$$

Setting the derivative to zero gives $p^*(x) = q(x) \exp\!\left(-1 - \sum_i \lambda_i a_i(x)\right) / Z$, i.e., $p^*$ has the exponential family form $p^*(x) = q(x) e^{\theta \cdot a(x)} / Z(\theta)$.

For any $p \in \mathcal{P}$:

$$D(p \| q) - D(p^* \| q) = \sum_x (p(x) - p^*(x)) \log \frac{p^*(x)}{q(x)} = \sum_x (p(x) - p^*(x)) \left(\theta \cdot a(x) - \log Z\right).$$

Since both $p$ and $p^*$ satisfy the same linear constraints, $\sum_x (p(x) - p^*(x)) a_i(x) = 0$ for all $i$. Therefore:

$$D(p \| q) - D(p^* \| q) = -\log Z \sum_x (p(x) - p^*(x)) = 0.$$

This gives $D(p \| q) = D(p^* \| q) + \sum_x (p(x) - p^*(x)) \log(p^*(x)/q(x))$. The remaining term equals $D(p \| p^*)$ by direct computation: $D(p \| p^*) = \sum_x p(x) \log(p(x)/p^*(x))$, and using $p^*(x) = q(x)e^{\theta \cdot a(x)}/Z$ with the linear constraints gives the result. $\square$

### The Blahut-Arimoto algorithm

**Definition (Blahut-Arimoto for channel capacity).** For a discrete memoryless channel $p(y|x)$, the Blahut-Arimoto algorithm computes:

$$C = \max_{p(x)} I(X; Y) = \max_{p(x)} \sum_{x,y} p(x) p(y|x) \log \frac{p(y|x)}{q(y)}$$

by iterating:

1. **Output update:** $q_k(y) = \sum_x p_k(x) p(y|x)$
2. **Input update:** $p_{k+1}(x) = \frac{p_k(x) \exp(I_k(x))}{\sum_{x'} p_k(x') \exp(I_k(x'))}$

where $I_k(x) = D(p(y|x) \| q_k(y)) = \sum_y p(y|x) \log \frac{p(y|x)}{q_k(y)}$.

The capacity estimate at step $k$ is $C_k = \sum_x p_k(x) I_k(x)$, and $C_k \nearrow C$ monotonically.

### Connection to iterative proximal methods

The Blahut-Arimoto algorithm can be viewed as an alternating proximal step in the geometry of KL divergence [44.06.01]. Each update minimises:

$$p_{k+1} = \arg\min_{p} \left[-I(X;Y)(p) + D(p \| p_k)\right],$$

which is a proximal iteration with KL divergence as the Bregman distance. This connects to mirror descent and other first-order methods in convex optimisation.

### Key theorem with proof [Intermediate+]

**Theorem (Blahut-Arimoto convergence).** The Blahut-Arimoto algorithm converges to the channel capacity $C$ from any initial distribution $p_0$ with full support. The iterates satisfy:

$$C_{k+1} \geq C_k \quad \text{and} \quad \lim_{k \to \infty} C_k = C.$$

*Proof.* Define the double maximisation:

$$C = \max_{p(x)} \max_{q(y)} \sum_{x,y} p(x) p(y|x) \log \frac{p(y|x)}{q(y)}.$$

The inner maximisation over $q$ (for fixed $p$) is achieved by $q^*(y) = \sum_x p(x) p(y|x)$. The outer maximisation over $p$ (for fixed $q$) is achieved by $p^*(x) \propto p(x) \exp(D(p(y|x) \| q(y)))$.

Each step of Blahut-Arimoto performs one of these maximisations, so the objective increases monotonically. By the alternating maximisation lemma (a consequence of convexity), the iterates converge to the global maximum. $\square$

**Bridge.** The I-projection and Pythagorean identity build toward the iterative algorithms in [44.06.01] where proximal methods and mirror descent use the same Bregman-divergence structure; this appears again in [46.02.05] where the rate-distortion function is computed by the dual Blahut-Arimoto algorithm; the foundational reason the Pythagorean identity holds is the linear constraint structure of the projection set, which is dual to the exponential family form of the optimal distribution; the central insight is that alternating I-projections converge monotonically, and putting these together gives a unified computational framework for all information-theoretic optimisation problems.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

Compute $D(p \| q)$ for $p = (0.6, 0.4)$ and $q = (0.5, 0.5)$ in bits.

<details class="exercise__answer"><summary>Answer</summary>

$D(p\|q) = 0.6\log_2(0.6/0.5) + 0.4\log_2(0.4/0.5) = 0.6 \times 0.263 + 0.4 \times (-0.322) = 0.158 - 0.129 = 0.029$ bits.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true/false).**

The I-projection of $q$ onto $\mathcal{P}$ always satisfies $D(p^* \| q) \leq D(p \| q)$ for all $p \in \mathcal{P}$.

<details class="exercise__answer"><summary>Answer</summary>

**True.** This is the definition of I-projection: $p^*$ minimises $D(p \| q)$ over $\mathcal{P}$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise (medium, short answer).**

Show that the Pythagorean identity implies $D(p \| q) \geq D(p^* \| q)$ for all $p \in \mathcal{P}$.

<details class="exercise__answer"><summary>Answer</summary>

By the Pythagorean identity, $D(p \| q) = D(p \| p^*) + D(p^* \| q)$. Since $D(p \| p^*) \geq 0$ (Gibbs' inequality), we have $D(p \| q) \geq D(p^* \| q)$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise (medium, short answer).**

Verify the Pythagorean identity for $p = (0.9, 0.1)$, $p^* = (0.7, 0.3)$, $q = (0.5, 0.5)$. (Assume $p^*$ is the I-projection of $q$ onto the set containing only $p$ and $p^*$.)

<details class="exercise__answer"><summary>Answer</summary>

$D(p\|q) = 0.9\log_2(1.8) + 0.1\log_2(0.2) = 0.9 \times 0.848 + 0.1 \times (-2.322) = 0.763 - 0.232 = 0.531$ bits.

$D(p\|p^*) = 0.9\log_2(0.9/0.7) + 0.1\log_2(0.1/0.3) = 0.9 \times 0.363 + 0.1 \times (-1.585) = 0.327 - 0.159 = 0.168$ bits.

$D(p^*\|q) = 0.7\log_2(1.4) + 0.3\log_2(0.6) = 0.7 \times 0.485 + 0.3 \times (-0.737) = 0.340 - 0.221 = 0.119$ bits.

Check: $0.168 + 0.119 = 0.287 \neq 0.531$. The Pythagorean identity does not hold here because the set $\{p, p^*\}$ is not a linear family. The identity requires the constraint set to be linear (defined by moment-matching constraints).

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise (medium, short answer).**

In the Blahut-Arimoto algorithm, why does fixing $q$ and optimising over $p$ yield an I-projection?

<details class="exercise__answer"><summary>Answer</summary>

The mutual information for fixed $q$ is $I(X;Y) = \sum_x p(x) D(p(y|x) \| q(y)) + \text{const}$, which is linear in $p$. The constraint is $\sum_x p(x) = 1$ (linear). The optimisation over $p$ with the constraint is equivalent to minimising the KL divergence $D(p \| p_k)$ plus the linear term, which has the I-projection structure.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise (hard, short answer).**

Prove that the Blahut-Arimoto capacity estimate $C_k = \sum_x p_k(x) I_k(x)$ is non-decreasing.

<details class="exercise__answer"><summary>Answer</summary>

Write $C_k = I(p_k, q_k)$ where $I(p, q) = \sum_{x,y} p(x) p(y|x) \log(p(y|x)/q(y))$. Step 1 updates $q_k$ to $q_{k+1} = p_k \cdot p(y|x)$ (the output distribution induced by $p_k$). Since $q_{k+1}$ maximises $I(p_k, q)$ over all $q$, $C_k = I(p_k, q_{k+1}) \geq I(p_k, q_k)$. Step 2 updates $p_k$ to $p_{k+1}$. Since $p_{k+1}$ maximises $I(p, q_{k+1})$ over all $p$, $I(p_{k+1}, q_{k+1}) \geq I(p_k, q_{k+1})$. Combining: $C_{k+1} \geq C_k$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise (hard, short answer).**

Show that the I-projection of $q$ onto the set of distributions with a given mean $\mu$ (i.e., $\mathcal{P} = \{p: \sum_x x \cdot p(x) = \mu\}$) has the exponential family form $p^*(x) \propto q(x) e^{\lambda x}$.

<details class="exercise__answer"><summary>Answer</summary>

The Lagrangian is $\mathcal{L} = D(p\|q) + \lambda(\mu - \sum_x x p(x)) + \nu(1 - \sum_x p(x))$. Setting $\partial \mathcal{L}/\partial p(x) = 0$ gives $\log(p(x)/q(x)) + 1 - \lambda x - \nu = 0$, hence $p^*(x) = q(x) e^{\lambda x - 1 + \nu} \propto q(x) e^{\lambda x}$, where $\lambda$ is chosen to satisfy $\sum_x x \cdot p^*(x) = \mu$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise (hard, short answer).**

Describe how Blahut-Arimoto is modified to compute the rate-distortion function $R(D)$ instead of channel capacity.

<details class="exercise__answer"><summary>Answer</summary>

For rate-distortion, the double minimisation is $R(D) = \min_{q(\hat{x})} \min_{p(\hat{x}|x): \mathbb{E}[d] \leq D} I(X;\hat{X})$. The algorithm alternates: (1) given output $q(\hat{x})$, update the test channel $p(\hat{x}|x) \propto q(\hat{x}) e^{s \cdot d(x,\hat{x})}$ (Gibbs form); (2) given test channel, update output $q(\hat{x}) = \sum_x p(x) p(\hat{x}|x)$. The Lagrange parameter $s$ controls the distortion level. This is the rate-distortion version of Blahut-Arimoto, and it converges by the same monotonicity argument.

</details>

</aside>

## Lean formalization [Intermediate+]

Mathlib has no formalisation of I-projection, the Pythagorean identity for KL divergence, or the Blahut-Arimoto algorithm. The KL divergence $D(p\|q)$ is not defined as a functional on distributions in Mathlib. The optimisation over conditional distributions required for the algorithm is beyond Mathlib's current capabilities. The convergence proof requires the alternating maximisation lemma, which is a result in convex optimisation not present in Mathlib. The exponential family form of the I-projection requires solving the KKT conditions for constrained optimisation over probability distributions, which Mathlib's optimisation library does not support. This unit ships without a lean_module.

## Advanced results [Master]

### The information geometry of I-projection

Imre Csiszar's 1975 paper established that the space of probability distributions equipped with KL divergence has a differential-geometric structure. The I-projection is the analogue of orthogonal projection in this geometry. The tangent space at a distribution $p$ consists of score functions $f(x)$ with $\mathbb{E}_p[f] = 0$, and the KL divergence induces a Riemannian metric given by the Fisher information matrix $g_{ij} = \mathbb{E}_p[(\partial_i \log p)(\partial_j \log p)]$.

The Pythagorean identity is a manifestation of the flatness of the exponential family manifold. Linear families (defined by moment constraints) and exponential families (defined by exponential tilting) are dual flat submanifolds in this geometry. The I-projection onto a linear family is the unique point where the exponential family "orthogonal" to the linear family intersects it.

### Iterative proportional fitting (IPF)

The iterative proportional fitting procedure, also known as the RAS algorithm or matrix scaling, is a special case of alternating I-projections. Given a target matrix of row and column marginals, IPF alternates between scaling rows to match row marginals and scaling columns to match column marginals. Each scaling step is an I-projection onto the set of matrices with the given marginal.

IPF converges by the same alternating projection principle as Blahut-Arimoto. The convergence rate depends on the condition number of the marginal constraints. The algorithm is used in contingency table analysis, transportation planning, and the calibration of input-output matrices in economics.

### The alternating minimisation lemma

The general principle underlying both Blahut-Arimoto and IPF is the alternating minimisation lemma: if $f(p, q)$ is convex in each argument separately and the feasible sets are convex, then the sequence generated by alternating minimisation converges to a stationary point. For the specific structure of KL divergence, the stationary point is the global optimum.

The connection to proximal methods [44.06.01] is that each step of Blahut-Arimoto can be written as:

$$p_{k+1} = \arg\min_p \left\{f(p) + \frac{1}{\eta} D(p \| p_k)\right\},$$

which is a mirror descent update with step size $\eta$ and KL divergence as the Bregman distance. The convergence rate is $O(1/k)$ for the capacity estimate, matching the rate of other first-order methods.

**Synthesis.** The I-projection and Pythagorean identity form the geometric backbone of information theory; the central insight is that KL divergence induces a flat geometry on the probability simplex, and putting these together we see that Blahut-Arimoto, IPF, and mirror descent are all instances of alternating I-projections in this geometry; this builds toward the computation of channel capacity [46.03.01] and rate-distortion [46.02.05] where these algorithms are deployed; the foundational reason for convergence is the convexity of KL divergence in its first argument combined with the linear structure of the constraint sets, and this generalises to any Bregman-divergence-based alternating optimisation.

## Full proof set [Master]

**Proposition (Existence and uniqueness of I-projection).** For a convex set $\mathcal{P}$ of distributions and a distribution $q$ with full support, the I-projection $p^* = \arg\min_{p \in \mathcal{P}} D(p \| q)$ exists and is unique.

*Proof.* The function $p \mapsto D(p \| q)$ is strictly convex on the simplex (since its Hessian is the Fisher information, which is positive definite). The feasible set $\mathcal{P}$ is convex by assumption. A strictly convex function on a convex set has at most one minimiser.

For existence, consider a minimising sequence $p_n \in \mathcal{P}$ with $D(p_n \| q) \to \inf_{p \in \mathcal{P}} D(p \| q)$. Since $\mathcal{P}$ is a subset of the compact simplex, a subsequence converges to some $p^*$. By continuity of $D(p \| q)$ in $p$ (for fixed $q$ with full support), $D(p^* \| q) = \inf D(p \| q)$. By strict convexity, $p^*$ is unique. $\square$

**Proposition (Dual Blahut-Arimoto for rate-distortion).** The rate-distortion function $R(D)$ can be computed by an alternating minimisation: initialise $q_0(\hat{x})$, then iterate $p_{k+1}(\hat{x}|x) \propto q_k(\hat{x}) e^{s \cdot d(x,\hat{x})}$ and $q_{k+1}(\hat{x}) = \sum_x p(x) p_{k+1}(\hat{x}|x)$. The sequence $R_k = I(X;\hat{X})_k$ converges to $R(D)$ where $s$ is the Lagrange multiplier corresponding to $D$.

*Proof.* The Lagrangian formulation gives $R(D) = \max_{s \leq 0} \min_{p(\hat{x}|x)} [I(X;\hat{X}) - s(\mathbb{E}[d] - D)]$. For fixed $s$, the inner minimisation is over $p(\hat{x}|x)$, which decomposes into independent problems for each $x$:

$$\min_{p(\hat{x}|x)} \left[\sum_{\hat{x}} p(\hat{x}|x) \log \frac{p(\hat{x}|x)}{q(\hat{x})} - s \sum_{\hat{x}} p(\hat{x}|x) d(x,\hat{x})\right].$$

Setting the derivative to zero gives $p^*(\hat{x}|x) \propto q(\hat{x}) e^{s \cdot d(x,\hat{x})}$. Given $p(\hat{x}|x)$, the optimal $q$ is $q^*(\hat{x}) = \sum_x p(x) p(\hat{x}|x)$ (the marginal). Alternating these two steps converges by the alternating minimisation lemma. $\square$

## Connections [Master]

- [46.04.01] — Hypothesis testing uses KL divergence as the error exponent; the I-projection of the null onto the alternative's typical set determines the optimal test.
- [46.01.02] — KL divergence and mutual information are the basic quantities that the I-projection minimises.
- [46.03.01] — Channel capacity is computed by Blahut-Arimoto; the capacity-achieving distribution is the fixed point of the I-projection iteration.
- [44.06.01] — Proximal and subgradient methods are the optimisation-theoretic counterparts of Blahut-Arimoto; mirror descent with KL divergence is equivalent.
- [46.02.05] — The rate-distortion function is computed by the dual Blahut-Arimoto algorithm; the same I-projection geometry governs both.

## Historical & philosophical context [Master]

Imre Csiszar introduced the I-projection in his 1975 paper "I-Divergence Geometry of Probability Distributions and Identification Problems" (Periodica Mathematica Hungarica 6(2-3):149-161). Csiszar recognised that the KL divergence endows the probability simplex with a differential-geometric structure, and the I-projection plays the role of orthogonal projection in this geometry. The Pythagorean identity is the information-geometric analogue of the Euclidean Pythagorean theorem.

Richard Blahut and Suguru Arimoto independently discovered the alternating maximisation algorithm for channel capacity in 1972. Blahut's paper ("Computation of Channel Capacity and Rate-Distortion Functions," IEEE Trans. IT 18(4):460-473) presented the algorithm in full generality for both channel capacity and rate-distortion. Arimoto's paper ("An Algorithm for Computing the Capacity of Arbitrary Discrete Memoryless Channels," IEEE Trans. IT 18(1):14-20) focused on channel capacity and proved the monotonic convergence property.

The iterative proportional fitting procedure predates both: it was introduced by Deming and Stephan in 1940 ("On a Least Squares Adjustment of a Sampled Frequency Table When the Expected Marginal Totals Are Known," Annals of Mathematical Statistics 11(4):427-444) for contingency table adjustment. The connection to I-projection and KL divergence was made by Csiszar in 1975, who showed that IPF is alternating I-projection onto marginal constraint sets.

The information geometry perspective was developed by Amari and Nagaoka in their 2000 book *Methods of Information Geometry* (AMS/Oxford), which established the dual flat structure of the exponential family manifold and unified the geometric treatment of I-projection, Pythagorean identities, and alternating projections.

## Bibliography [Master]

```bibtex
@article{csiszar1975,
  author  = {Csisz{\'a}r, I.},
  title   = {I-Divergence Geometry of Probability Distributions and Identification Problems},
  journal = {Periodica Mathematica Hungarica},
  volume  = {6},
  number  = {2-3},
  pages   = {149--161},
  year    = {1975},
}
@article{blahut1972,
  author  = {Blahut, R. E.},
  title   = {Computation of Channel Capacity and Rate-Distortion Functions},
  journal = {IEEE Transactions on Information Theory},
  volume  = {18},
  number  = {4},
  pages   = {460--473},
  year    = {1972},
}
@article{arimoto1972,
  author  = {Arimoto, S.},
  title   = {An Algorithm for Computing the Capacity of Arbitrary Discrete Memoryless Channels},
  journal = {IEEE Transactions on Information Theory},
  volume  = {18},
  number  = {1},
  pages   = {14--20},
  year    = {1972},
}
@article{deming-stephan1940,
  author  = {Deming, W. E. and Stephan, F. F.},
  title   = {On a Least Squares Adjustment of a Sampled Frequency Table When the Expected Marginal Totals Are Known},
  journal = {Annals of Mathematical Statistics},
  volume  = {11},
  number  = {4},
  pages   = {427--444},
  year    = {1940},
}
@book{amari-nagaoka2000,
  author    = {Amari, S. and Nagaoka, H.},
  title     = {Methods of Information Geometry},
  publisher = {AMS/Oxford University Press},
  year      = {2000},
}
@book{cover-thomas2006,
  author    = {Cover, T. M. and Thomas, J. A.},
  title     = {Elements of Information Theory},
  edition   = {2nd},
  publisher = {Wiley},
  year      = {2006},
}
```
