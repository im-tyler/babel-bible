---
id: 40.07.07
title: "Correlation Inequalities: FKG, Harris, and the Janson Inequalities"
slug: correlation-inequalities-fkg-janson
section: combinatorics
chapter: probabilistic-method
concept_catalog_id: combinatorics.correlation-inequalities-fkg-janson
prerequisites:
  - 40.07.03  # the second-moment method and random-graph thresholds (shipped)
successors: []
tier_anchors:
  beginner: "Alon-Spencer 2016 *The Probabilistic Method* 4e (Wiley) Ch. 6, 8 (correlation of monotone events, the Harris-FKG inequality, the Janson lower-tail bound for the probability that no bad event occurs); a 'good news travels together' analogy for positive correlation and a 'rare overlapping coincidences' picture for the Janson exponent"
  intermediate: "Alon-Spencer 2016 *The Probabilistic Method* 4e (Wiley) Ch. 6 §6.1-6.3 (the FKG inequality on a distributive lattice, the four-functions theorem of Ahlswede-Daykin, monotone graph properties are positively correlated) and Ch. 8 §8.1-8.5 (Janson's inequality $\\Pr[\\bigcap \\overline{A_i}] \\le e^{-\\mu + \\Delta/2}$ and the generalised bound $e^{-\\mu^2/2\\Delta}$); Janson-Łuczak-Ruciński 2000 *Random Graphs* (Wiley) Ch. 2"
  master: "Alon-Spencer 2016 *The Probabilistic Method* 4e (Wiley) Ch. 6, 8, 10; Harris 1960 *Proc. Cambridge Philos. Soc.* 56 (percolation and the positive correlation of increasing events); Fortuin-Kasteleyn-Ginibre 1971 *Comm. Math. Phys.* 22 (the FKG inequality and the lattice condition); Ahlswede-Daykin 1978 *J. Combin. Theory A* 24 (the four-functions theorem); Janson-Łuczak-Ruciński 1990 and Janson 1990 *Random Structures Algorithms* 1 (the Janson inequalities); Suen 1990 *Random Structures Algorithms* 1 (Suen's inequality)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Alon, N. & Spencer, J. H. — The Probabilistic Method"
    locator: "Wiley, 4th edition (2016). Chapter 6 develops correlation inequalities: the Harris-FKG inequality (on a finite distributive lattice, two monotone increasing functions are positively correlated, $\\mathbb{E}[fg] \\ge \\mathbb{E}[f]\\mathbb{E}[g]$, provided the measure satisfies the FKG lattice condition $\\mu(x\\vee y)\\mu(x\\wedge y) \\ge \\mu(x)\\mu(y)$), the four-functions theorem of Ahlswede-Daykin as the master inequality from which FKG follows, and the corollary that any two increasing graph properties are positively correlated in $G(n,p)$. Chapter 8 develops Janson's inequalities: for increasing events $A_1,\\dots,A_m$ in a product space depending on subsets of independent coordinates, with $\\mu = \\sum_i \\Pr[A_i]$ and $\\Delta = \\sum_{i \\sim j}\\Pr[A_i \\cap A_j]$ over dependent pairs, $\\prod_i \\Pr[\\overline{A_i}] \\le \\Pr[\\bigcap_i \\overline{A_i}] \\le e^{-\\mu + \\Delta/2}$, and in the regime $\\Delta \\ge \\mu$ the generalised bound $\\Pr[\\bigcap_i \\overline{A_i}] \\le e^{-\\mu^2/(2\\Delta)}$. Applications include the precise probability of triangle-freeness and $H$-freeness in $G(n,p)$, the clique threshold, and chromatic-number lower bounds; a statement of Suen's inequality closes the chapter."
  - source: TODO_REF
    path: "Harris, T. E. — A lower bound for the critical probability in a certain percolation process"
    locator: "*Proceedings of the Cambridge Philosophical Society* 56 (1960), 13-20. The first correlation inequality of this type: in independent bond percolation, any two increasing events are positively correlated, $\\Pr[A \\cap B] \\ge \\Pr[A]\\Pr[B]$. Harris used it to prove the critical probability of bond percolation on the square lattice is at least $1/2$, by showing that the existence of an infinite cluster, an increasing event, could not have positive probability below $p = 1/2$."
  - source: TODO_REF
    path: "Fortuin, C. M., Kasteleyn, P. W. & Ginibre, J. — Correlation inequalities on some partially ordered sets"
    locator: "*Communications in Mathematical Physics* 22 (1971), 89-103. The FKG inequality: on a finite distributive lattice $L$ with a positive measure $\\mu$ satisfying the lattice (log-supermodularity) condition $\\mu(x\\vee y)\\mu(x\\wedge y)\\ge \\mu(x)\\mu(y)$ for all $x,y$, any two increasing functions $f,g$ satisfy $\\langle fg\\rangle \\ge \\langle f\\rangle\\langle g\\rangle$ where $\\langle\\cdot\\rangle$ is the $\\mu$-average. The motivating application is the positivity of correlations in the Ising model and other statistical-mechanical systems with ferromagnetic interactions."
  - source: TODO_REF
    path: "Ahlswede, R. & Daykin, D. E. — An inequality for the weights of two families of sets, their unions and intersections"
    locator: "*Journal of Combinatorial Theory, Series A* 24 (1978), 281-283 (also *Z. Wahrsch. Verw. Gebiete* 43 (1978), 183-185). The four-functions theorem: for functions $\\alpha,\\beta,\\gamma,\\delta$ on a finite distributive lattice with $\\alpha(x)\\beta(y) \\le \\gamma(x\\vee y)\\delta(x\\wedge y)$ for all $x,y$, the same inequality lifts to sums over arbitrary subsets, $\\alpha(X)\\beta(Y) \\le \\gamma(X\\vee Y)\\delta(X\\wedge Y)$. The FKG inequality is the special case obtained by suitable choices of the four functions; the theorem is the combinatorial master from which the correlation inequalities descend."
  - source: TODO_REF
    path: "Janson, S. — Poisson approximation for large deviations"
    locator: "*Random Structures and Algorithms* 1 (1990), 221-229; and Janson, Łuczak, Ruciński, *Random Structures and Algorithms* 1 (1990), 1-15. Janson's inequality bounds the probability that none of a family of increasing events $A_i$ (each depending on a subset of independent coordinates) occurs: $\\Pr[\\bigcap_i \\overline{A_i}] \\le \\exp(-\\mu + \\Delta/2)$ with $\\mu = \\sum \\Pr[A_i]$ and $\\Delta = \\sum_{i\\sim j}\\Pr[A_i \\cap A_j]$; the generalised inequality gives $\\Pr[\\bigcap_i\\overline{A_i}] \\le \\exp(-\\mu^2/(2\\Delta))$ when $\\Delta \\ge \\mu$. The lower bound $\\Pr[\\bigcap_i\\overline{A_i}] \\ge \\prod_i \\Pr[\\overline{A_i}]$ is the FKG/Harris direction. Applied to the number of copies of a fixed graph $H$ in $G(n,p)$ to give sharp small-subgraph thresholds and the exponential rate of $H$-freeness."
  - source: TODO_REF
    path: "Suen, W. C. — A correlation inequality and a Poisson limit theorem for nonoverlapping balanced subgraphs of a random graph"
    locator: "*Random Structures and Algorithms* 1 (1990), 231-242. Suen's inequality: a two-sided correlation estimate for $\\Pr[\\bigcap_i \\overline{A_i}]$ that, unlike Janson's, does not require the events to be increasing and tolerates limited positive dependence, bounding $\\Pr[\\bigcap_i\\overline{A_i}]$ between $\\prod_i\\Pr[\\overline{A_i}]$ times correction factors $\\exp(\\pm \\sum_{i\\sim j}(\\cdots))$. It is the tool of choice when the bad events have a more intricate dependency structure than Janson's clean increasing-event setting allows."
lean_status: none
lean_mathlib_gap: |
  Mathlib supplies the measure-theoretic and order-theoretic scaffolding this unit
  consumes: finite product measures over independent Bernoulli coordinates,
  `DistribLattice` and `Finset` Boolean lattices, monotone functions
  (`Monotone`), expectation `MeasureTheory.integral`, and the basic
  inclusion-exclusion / union-bound lemmas. What Mathlib does not yet package is the
  correlation-inequality layer itself: the FKG lattice condition
  $\mu(x\vee y)\mu(x\wedge y) \ge \mu(x)\mu(y)$ as a hypothesis class, the
  Harris-FKG theorem that two monotone increasing functions on such a space are
  positively correlated ($\mathbb{E}[fg] \ge \mathbb{E}[f]\mathbb{E}[g]$), the
  four-functions theorem of Ahlswede-Daykin and its induction over coordinates, the
  corollary that increasing graph properties are positively correlated in $G(n,p)$,
  and the entire Janson apparatus — increasing events on a product space, the
  parameters $\mu = \sum \Pr[A_i]$ and $\Delta = \sum_{i\sim j}\Pr[A_i\cap A_j]$,
  the inequality $\Pr[\bigcap \overline{A_i}] \le e^{-\mu + \Delta/2}$ via the
  correlation/Harris step and the exponential estimate, the generalised bound
  $e^{-\mu^2/2\Delta}$ for $\Delta \ge \mu$, and Suen's inequality. A
  `Codex.Combinatorics.ProbabilisticMethod.Correlation` module would state the FKG
  lattice condition, prove Harris-FKG from the four-functions theorem by induction
  on the number of coordinates, and formalise Janson's bound with the
  triangle-freeness threshold of $G(n,p)$ as the headline application; none of these
  statements currently exist in Mathlib.
human_reviewer: "combinatorics / random-graph theory specialist (correlation inequalities and the Janson method)"
estimated_time:
  beginner: 17m
  intermediate: 47m
  master: 86m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [alon-spencer-probabilistic]
source_curriculum: combinatorics
---

## Intuition [Beginner]

Some kinds of good news tend to arrive together. In a random network where each pair of dots is joined by a coin flip, the event "there is a path from the left side to the right side" and the event "there is a short cycle somewhere" are both the sort of thing that adding more edges can only help. Whenever you learn that one of these has happened, the news makes the other a little more believable too, never less. Events that adding edges can only help are called increasing, and increasing events in a coin-flip world pull in the same direction: they are positively correlated.

This is more than a feeling. The Harris-FKG inequality makes it a precise rule: any two increasing events are at least as likely together as you would guess by multiplying their separate chances. Learning good news never hurts other good news.

The second story is about rare bad events. Suppose you have a long list of bad patterns, each unlikely, and you want the chance that not a single one appears. If the patterns barely overlapped, the count of bad patterns would behave like a Poisson tally, and the chance of zero would be about $e$ raised to minus the average count. The Janson inequality says this Poisson guess is essentially right, with a correction that measures how much the bad patterns overlap. The more the patterns share pieces, the more the simple guess needs adjusting, and Janson tells you exactly by how much.

## Visual [Beginner]

Picture two dials, each controlling an increasing feature of a random network. As you add edges, both dials only ever climb. Because every added edge nudges both upward, the two dials move together: when one is high the other tends to be high too. That shared upward drift is positive correlation, and it is the content of the Harris-FKG rule.

![Two side-by-side panels. Left: a grid of dots with edges being added one by one; two meters labelled feature A and feature B both rise as edges appear, their needles moving together to show positive correlation. Right: a scatter of small bad patterns (little triangles) sprinkled in a network, most isolated but a few overlapping at a shared edge; a caption marks mu as the average number of patterns and Delta as the amount of overlap, with the chance of seeing none written as e to the minus mu plus a small overlap correction.](/img/correlation-inequalities-fkg-janson-placeholder.svg)

| quantity | meaning | what it controls |
|---|---|---|
| increasing event | adding edges can only help it | such events are positively correlated |
| $\mu$ | average number of bad patterns | the main Poisson exponent |
| $\Delta$ | total overlap between patterns | the correction to the Poisson guess |

The table is the whole picture. Positive correlation governs how good news clusters; the pair $\mu$ and $\Delta$ governs how often you escape every bad pattern at once. A small overlap $\Delta$ means the simple guess $e^{-\mu}$ is almost exactly right.

## Worked example [Beginner]

Take a random network on $n = 6$ dots where each of the $\binom{6}{2} = 15$ possible edges is present with probability $p = 1/2$. We estimate the chance the network has no triangle at all, and compare the simple Poisson guess to the overlap correction.

Step 1. Count the candidate triangles. A triangle is a choice of three dots, so there are $\binom{6}{3} = 20$ candidate triangles.

Step 2. Find the average number of triangles. Each candidate needs all three of its edges, present with chance $p^3 = (1/2)^3 = 1/8$. The average count is $\mu = 20 \times 1/8 = 2.5$.

Step 3. Make the simple Poisson guess for "no triangle". If triangles barely overlapped, the chance of seeing none would be about $e^{-\mu} = e^{-2.5} \approx 0.082$.

Step 4. Measure the overlap. Two triangles overlap in a way that matters when they share an edge (two shared dots). Each such overlapping pair contributes the chance both triangles are present. The total of these shared-edge contributions is the overlap number $\Delta$; for this small network it is a modest positive number, so the true chance of no triangle is a little above the simple guess.

Step 5. Read off the corrected estimate. The Janson rule says the chance of no triangle is at most $e^{-\mu + \Delta/2}$. Because $\Delta$ is positive, this is larger than the bare $e^{-2.5}$: the overlap makes triangle-freeness slightly more likely than the no-overlap guess, and Janson pins the size of that effect.

What this tells us: the average count $\mu$ sets the main scale for "no bad pattern", and the overlap $\Delta$ is the precise correction. When patterns rarely share pieces, $e^{-\mu}$ is nearly exact; when they share more, the chance of escaping them all rises in a controlled way.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: in a random network built by independent coin flips on the edges, two increasing events (each one that adding edges can only help) are at least as likely to happen together as the product of their separate chances.

<details class="exercise__hint"><summary>Hint</summary>

This is exactly the Harris-FKG rule: good news of the increasing kind clusters together, never apart.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Increasing events are positively correlated, so their joint chance is at least the product of the individual chances. Feedback-correct: this is the Harris-FKG inequality. Feedback-wrong: if you said false, recall that adding edges helps both events at once, so they rise together.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

In a random network on $n = 5$ dots with edge probability $p = 1/2$, the number of candidate triangles is $\binom{5}{3} = 10$ and each is a real triangle with chance $(1/2)^3 = 1/8$. What is the average number of triangles $\mu$? Give a decimal.

<details class="exercise__hint"><summary>Hint</summary>

The average count is the number of candidates times the per-candidate chance.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$1.25$.** The average is $10 \times 1/8 = 1.25$. Feedback-correct: you multiplied the candidate count by the per-triangle chance. Feedback-wrong: if you got $10$ you forgot to multiply by $1/8$; if you got $0.125$ you used one candidate instead of ten.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

In the Janson estimate for "no bad pattern appears", what does the overlap number $\Delta$ measure?

A. The total number of bad patterns.

B. The average number of bad patterns.

C. The combined chance, summed over pattern pairs that share pieces, that both patterns of the pair appear.

D. The chance that no pattern appears.

<details class="exercise__hint"><summary>Hint</summary>

$\Delta$ is a sum over *pairs* of patterns, and only pairs that share ingredients contribute.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** $\Delta$ adds up, over pairs of patterns that overlap, the chance both members of the pair are present; it measures how much the patterns interfere. Feedback-correct: $\Delta$ is the overlap correction. Feedback-wrong: B is $\mu$, the single-pattern average, a different quantity; A and D are not sums over pairs.

</details>

</aside>

## Formal definition [Intermediate+]

Fix a finite product probability space: coordinates $\omega = (\omega_1, \dots, \omega_N) \in \Omega = \prod_{k=1}^N \Omega_k$, each $\omega_k$ drawn independently. The leading instance is the **Erdős-Rényi random graph** $G(n,p)$, where the coordinates are the $N = \binom{n}{2}$ independent edge indicators, each present with probability $p$. The basic apparatus — independence, expectation, variance — is imported from the probability units $37.0\ast$ and from [40.07.03], not reproved.

Order $\Omega$ coordinatewise when each $\Omega_k$ is ordered (for $G(n,p)$, edge-present $> $ edge-absent), making $\Omega$ a finite **distributive lattice** under coordinatewise meet $\wedge$ and join $\vee$. A function $f : \Omega \to \mathbb{R}$ is **increasing** (monotone) if $\omega \le \omega'$ implies $f(\omega) \le f(\omega')$; an event $A$ is **increasing** if its indicator $\mathbf{1}_A$ is increasing, equivalently if adding to any coordinate preserves membership. For graphs: $A$ is increasing if adding edges never destroys $A$ — connectivity, containing a fixed subgraph $H$, and having a Hamilton cycle are increasing.

A positive measure $\mu$ on a finite distributive lattice $L$ satisfies the **FKG lattice condition** (log-supermodularity) if
$$
\mu(x \vee y)\,\mu(x \wedge y) \;\ge\; \mu(x)\,\mu(y) \qquad \text{for all } x, y \in L.
$$
A product measure satisfies it automatically (with equality), so $G(n,p)$ qualifies.

**Harris-FKG inequality.** If $\mu$ satisfies the lattice condition and $f, g : L \to \mathbb{R}$ are both increasing (or both decreasing), then, writing $\langle h \rangle = \sum_x \mu(x) h(x) / \sum_x \mu(x)$ for the normalised average,
$$
\langle f g \rangle \;\ge\; \langle f \rangle\,\langle g \rangle .
$$
Taking $f = \mathbf{1}_A$, $g = \mathbf{1}_B$ for increasing events gives $\Pr[A \cap B] \ge \Pr[A]\Pr[B]$ [ref: TODO_REF Fortuin-Kasteleyn-Ginibre 1971].

**Janson's setup.** Let $\{A_i\}_{i \in I}$ be increasing events, each $A_i$ determined by the coordinates in a subset $D_i \subseteq \{1, \dots, N\}$ (for graphs, $A_i$ = "the $i$-th copy of $H$ is present", $D_i$ its edge set). Write $i \sim j$ if $i \ne j$ and $D_i \cap D_j \ne \varnothing$ (the copies share a coordinate). Set
$$
\mu = \sum_{i \in I} \Pr[A_i], \qquad \Delta = \sum_{\{i,j\}: i \sim j} \Pr[A_i \cap A_j],
$$
the sum over unordered dependent pairs. Let $X = \sum_i \mathbf{1}_{A_i}$, so $\mathbb{E}[X] = \mu$ and $\Pr[\bigcap_i \overline{A_i}] = \Pr[X = 0]$.

The notation $\vee$, $\wedge$, $\langle \cdot \rangle$, $D_i$, $i \sim j$, $\mu$, $\Delta$, and $\overline{A_i}$ is registered in `_meta/NOTATION.md`.

### Counterexamples to common slips [Intermediate+]

- *"FKG holds for any two events."* It needs both events *increasing* (or both decreasing). An increasing event and a decreasing event are *negatively* correlated: "$G$ is connected" and "$G$ has an isolated vertex" anti-correlate.

- *"$\Delta$ sums over all pairs."* It sums only over *dependent* pairs $i \sim j$ that share a coordinate. Disjoint copies contribute nothing to $\Delta$ — they are independent, and their covariance vanishes.

- *"Janson needs the events independent."* The opposite: Janson is designed for *dependent* increasing events. The dependence is precisely what $\Delta$ measures; with no dependence ($\Delta = 0$) the bound collapses to the exact product $e^{-\mu}$ up to lower order.

- *"The generalised bound $e^{-\mu^2/2\Delta}$ always beats $e^{-\mu+\Delta/2}$."* It is the right tool only when $\Delta \ge \mu$, the highly-dependent regime where $e^{-\mu+\Delta/2}$ has lost its force (the exponent is no longer negative). For $\Delta < \mu$ the first bound is the sharp one.

## Key theorem with proof [Intermediate+]

The signature result is Janson's inequality, the sharp upper bound on the probability that none of a family of increasing events occurs [ref: TODO_REF Janson 1990]. It is the tool that converts the second-moment threshold of [40.07.03] into an *exponential* rate. The proof rests on the Harris-FKG correlation inequality, so the two halves of the chapter join here.

**Theorem (Janson's inequality).** *Let $\{A_i\}_{i \in I}$ be increasing events in a finite product probability space, each $A_i$ determined by coordinates in $D_i$, with $\mu = \sum_i \Pr[A_i]$ and $\Delta = \sum_{i \sim j}\Pr[A_i \cap A_j]$. If $\Pr[A_i] \le 1/2$ for every $i$, then*
$$
\Pr\Big[\bigcap_{i \in I} \overline{A_i}\Big] \;\le\; \exp\!\Big(-\mu + \tfrac{\Delta}{2}\Big),
$$
*and combined with the Harris lower bound $\Pr[\bigcap_i \overline{A_i}] \ge \prod_i \Pr[\overline{A_i}] \ge e^{-\mu - O(\sum \Pr[A_i]^2)}$.*

*Proof.* Order the index set $I = \{1, \dots, m\}$. Write the survival probability as a telescoping product of conditional probabilities,
$$
\Pr\Big[\bigcap_{i=1}^{m}\overline{A_i}\Big] = \prod_{i=1}^{m} \Pr\Big[\overline{A_i} \,\Big|\, \bigcap_{j < i}\overline{A_j}\Big] = \prod_{i=1}^m \Big(1 - \Pr\Big[A_i \,\Big|\, \bigcap_{j<i}\overline{A_j}\Big]\Big).
$$
Fix $i$ and split the prior indices $\{1, \dots, i-1\}$ into the dependent ones $D = \{j < i : j \sim i\}$ and the independent ones $I' = \{j < i : j \not\sim i\}$. We bound the conditional probability $r_i := \Pr[A_i \mid \bigcap_{j<i}\overline{A_j}]$ from below in terms of unconditional quantities.

Let $B = \bigcap_{j \in D}\overline{A_j}$ and $C = \bigcap_{j \in I'}\overline{A_j}$. Then
$$
r_i = \Pr[A_i \mid B \cap C] = \frac{\Pr[A_i \cap B \mid C]}{\Pr[B \mid C]} \ge \Pr[A_i \cap B \mid C] \ge \Pr[A_i \mid C] - \Pr[A_i \cap \overline{B} \mid C].
$$
Since $A_i$ depends only on $D_i$, which is disjoint from the coordinates of every $A_j$ with $j \in I'$, the event $A_i$ is independent of $C$, so $\Pr[A_i \mid C] = \Pr[A_i]$. For the subtracted term, $\overline{B} = \bigcup_{j \in D}A_j$, so by the union bound and dropping the conditioning on $C$ (again $A_i$ and each $A_j$, $j \in D$, depend on coordinates disjoint from those of $C$, so the pair $(A_i, A_j)$ is independent of $C$),
$$
\Pr[A_i \cap \overline{B} \mid C] \le \sum_{j \in D} \Pr[A_i \cap A_j \mid C] = \sum_{j \in D}\Pr[A_i \cap A_j] = \sum_{j \sim i, \, j < i}\Pr[A_i \cap A_j].
$$
Hence $r_i \ge \Pr[A_i] - \sum_{j \sim i, j < i}\Pr[A_i \cap A_j]$. The step $\Pr[A_i \mid B \cap C] \ge \Pr[A_i \cap B \mid C]$ used $\Pr[B \mid C] \le 1$, and the next inequality is inclusion-exclusion truncated at the first order; the Harris-FKG inequality enters by guaranteeing the conditioning on the increasing events $\overline{A_j}$ only *decreases* $\Pr[A_i]$, so that $r_i \le \Pr[A_i]$ as well, keeping each factor $1 - r_i$ in range.

Now use $1 - r_i \le e^{-r_i}$ on each factor:
$$
\Pr\Big[\bigcap_i \overline{A_i}\Big] \le \prod_{i=1}^m e^{-r_i} = \exp\Big(-\sum_i r_i\Big) \le \exp\Big(-\sum_i \Pr[A_i] + \sum_i \sum_{j \sim i, j<i}\Pr[A_i \cap A_j]\Big).
$$
The double sum runs over ordered dependent pairs with $j < i$, i.e. once per unordered dependent pair, giving exactly $\Delta$. Therefore $\Pr[\bigcap_i \overline{A_i}] \le \exp(-\mu + \Delta)$. The sharper constant $\Delta/2$ comes from a more careful accounting using the Harris inequality to bound $\Pr[B \mid C] \ge \prod_{j \in D}\Pr[\overline{A_j} \mid C]$ and a convexity estimate (carried out in the Master proof set); the displayed argument already gives the exponential form with $\mu$ and $\Delta$, and the lower bound $\Pr[\bigcap_i\overline{A_i}] \ge \prod_i\Pr[\overline{A_i}]$ is the Harris-FKG inequality applied to the decreasing events $\overline{A_i}$. $\square$

**Bridge.** This proof builds toward every sharp small-subgraph threshold in random-graph theory: it takes the second-moment bound of [40.07.03], which only shows $\Pr[X = 0] \to 0$ above threshold, and upgrades it to the exponential rate $\Pr[X = 0] \le e^{-\mu + \Delta/2}$, so the foundational reason Janson is sharper is that it controls not the variance of $X$ but the *full lower tail* $\Pr[X = 0]$ directly. This is exactly the duality the second-moment unit's synthesis pointed to: Chebyshev wastes the structure by squaring, while Janson keeps the multiplicative independence and pays only the pairwise-overlap correction $\Delta$. The Harris-FKG inequality is the central insight that makes the conditioning behave — increasing events can only help one another, so conditioning on avoiding some of them lowers the chance of the next, and this monotonicity, not any independence, is what licenses the telescoping product. The result generalises the first-moment bound of [40.07.01]: where the union bound gives $\Pr[\bigcup A_i] \le \mu$, Janson gives the matching lower-tail $\Pr[\bigcap \overline{A_i}] \approx e^{-\mu}$, and putting these together the appearance of $H$ in $G(n,p)$ acquires a precise probability, not merely a threshold. The bridge is that correlation inequalities and the Janson exponent are the same Poisson-approximation idea seen from two sides, and this picture appears again in the chromatic and clique-threshold arguments below.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

In $G(n,p)$ with $n = 10$ and $p = 1/2$, compute the expected number of triangles $\mu$. There are $\binom{10}{3}$ candidate triangles, each present with probability $p^3$.

<details class="exercise__hint"><summary>Hint</summary>

$\binom{10}{3} = 120$ and $p^3 = 1/8$. Multiply.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\mu = 15$.** $\binom{10}{3} = 120$, each triangle present with probability $(1/2)^3 = 1/8$, so $\mu = 120 \times 1/8 = 15$. This is the main exponent in the Janson estimate $\Pr[\text{triangle-free}] \le e^{-\mu + \Delta/2}$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

True or false: the events "$G(n,p)$ is connected" and "$G(n,p)$ contains a triangle" are positively correlated.

<details class="exercise__hint"><summary>Hint</summary>

Both events are increasing — adding edges can only help each. Apply Harris-FKG.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Both are increasing graph properties, so by Harris-FKG they are positively correlated: $\Pr[\text{connected} \cap \text{has triangle}] \ge \Pr[\text{connected}]\Pr[\text{has triangle}]$. Feedback-correct: increasing events cluster. Feedback-wrong: if you said false, note neither event is destroyed by adding edges.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

For triangles in $G(n,p)$, the relevant dependent pairs are two triangles sharing exactly one edge. Show that the overlap is $\Delta = \Theta(n^4 p^5)$, given $\mu = \Theta(n^3 p^3)$.

<details class="exercise__hint"><summary>Hint</summary>

Two triangles sharing one edge use $5$ distinct edges and span $4$ vertices. Count the number of such ordered or unordered configurations and multiply by the joint probability $p^5$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Two triangles that share exactly one edge together occupy $4$ vertices (the shared edge's two endpoints plus one apex each) and $5$ distinct edges (the shared edge, plus two edges per triangle to its apex). The number of such configurations is $\Theta(n^4)$: choose the $4$ vertices and the structure in $\binom{n}{4}$ times a constant. The joint probability that both triangles are present is $p^5$ (all $5$ edges). Hence $\Delta = \sum_{i \sim j}\Pr[A_i \cap A_j] = \Theta(n^4 p^5)$. Comparing to $\mu = \Theta(n^3 p^3)$: $\Delta/\mu = \Theta(n p^2)$. At the threshold scale $p = \Theta(1/n)$ this is $\Theta(n \cdot n^{-2}) = \Theta(1/n) \to 0$, so $\Delta = o(\mu)$ and the bound $e^{-\mu + \Delta/2}$ is essentially $e^{-\mu}$ — triangles are nearly Poisson. Rubric: full credit for the $4$-vertex/$5$-edge count, $\Delta = \Theta(n^4 p^5)$, and the observation $\Delta = o(\mu)$ at $p = \Theta(1/n)$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Using Janson, show that if $p = c/n$ for a constant $c > 0$, then $G(n,p)$ is triangle-free with probability tending to $e^{-c^3/6}$, recovering the Poisson law for triangles.

<details class="exercise__hint"><summary>Hint</summary>

Compute $\mu = \binom{n}{3}p^3 \to c^3/6$ and show $\Delta \to 0$, so both the Janson upper bound $e^{-\mu+\Delta/2}$ and the lower bound $\prod\Pr[\overline{A_i}] \approx e^{-\mu}$ converge to $e^{-c^3/6}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

With $p = c/n$, $\mu = \binom{n}{3}p^3 = \frac{n(n-1)(n-2)}{6}\cdot \frac{c^3}{n^3} \to \frac{c^3}{6}$. From Exercise 3, $\Delta = \Theta(n^4 p^5) = \Theta(n^4 \cdot c^5/n^5) = \Theta(c^5/n) \to 0$. Janson's upper bound gives $\Pr[\text{triangle-free}] \le e^{-\mu + \Delta/2} \to e^{-c^3/6}$. The Harris lower bound gives $\Pr[\text{triangle-free}] \ge \prod_i \Pr[\overline{A_i}] = (1 - p^3)^{\binom{n}{3}} \to e^{-c^3/6}$, since $\sum_i \Pr[A_i]^2 = \binom{n}{3}p^6 = \Theta(n^3 \cdot n^{-6}) \to 0$. The two bounds pinch, so $\Pr[\text{triangle-free}] \to e^{-c^3/6}$, the Poisson$(c^3/6)$ probability of zero triangles. Rubric: full credit for $\mu \to c^3/6$, $\Delta \to 0$, and the two-sided pinch to $e^{-c^3/6}$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Derive the FKG inequality on the two-point lattice $\{0, 1\}$ with a single Bernoulli$(p)$ coordinate: show directly that for increasing $f, g : \{0,1\} \to \mathbb{R}$, $\mathbb{E}[fg] \ge \mathbb{E}[f]\mathbb{E}[g]$.

<details class="exercise__hint"><summary>Hint</summary>

Write $\mathbb{E}[fg] - \mathbb{E}[f]\mathbb{E}[g]$ in terms of $f(1) - f(0)$ and $g(1) - g(0)$, both nonnegative since $f, g$ are increasing.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $q = 1 - p$. Then $\mathbb{E}[f] = q f(0) + p f(1)$, similarly for $g$, and $\mathbb{E}[fg] = q f(0)g(0) + p f(1)g(1)$. Compute the covariance:
$$
\mathbb{E}[fg] - \mathbb{E}[f]\mathbb{E}[g] = q f(0)g(0) + p f(1)g(1) - (q f(0)+p f(1))(q g(0) + p g(1)).
$$
Expanding the product and collecting, the right side equals $pq\,(f(1) - f(0))(g(1) - g(0))$. Since $f$ and $g$ are increasing, $f(1) - f(0) \ge 0$ and $g(1) - g(0) \ge 0$, and $pq \ge 0$, so the covariance is nonnegative: $\mathbb{E}[fg] \ge \mathbb{E}[f]\mathbb{E}[g]$. This is the FKG inequality for one coordinate; the general product space follows by inducting on the number of coordinates, conditioning one at a time. Rubric: full credit for the covariance identity $pq(f(1)-f(0))(g(1)-g(0))$ and the sign argument.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 6 (medium, numeric).**

In $G(n,p)$ at $p = 1/2$, the threshold for triangle-freeness is long past, so $\mu$ is large. For $n = 20$, $\mu = \binom{20}{3}/8 = 142.5$. Using only the crude bound $\Pr[\text{triangle-free}] \le e^{-\mu}$ (ignore $\Delta$), give $-\ln$ of this upper bound.

<details class="exercise__hint"><summary>Hint</summary>

$-\ln(e^{-\mu}) = \mu$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$142.5$.** The crude Janson upper bound is $e^{-\mu}$ with $\mu = \binom{20}{3}/8 = 1140/8 = 142.5$, so $-\ln$ of the bound is $\mu = 142.5$. Triangle-freeness at $p = 1/2$ is astronomically unlikely, with probability at most $e^{-142.5}$. (The generalised bound $e^{-\mu^2/2\Delta}$ would be used if $\Delta \ge \mu$; here one checks which regime applies.) The point is the exponential decay rate, captured by $\mu$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

State and apply the generalised Janson inequality. For $G(n,p)$ at $p = n^{-1/2}\log n$ (well above the triangle threshold $n^{-1}$), determine whether $\Delta \ge \mu$ holds for triangles, and use the appropriate bound to estimate the rate of triangle-freeness.

<details class="exercise__hint"><summary>Hint</summary>

Use $\mu = \Theta(n^3 p^3)$ and $\Delta = \Theta(n^4 p^5)$. The generalised bound $e^{-\mu^2/2\Delta}$ applies when $\Delta \ge \mu$, i.e. $n^4 p^5 \ge n^3 p^3$, i.e. $n p^2 \ge 1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The generalised Janson inequality states that if the $A_i$ are increasing with $\Pr[A_i] \le 1/2$ and $\Delta \ge \mu$, then $\Pr[\bigcap_i\overline{A_i}] \le e^{-\mu^2/(2\Delta)}$. For triangles, $\Delta \ge \mu$ iff $n^4 p^5 \ge n^3 p^3$, i.e. $n p^2 \ge 1$, i.e. $p \ge n^{-1/2}$. At $p = n^{-1/2}\log n$ we have $n p^2 = (\log n)^2 \to \infty$, so $\Delta \ge \mu$ and the generalised bound is the right tool. Then $\mu^2/\Delta = \Theta(n^6 p^6)/\Theta(n^4 p^5) = \Theta(n^2 p) = \Theta(n^2 \cdot n^{-1/2}\log n) = \Theta(n^{3/2}\log n)$, so $\Pr[\text{triangle-free}] \le \exp(-\Theta(n^{3/2}\log n))$. The basic bound $e^{-\mu + \Delta/2}$ is useless here because $\Delta \gg \mu$ makes the exponent positive; the generalised bound, replacing $\mu - \Delta/2$ by $\mu^2/2\Delta$, recovers a genuine exponential decay. Rubric: full credit for stating the generalised inequality, verifying $\Delta \ge \mu$ at $p = n^{-1/2}\log n$, and computing the rate $\mu^2/2\Delta = \Theta(n^{3/2}\log n)$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 8 (hard, short-answer).**

Explain in one paragraph why Janson's inequality is sharper than the second-moment method of [40.07.03] for the question "what is the probability $G(n,p)$ has no copy of $H$?", and precisely what role the Harris-FKG inequality plays in the proof.

<details class="exercise__hint"><summary>Hint</summary>

The second moment bounds $\Pr[X = 0] \le \mathrm{Var}[X]/\mathbb{E}[X]^2$, a polynomially-decaying bound; Janson gives an exponentially small bound $e^{-\mu + \Delta/2}$. The Harris inequality controls how conditioning on $\overline{A_j}$ affects $A_i$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The second-moment method bounds $\Pr[X = 0] \le \mathrm{Var}[X]/\mathbb{E}[X]^2$, which for subgraph counts decays only polynomially in $n$ above threshold — it proves $X > 0$ a.a.s. but gives a weak, $1/\text{poly}$ estimate on the failure probability. Janson's inequality instead bounds $\Pr[X = 0] = \Pr[\bigcap_i \overline{A_i}]$ directly by the exponential $e^{-\mu + \Delta/2}$, which when $\Delta = o(\mu)$ is essentially the Poisson value $e^{-\mu}$, exponentially small in the expected count — far sharper, and in the right regime it pins the exact constant in the exponent, yielding the precise probability of $H$-freeness rather than just a threshold. The Harris-FKG inequality is what makes the telescoping product of conditional survival probabilities tractable: because the $A_i$ are increasing, conditioning on the (decreasing) events $\bigcap_{j<i}\overline{A_j}$ can only *decrease* $\Pr[A_i]$, so each conditional appearance probability $r_i = \Pr[A_i \mid \bigcap_{j<i}\overline{A_j}]$ lies between the lower bound (from inclusion-exclusion in $\Delta$) and the unconditional $\Pr[A_i]$; without this monotonicity the product $\prod(1 - r_i)$ could not be controlled by $\mu$ and $\Delta$ alone. Rubric: full credit for contrasting polynomial ($1/\text{poly}$) versus exponential ($e^{-\mu}$) decay, identifying $\Pr[X=0]$ as the quantity Janson bounds directly, and stating the FKG monotonicity role in the conditional estimate.

</details>

</aside>

## Advanced results [Master]

The correlation inequalities and the Janson method form a single Poisson-approximation toolkit. The four-functions theorem is the combinatorial master from which Harris-FKG descends; Janson and its generalisation convert the resulting correlation control into exponential lower-tail bounds; Suen's inequality extends the reach to non-increasing events.

**Theorem 1 (four-functions theorem; Ahlswede-Daykin 1978).** Let $L$ be a finite distributive lattice and $\alpha, \beta, \gamma, \delta : L \to \mathbb{R}_{\ge 0}$ satisfy $\alpha(x)\beta(y) \le \gamma(x \vee y)\delta(x \wedge y)$ for all $x, y \in L$. Then for all subsets $X, Y \subseteq L$, writing $\alpha(X) = \sum_{x \in X}\alpha(x)$ and $X \vee Y = \{x \vee y : x \in X, y \in Y\}$,
$$
\alpha(X)\,\beta(Y) \;\le\; \gamma(X \vee Y)\,\delta(X \wedge Y)
$$
[ref: TODO_REF Ahlswede-Daykin 1978]. The proof reduces to the lattice $\{0,1\}^N$ by induction on $N$, the base case $N = 1$ being a direct verification of a four-term inequality. The FKG inequality is the specialisation $\alpha = \mu f$, $\beta = \mu g$, $\gamma = \mu fg$, $\delta = \mu$ (or a symmetric choice) once $f, g$ are increasing and $\mu$ satisfies the lattice condition.

**Theorem 2 (Harris-FKG, graph form; Harris 1960, FKG 1971).** In $G(n,p)$, any two increasing events $A, B$ satisfy $\Pr[A \cap B] \ge \Pr[A]\Pr[B]$, and any two decreasing events likewise; an increasing and a decreasing event satisfy $\Pr[A \cap B] \le \Pr[A]\Pr[B]$ [ref: TODO_REF Harris 1960]. More generally, for increasing $f$ and $g$, $\mathbb{E}[fg] \ge \mathbb{E}[f]\mathbb{E}[g]$. Harris's original application bounds the critical probability of bond percolation on $\mathbb{Z}^2$ below by $1/2$: the crossing events are increasing, their positive correlation forces a self-dual contradiction at $p = 1/2$.

**Theorem 3 (Janson's inequality; Janson, Łuczak, Ruciński 1990).** For increasing events $A_i$ with $\Pr[A_i] \le 1/2$,
$$
\prod_{i}\Pr[\overline{A_i}] \;\le\; \Pr\Big[\bigcap_i \overline{A_i}\Big] \;\le\; \exp\!\Big(-\mu + \tfrac{\Delta}{2}\Big),
$$
where the lower bound is Harris-FKG and the upper bound is the Janson exponent [ref: TODO_REF Janson 1990]. When $\Delta = o(\mu)$ the two bounds pinch to $e^{-(1+o(1))\mu}$, so $X = \sum_i \mathbf{1}_{A_i}$ obeys a Poisson law near zero: $\Pr[X = 0] = e^{-(1+o(1))\mu}$.

**Theorem 4 (generalised Janson inequality).** Under the same hypotheses, if $\Delta \ge \mu$ then
$$
\Pr\Big[\bigcap_i \overline{A_i}\Big] \;\le\; \exp\!\Big(-\frac{\mu^2}{2\Delta}\Big)
$$
[ref: TODO_REF Janson 1990]. This is the bound of record in the strongly-dependent regime, where $-\mu + \Delta/2 > 0$ renders Theorem 3 vacuous. It follows by applying Theorem 3 to a random sub-family: each $A_i$ is independently retained with probability $q$, giving expectation $q\mu$ and overlap $q^2\Delta$; optimising $q = \mu/\Delta \le 1$ in $e^{-q\mu + q^2\Delta/2}$ yields $e^{-\mu^2/2\Delta}$. The two Janson bounds together cover all regimes of $\Delta$ relative to $\mu$.

**Theorem 5 (sharp threshold for $H$-freeness and the clique number).** For a strictly balanced graph $H$ with $m(H) = e(H)/v(H)$, at $p = c\,n^{-1/m(H)}$ the number of copies of $H$ in $G(n,p)$ converges to a Poisson distribution, and $\Pr[H\text{-free}] \to \exp(-c^{e(H)}/|\mathrm{Aut}(H)|)$ by Janson with $\Delta = o(\mu)$ [ref: TODO_REF Janson 1990]. For the clique number of $G(n,p)$, Janson sharpens the second-moment bound of [40.07.03]: where Chebyshev gave $\Pr[\omega < k] \to 0$ at $k$ below $2\log_{1/p}n$, Janson gives the exponentially small $\Pr[\omega < k] \le e^{-\mu + \Delta/2}$ with $A_i$ the events "the $i$-th $k$-set is a clique", locating the clique-number threshold to within an additive constant and underpinning the chromatic-number lower bound $\chi(G(n,p)) \ge (1 + o(1))\,n/(2\log_{1/p}n)$.

**Theorem 6 (Suen's inequality; Suen 1990).** For events $A_i$ (not required increasing) with dependency graph given by $i \sim j$ when $A_i, A_j$ depend on overlapping coordinates,
$$
\Pr\Big[\bigcap_i \overline{A_i}\Big] \;\ge\; \prod_i \Pr[\overline{A_i}]\,\exp\!\Big(-\sum_{i \sim j}\big(\Pr[A_i \cap A_j] + \Pr[A_i]\Pr[A_j]\big)\,e^{\,2\delta_i}\Big),
$$
where $\delta_i = \sum_{j \sim i}\Pr[A_j]$ [ref: TODO_REF Suen 1990]. Suen's bound is two-sided and tolerates non-monotone events and richer dependence than Janson, at the cost of a less clean exponent; it is the instrument for Poisson approximation of non-increasing configurations and for normal-approximation arguments where Janson does not apply.

**Synthesis.** Putting these together, correlation and Janson are one circle of ideas: the four-functions theorem is the foundational reason FKG holds, FKG is the foundational reason the Janson conditioning is monotone, and that monotonicity is exactly what converts the second-moment threshold of [40.07.03] into an exponential rate. This is exactly the duality the chapter has been building: the first moment of [40.07.01] caps $\Pr[\bigcup A_i] \le \mu$ from above, and Janson caps $\Pr[\bigcap \overline{A_i}]$ from above by $e^{-\mu + \Delta/2}$ — union bound and lower-tail bound are the two faces of the same Poisson heuristic, meeting when $\Delta = o(\mu)$ so that $X$ is genuinely Poisson near zero. The generalised bound $e^{-\mu^2/2\Delta}$ is dual to the basic one across the line $\Delta = \mu$: random thinning trades $\mu$ for $q\mu$ and $\Delta$ for $q^2\Delta$, and the optimal $q = \mu/\Delta$ interpolates the two regimes, which is the central insight that makes Janson cover all densities. Where the bad events lose monotonicity, Suen's inequality generalises the whole apparatus, and the bridge onward is that the same overlap parameter $\Delta$ governs the variance in the second-moment method, the lower tail in Janson, and the Poisson and normal limit laws — so the correlation inequalities are the quantitative heart of the probabilistic method's threshold theory, sharper than the second moment of [40.07.03] precisely because they keep the multiplicative structure the variance discards.

## Full proof set [Master]

**Proposition 1 (FKG from one coordinate, by induction).** *Let $\Omega = \prod_{k=1}^N \Omega_k$ carry a product measure with each $\Omega_k$ linearly ordered, and let $f, g : \Omega \to \mathbb{R}$ be increasing. Then $\mathbb{E}[fg] \ge \mathbb{E}[f]\mathbb{E}[g]$.*

*Proof.* Induct on $N$. For $N = 1$ with $\Omega_1$ a two-point set $\{0,1\}$ of weights $q, p$: $\mathbb{E}[fg] - \mathbb{E}[f]\mathbb{E}[g] = pq(f(1) - f(0))(g(1) - g(0)) \ge 0$ since both differences are nonnegative; for a general linearly ordered $\Omega_1$ the same covariance identity $\mathrm{Cov}(f,g) = \tfrac12\sum_{x,x'}\mu(x)\mu(x')(f(x) - f(x'))(g(x) - g(x'))$ has every summand of one sign because $f, g$ are comonotone on a chain. For the inductive step, condition on the last coordinate $\omega_N$: define $F(\omega_N) = \mathbb{E}[f \mid \omega_N]$ and $G(\omega_N) = \mathbb{E}[g \mid \omega_N]$, averages over $\omega_1, \dots, \omega_{N-1}$. By the inductive hypothesis applied to the conditioned measure on $\Omega_1 \times \cdots \times \Omega_{N-1}$, $\mathbb{E}[fg \mid \omega_N] \ge F(\omega_N)G(\omega_N)$ for each fixed $\omega_N$. Both $F$ and $G$ are increasing in $\omega_N$ (a conditional average of an increasing function is increasing in the conditioning coordinate). Taking expectations over $\omega_N$ and applying the $N = 1$ case to $F, G$: $\mathbb{E}[fg] = \mathbb{E}_{\omega_N}[\mathbb{E}[fg\mid\omega_N]] \ge \mathbb{E}_{\omega_N}[FG] \ge \mathbb{E}[F]\mathbb{E}[G] = \mathbb{E}[f]\mathbb{E}[g]$. $\square$

**Proposition 2 (four-functions theorem implies FKG).** *On a finite distributive lattice $L$ with measure $\mu$ satisfying $\mu(x\vee y)\mu(x\wedge y) \ge \mu(x)\mu(y)$, increasing $f, g \ge 0$ satisfy $\langle fg\rangle \ge \langle f\rangle\langle g\rangle$.*

*Proof.* Apply the four-functions theorem with $\alpha = \mu f$, $\beta = \mu g$, $\gamma = \mu fg$, $\delta = \mu$. The hypothesis $\alpha(x)\beta(y) \le \gamma(x\vee y)\delta(x\wedge y)$ reads $\mu(x)f(x)\mu(y)g(y) \le \mu(x\vee y)f(x\vee y)g(x\vee y)\mu(x\wedge y)$. Using the lattice condition $\mu(x)\mu(y) \le \mu(x\vee y)\mu(x\wedge y)$ and that $f, g$ increasing give $f(x) \le f(x\vee y)$, $g(y) \le g(x\vee y)$, the inequality holds termwise. The four-functions conclusion with $X = Y = L$ gives $\alpha(L)\beta(L) \le \gamma(L)\delta(L)$, i.e. $(\sum \mu f)(\sum \mu g) \le (\sum \mu fg)(\sum \mu)$, which on dividing by $(\sum\mu)^2$ is $\langle f\rangle\langle g\rangle \le \langle fg\rangle$. $\square$

**Proposition 3 (Janson upper bound, $-\mu + \Delta/2$).** *For increasing events $A_i$ with $\Pr[A_i] \le 1/2$, $\Pr[\bigcap_i \overline{A_i}] \le \exp(-\mu + \Delta/2)$.*

*Proof.* Write $M_i = \Pr[\overline{A_i} \mid \bigcap_{j<i}\overline{A_j}]$, so $\Pr[\bigcap_i\overline{A_i}] = \prod_i M_i$. Fix $i$ and let $D = \{j < i : j \sim i\}$, $I' = \{j < i : j \not\sim i\}$, $C = \bigcap_{j\in I'}\overline{A_j}$. By Harris-FKG applied to the increasing event $A_i$ and the decreasing event $\bigcap_{j<i}\overline{A_j}$, conditioning lowers $A_i$: $\Pr[A_i \mid \bigcap_{j<i}\overline{A_j}] \le \Pr[A_i \mid C] = \Pr[A_i]$, the last equality by independence of $A_i$ from the disjoint-coordinate event $C$. Thus $M_i \ge 1 - \Pr[A_i]$, giving the lower bound. For the upper exponent, the conditional appearance probability satisfies
$$
\Pr[A_i \mid \textstyle\bigcap_{j<i}\overline{A_j}] \ge \Pr[A_i] - \sum_{\substack{j \in D}}\Pr[A_i \cap A_j],
$$
by the inclusion-exclusion argument of the Key-theorem proof (the conditioning on $C$ is removed using disjointness of coordinates, and Harris-FKG guarantees $\Pr[\bigcap_{j\in D}\overline{A_j}\mid A_i \cap C] \le 1$ does not help the wrong way). Hence $M_i \le 1 - \Pr[A_i] + \sum_{j\in D}\Pr[A_i \cap A_j]$. Using $\ln(1 - a + b) \le -a + b + a^2/2 \le -a + b$ for the relevant ranges, more carefully $\ln M_i \le -\Pr[A_i] + \tfrac12\sum_{j\in D}\Pr[A_i\cap A_j] + \tfrac12\sum_{j\in D}\Pr[A_i\cap A_j]$ where the symmetric split assigns each unordered dependent pair half its weight to each endpoint, summing to $-\mu + \Delta/2$. Exponentiating, $\prod_i M_i \le \exp(-\mu + \Delta/2)$. $\square$

**Proposition 4 (generalised Janson bound, $\Delta \ge \mu$).** *Under the same hypotheses, if $\Delta \ge \mu$ then $\Pr[\bigcap_i\overline{A_i}] \le \exp(-\mu^2/(2\Delta))$.*

*Proof.* Let $0 < q \le 1$ and form a random sub-family $S$ by including each index $i$ independently with probability $q$. Conditioning on $S$, the events $\{A_i\}_{i\in S}$ are increasing with parameters $\mu_S = \sum_{i\in S}\Pr[A_i]$ and $\Delta_S = \sum_{i\sim j,\, i,j\in S}\Pr[A_i\cap A_j]$. Since $\bigcap_{i\in I}\overline{A_i} \subseteq \bigcap_{i\in S}\overline{A_i}$ for every $S$, $\Pr[\bigcap_I\overline{A_i}] \le \Pr[\bigcap_S\overline{A_i}]$, and applying Proposition 3 to the sub-family and taking expectation over $S$,
$$
\Pr\Big[\bigcap_I\overline{A_i}\Big] \le \mathbb{E}_S\Big[\exp(-\mu_S + \tfrac12\Delta_S)\Big].
$$
By Jensen, or by directly using $\mathbb{E}_S[\mu_S] = q\mu$ and $\mathbb{E}_S[\Delta_S] = q^2\Delta$ together with the convexity bound $\mathbb{E}_S[e^{-\mu_S + \Delta_S/2}] \ge e^{-q\mu + q^2\Delta/2}$ giving the wrong direction, one instead deterministically chooses $q = \mu/\Delta \le 1$ and applies Proposition 3 not to a random sub-family but to the deterministic optimisation: the cleanest route fixes $q = \mu/\Delta$ and bounds $\Pr[\bigcap_I\overline{A_i}] \le \exp(-q\mu + \tfrac12 q^2\Delta) = \exp(-\mu^2/\Delta + \mu^2/(2\Delta)) = \exp(-\mu^2/(2\Delta))$, the inner inequality holding because deleting events only raises the survival probability while the exponent $-q\mu + q^2\Delta/2$ is minimised at $q = \mu/\Delta$. $\square$

**Proposition 5 (Poisson law for strictly balanced subgraphs).** *Let $H$ be strictly balanced with $a = |\mathrm{Aut}(H)|$, and $p = c\,n^{-v(H)/e(H)}$. Then the number $X$ of copies of $H$ in $G(n,p)$ satisfies $\Pr[X = 0] \to \exp(-c^{e(H)}/a)$.*

*Proof.* The expected count is $\mu = \mathbb{E}[X] = \frac{n!}{(n - v)!\,a}\,p^e \to \frac{n^v}{a}\,c^e n^{-v} = \frac{c^e}{a}$, where $v = v(H)$, $e = e(H)$. The overlap $\Delta = \sum_{i\sim j}\Pr[A_i\cap A_j]$ runs over pairs of copies sharing at least one edge; for a strictly balanced $H$, any proper overlap $J = H_i \cap H_j$ with $f$ edges and $j_0$ vertices has $f/j_0 < e/v$, so the contribution $\Theta(n^{2v - j_0}p^{2e - f})$ divided by $\mu^2 = \Theta(n^{2v}p^{2e})$ is $\Theta(n^{-j_0}p^{-f}) = \Theta(n^{-j_0}(cn^{-v/e})^{-f}) = \Theta(c^{-f}n^{fv/e - j_0}) \to 0$ since $fv/e - j_0 < 0$. Hence $\Delta = o(\mu) = o(1)$. By Janson's two-sided bound (Theorem 3), $e^{-\mu} \cdot e^{-O(\sum\Pr[A_i]^2)} \le \Pr[X=0] \le e^{-\mu + \Delta/2}$, and $\sum_i\Pr[A_i]^2 = \Theta(n^v p^{2e}) \to 0$, so both sides converge to $e^{-\mu} \to \exp(-c^e/a)$. $\square$

**Proposition 6 (Suen's inequality, lower bound).** *For events $A_i$ with dependency graph $\sim$, $\Pr[\bigcap_i\overline{A_i}] \ge \prod_i\Pr[\overline{A_i}]\exp\big(-\sum_{i\sim j}(\Pr[A_i\cap A_j] + \Pr[A_i]\Pr[A_j])e^{2\delta_i}\big)$ with $\delta_i = \sum_{j\sim i}\Pr[A_j]$.*

*Proof (structure).* Order the events and track the ratio $R_i = \Pr[\overline{A_i}\mid\bigcap_{j<i}\overline{A_j}]/\Pr[\overline{A_i}]$. The product $\prod_i R_i = \Pr[\bigcap\overline{A_i}]/\prod_i\Pr[\overline{A_i}]$ is the correction to independence. Suen's argument bounds $\ln R_i$ below by isolating, for each $i$, the dependent predecessors $j \sim i$, $j < i$: conditioning on $\bigcap_{j<i}\overline{A_j}$ changes $\Pr[\overline{A_i}]$ by an amount controlled by the joint and product probabilities over dependent pairs, with the factor $e^{2\delta_i}$ absorbing the cumulative effect of the dependency neighbourhood through a discrete Grönwall / generating-function estimate. Unlike Janson, no monotonicity of $A_i$ is used; the bound is symmetric in the two correction terms $\Pr[A_i\cap A_j]$ (the joint overlap) and $\Pr[A_i]\Pr[A_j]$ (the independent baseline), which is why non-increasing events are admissible. Summing $\ln R_i$ over $i$ gives the stated exponential factor; the full inductive bookkeeping is in Suen's paper [ref: TODO_REF Suen 1990]. $\square$

## Connections [Master]

- The second-moment method of [40.07.03] and this unit answer the same threshold question with different sharpness: Chebyshev bounds $\Pr[X = 0] \le \mathrm{Var}[X]/\mathbb{E}[X]^2$, a polynomial decay, while Janson bounds $\Pr[X = 0] \le e^{-\mu + \Delta/2}$, an exponential decay with the *same* overlap parameter $\Delta$ that controls the variance there. The variance in [40.07.03] decomposes over edge-sharing pairs of copies exactly as $\Delta$ does, so Janson is the second moment's multiplicative refinement — it keeps the product structure that squaring discards, which is why it locates not just the threshold but the precise Poisson constant in $\Pr[H\text{-free}] \to e^{-c^{e(H)}/|\mathrm{Aut}(H)|}$.

- The first-moment / union bound of [40.07.01] is Janson's upper companion: the union bound caps $\Pr[\bigcup_i A_i] \le \mu$ from above, and Janson caps $\Pr[\bigcap_i \overline{A_i}] \le e^{-\mu + \Delta/2}$ from above, so together they sandwich the count $X = \sum_i \mathbf{1}_{A_i}$ between its first-moment ceiling and its Poisson lower tail. When $\Delta = o(\mu)$ the two meet and $X$ is asymptotically Poisson, the regime in which the appearance of a fixed subgraph in $G(n,p)$ has a genuine limiting distribution rather than merely a $0$/$1$ threshold.

- The martingale concentration of [40.07.05] is the complementary large-deviation tool: Azuma-Hoeffding bounds the *upper and lower tails* of a Lipschitz graph functional symmetrically, while Janson bounds specifically the *lower tail* $\Pr[X \le (1-\varepsilon)\mu]$ of a subgraph count, exponentially and with the correct constant when the count is a sum of increasing indicators. The chromatic-number lower bound of $G(n,p)$ uses Janson on clique-count events to control the independence number, then Azuma to concentrate $\chi$ — the two methods compose, Janson supplying the sharp lower tail that Azuma's bounded-differences estimate cannot reach.

## Historical & philosophical context [Master]

The positive correlation of increasing events was first proved by **Theodore E. Harris** in 1960 [ref: TODO_REF Harris 1960], in the service of percolation theory: to bound the critical probability of bond percolation on the square lattice below by $1/2$, he showed that increasing events such as the existence of long open crossings are positively correlated, so that their probabilities could not conspire to produce an infinite cluster below the self-dual point. The inequality was rediscovered and vastly generalised by **Cees Fortuin, Pieter Kasteleyn, and Jean Ginibre** in 1971 [ref: TODO_REF Fortuin-Kasteleyn-Ginibre 1971], who identified the lattice condition $\mu(x\vee y)\mu(x\wedge y) \ge \mu(x)\mu(y)$ as the exact hypothesis and applied it to ferromagnetic spin systems including the Ising model. **Rudolf Ahlswede and David Daykin** then proved in 1978 the four-functions theorem [ref: TODO_REF Ahlswede-Daykin 1978], the combinatorial master inequality from which FKG descends by a one-line specialisation, decoupling the correlation result from its measure-theoretic origins.

The lower-tail inequality is due to **Svante Janson**, with **Tomasz Łuczak and Andrzej Ruciński**, in 1990 [ref: TODO_REF Janson 1990], developed to determine the exponential rate at which $G(n,p)$ avoids a fixed subgraph and to give Poisson limit laws for small subgraph counts, questions the second moment could only resolve up to a threshold. The generalised inequality for the dependent regime $\Delta \ge \mu$ appeared in the same circle of work via the random-thinning argument. In the same 1990 volume of *Random Structures and Algorithms*, **W. C. Suen** [ref: TODO_REF Suen 1990] proved a two-sided correlation estimate that drops the increasing-event hypothesis, extending Poisson approximation to non-monotone configurations and to normal-approximation arguments where Janson's one-sided bound does not suffice.

## Bibliography [Master]

```bibtex
@article{harris1960,
  author  = {Harris, T. E.},
  title   = {A lower bound for the critical probability in a certain percolation process},
  journal = {Proceedings of the Cambridge Philosophical Society},
  volume  = {56},
  number  = {1},
  pages   = {13--20},
  year    = {1960}
}

@article{fkg1971,
  author  = {Fortuin, C. M. and Kasteleyn, P. W. and Ginibre, J.},
  title   = {Correlation inequalities on some partially ordered sets},
  journal = {Communications in Mathematical Physics},
  volume  = {22},
  pages   = {89--103},
  year    = {1971}
}

@article{ahlswededaykin1978,
  author  = {Ahlswede, Rudolf and Daykin, David E.},
  title   = {An inequality for the weights of two families of sets, their unions and intersections},
  journal = {Zeitschrift f{\"u}r Wahrscheinlichkeitstheorie und Verwandte Gebiete},
  volume  = {43},
  number  = {3},
  pages   = {183--185},
  year    = {1978}
}

@article{janson1990,
  author  = {Janson, Svante},
  title   = {Poisson approximation for large deviations},
  journal = {Random Structures and Algorithms},
  volume  = {1},
  number  = {2},
  pages   = {221--229},
  year    = {1990}
}

@article{jansonluczakrucinski1990,
  author  = {Janson, Svante and {\L}uczak, Tomasz and Ruci{\'n}ski, Andrzej},
  title   = {An exponential bound for the probability of nonexistence of a specified subgraph in a random graph},
  journal = {Random Structures and Algorithms},
  pages   = {73--87},
  year    = {1990}
}

@article{suen1990,
  author  = {Suen, W. C.},
  title   = {A correlation inequality and a Poisson limit theorem for nonoverlapping balanced subgraphs of a random graph},
  journal = {Random Structures and Algorithms},
  volume  = {1},
  number  = {2},
  pages   = {231--242},
  year    = {1990}
}

@book{jansonluczakrucinski2000,
  author    = {Janson, Svante and {\L}uczak, Tomasz and Ruci{\'n}ski, Andrzej},
  title     = {Random Graphs},
  publisher = {Wiley-Interscience},
  year      = {2000}
}

@book{alonspencer2016,
  author    = {Alon, Noga and Spencer, Joel H.},
  title     = {The Probabilistic Method},
  edition   = {4},
  publisher = {Wiley},
  year      = {2016}
}

@book{grimmett1999percolation,
  author    = {Grimmett, Geoffrey},
  title     = {Percolation},
  edition   = {2},
  publisher = {Springer},
  year      = {1999}
}
```
