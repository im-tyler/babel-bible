---
id: 04.12.11
title: Slab function and structure of a tropical manifold
slug: slab-function-structure
section: algebraic-geometry
chapter: tropical
concept_catalog_id: tropical.slab-function-structure
prerequisites:
  - 04.11.04  # Fan and the toric variety X_Σ
  - 04.11.10  # Polytope-fan dictionary; the line bundle L_P
  - 04.12.06  # Nishinou-Siebert correspondence theorem
  - 04.12.07  # Toric degeneration of a Calabi-Yau variety
  - 04.12.08  # Dual intersection complex; tropical manifold B
successors: []
tier_anchors:
  beginner: "Gross 2011 *Tropical Geometry and Mirror Symmetry* (CBMS Reg. Conf. Ser. 114), Lecture 5 informal opening — slab functions as the polynomial gluing data on the codimension-1 walls between chambers"
  intermediate: "Gross 2011 *TGMS* (CBMS 114) Lecture 5; Gross-Siebert 2006 *J. Differential Geom.* 72 *Mirror symmetry via logarithmic degeneration data I* §1-§2"
  master: "Gross-Siebert 2010 *J. Algebraic Geom.* 19 *Mirror symmetry via logarithmic degeneration data II* §1-§4 (slab functions and the consistency-of-structure theorem); Gross-Siebert 2011 *Annals of Mathematics* 174 *From real affine geometry to complex geometry* §2 (the data $(B, \\mathscr{P}, \\varphi, \\mathscr{S})$ and the reconstruction theorem from a consistent structure $\\mathscr{S}$); Gross 2011 *TGMS* CBMS 114 Lecture 5 (textbook synthesis); Carl-Pumperla-Siebert 2010 arXiv:1011.6228 (scattering diagrams from Gross-Siebert log-Gromov-Witten counts; the algorithm assembling the consistent structure from broken lines); Kontsevich-Soibelman 2006 *Birkhäuser* (affine structures with singularities and the non-archimedean SYZ); Gross-Hacking-Keel 2015 *Publ. Math. IHÉS* 122 (the cluster-algebra realisation of the same scattering / slab apparatus); Cox-Little-Schenck 2011 *Toric Varieties* (AMS GSM 124) Ch. 1-3 + Ch. 6 (the toric-coordinate language used at every step)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: true
references:
  - source: TODO_REF
    path: "Gross, M. & Siebert, B. — Mirror symmetry via logarithmic degeneration data II"
    locator: "*Journal of Algebraic Geometry* 19 (4), 679-780 (2010). §1 (data $(B, \\mathscr{P}, \\varphi)$ and the structure $\\mathscr{S}$); §2 (slab functions on codimension-1 cells and walls in the interior of maximal cells); §3 (consistency, scattering, and the order-by-order construction); §4 (the order-1 inconsistency that scattering corrects, illustrated on the focus-focus singularity)."
  - source: TODO_REF
    path: "Gross, M. & Siebert, B. — From real affine geometry to complex geometry"
    locator: "*Annals of Mathematics* 174 (3), 1301-1428 (2011). §2 (formalism of $(B, \\mathscr{P}, \\varphi, \\mathscr{S})$); §4-§5 (the reconstruction algorithm consuming a consistent structure $\\mathscr{S}$ and producing a one-parameter formal smoothing); the slab functions appear throughout §2 as the codimension-1 gluing-cocycle data."
  - source: TODO_REF
    path: "Gross, M. & Siebert, B. — Mirror symmetry via logarithmic degeneration data I"
    locator: "*Journal of Differential Geometry* 72 (2), 169-338 (2006). §1-§2 (the log-Calabi-Yau central fibre $X_0$ and its dual intersection complex $(B, \\mathscr{P})$); §3-§4 (the integral affine structure with singularities along the codimension-2 discriminant locus $\\Delta$); §5 (slab functions emerging as transition functions between toric charts on adjacent maximal cells)."
  - source: TODO_REF
    path: "Gross, M. — Tropical Geometry and Mirror Symmetry"
    locator: "CBMS Regional Conference Series in Mathematics 114, American Mathematical Society (2011). Lecture 5 — Slab functions, walls, and the structure $\\mathscr{S}$ on $(B, \\mathscr{P})$ (textbook synthesis of Gross-Siebert 2010 *JAG* 19 §1-§4); Lecture 4 — preparatory dual intersection complex and integral affine structure with singularities; Lecture 6 — theta functions and consequences of the consistent structure."
  - source: TODO_REF
    path: "Carl, M., Pumperla, M. & Siebert, B. — A tropical view on Landau-Ginzburg models"
    locator: "preprint arXiv:1011.6228 (2010), Acta Mathematica Sinica forthcoming. The algorithm assembling a consistent structure $\\mathscr{S}$ on a polarised tropical manifold $(B, \\mathscr{P}, \\varphi)$ from the Gross-Siebert log Gromov-Witten counts; the broken-line construction that realises slab functions and wall-crossing automorphisms as scattering output."
  - source: TODO_REF
    path: "Kontsevich, M. & Soibelman, Y. — Affine structures and non-Archimedean analytic spaces"
    locator: "in *The Unity of Mathematics*, Progress in Mathematics 244, Birkhäuser, 321-385 (2006). The non-archimedean SYZ proposal: integer affine structures with singularities on a topological base, the scattering-diagram formalism on the universal cover, the wall-crossing automorphism group $V_\\rho$ in a complete topological group — the model on which Gross-Siebert slab functions are built."
  - source: TODO_REF
    path: "Gross, M., Hacking, P. & Keel, S. — Mirror symmetry for log Calabi-Yau surfaces I"
    locator: "*Publications mathématiques de l'IHÉS* 122, 65-168 (2015). The two-dimensional log-Calabi-Yau case; explicit construction of the consistent structure $\\mathscr{S}$ in the surface setting, including a complete description of slab functions on codimension-1 cells and their interaction with the scattering walls of broken lines; cluster-algebra connection."
  - source: TODO_REF
    path: "Cox, D., Little, J. & Schenck, H. — Toric Varieties"
    locator: "Graduate Studies in Mathematics 124, American Mathematical Society (2011). Ch. 1-3 (cones, fans, affine toric varieties, gluing of toric charts); Ch. 6 (toric divisors and line bundles); Ch. 11 (toric morphisms and the cocycle calculus used in slab-function descriptions of adjacent-chamber transitions)."
  - source: TODO_REF
    path: "Strominger, A., Yau, S.-T. & Zaslow, E. — Mirror symmetry is T-duality"
    locator: "*Nuclear Physics B* 479 (1-2), 243-259 (1996). The SYZ heuristic: the base $B$ of a dual special-Lagrangian torus fibration is exactly the kind of integral affine manifold with singularities on which Gross-Siebert structures $\\mathscr{S}$ live; slab functions become the symplectic mirror map's algebraic shadow on $B$."
  - source: TODO_REF
    path: "Gross, M. & Siebert, B. — Logarithmic Gromov-Witten invariants"
    locator: "*Journal of the American Mathematical Society* 26 (2), 451-510 (2013). The log-Gromov-Witten foundations whose curve counts on $(X_0, M_{X_0})$ feed the Carl-Pumperla-Siebert algorithm; explains why the structure constants $f_\\rho$ of slab functions are integer-valued."
  - source: TODO_REF
    path: "Maclagan, D. & Sturmfels, B. — Introduction to Tropical Geometry"
    locator: "Graduate Studies in Mathematics 161, American Mathematical Society (2015). Ch. 3-§3 (tropical hypersurfaces and the dual subdivisions); the elementary tropical-polyhedral background used implicitly when discussing slab loci as codimension-1 cells."
  - source: TODO_REF
    path: "Kontsevich, M. & Soibelman, Y. — Stability structures, motivic Donaldson-Thomas invariants and cluster transformations"
    locator: "preprint arXiv:0811.2435 (2008). The wall-crossing-automorphism formalism abstracted from $(B, \\mathscr{P}, \\mathscr{S})$: pro-nilpotent Lie algebra completions and the consistency condition on scattering diagrams; the algebraic substrate on which slab functions act."
lean_status: partial
lean_module: Codex.AlgGeom.Tropical.SlabFunction
human_reviewer: "TBD"
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 95m
status: shipped
produced_by: claude-opus-4-7 (autonomous production driver, Cycle 12 tropical/mirror-symmetry T1 cluster)
reviewed_by: []
---

## Intuition [Beginner]

A tropical manifold is a piecewise-linear space cut into polyhedral chambers, with integer affine coordinates on each chamber but the gluing between adjacent chambers possibly twisted. When you try to lift this piecewise-linear picture back to a smooth complex Calabi-Yau variety, the gluing data on each chamber is a toric chart, and the data on each wall between two chambers must record how the two toric charts paste together. The polynomial that encodes this pasting is called a **slab function**.

A slab function lives on a codimension-one face — a "slab" between two adjacent maximal chambers — and it is a Laurent polynomial in the toric coordinates that are common to both chambers. The total collection of slab functions and other wall-crossing data is the **structure** of the tropical manifold, written $\mathscr{S}$. The structure is the bookkeeping object: it tells you, at every wall in the manifold, exactly how the two sides of the wall are glued so that the resulting complex manifold is well-defined.

Without slab functions, the chambers would just be isolated toric charts that happen to share a polyhedral boundary. With slab functions, the toric charts are glued into a single coherent smooth complex variety. Slab functions are what turn the combinatorial-tropical skeleton into honest algebraic geometry.

## Visual [Beginner]

A two-panel cartoon. Left panel: a piecewise-linear surface partitioned into polyhedral chambers (think hexagonal patches on a flat region of a tropical manifold), with the codimension-one boundaries between chambers highlighted in colour. Right panel: a zoom on one such boundary slab between two chambers, with a Laurent polynomial $f_\rho = 1 + c_1 z^{m_1} + c_2 z^{m_2}$ labelled along the slab, recording the transition data between the toric charts on the two adjacent chambers.

![A two-panel schematic of a tropical manifold partitioned into polyhedral chambers, with slab functions labelled along the codimension-one boundaries between adjacent chambers.](/img/slab-function-structure-placeholder.svg)

The picture captures the slab function as a piece of polynomial data attached to a codimension-one face of the polyhedral decomposition. Each maximal chamber carries its own toric coordinates; each slab between two chambers carries a polynomial in the coordinates common to both, and this polynomial is exactly the gluing rule.

## Worked example [Beginner]

Consider the simplest substantive tropical manifold: a one-dimensional piecewise-linear interval split into two segments at a single interior point. The two segments are two chambers; the single interior point is one slab.

Step 1. On the left chamber, toric coordinates are a single variable $z$. On the right chamber, toric coordinates are a single variable $w$.

Step 2. At the slab between them, the two coordinates must be identified by a transition rule. The simplest substantive slab function is
$$
f_\rho \;=\; 1 + t \cdot z,
$$
where $t$ is a deformation parameter measuring how far from the central fibre we are. The transition rule says: on the slab, glue $w = z \cdot f_\rho(z) = z + t \cdot z^2$.

Step 3. The resulting one-dimensional complex manifold is a one-parameter family. When $t = 0$, the slab function reduces to $f_\rho = 1$ and the two chambers paste along the identity transition $w = z$, giving a degenerate central fibre. When $t \neq 0$, the slab function deforms the transition, smoothing out the meeting point into a single smooth complex curve.

What this tells us: the slab function is the polynomial that interpolates between the identity central-fibre gluing and the smoothed generic-fibre transition. A whole tropical manifold has many chambers and many slabs; the collection of all slab functions is the structure $\mathscr{S}$, and consistency of $\mathscr{S}$ is what allows the chambers to paste into a single coherent complex Calabi-Yau.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A slab function lives on:

A. A maximal chamber of the polyhedral decomposition.  
B. A codimension-one face between two adjacent maximal chambers.  
C. A vertex of the polyhedral decomposition.  
D. The discriminant locus where the integral affine structure has monodromy.

<details class="exercise__hint"><summary>Hint</summary>

The word "slab" refers to a flat polyhedral region between two chambers — codimension one in the manifold.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** A slab is a codimension-one face between two adjacent maximal chambers of the polyhedral decomposition $\mathscr{P}$. The slab function $f_\rho$ attached to such a face records the polynomial gluing data between the toric charts of the two adjacent chambers. Option A confuses slabs with chambers — chambers carry toric coordinates, not slab functions. Option C confuses codimension-one with codimension-$\dim B$. Option D conflates slabs with the discriminant locus, which is codimension two and carries monodromy data rather than polynomial data.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: the collection of all slab functions on a polarised tropical manifold $(B, \mathscr{P}, \varphi)$, together with the scattering walls in the interiors of maximal cells, makes up the structure $\mathscr{S}$ on $(B, \mathscr{P})$.

<details class="exercise__hint"><summary>Hint</summary>

The structure $\mathscr{S}$ is the union of two kinds of data — the codimension-one slab data and the interior scattering walls.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** A structure $\mathscr{S}$ on $(B, \mathscr{P})$ is defined as the disjoint union of two collections: the slab functions $f_\rho$ on the codimension-one faces $\rho \in \mathscr{P}^{[n-1]}$ of the polyhedral decomposition, and the scattering walls $\mathfrak{d}$ in the interior of maximal cells, each carrying a wall-crossing automorphism. The slabs encode the codimension-one gluing of toric charts on adjacent chambers; the walls encode the higher-order corrections enforcing consistency of the resulting smoothing. Together they form the structure $\mathscr{S}$ that the Gross-Siebert reconstruction algorithm consumes as input.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A polarised tropical manifold has a polyhedral decomposition with $5$ maximal cells. Adjacent maximal cells share codimension-one faces; the number of shared codimension-one faces in this decomposition is $7$. How many slab functions does a structure on this manifold contain?

<details class="exercise__hint"><summary>Hint</summary>

A slab function is attached to each codimension-one face between two adjacent maximal cells.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$7$.** A structure $\mathscr{S}$ contains one slab function $f_\rho$ for every codimension-one face $\rho$ between adjacent maximal cells in the polyhedral decomposition $\mathscr{P}$. The maximal cells themselves do not carry slab functions: they carry toric coordinates. The structure may additionally contain scattering walls in the interiors of maximal cells, but those are counted separately from slabs.

</details>

</aside>

## Formal definition [Intermediate+]

Fix a tropical manifold $(B, \mathscr{P})$ of dimension $n$ in the sense of `[04.12.08]`: a topological manifold $B$ together with a polyhedral decomposition $\mathscr{P}$ and an integral affine structure on $B \setminus \Delta$ with singularities along a codimension-2 closed subset $\Delta \subseteq B$, satisfying the local-models conditions of Gross-Siebert (every point of $B \setminus \Delta$ admits an open affine chart in $\mathbb{R}^n$, and the singularities along $\Delta$ are of the focus-focus or join-up type). Fix in addition a strictly convex piecewise-linear function $\varphi$ on $(B, \mathscr{P})$ — the **polarisation** of the tropical manifold — making $(B, \mathscr{P}, \varphi)$ a polarised tropical manifold.

Write $\mathscr{P}^{[k]}$ for the set of $k$-dimensional cells of $\mathscr{P}$. Maximal cells are elements of $\mathscr{P}^{[n]}$; **slabs** are codimension-one cells in $\mathscr{P}^{[n-1]}$ that lie in $B \setminus \Delta$ and separate two maximal cells. For $\sigma \in \mathscr{P}^{[n]}$ a maximal cell, the tangent space $\Lambda_\sigma := T_\sigma B \cap N \cong \mathbb{Z}^n$ at any interior point of $\sigma$ is the integral lattice of the affine chart on $\sigma$, and the **toric ring** of $\sigma$ is
$$
R_\sigma \;:=\; \mathbb{C}[t, t^{-1}][\Lambda_\sigma] \;=\; \mathbb{C}[t, t^{-1}][z^{m} \mid m \in \Lambda_\sigma],
$$
the Laurent-polynomial ring on $\Lambda_\sigma$ over the deformation parameter ring $\mathbb{C}[t, t^{-1}]$.

**Definition (slab function).** Let $\rho \in \mathscr{P}^{[n-1]}$ be a slab with adjacent maximal cells $\sigma_+, \sigma_- \in \mathscr{P}^{[n]}$. Let $\Lambda_\rho := T_\rho B \cap N \cong \mathbb{Z}^{n-1}$ be the tangent integral lattice of $\rho$, sitting inside both $\Lambda_{\sigma_+}$ and $\Lambda_{\sigma_-}$ as the common codimension-one sublattice. A **slab function** on $\rho$ is a Laurent polynomial
$$
f_\rho \;\in\; \mathbb{C}[t][\Lambda_\rho] \;=\; \mathbb{C}[t][z^{m} \mid m \in \Lambda_\rho]
$$
in the toric coordinates of $\rho$, normalised so that
$$
f_\rho \big|_{t = 0} \;=\; 1 \in \mathbb{C}[\Lambda_\rho],
$$
and required to be **compatible at every codimension-two cell** $\tau \subseteq \overline{\rho}$ with $\tau \in \mathscr{P}^{[n-2]} \cap (B \setminus \Delta)$ — meaning the local slab-function pull-back at $\tau$ is an analytic restriction from a Laurent polynomial in $\Lambda_\tau \otimes \mathbb{C}[t]$ that is itself the slab-function output at $\tau$ for any neighbouring slab.

**Definition (wall).** A **wall** is a codimension-one rational polyhedral subset $\mathfrak{d}$ in the interior of a maximal cell $\sigma \in \mathscr{P}^{[n]}$ (so $\mathfrak{d}$ does not coincide with a face of $\mathscr{P}$) together with a wall-crossing automorphism
$$
\theta_\mathfrak{d} \;\in\; \mathrm{Aut}\big(R_\sigma \big/ (t^N)\big)
$$
of the truncated toric ring of $\sigma$, for some order $N \in \mathbb{Z}_{\geq 1}$. The wall-crossing automorphism is required to be of the form
$$
\theta_\mathfrak{d}(z^m) \;=\; z^m \cdot f_\mathfrak{d}^{\langle n_\mathfrak{d}, m\rangle},
$$
where $f_\mathfrak{d} \in 1 + t \cdot R_\sigma$ is a wall function and $n_\mathfrak{d} \in \Lambda_\sigma^\vee$ is a primitive normal covector to $\mathfrak{d}$ in $\sigma$. The exponent $\langle n_\mathfrak{d}, m\rangle$ is the signed transverse-crossing number of the monomial direction $m$ across $\mathfrak{d}$.

**Definition (structure).** A **structure** $\mathscr{S}$ on $(B, \mathscr{P}, \varphi)$ is a pair $\mathscr{S} = (\mathscr{F}, \mathscr{W})$ consisting of:

- A collection $\mathscr{F} = \{f_\rho\}_{\rho \in \mathscr{P}^{[n-1]} \cap (B \setminus \Delta)}$ of slab functions, one on each slab away from the discriminant locus, normalised as above.

- A locally finite collection $\mathscr{W} = \{(\mathfrak{d}, \theta_\mathfrak{d})\}$ of walls in the interiors of maximal cells, satisfying a **consistency condition** at every codimension-two cell $\tau$: the product of wall-crossing automorphisms around a small loop encircling $\tau$ in $B \setminus \Delta$, taken in the order prescribed by the cyclic ordering of walls and slabs at $\tau$, is the identity automorphism modulo $t^N$ for every $N \geq 1$.

The locally-finite condition means that for every compact $K \subseteq B$ and every $N \geq 1$, only finitely many $(\mathfrak{d}, \theta_\mathfrak{d})$ with $\mathfrak{d} \cap K \neq \emptyset$ have non-identity wall-crossing automorphism modulo $t^N$.

### Counterexamples to common slips

- *A slab function $f_\rho$ is not an arbitrary Laurent polynomial in $\Lambda_\rho$.* The normalisation $f_\rho|_{t = 0} = 1$ is the defining condition: $f_\rho$ is a $t$-deformation of the constant function $1$. Without this normalisation, the central-fibre gluing would not be the identity pasting that the toric-degeneration setup requires.

- *A slab is not the same as a wall.* A slab $\rho \in \mathscr{P}^{[n-1]}$ is a codimension-one face of the polyhedral decomposition $\mathscr{P}$ in the away-from-discriminant region $B \setminus \Delta$; it is a constituent of $(B, \mathscr{P})$. A wall $\mathfrak{d}$ is a separate codimension-one polyhedral subset added in the interior of a maximal cell to enforce consistency; it is not a face of $\mathscr{P}$. Both are codimension one in $B$, but their roles in $\mathscr{S}$ are different: slab functions are imposed input; walls are computed output of the scattering algorithm.

- *The consistency condition is not automatic from a choice of slab functions.* The slab functions $\mathscr{F}$ alone produce a system of toric-chart transition rules that, in general, fails to glue consistently around a codimension-2 cell $\tau$ — the round-trip product around $\tau$ deviates from the identity at order $t$ or higher. The walls $\mathscr{W}$ are added precisely to absorb this deviation: their wall-crossing automorphisms must compose to cancel the slab-function monodromy at every $\tau$. Consistency is a substantive condition that, in the Gross-Siebert programme, is solved order-by-order in $t$ by the scattering algorithm of Kontsevich-Soibelman / Carl-Pumperla-Siebert.

## Key theorem with proof [Intermediate+]

**Theorem (consistency-of-structure, Gross-Siebert 2010).** *Let $(B, \mathscr{P}, \varphi)$ be a polarised tropical manifold of dimension $n$, and let $\mathscr{S} = (\mathscr{F}, \mathscr{W})$ be a consistent structure on it. Then for every order $N \geq 1$ there is a flat $\mathbb{C}[t]/(t^{N+1})$-family of schemes $\mathfrak{X}_N \to \mathrm{Spec}\,\mathbb{C}[t]/(t^{N+1})$ with central fibre the toric central fibre $X_0(B, \mathscr{P}, \varphi)$ of the polarised tropical manifold, and the families $\mathfrak{X}_N$ for $N \to \infty$ assemble into a formal flat family $\hat{\mathfrak{X}} \to \mathrm{Spf}\,\mathbb{C}[[t]]$ whose generic fibre is a smoothing of $X_0$.*

**Proof.** The proof has three movements. We give the intermediate version; the Master tier elaborates the Čech-cohomological framing.

*Step 1 (chartwise scheme structure from the toric data).* Each maximal cell $\sigma \in \mathscr{P}^{[n]}$ carries a toric ring $R_\sigma = \mathbb{C}[t][\Lambda_\sigma]$. The chartwise scheme $\mathrm{Spec}\,R_\sigma/(t^{N+1})$ is the affine toric scheme over $\mathbb{C}[t]/(t^{N+1})$ associated to the cocharacter lattice $\Lambda_\sigma$. The central-fibre scheme $X_0(\sigma) = \mathrm{Spec}\,R_\sigma/(t)$ is the affine toric variety $\mathrm{Spec}\,\mathbb{C}[\Lambda_\sigma]$.

*Step 2 (codimension-one gluing via slab functions).* For adjacent maximal cells $\sigma_+, \sigma_-$ sharing a slab $\rho$, the slab function $f_\rho \in \mathbb{C}[t][\Lambda_\rho]$ defines a transition rule between the toric coordinates on $\sigma_+$ and $\sigma_-$. Specifically, if $z_+ \in \Lambda_{\sigma_+}$ and $z_- \in \Lambda_{\sigma_-}$ are the toric monomials adjacent to $\rho$ (i.e., the primitive normals to $\rho$ pointing into $\sigma_+$ and $\sigma_-$ respectively), the transition is
$$
z_- \;=\; z_+^{-1} \cdot f_\rho \cdot \pi_\rho,
$$
where $\pi_\rho \in R_\sigma$ is a power of $t$ determined by the polarisation $\varphi$ across $\rho$. The relation defines the gluing of the two affine schemes $\mathrm{Spec}\,R_{\sigma_+}/(t^{N+1})$ and $\mathrm{Spec}\,R_{\sigma_-}/(t^{N+1})$ along the codimension-one stratum $\rho$. This is the local model on every slab.

*Step 3 (consistency at codimension two via walls).* At every codimension-two cell $\tau \in \mathscr{P}^{[n-2]} \cap (B \setminus \Delta)$, the slab functions on the slabs adjacent to $\tau$ produce a transition cocycle on the small loop around $\tau$ in $B \setminus \Delta$. By the consistency condition built into $\mathscr{S}$, the product of slab transitions around $\tau$, composed with the wall-crossing automorphisms $\theta_\mathfrak{d}$ of the walls crossing the small loop, is the identity automorphism modulo $t^{N+1}$. Therefore the chartwise schemes $\mathrm{Spec}\,R_\sigma/(t^{N+1})$ glue consistently around every $\tau$.

Combining the three movements: for every $N \geq 1$ the chartwise affine schemes glue into a single flat $\mathbb{C}[t]/(t^{N+1})$-scheme $\mathfrak{X}_N$. The compatibility $\mathfrak{X}_{N+1} \times_{\mathbb{C}[t]/(t^{N+2})} \mathbb{C}[t]/(t^{N+1}) = \mathfrak{X}_N$ is automatic from the chartwise constructions. The formal limit $\hat{\mathfrak{X}} = \varinjlim \mathfrak{X}_N$ is the desired flat formal family with central fibre $X_0$. Effective algebraisation of $\hat{\mathfrak{X}}$ to an honest one-parameter family $\mathfrak{X} \to \mathbb{A}^1$ is the *reconstruction theorem* of `[04.12.09]`; the present theorem provides the order-by-order construction. $\square$

**Bridge.** The consistency-of-structure theorem builds toward `[04.12.09]` Gross-Siebert reconstruction theorem, where the formal family $\hat{\mathfrak{X}}$ is promoted to a one-parameter algebraic smoothing of $X_0$; appears again in `[04.12.12]` theta functions, where the slab-and-wall data $\mathscr{S}$ are exactly what determines the global theta function coefficients on the smoothing; and identifies slab functions with the codimension-one component of the Calabi-Yau Čech cocycle. The foundational reason this works is that toric charts on adjacent maximal cells already paste consistently at the central-fibre level; the slab functions are the order-$t$ deformations of these identity pastings, and consistency at every codimension-two cell forces the order-$t$ deformations to glue into a smooth complex structure. Putting these together generalises the polytope-fan dictionary of `[04.11.10]` from the smooth toric case to the degenerate Calabi-Yau case: in the toric case, the identity slab functions $f_\rho = 1$ paste affine toric charts into the projective toric variety $X_P$; in the Calabi-Yau case, substantive slab functions paste a more elaborate central fibre into a smooth Calabi-Yau via the formal one-parameter family.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Let $(B, \mathscr{P}, \varphi)$ be a polarised tropical manifold of dimension $n = 3$, with polyhedral decomposition $\mathscr{P}$ having $|\mathscr{P}^{[3]}| = 8$ maximal cells. Suppose every codimension-one face is shared between exactly two adjacent maximal cells, and the total number of codimension-one faces in the interior of $B$ is $12$. How many slab functions does a structure on $B$ assign?

<details class="exercise__hint"><summary>Hint</summary>

A slab function is assigned to every codimension-one face in $\mathscr{P}^{[n-1]}$ that lies in $B \setminus \Delta$ and separates two maximal cells.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$12$.** A structure $\mathscr{S}$ assigns one slab function per codimension-one face in $\mathscr{P}^{[2]}$ (for $n = 3$) in the interior of $B$. With $12$ such faces, the structure has $12$ slab functions. Faces in the boundary of $B$ (if any) and faces lying inside the discriminant locus $\Delta$ are excluded; the count above implicitly assumes $\mathscr{P}^{[2]} \cap (B \setminus \Delta) = \mathscr{P}^{[2]}$ minus those on $\partial B$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

A slab function $f_\rho = 1 + a_1 t z + a_2 t^2 z^2 + a_3 t^3 z^3 \in \mathbb{C}[t][z, z^{-1}]$ on a one-dimensional slab is given. What is $f_\rho$ at $t = 0$, and what is the leading-order correction at first order in $t$?

<details class="exercise__hint"><summary>Hint</summary>

Substitute $t = 0$ for the central-fibre value, and read off the coefficient of $t^1$ for the leading correction.

</details>

<details class="exercise__answer"><summary>Answer</summary>

At $t = 0$: $f_\rho|_{t = 0} = 1$, in agreement with the normalisation condition of the slab function. At first order in $t$: $f_\rho \equiv 1 + a_1 t z \pmod{t^2}$. The leading-order correction is the monomial $a_1 t z$. In the order-by-order reconstruction algorithm, the linear-in-$t$ part of the slab function is the order-$1$ deformation of the central-fibre toric gluing, and it is from this data that the order-$1$ inconsistency at codimension-2 cells is computed.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Verify that for two adjacent maximal cells $\sigma_+, \sigma_-$ sharing a slab $\rho$ with primitive normals $z_+$ and $z_-$ respectively, the transition rule $z_- = z_+^{-1} f_\rho \pi_\rho$ is consistent with the toric central-fibre pasting $z_- = z_+^{-1}$ when $t = 0$ and $\pi_\rho|_{t = 0} = 1$.

<details class="exercise__hint"><summary>Hint</summary>

Substitute $t = 0$ into the transition rule and use the normalisation $f_\rho|_{t = 0} = 1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Substituting $t = 0$ into the transition rule:
$$
z_-|_{t = 0} \;=\; z_+^{-1}|_{t = 0} \cdot f_\rho|_{t = 0} \cdot \pi_\rho|_{t = 0} \;=\; z_+^{-1} \cdot 1 \cdot 1 \;=\; z_+^{-1}.
$$
The central-fibre toric pasting requires exactly this: two adjacent toric charts paste by inversion of the primitive monomial. The slab-function deformation $f_\rho = 1 + \mathcal{O}(t)$ and the polarisation power $\pi_\rho = 1 + \mathcal{O}(t)$ (or some integer power of $t$ when the polarisation increases across $\rho$) are both compatible with the identity central-fibre toric pasting. The substantive gluing data appears only at positive order in $t$, which is the content of the order-by-order reconstruction.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 4 (medium, short-answer).**

State the order-$1$ consistency condition that the slab functions must satisfy at a codimension-2 cell $\tau$ in the away-from-discriminant region $B \setminus \Delta$. Explain why this condition forces the existence of compensating walls in general.

<details class="exercise__hint"><summary>Hint</summary>

The condition says that the order-$t$ contribution of the slab-function transitions around $\tau$ must vanish, equivalently the sum of order-$t$ corrections at incident slabs cancels.

</details>

<details class="exercise__answer"><summary>Answer</summary>

At a codimension-2 cell $\tau$, the chamber-by-chamber transitions of the toric coordinates around a small loop encircling $\tau$ in $B \setminus \Delta$ must compose to the identity modulo $t^{N+1}$ for each order $N$. At order $N = 1$, the contribution is the sum
$$
\sum_{\rho \ni \tau} \left( \log f_\rho \right)^{(1)} \otimes n_\rho \;=\; 0 \;\in\; \Lambda_\tau \otimes \mathbb{C},
$$
where the sum runs over slabs $\rho$ containing $\tau$ as a face, the superscript $(1)$ denotes the order-$t$ coefficient, and $n_\rho$ is the primitive normal to $\rho$ at $\tau$. In general, this sum does not vanish from arbitrary choices of slab functions: the slab functions encode the toric gluing on each codimension-one stratum, but the codimension-two intersection of these strata is over-determined. To enforce consistency, additional walls $\mathfrak{d}$ in the interiors of maximal cells incident at $\tau$ are added, each contributing a wall-crossing automorphism $\theta_\mathfrak{d}$. The order-$1$ part of $\theta_\mathfrak{d}$ provides a correction term, and the *scattering theorem* (Kontsevich-Soibelman 2006; Gross-Siebert 2010) shows that for any inconsistency at order $1$ there exists a unique finite collection of walls correcting it. Iterating order-by-order produces the consistent structure $\mathscr{S}$ that the reconstruction theorem requires.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 5 (medium, multiple choice).**

Which of the following best describes the relationship between slab functions and walls in a structure $\mathscr{S} = (\mathscr{F}, \mathscr{W})$?

A. Slab functions and walls are different names for the same object.  
B. Slab functions live on codimension-1 faces of $\mathscr{P}$; walls are additional codimension-1 subsets in the interiors of maximal cells added to enforce consistency at codimension-2 cells.  
C. Walls live on codimension-1 faces of $\mathscr{P}$; slab functions are interior corrections.  
D. Slab functions and walls together form the discriminant locus $\Delta$.

<details class="exercise__hint"><summary>Hint</summary>

The polyhedral decomposition $\mathscr{P}$ provides the slabs (codimension-1 faces). Walls are separate from the cell structure and are added in the interiors of maximal cells.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Slab functions are attached to codimension-1 faces of $\mathscr{P}$ — that is, slabs $\rho \in \mathscr{P}^{[n-1]}$ — and encode the polynomial gluing data between two adjacent maximal cells. Walls are additional codimension-1 subsets in the interiors of maximal cells, not part of the cell structure of $\mathscr{P}$, added by the scattering algorithm to enforce consistency at codimension-2 cells of $\mathscr{P}$. The two types of data are functionally analogous (both are codimension-1 polynomial gluing data) but structurally distinct: slabs are imposed by $\mathscr{P}$, walls are computed by scattering. Option A conflates them. Option C inverts the roles. Option D conflates the structure $\mathscr{S}$ with the discriminant locus $\Delta$, which is codimension-2 and carries integral-affine monodromy.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 6 (hard, short-answer).**

Discuss the role of the polarisation $\varphi$ in determining the powers $\pi_\rho$ of $t$ appearing in the codimension-one transition rule $z_- = z_+^{-1} f_\rho \pi_\rho$. Why is the polarisation essential rather than incidental?

<details class="exercise__hint"><summary>Hint</summary>

The polarisation $\varphi$ is a strictly convex piecewise-linear function on $(B, \mathscr{P})$. Its difference across the slab $\rho$ between the two adjacent chambers gives an integer height jump.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The polarisation $\varphi$ is a strictly convex piecewise-linear function on $(B, \mathscr{P})$ taking integer values on every vertex. Across a slab $\rho$ between two adjacent maximal cells $\sigma_+, \sigma_-$, the function $\varphi$ is affine on each side; the difference of the two affine extensions across $\rho$ is a linear function $\Delta \varphi|_\rho$ on $\rho$ with integer slope. The integer $\kappa_\rho := $ (jump of slope of $\varphi$ across $\rho$) measures the strict-convexity-increase of $\varphi$ across $\rho$.

In the transition rule $z_- = z_+^{-1} f_\rho \pi_\rho$, the factor $\pi_\rho = t^{\kappa_\rho}$ records this polarisation jump as a power of $t$. Without the polarisation, the transition would only contain $z_+^{-1} f_\rho$ — yielding a flat-but-unpolarised family. The polarisation $\pi_\rho = t^{\kappa_\rho}$ makes the family **polarised**: it ensures the line bundle $L \to \mathfrak{X}$ extending the toric polarisation $L_P$ on $X_P$ is well-defined globally, with integer fibre degree given by $\varphi$. Without $\varphi$, the slab transitions could glue the chartwise schemes into a flat family, but the family would lack a global polarisation, and the resulting smoothing would not be projective. The polarisation is therefore essential to the construction of a polarised mirror Calabi-Yau pair, not an optional decoration.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Discuss the role of slab functions in the broken-line construction of theta functions on a polarised tropical manifold.

<details class="exercise__hint"><summary>Hint</summary>

Theta functions $\vartheta_m$ are defined by summing over broken lines ending at a chamber, with coefficients given by products of slab functions and wall-crossing automorphisms along the broken-line path.

</details>

<details class="exercise__answer"><summary>Answer</summary>

A **broken line** on a polarised tropical manifold $(B, \mathscr{P}, \varphi, \mathscr{S})$ is a piecewise-linear path $\gamma : (-\infty, 0] \to B$ that starts at infinity in some unbounded chamber, ends at a chosen base point $Q$ in some chamber, and bends only at intersections with walls or slabs of $\mathscr{S}$. At every bend, the monomial label carried along $\gamma$ is multiplied by a specific term from the relevant slab function $f_\rho$ or wall function $f_\mathfrak{d}$, weighted by the transverse-crossing combinatorics. The theta function indexed by an integral asymptotic direction $m \in B(\mathbb{Z})$ is the sum
$$
\vartheta_m(Q) \;=\; \sum_{\gamma : m \to Q} (\text{monomial label at end of } \gamma),
$$
running over all broken lines starting at infinity in the direction $m$ and ending at $Q$.

The slab functions and walls of $\mathscr{S}$ are exactly the "scattering coefficients" of the broken-line construction: each bend in $\gamma$ contributes a specific summand drawn from $f_\rho$ or $f_\mathfrak{d}$. The structure $\mathscr{S}$ thus determines the theta functions; and conversely, the broken-line construction is well-defined (gives the same $\vartheta_m$ regardless of $Q$ in a given chamber) **if and only if** $\mathscr{S}$ is consistent. Consequently the consistency of $\mathscr{S}$ is exactly the condition that theta functions form a well-defined global module of sections of the polarisation $L$ on the formal family $\hat{\mathfrak{X}}$, generalising the lattice-point monomial basis $H^0(X_P, L_P) = \bigoplus_{m \in P \cap M} \chi^m$ from the toric case of `[04.11.10]` to the Calabi-Yau case of `[04.12.12]`.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Compute the order-$1$ wall-crossing automorphism $\theta_\mathfrak{d}$ for a wall with wall function $f_\mathfrak{d} = 1 + t z^v$ and primitive normal $n_\mathfrak{d}$, applied to a monomial $z^m$. State the result modulo $t^2$.

<details class="exercise__hint"><summary>Hint</summary>

The wall-crossing automorphism is $\theta_\mathfrak{d}(z^m) = z^m \cdot f_\mathfrak{d}^{\langle n_\mathfrak{d}, m\rangle}$. Expand $f_\mathfrak{d}^{\langle n_\mathfrak{d}, m\rangle}$ to first order in $t$ using the binomial / logarithm expansion.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The wall-crossing automorphism is
$$
\theta_\mathfrak{d}(z^m) \;=\; z^m \cdot f_\mathfrak{d}^{\langle n_\mathfrak{d}, m\rangle} \;=\; z^m \cdot (1 + t z^v)^{\langle n_\mathfrak{d}, m\rangle}.
$$
Expanding to first order in $t$:
$$
(1 + t z^v)^{\langle n_\mathfrak{d}, m\rangle} \;\equiv\; 1 + \langle n_\mathfrak{d}, m\rangle \cdot t z^v \pmod{t^2}.
$$
Therefore
$$
\theta_\mathfrak{d}(z^m) \;\equiv\; z^m + \langle n_\mathfrak{d}, m\rangle \cdot t z^{m + v} \pmod{t^2}.
$$
The order-$1$ correction is the term $\langle n_\mathfrak{d}, m\rangle \cdot t z^{m + v}$, with the exponent of the new monomial offset by $v$ (the lattice direction of $z^v$ in the wall function) and the coefficient given by the signed crossing-number $\langle n_\mathfrak{d}, m\rangle$. This formula is the local model for the wall-crossing operations of the Kontsevich-Soibelman scattering algorithm, generalising directly to the slab-function setting where $f_\mathfrak{d}$ is replaced by a slab function $f_\rho$ on a codimension-1 face of $\mathscr{P}$.

</details>

</aside>

## Lean formalisation [Intermediate+]

The Lean module `Codex.AlgGeom.Tropical.SlabFunction` schematises the data of a slab function, a wall, and a structure $\mathscr{S}$ on a polarised tropical manifold. Current Mathlib supplies the basics of Laurent polynomial rings via `Mathlib.RingTheory.LaurentPolynomial`, the truncated polynomial rings via `Mathlib.Algebra.Polynomial.AlgebraMap`, and the lattice / free module infrastructure via `Mathlib.LinearAlgebra.FreeModule.Basic`. The polyhedral-decomposition and integral-affine-with-singularities packages, the toric-degeneration constructions, and the wall-crossing automorphism groups are absent and are the principal Mathlib gaps for the present unit.

The module declares: a `Lattice` placeholder (the integral lattice $\Lambda$ at a chamber), a `PolarisedTropicalManifold` structure recording $(B, \mathscr{P}, \varphi)$ with witnesses for the polyhedral decomposition, the polarisation, and the integral affine structure (deferred to `04.12.08`); a `Slab` type recording a codimension-1 face $\rho$ of $\mathscr{P}$; a `SlabFunction` structure consisting of a Laurent polynomial $f_\rho \in \mathbb{C}[t][\Lambda_\rho]$ together with a witness that $f_\rho|_{t = 0} = 1$; a `Wall` structure recording a codimension-1 subset $\mathfrak{d}$ in the interior of a maximal cell together with a wall-crossing automorphism $\theta_\mathfrak{d}$ of the truncated toric ring; and a `Structure` type packaging the two collections together with a consistency witness.

Two named theorems are recorded with `sorry`-equivalent proof bodies: `structure_glues_orderwise` records the consistency-of-structure theorem (Gross-Siebert 2010), asserting that a consistent structure $\mathscr{S}$ produces a flat $\mathbb{C}[t]/(t^{N+1})$-family for every $N$; and `polarisation_compatibility` records the compatibility of the slab-function transitions with the polarisation $\varphi$, asserting that the $\pi_\rho = t^{\kappa_\rho}$ factor in the transition rule encodes the height jump of $\varphi$ across $\rho$.

The Mathlib gap is: (i) polyhedral decompositions of a topological manifold with integral affine charts and discriminant-locus singularities; (ii) Laurent polynomial rings parametrised by a lattice $\Lambda$ with a deformation parameter $t$; (iii) the wall-crossing automorphism groups of truncated toric rings, requiring pro-nilpotent Lie-algebra completions; (iv) the order-by-order scattering algorithm of Kontsevich-Soibelman / Carl-Pumperla-Siebert; (v) the formal-smoothing-from-consistent-structure theorem of Gross-Siebert 2010 *JAG* 19. The aggregated gap is the natural target for upstream Mathlib contributions extending the present partial Lean development.

## Definitions: slab functions and walls [Master]

We elaborate the technical content of the slab-function and wall definitions, with attention to the integer-affine-with-singularities setting in which they live.

**The polarised tropical manifold revisited.** A **polarised tropical manifold** in the sense of Gross-Siebert 2011 *Annals* 174 §2 is a quadruple $(B, \mathscr{P}, \varphi, \mathscr{S})$ where $B$ is a topological $n$-manifold, $\mathscr{P}$ is a polyhedral cell decomposition of $B$, $\varphi : (B, \mathscr{P}) \to \mathbb{R}$ is a strictly convex piecewise-linear polarisation, and $\mathscr{S}$ is a structure as defined below. The pair $(B, \mathscr{P})$ admits an integral affine atlas away from a closed singular locus $\Delta \subseteq B$ of codimension $2$, with focus-focus / join-up local models near $\Delta$ controlling the local monodromy of the affine charts (Gross-Siebert 2006 *JDG* 72 §3). Each maximal cell $\sigma \in \mathscr{P}^{[n]}$ inherits a global integral affine chart (since $\Delta$ is codimension $2$, every maximal cell is contained in $B \setminus \Delta$).

**Tangent lattices at faces.** For each cell $\tau \in \mathscr{P}^{[k]}$ in the away-from-discriminant region, the **tangent integral lattice** $\Lambda_\tau \subseteq T_\tau B$ is the rank-$k$ subgroup of the integral affine lattice $N \cong \mathbb{Z}^n$ parallel to $\tau$. For $\tau \subseteq \tau'$ a face inclusion, $\Lambda_\tau \subseteq \Lambda_{\tau'}$ is a primitive sublattice. The maximal-cell tangent lattices are rank $n$; the codimension-one slab tangent lattices are rank $n - 1$; the codimension-two cell tangent lattices are rank $n - 2$. The dual lattices $\Lambda_\tau^\vee$ encode normal directions: for a slab $\rho$ with adjacent maximal cells $\sigma_+, \sigma_-$, the quotients $\Lambda_{\sigma_\pm}/\Lambda_\rho \cong \mathbb{Z}$ each carry a canonical primitive generator pointing into the respective chamber, recording the local toric-coordinate change across $\rho$.

**Slab functions in detail.** Let $\rho \in \mathscr{P}^{[n-1]}$ be a slab adjacent to maximal cells $\sigma_+, \sigma_- \in \mathscr{P}^{[n]}$. Denote by $\Lambda_\rho \subseteq \Lambda_{\sigma_+} \cap \Lambda_{\sigma_-}$ the common rank-$(n-1)$ tangent lattice of $\rho$. A **slab function** on $\rho$ is an element
$$
f_\rho \;\in\; \mathbb{C}[t]\big[\Lambda_\rho\big] \;=\; \mathbb{C}[t][z^m \mid m \in \Lambda_\rho]
$$
satisfying:

(a) **Normalisation.** $f_\rho|_{t = 0} = 1 \in \mathbb{C}[\Lambda_\rho]$; equivalently $f_\rho = 1 + t \cdot g_\rho$ for some $g_\rho \in \mathbb{C}[t][\Lambda_\rho]$.

(b) **Positive-Newton condition.** Every monomial $c_m \cdot t^k \cdot z^m$ appearing in $f_\rho$ with $k \geq 1$ has the property that $m$ points "outward" with respect to a chosen orientation of the slab (specifically: $\langle n_\rho, m\rangle \leq 0$ in the convention of Gross-Siebert 2010 *JAG* 19 §1, where $n_\rho \in \Lambda_{\sigma_+}^\vee$ is the primitive outward normal of $\sigma_+$ at $\rho$). This condition records the analytic positivity of the slab-function transition.

(c) **Compatibility at codimension-2.** For every $\tau \in \mathscr{P}^{[n-2]} \cap \overline{\rho} \cap (B \setminus \Delta)$, the restriction $f_\rho|_\tau \in \mathbb{C}[t][\Lambda_\tau]$ — obtained by projecting the monomials of $f_\rho$ to $\Lambda_\tau$ via $\Lambda_\rho \to \Lambda_\tau \oplus (\Lambda_\rho/\Lambda_\tau)$ and integrating out the transverse direction — agrees with the codimension-2 slab-function data at $\tau$.

**Walls in detail.** Let $\sigma \in \mathscr{P}^{[n]}$ be a maximal cell. A **wall** in $\sigma$ is a pair $(\mathfrak{d}, \theta_\mathfrak{d})$ where $\mathfrak{d}$ is a closed rational polyhedral subset of $\sigma$ of dimension $n - 1$, not contained in any face of $\mathscr{P}$, and $\theta_\mathfrak{d}$ is an automorphism of the truncated toric ring $R_\sigma/(t^N)$ of the form
$$
\theta_\mathfrak{d}(z^m) \;=\; z^m \cdot f_\mathfrak{d}^{\langle n_\mathfrak{d}, m\rangle}
$$
where $n_\mathfrak{d} \in \Lambda_\sigma^\vee$ is a primitive covector normal to $\mathfrak{d}$ in $\sigma$, and $f_\mathfrak{d} \in 1 + t \cdot \mathbb{C}[t][\Lambda_\sigma]$ is the **wall function**. The wall-crossing automorphism is uniquely determined by the pair $(\mathfrak{d}, f_\mathfrak{d})$; conversely, the wall function $f_\mathfrak{d}$ is determined by $\theta_\mathfrak{d}$ once a primitive normal $n_\mathfrak{d}$ is chosen.

The wall function $f_\mathfrak{d}$ satisfies a structural condition: its monomials $c_m t^k z^m$ have $\langle n_\mathfrak{d}, m\rangle = 0$ — that is, every monomial of $f_\mathfrak{d}$ is tangent to $\mathfrak{d}$. This ensures the wall-crossing automorphism preserves the codimension and acts non-identically only in the wall-crossing direction.

**The toric central fibre $X_0(B, \mathscr{P}, \varphi)$.** Before assembling a smoothing via slab functions, the polarised tropical manifold determines a toric central-fibre scheme $X_0 = X_0(B, \mathscr{P}, \varphi)$. The construction: for each maximal cell $\sigma \in \mathscr{P}^{[n]}$, take the affine toric variety $X_\sigma = \mathrm{Spec}\,\mathbb{C}[\Lambda_\sigma]$ of the cocharacter lattice; the central fibre $X_0$ is the union $\bigcup_\sigma X_\sigma$ glued along the toric strata indexed by faces of $\mathscr{P}$. The strict convexity of $\varphi$ provides an ample line bundle $L_0 \to X_0$ extending the toric polarisation on each piece. This $(X_0, L_0)$ is the singular degenerate Calabi-Yau pair that the structure $\mathscr{S}$ then smooths.

## The structure $\mathscr{S}$ on $(B, \mathscr{P})$ [Master]

A **structure** $\mathscr{S}$ on a polarised tropical manifold $(B, \mathscr{P}, \varphi)$ is the bookkeeping object recording the codimension-one polynomial gluing data needed to smooth the toric central fibre $X_0$.

**Definition (structure).** A **structure** $\mathscr{S} = (\mathscr{F}, \mathscr{W})$ on $(B, \mathscr{P}, \varphi)$ consists of:

(I) A collection $\mathscr{F} = \{f_\rho\}_{\rho \in \mathscr{P}^{[n-1]} \cap (B \setminus \Delta)}$ of slab functions, one $f_\rho$ on each slab in the away-from-discriminant region, each satisfying the normalisation, positive-Newton, and codimension-2 compatibility conditions above.

(II) A locally finite collection $\mathscr{W} = \{(\mathfrak{d}, \theta_\mathfrak{d})\}$ of walls in the interiors of maximal cells, with wall-crossing automorphisms $\theta_\mathfrak{d}$ as defined above.

(III) **Consistency at codimension two.** For every $\tau \in \mathscr{P}^{[n-2]} \cap (B \setminus \Delta)$ and every $N \geq 1$, the product of slab-function transitions and wall-crossing automorphisms taken in cyclic order around a small loop encircling $\tau$ is the identity modulo $t^{N+1}$ in the appropriate truncated toric ring.

The locally finite condition in (II) is that for every compact $K \subseteq B$ and every $N \geq 1$, only finitely many $(\mathfrak{d}, \theta_\mathfrak{d}) \in \mathscr{W}$ with $\mathfrak{d} \cap K \neq \emptyset$ act non-identically modulo $t^{N+1}$. This local finiteness is what permits the order-by-order assembly of the formal smoothing.

**The wall-crossing automorphism group.** The automorphisms $\theta_\mathfrak{d}$ live in a pro-nilpotent group $V_\rho$ associated to each slab $\rho$ (Kontsevich-Soibelman 2006). Concretely, $V_\rho$ is the group of automorphisms of $\mathbb{C}[t][\Lambda_\sigma]/(t^N)$ of the form $z^m \mapsto z^m \cdot f^{\langle n, m\rangle}$ for $n \in \Lambda_\rho^\vee$ a normal direction and $f \in 1 + t \cdot \mathbb{C}[t][\Lambda_\sigma]/(t^N)$ a normalised polynomial. The group law is composition, taken in the order determined by the angle of $n$ in the normal $1$-circle to $\rho$. Pro-nilpotence ensures convergence of order-by-order products.

**The slab attachment of a wall.** A wall $(\mathfrak{d}, \theta_\mathfrak{d})$ is **attached** to a slab $\rho$ if $\mathfrak{d}$ ends on the slab $\rho$ in the closure of the maximal cell where $\mathfrak{d}$ lives. The collection of walls attached to a slab is locally finite (by the local-finiteness condition on $\mathscr{W}$). The Kontsevich-Soibelman / Gross-Siebert scattering algorithm produces walls attached to slabs in a controlled order-by-order fashion, with each newly-added wall correcting the order-$N$ inconsistency at a specific codimension-2 cell.

**The order-by-order construction.** Given the polarised tropical manifold $(B, \mathscr{P}, \varphi)$ and an initial choice of slab functions $\mathscr{F}^{(1)} = \{f_\rho^{(1)} = 1 + t \cdot \alpha_\rho\}_\rho$ where $\alpha_\rho \in \mathbb{C}[\Lambda_\rho]$ records the linear-in-$t$ coefficient, the scattering algorithm of Carl-Pumperla-Siebert / Kontsevich-Soibelman iterates:

1. *Order $N$ input:* slab functions $\mathscr{F}^{(N)}$ and walls $\mathscr{W}^{(N)}$ truncated at $t^N$.

2. *Consistency check at codimension-2:* for each $\tau \in \mathscr{P}^{[n-2]} \cap (B \setminus \Delta)$, compute the product of order-$N$ transitions and automorphisms around $\tau$.

3. *Wall addition:* for every codimension-2 cell where the order-$N$ check fails, add new walls of order $N + 1$ whose order-$(N+1)$ wall-crossing automorphisms cancel the failure.

4. *Increment:* the augmented $\mathscr{F}^{(N+1)}$ and $\mathscr{W}^{(N+1)}$ are consistent modulo $t^{N+2}$ at all codimension-2 cells.

5. *Iterate:* the procedure terminates at every order in finitely many wall-additions per codimension-2 cell (Carl-Pumperla-Siebert 2010 + Kontsevich-Soibelman 2006), producing a consistent structure $\mathscr{S}$ as $N \to \infty$.

**The Gross-Siebert input.** The slab functions $f_\rho^{(1)}$ at order $1$ are not arbitrary: they are determined by the **log Gromov-Witten counts** of the central fibre $X_0$ via the Gross-Siebert log-Gromov-Witten theory of Gross-Siebert 2013 *JAMS* 26. Specifically, the order-$1$ coefficient $\alpha_\rho$ is a sum over genus-$0$ log-curves on $X_0$ contributing to the slab $\rho$. This is the bridge from the algebraic-enumerative content of `[04.12.06]` (Nishinou-Siebert correspondence) to the slab-function content of the structure: the curve counts on the algebraic side become the polynomial coefficients on the tropical side.

## Scattering diagrams and consistency [Master]

The technical heart of the structure $\mathscr{S}$ is the **consistency condition** at codimension-2 cells of $\mathscr{P}$ — equivalently, the closing-up of the **scattering diagram** built from slabs and walls.

**The scattering diagram.** Around each codimension-2 cell $\tau \in \mathscr{P}^{[n-2]} \cap (B \setminus \Delta)$, the slabs and walls incident at $\tau$ form a local **scattering diagram**. Choose a small open neighbourhood $U_\tau \subseteq B$ of a point of $\tau$ in $B \setminus \Delta$, and a small loop $\gamma$ in $U_\tau \setminus \tau$ encircling $\tau$. Choose a starting chamber $\sigma_\star \in \mathscr{P}^{[n]}$ incident at $\tau$, and a starting monomial $z^m$ in the toric coordinates of $\sigma_\star$.

As $\gamma$ crosses each slab $\rho_i$ (or wall $\mathfrak{d}_j$) in cyclic order, the carried monomial picks up a transition factor:
$$
z^m \;\longmapsto\; z^m \cdot f_{\rho_i}^{\langle n_{\rho_i}, m\rangle} \;\text{ or }\; z^m \cdot f_{\mathfrak{d}_j}^{\langle n_{\mathfrak{d}_j}, m\rangle}.
$$
After traversing the full loop, the carried monomial has been transformed by a total automorphism $\Theta_\tau$ of the truncated toric ring $\mathbb{C}[t][\Lambda_{\sigma_\star}]/(t^{N+1})$.

**Definition (consistency).** The structure $\mathscr{S}$ is **consistent** at $\tau$ to order $N$ if $\Theta_\tau \equiv \mathrm{id} \pmod{t^{N+1}}$. The structure $\mathscr{S}$ is **consistent** if it is consistent at every codimension-2 cell $\tau$ to every order $N \geq 1$.

**The Kontsevich-Soibelman scattering theorem (2006).** *Let $(\mathfrak{d}_1, \theta_1)$ and $(\mathfrak{d}_2, \theta_2)$ be two walls meeting at a point in $\mathbb{R}^n$, with primitive normals $n_1, n_2 \in \Lambda^\vee$ not proportional. Then there exists a unique locally finite collection of additional walls $\{(\mathfrak{d}_k, \theta_k)\}_{k \geq 3}$ in $\mathbb{R}^n$, each contained in the closed half-plane bounded by $\mathfrak{d}_1, \mathfrak{d}_2$, such that the union $\{(\mathfrak{d}_k, \theta_k)\}_{k \geq 1}$ is consistent at the meeting point to every order $N \geq 1$.*

This is the local model for codimension-2 scattering. Applied globally to $(B, \mathscr{P}, \varphi)$ at every codimension-2 cell $\tau$, the theorem produces the walls $\mathscr{W}$ needed to complement the initial slabs $\mathscr{F}^{(1)}$ into a consistent structure $\mathscr{S}$.

**The order-1 obstruction.** The simplest substantive illustration of consistency arises at a **focus-focus singularity** of the integral affine structure (Gross-Siebert 2010 *JAG* 19 §4). At a focus-focus singularity, the local model has two slabs $\rho_1, \rho_2$ meeting at a singular codimension-2 cell $\tau$ which is removed from the away-from-discriminant region. In a punctured neighbourhood, the two slabs carry order-1 functions $f_{\rho_i} = 1 + t z^{m_i}$ with monomials $m_1, m_2 \in \Lambda$ related by the focus-focus monodromy $T_{\text{ff}} : m \mapsto m + \langle n, m\rangle v$ for primitive $n, v$ with $\langle n, v\rangle = 0$. The order-1 transition cocycle around $\tau$ fails to vanish; the order-1 wall added by the scattering algorithm has wall function $f_\mathfrak{d} = 1 + t z^{m_1 + m_2}$ and corrects the cocycle.

**The Gross-Siebert reconstruction algorithm.** Given the order-1 slabs $\mathscr{F}^{(1)}$ determined by the central-fibre log-curve counts, the Carl-Pumperla-Siebert / Gross-Siebert algorithm iterates the Kontsevich-Soibelman scattering theorem at every codimension-2 cell of $(B, \mathscr{P})$:

1. *Initial data.* Slab functions $\mathscr{F}^{(1)} = \{f_\rho^{(1)} = 1 + t \alpha_\rho\}_\rho$ and the empty wall collection $\mathscr{W}^{(1)} = \emptyset$.

2. *Step $N \to N + 1$.* At every codimension-2 cell $\tau$, the order-$N$ inconsistency $\Theta_\tau - \mathrm{id} \in t^{N+1} \cdot \mathrm{Lie}(V_\tau)$ is a Lie-algebra-valued vector. Add new walls at order $N + 1$ near $\tau$ whose order-$(N+1)$ wall functions cancel the inconsistency.

3. *Termination at every $N$.* At each order the number of added walls is finite per codimension-2 cell, by the Kontsevich-Soibelman scattering theorem.

4. *Output.* The infinite-order structure $\mathscr{S} = (\mathscr{F}^{(\infty)}, \mathscr{W}^{(\infty)}) = \varprojlim_N (\mathscr{F}^{(N)}, \mathscr{W}^{(N)})$ is consistent at every $\tau$ and provides the input for the Gross-Siebert reconstruction theorem `[04.12.09]`.

**Synthesis.** The slab-function-and-wall apparatus on a polarised tropical manifold is the codimension-one polynomial bookkeeping that the Gross-Siebert reconstruction theorem consumes; the consistency condition at codimension-2 cells is the cocycle condition the codimension-one data must satisfy to produce a global smoothing. The foundational reason this works is that the toric charts on maximal cells of $\mathscr{P}$ already paste consistently at the central-fibre level, and the slab functions are the order-$t$ deformations of these pastings; consistency at codimension-2 forces the deformations to glue order-by-order into a flat formal family. Putting these together generalises the polytope-fan dictionary of `[04.11.10]` from the toric case to the degenerate Calabi-Yau case: identity slab functions $f_\rho = 1$ in the toric case give the projective toric variety $X_P$, substantive slab functions in the Calabi-Yau case give the smooth mirror. The pattern recurs in `[04.12.12]` theta functions, where the slab functions determine the broken-line coefficients of the canonical $\mathbb{Z}$-basis of sections; this is exactly the structure constants of multiplication on the mirror Calabi-Yau. The bridge is the Carl-Pumperla-Siebert algorithm, identifying the central-fibre log-curve counts of `[04.12.06]` with the order-1 slab-function coefficients, and identifying the higher-order scattering corrections with the higher-order log-curve counts.

## Connections [Master]

- **Dual intersection complex; tropical manifold $B$ [04.12.08].** The slab-function-and-structure apparatus of the present unit is the codimension-one polynomial enhancement of the dual intersection complex from the prerequisite unit. The polyhedral decomposition $\mathscr{P}$ and the integral affine structure with singularities on $B$ are the ambient data; the slab functions $f_\rho$ on codimension-one cells $\rho \in \mathscr{P}^{[n-1]}$ are the additional polynomial data layered on top, making $(B, \mathscr{P})$ into a polarised tropical manifold equipped with a structure $\mathscr{S}$. Conversely, every consistent structure $\mathscr{S}$ presupposes the dual intersection complex as its underlying combinatorial substrate.

- **Toric degeneration of a Calabi-Yau variety [04.12.07].** The Gross-Siebert reconstruction starts from a toric degeneration $\mathfrak{X} \to \mathrm{Spec}\,\mathbb{C}[[t]]$ of a Calabi-Yau variety, whose dual intersection complex is $(B, \mathscr{P})$ and whose codimension-one transition data is recorded by the slab functions of the structure $\mathscr{S}$. The slab function $f_\rho$ on slab $\rho$ is precisely the transition function in the central-fibre Čech cocycle of the toric degeneration, restricted to the codimension-one stratum dual to $\rho$. Conversely, given a consistent structure $\mathscr{S}$ on $(B, \mathscr{P}, \varphi)$, the reconstruction theorem produces a toric degeneration $\mathfrak{X}$ whose codimension-one transitions are exactly the slabs of $\mathscr{S}$.

- **Gross-Siebert reconstruction theorem [04.12.09].** The consistency-of-structure theorem in this unit is the order-by-order input to the Gross-Siebert reconstruction theorem of `[04.12.09]`: every consistent structure $\mathscr{S}$ produces a formal flat family $\hat{\mathfrak{X}} \to \mathrm{Spf}\,\mathbb{C}[[t]]$, and the reconstruction theorem then algebraises this formal family to an honest one-parameter family $\mathfrak{X} \to \mathbb{A}^1$ with generic fibre the mirror Calabi-Yau. The two units pair: `[04.12.09]` consumes the consistent $\mathscr{S}$; the present unit defines $\mathscr{S}$ and proves the order-by-order assembly.

- **Theta function of a polarised tropical manifold [04.12.12].** The broken-line construction of theta functions on a polarised tropical manifold reads directly from the slab functions and walls of the structure $\mathscr{S}$: each broken line bends at slabs and walls, picking up the corresponding monomials as scattering coefficients. The well-definedness of theta functions — independence of the broken-line construction from the choice of base point — is exactly the consistency condition of $\mathscr{S}$. The pair of units `[04.12.11]` and `[04.12.12]` are inverse: the present unit defines $\mathscr{S}$; `[04.12.12]` consumes $\mathscr{S}$ to define $\vartheta_m$.

- **Nishinou-Siebert correspondence theorem [04.12.06].** The order-$1$ slab-function coefficients $\alpha_\rho$ in $f_\rho^{(1)} = 1 + t \alpha_\rho$ are determined by the genus-$0$ log Gromov-Witten counts of curves on the central fibre $X_0$ contributing to the slab $\rho$. The Nishinou-Siebert correspondence theorem expresses these log GW counts as Mikhalkin-style tropical-curve counts in the dual intersection complex $B(\Xi)$. The structure $\mathscr{S}$ thus reads its order-$1$ data directly from the enumerative content of the correspondence theorem; higher-order corrections (the walls of $\mathscr{S}$) are determined by higher-order log GW counts via the Carl-Pumperla-Siebert algorithm.

- **Polytope-fan dictionary; the line bundle $L_P$ [04.11.10].** The toric case of the slab-function-and-structure formalism is exactly the polytope-fan dictionary: when $(B, \mathscr{P}, \varphi)$ is the polarised tropical manifold associated to a polytope $P$ (interior of $P$, cell decomposition into vertex stars, $\varphi$ = strictly convex piecewise-linear support function), all slab functions reduce to $f_\rho = 1$ and the structure $\mathscr{S} = (\{1\}, \emptyset)$ is consistent. The reconstruction theorem applied to this identity structure recovers the projective toric variety $X_P$ with its ample line bundle $L_P$, generalising in the present setup to substantive $\mathscr{F}$ when the central fibre is no longer smooth toric but rather a degenerate Calabi-Yau.

- **Fan and toric variety $X_\Sigma$ [04.11.04].** The local model on every maximal cell $\sigma \in \mathscr{P}^{[n]}$ of $(B, \mathscr{P})$ is the affine toric variety $X_\sigma = \mathrm{Spec}\,\mathbb{C}[\Lambda_\sigma]$, the toric variety of the apex fan in $\Lambda_\sigma^\vee$. The slab functions paste these affine toric pieces into the central fibre $X_0$, exactly as the fan-gluing prescription of `[04.11.04]` pastes affine toric pieces $U_\sigma$ into $X_\Sigma$. The structure $\mathscr{S}$ then deforms this pasting order-by-order in $t$, producing the smooth mirror Calabi-Yau as a one-parameter generic-fibre family.

- **Tropical curve as balanced rational metric graph [04.12.02].** The walls of the structure $\mathscr{S}$ are sub-pieces of tropical hypersurfaces in $B$ — their wall functions $f_\mathfrak{d}$ define codimension-one tropical varieties in the maximal-cell coordinates whose top-cells carry the lattice multiplicities of `[04.12.02]`. Broken lines through the structure are tropical-curve fragments with prescribed boundary conditions on slabs and walls; the genus-0 broken-line ensemble assembling theta functions is a tropical-curve enumeration weighted by slab-and-wall data. The present apparatus is therefore a polynomial-coefficient enrichment of the foundational balanced-metric-graph framework of the prerequisite unit.

## Historical & philosophical context [Master]

The slab function and the structure $\mathscr{S}$ as defined here were introduced by Gross and Siebert in 2006 and 2010 as the codimension-one polynomial bookkeeping object of their mirror-symmetry programme. The Gross-Siebert 2006 *Journal of Differential Geometry* 72 paper [ref: TODO_REF Gross-Siebert 2006] set up the log-Calabi-Yau central fibre and its dual intersection complex with integer affine structure; the Gross-Siebert 2010 *Journal of Algebraic Geometry* 19 paper [ref: TODO_REF Gross-Siebert 2010] introduced slab functions on codimension-one cells, defined the structure $\mathscr{S}$, and proved the consistency-of-structure theorem assembling a formal smoothing of the central fibre. The 2011 *Annals* paper [ref: TODO_REF Gross-Siebert 2011] then promoted the formal smoothing to an algebraic one-parameter family.

The conceptual ancestor of the slab function is the **toric gluing cocycle**: in the construction of a toric variety $X_\Sigma$ from a fan $\Sigma$, the affine toric charts $U_\sigma$ are pasted via Laurent-monomial transitions $z_{\sigma'} = z_\sigma \cdot \chi^{m(\sigma, \sigma')}$ on the overlap. In the degenerate Calabi-Yau setting, the central fibre $X_0$ is built from affine toric pieces glued by plain transitions $\chi^{m(\sigma, \sigma')}$, but smoothing to the generic fibre requires deformation of these transitions to $\chi^{m(\sigma, \sigma')} \cdot f_\rho \cdot t^{\kappa_\rho}$ — and $f_\rho$ is the slab function. The conceptual lineage runs through Kempf-Knudsen-Mumford-Saint-Donat 1973 *Toroidal Embeddings I* (gluing toric pieces via combinatorial data), Mumford 1972 *Compositio Mathematica* (the first explicit Tate-curve construction of an Abelian variety by a slab-function-like cocycle), and the toric mirror-symmetry foundations of Batyrev 1994 *J. Algebraic Geometry* 3 and Hori-Vafa 2000 *arXiv:hep-th/0002222*.

The wall apparatus of the structure $\mathscr{S}$ is due to Kontsevich and Soibelman in their 2006 *Birkhäuser* paper *Affine structures and non-archimedean analytic spaces* [ref: TODO_REF Kontsevich-Soibelman 2006], which introduced scattering diagrams in the non-archimedean SYZ programme. The Carl-Pumperla-Siebert 2010 arXiv preprint [ref: TODO_REF Carl-Pumperla-Siebert 2010] provided the algorithmic recipe assembling a consistent structure $\mathscr{S}$ from the Gross-Siebert log Gromov-Witten counts (Gross-Siebert 2013 *JAMS* 26 [ref: TODO_REF Gross-Siebert 2013]). The full consistency-of-structure theorem appeared in Gross-Siebert 2010 *JAG* 19, with simplified expositions in Gross's 2011 CBMS lecture notes [ref: TODO_REF Gross 2011 CBMS]. The two-dimensional log-Calabi-Yau case was worked out in detail by Gross-Hacking-Keel 2015 *Publications IHÉS* 122 [ref: TODO_REF Gross-Hacking-Keel 2015], connecting slabs and walls to cluster-algebra structure and X-cluster transformations.

The philosophical thread running through the apparatus is the **algebraic realisation of the SYZ heuristic**: Strominger-Yau-Zaslow 1996 [ref: TODO_REF Strominger-Yau-Zaslow 1996] proposed that mirror Calabi-Yau pairs arise as dual special-Lagrangian torus fibrations over a common integral affine base $B$, with mirror symmetry implemented as fibrewise T-duality. Gross-Siebert replace the analytic SYZ heuristic with an algebraic-combinatorial machine: $(B, \mathscr{P}, \varphi, \mathscr{S})$ realises algebraically what SYZ realises symplectically, and the slab function is the algebraic shadow of the symplectic mirror-map transition along a codimension-1 wall in the base.

## Bibliography [Master]

```bibtex
@article{GrossSiebert2010,
  author = {Gross, Mark and Siebert, Bernd},
  title = {Mirror symmetry via logarithmic degeneration data {II}},
  journal = {Journal of Algebraic Geometry},
  volume = {19},
  number = {4},
  year = {2010},
  pages = {679--780},
}

@article{GrossSiebert2006,
  author = {Gross, Mark and Siebert, Bernd},
  title = {Mirror symmetry via logarithmic degeneration data {I}},
  journal = {Journal of Differential Geometry},
  volume = {72},
  number = {2},
  year = {2006},
  pages = {169--338},
}

@article{GrossSiebert2011,
  author = {Gross, Mark and Siebert, Bernd},
  title = {From real affine geometry to complex geometry},
  journal = {Annals of Mathematics},
  volume = {174},
  number = {3},
  year = {2011},
  pages = {1301--1428},
}

@article{GrossSiebert2013,
  author = {Gross, Mark and Siebert, Bernd},
  title = {Logarithmic {Gromov-Witten} invariants},
  journal = {Journal of the American Mathematical Society},
  volume = {26},
  number = {2},
  year = {2013},
  pages = {451--510},
}

@book{Gross2011CBMS,
  author = {Gross, Mark},
  title = {Tropical Geometry and Mirror Symmetry},
  series = {CBMS Regional Conference Series in Mathematics},
  volume = {114},
  publisher = {American Mathematical Society},
  year = {2011},
}

@article{CarlPumperlaSiebert2010,
  author = {Carl, Michael and Pumperla, Max and Siebert, Bernd},
  title = {A tropical view on {Landau-Ginzburg} models},
  journal = {preprint arXiv:1011.6228},
  year = {2010},
}

@article{KontsevichSoibelman2006,
  author = {Kontsevich, Maxim and Soibelman, Yan},
  title = {Affine structures and non-{A}rchimedean analytic spaces},
  booktitle = {The Unity of Mathematics},
  series = {Progress in Mathematics},
  volume = {244},
  publisher = {Birkh{\"a}user},
  year = {2006},
  pages = {321--385},
}

@article{GrossHackingKeel2015,
  author = {Gross, Mark and Hacking, Paul and Keel, Sean},
  title = {Mirror symmetry for log {Calabi-Yau} surfaces {I}},
  journal = {Publications math{\'e}matiques de l'IH{\'E}S},
  volume = {122},
  year = {2015},
  pages = {65--168},
}

@book{CoxLittleSchenck2011,
  author = {Cox, David A. and Little, John B. and Schenck, Henry K.},
  title = {Toric Varieties},
  series = {Graduate Studies in Mathematics},
  volume = {124},
  publisher = {American Mathematical Society},
  year = {2011},
}

@article{StromingerYauZaslow1996,
  author = {Strominger, Andrew and Yau, Shing-Tung and Zaslow, Eric},
  title = {Mirror symmetry is {T}-duality},
  journal = {Nuclear Physics B},
  volume = {479},
  number = {1-2},
  year = {1996},
  pages = {243--259},
}

@book{MaclaganSturmfels2015,
  author = {Maclagan, Diane and Sturmfels, Bernd},
  title = {Introduction to Tropical Geometry},
  series = {Graduate Studies in Mathematics},
  volume = {161},
  publisher = {American Mathematical Society},
  year = {2015},
}

@article{KontsevichSoibelman2008,
  author = {Kontsevich, Maxim and Soibelman, Yan},
  title = {Stability structures, motivic {D}onaldson-{T}homas invariants and cluster transformations},
  journal = {preprint arXiv:0811.2435},
  year = {2008},
}

@incollection{KempfKnudsenMumfordSaintDonat1973,
  author = {Kempf, George and Knudsen, Finn and Mumford, David and Saint-Donat, Bernard},
  title = {Toroidal Embeddings {I}},
  series = {Lecture Notes in Mathematics},
  volume = {339},
  publisher = {Springer},
  year = {1973},
}

@article{Batyrev1994,
  author = {Batyrev, Victor V.},
  title = {Dual polyhedra and mirror symmetry for {C}alabi-{Y}au hypersurfaces in toric varieties},
  journal = {Journal of Algebraic Geometry},
  volume = {3},
  year = {1994},
  pages = {493--535},
}
```
