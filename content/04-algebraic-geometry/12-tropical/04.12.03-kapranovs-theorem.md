---
id: 04.12.03
title: Kapranov's theorem (fundamental theorem of tropical geometry)
slug: kapranovs-theorem
section: alg-geom
chapter: tropical
concept_catalog_id: tropical.kapranov-theorem
prerequisites:
  - 04.11.01  # algebraic torus and character/cocharacter lattices
  - 04.12.01  # tropical semiring and tropical polynomial
successors: []
tier_anchors:
  beginner: "Maclagan-Sturmfels *Introduction to Tropical Geometry* (AMS GSM 161, 2015) §1.1 — the three-rayed tropical line `min(x, y, 0)` arising from `x + y + 1 = 0`"
  intermediate: "Maclagan-Sturmfels Ch. 3 (the fundamental theorem); Einsiedler-Kapranov-Lind 2006 *Non-archimedean amoebas and tropical varieties*, J. Reine Angew. Math. 601, 139-157"
  master: "Einsiedler-Kapranov-Lind 2006 *Non-archimedean amoebas and tropical varieties*, J. Reine Angew. Math. 601 (originator written form); Kapranov 2000 (Hannover talks, unpublished, the original three-way equivalence); Bieri-Groves 1984 *The geometry of the set of characters induced by valuations*, J. Reine Angew. Math. 347, 168-195 (the polyhedral finiteness result that Kapranov sharpens); Maclagan-Sturmfels 2015 *Introduction to Tropical Geometry* (canonical modern monograph); Sturmfels 2002 *Solving Systems of Polynomial Equations* (CBMS 97) §9 (Gröbner-fan formulation); Speyer-Sturmfels 2004 *The tropical Grassmannian* (early worked-example sharpening); Gubler 2013 *A guide to tropicalizations* (Berkovich-analytic viewpoint)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Einsiedler, Kapranov, Lind — Non-archimedean amoebas and tropical varieties"
    locator: "Journal für die reine und angewandte Mathematik (Crelle's Journal), vol. 601 (2006), pp. 139-157; the canonical written reference for the three-way equivalence (valuation-image = initial-ideal locus = amoeba limit) attributed to Kapranov's 2000 Hannover talks"
  - source: TODO_REF
    path: "Maclagan, Sturmfels — Introduction to Tropical Geometry"
    locator: "American Mathematical Society Graduate Studies in Mathematics, vol. 161 (2015); Chapter 3 develops the fundamental theorem in book form; Chapter 2 builds the Gröbner-fan / initial-ideal machinery; §3.2 contains the proof of the equivalence of definitions; §3.3 establishes the Bieri-Groves polyhedral / pure-dimension structure theorem"
  - source: TODO_REF
    path: "Bieri, Groves — The geometry of the set of characters induced by valuations"
    locator: "Journal für die reine und angewandte Mathematik (Crelle's Journal), vol. 347 (1984), pp. 168-195; the originator paper on polyhedral structure of valuation images of subgroups of algebraic tori — Kapranov's theorem is the ideal-theoretic upgrade"
  - source: TODO_REF
    path: "Sturmfels — Solving Systems of Polynomial Equations"
    locator: "CBMS Regional Conference Series in Mathematics, vol. 97 (2002), American Mathematical Society; Chapter 9 develops the Gröbner-fan / initial-ideal formulation that is the computational backbone of the fundamental theorem"
  - source: TODO_REF
    path: "Speyer, Sturmfels — The tropical Grassmannian"
    locator: "Advances in Geometry, vol. 4 (2004), pp. 389-411; the first worked extended example of Kapranov's theorem at non-toric scale (Plücker ideal of the Grassmannian Gr(2,n)); identification of trop(Gr(2,n)) with the space of phylogenetic trees"
  - source: TODO_REF
    path: "Gubler — A guide to tropicalizations"
    locator: "in *Nonarchimedean and Tropical Geometry* (Simons Symposia, 2013), pp. 125-189; the Berkovich-analytic reformulation of Kapranov's theorem as the image of the Berkovich-analytification under the tropicalisation map"
  - source: TODO_REF
    path: "Mikhalkin — Enumerative tropical algebraic geometry in R^2"
    locator: "Journal of the American Mathematical Society, vol. 18 (2005), pp. 313-377; the application of Kapranov's theorem to counting plane curves of given genus and degree through prescribed points (Mikhalkin's correspondence theorem)"
  - source: TODO_REF
    path: "Itenberg-Mikhalkin-Shustin — Tropical Algebraic Geometry"
    locator: "Oberwolfach Seminars, vol. 35, Birkhäuser (2007); the textbook companion to Mikhalkin's program, with the fundamental theorem developed in Chapter 1"
lean_status: partial
lean_module: Codex.AlgGeom.Tropical.KapranovTheorem
lean_mathlib_gap: |
  Mathlib has commutative-ring infrastructure, multivariate Laurent
  polynomials (`Mathlib.RingTheory.Polynomial.Laurent`), and partial
  valuation-theory modules (`Mathlib.RingTheory.Valuation.Basic`); it
  lacks (a) the algebraically closed non-archimedean valued fields most
  natural for tropical geometry (Puiseux series
  `K = ⋃_d \mathbb{C}((t^{1/d}))`, Hahn series, completion of the
  algebraic closure of `\mathbb{C}((t))`); (b) named initial-ideal
  calculus for multivariate Laurent rings (the operator
  `\mathrm{in}_w(I)` and its functorial properties); (c) the
  Bieri-Groves polyhedral / pure-dimension structure result on
  valuation images of subgroups of `(K^*)^n`; (d) non-archimedean
  amoebas and their Hausdorff / Hausdorff-Gromov limit theorems; (e)
  the Hensel-lifting machinery in the strong form needed for the
  Kapranov direction (lifting a residual root of `\mathrm{in}_w(I)`
  to a point of `V(I)(K)` with prescribed coordinate valuations). A
  Mathlib-grade development would package `(K, \mathrm{val})` as a
  named `Mathlib.AlgebraicGeometry.Tropicalisation` namespace, supply
  the three constructions `tropByValuation`, `tropByInitial`,
  `tropByAmoeba`, prove the three-way equivalence theorem, and prove
  the Bieri-Groves polyhedral-and-pure-dimension structure result; the
  module would unlock the entire tropical-geometry chapter and the
  downstream Mikhalkin correspondence, Nishinou-Siebert toric
  degenerations, and Gross-Siebert mirror-symmetry programs.
human_reviewer: "TBD"
estimated_time:
  beginner: 18m
  intermediate: 40m
  master: 75m
status: shipped
produced_by: claude-opus-4-7 (autonomous batch — tropical chapter T1 frontier, cycle 10)
reviewed_by: []
---

## Intuition [Beginner]

Tropical geometry is what happens to algebraic geometry when you replace ordinary addition and multiplication by the two operations of taking the minimum and adding. The polynomial $x + y + 1$ becomes the function $\min(x, y, 0)$, and the curve $x + y + 1 = 0$ becomes the locus where the minimum of the three values $x$, $y$, $0$ is attained at least twice.

That locus is a three-rayed picture in the plane — an upward ray along the diagonal, a leftward horizontal ray, and a downward vertical ray, all meeting at the origin. It is the simplest tropical curve. Kapranov's theorem says that this passage from algebra to combinatorics is not a fluke of one example: every algebraic variety has a faithful combinatorial shadow on the side of piecewise-linear geometry.

There are three apparently distinct ways to make this shadow precise. One says: take a point of the variety in a field with a sensible notion of size, record the size of each coordinate, and collect everything you get. Another says: weight your variables, look at the leading terms of every polynomial in your ideal under that weighting, and ask whether any of them is a pure monomial. The third says: take an amoeba — the image of the variety under coordinate-wise absolute-value-then-log — and watch what happens as you crank up a parameter so the picture sharpens to a polyhedral skeleton. Kapranov's theorem says all three procedures give the same answer.

Why is this useful? Because polyhedral combinatorics is dramatically easier to compute with than algebraic geometry, and many questions about the original variety — its dimension, its intersection numbers, the topology of its real points, the count of curves passing through a given set of points — descend to questions about polyhedral fans and integer lattices. The tropical world has fewer phenomena than the algebraic world, but the phenomena it does see are the ones a computer can enumerate.

## Visual [Beginner]

A schematic of the tropical line in the plane $\mathbb{R}^2$: three rays meeting at the origin. One ray points along the positive diagonal in the direction $(1, 1)$; one points along the negative horizontal axis in the direction $(-1, 0)$; one points along the negative vertical axis in the direction $(0, -1)$.

The three direction vectors add to zero, which is the balancing condition that every tropical curve must satisfy at each vertex. Behind the picture is the tropical polynomial $\min(x, y, 0)$ derived from $x + y + 1$: the three rays mark the locus where the minimum is achieved at least twice, separating the plane into three regions in which each of the three terms takes its turn as the unique minimum.

![A schematic of the tropical line in the plane, with three rays meeting at the origin and a tropical polynomial label.](/img/kapranovs-theorem-placeholder.svg)

The picture is the canonical example because every part of Kapranov's theorem can be checked by hand on it. The valuation-image side: pick points $(x, y)$ on the algebraic curve $x + y + 1 = 0$ in a Puiseux-series field, and watch where their coordinate valuations land. The initial-ideal side: for each weight $w$ in the plane, look at the leading term of $x + y + 1$ under that weighting and ask whether the resulting initial ideal contains a monomial. The amoeba side: draw the actual amoeba of the curve over the complex numbers and watch it converge to the three-rayed fan as a parameter sharpens the picture.

## Worked example [Beginner]

Take the polynomial $f(x, y) = x + y + 1$ in two variables and consider the curve $V(f)$ that it defines. The field where the coordinates live is the field of Puiseux series — formal series $a_{q_1} t^{q_1} + a_{q_2} t^{q_2} + \cdots$ in fractional powers of an indeterminate $t$, with a smallest exponent (the valuation) attached to each nonzero element.

Step 1. Pick a one-parameter family of points on the curve. For each Puiseux series $a(t) \in K^*$, set $x = a(t)$ and $y = -1 - a(t)$. Then $x + y + 1 = a(t) + (-1 - a(t)) + 1 = 0$, so $(x, y) = (a(t), -1 - a(t))$ is on the curve.

Step 2. Compute the coordinate valuations for a specific choice. Choose $a(t) = t^{0.5}$. Then $\operatorname{val}(x) = 0.5$ and $y = -1 - t^{0.5}$, whose leading term is $-1$, giving $\operatorname{val}(y) = 0$. The pair of valuations is the point $(0.5, 0)$ in the plane.

Step 3. Now choose $a(t) = 1 + t$. Then $x = 1 + t$ with $\operatorname{val}(x) = 0$, and $y = -2 - t$ with $\operatorname{val}(y) = 0$. The pair is $(0, 0)$ — the vertex of the tropical line.

Step 4. Pick $a(t) = -1 + t^{0.7}$. Then $x = -1 + t^{0.7}$ with $\operatorname{val}(x) = 0$, and $y = -t^{0.7}$ with $\operatorname{val}(y) = 0.7$. The pair is $(0, 0.7)$.

Step 5. By varying $a(t)$ over all Puiseux series, the pair $(\operatorname{val}(x), \operatorname{val}(y))$ traces out the entire three-rayed tropical line: the ray $(0, *)$ with $* \geq 0$ (from choices with $\operatorname{val}(a) = 0$ that push the size of $y$ up), the ray $(*, 0)$ with $* \geq 0$ (from choices with $\operatorname{val}(-1 - a) = 0$ that push the size of $x$ up), and the diagonal $(*, *)$ with $* \leq 0$ (from choices with $\operatorname{val}(a) < 0$, in which case both $x$ and $y$ have negative valuation tied together).

What this tells us: the image of the algebraic curve $x + y + 1 = 0$ under the coordinate-valuation map is exactly the three-rayed tropical line. Kapranov's theorem promises that the same set is also the locus of weights at which the initial form of $x + y + 1$ is a binomial (not a monomial), and the same set is also the limiting picture of the complex amoeba. Three viewpoints, one combinatorial object.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

In tropical geometry, the ordinary algebraic operations $+$ and $\times$ are replaced by what two operations?

A. $\min$ and $+$
B. $+$ and $\min$
C. $\max$ and $\times$
D. $+$ and $\times$ — the same operations, but on a different field

<details class="exercise__hint"><summary>Hint</summary>

The "addition" of tropical geometry is taking the minimum (or maximum, depending on convention); the "multiplication" of tropical geometry is ordinary addition. This is the so-called min-plus or tropical semiring.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**A.** Tropical addition is $\min$ (or in some conventions $\max$); tropical multiplication is ordinary $+$. With these conventions, an ordinary polynomial $c_1 x^{u_1} + c_2 x^{u_2} + \cdots$ over an appropriate field becomes the tropical polynomial $\min_u(\operatorname{val}(c_u) + \langle u, w\rangle)$ when evaluated at a weight vector $w$. The tropical vanishing locus is the set of $w$ where the minimum is attained by at least two distinct terms, which is the piecewise-linear shadow of the algebraic vanishing locus.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: the tropical curve associated to $f(x, y) = x + y + 1$ in $\mathbb{R}^2$ is a single point.

<details class="exercise__hint"><summary>Hint</summary>

A tropical curve is one-dimensional in $\mathbb{R}^2$. The example above traced out a fan with three rays meeting at the origin.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** The tropical curve of $x + y + 1$ is a one-dimensional polyhedral object — the three-rayed tropical line consisting of three rays from the origin in the directions $(1, 1)$, $(-1, 0)$, and $(0, -1)$. The set where $\min(x, y, 0)$ is attained at least twice is the union of these three rays. The dimension equals the dimension of the underlying algebraic curve, which is one — this matches the Bieri-Groves part of Kapranov's theorem about pure dimensionality.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

How many rays does the tropical line $\min(x, y, 0)$ in $\mathbb{R}^2$ have meeting at the origin?

<details class="exercise__hint"><summary>Hint</summary>

A tropical line in $\mathbb{R}^n$ defined by a polynomial with $k$ terms has $k$ rays meeting at the origin, one for each pair of variables (and one for the constant term), in directions summing to zero.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**3.** The polynomial $x + y + 1$ has three terms, giving three rays in directions $(1, 1)$, $(-1, 0)$, and $(0, -1)$ — pointing in the diagonal, leftward, and downward directions respectively. The three direction vectors sum to zero, which is the balancing condition every tropical curve satisfies at each vertex. Tropical lines in $\mathbb{R}^n$ defined by polynomials with $n + 1$ terms generalise this: they always have $n + 1$ rays balanced at the origin.

</details>

</aside>

## Formal definition [Intermediate+]

Let $K$ be an algebraically closed field equipped with a surjective non-archimedean valuation $\operatorname{val} : K^* \to \mathbb{R}$. The canonical example is the field of Puiseux series
$$
K = \bigcup_{d \geq 1} \mathbb{C}((t^{1/d})),
$$
the union of fields of formal Laurent series in $t^{1/d}$ for $d \in \mathbb{Z}_{\geq 1}$, with valuation $\operatorname{val}(\sum_q a_q t^q) = \min\{q : a_q \neq 0\}$. This field is algebraically closed (each finite algebraic extension of $\mathbb{C}((t))$ is contained in some $\mathbb{C}((t^{1/d}))$), and the valuation surjects onto $\mathbb{Q} \subseteq \mathbb{R}$; after taking the completion the value group can be enlarged to $\mathbb{R}$. The residue field is $\mathbb{C}$.

Write $T = (K^*)^n$ for the algebraic torus over $K$, and write $K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ for the Laurent polynomial ring whose Spec is $T$. The coordinate-wise valuation map is
$$
\operatorname{val} : T \to \mathbb{R}^n, \quad z = (z_1, \ldots, z_n) \mapsto (\operatorname{val}(z_1), \ldots, \operatorname{val}(z_n)).
$$
The pairing of the character lattice $M = \mathbb{Z}^n$ with $\mathbb{R}^n$ extends the integer pairing $\langle u, v\rangle = \sum u_i v_i$ to $\langle u, w\rangle$ for real $w$.

**Definition (initial form).** Given a Laurent polynomial $f = \sum_{u \in M} c_u x^u \in K[x^{\pm 1}]$ with finite support, and a weight vector $w \in \mathbb{R}^n$, the **$w$-initial form** of $f$ is the residue polynomial obtained by selecting the terms of minimal "tropical value":
$$
\operatorname{in}_w(f) = \overline{\sum_{u : \, \operatorname{val}(c_u) + \langle u, w\rangle = \mu} c_u t^{-\operatorname{val}(c_u)} \cdot x^u},
$$
where $\mu = \min_u (\operatorname{val}(c_u) + \langle u, w\rangle)$ is the tropical minimum and the overline denotes reduction modulo the maximal ideal of the valuation ring (taking each $c_u t^{-\operatorname{val}(c_u)}$ to its residue in $\mathbb{C}$). The result is a Laurent polynomial in $\mathbb{C}[x^{\pm 1}]$.

**Definition (initial ideal).** Given an ideal $I \subseteq K[x^{\pm 1}]$ and a weight $w \in \mathbb{R}^n$, the **$w$-initial ideal** is
$$
\operatorname{in}_w(I) = \langle \operatorname{in}_w(f) : f \in I \rangle \subseteq \mathbb{C}[x^{\pm 1}].
$$
The initial ideal is the heart of the Gröbner-theoretic side of tropical geometry. It is finitely generated by initial forms of any tropical Gröbner basis of $I$.

**Definition (three constructions of $\operatorname{trop}(I)$).** Let $I \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ be an ideal cutting out a subvariety $V(I) \subseteq T = (K^*)^n$.

*(i) Valuation-image construction:*
$$
\operatorname{trop}(I)_{\text{val}} \, := \, \overline{\{ \operatorname{val}(z) : z \in V(I)(K) \}} \, \subseteq \, \mathbb{R}^n,
$$
the topological closure of the image of $V(I)(K)$ under the coordinate-wise valuation map.

*(ii) Initial-ideal construction:*
$$
\operatorname{trop}(I)_{\text{init}} \, := \, \{ w \in \mathbb{R}^n : \operatorname{in}_w(I) \text{ contains no monomial} \},
$$
the locus of weights at which the initial ideal has no monomial generator. (Equivalently, at which $V(\operatorname{in}_w(I)) \subseteq (\mathbb{C}^*)^n$ is non-empty, by Hilbert's Nullstellensatz over the algebraically closed residue field.)

*(iii) Amoeba-limit construction.* For $t > 0$ sufficiently small, let $I_t \subseteq \mathbb{C}[x^{\pm 1}]$ be the ideal obtained from $I$ by specialising the indeterminate $t$ of the Puiseux field. The complex amoeba is
$$
\mathcal{A}(V(I_t)) := \{ (-\log_t |z_1|, \ldots, -\log_t |z_n|) : z \in V(I_t)(\mathbb{C}^*) \} \subseteq \mathbb{R}^n.
$$
Then
$$
\operatorname{trop}(I)_{\text{amoeba}} \, := \, \lim_{t \to 0^+} \mathcal{A}(V(I_t)),
$$
the Hausdorff limit of the amoebas as $t \to 0^+$ (the limit exists for a suitable coherent family $(I_t)$).

### Counterexamples to common slips

- *The valuation-image must be closed.* The set $\{\operatorname{val}(z) : z \in V(I)(K)\}$ before closure can fail to be closed in $\mathbb{R}^n$ — for example, when the value group is the rationals $\mathbb{Q}$ rather than all of $\mathbb{R}$, the image is dense in but not equal to the polyhedral complex. The closure is what produces the polyhedral complex in $\mathbb{R}^n$.

- *Initial ideal contains a monomial is the right vanishing condition, not "initial ideal equals an ideal of monomials."* The initial ideal can have monomial elements alongside non-monomial generators; the relevant criterion is mere containment of one monomial. The reason is that a monomial $x^u \in \operatorname{in}_w(I)$ forces $V(\operatorname{in}_w(I))$ to miss the locus $x^u = 0$ — but since we are working in the torus $(\mathbb{C}^*)^n$ where every $x^u$ is non-vanishing, this means $V(\operatorname{in}_w(I)) = \varnothing$ in the torus, and by the Kapranov direction the original valuation-image cannot reach $w$.

- *The amoeba limit is sensitive to the parametrisation.* Different choices of how to specialise the Puiseux indeterminate $t$ give different amoeba families $(\mathcal{A}_t)$, but they all share the same Hausdorff limit as $t \to 0^+$. The intrinsic object is the limit, not the family.

- *Tropical varieties are not the same as Bergman fans of matroids.* The Bergman fan of a linear matroid is the tropicalisation of a linear ideal, a special case of Kapranov's construction. General tropical varieties of non-linear ideals are richer and need not arise as Bergman fans of any matroid.

## Key theorem with proof [Intermediate+]

**Theorem (Kapranov; Einsiedler-Kapranov-Lind 2006).** *Let $K$ be an algebraically closed field with a surjective non-archimedean valuation $\operatorname{val} : K^* \to \mathbb{R}$, let $I \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ be an ideal, and let $V(I) \subseteq (K^*)^n$ be the associated subvariety. The three sets defined above coincide:*
$$
\operatorname{trop}(I)_{\text{val}} \; = \; \operatorname{trop}(I)_{\text{init}} \; = \; \operatorname{trop}(I)_{\text{amoeba}}.
$$
*The common set $\operatorname{trop}(I)$ is a closed polyhedral subcomplex of $\mathbb{R}^n$ of pure dimension equal to the Krull dimension of $K[x^{\pm 1}]/I$.*

*Proof.* We prove the equivalence $\operatorname{trop}(I)_{\text{val}} = \operatorname{trop}(I)_{\text{init}}$ in two directions; the amoeba equivalence is sketched in the Advanced-results section under Sub-section §3 and treated in detail in the literature.

**Step 1: $\operatorname{trop}(I)_{\text{val}} \subseteq \operatorname{trop}(I)_{\text{init}}$.** Let $w \in \mathbb{R}^n$ be a valuation of a point $z \in V(I)(K)$, so $\operatorname{val}(z) = w$. We must show that $\operatorname{in}_w(I)$ contains no monomial.

Suppose for contradiction that $x^v \in \operatorname{in}_w(I)$ for some $v \in M = \mathbb{Z}^n$. Then there exists $f \in I$ with $\operatorname{in}_w(f) = x^v$, which means
$$
f = \alpha t^a x^v + \sum_{u \neq v} c_u x^u,
$$
where $\alpha \in \mathbb{C}^*$, $a = -\operatorname{val}(\alpha t^a) = -\mu + \langle v, w\rangle$ for the tropical minimum $\mu = \operatorname{val}(\alpha t^a) + \langle v, w\rangle$, and every other term $c_u x^u$ has strictly larger tropical value: $\operatorname{val}(c_u) + \langle u, w\rangle > \mu$.

Evaluating $f$ at $z$ and using $f(z) = 0$:
$$
0 = f(z) = \alpha t^a z^v + \sum_{u \neq v} c_u z^u.
$$
The valuation of the first term is $\operatorname{val}(\alpha t^a) + \langle v, \operatorname{val}(z) \rangle = \operatorname{val}(\alpha t^a) + \langle v, w\rangle = \mu$. The valuation of every other term is $\operatorname{val}(c_u) + \langle u, w\rangle > \mu$. By the strong triangle inequality of the non-archimedean valuation, the sum has valuation exactly $\mu$ (the minimum is attained once, by the first term, with a strict gap). This contradicts $f(z) = 0$ (which would require $\operatorname{val}(f(z)) = \infty$). Therefore $\operatorname{in}_w(I)$ contains no monomial, and $w \in \operatorname{trop}(I)_{\text{init}}$.

**Step 2: $\operatorname{trop}(I)_{\text{init}} \subseteq \operatorname{trop}(I)_{\text{val}}$.** This is the Kapranov direction, the substantive content of the theorem. Let $w \in \operatorname{trop}(I)_{\text{init}}$, so $\operatorname{in}_w(I)$ contains no monomial. We must produce $z \in V(I)(K)$ with $\operatorname{val}(z) = w$.

The strategy is a Hensel-style lifting argument. Since $\operatorname{in}_w(I)$ contains no monomial, the variety $V(\operatorname{in}_w(I)) \subseteq (\mathbb{C}^*)^n$ is non-empty by Hilbert's Nullstellensatz over the algebraically closed residue field $\mathbb{C}$. (If $V(\operatorname{in}_w(I)) = \varnothing$, then by the Nullstellensatz $\operatorname{in}_w(I) = (1) = \mathbb{C}[x^{\pm 1}]$, which contains the monomial $1$, contradiction.) Pick a residual point $\overline{z} = (\overline{z}_1, \ldots, \overline{z}_n) \in V(\operatorname{in}_w(I))$ in the residue torus.

We now lift $\overline{z}$ to a point of $V(I)(K)$ with valuation $w$. After a unimodular linear change of coordinates on $\mathbb{Z}^n$, assume $w \in \mathbb{Q}^n$; the case of irrational $w$ is handled by approximation. Choose a finite tropical Gröbner basis $\{g_1, \ldots, g_r\}$ of $I$ with respect to the weight $w$ — such bases exist by the Gröbner-fan theory of Sturmfels and others. Write each $g_i$ in $w$-leading-form / lower-order decomposition:
$$
g_i = \operatorname{in}_w(g_i) + (\text{higher-order in } t).
$$

Define the rescaling map $\Phi_w : \mathbb{C}[x^{\pm 1}] \to K[x^{\pm 1}]$ by $\Phi_w(x_i) = t^{w_i} y_i$ where $y_i \in K^*$ are new coordinates. In the new coordinates, the polynomials $\Phi_w(g_i)$ have constant tropical-minimal terms at the origin $y = (1, \ldots, 1)$ (after dividing by an overall power of $t$); the higher-order corrections are $O(t^\varepsilon)$ for some $\varepsilon > 0$ depending on the gap between the tropical minimum and the next-smallest tropical value of each $g_i$.

We now apply the multivariate non-archimedean Hensel lemma: given a system of polynomials whose residual Jacobian at $\overline{y} = (\overline{z}_1, \ldots, \overline{z}_n)$ has the right rank (here, the codimension of $V(\operatorname{in}_w(I))$ inside the torus), there exists a lift $y^* \in (K^*)^n$ of $\overline{y}$ satisfying $\Phi_w(g_i)(y^*) = 0$ for all $i$. The lifted point $z^* = (t^{w_1} y^*_1, \ldots, t^{w_n} y^*_n)$ then satisfies $g_i(z^*) = 0$ for all $i$, hence $z^* \in V(I)(K)$; its coordinate valuation is $\operatorname{val}(z^*) = w$ as required.

The Hensel-lemma application at residually-singular points of $V(\operatorname{in}_w(I))$ requires a finer argument involving the formal completion and approximation by analytic Berkovich points — Gubler 2013 develops this carefully — but for residually-smooth points the classical Hensel lemma suffices. This completes the substantive direction.

**Step 3: Polyhedral structure and pure dimensionality.** Each of $\operatorname{trop}(I)_{\text{init}}$ has a polyhedral structure as the support of the Gröbner fan of $I$ — the fan whose cones partition $\mathbb{R}^n$ according to the equivalence relation $w \sim w' \iff \operatorname{in}_w(I) = \operatorname{in}_{w'}(I)$. The Bieri-Groves theorem (Bieri-Groves 1984 *Crelle* 347) refined for ideals shows that this polyhedral complex is of pure dimension $\dim K[x^{\pm 1}]/I$. Combining Step 1, Step 2, and Step 3 establishes the full theorem. $\square$

**Bridge.** The proof builds toward [04.12.05] Mikhalkin's correspondence theorem, where the tropical-side combinatorial count of plane curves is shown to equal the Gromov-Witten count of complex plane curves, and the central insight is that the bridge between algebraic and tropical worlds is the coordinate valuation, refined by the Bieri-Groves polyhedral structure and the Hensel-lift Kapranov direction. This is exactly the foundational reason that polyhedral combinatorics can compute algebraic-geometric invariants — the polyhedral side captures all the dimensional and degree-theoretic information of the algebraic side, and the lifting of polyhedral points to algebraic points is constructive enough to count.

Putting these together, the equivalence of the three definitions identifies a single object $\operatorname{trop}(I)$ that simultaneously records: the valuative behaviour of $V(I)$ on a non-archimedean field, the Gröbner-fan structure of $I$, and the asymptotic Hausdorff geometry of the complex amoeba. This pattern of three definitions converging on one object generalises in [04.12.04] non-archimedean amoebas, where the Berkovich-analytic viewpoint identifies the tropical variety with the image of the analytic space under a continuous tropicalisation map; the bridge is between the algebraic and analytic categories, and the foundational reason for the equivalence is the universal property of the Berkovich analytification as the "right" topological space for non-archimedean fields.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Compute $\operatorname{in}_w(x + y + 1)$ for $w = (1, 2)$ over the Puiseux field $K = \bigcup_d \mathbb{C}((t^{1/d}))$ (where the coefficients of $x + y + 1$ are $c_x = 1$, $c_y = 1$, $c_0 = 1$, all with valuation $0$).

<details class="exercise__hint"><summary>Hint</summary>

The tropical value of the term $x$ at weight $w$ is $\operatorname{val}(1) + \langle (1, 0), (1, 2)\rangle = 0 + 1 = 1$. The tropical value of $y$ is $0 + 2 = 2$. The tropical value of $1$ is $0 + 0 = 0$. The initial form selects the term(s) of minimum tropical value.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\operatorname{in}_w(x + y + 1) = 1$.** Tropical values: $x \mapsto 1$, $y \mapsto 2$, $1 \mapsto 0$. The minimum $0$ is attained uniquely by the constant term $1$, so $\operatorname{in}_w(f) = 1$ — a monomial (the constant $1$). Consequently $\operatorname{in}_w(I) = (1) = \mathbb{C}[x^{\pm 1}]$ contains the monomial $1$, so $w = (1, 2)$ is not in $\operatorname{trop}(I)_{\text{init}}$. Geometrically: the point $(1, 2)$ lies in the region of $\mathbb{R}^2$ where the constant $0$ is the unique minimum of $\min(x, y, 0)$, outside the tropical line.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 2 (easy, multiple choice).**

Which of the following sets is the tropicalisation of the principal ideal $I = (x + y + 1)$ in the Puiseux field?

A. The single point $\{(0, 0)\} \subset \mathbb{R}^2$.
B. The full plane $\mathbb{R}^2$.
C. The three-rayed fan in $\mathbb{R}^2$ with rays in directions $(1, 1)$, $(-1, 0)$, $(0, -1)$ meeting at the origin.
D. The diagonal line $\{(s, s) : s \in \mathbb{R}\}$.

<details class="exercise__hint"><summary>Hint</summary>

The tropical curve of a Laurent polynomial with $k$ terms in $\mathbb{R}^n$ is a union of polyhedra of codimension $1$, and for $k = n + 1$ terms it is a balanced fan with $k$ rays.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** The tropicalisation of $x + y + 1$ is the three-rayed fan (often drawn with rays in directions $(1, 1)$, $(-1, 0)$, $(0, -1)$ — but the exact sign convention depends on whether one tropicalises $\min$ or $\max$; here the $\min$-convention is used). At a point of any of these three rays, the tropical polynomial $\min(x, y, 0)$ has its minimum attained at least twice — for example, on the ray $(s, s)$ with $s \geq 0$ the minimum is attained simultaneously by the terms $x$ and $y$ (both equalling $s$, both larger than the constant $0$); on $(-r, 0)$ with $r \geq 0$ the minimum is attained by $x = -r$ and the constant $0$, both smaller than $y = 0$; etc. The fan is the locus where the minimum is non-unique, exactly the tropical-vanishing condition.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Consider the Laurent polynomial $f = x^2 + xy + y^2 + 1$ in two variables. Compute the tropical curve $\operatorname{trop}(V(f))$ as a polyhedral complex in $\mathbb{R}^2$, identifying its vertices, edges (with directions and multiplicities), and the unbounded rays.

<details class="exercise__hint"><summary>Hint</summary>

Form the tropical polynomial $F(w_1, w_2) = \min(2 w_1, w_1 + w_2, 2 w_2, 0)$. The tropical curve is the locus in $\mathbb{R}^2$ where this minimum is attained by at least two distinct terms. Find the locus of pairwise equality, then trim to where each equal value is actually the global minimum.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The tropical polynomial is $F(w) = \min(2 w_1, \, w_1 + w_2, \, 2 w_2, \, 0)$. Setting pairs of terms equal:

- $2 w_1 = w_1 + w_2 \iff w_1 = w_2$, with the equal value $2 w_1 = 2 w_2$ becoming the minimum when $2 w_1 \leq 2 w_2$ (automatic on this line) and $2 w_1 \leq 0$, giving the half-line $\{(s, s) : s \leq 0\}$.
- $w_1 + w_2 = 2 w_2 \iff w_1 = w_2$, the same line; together with the half-line $\{(s, s) : s \geq 0\}$ when the equal value $w_1 + w_2 = 2 w_2$ becomes the minimum, i.e., when $2 w_2 \leq 2 w_1$ (automatic on this line) and $w_1 + w_2 \leq 0$, giving $s \leq 0$. Reconciling, the entire diagonal $\{(s, s) : s \in \mathbb{R}\}$ appears.
- $2 w_1 = 0 \iff w_1 = 0$, equal value $0$, minimum when $0 \leq w_1 + w_2$ and $0 \leq 2 w_2$, giving the vertical half-line $\{(0, w_2) : w_2 \geq 0\}$.
- $2 w_2 = 0 \iff w_2 = 0$, equal value $0$, minimum when $0 \leq 2 w_1$ and $0 \leq w_1 + w_2$, giving the horizontal half-line $\{(w_1, 0) : w_1 \geq 0\}$.
- $w_1 + w_2 = 0 \iff w_2 = -w_1$, equal value $0$, minimum when $0 \leq 2 w_1$ and $0 \leq 2 w_2$, requiring $w_1 \geq 0$ and $w_2 \geq 0$ simultaneously with $w_1 + w_2 = 0$, forcing $w_1 = w_2 = 0$ — so this case contributes only the origin.

Re-examining the picture: at $(s, s)$ with $s > 0$, we have $F = \min(2s, 2s, 2s, 0) = 0$, attained uniquely by the constant; at $(s, s)$ with $s < 0$, $F = \min(2s, 2s, 2s, 0) = 2s < 0$, attained simultaneously by all three of $x^2, xy, y^2$ (multiplicity-$3$ minimum). The tropical curve therefore has rays from the origin in primitive directions $(-1, -1)$, $(1, 0)$, $(0, 1)$.

The diagonal ray $(-1, -1)$ carries weight $2$ — the lattice length of the segment from $(2, 0)$ to $(0, 2)$ inside the Newton polytope. The horizontal ray $(1, 0)$ and the vertical ray $(0, 1)$ each carry weight $2$ as well, from the lattice lengths of the corresponding Newton-polytope edges between $(2, 0)$ and $(0, 0)$, and between $(0, 2)$ and $(0, 0)$.

Balancing then holds: $2 \cdot (-1, -1) + 2 \cdot (1, 0) + 2 \cdot (0, 1) = (0, 0)$. Rubric: full credit for identifying the three-rayed structure, the multiplicity-$2$ weighting on each ray from the Newton-polygon lattice lengths, and the balancing relation $2 \cdot (-1, -1) + 2 \cdot (1, 0) + 2 \cdot (0, 1) = (0, 0)$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Verify the strong-triangle-inequality step in the forward direction of Kapranov's theorem (Step 1 of the proof) for the polynomial $f = t x + t^2 y + 1 \in K[x^{\pm 1}, y^{\pm 1}]$ over the Puiseux field, at the weight $w = (1, 1) \in \mathbb{R}^2$.

<details class="exercise__hint"><summary>Hint</summary>

Compute the tropical values $\operatorname{val}(c_u) + \langle u, w\rangle$ for each term. Find the minimum and check whether it is attained at least twice. If yes, $w \in \operatorname{trop}(V(f))_{\text{init}}$; if no, the strong-triangle-inequality argument shows the initial form is a substantive monomial.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Tropical values of the three terms at $w = (1, 1)$:

- $t x$: $\operatorname{val}(t) + \langle (1, 0), (1, 1)\rangle = 1 + 1 = 2$.
- $t^2 y$: $\operatorname{val}(t^2) + \langle (0, 1), (1, 1)\rangle = 2 + 1 = 3$.
- $1$: $\operatorname{val}(1) + \langle (0, 0), (1, 1)\rangle = 0 + 0 = 0$.

The minimum is $0$, attained uniquely by the constant term $1$. Therefore $\operatorname{in}_w(f) = 1$, a monomial. By Step 1 of the proof, this forces $w = (1, 1)$ to lie outside $\operatorname{trop}(V(f))_{\text{val}}$ — that is, no point $z \in V(f)(K) = \{z : t z_1 + t^2 z_2 + 1 = 0\}$ has $\operatorname{val}(z) = (1, 1)$. Verification: if $\operatorname{val}(z_1) = 1$ and $\operatorname{val}(z_2) = 1$, then $\operatorname{val}(t z_1) = 2$, $\operatorname{val}(t^2 z_2) = 3$, $\operatorname{val}(1) = 0$, and the strong triangle inequality gives $\operatorname{val}(t z_1 + t^2 z_2 + 1) = \min(2, 3, 0) = 0$, so $f(z) \neq 0$ (its valuation is finite, not $\infty$). Rubric: full credit for the tropical-value table, the unique minimum, and the strong-triangle-inequality conclusion that $w = (1, 1)$ is not in the tropical curve.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

For the Laurent polynomial $f = x + 2 y + 3 \in \mathbb{Q}[x, y]$ — coefficients $1, 2, 3$ all in $\mathbb{Q} \subset K$ with valuation zero — the tropical line is the three-rayed fan from the origin. State the directions of the three rays and the multiplicity carried by each.

<details class="exercise__hint"><summary>Hint</summary>

The directions are determined by the lattice exponent vectors of the three terms. Multiplicities come from the lattice lengths of the dual edges in the Newton polygon.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The three monomials have lattice exponents $(1, 0)$ (from $x$), $(0, 1)$ (from $y$), and $(0, 0)$ (from the constant $3$). The dual-edge directions, after primitive normalisation, are the outward normals to the three edges of the Newton triangle, i.e., the directions $(1, 1)$ (the negative-of-normal to the edge from $(1, 0)$ to $(0, 1)$), $(-1, 0)$ (the negative-of-normal to the edge from $(0, 1)$ to $(0, 0)$), and $(0, -1)$ (the negative-of-normal to the edge from $(0, 0)$ to $(1, 0)$). All three rays carry multiplicity $1$ because each edge of the Newton triangle has lattice length $1$. The balancing relation $1 \cdot (1, 1) + 1 \cdot (-1, 0) + 1 \cdot (0, -1) = (0, 0)$ holds. The coefficient values $1, 2, 3$ shift the location of the apex of the tropical curve but do not change the three directions or multiplicities (the apex sits at the origin only if all coefficients have valuation zero, which is the case here). Rubric: full credit for the three primitive directions, the multiplicities all equal to one, and the balancing relation.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 6 (medium, numeric).**

The Krull dimension of $\mathbb{Q}[x, y] / (x + y + 1)$ as a $\mathbb{Q}$-algebra equals what integer? Use Kapranov's pure-dimension statement to predict the dimension of the tropical curve in $\mathbb{R}^2$.

<details class="exercise__hint"><summary>Hint</summary>

A single non-vacuous equation in two variables cuts dimension by one.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Krull dimension $= 1$; tropical-curve dimension $= 1$.** The hypersurface $V(x + y + 1)$ inside $(K^*)^2$ is one-dimensional (one equation cutting out a curve in two ambient dimensions, with $V(I) \neq \varnothing$ since the equation is honest, not the unit ideal). By Bieri-Groves / Kapranov's pure-dimension theorem (Sub-section §3 of the Master tier), $\operatorname{trop}(V(I))$ is a polyhedral complex of pure dimension equal to the Krull dimension, hence pure dimension $1$ in $\mathbb{R}^2$. This matches the three-rayed picture: each ray is one-dimensional, and no isolated vertices appear in pure-dimension-$1$ complexes (the central vertex is the intersection of three one-dimensional rays). Rubric: full credit for Krull dimension $1$ and the matched tropical dimension $1$, with a sentence justifying the matching via Kapranov's theorem.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Sketch the Hensel-lift step (Step 2 of the proof) for the polynomial $f = x + y + 1$ at the weight $w = (-1, 0) \in \operatorname{trop}(V(f))_{\text{init}}$. That is, given that $w = (-1, 0)$ satisfies $\operatorname{in}_w(f)$ contains no monomial, explicitly construct a point $z \in V(f)(K)$ with $\operatorname{val}(z) = (-1, 0)$.

<details class="exercise__hint"><summary>Hint</summary>

At $w = (-1, 0)$ the tropical values of $x, y, 1$ are $-1, 0, 0$. The minimum $-1$ is uniquely attained — wait, this seems wrong. Re-check: in the $\min$-convention the relevant tropical-valuation candidates are $\operatorname{val}(c_u) + \langle u, w\rangle$ where $w = (-1, 0)$. For $x$ at exponent $(1, 0)$: $0 + (1)(-1) + (0)(0) = -1$. For $y$ at exponent $(0, 1)$: $0 + 0 + 0 = 0$. For $1$ at exponent $(0, 0)$: $0$. So the minimum is $-1$, attained uniquely by $x$ — meaning $w = (-1, 0)$ is NOT in the tropical curve, and the exercise's premise is incorrect. (Replace by $w = (0, -1)$ or $w = (-1, -1)$ as desired; the substantive content is the Hensel lift.)

</details>

<details class="exercise__answer"><summary>Answer</summary>

The premise is mis-stated; corrected below. Take $w = (-1, -1)$. Tropical values: $x \mapsto -1$, $y \mapsto -1$, $1 \mapsto 0$. The minimum $-1$ is attained at two terms ($x$ and $y$), so $\operatorname{in}_w(f) = x + y$ (after the residue-field reduction; $x$ and $y$ have valuation-leading coefficient $1$, the constant $1$ has tropical value $0 > -1$ and so vanishes in the initial form). The variety $V(\operatorname{in}_w(f)) = V(x + y) \subset (\mathbb{C}^*)^2$ is the line $y = -x$; pick the residual point $(\overline{z}_1, \overline{z}_2) = (1, -1)$.

The Hensel lift: write $x = t^{-1} y_1$ and $y = t^{-1} y_2$ with $y_1, y_2 \in K^*$. Substituting,
$$
f(t^{-1} y_1, t^{-1} y_2) = t^{-1} y_1 + t^{-1} y_2 + 1 = t^{-1} (y_1 + y_2 + t).
$$
The bracketed polynomial $g(y_1, y_2) = y_1 + y_2 + t$ is to be solved in $K^*$ near the residual root $(\overline{y}_1, \overline{y}_2) = (1, -1)$ (which is the lift of $(\overline{z}_1, \overline{z}_2) \cdot (\text{residue scale}) = (1, -1)$, since the substitution scaled coordinates by $t^{-1}$ and the residual scale is $1$). The residual $g \mod t$ is $\overline{g} = y_1 + y_2$, with residual root $(1, -1)$, residually smooth (the Jacobian $(1, 1)$ has rank $1$, matching the codimension).

Apply Hensel's lemma in the one-variable form (after parametrising $y_2 = -y_1 - t$ to eliminate one variable from $g$): any $y_1 \in K^*$ with $\operatorname{val}(y_1) = 0$ yields a solution. Set $y_1 = 1$; then $y_2 = -1 - t$, with $\operatorname{val}(y_2) = 0$ and residue $-1$. The lifted point is
$$
z = (t^{-1} \cdot 1, \, t^{-1} \cdot (-1 - t)) = (t^{-1}, \, -t^{-1} - 1).
$$
Verification: $f(z) = t^{-1} + (-t^{-1} - 1) + 1 = 0$, so $z \in V(f)(K)$. And $\operatorname{val}(z) = (\operatorname{val}(t^{-1}), \operatorname{val}(-t^{-1} - 1)) = (-1, -1) = w$ as required. Rubric: full credit for the rescaling $x = t^{-1} y_1$, the residual-polynomial computation, the explicit Hensel lift via $y_1 = 1$, and the verification that $\operatorname{val}(z) = w$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 8 (hard, short-answer).**

State and outline the proof of the Bieri-Groves polyhedral-and-pure-dimension theorem: for an equidimensional ideal $I \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ of Krull dimension $d$, the tropical variety $\operatorname{trop}(I)$ is a closed polyhedral subcomplex of $\mathbb{R}^n$ of pure dimension $d$.

<details class="exercise__hint"><summary>Hint</summary>

The polyhedral structure comes from the Gröbner-fan partition of $\mathbb{R}^n$ into open cones on which the initial ideal is constant. Pure dimensionality comes from a degeneration argument: each top-dimensional cone of $\operatorname{trop}(I)$ corresponds to a Gröbner degeneration of $I$ to a binomial ideal, and the binomial ideal's tropical variety is a translate of a $d$-dimensional rational subspace.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Statement.** Let $I \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ be an ideal of Krull dimension $d$, with all minimal primes of dimension $d$ (equidimensional). Then $\operatorname{trop}(I)$ is a closed polyhedral subcomplex of $\mathbb{R}^n$, every maximal cone has affine dimension $d$, and the multiplicity-weighted balancing condition holds at every codimension-one face.

**Proof outline.**

*(Polyhedral structure.)* The Gröbner fan of $I$ is the fan in $\mathbb{R}^n$ whose open cones are the equivalence classes of weights $w \sim w' \iff \operatorname{in}_w(I) = \operatorname{in}_{w'}(I)$. This is finite (Sturmfels 1996 *Gröbner bases and convex polytopes*) and rational-polyhedral. The locus $\operatorname{trop}(I)_{\text{init}}$ is a subfan — it consists of those cones whose initial ideal contains no monomial, a property that is constant on each Gröbner-fan cone. Hence $\operatorname{trop}(I)$ inherits a closed polyhedral-subcomplex structure.

*(Pure dimensionality, $\dim \leq d$.)* At any $w \in \operatorname{trop}(I)$, $\operatorname{in}_w(I)$ has Krull dimension equal to $\dim I = d$ (initial ideals preserve Hilbert series and hence Krull dimension). A flat-family / specialisation argument identifies the maximal cone of $\operatorname{trop}(I)$ containing $w$ with the "tropical dimension" of $V(\operatorname{in}_w(I))$ inside the residue torus, which is at most $d$ (the algebraic dimension is $d$, and tropicalisation in the residue field can only drop dimension).

*(Pure dimensionality, $\dim \geq d$.)* This is the substantive direction. Refine the Gröbner fan to a fan whose top-dimensional cones each correspond to maximal Gröbner degenerations of $I$ to monomial ideals. For an equidimensional ideal, a generic Gröbner degeneration produces a Cohen-Macaulay monomial ideal of the same Krull dimension $d$, whose tropical variety is the union of coordinate $d$-flats inside $\mathbb{R}^n$. Each top-dimensional cone of $\operatorname{trop}(I)$ thus contains an honest $d$-flat from this generic degeneration. The closure of these $d$-flats inside $\operatorname{trop}(I)$ gives the pure-dimensional structure.

*(Balancing.)* At each codimension-one face $\sigma$ of $\operatorname{trop}(I)$, let $\sigma_1, \ldots, \sigma_k$ be the top-dimensional cones meeting $\sigma$, with primitive normal directions $v_i$ (modulo the linear span of $\sigma$) and multiplicities $m_i$ (the lattice index of the tropical degeneration in direction $v_i$). The balancing condition $\sum_i m_i v_i = 0$ in $\mathbb{R}^n / \mathrm{span}(\sigma)$ is the tropical reformulation of the fact that the local Hilbert function near $\sigma$ is continuous across the codimension-one wall. This was first identified by Bieri-Groves 1984 for valuation images and reformulated for ideals in Speyer-Sturmfels 2004 and Maclagan-Sturmfels 2015 Ch. 3.

Together these three pieces give the full polyhedral / pure-dimension / balanced-complex structure of $\operatorname{trop}(I)$. Rubric: full credit for the Gröbner-fan polyhedral structure, the dimension-bound + dimension-lower-bound argument via initial ideals, and the balancing condition (with or without proof).

</details>

</aside>

## Lean formalization [Intermediate+]

The companion Lean module is `Codex.AlgGeom.Tropical.KapranovTheorem`, with `lean_status: partial`. The module records the three constructions $\operatorname{trop}(I)_{\text{val}}$, $\operatorname{trop}(I)_{\text{init}}$, $\operatorname{trop}(I)_{\text{amoeba}}$ as named definitions, declares the fundamental-theorem equivalences `kapranov_val_eq_init`, `kapranov_amoeba_eq_val`, `kapranov_three_definitions`, and declares the Bieri-Groves polyhedral / pure-dimension structure result `trop_is_polyhedral_pure_dim`. Proof bodies are `sorry`-stubbed pending the infrastructure listed in the `lean_mathlib_gap` field of the frontmatter.

The schematic structure of the formalisation reads:

```lean
import Mathlib.RingTheory.Polynomial.Laurent
import Mathlib.Data.Real.Basic

namespace Codex.AlgGeom.Tropical

structure ValuedField where
  carrier : Type
  is_alg_closed : Prop
  has_surjective_real_valuation : Prop

structure LaurentIdeal (K : ValuedField) (n : ℕ) where
  carrier : Type
  is_ideal : Prop

def tropByValuation (K : ValuedField) (n : ℕ) (I : LaurentIdeal K n) :
    Set (Fin n → ℝ) := sorry

def tropByInitial (K : ValuedField) (n : ℕ) (I : LaurentIdeal K n) :
    Set (Fin n → ℝ) := sorry

theorem kapranov_val_eq_init
    (K : ValuedField) (n : ℕ) (I : LaurentIdeal K n) :
    tropByValuation K n I = tropByInitial K n I := by
  sorry

theorem trop_is_polyhedral_pure_dim
    (K : ValuedField) (n : ℕ) (I : LaurentIdeal K n) :
    IsPolyhedralOfPureDim n (tropByValuation K n I) (krullDim K n I) := by
  trivial

end Codex.AlgGeom.Tropical
```

The substantive Mathlib gap is the algebraically closed Puiseux field with surjective real valuation, the initial-ideal calculus on multivariate Laurent rings, the non-archimedean Hensel-lift lemma at the level of multivariate ideals, and the Bieri-Groves polyhedral-finiteness theorem. Once those are in Mathlib, the Kapranov-direction proof is reducible to (a) the residue-field Nullstellensatz (already in Mathlib) plus (b) the lifting step from the Gröbner-fan / initial-ideal calculus.

## Advanced results [Master]

### The fundamental theorem of tropical geometry — three equivalent definitions

**Theorem 1 (Kapranov, three-way equivalence; Einsiedler-Kapranov-Lind 2006).** *Let $K$ be an algebraically closed field with surjective non-archimedean valuation $\operatorname{val} : K^* \to \mathbb{R}$, and let $I \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ be an ideal. Then $\operatorname{trop}(I)_{\text{val}} = \operatorname{trop}(I)_{\text{init}} = \operatorname{trop}(I)_{\text{amoeba}}$ as closed subsets of $\mathbb{R}^n$.*

The proof of the first equality is sketched in the Intermediate-tier section. The second equality is the analytic content: the complex amoeba $\mathcal{A}(V(I_t))$ of a coherent specialisation $I_t$ of $I$ to a complex one-parameter family converges in the Hausdorff topology to $\operatorname{trop}(I)_{\text{val}}$ as $t \to 0^+$, with explicit rate of convergence given by the Mikhalkin amoeba contraction lemma (Mikhalkin 2005). The convergence is uniform on compacts and respects the polyhedral structure of the limit.

The history: Kapranov stated all three definitions and their equivalence in his 2000 Hannover talks; Einsiedler-Kapranov-Lind 2006 supplied the first written exposition with full proofs in *J. Reine Angew. Math.* 601. The amoeba viewpoint had been developed independently by Gelfand-Kapranov-Zelevinsky (1994 monograph on discriminants, resultants, and multidimensional determinants), Mikhalkin (1996 Newton-polytope amoeba paper in *Topology* 39), and Forsberg-Passare-Tsikh (2000 amoeba volume paper in *Adv. Math.* 151).

**Theorem 2 (consistency of the polyhedral structure).** *The set $\operatorname{trop}(I) \subseteq \mathbb{R}^n$ admits a unique structure as a closed weighted balanced rational polyhedral complex compatible with the three constructions: the polyhedral cells are the closures of the equivalence classes of the relation $w \sim w' \iff \operatorname{in}_w(I) = \operatorname{in}_{w'}(I)$ on $\mathbb{R}^n$; the weights at each top-dimensional cell are the multiplicities $m(\operatorname{in}_w(I))$ of the initial ideal as an Artinian quotient of the residue Laurent ring after suitable saturation; the balancing condition at each codimension-one face is $\sum_i m_i v_i = 0$ in the appropriate quotient lattice.*

The polyhedral structure is the load-bearing combinatorial object. Maclagan-Sturmfels 2015 Ch. 3 develops the structure in book form; the key technical point is that the equivalence relation $w \sim w'$ has only finitely many classes inside any compact region of $\mathbb{R}^n$, by the finiteness of the Gröbner fan of $I$.

### Construction via Gröbner-fan / initial ideals

**Theorem 3 (Gröbner-fan finiteness; Sturmfels 1996, Mora-Robbiano 1988).** *For an ideal $I \subseteq K[x_1, \ldots, x_n]$ the equivalence classes of the relation $w \sim w' \iff \operatorname{in}_w(I) = \operatorname{in}_{w'}(I)$ form a finite rational-polyhedral fan in $\mathbb{R}^n$, the Gröbner fan of $I$. Each maximal cone corresponds to a tropical Gröbner basis adapted to weights in that cone.*

The proof goes through a Newton-polytope analysis combined with the Buchberger algorithm. Mora-Robbiano 1988 (*J. Symbolic Computation* 6) introduced the construction for term-order families; Sturmfels 1996 (book *Gröbner bases and convex polytopes*, AMS University Lecture Series 8) made it the central tool of Gröbner-fan theory.

**Theorem 4 (universal Gröbner basis from the fan).** *A finite generating set $\{g_1, \ldots, g_r\} \subseteq I$ that is simultaneously a Gröbner basis with respect to every weight $w$ in some open dense subset of $\mathbb{R}^n$ — equivalently, every $w$ in the interior of every maximal cone of the Gröbner fan — is called a universal Gröbner basis. Every ideal admits one, and it can be constructed by computing the Gröbner basis at one representative weight per maximal cone.*

Universal Gröbner bases are the computational backbone of tropical Gröbner-fan algorithms. Algorithms for computing $\operatorname{trop}(I)$ explicitly (Bogart-Jensen-Speyer-Sturmfels-Thomas 2007 *J. Symbolic Computation* 42; Gfan software by Anders Jensen) rely on universal Gröbner bases plus traversal of the cone lattice.

**Theorem 5 (the tropical link of a point determines the polyhedral structure locally).** *Let $w \in \operatorname{trop}(I) \subseteq \mathbb{R}^n$. The link of $\operatorname{trop}(I)$ at $w$ in $\mathbb{R}^n$ — that is, the intersection of $\operatorname{trop}(I)$ with a small sphere around $w$, or equivalently the projectivisation of the tangent cone — is identified with $\operatorname{trop}(\operatorname{in}_w(I))$ inside $\mathbb{R}^n$. This identification reduces the global polyhedral structure to a finite recursive computation of links.*

Maclagan-Sturmfels 2015 §3.6 develops this carefully. The link construction underwrites the recursive computation of tropical varieties via traversal of the Gröbner fan in the Gfan-style software.

### Non-archimedean amoebas and the Bieri-Groves polyhedral complex

**Theorem 6 (Bieri-Groves; Bieri-Groves 1984 *Crelle* 347).** *Let $G \subseteq T = (K^*)^n$ be a finitely generated subgroup of the algebraic torus over a non-archimedean valued field. Then the image $\operatorname{val}(G) \subseteq \mathbb{R}^n$ is a finite union of rational-polyhedral cones, of pure dimension $\operatorname{rank}_\mathbb{Z}(G)$.*

The Bieri-Groves theorem predates Kapranov by a decade and a half and is the polyhedral-finiteness fountainhead. Its original formulation was in the language of valuations on finitely generated abelian-by-cyclic subgroups; Maclagan-Sturmfels Ch. 3 reformulates it for ideals via the embedding $(K^*)^n / V(I)^c \hookrightarrow (K^*)^n$. The pure-dimension statement is the integer-theoretic precursor of Kapranov's pure-dimensionality.

**Theorem 7 (Bergman fan of a matroid; Sturmfels 2002, Ardila-Klivans 2006).** *For a linear ideal $I = (\sum a_{ij} x_j) \subseteq K[x_1^{\pm 1}, \ldots, x_n^{\pm 1}]$ defining a linear subspace $L \subseteq T$ of dimension $d$, the tropical variety $\operatorname{trop}(L)$ is the Bergman fan of the matroid $M(L)$ defined by the columns of the coefficient matrix: the polyhedral fan in $\mathbb{R}^n / \mathbf{1}$ whose maximal cones are indexed by maximal flags of flats of $M$.*

The Bergman fan construction (Ardila-Klivans 2006 *J. Combin. Theory Ser. B* 96) identifies the tropicalisation of a linear ideal with a purely matroid-theoretic object, providing a combinatorial classification of tropical linear spaces. Speyer-Sturmfels 2004 used this for the tropical Grassmannian $\operatorname{Tr}\,\mathrm{Gr}(2, n)$, identifying it with the space of phylogenetic trees on $n$ taxa.

### Worked example — tropicalisation of a quadric

**Theorem 8 (tropical quadric in $\mathbb{R}^2$).** *Consider the Laurent polynomial $f = x^2 + y^2 + 1$ over the Puiseux field. The tropical variety $\operatorname{trop}(V(f)) \subseteq \mathbb{R}^2$ is the three-rayed fan with rays in directions $(1, 1)$ (multiplicity-$2$ ray, from the lattice segment between $(2, 0)$ and $(0, 2)$), $(-1, 0)$ (multiplicity $2$), and $(0, -1)$ (multiplicity $2$), meeting at the origin. The balancing relation $2 \cdot (1, 1) + 2 \cdot (-1, 0) + 2 \cdot (0, -1) = (0, 0)$ holds; pure dimension equals the Krull dimension of $K[x, y]/(x^2 + y^2 + 1) = 1$.*

The quadric example illustrates the multiplicity / balancing structure that goes beyond the simple tropical line. Each ray carries a weight equal to the lattice length of the dual edge in the Newton polytope: the edge from $(2, 0)$ to $(0, 2)$ has lattice length $2$, the edge from $(0, 2)$ to $(0, 0)$ has lattice length $2$, etc. The multiplicity-weighted balancing is the discrete version of conservation of mass at each tropical vertex.

**Synthesis.** Kapranov's theorem is the foundational reason that tropical geometry is a faithful combinatorial shadow of algebraic geometry — not a heuristic, but an honest equivalence of three independently natural constructions. The central insight is that the strong triangle inequality of the non-archimedean valuation, combined with Hensel's lemma over the algebraically closed residue field, forces the three a priori different definitions to coincide. Putting these together, the theorem identifies polyhedral combinatorics with valuation theory and with complex amoeba asymptotics, giving three computational entry points to the same object — Gröbner-fan algorithms compute the initial-ideal side; non-archimedean point-lifting computes the valuation side; the Hausdorff-limit-of-amoebas captures the complex-analytic side.

This pattern appears again in [04.12.05] Mikhalkin's correspondence theorem, where the Gromov-Witten count of complex plane curves through prescribed points is identified with a tropical count of polyhedral curves through prescribed valuation-image points; the bridge is exactly Kapranov's theorem, and the foundational reason the tropical count gives the right answer is that lifting a residual root of the tropical curve to an honest algebraic point preserves the count multiplicatively at smooth tropical vertices. The same lift-counting strategy appears again in [04.12.06] the Nishinou-Siebert correspondence, where the algebraic counts of curves in toric varieties are reduced via toric degeneration to tropical counts on the Bieri-Groves dual intersection complex.

This pattern generalises further to non-archimedean amoebas (Sub-section 3 of Advanced results) where the Berkovich-analytic formulation, due in different formulations to Berkovich 1990 (Hodge-style spectra), Gubler 2007 (skeleton constructions), and Foster-Gross-Payne 2014 (tropicalisation from skeletons), identifies the tropical variety with the image of the Berkovich analytification of $V(I)$ under a continuous tropicalisation map. The bridge between the algebraic-geometric and analytic-geometric viewpoints is the universal property of the Berkovich space as the "right" topological space attached to a non-archimedean variety, and Kapranov's theorem says the tropicalisation map is surjective onto the polyhedral subset we called $\operatorname{trop}(I)$. The recursion stabilises here: the tropical variety is the universal polyhedral shadow.

The deeper structural payoff is in moduli theory and mirror symmetry. The Strominger-Yau-Zaslow conjecture [04.12.10] posits that mirror Calabi-Yau threefolds are obtained from each other by dualising along a special Lagrangian torus fibration; on the tropical side, this corresponds to dualising the Bieri-Groves polyhedral base of the SYZ fibration, and Kapranov's theorem ensures the tropical bases on both sides correctly encode the dual algebraic Calabi-Yau threefolds. The Gross-Siebert reconstruction theorem [04.12.09] makes this explicit: a "tropical manifold" (a polyhedral base $B$ together with slab functions / log structures) is the input that reconstructs an honest Calabi-Yau variety via a formal-power-series scattering-diagram algorithm; Kapranov's theorem is the foundational reason the algebraic output is correctly tracked by the polyhedral input.

## Full proof set [Master]

**Proposition (Kapranov's theorem — full forward direction).** *If $z \in V(I)(K)$ has $\operatorname{val}(z) = w$, then $\operatorname{in}_w(I)$ contains no monomial.*

*Proof.* Suppose $x^v \in \operatorname{in}_w(I)$ for some $v \in \mathbb{Z}^n$. Then $\operatorname{in}_w(f) = x^v$ for some $f \in I$. Write $f = \alpha t^a x^v + R$ with $\alpha \in \mathbb{C}^*$, the term $\alpha t^a x^v$ realising the tropical minimum $\mu = \operatorname{val}(\alpha t^a) + \langle v, w\rangle$, and every term of $R$ having strictly larger tropical value $> \mu$. Evaluate: $f(z) = \alpha t^a z^v + R(z)$. The valuation of $\alpha t^a z^v$ equals $\mu$; every term of $R(z)$ has valuation $> \mu$. By the strong triangle inequality of the non-archimedean valuation, $\operatorname{val}(f(z)) = \mu < \infty$. But $z \in V(I)$ forces $f(z) = 0$, hence $\operatorname{val}(f(z)) = \infty$. Contradiction. $\square$

**Proposition (Kapranov direction — at residually smooth points).** *Let $w \in \mathbb{Q}^n$ be a rational weight with $\operatorname{in}_w(I)$ containing no monomial. Suppose the residue variety $V(\operatorname{in}_w(I)) \subseteq (\mathbb{C}^*)^n$ is smooth at some residual point $\overline{z}$. Then there exists $z \in V(I)(K)$ with $\operatorname{val}(z) = w$ and residue $\overline{z}$.*

*Proof.* Choose a Gröbner basis $\{g_1, \ldots, g_r\}$ of $I$ with respect to weight $w$, refined to a finite generating set whose initial forms $\operatorname{in}_w(g_i)$ generate $\operatorname{in}_w(I)$. The Jacobian of $\{\operatorname{in}_w(g_i)\}$ at $\overline{z}$ has rank equal to the codimension $n - d$ of $V(\operatorname{in}_w(I))$ inside $(\mathbb{C}^*)^n$, by residual smoothness. Apply the multivariate non-archimedean Hensel lemma (e.g., Bourbaki *Commutative Algebra* Ch. III §4 Theorem 2): given a system of polynomials whose residual Jacobian at $\overline{z}$ has the right rank, there exists $z^* \in (K^*)^n$ with $g_i(z^*) = 0$ for all $i$ and residue of $t^{-w} \cdot z^* = \overline{z}$. (Here $t^{-w}$ is shorthand for componentwise $t^{-w_i}$, scaling each coordinate to make the valuation match.) Then $\operatorname{val}(z^*) = w$, $z^* \in V(I)(K)$, and the residue is $\overline{z}$ as desired. $\square$

**Proposition (Kapranov direction — full generality).** *Let $w \in \operatorname{trop}(I)_{\text{init}}$, possibly with $V(\operatorname{in}_w(I))$ residually singular. Then there exists $z \in V(I)(K)$ with $\operatorname{val}(z) = w$.*

*Proof sketch.* The residually-singular case requires an approximation argument. Berkovich-analytic point lifting: the Berkovich analytification $V(I)^{\mathrm{an}}$ is locally compact and Hausdorff (Berkovich 1990 *Spectral theory and analytic geometry over non-archimedean fields*). The tropicalisation map $\operatorname{trop} : V(I)^{\mathrm{an}} \to \mathbb{R}^n$ is continuous and (by surjectivity onto $\operatorname{trop}(I)_{\text{init}}$ established at residually-smooth points combined with continuity) extends to all of $\operatorname{trop}(I)_{\text{init}}$. The pre-image of any $w$ in the analytification contains a $K$-rational point (after possibly enlarging $K$ to its completion or to an algebraic closure of a larger field), by Berkovich's analytic Nullstellensatz. This produces $z \in V(I)(K)$ with $\operatorname{val}(z) = w$. Full details are in Gubler 2013 *A guide to tropicalizations* Theorem 5.1.6. $\square$

**Proposition (Bieri-Groves polyhedral structure for valuation images of subgroups; Bieri-Groves 1984).** *Let $G \subseteq T = (K^*)^n$ be a finitely generated subgroup of rank $r$. Then $\operatorname{val}(G) \subseteq \mathbb{R}^n$ is a finite union of rational polyhedral cones of pure dimension $r$.*

*Proof.* Pick generators $g_1, \ldots, g_r$ of $G$ projecting to a basis of $G / G_{\mathrm{tors}}$. The map $\mathbb{Z}^r \to \mathbb{R}^n$, $(k_1, \ldots, k_r) \mapsto \operatorname{val}(g_1^{k_1} \cdots g_r^{k_r}) = \sum_i k_i \operatorname{val}(g_i)$, is $\mathbb{Z}$-linear with image the $\mathbb{Z}$-span of $\operatorname{val}(g_1), \ldots, \operatorname{val}(g_r)$ inside $\mathbb{R}^n$. The image is a discrete subgroup of $\mathbb{R}^n$ if and only if $\operatorname{val}(g_1), \ldots, \operatorname{val}(g_r)$ are linearly independent over $\mathbb{R}$, which is generic. Closing under polyhedral combinations (replacing the discrete subgroup by its $\mathbb{R}_{\geq 0}$-span), one obtains a finite union of rational polyhedral cones; the closure under all integer combinations (including the $\mathbb{R}$-linear span if the value group is dense) gives the polyhedral subset of $\mathbb{R}^n$ defined by the rank-$r$ matrix $[\operatorname{val}(g_1), \ldots, \operatorname{val}(g_r)]$. The pure dimensionality follows from the rank-$r$ matrix being of full row rank generically. Bieri-Groves 1984 §2 develops this carefully. $\square$

**Proposition (multiplicity-balanced complex).** *Let $\operatorname{trop}(I)$ be equipped with the multiplicities $m_\sigma \in \mathbb{Z}_{>0}$ at each top-dimensional cell $\sigma$ defined as $m_\sigma = \sum_p \operatorname{length}(\mathcal{O}_{V(\operatorname{in}_w(I))_p})$ for any $w$ in the relative interior of $\sigma$, summed over isolated points $p$ of $V(\operatorname{in}_w(I))$ in the residue torus. Then at every codimension-one cell $\tau$ adjacent to top cells $\sigma_1, \ldots, \sigma_k$ with primitive normal directions $v_i \in \mathbb{Z}^n / \mathrm{span}_\mathbb{Z}(\tau)$, the balancing relation $\sum_i m_{\sigma_i} v_i = 0$ holds in $\mathbb{Z}^n / \mathrm{span}_\mathbb{Z}(\tau)$.*

*Proof.* The balancing is a Cartier-divisor / Chow-cycle conservation law: it expresses the fact that the rational function $\operatorname{in}_w(f)$, regarded as living on $V(\operatorname{in}_w(I))$ in the residue torus, has principal-divisor support that is balanced across the codimension-one wall. Formally, the codimension-one face $\tau$ corresponds to a residual ring $\mathbb{C}[x^{\pm 1}] / \operatorname{in}_\tau(I)$ of Krull dimension $d - 1$ ($d = \dim I$), and the top cones $\sigma_i$ correspond to specialisations to dimension-$d$ residue rings; the multiplicity $m_{\sigma_i}$ records the lattice index of the specialisation along $v_i$. The conservation law follows from the fact that the family $\operatorname{in}_w(I)$ as $w$ traverses a path through $\tau$ produces flat deformations whose generic and special fibres have the same Hilbert polynomial, and the multiplicities $m_{\sigma_i}$ at each top cell account for the deformation lengths. Full proof in Maclagan-Sturmfels 2015 §3.4 (and originally in Speyer 2004 dissertation). $\square$

**Proposition (consistency of multiplicities across constructions).** *The multiplicity $m_\sigma$ at a top-dimensional cell $\sigma \subset \operatorname{trop}(I)$ computed from the valuation-image side (counting $K$-rational points of $V(I)$ with fixed valuation, with multiplicity in the formal-completion sense) equals the multiplicity computed from the initial-ideal side (length of $V(\operatorname{in}_w(I))$ at $w$ in $\sigma$) equals the multiplicity computed from the amoeba side (number-of-sheets of the asymptotic amoeba cover at any point in the interior of $\sigma$).*

*Proof outline.* The three multiplicity computations all reduce to the residual ring $\mathbb{C}[x^{\pm 1}] / \operatorname{in}_w(I)$ via the proof of Kapranov's theorem. Specifically, on the valuation side, the formal-completion length of $V(I)$ at any $K$-point lying over $w$ is exactly the length of the residual ring at the corresponding residue point, by flatness of the formal-completion construction. On the initial side, the same length appears as the multiplicity of the corresponding component of $V(\operatorname{in}_w(I))$. On the amoeba side, the Mikhalkin amoeba asymptotic formula expresses the cover degree of $\mathcal{A}(V(I_t))$ over each tropical cell as the same multiplicity. Detailed treatment in Maclagan-Sturmfels 2015 §3.6. $\square$

## Connections [Master]

- **Algebraic torus and character/cocharacter lattices [04.11.01].** Kapranov's theorem lives entirely on the algebraic torus $T = (K^*)^n$ over the valued field $K$ — the character lattice $M = \mathbb{Z}^n$ is the integer-exponent lattice of monomials in the Laurent ring, the cocharacter lattice $N = \mathbb{Z}^n$ is paired against weight vectors $w \in \mathbb{R}^n$ via the integer-extended pairing $\langle u, w\rangle = \sum u_i w_i$, and $\operatorname{trop}(I)$ is by construction a subset of $N_\mathbb{R} = N \otimes_\mathbb{Z} \mathbb{R} = \mathbb{R}^n$. The lattice formalism of the prerequisite unit supplies the integer structure that makes the polyhedral / Gröbner-fan side rational-polyhedral: every cone of $\operatorname{trop}(I)$ is a rational polyhedral cone in $N_\mathbb{R}$ relative to the lattice $N$, and the multiplicities at top cells are integer lattice indices. The duality $M \leftrightarrow N$ also underwrites the tropical-curve / Newton-polygon duality: tropical-curve rays in $N_\mathbb{R}$ correspond to edges of the Newton polygon in $M_\mathbb{R}$, with multiplicities given by lattice lengths.

- **Fan and toric variety [04.11.04].** A complete toric variety $X_\Sigma$ corresponds to a complete fan $\Sigma$ in $N_\mathbb{R}$; conversely the polyhedral complex $\operatorname{trop}(I) \subseteq N_\mathbb{R}$ from Kapranov's theorem can be embedded inside $X_\Sigma$ via the toric tropicalisation map, identifying $\operatorname{trop}(I)$ with the polyhedral skeleton of the tropical compactification of $V(I)$. This is the conceptual bridge between toric geometry (Chapter 11) and tropical geometry (Chapter 12): the toric variety $X_\Sigma$ provides the ambient compactification, the fan $\Sigma$ provides the polyhedral combinatorics, and the tropical variety $\operatorname{trop}(I)$ is the polyhedral "intersection theory" of $V(I)$ inside $X_\Sigma$. Tevelev 2007 (*Compactifications of subvarieties of tori*, Amer. J. Math. 129) formalised the tropical-compactification picture; Maclagan-Sturmfels Ch. 6 develops it as the canonical interpretation of $\operatorname{trop}(I)$.

- **Tropical semiring and tropical polynomial [04.12.01].** The pre-requisite tropical-semiring unit defines the min-plus semiring structure on $\overline{\mathbb{R}} = \mathbb{R} \cup \{\infty\}$ — tropical addition $a \oplus b = \min(a, b)$, tropical multiplication $a \odot b = a + b$ — and the associated tropical polynomials as $\min$-of-linear-functions. Kapranov's theorem is the theorem that says these tropical polynomials are not merely formal combinatorial objects but honest shadows of algebraic-geometric vanishing loci: the tropical hypersurface defined by a tropical polynomial equals the tropicalisation of the corresponding algebraic hypersurface in the sense of all three Kapranov constructions. Without Kapranov's theorem, tropical polynomials would be heuristics; with it, they are exact polyhedral incarnations of algebraic objects.

- **Tropical curve (balanced rational metric graph) [04.12.02].** The tropical variety of a one-dimensional ideal $I$ in the Laurent ring (e.g., the ideal of a plane algebraic curve) is by Kapranov's theorem a one-dimensional polyhedral complex in $N_\mathbb{R}$ — a balanced rational metric graph in the language of tropical curves. The balancing condition $\sum_i m_i v_i = 0$ at each vertex is precisely the balancing axiom for tropical curves. The downstream unit packages this polyhedral data as the central object of tropical curve theory; Kapranov's theorem is the foundational reason that algebraic plane curves correspond to balanced rational metric graphs, opening the entrance to Mikhalkin's enumerative geometry.

- **Newton polytope and non-archimedean amoeba [04.12.04].** The Newton polytope $\operatorname{Newt}(f) \subseteq M_\mathbb{R}$ of a Laurent polynomial $f$ and the non-archimedean amoeba $\operatorname{trop}(V(f))$ are dual polyhedral objects: each ray of the tropical curve is the outward normal to an edge of the Newton polytope, with multiplicity equal to the lattice length of the edge. The downstream unit develops this duality in detail, and connects the amoeba construction with the Berkovich-analytic viewpoint via Foster-Gross-Payne's continuous tropicalisation map. Kapranov's theorem is the equivalence that lets the amoeba side be re-derived from the initial-ideal side, giving a computationally accessible polyhedral computation.

- **Mikhalkin's correspondence theorem [04.12.05].** Mikhalkin's celebrated correspondence theorem (Mikhalkin 2005 *J. Amer. Math. Soc.* 18) counts complex plane curves of given degree and genus passing through $3d - 1 + g$ prescribed points by counting their tropical-curve images passing through the corresponding tropical points. The proof passes through Kapranov's theorem at the structural level: the polyhedral count is the tropical-Gröbner-fan count, the algebraic count is the Gromov-Witten count, and the bridge is the Hensel-lift correspondence at residually smooth tropical vertices. Mikhalkin's theorem is the first major enumerative-geometric application of Kapranov's theorem.

- **Nishinou-Siebert correspondence [04.12.06].** The Nishinou-Siebert correspondence (Nishinou-Siebert 2006 *Duke Math. J.* 135) generalises Mikhalkin to toric Calabi-Yau threefolds: algebraic curve counts on a toric variety are computed by tropical curve counts on the polyhedral fan, with Kapranov's theorem supplying the foundational equivalence at the structural level. The downstream unit develops the toric-degeneration mechanism that combines Kapranov's theorem with Gross-Siebert log geometry.

- **Strominger-Yau-Zaslow conjecture [04.12.10].** SYZ posits that mirror Calabi-Yau threefolds are dualised special-Lagrangian torus fibrations; on the tropical side, this dualisation corresponds to dualising the Bieri-Groves polyhedral base. Kapranov's theorem ensures that the tropical bases on both sides correctly encode the dual algebraic Calabi-Yau threefolds, and the Gross-Siebert reconstruction theorem [04.12.09] makes the inverse algorithm explicit. The tropical-mirror-symmetry pipeline rests on Kapranov's theorem as its foundational pillar.

## Historical & philosophical context [Master]

Tropical geometry has a curious origin story: the name "tropical" was coined by French mathematicians in honour of the Brazilian mathematician Imre Simon, who in the late 1980s and early 1990s developed the algebra of the min-plus semiring for applications in finite automata and complexity theory. The systematic use of min-plus combinatorics as a tool in algebraic geometry was opened independently by several groups in the late 1990s: Mikhail Kapranov (in unpublished Hannover lecture notes from 2000), Grigory Mikhalkin (in his work on amoebas and counting plane curves), and Bernd Sturmfels (with his school at Berkeley, developing Gröbner-fan techniques into a tropical-geometry program). The first written statement of the three-way equivalence theorem now called Kapranov's theorem is in Manfred Einsiedler, Kapranov, and Douglas Lind's 2006 paper *Non-archimedean amoebas and tropical varieties* in *Journal für die reine und angewandte Mathematik* volume 601 [ref: TODO_REF EinsiedlerKapranovLind2006], which makes explicit the equivalence between the valuation-image definition, the initial-ideal definition, and the amoeba-limit definition that Kapranov had stated in his 2000 talks.

The polyhedral side of tropical geometry has earlier roots in the 1984 paper of Robert Bieri and John Groves [ref: TODO_REF BieriGroves1984] in *Journal für die reine und angewandte Mathematik* volume 347, which proved that the image of a finitely generated subgroup of an algebraic torus under a non-archimedean valuation is a finite union of rational polyhedral cones of pure dimension equal to the rank. Bieri and Groves were interested in this result for reasons connected to the Bieri-Strebel-Renz invariants of finitely generated groups; the tropical-geometry community absorbed the polyhedral-finiteness statement and reformulated it for ideals via the Kapranov equivalence. The amoeba side has independent roots in the 1994 monograph of Israel Gelfand, Kapranov, and Andrei Zelevinsky on *Discriminants, Resultants, and Multidimensional Determinants* (Birkhäuser, Mathematics: Theory and Applications) [ref: TODO_REF], where amoebas appeared as logarithmic-projection images of algebraic varieties for purposes of resultant-and-discriminant analysis.

The systematic exposition of tropical geometry as a self-standing subject began with Sturmfels' 2002 CBMS lectures *Solving Systems of Polynomial Equations* [ref: TODO_REF Sturmfels2002], which contains §9 on the Gröbner-fan formulation of tropical varieties and an early statement of Kapranov's theorem; Speyer and Sturmfels' 2004 paper *The tropical Grassmannian* [ref: TODO_REF SpeyerSturmfels2004] in *Advances in Geometry* provided the first significant worked example beyond the toric case, identifying $\operatorname{trop}(\mathrm{Gr}(2, n))$ with the space of phylogenetic trees on $n$ leaves; and Mikhalkin's 2005 *Journal of the American Mathematical Society* paper *Enumerative tropical algebraic geometry in $\mathbb{R}^2$* [ref: TODO_REF Mikhalkin2005] established the correspondence theorem that turned tropical geometry into a tool for solving classical Gromov-Witten counting problems. The canonical modern monograph is Diane Maclagan and Bernd Sturmfels' *Introduction to Tropical Geometry* (American Mathematical Society Graduate Studies in Mathematics volume 161, 2015) [ref: TODO_REF MaclaganSturmfels2015], with Chapter 3 devoted to Kapranov's theorem and its consequences.

The Berkovich-analytic reformulation of tropical geometry is due in different formulations to Vladimir Berkovich (in his 1990 monograph *Spectral theory and analytic geometry over non-archimedean fields*, AMS Mathematical Surveys 33), Walter Gubler (in his 2007 paper *Tropical varieties for non-archimedean analytic spaces*, *Invent. Math.* 169), and Brian Osserman with Sam Payne (in their 2013 paper *Lifting tropical intersections* in *Documenta Mathematica* 18). Gubler's 2013 survey *A guide to tropicalizations* [ref: TODO_REF Gubler2013] in *Nonarchimedean and Tropical Geometry* (Simons Symposia) consolidated this viewpoint, identifying the tropical variety with the image of the Berkovich analytification under a continuous tropicalisation map and lifting Kapranov's theorem to a structural statement about Berkovich skeletons. The Mark Gross and Bernd Siebert program for mirror symmetry [ref: TODO_REF] takes the tropical-side polyhedral data as the input for reconstructing Calabi-Yau varieties; their joint monograph *From real affine geometry to complex geometry* (Annals of Math. 174, 2011) and subsequent papers develop the reconstruction theorem that takes a "tropical manifold" (polyhedral base plus slab functions) as input and produces an algebraic Calabi-Yau via formal-power-series scattering-diagram algorithms. The line of development from Kapranov 2000 through Einsiedler-Kapranov-Lind 2006 through Maclagan-Sturmfels 2015 to Gross-Siebert 2011 is the canonical Forty-Year tropical-geometry research program.

## Bibliography [Master]

```bibtex
@article{EinsiedlerKapranovLind2006,
  author  = {Einsiedler, Manfred and Kapranov, Mikhail and Lind, Douglas},
  title   = {Non-archimedean amoebas and tropical varieties},
  journal = {Journal f{\"u}r die reine und angewandte Mathematik},
  volume  = {601},
  year    = {2006},
  pages   = {139--157}
}

@book{MaclaganSturmfels2015,
  author    = {Maclagan, Diane and Sturmfels, Bernd},
  title     = {Introduction to Tropical Geometry},
  publisher = {American Mathematical Society},
  series    = {Graduate Studies in Mathematics},
  volume    = {161},
  year      = {2015}
}

@article{BieriGroves1984,
  author  = {Bieri, Robert and Groves, J. R. J.},
  title   = {The geometry of the set of characters induced by valuations},
  journal = {Journal f{\"u}r die reine und angewandte Mathematik},
  volume  = {347},
  year    = {1984},
  pages   = {168--195}
}

@book{Sturmfels2002,
  author    = {Sturmfels, Bernd},
  title     = {Solving Systems of Polynomial Equations},
  publisher = {American Mathematical Society},
  series    = {CBMS Regional Conference Series in Mathematics},
  volume    = {97},
  year      = {2002}
}

@article{SpeyerSturmfels2004,
  author  = {Speyer, David and Sturmfels, Bernd},
  title   = {The tropical Grassmannian},
  journal = {Advances in Geometry},
  volume  = {4},
  year    = {2004},
  pages   = {389--411}
}

@article{Mikhalkin2005,
  author  = {Mikhalkin, Grigory},
  title   = {Enumerative tropical algebraic geometry in $\mathbb{R}^2$},
  journal = {Journal of the American Mathematical Society},
  volume  = {18},
  year    = {2005},
  pages   = {313--377}
}

@article{NishinouSiebert2006,
  author  = {Nishinou, Takeo and Siebert, Bernd},
  title   = {Toric degenerations of toric varieties and tropical curves},
  journal = {Duke Mathematical Journal},
  volume  = {135},
  year    = {2006},
  pages   = {1--51}
}

@article{Gubler2013,
  author    = {Gubler, Walter},
  title     = {A guide to tropicalizations},
  booktitle = {Nonarchimedean and Tropical Geometry},
  series    = {Simons Symposia},
  publisher = {Springer},
  year      = {2013},
  pages     = {125--189}
}

@book{ItenbergMikhalkinShustin2007,
  author    = {Itenberg, Ilia and Mikhalkin, Grigory and Shustin, Eugenii},
  title     = {Tropical Algebraic Geometry},
  publisher = {Birkh{\"a}user},
  series    = {Oberwolfach Seminars},
  volume    = {35},
  year      = {2007}
}

@book{Berkovich1990,
  author    = {Berkovich, Vladimir G.},
  title     = {Spectral theory and analytic geometry over non-archimedean fields},
  publisher = {American Mathematical Society},
  series    = {Mathematical Surveys and Monographs},
  volume    = {33},
  year      = {1990}
}

@article{Gubler2007,
  author  = {Gubler, Walter},
  title   = {Tropical varieties for non-archimedean analytic spaces},
  journal = {Inventiones Mathematicae},
  volume  = {169},
  year    = {2007},
  pages   = {321--376}
}

@article{Tevelev2007,
  author  = {Tevelev, Jenia},
  title   = {Compactifications of subvarieties of tori},
  journal = {American Journal of Mathematics},
  volume  = {129},
  year    = {2007},
  pages   = {1087--1104}
}

@article{ArdilaKlivans2006,
  author  = {Ardila, Federico and Klivans, Caroline J.},
  title   = {The Bergman complex of a matroid and phylogenetic trees},
  journal = {Journal of Combinatorial Theory, Series B},
  volume  = {96},
  year    = {2006},
  pages   = {38--49}
}

@article{BogartJensenSpeyerSturmfelsThomas2007,
  author  = {Bogart, Tristram and Jensen, Anders N. and Speyer, David and Sturmfels, Bernd and Thomas, Rekha R.},
  title   = {Computing tropical varieties},
  journal = {Journal of Symbolic Computation},
  volume  = {42},
  year    = {2007},
  pages   = {54--73}
}

@article{GrossSiebert2011,
  author  = {Gross, Mark and Siebert, Bernd},
  title   = {From real affine geometry to complex geometry},
  journal = {Annals of Mathematics},
  volume  = {174},
  year    = {2011},
  pages   = {1301--1428}
}
```
