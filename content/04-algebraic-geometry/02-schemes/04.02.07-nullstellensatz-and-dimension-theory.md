---
id: 04.02.07
title: "Nullstellensatz and dimension theory"
slug: nullstellensatz-and-dimension-theory
section: algebraic-geometry
chapter: schemes
concept_catalog_id: alg-geom.nullstellensatz-dimension
prerequisites:
  - 04.02.02  # affine scheme
  - 04.02.04  # morphism of schemes
successors: []
tier_anchors:
  beginner: "Strogatz analogous level for the dictionary between polynomial equations and their common zero sets"
  intermediate: "Atiyah-Macdonald *Introduction to Commutative Algebra* Ch. 5, 7, 11; Hartshorne §I.1; Eisenbud *Commutative Algebra* Ch. 1, 8–10; Reid *Undergraduate Commutative Algebra* §3, §6, §8"
  master: "Hilbert 1893 (originator); Krull 1928 (Hauptidealsatz); Zariski 1947 (modern Zariski-lemma proof); Eisenbud *Commutative Algebra with a View Toward Algebraic Geometry* Ch. 8–13; Matsumura *Commutative Ring Theory* Ch. 5, 14, 17; Stacks Project Tags 00FV (Nullstellensatz), 00KW (Krull dimension), 00KZ (Hauptidealsatz)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Hilbert 1893 — Ueber die vollen Invariantensysteme"
    locator: "Math. Ann. 42, 313–373; Nullstellensatz appears in the appendix via elimination theory"
  - source: TODO_REF
    path: "Krull 1928 — Primidealketten in allgemeinen Ringbereichen"
    locator: "S.-B. Heidelberg Akad. Wiss. (Hauptidealsatz / height theorem)"
  - source: TODO_REF
    path: "Noether 1921 — Idealtheorie in Ringbereichen"
    locator: "Math. Ann. 83, 24–66 (modern Noetherian foundations of ideal theory)"
  - source: TODO_REF
    path: "Zariski 1947 — A new proof of Hilbert's Nullstellensatz"
    locator: "Bull. AMS 53, 362–368 (modern Zariski-lemma proof)"
  - source: TODO_REF
    path: "Rainich (Rabinowitsch) 1929 — Zum Hilbertschen Nullstellensatz"
    locator: "Math. Ann. 102, 520 (the Rabinowitsch trick: weak ⇒ strong via auxiliary variable)"
  - source: TODO_REF
    path: "Lasker 1905 — Zur Theorie der Moduln und Ideale"
    locator: "Math. Ann. 60, 20–116 (primary decomposition; companion to Nullstellensatz)"
  - source: TODO_REF
    path: "Atiyah-Macdonald — Introduction to Commutative Algebra"
    locator: "Addison-Wesley 1969; Ch. 5 (integral dependence, Nullstellensatz), Ch. 11 (dimension theory)"
  - source: TODO_REF
    path: "Hartshorne — Algebraic Geometry"
    locator: "§I.1 (Nullstellensatz and the variety/ideal correspondence)"
  - source: TODO_REF
    path: "Eisenbud — Commutative Algebra with a View Toward Algebraic Geometry"
    locator: "Springer GTM 150, 1995; Ch. 1 (Nullstellensatz), Ch. 8 (integral extensions, going-up/going-down), Ch. 9–10 (dimension theory, Hilbert-Samuel polynomial), Ch. 13 (Cohen-Macaulay rings)"
  - source: TODO_REF
    path: "Matsumura — Commutative Ring Theory"
    locator: "Cambridge Studies in Advanced Mathematics 8, 1986; Ch. 5 (dimension), Ch. 14 (Hauptidealsatz), Ch. 17 (Cohen-Macaulay rings, catenary rings)"
  - source: TODO_REF
    path: "Stacks Project"
    locator: "Tags 00FV (Nullstellensatz), 00FW (Zariski lemma), 00KW (Krull dimension), 00KZ (Hauptidealsatz); https://stacks.math.columbia.edu"
  - source: TODO_REF
    path: "Brownawell 1987 — Bounds for the degrees in the Nullstellensatz"
    locator: "Annals of Math. 126, 577–591 (effective Nullstellensatz, polynomial degree bounds)"
  - source: TODO_REF
    path: "Kollár 1988 — Sharp effective Nullstellensatz"
    locator: "Journal AMS 1, 963–975 (sharpens Brownawell to optimal $d^n$)"
  - source: TODO_REF
    path: "Stengle 1974 — A Nullstellensatz and a Positivstellensatz in semialgebraic geometry"
    locator: "Math. Ann. 207, 87–97 (real Nullstellensatz / Positivstellensatz)"
  - source: TODO_REF
    path: "Ananyan-Hochster 2020 — Small subalgebras of polynomial rings and Stillman's conjecture"
    locator: "Journal AMS 33, 291–309 (Stillman's conjecture, formerly Ananyan-Hochster theorem)"
lean_status: none
lean_mathlib_gap: |
  Mathlib has the Hilbert basis theorem (`Mathlib.RingTheory.Polynomial.Basic`),
  the Nullstellensatz over $\mathbb{C}$ via the Zariski lemma
  (`Mathlib.RingTheory.Polynomial.Nullstellensatz` and the
  `IsAlgClosed.algebraicallyClosed` infrastructure), the predicate
  `Ideal.IsJacobson` capturing the maximal-ideal-as-radical content of
  the strong form, and Krull-dimension predicates `Ring.KrullDim` /
  `Module.support` for noetherian rings. What is not yet assembled in
  Mathlib at the time of writing: the full equivalence of the three
  classical dimension invariants for finitely generated algebras over a
  field (Krull dimension = transcendence degree of the function field
  $= \deg$ Hilbert-Samuel polynomial of the local ring at a closed
  point); the Hauptidealsatz / Krull height theorem in its general form
  (`PrimeSpectrum.height_minimal_le_one` over a principal ideal and the
  multivariable generalisation are absent); the dimension formula for
  catenary rings; the going-up / going-down theorems of
  Cohen-Seidenberg as stated for arbitrary integral extensions; the
  Cohen-Macaulay predicate and the unmixedness theorem; the effective
  Nullstellensatz with explicit Bezout-style degree bounds; and the
  real Nullstellensatz / Positivstellensatz of Stengle and Krivine. The
  aggregated gap is the chapter 8–13 backbone of Eisenbud's commutative
  algebra book translated into Mathlib's ring-theoretic vocabulary.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 45m
  master: 70m
status: shipped
produced_by: claude-opus-4-7 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Algebraic geometry begins with a dictionary. On one side: systems of polynomial equations. On the other: their common solution sets. The Nullstellensatz, Hilbert's *theorem of zeros* from 1893, says the dictionary is honest — for an algebraically closed field like the complex numbers, an ideal of polynomials and the geometric set it cuts out determine each other up to a precise correction. That correction is the *radical* of the ideal: the set of polynomials some power of which lies in the ideal. Once you account for it, ideals and their zero sets are two sides of the same coin.

Two payoff statements live inside this dictionary. First, a consistent system of polynomial equations — one whose ideal is a proper subset of the polynomial ring — always has a solution over an algebraically closed field. There is no way to write down equations that secretly have no answer. Second, every maximal ideal in $k[x_1, \ldots, x_n]$ corresponds to a single point of $k^n$ — the most rigid algebraic structure matches the most basic geometric structure.

Once geometry and algebra are paired, you can ask how big the geometric object is. The dimension of an affine variety has three matching descriptions: the largest number of independent coordinates you can vary on it, the longest chain of nested irreducible subvarieties inside it, and the longest chain of prime ideals in its coordinate ring. They agree, and the agreement is the start of dimension theory.

## Visual [Beginner]

Two parallel pictures. Left: an ideal in $k[x, y]$ generated by $y - x^2$ and $y$. Right: the common solution set in $k^2$, the single point at the origin. The Nullstellensatz says one side recovers the other up to taking radicals.

![A polynomial ideal on the left and its common zero set on the right, joined by the variety / ideal correspondence of the Nullstellensatz, with a third panel showing a chain of prime ideals matched against a chain of nested irreducible varieties to illustrate dimension.](/img/nullstellensatz-and-dimension-theory-placeholder.svg)

## Worked example [Beginner]

Take the ideal $\mathfrak{a} = (x^2, xy)$ inside $\mathbb{C}[x, y]$. Its zero set $V(\mathfrak{a})$ in $\mathbb{C}^2$ is the set of points $(a, b)$ where $a^2 = 0$ and $ab = 0$ — exactly the line $a = 0$, that is, the $y$-axis. The defining ideal of that line — the polynomials vanishing on the whole $y$-axis — is $(x)$, the principal ideal generated by $x$.

So we have $V(\mathfrak{a}) = V(x)$ even though $\mathfrak{a} = (x^2, xy) \neq (x)$. The Nullstellensatz says these two ideals share the same zero set precisely because their radicals agree. And indeed $x^2 \in \mathfrak{a}$ and $xy = x \cdot y \in \mathfrak{a}$, so $x \in \sqrt{\mathfrak{a}}$; conversely, $(x) \supseteq \mathfrak{a}$ contains $\mathfrak{a}$, so $\sqrt{(x)} = (x) \supseteq \sqrt{\mathfrak{a}}$. Therefore $\sqrt{\mathfrak{a}} = (x)$, and the dictionary checks out.

Now the dimension count. The $y$-axis is a one-dimensional variety: you can parametrise it by the single coordinate $y$, so it has one degree of freedom. On the algebra side, the coordinate ring is $\mathbb{C}[x, y]/(x) = \mathbb{C}[y]$, a polynomial ring in one variable. Its prime ideals form the chain $(0) \subset (y - c)$ for any $c \in \mathbb{C}$, of length one. Its function field is $\mathbb{C}(y)$, with transcendence degree one over $\mathbb{C}$. All three counts give the same number.

What this tells us. The Nullstellensatz is the bridge that makes algebraic geometry possible: you can compute on either side of the dictionary and translate the answer. Dimension is the first invariant that the dictionary lets you read off, and the three descriptions agree precisely because the dictionary is honest.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Working over $\mathbb{C}$, what is the relationship between an ideal $\mathfrak{a} \subseteq \mathbb{C}[x_1, \ldots, x_n]$ and the ideal $I(V(\mathfrak{a}))$ of polynomials vanishing on its zero set?

A. They are always equal.
B. $I(V(\mathfrak{a}))$ is the radical of $\mathfrak{a}$.
C. $I(V(\mathfrak{a}))$ is the kernel of the natural quotient.
D. They have no general relationship.

<details class="exercise__hint"><summary>Hint</summary>

The Nullstellensatz states the precise correction: passing from $\mathfrak{a}$ to its zero set and back recovers $\mathfrak{a}$ only up to taking some root.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Feedback-correct: yes; Hilbert's strong Nullstellensatz says $I(V(\mathfrak{a})) = \sqrt{\mathfrak{a}}$ for an algebraically closed base field. Feedback-wrong: A holds only when $\mathfrak{a}$ is already a radical ideal; the example $\mathfrak{a} = (x^2)$ has $\sqrt{\mathfrak{a}} = (x) \neq \mathfrak{a}$. C and D miss the structural content of the theorem.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true/false).**

Over $\mathbb{C}$, every maximal ideal of $\mathbb{C}[x_1, \ldots, x_n]$ has the form $(x_1 - a_1, \ldots, x_n - a_n)$ for some point $(a_1, \ldots, a_n) \in \mathbb{C}^n$.

<details class="exercise__hint"><summary>Hint</summary>

This is the maximal-ideal form of the Nullstellensatz; it identifies maximal ideals with the closed points of $\mathbb{C}^n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Feedback-correct: yes; over an algebraically closed field, the maximal-ideal form of the Nullstellensatz says every maximal ideal corresponds to a single point of affine $n$-space. The quotient $\mathbb{C}[x_1, \ldots, x_n] / (x_1 - a_1, \ldots, x_n - a_n)$ is $\mathbb{C}$, the residue field. Feedback-wrong: this fails for non-algebraically-closed fields — for instance, $(x^2 + 1) \subset \mathbb{R}[x]$ is maximal but does not correspond to a real point.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise (medium, numeric).**

What is the dimension of the affine variety $V(xy - 1) \subseteq \mathbb{A}^2_{\mathbb{C}}$?

<details class="exercise__hint"><summary>Hint</summary>

The variety is a hypersurface — the zero set of a single non-constant polynomial in two variables. Krull's height theorem tells you the codimension, and the dimension formula does the rest.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$1$.** Feedback-correct: yes; $\mathbb{A}^2_{\mathbb{C}}$ has dimension $2$, and a hypersurface inside it — the zero set of a single irreducible polynomial — has codimension $1$ by Krull's height theorem, so its dimension is $2 - 1 = 1$. The variety is the hyperbola $xy = 1$, parametrised by the single coordinate $x \neq 0$. Feedback-wrong: $0$ would be a single point, $2$ would be all of $\mathbb{A}^2$.

</details>

</aside>

## Formal definition [Intermediate+]

Fix an algebraically closed field $k$ and the polynomial ring $R = k[x_1, \ldots, x_n]$. Two operations link ideals of $R$ to subsets of $k^n$. For an ideal $\mathfrak{a} \subseteq R$, the *vanishing locus* is

$$
V(\mathfrak{a}) = \{ a = (a_1, \ldots, a_n) \in k^n : f(a) = 0 \text{ for all } f \in \mathfrak{a} \}.
$$

For a subset $S \subseteq k^n$, the *vanishing ideal* is

$$
I(S) = \{ f \in R : f(a) = 0 \text{ for all } a \in S \}.
$$

The *radical* of an ideal $\mathfrak{a}$ is $\sqrt{\mathfrak{a}} = \{ f \in R : f^N \in \mathfrak{a} \text{ for some } N \geq 1 \}$. An ideal is *radical* if $\sqrt{\mathfrak{a}} = \mathfrak{a}$.

**Theorem (Hilbert's Nullstellensatz, three forms).** *Over an algebraically closed field $k$ and $R = k[x_1, \ldots, x_n]$:*

*(Weak form.) For every proper ideal $\mathfrak{a} \subsetneq R$, the locus $V(\mathfrak{a}) \subseteq k^n$ is non-empty.*

*(Strong form.) For every ideal $\mathfrak{a} \subseteq R$, the equality $I(V(\mathfrak{a})) = \sqrt{\mathfrak{a}}$ holds. Equivalently, $f \in I(V(\mathfrak{a}))$ if and only if some power $f^N$ lies in $\mathfrak{a}$.*

*(Maximal-ideal form.) Every maximal ideal $\mathfrak{m} \subset R$ has the form $\mathfrak{m}_a = (x_1 - a_1, \ldots, x_n - a_n)$ for a unique point $a = (a_1, \ldots, a_n) \in k^n$.*

The three forms are equivalent over an algebraically closed field. The maximal-ideal form is most often the working version: it identifies the closed points of $\mathrm{Spec}(R)$ with $k^n$ and gives a clean correspondence between radical ideals and Zariski-closed subsets of $k^n$.

**Definition (Krull dimension).** For a commutative ring $R$, the *Krull dimension* $\dim R$ is the supremum of lengths of chains of prime ideals $\mathfrak{p}_0 \subsetneq \mathfrak{p}_1 \subsetneq \cdots \subsetneq \mathfrak{p}_d$ in $R$. The *height* of a prime $\mathfrak{p}$ is $\dim R_{\mathfrak{p}}$, equivalently the supremum of lengths of chains of primes ending at $\mathfrak{p}$.

**Definition (transcendence-degree dimension).** Let $X \subseteq k^n$ be an irreducible affine variety with coordinate ring $k[X] = R/I(X)$ and function field $k(X) = \mathrm{Frac}(k[X])$. The *transcendence-degree dimension* of $X$ is $\mathrm{tr.deg}_k k(X)$ — the size of any transcendence basis of $k(X)$ over $k$.

**Theorem (equivalence of dimension invariants).** *For a finitely generated $k$-algebra $A = k[X]$ with $X$ irreducible over the algebraically closed field $k$:*

$$
\dim X = \dim_{\mathrm{Krull}} A = \mathrm{tr.deg}_k \mathrm{Frac}(A).
$$

*Moreover, for a maximal ideal $\mathfrak{m} \subset A$ corresponding to a closed point of $X$:*

$$
\dim A_{\mathfrak{m}} = \dim A
$$

*when $A$ is the coordinate ring of an irreducible affine variety; the Krull dimension is the same at every closed point.*

**Theorem (Krull's Hauptidealsatz, 1928).** *Let $R$ be a noetherian ring and $f \in R$. Every minimal prime $\mathfrak{p}$ over the principal ideal $(f)$ has height $\mathrm{ht}(\mathfrak{p}) \leq 1$. If $f$ is a non-zero-divisor and not a unit, $\mathrm{ht}(\mathfrak{p}) = 1$. More generally, every minimal prime over an ideal $(f_1, \ldots, f_r) \subseteq R$ has height at most $r$.*

In geometric language, intersecting an irreducible variety $X$ with the zero locus of a single equation $f = 0$ either gives back $X$ (when $f$ vanishes identically) or cuts the dimension down by at most one. Cutting with $r$ equations can drop dimension by at most $r$.

### Counterexamples to common slips

- The Nullstellensatz fails over non-algebraically-closed fields. Over $\mathbb{R}$, the ideal $(x^2 + 1) \subset \mathbb{R}[x]$ is proper but $V(x^2 + 1) = \emptyset$ in $\mathbb{R}$.
- The strong form requires *radical*, not just inclusion: $V((x^2)) = V((x)) = \{0\} \subset \mathbb{A}^1$, and $I(\{0\}) = (x) = \sqrt{(x^2)} \neq (x^2)$.
- Krull dimension is not always finite. The ring $k[x_1, x_2, \ldots]$ in countably many variables has infinite Krull dimension. Even noetherian rings can have infinite Krull dimension — Nagata exhibited such a ring in 1962.
- For non-irreducible varieties the equality $\dim A_{\mathfrak{m}} = \dim A$ fails: the ring $k[x, y]/(xy)$ has two minimal primes corresponding to the two coordinate axes, both of height $0$ but each contributing dimension $1$.
- The Hauptidealsatz needs the noetherian hypothesis. In a non-noetherian ring, principal ideals can have minimal primes of arbitrarily high height.

## Key theorem with proof [Intermediate+]

**Theorem (Strong Nullstellensatz via the Rabinowitsch trick).** *Let $k$ be algebraically closed, $R = k[x_1, \ldots, x_n]$, $\mathfrak{a} \subseteq R$ an ideal, and $f \in R$. If $f$ vanishes on $V(\mathfrak{a}) \subseteq k^n$, then $f^N \in \mathfrak{a}$ for some $N \geq 1$.*

The argument splits in two: first prove the weak form (every proper ideal has a zero), then derive the strong form by Rabinowitsch's auxiliary-variable trick.

**Step 1 (Weak Nullstellensatz via the Zariski lemma).** *If $\mathfrak{a} \subsetneq R$ is a proper ideal, then $V(\mathfrak{a}) \neq \emptyset$.*

*Proof of Step 1.* Pick a maximal ideal $\mathfrak{m}$ containing $\mathfrak{a}$, which exists by Zorn's lemma since $\mathfrak{a}$ is proper. The quotient $K = R/\mathfrak{m}$ is a field, and $K$ is finitely generated as a $k$-algebra because $R$ is.

Apply the *Zariski lemma*: if $K$ is a field finitely generated as a $k$-algebra, then $K$ is a finite algebraic extension of $k$. Since $k$ is algebraically closed, $K = k$.

The composite $R \to K = k$ is a $k$-algebra homomorphism, so it sends each $x_i$ to some $a_i \in k$. Set $a = (a_1, \ldots, a_n) \in k^n$. Then the kernel of $R \to k$ is exactly $\mathfrak{m}_a = (x_1 - a_1, \ldots, x_n - a_n)$, hence $\mathfrak{m} = \mathfrak{m}_a$ and $a \in V(\mathfrak{m}) \subseteq V(\mathfrak{a})$. $\square$

The Zariski lemma itself is proved by Noether normalisation: a finitely generated $k$-algebra that is also a domain admits algebraically independent elements $y_1, \ldots, y_d$ such that the algebra is module-finite over $k[y_1, \ldots, y_d]$. If the algebra is a field, the polynomial ring $k[y_1, \ldots, y_d]$ must also be a field — which forces $d = 0$, so the algebra is module-finite over $k$, hence algebraic. The reference is Atiyah-Macdonald §5 [ref: TODO_REF Atiyah-Macdonald Ch. 5] and Eisenbud §13 [ref: TODO_REF Eisenbud §13].

**Step 2 (strong form via Rabinowitsch).** Suppose $f \in R$ vanishes on $V(\mathfrak{a})$. Introduce a new variable $y$ and form the ideal

$$
\mathfrak{b} = (\mathfrak{a}, 1 - yf) \subseteq R[y] = k[x_1, \ldots, x_n, y].
$$

Consider the locus $V(\mathfrak{b}) \subseteq k^{n+1}$. A point $(a_1, \ldots, a_n, b)$ lies in $V(\mathfrak{b})$ if and only if $(a_1, \ldots, a_n) \in V(\mathfrak{a})$ and $1 - bf(a) = 0$. Since $f$ vanishes on $V(\mathfrak{a})$ by hypothesis, $f(a) = 0$ at every $a \in V(\mathfrak{a})$, and the equation $1 - bf(a) = 1 = 0$ has no solution. Therefore $V(\mathfrak{b}) = \emptyset$.

By the weak form (Step 1), $\mathfrak{b}$ is not proper, so $\mathfrak{b} = R[y]$. Write $1$ as a $R[y]$-linear combination of generators:

$$
1 = \sum_{i} h_i(x, y) g_i(x) + h_0(x, y) (1 - yf(x)),
$$

where $g_1, \ldots, g_m$ generate $\mathfrak{a}$ and the $h_i$ lie in $R[y]$.

Now substitute $y = 1/f$ — formally, work in the localisation $R[1/f]$. The factor $(1 - yf)$ becomes $0$, so

$$
1 = \sum_{i} h_i(x, 1/f) g_i(x) \quad \text{in } R[1/f].
$$

Multiply through by a high enough power $f^N$ to clear all denominators on the right. The result is an identity $f^N = \sum_i \tilde h_i(x) g_i(x)$ in $R$, where $\tilde h_i(x) = f^{N_i} h_i(x, 1/f) \in R$ for sufficiently large $N$. Thus $f^N \in \mathfrak{a}$. $\square$

The Rabinowitsch trick converts vanishing on the zero set to algebraic membership in the ideal. The trick is a recurrent technique in commutative algebra — adjoining a free variable to invert an element creates an empty locus precisely when the element vanishes everywhere on the original locus.

**Bridge.** This dictionary identifies radical ideals with Zariski-closed sets and builds toward [04.02.04] (morphism of schemes), where the same correspondence becomes the contravariant equivalence between affine schemes and commutative rings, and appears again in [04.05.01] (Weil divisor) where height-one primes correspond to codimension-one irreducible subvarieties via the Hauptidealsatz. The dimension equivalences put down here generalise to schemes through Krull-dimension localisation, and dimension theory is dual to depth theory — Cohen-Macaulay rings are exactly those for which the two agree. Putting these together, the foundational reason a noetherian local ring has a finite, well-defined dimension is that its maximal ideal can be cut down to a parameter system whose length is exactly the Krull dimension, and that length is precisely what the Hauptidealsatz controls.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 1 (easy, symbolic).**

Compute $\sqrt{\mathfrak{a}}$ for $\mathfrak{a} = (x^3, x^2 y, x y^2) \subseteq k[x, y]$. Then identify $V(\mathfrak{a}) \subseteq k^2$ and confirm $I(V(\mathfrak{a})) = \sqrt{\mathfrak{a}}$.

<details class="exercise__hint"><summary>Hint</summary>

Each generator is $x^a y^b$ with $a \geq 1$, so the ideal contains $x^k$ for some $k$ but not necessarily $x$ or $y$ directly. Look at which monomials are forced into the radical.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The ideal $\mathfrak{a}$ contains $x^3$ and $xy^2$. From $x^3 \in \mathfrak{a}$ and the radical's defining property, $x \in \sqrt{\mathfrak{a}}$. From $xy^2 \in \mathfrak{a}$ and $x \in \sqrt{\mathfrak{a}}$ alone we cannot deduce $y \in \sqrt{\mathfrak{a}}$ — but $V(\mathfrak{a}) = \{(a, b) \in k^2 : a^3 = a^2 b = a b^2 = 0\}$, which forces $a = 0$, so $V(\mathfrak{a}) = \{(0, b) : b \in k\}$ is the $y$-axis.

The ideal of the $y$-axis is $(x)$. Therefore $I(V(\mathfrak{a})) = (x)$, and the strong Nullstellensatz says $\sqrt{\mathfrak{a}} = (x)$. Direct check: every generator of $\mathfrak{a}$ has $x$ as a factor, so $\mathfrak{a} \subseteq (x)$ and hence $\sqrt{\mathfrak{a}} \subseteq (x)$; conversely $x \in \sqrt{\mathfrak{a}}$ since $x^3 \in \mathfrak{a}$, giving the reverse inclusion. $\sqrt{\mathfrak{a}} = (x)$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

Determine the dimension of the twisted cubic $C = \{(t, t^2, t^3) : t \in \mathbb{A}^1_k\} \subseteq \mathbb{A}^3_k$ over an algebraically closed field $k$.

<details class="exercise__hint"><summary>Hint</summary>

Compute the function field of $C$ and find a transcendence basis over $k$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$1$.** The coordinate ring of $C$ is $k[t]$ (project onto the first coordinate, which gives an isomorphism $k[x, y, z]/(y - x^2, z - x^3) \xrightarrow{\sim} k[t]$ via $x \mapsto t$, $y \mapsto t^2$, $z \mapsto t^3$). The function field is $k(t)$, and a transcendence basis over $k$ is $\{t\}$, so $\mathrm{tr.deg}_k k(t) = 1$.

Equivalently, the prime-chain length in $k[t]$ is $1$ — the chain $(0) \subsetneq (t - c)$ — and the Krull dimension equals $1$. All three dimension counts agree.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Show that the ideal of the twisted cubic in $\mathbb{A}^3_k$ is $I = (y - x^2, z - x^3)$ — verifying this is the ideal of the variety, not merely a defining one.

<details class="exercise__hint"><summary>Hint</summary>

Show the quotient $k[x, y, z]/(y - x^2, z - x^3)$ is isomorphic to $k[x]$, hence a domain. The radical of a prime ideal is itself.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Set $J = (y - x^2, z - x^3)$. Define the $k$-algebra homomorphism $\phi : k[x, y, z] \to k[t]$ by $\phi(x) = t$, $\phi(y) = t^2$, $\phi(z) = t^3$. Then $\phi(y - x^2) = t^2 - t^2 = 0$ and $\phi(z - x^3) = t^3 - t^3 = 0$, so $J \subseteq \ker \phi$.

Conversely, given $f \in \ker \phi$, use the relations $y = x^2 + (y - x^2)$ and $z = x^3 + (z - x^3)$ to rewrite $f$ modulo $J$ as a polynomial in $x$ alone. If this $\bar f \in k[x]$ satisfies $\phi(\bar f) = \bar f(t) = 0$, then $\bar f = 0$ in $k[x]$, so $f \in J$. Therefore $J = \ker \phi$, the quotient $k[x, y, z]/J \cong k[t]$ is a domain, and $J$ is prime, hence radical.

By the strong Nullstellensatz, $J = \sqrt{J} = I(V(J)) = I(C)$. The image of $\phi$ is exactly the coordinate ring of $C$, confirming $J$ is the ideal of the variety.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 4 (medium, numeric).**

Let $X = V(xz - y^2, x^3 - yz) \subseteq \mathbb{A}^3_k$. Determine $\dim X$ and write down a chain of prime ideals of maximal length in the coordinate ring.

<details class="exercise__hint"><summary>Hint</summary>

The variety is the closure of the rational normal curve image of the parametrisation $t \mapsto (t^3, t^4, t^5)$. Compare with the twisted cubic.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$1$.** Define $\phi : k[x, y, z] \to k[t]$ by $x \mapsto t^3$, $y \mapsto t^4$, $z \mapsto t^5$. Then $\phi(xz - y^2) = t^8 - t^8 = 0$ and $\phi(x^3 - yz) = t^9 - t^9 = 0$, so the image lies in $V(xz - y^2, x^3 - yz)$ and $X$ contains the curve $\{(t^3, t^4, t^5)\}$.

The image of $\phi$ is the subring $k[t^3, t^4, t^5] \subseteq k[t]$, a one-dimensional domain. After taking the radical of the defining ideal we get exactly the ideal $\ker \phi$, so $\dim X = \dim k[t^3, t^4, t^5] = 1$.

A chain of prime ideals of length $1$ in $k[t^3, t^4, t^5]$ is $(0) \subsetneq (t^3 - c, t^4 - c^{4/3}, t^5 - c^{5/3})$ for some non-zero $c \in k$ — that is, the prime corresponding to a closed point on the curve. No longer chain exists because the dimension is $1$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="proof" markdown="1">

**Exercise 5 (medium, proof).**

Prove the maximal-ideal form of the Nullstellensatz from the weak form, over algebraically closed $k$.

<details class="exercise__hint"><summary>Hint</summary>

Apply the weak form to $\mathfrak{m}$ itself. The point produced gives the form of $\mathfrak{m}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\mathfrak{m} \subset R = k[x_1, \ldots, x_n]$ be a maximal ideal. Then $\mathfrak{m}$ is in particular proper, so by the weak form $V(\mathfrak{m}) \neq \emptyset$. Pick a point $a = (a_1, \ldots, a_n) \in V(\mathfrak{m})$.

Every $f \in \mathfrak{m}$ vanishes at $a$, so $f \in (x_1 - a_1, \ldots, x_n - a_n) =: \mathfrak{m}_a$. Thus $\mathfrak{m} \subseteq \mathfrak{m}_a$. The ideal $\mathfrak{m}_a$ is proper (it does not contain the constant $1$), and $\mathfrak{m}$ is maximal, so $\mathfrak{m} = \mathfrak{m}_a$.

Uniqueness: if also $\mathfrak{m} = \mathfrak{m}_b$, then $x_i - a_i$ and $x_i - b_i$ both lie in $\mathfrak{m}$, so $a_i - b_i \in \mathfrak{m}$ as a constant, forcing $a_i = b_i$. So the point is unique. $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="proof" markdown="1">

**Exercise 6 (hard, proof).**

Prove Krull's Hauptidealsatz for principal ideals: in a noetherian ring $R$, every minimal prime $\mathfrak{p}$ over a principal ideal $(f)$ has height at most $1$.

<details class="exercise__hint"><summary>Hint</summary>

Localise at $\mathfrak{p}$ to reduce to a noetherian local ring with a single maximal ideal. Use symbolic powers and the descending chain condition on a quotient ring.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Replace $R$ by $R_{\mathfrak{p}}$, so we may assume $R$ is a noetherian local ring with maximal ideal $\mathfrak{p}$ and $\mathfrak{p}$ is the unique minimal prime over $(f)$. We must show every prime $\mathfrak{q} \subsetneq \mathfrak{p}$ has $\mathrm{ht}(\mathfrak{q}) = 0$, i.e., $\mathfrak{q}$ is a minimal prime of $R$.

Suppose for contradiction $\mathfrak{q}_1 \subsetneq \mathfrak{q}_2 \subsetneq \mathfrak{p}$ is a chain of primes of length $2$. Pass to $R/\mathfrak{q}_1$, again a noetherian local ring, where $\mathfrak{p}/\mathfrak{q}_1$ is the unique minimal prime over $(\bar f)$. So we may assume $R$ is also a domain (replacing $\mathfrak{q}_1$ by $(0)$).

In this local domain $R$ with $\mathfrak{p}$ minimal over $(f)$, consider the ring $\bar R = R/(f)$. Since $\mathfrak{p}$ is the unique minimal prime of $\bar R$, the ring $\bar R$ has $\mathrm{Spec}(\bar R)$ a single point with $\mathfrak{p}/(f)$ as nilradical, so $\bar R$ is *artinian* (a noetherian ring with finitely many primes, all maximal, has the descending chain condition).

For each $n \geq 1$, define the *symbolic power* $\mathfrak{q}_2^{(n)} = \mathfrak{q}_2^n R_{\mathfrak{q}_2} \cap R$, the saturation of $\mathfrak{q}_2^n$ at $\mathfrak{q}_2$. The chain $\mathfrak{q}_2^{(1)} \supseteq \mathfrak{q}_2^{(2)} \supseteq \cdots$ has images in $\bar R$ which descend, hence stabilise: $(\mathfrak{q}_2^{(n)} + (f))/(f) = (\mathfrak{q}_2^{(n+1)} + (f))/(f)$ for some $n$, equivalently $\mathfrak{q}_2^{(n)} \subseteq \mathfrak{q}_2^{(n+1)} + (f)$.

Now $f \notin \mathfrak{q}_2$ (else $\mathfrak{p}$ would not be minimal over $(f)$ — a contradiction since $\mathfrak{q}_2 \subsetneq \mathfrak{p}$). Working in $R_{\mathfrak{q}_2}$, the element $f$ is a unit, so $\mathfrak{q}_2^{(n)} R_{\mathfrak{q}_2} \subseteq \mathfrak{q}_2^{(n+1)} R_{\mathfrak{q}_2}$, forcing equality $\mathfrak{q}_2^n R_{\mathfrak{q}_2} = \mathfrak{q}_2^{n+1} R_{\mathfrak{q}_2}$. By Nakayama's lemma applied to the noetherian local ring $R_{\mathfrak{q}_2}$, this gives $\mathfrak{q}_2^n R_{\mathfrak{q}_2} = 0$, hence $\mathfrak{q}_2 R_{\mathfrak{q}_2} = 0$ since $R_{\mathfrak{q}_2}$ is a domain — contradicting $\mathfrak{q}_1 \subsetneq \mathfrak{q}_2$.

So no length-$2$ chain $\mathfrak{q}_1 \subsetneq \mathfrak{q}_2 \subsetneq \mathfrak{p}$ exists, hence $\mathrm{ht}(\mathfrak{p}) \leq 1$. $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="proof" markdown="1">

**Exercise 7 (hard, proof).**

Show that for an irreducible affine variety $X$ over algebraically closed $k$, the Krull dimension of the coordinate ring $A = k[X]$ equals the transcendence degree $\mathrm{tr.deg}_k k(X)$.

<details class="exercise__hint"><summary>Hint</summary>

Use Noether normalisation to reduce to the polynomial-ring case, then combine the going-up theorem of Cohen-Seidenberg with the dimension formula for module-finite extensions.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Apply Noether normalisation to the finitely generated $k$-algebra $A$: there exist algebraically independent elements $y_1, \ldots, y_d \in A$ over $k$ such that the inclusion $k[y_1, \ldots, y_d] \hookrightarrow A$ is module-finite. The transcendence degree of $\mathrm{Frac}(A)$ over $k$ equals $d$, by definition of algebraic independence and the inclusion $k(y_1, \ldots, y_d) \subseteq \mathrm{Frac}(A)$ being algebraic (a module-finite extension is algebraic on fraction fields).

The polynomial ring $B = k[y_1, \ldots, y_d]$ has Krull dimension $d$ — exhibit the chain $(0) \subsetneq (y_1) \subsetneq (y_1, y_2) \subsetneq \cdots \subsetneq (y_1, \ldots, y_d)$ — and the upper bound $d$ follows from the Hauptidealsatz applied iteratively.

For the module-finite extension $B \hookrightarrow A$, the going-up theorem of Cohen-Seidenberg lifts every chain of primes in $B$ to a chain in $A$, and the incomparability of primes in module-finite extensions ensures lifted chains have the same length. The going-down theorem (when $B$ is normal, which it is for a polynomial ring) lifts chains in the other direction. Together, $\dim A = \dim B = d = \mathrm{tr.deg}_k \mathrm{Frac}(A)$. $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="conceptual" markdown="1">

**Exercise 8 (hard, conceptual).**

State the dimension formula for catenary rings and explain how it sharpens the equivalence of dimension invariants.

<details class="exercise__hint"><summary>Hint</summary>

A noetherian ring is catenary when every two saturated chains of primes between the same endpoints have the same length. The dimension formula relates $\mathrm{ht}(\mathfrak{p}) + \dim(R/\mathfrak{p})$ to $\dim R$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

A noetherian ring $R$ is *catenary* if for every pair of primes $\mathfrak{p} \subseteq \mathfrak{q}$ in $R$, all saturated chains of primes from $\mathfrak{p}$ to $\mathfrak{q}$ have the same length. $R$ is *universally catenary* if every finitely generated $R$-algebra is catenary.

For a universally catenary noetherian local domain $R$ with $\mathfrak{p}$ a prime, the *dimension formula* reads

$$
\mathrm{ht}(\mathfrak{p}) + \dim(R/\mathfrak{p}) = \dim R.
$$

Geometrically, for an irreducible affine variety $X$ and an irreducible closed subvariety $Y \subseteq X$, the codimension of $Y$ in $X$ plus the dimension of $Y$ equals the dimension of $X$. This is the formula students expect from intuition — but it requires the catenary hypothesis. Nagata exhibited a noetherian local domain with a non-catenary chain in which the formula fails.

Finitely generated algebras over a field are universally catenary (Eisenbud Ch. 13), so for the affine varieties in this unit the formula applies without comment. The catenary hypothesis is the structural condition under which Krull dimension behaves additively over inclusion of closed subvarieties — a refinement of the equivalence-of-dimensions theorem that promotes the equality of three numbers to a coherent dimension theory across a stratification.

</details>

</aside>

## Lean formalization [Intermediate+]

`lean_status: none` — Mathlib has the Hilbert basis theorem, the Nullstellensatz over $\mathbb{C}$ via the Zariski lemma, and Krull-dimension predicates, but the equivalence of the three classical dimension invariants and the Hauptidealsatz in its general form are not yet assembled.

```lean
-- Mathlib has, at the time of writing:
--
--   Mathlib.RingTheory.Polynomial.Basic    -- Hilbert basis theorem
--   Mathlib.RingTheory.Polynomial.Nullstellensatz
--                                          -- weak Nullstellensatz over C
--   Mathlib.RingTheory.Jacobson            -- IsJacobson predicate;
--                                          -- maximal-ideal form
--   Mathlib.RingTheory.Polynomial.Tower    -- transcendence degree
--   Mathlib.Order.KrullDimension           -- KrullDim of a ring
--
-- What is not yet assembled:
--   * the chain of equivalences
--       Ring.KrullDim A
--         = Polynomial.transcendenceDegree (Frac A)
--         = HilbertSamuelPolynomial.degree (A.localization m)
--     for finitely generated k-algebras with k algebraically closed;
--   * Krull's Hauptidealsatz over a principal ideal:
--       theorem Krull_Hauptidealsatz {R : Type*} [CommRing R]
--         [IsNoetherianRing R] (f : R) :
--         ∀ p ∈ minimalPrimes (Ideal.span {f}), p.height ≤ 1
--   * the multivariable height theorem;
--   * Cohen-Seidenberg going-up / going-down for arbitrary integral
--     extensions (incomparability is in Mathlib; lying-over is not
--     yet packaged with going-down for normal domains).
```

## Advanced results [Master]

**Going-up and going-down (Cohen-Seidenberg).** Let $R \subseteq S$ be an integral extension of rings. *Lying over*: every prime $\mathfrak{p} \subset R$ has a prime $\mathfrak{q} \subset S$ with $\mathfrak{q} \cap R = \mathfrak{p}$. *Incomparability*: if $\mathfrak{q}_1 \subsetneq \mathfrak{q}_2$ in $S$ then their contractions in $R$ are distinct. *Going-up*: for primes $\mathfrak{p}_1 \subseteq \mathfrak{p}_2$ in $R$ and $\mathfrak{q}_1$ in $S$ with $\mathfrak{q}_1 \cap R = \mathfrak{p}_1$, there exists $\mathfrak{q}_2 \supseteq \mathfrak{q}_1$ with $\mathfrak{q}_2 \cap R = \mathfrak{p}_2$. *Going-down* (when $R$ is a normal domain and $S$ is a domain): the analogous downward lifting. These theorems were established by Cohen and Seidenberg in the 1940s and form the structural foundation underneath the equivalence of Krull and transcendence-degree dimension.

**Catenary and universally catenary rings.** A noetherian ring $R$ is *catenary* if every saturated chain of primes between any two primes has the same length, and *universally catenary* if every finitely generated $R$-algebra is catenary. Finitely generated algebras over a field, complete noetherian local rings, and Cohen-Macaulay rings are universally catenary. Nagata 1956 exhibited a noetherian local domain that is not catenary, showing the hypothesis is genuinely needed for the dimension formula $\mathrm{ht}(\mathfrak{p}) + \dim(R/\mathfrak{p}) = \dim R$.

**Cohen-Macaulay rings.** A noetherian local ring $(R, \mathfrak{m})$ is *Cohen-Macaulay* when its depth equals its Krull dimension. Equivalently, every system of parameters is a regular sequence — generators of an $\mathfrak{m}$-primary ideal that act non-zero-divisor-by-non-zero-divisor on each successive quotient. Cohen-Macaulay rings are universally catenary; their unmixedness theorem says every minimal prime over an ideal generated by a regular sequence has the same height. Hochster and Huneke developed tight closure as a structural extension of Cohen-Macaulay theory in characteristic $p$.

**Krull's intersection theorem.** For a noetherian local ring $(R, \mathfrak{m})$ and any ideal $I$ with $I \subseteq \mathfrak{m}$, the intersection $\bigcap_{n \geq 1} I^n$ vanishes when $I = \mathfrak{m}$ and $R$ is a domain — and more generally equals the kernel of the $I$-adic completion map. The proof goes through the Artin-Rees lemma. Krull's intersection theorem is the structural input that lets formal power series rings $\widehat{R}_{\mathfrak{m}}$ act as "infinitesimal completions" of $R$ at $\mathfrak{m}$.

**Hilbert-Samuel polynomial.** For a noetherian local ring $(R, \mathfrak{m})$ and an $\mathfrak{m}$-primary ideal $\mathfrak{q}$, the function $n \mapsto \mathrm{length}(R/\mathfrak{q}^n)$ agrees with a polynomial $P_{\mathfrak{q}}(n)$ for $n \gg 0$. Its degree equals $\dim R$, and the leading coefficient times $\dim(R)!$ is the *multiplicity* $e_{\mathfrak{q}}(R)$. The dimension of $R$ admits three equivalent descriptions: Krull dimension, $\deg P_{\mathfrak{q}}$, and the minimum number of generators of an $\mathfrak{m}$-primary ideal — the "system-of-parameters" definition.

**Effective Nullstellensatz.** For an algebraically closed field $k$ and polynomials $f, g_1, \ldots, g_m \in k[x_1, \ldots, x_n]$ with $\max \deg(g_i) \leq d$ and $f$ vanishing on the common zero set of the $g_i$, there exist polynomials $h_i$ and an exponent $N$ with $f^N = \sum h_i g_i$ and explicit degree bounds. Brownawell 1987 proved $N \leq n d^n$ in characteristic zero; Kollár 1988 sharpened to the optimal $N \leq d^n$ for $d \geq 3$, with a separate sharp bound at $d = 2$. The effective bounds make the Nullstellensatz a quantitative tool in arithmetic geometry and complexity theory.

**Real Nullstellensatz and Positivstellensatz.** Stengle 1974 and independently Krivine 1964 proved a Nullstellensatz for the real-field case, replacing radicals by *real radicals* and using sums of squares. Stengle's *Positivstellensatz* characterises polynomials non-negative (resp. positive, vanishing) on a basic semialgebraic set $\{g_1 \geq 0, \ldots, g_m \geq 0\}$ via cone identities involving sums of squares. This is the foundation of real algebraic geometry — the Tarski-Seidenberg quantifier-elimination theorem and the modern theory of polynomial optimisation rest on it.

**Arithmetic Nullstellensatz over $\mathbb{Z}$.** A version over $\mathbb{Z}$ replaces "no zeros over algebraic closure" by "the ideal contains $1$ in $\mathbb{Z}[x_1, \ldots, x_n]$, possibly after passing to a localisation." The arithmetic Nullstellensatz of Masser-Wüstholz, Krick-Pardo and others gives explicit bounds combining the polynomial degrees and the heights of coefficients.

**Model-theoretic content: quantifier elimination.** The theory $\mathrm{ACF}_p$ of algebraically closed fields of characteristic $p$ is *complete and model-complete*, and admits *quantifier elimination*. A first-order formula in $\mathrm{ACF}$ is equivalent to a quantifier-free formula. The Nullstellensatz is the algebraic content: the set of points where a system of polynomial equations has a solution is itself defined by a system of polynomial equations — no genuine projection / existential quantifier is needed once the field is algebraically closed. The Ax-Grothendieck theorem (every injective polynomial map $\mathbb{C}^n \to \mathbb{C}^n$ is surjective) and Chevalley's theorem on constructible sets are downstream consequences.

**Stillman's conjecture (Ananyan-Hochster theorem).** Stillman conjectured in the early 2000s that the projective dimension of a homogeneous ideal in a polynomial ring is bounded by a function of the *number* and *degrees* of generators alone, independent of the ambient number of variables. Ananyan and Hochster 2020 proved the conjecture using small-subalgebra arguments, providing universal bounds. The result reframes ideal-theoretic dimension invariants as functions of the generator data, decoupled from the ambient ring size.

**Synthesis.** The Nullstellensatz is a translation theorem; dimension theory is what the translated language can say. The three forms — weak, strong ($I(V(\mathfrak{a})) = \sqrt{\mathfrak{a}}$), maximal-ideal — are equivalent reformulations of the same data, each tuned to a different working setting. The Rabinowitsch trick is the bridge that makes weak ⇒ strong: introducing an auxiliary variable converts vanishing-on-the-zero-set into emptiness-of-an-extended-locus. Putting these together with Krull's Hauptidealsatz, the central insight is that *codimension* is genuinely well-defined: cutting an irreducible variety by a single non-vanishing equation drops dimension by exactly one, and dimension itself admits a triple description as Krull dimension, transcendence degree of the function field, and Hilbert-Samuel polynomial degree. This trichotomy generalises to noetherian local rings via the system-of-parameters characterisation, identifies dimension theory with the spectral theory of the Zariski topology, and is dual to depth theory through the Cohen-Macaulay condition. Dimension is an instance of the deeper pattern that algebraic and geometric structure on a variety are interchangeable through the coordinate-ring functor — a pattern that appears again in [04.05.01] (Weil divisor) when codimension-one subvarieties are matched against height-one primes, and in [04.02.04] (morphism of schemes) when morphisms of varieties become ring maps in the opposite direction.

## Full proof set [Master]

The strong Nullstellensatz via the Rabinowitsch trick is proved in the key-theorem section. Detailed proofs of Krull's Hauptidealsatz (Exercise 6 sketches the principal-ideal case; the multivariable case follows by induction using minimal-prime decomposition), the equivalence of Krull, transcendence-degree, and Hilbert-Samuel-polynomial dimension for finitely generated algebras over a field (Exercise 7 for the first equivalence; the Hilbert-Samuel agreement via the standard noetherian-local argument with system of parameters), Cohen-Seidenberg going-up and going-down, and the dimension formula for universally catenary rings are referenced to Atiyah-Macdonald §11 [ref: TODO_REF Atiyah-Macdonald Ch. 11], Eisenbud Ch. 8–13 [ref: TODO_REF Eisenbud Ch. 8–13], and Matsumura Ch. 14, 17 [ref: TODO_REF Matsumura Ch. 14]. The Brownawell-Kollár effective Nullstellensatz, Stengle's Positivstellensatz, and the Ananyan-Hochster theorem are stated without proof — see the bibliography for the original papers.

## Connections [Master]

- **Affine scheme [04.02.02]** — the Nullstellensatz makes the contravariant functor $\mathrm{Spec}$ from commutative rings to affine schemes an equivalence on the affine-variety side, identifying radical ideals of $k[x_1, \ldots, x_n]$ with closed subschemes of $\mathbb{A}^n_k$ and maximal ideals with closed points.

- **Morphism of schemes [04.02.04]** — the maximal-ideal correspondence is the reason morphisms of affine varieties over an algebraically closed field correspond to $k$-algebra homomorphisms in the opposite direction, generalising to the anti-equivalence between commutative rings and affine schemes.

- **Weil divisor [04.05.01]** — Krull's Hauptidealsatz is the structural input that makes Weil divisors well-defined: codimension-one irreducible subvarieties of an irreducible variety correspond exactly to height-one primes in the coordinate ring, and the divisor of a rational function counts vanishing orders along these height-one primes.

- **Smooth, étale, and unramified morphisms [04.02.05]** — dimension theory underwrites the smoothness criterion, where a morphism $f : X \to Y$ is smooth of relative dimension $n$ when its fibres are regular of dimension $n$. The cotangent-rank criterion ($\Omega^1_{X/Y}$ locally free of rank $n$) compares against the dimension of the fibre via the Jacobian.

- **Sheaf of differentials [04.08.01]** — the dimension of a smooth variety equals the rank of its sheaf of Kähler differentials, tying the abstract dimension invariants of this unit to the geometric cotangent calculus on a variety.

## Historical & philosophical context [Master]

David Hilbert's *Ueber die vollen Invariantensysteme* (1893, *Math. Ann.* 42, 313–373) introduced what is now called the Nullstellensatz in the appendix, where Hilbert used elimination theory and the resultant to establish the variety-ideal correspondence. The 1893 paper is also where the Hilbert basis theorem appears, foreshadowing the noetherian foundations. The proof technique was elementary but elimination-theoretic; the modern proof via the Zariski lemma is due to Oscar Zariski's 1947 *Bull. AMS* note *A new proof of Hilbert's Nullstellensatz* (53, 362–368).

Wolfgang Krull's *Primidealketten in allgemeinen Ringbereichen* (1928, *Sitzungsberichte der Heidelberger Akademie der Wissenschaften*) introduced the Hauptidealsatz — the height theorem — establishing that a principal ideal cuts dimension by at most one in any noetherian ring. Krull's contribution shifted dimension theory from a counting principle on varieties to a structural theorem on chains of primes in arbitrary noetherian rings, and the Hauptidealsatz is the algebraic engine behind Bezout-style intersection-counting in modern algebraic geometry. The auxiliary-variable proof of strong-from-weak is due to George Yuri Rainich, writing under the pen name "Rabinowitsch" (1929, *Math. Ann.* 102, 520) — a one-page note that is one of the more memorable two-line proofs in mathematics.

Emmy Noether's *Idealtheorie in Ringbereichen* (1921, *Math. Ann.* 83, 24–66) established the noetherian foundations on which the modern statements rest. Noether's *ascending chain condition* and her abstract treatment of ideals replaced the case-by-case constructive arguments of Hilbert and Lasker with a uniform structural theory. Emanuel Lasker's *Zur Theorie der Moduln und Ideale* (1905, *Math. Ann.* 60, 20–116), written during Lasker's career as world chess champion, gave primary decomposition for ideals in polynomial rings, generalised by Noether to all noetherian rings.

The model-theoretic perspective came later. Tarski, Seidenberg, Robinson, and Ax established that the theory of algebraically closed fields admits quantifier elimination and is model-complete, recasting the Nullstellensatz as the algebraic content of a logical reduction. Effective bounds in the Nullstellensatz were established by W. D. Brownawell (1987, *Annals of Math.* 126, 577–591) and sharpened by János Kollár (1988, *Journal AMS* 1, 963–975), giving polynomial-time-relevant degree bounds for the Bezout-style identity. The real-algebraic counterpart — the Positivstellensatz of Gilbert Stengle (1974, *Math. Ann.* 207, 87–97) and Jean-Louis Krivine — completes the picture for ordered fields and grounds the field of polynomial optimisation. Most recently, Tigran Ananyan and Melvin Hochster proved Stillman's conjecture (2020, *Journal AMS* 33, 291–309), confirming that projective dimension of a homogeneous ideal depends only on generator data, not on the ambient polynomial ring.

## Bibliography [Master]

**Originators.**

- **Hilbert**, *Ueber die vollen Invariantensysteme*, Math. Ann. 42 (1893), 313–373. The Nullstellensatz in the appendix, alongside the Hilbert basis theorem.
- **Lasker**, *Zur Theorie der Moduln und Ideale*, Math. Ann. 60 (1905), 20–116. Primary decomposition.
- **Noether**, *Idealtheorie in Ringbereichen*, Math. Ann. 83 (1921), 24–66. Noetherian foundations.
- **Krull**, *Primidealketten in allgemeinen Ringbereichen*, S.-B. Heidelberg Akad. Wiss. (1928). Hauptidealsatz.
- **Rainich (Rabinowitsch)**, *Zum Hilbertschen Nullstellensatz*, Math. Ann. 102 (1929), 520. Auxiliary-variable trick.
- **Zariski**, *A new proof of Hilbert's Nullstellensatz*, Bull. AMS 53 (1947), 362–368.

**Structural follow-ups.**

- **Cohen-Seidenberg**, *Prime ideals and integral dependence*, Bull. AMS 52 (1946), 252–261. Going-up and going-down.
- **Nagata**, *On the chain problem of prime ideals*, Nagoya Math. J. 10 (1956), 51–64. Non-catenary noetherian local domain.
- **Stengle**, *A Nullstellensatz and a Positivstellensatz in semialgebraic geometry*, Math. Ann. 207 (1974), 87–97.
- **Brownawell**, *Bounds for the degrees in the Nullstellensatz*, Annals of Math. 126 (1987), 577–591.
- **Kollár**, *Sharp effective Nullstellensatz*, Journal AMS 1 (1988), 963–975. Optimal bound $d^n$.
- **Ananyan-Hochster**, *Small subalgebras of polynomial rings and Stillman's conjecture*, Journal AMS 33 (2020), 291–309.

**Textbook references.**

- **Atiyah-Macdonald**, *Introduction to Commutative Algebra*, Addison-Wesley, 1969. Ch. 5, 7, 11.
- **Hartshorne**, *Algebraic Geometry*, Springer GTM 52, 1977. §I.1.
- **Eisenbud**, *Commutative Algebra with a View Toward Algebraic Geometry*, Springer GTM 150, 1995. Ch. 1, 8–10, 13.
- **Matsumura**, *Commutative Ring Theory*, Cambridge Studies in Advanced Mathematics 8, 1986. Ch. 5, 14, 17.
- **Reid**, *Undergraduate Commutative Algebra*, LMS Student Texts 29, 1995. §3, §6, §8.
- **Stacks Project**, https://stacks.math.columbia.edu. Tags 00FV, 00FW, 00KW, 00KZ.
