---
id: 48.02.21
title: "Rearrangement and self-indexing Morse functions"
slug: rearrangement-and-self-indexing-morse-functions
section: differential-geometry
chapter: manifolds
concept_catalog_id: differential-topology.rearrangement-self-indexing
prerequisites:
  - 48.02.20  # handles, surgery, and the cobordism category
  - 48.02.30  # Morse functions, Morse lemma, index
successors: []
tier_anchors:
  beginner: "Milnor Lectures on the h-Cobordism Theorem §4 informal; Matsumoto An Introduction to Morse Theory Ch. 3 (ordering the handles)"
  intermediate: "Milnor Lectures on the h-Cobordism Theorem §4 (Theorems 4.1, 4.8); Kosinski Differential Manifolds Ch. VI §§4-5"
  master: "Milnor Lectures on the h-Cobordism Theorem §§4-6; Smale On the structure of manifolds (1962); Kosinski Ch. VI-VII"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: fasttrack-texts
    path: "03-modern-geometry/Bott-LecturesOnMorseTheory.pdf"
    locator: "Gradient-like vector fields; ordering critical values; the first deformation theorem"
  - source: TODO_REF
    path: "Milnor — Lectures on the h-Cobordism Theorem (Princeton Mathematical Notes, 1965)"
    locator: "§4: Rearrangement of cobordisms (Theorem 4.1), self-indexing Morse functions (Theorem 4.8), gradient-like vector fields (notes by L. Siebenmann and J. Sondow)"
  - source: TODO_REF
    path: "Smale — On the structure of manifolds (1962)"
    locator: "American Journal of Mathematics 84, pp. 387--399; self-indexing functions and the handlebody filtration"
  - source: TODO_REF
    path: "Smale — Generalized Poincaré's conjecture in dimensions greater than four (1961)"
    locator: "Annals of Mathematics 74, pp. 391--406; ordering handles by index in the proof"
  - source: TODO_REF
    path: "Morse — The existence of polar non-degenerate functions on differentiable manifolds (1960)"
    locator: "Annals of Mathematics 71, pp. 352--383; functions with one maximum and one minimum, an antecedent of self-indexing"
lean_status: none
lean_mathlib_gap: |
  Mathlib has smooth manifolds (`SmoothManifoldWithCorners`), vector fields,
  and flows of time-dependent vector fields (`ODE`/`integralCurve`), but no
  Morse-theoretic or gradient-like layer, so none of the rearrangement
  machinery is expressible yet. Missing in particular: (1) a `GradientLike`
  predicate pairing a Morse function $f : W \to [0,1]$ with a vector field
  $\xi$ satisfying $\xi(f) > 0$ off the critical set and the Euclidean normal
  form near each critical point; (2) the ascending and descending manifolds
  $W^s(p), W^u(p)$ of $\xi$ as embedded discs, which require the stable-manifold
  theorem for a hyperbolic rest point in the manifold setting (Mathlib has no
  stable-manifold theorem); (3) the First Deformation / Rearrangement theorem,
  whose proof modifies $f$ to $f + \rho\cdot(\text{shift})$ along a region
  swept by the flow that meets no other descending manifold, needing both the
  flow-box theorem and a compactly-supported reparametrisation lemma; (4) the
  existence of a self-indexing function $f(p) = \operatorname{ind}(p)$, an
  induction over index that repeatedly applies (3); and (5) the Morse-Smale
  chain complex $C_*(f,\xi)$ with boundary map given by the signed count of
  $\xi$-trajectories between adjacent-index critical points, which needs the
  transversality of $W^u(p) \pitchfork W^s(q)$ and the compactness of the
  $1$-dimensional trajectory moduli. Each is a concrete formalization target
  on top of the existing flow and manifold hierarchy.
human_reviewer: "Tyler (pending external differential-topology reviewer)"
estimated_time:
  beginner: 16m
  intermediate: 42m
  master: 80m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Building a shape by gluing on handles, one at a time, leaves you with a pile of handles attached in whatever order the height function happened to visit them. A low pass might come before a high valley; the bookkeeping is a jumble. The natural wish is to tidy it: glue all the simplest handles first, then the next-simplest, and so on up to the most complicated. Sorting the handles by how they attach is the goal of this unit.

The key freedom is that you may slide the heights of the level spots up and down, so long as you do not let two of them collide while one is trying to pass the other. Think of two climbers on the same rope at different heights. As long as their paths down the mountain never cross, you can raise one and lower the other freely, swapping which is higher. Two handles can be reordered exactly when their attaching paths miss each other.

The tidiest arrangement of all is the one where the height of each level spot is its own complexity number. A valley sits at height zero, the simplest passes at height one, the next at height two, on up to the peaks at the top. A height function arranged this way reads off the complexity of each spot directly from how high it sits. This is called *self-indexing*, because the height equals the index — the count of downhill directions.

The payoff is a clean ladder. The shape is built floor by floor, each floor adding only handles of one fixed complexity. That orderly ladder is the scaffolding on which the deepest theorems about smooth shapes are later raised.

## Visual [Beginner]

On the left, a tall cobordism slab with four level spots scattered at random heights: a valley low down, a complicated peak just above it, then a simple pass higher up, then another. Arrows show two of the spots being slid past each other — one raised, one lowered — because the downhill paths streaming out of them never touch.

![On the left, a vertical cobordism with four critical points at jumbled heights, two of them marked with up and down arrows indicating they are being swapped; faint downhill streamlines from each show the two never cross. On the right, the same cobordism after rearrangement: the four critical points now sit on evenly spaced floors, each labelled with its index 0, 1, 2, 3, the height of each floor equal to its index — a self-indexing arrangement drawn as a clean ladder.](/img/rearrangement-and-self-indexing-morse-functions-placeholder.svg)

On the right, the finished, self-indexing picture: the same four spots now sit on evenly spaced floors, each floor labelled with a complexity number, and the height of every spot equals its number. The jumble has become a ladder. The takeaway: when downhill paths miss, heights can be sorted, and the tidiest sort puts each spot at the floor named by its index.

## Worked example [Beginner]

**Sorting a two-handle stack on the doughnut cobordism.** Recall the doughnut surface, built from a disk and two bent-bar handles. Place it inside a slab and give the slab a height function whose level spots are: one valley (a $0$-handle), then — by accident of how it was drawn — one of the two bars sitting *below* the other, even though both are the same kind of handle.

The two bars are both index-$1$ handles. Their attaching feet land on different parts of the rim, and the downhill streams pouring off them run to separate places. Because those streams never meet, we may raise the lower bar and lower the higher one until they sit at the *same* floor. Now both index-$1$ handles live together on floor one.

Next picture the cap that closes the doughnut off — an index-$2$ handle. It attaches above everything else, and nothing of lower complexity sits above it. So it rises to floor two.

The final stack reads, bottom to top: floor $0$ holds the valley, floor $1$ holds both bars, floor $2$ holds the cap. Heights $0, 1, 2$ match the complexity numbers $0, 1, 2$. **What this tells us:** the messy original order has been combed into a clean ladder where each floor carries exactly one complexity of handle, and the floor number is the complexity itself.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice">
<p class="exercise__label"><strong>Exercise (easy, multiple-choice).</strong></p>
<p>Two level spots of a height function can be slid past each other (their heights swapped) exactly when:</p>
<ol class="exercise__options" type="A">
<li>They have the same complexity number.</li>
<li>The downhill paths streaming out of one never meet the uphill paths feeding the other.</li>
<li>They are at the same height to begin with.</li>
<li>One of them is a valley.</li>
</ol>
<details class="exercise__hint"><summary>Hint</summary>
Recall the two climbers whose paths down the mountain must not cross.
</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>B.</strong> Reordering is allowed when the paths emanating from the two spots miss each other, so that raising one and lowering the other never forces a collision. Same complexity is neither needed nor enough.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric">
<p class="exercise__label"><strong>Exercise (easy, numeric).</strong></p>
<p>In a self-indexing arrangement, at what height does a level spot of complexity number $2$ sit?</p>
<details class="exercise__hint"><summary>Hint</summary>
Self-indexing means the height equals the complexity number.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>$2$.</strong> The defining property of a self-indexing arrangement is that the height of each spot equals its complexity number, so an index-$2$ spot sits at height $2$.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="medium" data-type="true-false">
<p class="exercise__label"><strong>Exercise (medium, true-false).</strong></p>
<p>True or false: in a self-indexing arrangement, every handle of a given complexity is attached before any handle of higher complexity.</p>
<details class="exercise__hint"><summary>Hint</summary>
Lower complexity means lower floor, and lower floors are built first.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>True.</strong> Lower complexity sits on a lower floor, and the shape is assembled floor by floor from the bottom up, so all the simpler handles are in place before the more complicated ones are attached. This is precisely the orderly ladder the sorting produces.</p>
</details>
</aside>

## Formal definition [Intermediate+]

Throughout, $(W; V_0, V_1)$ is a compact cobordism of dimension $n$ with a Morse function $f : W \to [0,1]$ adapted to the boundary, $f^{-1}(0) = V_0$, $f^{-1}(1) = V_1$, all interior critical points nondegenerate, as in [48.02.20]. Fix also a **gradient-like vector field** $\xi$ for $f$: $\xi(f) > 0$ away from the critical points, and near each critical point $p$ of index $\lambda$ there are Morse coordinates $(u,v) \in \mathbb{R}^\lambda \times \mathbb{R}^{n-\lambda}$ with $f = f(p) - |u|^2 + |v|^2$ and $\xi = (-u, v)$.

**Definition (ascending and descending manifolds).** For a critical point $p$, the **descending (left-hand) manifold** is the set of points whose forward $\xi$-trajectory limits onto $p$ as $t \to -\infty$ flows them up out of $p$; concretely
$$
W^u(p) = \{x \in W : \lim_{t \to -\infty} \phi_t(x) = p\},
$$
where $\phi_t$ is the flow of $\xi$. It is an embedded open disc of dimension $\lambda$. The **ascending (right-hand) manifold** $W^s(p)$, with $t \to +\infty$, is an embedded open disc of dimension $n - \lambda$. Their intersections with a regular level set $f^{-1}(c)$, for $c$ just below resp. just above $f(p)$, are the **descending sphere** $S^{\lambda-1}$ and **ascending sphere** $S^{n-\lambda-1}$ of [48.02.20].

**Definition (rearrangeable pair).** Two critical points $p, q$ at distinct critical levels, with $f(p) < f(q)$ say, are **independent** for $\xi$ if no $\xi$-trajectory runs from $q$ down to $p$: equivalently, the descending manifold $W^u(q)$ is disjoint from the ascending manifold $W^s(p)$. A finite set of critical points lying in a common interval of regular values is **rearrangeable** if it is pairwise independent.

**Definition (self-indexing Morse function).** A Morse function $f : W \to [0, n]$ adapted to the boundary is **self-indexing** (Smale's term: a **nice function**) if
$$
f(p) = \operatorname{ind}(p)
$$
for every critical point $p$. Equivalently, every index-$\lambda$ critical point lies on the level $\lambda$, and the half-integer levels $\lambda + \tfrac12$ are regular and separate the index-$\lambda$ handles below from the index-$(\lambda+1)$ handles above.

**Definition (handlebody filtration).** Given a self-indexing $f$, set $W_\lambda = f^{-1}[0, \lambda + \tfrac12]$. Then $V_0 \subset W_0 \subset W_1 \subset \cdots \subset W_n = W$, and $W_\lambda$ is obtained from $W_{\lambda-1}$ by attaching all the $\lambda$-handles simultaneously along disjoint attaching tubes in $\partial_+ W_{\lambda-1}$. This is the **handle decomposition sorted by index**.

### Counterexamples to common slips

- Independence is a property of the *pair together with $\xi$*, not of the points alone. The same two critical points may be joined by a trajectory for one gradient-like field and independent for another; rearrangement often proceeds by first perturbing $\xi$ to remove an unwanted connecting trajectory.
- Self-indexing does **not** require $f(p) < f(q)$ whenever $\operatorname{ind}(p) < \operatorname{ind}(q)$ *strictly handle by handle within a level* — all critical points of one index share a single level, so equal indices give equal values. The ordering is by index class, not a total order on points.
- A trajectory from $q$ to $p$ forces $\operatorname{ind}(q) > \operatorname{ind}(p)$ generically (the dimension count $\dim W^u(q) + \dim W^s(p) > n$ is what permits an intersection), but in the non-generic case a trajectory can connect critical points of the *same* index; such a connection is an obstruction to independence that a small perturbation of $\xi$ removes.

## Key theorem with proof [Intermediate+]

The structural result is Milnor's Rearrangement Theorem [ref: TODO_REF Milnor §4], LHC Theorem 4.1, with the self-indexing corollary as its repeated application.

**Theorem (Rearrangement).** *Let $f : W \to [0,1]$ be a Morse function with gradient-like field $\xi$, and let $p_1, \dots, p_k$ be the critical points on a single critical level $c$, together with possibly some critical points above and below. Suppose the critical points can be partitioned into two sets $A, B$ such that no $\xi$-trajectory runs from a point of $B$ to a point of $A$. Then for any $a < b$ in $(0,1)$ avoiding the boundary critical values there is a new Morse function $f'$, with the same critical points and the same gradient-like field $\xi$, such that $f' = f$ outside a compact set, $f'(p) < a$ for all $p \in A$, and $f'(p) > b$ for all $p \in B$.*

In words: independent critical points can be pushed to different ranges of values, $A$ below $B$, without changing the manifold, the critical points, or the gradient-like field.

*Proof.* The hypothesis says $W^u(B) \cap W^s(A) = \varnothing$, where $W^u(B) = \bigcup_{q\in B} W^u(q)$ and likewise for $A$. Consider the set $K_A = W^s(A) \cup W^u(A)$ of points whose trajectory limits onto $A$ in forward or backward time, together with $A$ itself, and similarly $K_B$. The closure of the union of all trajectories that *pass through* a neighbourhood of $A$ — that is, the set of $x$ whose forward limit lies in $A$ or whose backward limit lies in $A$ — is a compact set saturated by the flow. The disjointness $W^u(B)\cap W^s(A)=\varnothing$, plus the fact that every non-critical trajectory limits onto critical points or the boundary at both ends, lets one build a smooth function $\pi : W \to [0,1]$ that is constant along trajectories ($\xi(\pi) = 0$), equal to $0$ on the trajectories through $A$ and to $1$ on the trajectories through $B$. (Construct $\pi$ from the flow-out of disjoint closed neighbourhoods of $K_A$ and $K_B$ and extend by a $\xi$-invariant partition of unity; invariance is what makes $\pi$ depend only on which trajectory a point lies on.)

Now choose a smooth $G : [0,1] \times [0,1] \to [0,1]$, $G = G(s, t)$, with $\partial G / \partial t > 0$ for all $s$ (so $t \mapsto G(s,t)$ is an increasing reparametrisation of the value), such that $G(0, c) < a$ and $G(1, c) > b$, and $G(s, \cdot) = \mathrm{id}$ near $t = 0, 1$. Define
$$
f'(x) = G\big(\pi(x),\, f(x)\big).
$$
Then $f'$ agrees with $f$ near the boundary, and $\xi(f') = G_s \cdot \xi(\pi) + G_t \cdot \xi(f) = G_t\,\xi(f)$, which has the *same sign* as $\xi(f)$ because $G_t > 0$. Hence $f'$ has exactly the same critical points as $f$, with the same indices (the Hessian is scaled by the positive factor $G_t$), and $\xi$ remains gradient-like for $f'$. At a critical point $p \in A$ the trajectory through $p$ has $\pi = 0$, so $f'(p) = G(0, f(p)) = G(0, c) < a$; at $p \in B$, $\pi = 1$ and $f'(p) = G(1, c) > b$. $\square$

**Bridge.** This is exactly the freedom to slide independent level spots past one another, made rigorous: the trajectory-constant function $\pi$ separates the two families, and reparametrising the value along $\pi$ is the foundational reason the heights can be shifted without disturbing the gradient flow. Iterating it across all indices builds toward the self-indexing function, where every index-$\lambda$ handle is sorted onto the level $\lambda$; this is exactly the smooth filtration $W_0 \subset \cdots \subset W_n$, and the sorting generalises the cell-by-dimension filtration of a CW complex from [03.12.13] to the smooth handle category of [48.02.20]. The central insight is that the descending sphere of a lower-index handle and the ascending sphere of a higher-index handle live in complementary dimensions, so they can be made disjoint, and disjointness is precisely what licenses the swap. Putting these together, rearrangement appears again in [48.02.20]'s programme as the combinatorial spine on which handle cancellation and the $h$-cobordism theorem are built. The bridge is that the analytic move (reparametrise $f$ along $\pi$) and the geometric move (slide the handles) are one and the same operation.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric">
<p class="exercise__label"><strong>Exercise (easy, numeric).</strong></p>
<p>A self-indexing Morse function on a $5$-dimensional cobordism has critical points of indices $0, 1, 1, 2, 3, 5$. On how many distinct critical levels do these sit?</p>
<details class="exercise__hint"><summary>Hint</summary>
All critical points of one index share a single level; count distinct indices.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>$5$.</strong> The distinct indices are $0, 1, 2, 3, 5$, so the critical values are $0, 1, 2, 3, 5$ — five levels (the two index-$1$ points share the level $1$). There is no critical point of index $4$, so the level $4$ is regular.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice">
<p class="exercise__label"><strong>Exercise (easy, multiple-choice).</strong></p>
<p>For a self-indexing $f$ the half-level $f^{-1}(\lambda + \tfrac12)$ is regular and separates which handles?</p>
<ol class="exercise__options" type="A">
<li>Index-$\lambda$ handles below from index-$(\lambda+1)$ handles above.</li>
<li>All handles below from all handles above equally.</li>
<li>Even-index from odd-index handles.</li>
<li>It is never regular.</li>
</ol>
<details class="exercise__hint"><summary>Hint</summary>
The level $\lambda + \tfrac12$ lies between the level $\lambda$ and the level $\lambda + 1$.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>A.</strong> Since index-$\mu$ critical points sit at level $\mu$, the value $\lambda + \tfrac12$ has no critical points; it lies above all index-$\le\lambda$ handles and below all index-$\ge\lambda+1$ handles, so $W_\lambda = f^{-1}[0,\lambda+\tfrac12]$ contains exactly the handles of index $\le \lambda$.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer">
<p class="exercise__label"><strong>Exercise (medium, short-answer).</strong></p>
<p>Two critical points $p$ (index $1$) and $q$ (index $1$) lie on the same level of a Morse function on a cobordism of dimension $n = 4$. Using a dimension count, explain why a generic gradient-like field has no trajectory from $q$ to $p$, so the pair is independent.</p>
<details class="exercise__hint"><summary>Hint</summary>
A trajectory from $q$ to $p$ is a point of $W^u(q) \cap W^s(p)$; compare $\dim W^u(q) + \dim W^s(p)$ with $n$.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>Argument.</strong> $\dim W^u(q) = \operatorname{ind}(q) = 1$ and $\dim W^s(p) = n - \operatorname{ind}(p) = 4 - 1 = 3$. A connecting trajectory lies in $W^u(q) \cap W^s(p)$; since the flow is $1$-parameter, a transverse intersection of these manifolds on a fixed regular level (dimensions $0$ and $2$ inside the $3$-manifold level set) is generically empty: $0 + 2 = 2 < 3$. So for a generic $\xi$ no trajectory connects $q$ to $p$, and the pair is independent and rearrangeable.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic">
<p class="exercise__label"><strong>Exercise (medium, symbolic).</strong></p>
<p>In the rearrangement proof, $f'(x) = G(\pi(x), f(x))$ with $\xi(\pi) = 0$ and $G_t > 0$. Write $\xi(f')$ in terms of $\xi(f)$ and confirm the critical points of $f'$ coincide with those of $f$.</p>
<details class="exercise__hint"><summary>Hint</summary>
Apply the chain rule to $\xi(f') = dG(\pi, f)\cdot \xi$.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>$\xi(f') = G_t(\pi, f)\,\xi(f)$.</strong> By the chain rule $\xi(f') = G_s\,\xi(\pi) + G_t\,\xi(f) = G_t\,\xi(f)$ since $\xi(\pi) = 0$. As $G_t > 0$, the sign of $\xi(f')$ equals that of $\xi(f)$, so $\xi(f')$ vanishes precisely where $\xi(f)$ does, i.e. at the same critical points. Near each, the Hessian of $f'$ is $G_t$ times that of $f$ plus terms in $df$, which vanish at the critical point, so indices are preserved.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric">
<p class="exercise__label"><strong>Exercise (medium, numeric).</strong></p>
<p>A self-indexing Morse function on a closed $3$-manifold $M$ (treat $M$ as a cobordism from $\varnothing$ to $\varnothing$) has $c_0 = 1$, $c_1 = 2$, $c_2 = 2$, $c_3 = 1$ critical points by index. Compute $\sum_\lambda (-1)^\lambda c_\lambda$ and state what it equals.</p>
<details class="exercise__hint"><summary>Hint</summary>
The alternating sum of handle counts is the Euler characteristic, which vanishes for a closed odd-dimensional manifold.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>$0 = \chi(M)$.</strong> $1 - 2 + 2 - 1 = 0$. Every closed odd-dimensional manifold has Euler characteristic $0$, consistent with the handle count; the self-indexing filtration realises $M$ as a CW complex with $c_\lambda$ cells in dimension $\lambda$, whose cellular Euler characteristic is this alternating sum.</p>
</details>
</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer">
<p class="exercise__label"><strong>Exercise (hard, short-answer).</strong></p>
<p>Prove that the Rearrangement Theorem implies the existence of a Morse function on $(W; V_0, V_1)$ whose critical values are strictly increasing in the index: all index-$0$ points below all index-$1$ points, etc. (the "ordered" case, weaker than self-indexing).</p>
<details class="exercise__hint"><summary>Hint</summary>
Apply the theorem to $A = \{\text{all critical points of index} \le \lambda\}$, $B = \{\text{index} > \lambda\}$, and use the dimension count to verify independence.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>Argument.</strong> First make $\xi$ Morse-Smale by a small perturbation, so that $W^u(q) \pitchfork W^s(p)$ for all pairs. Then a trajectory from $q$ to $p$ forces $\dim W^u(q) + \dim W^s(p) \ge n + 1$, i.e. $\operatorname{ind}(q) + (n - \operatorname{ind}(p)) \ge n+1$, i.e. $\operatorname{ind}(q) > \operatorname{ind}(p)$. Hence no trajectory runs from a lower-index to a higher-index point: taking $A = \{\operatorname{ind} \le \lambda\}$ and $B = \{\operatorname{ind} > \lambda\}$, there is no trajectory from $B$ to $A$, so the theorem pushes $A$ below $B$. Doing this for $\lambda = 0, 1, \dots, n-1$ in turn (each step preserving the earlier separations, as those involve already-separated value ranges) orders all indices. $\square$</p>
</details>
</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer">
<p class="exercise__label"><strong>Exercise (hard, short-answer).</strong></p>
<p>Starting from an ordered Morse function (index-$\lambda$ points all on a level $a_\lambda$, with $a_0 < a_1 < \cdots < a_n$), explain how to rescale to a self-indexing one with $f(p) = \operatorname{ind}(p)$, and why the rescaling preserves the gradient-like field up to positive reparametrisation.</p>
<details class="exercise__hint"><summary>Hint</summary>
Choose an increasing smooth $h : [0,1] \to [0,n]$ sending each $a_\lambda$ to $\lambda$ and apply it to $f$.</details>
<details class="exercise__answer"><summary>Answer</summary>
<p><strong>Argument.</strong> Pick a strictly increasing smooth $h : [\,f(V_0), f(V_1)\,] \to [0, n]$ with $h(a_\lambda) = \lambda$ for each occupied index $\lambda$ and $h' > 0$ throughout (interpolate smoothly between the prescribed values, possible since the $a_\lambda$ are strictly increasing). Set $\tilde f = h \circ f$. Then $\tilde f(p) = h(a_{\operatorname{ind}(p)}) = \operatorname{ind}(p)$, so $\tilde f$ is self-indexing. Moreover $\xi(\tilde f) = h'(f)\,\xi(f)$ with $h' > 0$, so $\xi$ stays gradient-like for $\tilde f$, the critical points and indices are unchanged, and the half-levels $\lambda + \tfrac12$ are regular. $\square$</p>
</details>
</aside>

## Advanced results [Master]

**The Morse-Smale chain complex.** Fix a self-indexing Morse function $f$ on $(W; V_0, V_1)$ with a gradient-like field $\xi$ chosen **Morse-Smale**: $W^u(p) \pitchfork W^s(q)$ for every pair of critical points (a generic condition on $\xi$, by the transversality theory of [03.15.01]). Let $C_\lambda$ be the free abelian group on the index-$\lambda$ critical points, oriented by a choice of orientation of each descending disc $W^u(p)$. For adjacent indices, $\dim W^u(p) + \dim W^s(q) = \lambda + (n - (\lambda-1)) = n + 1$, so on a regular level between the levels $\lambda$ and $\lambda - 1$ the descending sphere $S^{\lambda-1}_p$ of $p$ and the ascending sphere $S^{n-\lambda}_q$ of $q$ meet in a finite set of points, each carrying a sign from the orientations. Their signed count is the **intersection number** $[p : q] \in \mathbb{Z}$, and
$$
\partial p = \sum_{\operatorname{ind}(q) = \lambda - 1} [p : q]\, q
$$
defines $\partial : C_\lambda \to C_{\lambda-1}$. The geometric heart is that $\partial^2 = 0$: a broken trajectory from an index-$\lambda$ point to an index-$(\lambda-2)$ point through an intermediate point is one end of a $1$-parameter family of trajectories whose other end is a second broken trajectory, so the boundary terms cancel in pairs. The homology of $(C_*, \partial)$ is $H_*(W, V_0; \mathbb{Z})$.

**Why self-indexing is the right frame.** The intersection-number boundary map is well-posed precisely because, in a self-indexing function, the ascending sphere of an index-$(\lambda-1)$ point and the descending sphere of an index-$\lambda$ point sit on a common regular level $\lambda - \tfrac12$, where their dimensions $n - \lambda$ and $\lambda - 1$ add to $n - 1 = \dim(\text{level})$, the complementary-dimension condition for a transverse $0$-dimensional intersection. Without sorting by index, descending and ascending spheres of the wrong index pairs would clutter the intermediate levels, and the count would not be a chain map. This is the sense in which rearrangement is the *combinatorial spine* of the handle calculus: it produces exactly the filtration $W_\lambda = f^{-1}[0, \lambda + \tfrac12]$ for which $C_\lambda = H_\lambda(W_\lambda, W_{\lambda-1})$ is free on the $\lambda$-handles, recovering the cellular chain complex of [03.12.13] in its smooth, handle-theoretic incarnation.

**Existence and the polar refinement.** Smale's existence theorem [ref: TODO_REF Smale 1962] states that every cobordism admits a self-indexing Morse function with a Morse-Smale gradient-like field. On a *closed* connected manifold one can further demand exactly one minimum and one maximum — Marston Morse's polar (or "$1$-$1$") functions [ref: TODO_REF Morse 1960] — by cancelling surplus index-$0$ and index-$n$ critical points against index-$1$ and index-$(n-1)$ ones; this trims $C_0$ and $C_n$ to rank one, the normalisation used when the chain complex is run against simple connectivity in the $h$-cobordism argument.

**Synthesis.** Rearrangement is the central insight that converts the unordered handle decomposition of [48.02.20] into a computable algebraic object. It is the foundational reason a smooth cobordism carries a finite free chain complex: sorting the handles by index produces the filtration $W_0 \subset \cdots \subset W_n$, and this is exactly the smooth realisation of the CW filtration of [03.12.13], so the Morse-Smale complex $C_*$ is dual to — in fact identified with — the cellular chain complex computing $H_*(W, V_0)$. Putting these together, the self-indexing function and its Morse-Smale field generalise the height-function-on-a-torus picture of [48.02.30] into the full machine that builds toward handle cancellation and the $h$-cobordism theorem: the intersection numbers $[p:q]$ are the entries of the boundary matrix, an acyclic such matrix signals geometrically cancelling handles, and the bridge from algebra back to geometry — Whitney's trick turning an algebraic $\pm 1$ into a single geometric intersection point — is the content of the next unit in the programme.

## Full proof set [Master]

**Proposition (independence is achievable by perturbing $\xi$, generic case).** *Let $p, q$ be critical points with $\operatorname{ind}(p) \ge \operatorname{ind}(q)$ lying in a common interval of regular values, and suppose $\xi$ is Morse-Smale. Then there is no $\xi$-trajectory from $p$ to $q$ unless $\operatorname{ind}(p) > \operatorname{ind}(q)$; in particular two critical points of equal index in a common regular interval are independent.*

*Proof.* A $\xi$-trajectory from $p$ to $q$ is a point of $W^u(p) \cap W^s(q)$ other than the rest points. Under the Morse-Smale hypothesis this intersection is transverse, so where non-empty it is a manifold of dimension $\dim W^u(p) + \dim W^s(q) - n = \operatorname{ind}(p) + (n - \operatorname{ind}(q)) - n = \operatorname{ind}(p) - \operatorname{ind}(q)$. The flow $\xi$ acts freely on this intersection (away from rest points) with $1$-dimensional orbits, so a non-empty trajectory set has dimension $\ge 1$, forcing $\operatorname{ind}(p) - \operatorname{ind}(q) \ge 1$. Hence a connecting trajectory requires $\operatorname{ind}(p) > \operatorname{ind}(q)$. When $\operatorname{ind}(p) = \operatorname{ind}(q)$ the intersection has formal dimension $0$ yet must be flow-invariant and so contain no non-rest points: it is empty, and $p, q$ are independent. $\square$

**Proposition (the half-level slices are handlebodies).** *For a self-indexing $f$ with gradient-like $\xi$, the sublevel $W_\lambda = f^{-1}[0, \lambda + \tfrac12]$ is obtained from $W_{\lambda-1}$ by attaching one $\lambda$-handle per index-$\lambda$ critical point, along disjoint embeddings of the attaching tubes into $\partial_+ W_{\lambda-1}$.*

*Proof.* The slab $f^{-1}[\lambda - \tfrac12, \lambda + \tfrac12]$ contains exactly the index-$\lambda$ critical points, all on the single level $\lambda$. By the elementary-cobordism theorem of [48.02.20] each contributes a $\lambda$-handle attached to $\partial_+ W_{\lambda-1} = f^{-1}(\lambda - \tfrac12)$ along the descending sphere of $\xi$. The attaching tubes are disjoint: the attaching tube of the handle at $p$ is a neighbourhood of the descending sphere $S^{\lambda-1}_p \subset f^{-1}(\lambda - \tfrac12)$, and for two index-$\lambda$ points $p \ne p'$ the descending manifolds $W^u(p), W^u(p')$ are disjoint embedded discs whose level-set slices are disjoint spheres (distinct trajectories never coincide). Shrinking the tubular neighbourhoods makes the tubes disjoint, so all the $\lambda$-handles attach simultaneously. The result is $W_\lambda$ after straightening corners. $\square$

**Proposition ($\partial \circ \partial = 0$ for the Morse-Smale complex).** *With the intersection-number boundary $\partial p = \sum_q [p:q]\,q$ on a Morse-Smale self-indexing complex, $\partial^2 = 0$.*

*Proof sketch (the standard compactness-of-the-moduli argument).* Fix $p$ of index $\lambda$ and $r$ of index $\lambda - 2$. The coefficient of $r$ in $\partial^2 p$ is $\sum_{q} [p:q][q:r]$, summed over index-$(\lambda-1)$ points $q$. Consider the moduli space $\mathcal{M}(p, r)$ of unparametrised $\xi$-trajectories from $p$ to $r$; by transversality it is a $1$-manifold (dimension $\operatorname{ind}(p) - \operatorname{ind}(r) - 1 = 1$). Its ends, by the broken-trajectory compactification, are in bijection with pairs of trajectories $p \to q \to r$ through intermediate index-$(\lambda-1)$ points — exactly the terms $[p:q][q:r]$ with sign. A compact $1$-manifold has an even number of boundary points counted with sign, so $\sum_q [p:q][q:r] = 0$. The compactness and gluing that justify this are the content of [03.15.01]; here they are imported. Hence $\partial^2 = 0$. $\square$

## Connections [Master]

The rearrangement and self-indexing theory of this unit is the sorting step that organises the unordered handle decomposition of [48.02.20] into the filtration $W_0 \subset W_1 \subset \cdots \subset W_n$; the elementary-cobordism dictionary established there — one critical point is one handle is one surgery, with the descending sphere carrying the attaching datum — is exactly the input the Rearrangement Theorem permutes, and the gradient-like vector field defined there is the object whose ascending and descending manifolds make independence a checkable condition.

The Morse functions, the index, and the Morse lemma of [48.02.30] supply the local normal form $f = c - |u|^2 + |v|^2$ that defines the gradient-like field near each critical point and hence the ascending and descending spheres whose disjointness drives the swap; the chart-independence of the index proved there is what makes "sort by index" a well-defined instruction, and the worked torus example there is the seed that this unit grows into a full handlebody ladder.

The Morse-Smale chain complex built here from a self-indexing function is the smooth, handle-theoretic realisation of the cellular chain complex of [03.12.13]: the filtration by half-levels is a CW filtration, $C_\lambda$ is free on the index-$\lambda$ handles, and the intersection-number boundary map agrees with the cellular boundary, so both compute $H_*(W, V_0)$; the analytic underpinning of the boundary map — transversality of ascending and descending manifolds and compactness of the trajectory moduli — is developed in [03.15.01], whose gradient-flow Morse homology is this complex read on a closed manifold.

## Historical & philosophical context [Master]

The idea of ordering critical points by index has roots in Marston Morse's own work: his *The existence of polar non-degenerate functions on differentiable manifolds* (Annals of Mathematics 71, 1960, 352–383) [ref: TODO_REF Morse 1960] constructed functions with a single maximum and single minimum, an early instance of normalising a Morse function's critical-value structure to a prescribed pattern. The decisive step — that handles can be freely reordered by index and that a *self-indexing* function exists on any cobordism — is due to Stephen Smale, who introduced "nice functions" with $f(p) = \operatorname{ind}(p)$ in *On the structure of manifolds* (American Journal of Mathematics 84, 1962, 387–399) [ref: TODO_REF Smale 1962] and used the resulting handlebody filtration as the organising device of his proof of the high-dimensional Poincaré conjecture in *Generalized Poincaré's conjecture in dimensions greater than four* (Annals of Mathematics 74, 1961, 391–406) [ref: TODO_REF Smale 1961].

John Milnor's *Lectures on the h-Cobordism Theorem* (Princeton Mathematical Notes, 1965), with notes by Larry Siebenmann and John Sondow, gave the rearrangement argument its canonical form as the First Cancellation/Rearrangement theory of §4, organised around the gradient-like vector field and its trajectory-constant separating function. Philosophically, the move is a turning point in twentieth-century topology: it converts a purely analytic object, a smooth function and its gradient flow, into a *combinatorial* one, an ordered list of handles with intersection numbers, and thereby makes the smooth topology of high-dimensional manifolds amenable to linear algebra over $\mathbb{Z}[\pi_1]$. The reduction of a geometric classification problem to the algebra of a chain complex — later abstracted into surgery theory and the algebraic $K$- and $L$-theory of Wall and others — begins with the humble observation that level spots whose downhill paths miss can be slid past one another.

## Bibliography [Master]

```bibtex
@book{milnor1965hcobordism,
  author    = {Milnor, John W.},
  title     = {Lectures on the h-Cobordism Theorem},
  series    = {Princeton Mathematical Notes},
  publisher = {Princeton University Press},
  year      = {1965},
  note      = {Notes by L. Siebenmann and J. Sondow}
}

@article{smale1962structure,
  author  = {Smale, Stephen},
  title   = {On the structure of manifolds},
  journal = {American Journal of Mathematics},
  volume  = {84},
  number  = {3},
  pages   = {387--399},
  year    = {1962}
}

@article{smale1961poincare,
  author  = {Smale, Stephen},
  title   = {Generalized {P}oincar{\'e}'s conjecture in dimensions greater than four},
  journal = {Annals of Mathematics},
  volume  = {74},
  number  = {2},
  pages   = {391--406},
  year    = {1961}
}

@article{morse1960polar,
  author  = {Morse, Marston},
  title   = {The existence of polar non-degenerate functions on differentiable manifolds},
  journal = {Annals of Mathematics},
  volume  = {71},
  number  = {2},
  pages   = {352--383},
  year    = {1960}
}

@book{kosinski1993,
  author    = {Kosinski, Antoni A.},
  title     = {Differential Manifolds},
  series    = {Pure and Applied Mathematics},
  volume    = {138},
  publisher = {Academic Press},
  year      = {1993}
}

@book{matsumoto2002morse,
  author    = {Matsumoto, Yukio},
  title     = {An Introduction to Morse Theory},
  series    = {Translations of Mathematical Monographs},
  volume    = {208},
  publisher = {American Mathematical Society},
  year      = {2002}
}
```
