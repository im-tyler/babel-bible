---
id: 47.01.06
title: "Pumping Lemma for Context-Free Languages and CYK Parsing"
slug: pumping-lemma-cfl-cyk-parsing
section: theoretical-cs
chapter: formal-languages-automata
concept_catalog_id: theoretical-cs.pumping-lemma-cfl-cyk-parsing
prerequisites:
  - 47.01.05
successors: []
tier_anchors:
  beginner: "Sipser 2013 *Introduction to the Theory of Computation* 3e (Cengage) §2.3 (the pumping lemma for context-free languages: long enough strings in a CFL have two pumpable regions)"
  intermediate: "Sipser 2013 *Introduction to the Theory of Computation* 3e (Cengage) §2.3 and §2.4 (formal CFL pumping lemma via parse-tree structure, CYK algorithm for membership in $O(n^3)$ time)"
  master: "Sipser 2013 *Introduction to the Theory of Computation* 3e (Cengage) §2.3-2.4; Hopcroft, Motwani & Ullman 2006 *Introduction to Automata Theory, Languages, and Computation* 3e (Pearson) §7.4 (CYK with Chomsky normal form conversion, Ogden's lemma as a strengthening)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: true
references:
  - source: TODO_REF
    path: "Sipser, M. — Introduction to the Theory of Computation (3rd ed.)"
    locator: 'Cengage (2013). Section 2.3 states the pumping lemma for context-free languages: if $L$ is context-free, then there exists $p$ such that every $s \in L$ with $|s| \ge p$ can be divided as $s = uvxyz$ with (1) for each $i \ge 0$, $uv^ixy^iz \in L$, (2) $|vy| > 0$, and (3) $|vxy| \le p$; the proof uses the fact that in a parse tree of height $h$ for a grammar in Chomsky normal form, the yield has length at most $2^{h-1}$, so a sufficiently long string forces a repeated nonterminal on some path, creating two pumpable regions $v$ and $y$. Section 2.4 presents the CYK algorithm for deciding membership in a CFL in $O(n^3)$ time using dynamic programming on substrings of the input.'
  - source: TODO_REF
    path: "Hopcroft, J. E., Motwani, R. & Ullman, J. D. — Introduction to Automata Theory, Languages, and Computation (3rd ed.)"
    locator: 'Pearson (2006). Section 7.4 develops the CYK algorithm in detail: convert the grammar to Chomsky normal form (CNF) where every production is either $A \to BC$ or $A \to a$, then fill a triangular table where entry $T[i,j]$ is the set of nonterminals deriving the substring $a_i \cdots a_j$; the algorithm runs in $O(n^3 \cdot |G|)$ time where $|G|$ is the grammar size. Ogden''s lemma is presented as a strengthening that marks certain positions as distinguished, forcing the pumpable regions to include distinguished positions.'
lean_status: none
lean_mathlib_gap: |
  Mathlib has `Computability.ContextFreeGrammar` with basic definitions of context-free grammars and derivations, but it does not formalise the pumping lemma for context-free languages, the CYK algorithm, or Chomsky normal form conversion. The foundational gap is the absence of a `ContextFree.pumping_lemma` theorem and a verified CYK membership test; this unit ships without a lean_module.
human_reviewer: "Tyler (pending external automata-theory / theoretical-cs reviewer)"
source_books: [sipser-2013]
source_curriculum: theoretical-cs
estimated_time:
  beginner: 15m
  intermediate: 40m
  master: 75m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
hooks_out:
  - target: 47.02.01
    kind: proposed
    why: >
      The CFL pumping lemma demonstrates that the Chomsky hierarchy has strict
      levels: regular languages are properly contained in context-free
      languages, which are themselves properly contained in decidable
      languages. This hierarchy motivates the complexity-class landscape of
      P, NP, and beyond.
  - target: 47.02.02
    kind: proposed
    why: >
      The CYK algorithm runs in polynomial time ($O(n^3)$) for CFL membership,
      but parsing arbitrary grammars is PSPACE-hard; the boundary between
      polynomial and harder parsing problems connects to the Cook-Levin
      SAT-to-CFL reduction.
---

## Intuition [Beginner]

Think of a family tree. Each person has two parents. If you go back enough generations, with enough people in each generation, you will eventually find that two people on the same ancestral path share the same last name. That repeated name creates a loop in the tree.

A parse tree for a context-free grammar works the same way. The grammar has rules like "a sentence is a noun phrase plus a verb phrase." Each rule expands one symbol into two or more children. For a sufficiently long sentence, the tree must be tall. And since there are only finitely many grammar symbols, some symbol must appear twice on the same path from root to leaf.

That repeated symbol is the key. The subtree between the two appearances can be copied or deleted. Because the grammar is recursive (the same symbol produces itself), you can repeat the middle part as many times as you want, or remove it entirely, and the result is still a valid sentence in the language.

This is the context-free pumping lemma. Unlike the regular version (which has one pumpable region), the context-free version has two. The two regions come from the two subtrees of the repeated nonterminal. And unlike the regular version, the pumpable regions are bounded in total length: they must fit within a window of $p$ symbols.

The CYK algorithm solves the other direction: given a string, is it in the language? It works bottom-up, building a table of which substrings each nonterminal can produce. For every pair of adjacent substrings that $A$ can produce from $B$ and $C$ (where $A \to BC$ is a rule), mark $A$ as producing the combined span. After filling the table, the start symbol produces the whole string if and only if the string is in the language.

## Visual [Beginner]

A parse tree with a repeated nonterminal on a path:

```
         S
        / \
       A    ...
      / \
     A    y        ← A repeats on the same path
    / \
   u    v          ← v and y are the two pumpable parts
        |
        x
```

Pumping both $v$ and $y$ simultaneously:

| Version | String | In language? |
|---|---|---|
| $uv^0xy^0z = uxz$ | Remove both $v$ and $y$ | Yes (by pumping lemma) |
| $uv^1xy^1z = uvxyz$ | Original string | Yes |
| $uv^2xy^2z = uvvxyyz$ | Repeat both $v$ and $y$ once | Yes |
| $uv^3xy^3z = uvvvxyyyz$ | Repeat both three times | Yes |

The CYK table for deciding membership:

```
Input:  b  a  a  b  a

Span 1: [b] [a] [a] [b] [a]
Span 2: [ba] [aa] [ab] [ba]
Span 3: [baa] [aab] [aba]
Span 4: [baab] [aaba]
Span 5: [baaba]  ← Does S appear here?
```

Each cell stores the set of nonterminals that generate that substring.

## Worked example [Beginner]

We prove that $L = \{a^nb^nc^n : n \ge 0\}$ is not context-free.

Suppose $L$ is context-free with pumping length $p$. Choose $s = a^pb^pc^p$. By the pumping lemma, $s = uvxyz$ where $|vy| > 0$, $|vxy| \le p$, and $uv^ixy^iz \in L$ for all $i \ge 0$.

Since $|vxy| \le p$, the window $vxy$ cannot span all three letter types ($a$, $b$, and $c$) at once. So $v$ and $y$ together contain at most two types of letters.

Case 1: $vxy$ lies entirely in the $a$s and $b$s. Pumping up ($i = 2$) adds more $a$s and/or $b$s without adding $c$s. The pumped string has more $a$s and/or $b$s than $c$s, so it is not in $L$.

Case 2: $vxy$ lies entirely in the $b$s and $c$s. Pumping up adds more $b$s and/or $c$s without adding $a$s. The pumped string has more $b$s and/or $c$s than $a$s, so it is not in $L$.

In every case, pumping produces a string outside $L$. Contradiction. So $\{a^nb^nc^n\}$ is not context-free.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: every regular language is also context-free.

<details class="exercise__answer"><summary>Answer</summary>

**True.** Every regular language is recognised by a DFA, and any DFA can be simulated by a right-linear grammar (a restricted context-free grammar where every production has at most one nonterminal, always at the right end). So the regular languages form a proper subset of the context-free languages.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple-choice).**

In the CFL pumping lemma, why are there two pumpable regions ($v$ and $y$) instead of one?

<ol class="exercise__options" type="A">
<li>Because the grammar has two start symbols.</li>
<li>Because the repeated nonterminal on the parse tree path has two children.</li>
<li>Because the input alphabet has two symbols.</li>
<li>Because the pumping length is always even for CFLs.</li>
</ol>

<details class="exercise__answer"><summary>Answer</summary>

**B.** In Chomsky normal form, every nonterminal production has exactly two children ($A \to BC$). When a nonterminal $A$ repeats on a path, the upper occurrence generates a subtree containing $v$ and the lower generates a subtree containing $y$ (or vice versa), giving two pumpable regions corresponding to the two branches.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: the CYK algorithm works directly on any context-free grammar without preprocessing.

<details class="exercise__answer"><summary>Answer</summary>

**False.** The CYK algorithm requires the grammar to be in Chomsky normal form (CNF), where every production is either $A \to BC$ or $A \to a$. Any context-free grammar can be converted to CNF, but this preprocessing step is necessary before running CYK.

</details>

</aside>

## Formal definition [Intermediate+]

**Lemma (Pumping lemma for context-free languages).** Let $L$ be a context-free language. Then there exists a constant $p \ge 1$ such that every string $s \in L$ with $|s| \ge p$ can be written as $s = uvxyz$ where:

1. For each $i \ge 0$, $uv^ixy^iz \in L$.
2. $|vy| > 0$ (at least one of $v, y$ is nonempty).
3. $|vxy| \le p$ (the two pumpable regions fit within a window of length $p$).

**Proof.** Let $G = (V, \Sigma, R, S)$ be a CFG in Chomsky normal form generating $L \setminus \{\varepsilon\}$ with $|V| = k$ nonterminals. Set $p = 2^{k}$. Let $s \in L$ with $|s| \ge p = 2^{k}$. Consider the parse tree for $s$. In CNF, every internal node has either 2 children (a nonterminal production $A \to BC$) or 1 child (a terminal production $A \to a$). The yield of a subtree of height $h$ has length at most $2^{h-1}$ (binary branching doubles at each level).

Since $|s| \ge 2^{k}$, the parse tree has height at least $k + 1$. The longest path from root to leaf has at least $k + 1$ internal nodes. By pigeonhole, some nonterminal $A$ appears at least twice on this path. Consider the upper and lower occurrences of $A$.

The upper $A$ generates a subtree whose yield contains $vxy$. The lower $A$ generates a subtree whose yield contains $v$ and $y$ separated by $x$. Since the path from the upper $A$ to the lower $A$ has at most $k + 1$ nodes, the yield of the subtree rooted at the upper $A$ has length at most $2^{k} = p$, giving $|vxy| \le p$. The yield of the lower $A$ subtree has $|vy| > 0$ (otherwise $A \Rightarrow^* A$, which is impossible in a proper CNF grammar without unit productions).

For any $i \ge 0$, replacing the upper $A$ subtree by its $i$-fold repetition (pump $i$ copies of the $A \to \cdots A \cdots$ derivation) produces a valid parse tree with yield $uv^ixy^iz$. $\square$

**Algorithm (CYK).** Given a CFG $G = (V, \Sigma, R, S)$ in Chomsky normal form and input string $w = a_1 a_2 \cdots a_n$:

1. For $1 \le i \le n$: set $T[i,i] = \{A \in V : A \to a_i \in R\}$.
2. For length $\ell = 2, 3, \ldots, n$: for $1 \le i \le n - \ell + 1$: set $j = i + \ell - 1$, then $T[i,j] = \{A : A \to BC \in R, \exists k \text{ with } i \le k < j, B \in T[i,k], C \in T[k+1,j]\}$.
3. Accept iff $S \in T[1,n]$.

The running time is $O(n^3 \cdot |R|)$: there are $O(n^2)$ table entries, and each entry examines $O(n)$ split points and $O(|R|)$ rules.

### Counterexamples to common slips

- *"The CFL pumping lemma can prove a language is context-free."* No. It is a necessary condition, not sufficient. There exist non-context-free languages that satisfy the pumping lemma conditions.

- *"Both $v$ and $y$ must be nonempty."* Only their combined length $|vy| > 0$ is required. One of them can be empty.

- *"The CYK algorithm produces a parse tree."* The basic CYK algorithm only decides membership. It can be extended to recover a parse tree by storing back-pointers, but this is an additional step.

### Key theorem with proof [Intermediate+]

**Theorem.** *The following languages are not context-free:*

*(a) $\{a^nb^nc^n : n \ge 0\}$.*
*(b) $\{ww : w \in \{a,b\}^*\}$.*
*(c) $\{a^{n^2} : n \ge 0\}$.*

*Proof of (a).* Given pumping length $p$, choose $s = a^pb^pc^p$. Since $|vxy| \le p$, the window cannot span all three letter types. If $vxy$ is within the $a$s and $b$s, pumping up increases $a$s or $b$s but not $c$s; if within the $b$s and $c$s, pumping up increases $b$s or $c$s but not $a$s. In both cases the pumped string has unequal counts. $\square$

*Proof of (b).* Choose $s = a^pb^pa^pb^p$. The window $|vxy| \le p$ fits within at most two of the four blocks. Pumping changes the counts within those blocks without matching the change in the corresponding position of the would-be repeated half. The resulting string is not of the form $ww$. $\square$

*Proof of (c).* Choose $s = a^{p^2}$. Pumping up gives $a^{p^2 + |v| + |y|}$. Since $1 \le |vy| \le p$, we have $p^2 < p^2 + |vy| \le p^2 + p$. For $p \ge 2$: $(p+1)^2 = p^2 + 2p + 1 > p^2 + p$, so $p^2 + |vy|$ is not a perfect square. $\square$

**Bridge.** The CFL pumping lemma builds toward the Chomsky hierarchy where each level (regular, context-free, context-sensitive, recursively enumerable) is separated by pumping arguments of increasing sophistication. This is exactly the structural pattern established by the regular pumping lemma in [47.01.04], now generalised from one pumpable region to two. The lemma appears again in [47.02.01] where the P-vs-NP question asks whether polynomial-time computation can solve all of NP, an analogous containment question to whether context-free grammars generate all of $\{a^nb^nc^n\}$. The central insight is that parse-tree structure — specifically the branching factor of grammar rules — constrains the kinds of patterns a language can exhibit, and this is exactly what makes the CFL pumping lemma a strictly stronger tool than its regular counterpart. Putting these together with the CYK algorithm gives a complete picture: the pumping lemma provides negative evidence (what CFLs cannot do) while CYK provides positive evidence (efficient membership testing for what CFLs can do), and this duality between lower bounds and algorithms is the foundational reason theoretical computer science studies both simultaneously.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 1 (easy, symbolic).**

Use the CFL pumping lemma to prove that $L = \{a^nb^nc^{2n} : n \ge 0\}$ is not context-free.

<details class="exercise__hint"><summary>Hint</summary>

Choose $s = a^pb^pc^{2p}$ and consider where $vxy$ falls.

</details>

<details class="exercise__answer"><summary>Answer</summary>

With pumping length $p$, choose $s = a^pb^pc^{2p}$. Since $|vxy| \le p$, the window cannot contain all three letter types. If $vxy$ is within $a$s and $b$s, pumping changes the $a$-count or $b$-count but not the $c$-count, breaking the ratio. If $vxy$ is within $b$s and $c$s, pumping changes $b$s and/or $c$s without changing $a$s, again breaking the ratio. Contradiction.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

True or false: $\{a^nb^n : n \ge 0\}$ is context-free.

<details class="exercise__answer"><summary>Answer</summary>

**True.** This language is generated by the grammar $S \to aSb \mid \varepsilon$. It is a classic example of a context-free language that is not regular.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Convert the following grammar to Chomsky normal form: $S \to aSb \mid \varepsilon$.

<details class="exercise__hint"><summary>Hint</summary>

Introduce new nonterminals for terminals and eliminate the $\varepsilon$-production.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Step 1: Remove $\varepsilon$-production. Since $S \to \varepsilon$, we must handle this. Add a new start symbol $S'$: $S' \to S \mid \varepsilon$, $S \to aSb \mid ab$.

Step 2: Introduce terminal nonterminals: $A \to a$, $B \to b$. Now $S \to ASB \mid AB$.

Step 3: Binarise: $S \to AU_1$, $U_1 \to SB$, and $S \to AB$.

Final CNF: $S' \to S \mid \varepsilon$, $S \to AU_1 \mid AB$, $U_1 \to SB$, $A \to a$, $B \to b$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Run the CYK algorithm on the string "baab" with grammar $S \to AB \mid BA \mid SS$, $A \to a \mid AS$, $B \to b \mid BS$ (in CNF). Is "baab" in the language?

<details class="exercise__hint"><summary>Hint</summary>

Fill the triangular table bottom-up. Start with single characters, then pairs, then triples, then the full string.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$T[1,1] = \{B\}$ ($B \to b$), $T[2,2] = \{A\}$ ($A \to a$), $T[3,3] = \{A\}$, $T[4,4] = \{B\}$.

$T[1,2]$: split at $k=1$: $T[1,1] \times T[2,2] = \{BA\}$. $S \to BA$ gives $T[1,2] = \{S\}$.

$T[2,3]$: $T[2,2] \times T[3,3] = \{AA\}$. No rule produces $AA$. $T[2,3] = \emptyset$.

$T[3,4]$: $T[3,3] \times T[4,4] = \{AB\}$. $S \to AB$ gives $T[3,4] = \{S\}$.

$T[1,3]$: splits $k=1,2$. $k=1$: $T[1,1] \times T[2,3] = \emptyset$. $k=2$: $T[1,2] \times T[3,3] = \{S\} \times \{A\} = \{SA\}$. $A \to AS$ gives $T[1,3] = \{A\}$.

$T[2,4]$: splits $k=2,3$. $k=2$: $T[2,2] \times T[3,4] = \{A\} \times \{S\} = \{AS\}$. $A \to AS$ gives $\{A\}$. $k=3$: $T[2,3] \times T[4,4] = \emptyset$. $T[2,4] = \{A\}$.

$T[1,4]$: splits $k=1,2,3$. $k=1$: $T[1,1] \times T[2,4] = \{B\} \times \{A\} = \{BA\}$. $S \to BA$ gives $\{S\}$. $k=2$: $T[1,2] \times T[3,4] = \{S\} \times \{S\} = \{SS\}$. $S \to SS$ gives $\{S\}$. $k=3$: $T[1,3] \times T[4,4] = \{A\} \times \{B\} = \{AB\}$. $S \to AB$ gives $\{S\}$. $T[1,4] = \{S\}$.

Since $S \in T[1,4]$, "baab" is in the language.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Prove that $L = \{a^ib^jc^k : i < j < k\}$ is not context-free.

<details class="exercise__hint"><summary>Hint</summary>

Choose $s = a^pb^{p+1}c^{p+2}$. The window $|vxy| \le p$ restricts which letters can appear together.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Choose $s = a^pb^{p+1}c^{p+2}$ with pumping length $p$. The window $vxy$ with $|vxy| \le p$ spans at most two consecutive letter types. If $vxy$ is in $a$s and $b$s, pumping down reduces $a$s or $b$s. Pumping down $a$s gives $a^{p-|v_y|}b^{p+1}c^{p+2}$ where $|v_y| > 0$ within the $a$-$b$ region. If $v$ consists of $a$s, then after pumping down, the $a$-count decreases while $b$ and $c$ counts stay the same, potentially still satisfying $i < j < k$. However, pumping up adds $a$s or $b$s: if pumping adds only $b$s, then $b$-count becomes $p+1+|y| \ge p+2 = c$-count, violating $j < k$. If $vxy$ is in $b$s and $c$s, similar analysis with pumping shows the strict inequality fails. In every case, some pumped version escapes the language. Contradiction.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Prove that $L = \{a^nb^n a^nb^n : n \ge 0\}$ is not context-free.

<details class="exercise__hint"><summary>Hint</summary>

Choose $s = a^pb^pa^pb^p$. The window $|vxy| \le p$ cannot span both "halves" of the string.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Choose $s = a^pb^pa^pb^p$ with pumping length $p$. Since $|vxy| \le p$, the window cannot simultaneously cover part of the first $a^pb^p$ and the second $a^pb^p$ (the gap between them is too large). So $vxy$ is contained within either the first half ($a^pb^p$) or straddles the boundary between the first $b^p$ and the second $a^p$ or is in the second half.

If $vxy$ is in the first $a^pb^p$: pumping changes the first half without changing the second half, so the two halves no longer match.

If $vxy$ straddles the $b$-to-$a$ boundary in the middle: pumping changes the middle $b$s and $a$s, disrupting the equality of both the $b$-counts and the second $a$-count simultaneously.

If $vxy$ is in the second $a^pb^p$: pumping changes the second half without matching the first. In all cases, pumping produces a string not in $L$. Contradiction.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Show that the CFL pumping lemma cannot prove that $\{a^nb^nc^n : n \ge 0\}$ is not context-free if we only use the regular pumping lemma style (one pumpable region).

<details class="exercise__answer"><summary>Answer</summary>

Consider a hypothetical "single-region" pumping lemma for CFLs where $s = xyz$ and $xy^iz \in L$. The language $\{a^nb^nc^n\}$ would not be disproved by such a lemma: any string $a^pb^pc^p$ can have $y$ within a single letter block, and pumping within a single block changes only one count while the other two stay equal, which might still fail the three-way equality. But the crucial point is that the CFL pumping lemma's two-region structure is necessary to capture the branching nature of parse trees. A single-region version would fail to exploit the grammar structure. The actual proof works because the two regions $v$ and $y$ are generated by the two branches of a repeated nonterminal, and the constraint $|vxy| \le p$ forces both regions to be "local" in the string while being "distant" in the parse tree.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Prove that the CYK algorithm correctly decides CFL membership: $S \in T[1,n]$ if and only if $w \in L(G)$.

<details class="exercise__answer"><summary>Answer</summary>

By induction on span length $\ell$. Base case $\ell = 1$: $T[i,i]$ contains exactly the nonterminals $A$ with $A \to a_i \in R$, which are precisely the nonterminals deriving the single character $a_i$. Inductive step: assume the claim holds for all spans of length $< \ell$. For span $[i,j]$ of length $\ell$, $A \in T[i,j]$ iff there exists a split $k$ with $A \to BC \in R$ and $B \in T[i,k]$, $C \in T[k+1,j]$. By the inductive hypothesis, $B \in T[i,k]$ iff $B \Rightarrow^* a_i \cdots a_k$, and $C \in T[k+1,j]$ iff $C \Rightarrow^* a_{k+1} \cdots a_j$. So $A \in T[i,j]$ iff $A \Rightarrow BC \Rightarrow^* (a_i \cdots a_k)(a_{k+1} \cdots a_j) = a_i \cdots a_j$. The claim follows. Therefore $S \in T[1,n]$ iff $S \Rightarrow^* w$ iff $w \in L(G)$.

</details>

</aside>

## Lean formalization [Intermediate+]

Mathlib has `Computability.ContextFreeGrammar` with basic grammar definitions but lacks CNF conversion, the CFL pumping lemma, and the CYK algorithm. The foundational gap is the absence of a verified CNF conversion procedure, a `ContextFree.pumping_lemma` theorem, and a `ContextFree.cyk_membership` decision procedure with a correctness proof. The CYK algorithm would be a natural target for formalisation because its dynamic-programming structure maps well to induction on span length. This unit ships without a lean_module.

## Advanced results [Master]

Three results sharpen the CFL pumping lemma and its algorithmic consequences.

**Theorem 1 (Ogden's lemma).** *Let $L$ be context-free. Then there exists $p$ such that for any string $s \in L$ with $|s| \ge p$ and any marking of at least $p$ positions of $s$ as "distinguished," $s = uvxyz$ with (1) $vy$ contains at least one distinguished position, (2) $vxy$ contains at most $p$ distinguished positions, and (3) $uv^ixy^iz \in L$ for all $i \ge 0$* [ref: TODO_REF Hopcroft, Motwani & Ullman §7.4].

Ogden's lemma is strictly stronger than the CFL pumping lemma. It can prove languages non-context-free that the ordinary pumping lemma cannot handle. For example, $L = \{a^ib^jc^kd^l : i = 0 \text{ or } j = k = l\}$ is not context-free, but the ordinary pumping lemma fails to prove it because the $i = 0$ case gives too many strings that pump without issue. Ogden's lemma resolves this by marking the $b$, $c$, and $d$ positions as distinguished.

**Theorem 2 (CYK optimality).** *The CYK algorithm runs in $O(n^3 |G|)$ time and $\Theta(n^2)$ space. For general CFGs, this is optimal under the strong exponential time hypothesis (SETH), though faster algorithms exist for restricted grammar classes.*

The Valiant algorithm (1975) reduces CYK-style parsing to Boolean matrix multiplication, achieving $O(n^{2.373})$ time using the current best matrix multiplication algorithms. However, the practical constant factors make CYK preferable for moderate-length inputs.

**Theorem 3 (Parikh's theorem).** *Every context-free language over a unary alphabet is regular. More generally, the Parikh image of any CFL (the set of letter-count vectors) is a semilinear set* [ref: TODO_REF Sipser §2.3].

Parikh's theorem implies that the CFL pumping lemma can never prove non-context-freeness of a unary language, because all unary CFLs are regular and hence satisfy the regular pumping lemma. The theorem connects the algebraic structure of CFLs to Presburger arithmetic.

**Synthesis.** The CFL pumping lemma is the foundational reason that context-free languages form a proper superset of regular languages while remaining strictly below context-sensitive languages: the central insight is that the binary branching of parse trees allows two simultaneous pumping regions, and this is exactly what enables CFLs to match paired structures like $\{a^nb^n\}$ while still failing on triple structures like $\{a^nb^nc^n\}$. This builds toward the Chomsky hierarchy where each level is separated by increasingly sophisticated pumping arguments. The CYK algorithm provides the algorithmic dual: it exploits the same binary branching structure for efficient membership testing. The lemma appears again in [47.02.01] where NP-completeness plays an analogous role to non-context-freeness — proving that certain problems lie outside a complexity class. The generalises from the regular pumping lemma of [47.01.04] by replacing the state-sequence pigeonhole argument with a parse-tree pigeonhole argument, and putting these together gives the full picture of how structural properties of grammars constrain the languages they generate.

## Full proof set [Master]

**Proposition 1 (CFL pumping lemma).** *If $L$ is a CFL, then the pumping lemma conditions hold.*

*Proof.* Let $G = (V, \Sigma, R, S)$ be a CFG in CNF generating $L \setminus \{\varepsilon\}$ with $|V| = k$. Set $p = 2^k$. For $s \in L$ with $|s| \ge p$, consider a parse tree for $s$. The yield of a subtree of height $h$ has length at most $2^{h-1}$ (each internal node has exactly 2 children in CNF). Since $|s| \ge 2^k$, the height is at least $k + 1$, so the longest root-to-leaf path has at least $k + 1$ internal nodes with nonterminal labels. By pigeonhole, some $A \in V$ appears at least twice.

Let the upper occurrence of $A$ be at node $n_1$ and the lower at $n_2$. The subtree at $n_1$ has yield $vxy$ and the subtree at $n_2$ has yield $v$ (or $vy$, depending on position). Since $n_2$ is a proper descendant of $n_1$ and $G$ has no unit productions in CNF, $|vy| > 0$. The path from $n_1$ to $n_2$ uses at most $k + 1$ nonterminals, so the subtree at $n_1$ has height at most $k + 1$, giving $|vxy| \le 2^k = p$.

For any $i \ge 0$, the derivation $A \Rightarrow^* vAy$ (from $n_1$'s expansion to $n_2$) can be iterated $i$ times to get $A \Rightarrow^* v^i A y^i$. This produces a valid derivation of $uv^ixy^iz$. $\square$

**Proposition 2 (CYK correctness).** *After running CYK on input $w = a_1 \cdots a_n$ with CNF grammar $G$, $S \in T[1,n]$ if and only if $w \in L(G)$.*

*Proof.* By induction on span length $\ell = j - i + 1$. Base $\ell = 1$: $A \in T[i,i]$ iff $A \to a_i \in R$ iff $A \Rightarrow^* a_i$. Inductive step: $A \in T[i,j]$ iff $\exists k$ with $A \to BC \in R$, $B \in T[i,k]$, $C \in T[k+1,j]$. By induction, $B \Rightarrow^* a_i \cdots a_k$ and $C \Rightarrow^* a_{k+1} \cdots a_j$. So $A \Rightarrow BC \Rightarrow^* (a_i \cdots a_k)(a_{k+1} \cdots a_j)$. Conversely, if $A \Rightarrow^* a_i \cdots a_j$ with $\ell > 1$, the first production step must be $A \to BC$ (CNF), and the yield splits at some $k$ with $B \Rightarrow^* a_i \cdots a_k$ and $C \Rightarrow^* a_{k+1} \cdots a_j$. By induction, $B \in T[i,k]$ and $C \in T[k+1,j]$, so $A \in T[i,j]$. $\square$

## Connections [Master]

- [47.01.04] — The regular pumping lemma is the structural precursor: one pumpable region from DFA state repetition versus two from parse-tree nonterminal repetition.

- [47.01.05] — Context-free grammars and pushdown automata define the CFL class; the pumping lemma and CYK algorithm provide the analytical and algorithmic tools for working with this class.

- [47.02.01] — The P-vs-NP question mirrors the regular-vs-CFL containment question: both ask whether a richer computational model solves problems that a weaker one cannot.

- [47.02.05] — Space complexity provides another hierarchy where pumping-style arguments (via the space hierarchy theorem) prove strict separations between complexity classes.

- [40.04.01] — Graph-theoretic parsing methods (such as parse-forest intersection and shared parse forests) use the same dynamic-programming structure as CYK.

## Historical & philosophical context [Master]

The context-free pumping lemma was proved by Bar-Hillel, Perles, and Shamir in 1961 as part of their systematic study of phrase-structure grammars. Their paper "On Formal Properties of Simple Phrase Structure Grammars" established both the pumping lemma and several closure properties of CFLs [ref: TODO_REF Sipser §2.3].

The CYK algorithm is named after Cocke, Younger, and Kasami, who discovered it independently. Younger (1967) proved that context-free language recognition can be done in $O(n^3)$ time using dynamic programming. Kasami described a similar algorithm in an internal technical report (1965). The connection to Chomsky normal form as the enabling preprocessing step was made explicit by Hopcroft and Ullman (1969).

Ogden's lemma (1968) strengthened the pumping lemma by introducing the concept of "distinguished positions," resolving several open questions about the boundaries of the CFL class. William Ogden's paper "A Helpful Result for Proving inherent Ambiguity" showed that the language $\{a^ib^jc^k : i = j \text{ or } j = k\}$ is inherently ambiguous (every grammar for it is ambiguous), using his strengthened lemma as a key tool.

Parikh's theorem (1966) revealed a deep algebraic property of CFLs: their commutative image is always semilinear. This connected formal language theory to the theory of Presburger arithmetic and showed that the "counting" power of CFLs is fundamentally limited.

## Bibliography [Master]

```bibtex
@book{sipser2013,
  author    = {Sipser, Michael},
  title     = {Introduction to the Theory of Computation},
  edition   = {3rd},
  publisher = {Cengage Learning},
  year      = {2013},
}
@book{hopcroft2006,
  author    = {Hopcroft, John E. and Motwani, Rajeev and Ullman, Jeffrey D.},
  title     = {Introduction to Automata Theory, Languages, and Computation},
  edition   = {3rd},
  publisher = {Pearson},
  year      = {2006},
}
@article{barhillel1961formal,
  author  = {Bar-Hillel, Yehoshua and Perles, Micha and Shamir, Eli},
  title   = {On Formal Properties of Simple Phrase Structure Grammars},
  journal = {Zeitschrift f\"ur Phonetik, Sprachwissenschaft und Kommunikationsforschung},
  volume  = {14},
  pages   = {143--172},
  year    = {1961},
}
@article{ogden1968helpful,
  author  = {Ogden, William},
  title   = {A Helpful Result for Proving Inherent Ambiguity},
  journal = {Mathematical Systems Theory},
  volume  = {2},
  number  = {3},
  pages   = {191--194},
  year    = {1968},
}
@article{youngers1967recognition,
  author  = {Youngers, Daniel H.},
  title   = {Recognition and Parsing of Context-Free Languages in Time $n^3$},
  journal = {Information and Control},
  volume  = {10},
  number  = {2},
  pages   = {189--208},
  year    = {1967},
}
```
