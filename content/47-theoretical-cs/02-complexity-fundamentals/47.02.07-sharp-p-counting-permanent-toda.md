---
id: 47.02.07
title: "#P, Counting, the Permanent, and Toda's Theorem"
slug: sharp-p-counting-permanent-toda
section: theoretical-cs
chapter: 02-complexity-fundamentals
concept_catalog_id: theoretical-cs.sharp-p-counting-permanent-toda
prerequisites:
  - 47.02.02  # NP-completeness Cook-Levin
  - 37.01.01  # probability spaces and Kolmogorov extension
successors: []
tier_anchors:
  beginner: 'Arora and Barak 2009 *Computational Complexity: A Modern Approach* (Cambridge) §17.1 (the idea of counting solutions rather than just asking whether one exists, and why this is harder)'
  intermediate: 'Arora and Barak 2009 *Computational Complexity: A Modern Approach* (Cambridge) §17.1-17.3 (the class #P, #P-completeness, the permanent of a matrix and Valiant''s theorem, and Toda''s theorem that $\mathrm{PH} \subseteq \mathrm{P}^{\#P}$)'
  master: 'Arora and Barak 2009 *Computational Complexity: A Modern Approach* (Cambridge) §17.1-17.4 (#P and its structural properties, Valiant''s theorem that the permanent is #P-complete, Toda''s theorem with the full proof via probabilistic reductions and the pumping lemma, and connections to interactive proofs and hardness of approximation)'
tiers_present: [beginner, intermediate, master]
pending_prereqs: true
references:
  - source: TODO_REF
    path: "Arora, S. and Barak, B. — Computational Complexity: A Modern Approach"
    locator: 'Cambridge University Press (2009). §17.1 defines the class $\#P$ as the class of functions $f : \{0,1\}^* \to \mathbb{N}$ where $f(x)$ equals the number of accepting paths of a polynomial-time nondeterministic Turing machine on input $x$, distinguishes $\#P$ from decision classes by noting that $\#P$ is a class of functions not languages, defines $\#P$-completeness under polynomial-time Turing reductions and under parsimonious reductions; §17.2 presents Valiant''s 1979 theorem that the permanent $\mathrm{perm}(A) = \sum_{\sigma \in S_n} \prod_{i=1}^n A_{i,\sigma(i)}$ is $\#P$-complete even for 0-1 matrices, with the proof constructing a reduction from $\#3SAT$ to the permanent by encoding variable assignments and clause satisfactions as matrix entries and using cycle covers to ensure correct counting; §17.3 states and proves Toda''s theorem $\mathrm{PH} \subseteq \mathrm{P}^{\#P}$ by first showing $\mathrm{P}^{\#P} = \mathrm{P}^{\mathrm{PP}}$ and then using a randomised reduction from any $\Sigma_k$ predicate to a single $\#P$ query, with derandomisation via the Valiant-Vazirani isolation lemma.'
lean_status: none
lean_mathlib_gap: |
  Mathlib has the permanent of a matrix defined and some basic properties, but no
  complexity-theoretic formalisation of #P. Specifically absent are: the class #P
  defined via counting accepting paths of nondeterministic polynomial-time machines;
  #P-completeness and the parsimonious reduction framework; Valiant's reduction
  from #3SAT to the permanent; Toda's theorem and its probabilistic reduction
  machinery; and the PP completeness of the permanent. The permanent itself exists
  in Mathlib as a matrix computation, but its computational complexity significance
  is entirely absent.
human_reviewer: "Tyler (pending external complexity-theory reviewer)"
source_books: [arora-barak-2009]
source_curriculum: theoretical-cs
estimated_time:
  beginner: 15m
  intermediate: 50m
  master: 95m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
hooks_out:
  - target: "47.05.04"
    kind: proposed
    why: >
      Counting problems in #P are the natural setting for FPRAS
      (fully polynomial-time randomised approximation schemes), which
      provide efficient approximate counting for #P-complete problems
      like the permanent of matrices with non-negative entries.
---
## Intuition [Beginner]

In NP problems, the question is "does at least one solution exist?" A different question is "how many solutions exist?" This is a counting problem, and it is often much harder than the existence question.

Think of a jigsaw puzzle. Asking "can this puzzle be solved?" is an existence question. Asking "in how many different ways can this puzzle be solved?" is a counting question. The second question contains the answer to the first (if the count is zero, no solution exists), but it also gives much more information.

The class #P (pronounced "sharp P") captures this counting perspective. A function is in #P if it counts the number of accepting computation paths of a polynomial-time nondeterministic machine. This is the counting analogue of NP: NP asks whether the count is positive, while #P asks for the exact count.

A remarkable theorem by Toda shows that #P is at least as powerful as the entire polynomial hierarchy. Any problem that can be expressed with any number of alternating "there exists" and "for all" quantifiers can be solved with a single query to a #P oracle. This means counting is extraordinarily powerful — far more powerful than mere existence.

The permanent of a matrix, a simple-looking algebraic expression that swaps the signs in the determinant, turns out to be #P-complete. Computing it is as hard as any counting problem.

## Visual [Beginner]

The relationship between NP, #P, and the polynomial hierarchy.

```
               P^#P
              / |  \
            PH  |   \
           /|\  |    \
          / | \ |     \
         P  NP  coNP   ...
```

Toda's theorem says the entire polynomial hierarchy (PH) sits inside P with access to a #P oracle. The permanent function is #P-complete: it encodes every counting problem.

| concept | NP version | #P version |
|---|---|---|
| SAT | is there a satisfying assignment? | how many satisfying assignments? |
| matching | is there a perfect matching? | how many perfect matchings? |
| paths | is there a path from s to t? | how many paths from s to t? |

## Worked example [Beginner]

Consider a 3-CNF formula $\phi$ with three variables and two clauses: $(x_1 \lor x_2 \lor x_3) \land (\neg x_1 \lor \neg x_2 \lor x_3)$.

The SAT question: is at least one of the 8 possible assignments satisfying? Let us check. Assignment $(0, 0, 1)$ gives both clauses true. Assignment $(1, 1, 1)$ gives first clause true, second clause false. So yes, $\phi$ is satisfiable.

The #SAT question: how many satisfying assignments are there? Checking all 8: $(0,0,0)$ fails clause 1. $(0,0,1)$ satisfies both. $(0,1,0)$ satisfies both. $(0,1,1)$ satisfies both. $(1,0,0)$ satisfies both. $(1,0,1)$ satisfies both. $(1,1,0)$ fails clause 2. $(1,1,1)$ fails clause 2. Total: 5 satisfying assignments.

The #SAT function maps $\phi$ to 5. This function is in #P: a nondeterministic machine guesses an assignment and verifies it in polynomial time, and the number of accepting paths equals the number of satisfying assignments. The #P-completeness of #SAT means every #P function reduces to counting satisfying assignments.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: #P is a class of decision problems (languages).

<details class="exercise__hint"><summary>Hint</summary>

#P deals with counting, not yes/no questions.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** #P is a class of functions from strings to natural numbers. Each function in #P counts the accepting paths of a nondeterministic polynomial-time machine. Decision classes like NP and P consist of languages (sets of strings).

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple-choice).**

Toda's theorem states:

<ol class="exercise__options" type="A">
<li>$\mathrm{P} = \mathrm{NP}$</li>
<li>$\mathrm{PH} \subseteq \mathrm{P}^{\#P}$</li>
<li>$\#P = \mathrm{NP}$</li>
<li>$\mathrm{NP} \subseteq \#P$</li>
</ol>

<details class="exercise__hint"><summary>Hint</summary>

The theorem relates the polynomial hierarchy to the power of counting.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Toda's theorem states that the entire polynomial hierarchy is contained in $\mathrm{P}^{\#P}$ — deterministic polynomial time with access to a #P oracle. This means a single counting query is at least as powerful as any fixed number of alternating quantifiers.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: the permanent of a 0-1 matrix counts the number of perfect matchings in the corresponding bipartite graph.

<details class="exercise__hint"><summary>Hint</summary>

Each permutation in the permanent corresponds to selecting exactly one edge per vertex pair.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** For a bipartite graph with adjacency matrix $A$ (where $A_{ij} = 1$ if there is an edge from vertex $i$ to vertex $j$), each permutation $\sigma$ contributes 1 to the permanent if all selected edges exist. The permanent counts exactly the number of perfect matchings.

</details>

</aside>

## Formal definition [Intermediate+]

A function $f : \{0,1\}^* \to \mathbb{N}$ is in **#P** if there exists a polynomial-time nondeterministic Turing machine $M$ such that for every input $x$,
$$
f(x) = |\{ \text{accepting computation paths of } M \text{ on } x \}|.
$$
The class $\#P$ is a function class, not a language class. To relate it to decision classes, define $\mathrm{P}^{\#P}$ as the class of languages decidable by a polynomial-time deterministic Turing machine with oracle access to a function in $\#P$: the machine may write a query $q$ on a special tape, receive $f(q)$ as the answer, and use it in subsequent computation. Define $\mathrm{PP}$ (probabilistic polynomial time) as the class of languages $L$ for which there exists a polynomial-time probabilistic Turing machine $M$ such that $x \in L \iff \Pr[M(x) \text{ accepts}] > 1/2$.

The **permanent** of an $n \times n$ matrix $A$ is
$$
\mathrm{perm}(A) = \sum_{\sigma \in S_n} \prod_{i=1}^{n} A_{i, \sigma(i)},
$$
where $S_n$ is the symmetric group on $\{1, \ldots, n\}$. Unlike the determinant, the permanent omits the sign factor $\mathrm{sgn}(\sigma)$. This seemingly small change has enormous computational consequences: the determinant is computable in polynomial time by Gaussian elimination, but the permanent is #P-complete [ref: TODO_REF Arora-Barak §17.2].

A **parsimonious reduction** from $\#P$ function $f$ to $\#P$ function $g$ is a polynomial-time computable function $h$ with $f(x) = g(h(x))$ for all $x$. A **polynomial-time Turing reduction** from $f$ to $g$ is a polynomial-time oracle machine computing $f$ with access to $g$.

### Counterexamples to common slips [Intermediate+]

- *"#P is just NP with counting."* The distinction is subtler. NP is a class of languages (decision problems); #P is a class of functions. A #P function gives the exact number of witnesses, which contains more information than the mere existence question that NP asks. The decision version "is the count positive?" gives NP, but recovering the exact count from NP queries may require exponentially many queries.

- *"The permanent is the determinant without signs, so it should be equally easy to compute."* The sign in the determinant enables row-reduction (Gaussian elimination) without changing the answer. Without the sign, row operations can change the permanent, so the efficient algorithm breaks down entirely. Valiant's theorem shows the permanent is #P-complete even for 0-1 matrices.

- *"Toda's theorem means PH collapses."* It does not. Toda's theorem places PH inside $\mathrm{P}^{\#P}$, but $\mathrm{P}^{\#P}$ could be much larger than PH. The theorem shows that counting is powerful, not that the hierarchy collapses. A collapse would mean some level $\Sigma_k$ equals $\Sigma_{k+1}$.

## Key theorem with proof [Intermediate+]

**Theorem (Valiant, 1979).** *The permanent of 0-1 matrices is #P-complete under polynomial-time reductions.*

*Proof sketch.* Membership in #P: the permanent of an $n \times n$ matrix $A$ equals the number of perfect matchings in the weighted bipartite graph defined by $A$. A nondeterministic machine guesses a permutation $\sigma$ and verifies that $\prod_i A_{i,\sigma(i)} = 1$, accepting if so. The number of accepting paths is $\mathrm{perm}(A)$.

Hardness: reduce #3SAT to the permanent. Given a 3-CNF formula $\phi$ with $n$ variables and $m$ clauses, construct an integer matrix $A$ such that $\mathrm{perm}(A) = 4^{3m} \cdot |\{\text{satisfying assignments}\}|$. The construction uses a graph whose cycle covers encode variable assignments and clause satisfactions. Each variable $x_i$ is represented by a gadget with two possible traversal modes (true or false). Each clause is a gadget that contributes a weight of 1 for each satisfied literal. The matrix entries are 0 or 1, and the permanent counts exactly the number of satisfying assignments, up to a known multiplicative factor that can be divided out. The construction is polynomial in $n + m$. $\square$

**Theorem (Toda, 1991).** *$\mathrm{PH} \subseteq \mathrm{P}^{\#P}$.*

*Proof sketch.* The proof has two stages. Stage 1: show $\Sigma_k \subseteq \mathrm{BP} \cdot \oplus \cdot \mathrm{P}$ for every $k$, where $\mathrm{BP} \cdot \oplus \cdot \mathrm{P}$ is the class of languages for which there exists a polynomial-time predicate $R$ such that $x \in L \Rightarrow \Pr_y[|\{z : R(x, y, z)\}| \text{ is odd}] \ge 2/3$ and $x \notin L \Rightarrow \Pr_y[|\{z : R(x, y, z)\}| \text{ is odd}] \le 1/3$. Stage 2: show $\mathrm{BP} \cdot \oplus \cdot \mathrm{P} \subseteq \mathrm{P}^{\#P}$.

Stage 1 uses the Valiant-Vazirani lemma: given a Boolean formula $\phi$, one can produce a formula $\psi$ (by adding random constraints) such that if $\phi$ is unsatisfiable then $\psi$ is unsatisfiable, and if $\phi$ has at least one satisfying assignment then $\psi$ has exactly one with probability $\ge 1/n$. This "isolation" converts a "there exists" quantifier into a parity constraint, and the probability boost is handled by amplification [ref: TODO_REF Arora-Barak §17.3].

Stage 2: computing $\Pr_y[|\{z : R(x, y, z)\}| \text{ is odd}]$ reduces to a single #P query, since the probability is a ratio of counts that can be extracted from a #P oracle. $\square$

**Bridge.** This theorem is the foundational reason counting is more powerful than alternating quantification: a single #P query captures the entire polynomial hierarchy, and this is exactly the structural insight that makes the permanent such a potent computational object — it encodes every counting problem into a purely algebraic expression. The isolation lemma of Valiant-Vazirani builds toward the PCP theorem [47.03.05] where similar hashing techniques isolate witness structures, and the counting perspective appears again in the FPRAS algorithms [47.05.04] where approximate counting for #P-complete problems like the permanent becomes feasible when randomisation is allowed. The central insight is that parity (counting modulo 2) plus randomisation already captures existential quantification, and putting these together through the Toda pipeline, the entire quantifier hierarchy is swallowed by a single counting query. The bridge is that the permanent, as the simplest algebraic expression that is #P-complete, connects the discrete world of Boolean satisfiability to the continuous world of linear algebra, and this connection generalises to the entire theory of algebraic complexity.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 1 (easy, true-false).**

True or false: the determinant of an $n \times n$ integer matrix can be computed in polynomial time.

<details class="exercise__hint"><summary>Hint</summary>

Gaussian elimination works for determinants.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** The determinant can be computed in $O(n^3)$ time by Gaussian elimination with fraction-free algorithms or modular arithmetic to control coefficient growth. This is a fundamental contrast with the permanent.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 2 (easy, multiple-choice).**

For a bipartite graph $G$ with $n$ vertices on each side and adjacency matrix $A$, what does $\mathrm{perm}(A)$ count?

<ol class="exercise__options" type="A">
<li>The number of edges in $G$.</li>
<li>The number of perfect matchings in $G$.</li>
<li>The number of connected components of $G$.</li>
<li>The determinant of $A$.</li>
</ol>

<details class="exercise__hint"><summary>Hint</summary>

Each permutation selects one edge per vertex. A perfect matching selects exactly one edge incident to each vertex.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** Each permutation $\sigma$ selects edges $(i, \sigma(i))$ for $i = 1, \ldots, n$. The product $\prod_i A_{i,\sigma(i)}$ is 1 precisely when all selected edges exist in $G$. The permanent sums over all permutations, counting perfect matchings.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Compute the permanent of the matrix $A = \begin{pmatrix} 1 & 1 & 0 \\ 1 & 0 & 1 \\ 0 & 1 & 1 \end{pmatrix}$. Interpret the result in terms of bipartite graph matchings.

<details class="exercise__hint"><summary>Hint</summary>

Enumerate all 6 permutations of $\{1, 2, 3\}$ and compute the product for each.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The six permutations and their contributions: $\text{id} = (1)(2)(3)$: $1 \cdot 0 \cdot 1 = 0$. $(12)$: $1 \cdot 1 \cdot 1 = 1$. $(13)$: $0 \cdot 1 \cdot 1 = 0$. $(23)$: $1 \cdot 1 \cdot 1 = 1$. $(123)$: $1 \cdot 1 \cdot 0 = 0$. $(132)$: $0 \cdot 1 \cdot 1 = 0$. Permanent $= 0 + 1 + 0 + 1 + 0 + 0 = 2$. The corresponding bipartite graph has 2 perfect matchings: $\{(1,2), (2,1), (3,3)\}$ via $(12)$ and $\{(1,1), (2,3), (3,2)\}$ via $(23)$. Rubric: full credit for the enumeration and the matching interpretation.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Show that $\mathrm{NP} \subseteq \mathrm{P}^{\#P}$ by reducing SAT to a #P oracle query.

<details class="exercise__hint"><summary>Hint</summary>

A formula is satisfiable iff the number of satisfying assignments is at least 1. The #SAT function is in #P.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\phi$ be a CNF formula. Query the #P oracle for $\#\mathrm{SAT}(\phi)$, the number of satisfying assignments. If $\#\mathrm{SAT}(\phi) \ge 1$, accept (the formula is satisfiable). If $\#\mathrm{SAT}(\phi) = 0$, reject. This is a single query to a #P function, so SAT is in $\mathrm{P}^{\#P}$. Since SAT is NP-complete, every NP language reduces to SAT and then to the oracle query, giving $\mathrm{NP} \subseteq \mathrm{P}^{\#P}$. Rubric: full credit for the single-query reduction.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 5 (medium, multiple-choice).**

The Valiant-Vazirani lemma states that there is a probabilistic polynomial-time reduction from SAT to:

<ol class="exercise__options" type="A">
<li>3SAT</li>
<li>Unique-SAT (formulas with at most one satisfying assignment)</li>
<li>UNSAT</li>
<li>Circuit-SAT</li>
</ol>

<details class="exercise__hint"><summary>Hint</summary>

The lemma "isolates" a single witness by adding random hash constraints.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** The Valiant-Vazirani lemma gives a randomised reduction from SAT to Unique-SAT: given a satisfiable formula with $k$ satisfying assignments, the reduction produces a formula that has exactly one satisfying assignment with probability at least $1/n$. This isolation is a key ingredient in Toda's theorem.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Prove that $\mathrm{PP} \subseteq \mathrm{P}^{\#P}$, where $\mathrm{PP}$ is the class of languages decidable by a probabilistic polynomial-time machine with error probability $< 1/2$ (unbounded gap from 1/2).

<details class="exercise__hint"><summary>Hint</summary>

The acceptance probability of a PP machine is a ratio of counts, which can be computed with a #P query.

</details>

<details class="exercise__answer"><summary>Answer</summary>

A PP machine $M$ on input $x$ has $N = 2^{p(|x|)}$ possible random strings. Let $a(x)$ be the number of random strings leading to acceptance. Then $x \in L$ iff $a(x) > N/2$. The function $a(x)$ is in #P: the nondeterministic machine guesses a random string $r$ and simulates $M(x, r)$, accepting iff $M$ accepts. So $a(x) = \#\mathrm{acc}(x)$ is a #P function. A $\mathrm{P}^{\#P}$ machine queries the oracle for $a(x)$ and $N = 2^{p(|x|)}$, then checks whether $a(x) > N/2$. This is a single #P query with polynomial post-processing. Hence $\mathrm{PP} \subseteq \mathrm{P}^{\#P}$. Rubric: full credit for identifying $a(x)$ as a #P function and the threshold comparison.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove that $\mathrm{P}^{\#P} = \mathrm{P}^{\mathrm{PP}}$.

<details class="exercise__hint"><summary>Hint</summary>

Show both inclusions. PP within P^#P is Exercise 6. For the other direction, show that a #P query can be answered by binary search with PP queries.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\mathrm{PP} \subseteq \mathrm{P}^{\#P}$ was shown in Exercise 6, so $\mathrm{P}^{\mathrm{PP}} \subseteq \mathrm{P}^{\mathrm{P}^{\#P}} = \mathrm{P}^{\#P}$ (a P machine with a PP oracle is simulated by a P machine with a #P oracle by replacing each PP query with the #P-based simulation).

For $\mathrm{P}^{\#P} \subseteq \mathrm{P}^{\mathrm{PP}}$: to compute $f(x) \in \#P$, use binary search with PP queries. The value $f(x)$ is between 0 and $N = 2^{p(|x|)}$. Query the PP oracle: "is $f(x) \ge m$?" This is a PP question since $f(x) \ge m \iff$ the fraction of accepting paths is at least $m/N$, which a PP machine can decide. Binary search over $[0, N]$ uses $O(p(|x|))$ PP queries, giving $\mathrm{P}^{\#P} \subseteq \mathrm{P}^{\mathrm{PP}}$. Rubric: full credit for both inclusions.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Let $\mathrm{MAJ}$-SAT denote the language of Boolean formulas for which more than half of all assignments are satisfying. Show that $\mathrm{MAJ}$-SAT is PP-complete.

<details class="exercise__hint"><summary>Hint</summary>

Reduce an arbitrary PP computation to MAJ-SAT by encoding random strings as variable assignments.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Membership: a PP machine on input $\phi$ with $n$ variables has $2^n$ possible assignments. The question "are more than half satisfying?" is decided by a PP machine that picks a random assignment and checks it, accepting iff the assignment satisfies $\phi$. The acceptance probability exceeds 1/2 iff more than half the assignments satisfy $\phi$.

Hardness: let $L \in \mathrm{PP}$ via machine $M$ using $p(n)$ random bits. Construct a Boolean formula $\psi(x, r)$ that is true iff $M$ accepts $x$ using random string $r$. The number of satisfying assignments of $\psi$ (treating $x$ as fixed constants) equals the number of accepting random strings. So $x \in L$ iff more than half of the $2^{p(|x|)}$ assignments to $r$ satisfy $\psi$, i.e., $\psi$ is in MAJ-SAT. The reduction is polynomial-time. Rubric: full credit for both directions.

</details>

</aside>

## Advanced results [Master]

The theory of #P extends beyond the foundational theorems into structural complexity, algebraic methods, and the surprising power of approximate counting.

**Theorem 1 (Valiant's theorem, detailed).** The reduction from #3SAT to the permanent constructs a matrix $A$ of size $O(n + m)$ for a formula with $n$ variables and $m$ clauses. Each variable gadget is a subgraph with two cycle-cover modes: one corresponding to "true" and one to "false." Clause gadgets connect to the variable gadgets through edges that are present only when the corresponding literal satisfies the clause. The permanent of $A$ counts cycle covers of the resulting graph, which are in bijection with pairs (variable assignment, clause satisfaction pattern). A carefully designed weighting scheme ensures that only satisfying assignments contribute non-zero weight, and each contributes $4^{3m}$. Dividing by $4^{3m}$ gives the exact count of satisfying assignments [ref: TODO_REF Arora-Barak §17.2].

**Theorem 2 (PP-completeness of MAJ-SAT).** The language MAJ-SAT $= \{\phi : \text{more than half of all assignments satisfy } \phi\}$ is PP-complete. More generally, $\mathrm{PP}$ captures "threshold" decision problems: does a count exceed a specified fraction? The PP hierarchy collapses ($\mathrm{PP}^{\mathrm{PP}} = \mathrm{PP}$), unlike the polynomial hierarchy which is not known to collapse. This makes PP structurally simpler than PH despite being computationally powerful [ref: TODO_REF Arora-Barak §17.3].

**Theorem 3 (Toda's theorem, proof outline).** The proof proceeds in three steps. Step 1: the Valiant-Vazirani isolation lemma converts a SAT instance $\phi$ into a formula $\psi$ such that if $\phi$ is unsatisfiable then $\psi$ is unsatisfiable, and if $\phi$ is satisfiable then $\psi$ has exactly one satisfying assignment with probability $\Omega(1/n)$. Step 2: this converts existential quantification into parity ($\oplus P$), since "exactly one satisfying assignment" is detected by parity. Step 3: iterate this conversion across all quantifier levels, using amplification to boost the success probability at each level. The final result is that any $\Sigma_k$ predicate reduces to a $\mathrm{BP} \cdot \oplus \mathrm{P}$ predicate, and $\mathrm{BP} \cdot \oplus \mathrm{P} \subseteq \mathrm{P}^{\#P}$ by computing the relevant counts exactly [ref: TODO_REF Arora-Barak §17.3].

**Theorem 4 (permanent for matrices with non-negative entries).** While the permanent of general 0-1 matrices is #P-complete, the permanent of matrices with non-negative entries can be approximated efficiently. The Jerrum-Sinclair-Vigoda algorithm (2004) gives an FPRAS (fully polynomial-time randomised approximation scheme) for the permanent of matrices with non-negative entries. This is a rare example of a #P-complete problem that admits efficient approximation, connecting to [47.05.04].

**Theorem 5 (algebraic characterisation).** The permanent can be expressed as a multivariate polynomial: $\mathrm{perm}(A) = \sum_\sigma \prod_i x_{i,\sigma(i)}$ where the matrix entries are variables. This polynomial has degree $n$ in $n^2$ variables. The theory of arithmetic circuits studies the complexity of computing such polynomials, and the permanent plays a role analogous to SAT in algebraic complexity. The VP versus VNP question (the algebraic analogue of P versus NP) asks whether the permanent polynomial can be computed by polynomial-size arithmetic circuits [ref: TODO_REF Arora-Barak §17.4].

**Synthesis.** The foundational reason #P is so powerful is that a single count encodes exponentially many bits of information about the structure of a computation, and this is exactly what Toda's theorem exploits: the entire polynomial hierarchy, built from unbounded quantifier alternation, collapses into a single counting query. The Valiant-Vazirani isolation lemma builds toward the PCP theorem [47.03.05] and appears again in the design of interactive proof systems [47.03.04] where isolating witnesses is a key technique. The central insight is that parity plus randomisation can simulate existential quantification, and putting these together through iteration, any fixed level of the hierarchy is captured. The bridge is that the permanent, as the simplest #P-complete problem, connects combinatorial counting to algebra, and this connection generalises through the theory of arithmetic circuits to the VP versus VNP question, which is dual to the P versus NP question in the algebraic setting. The #P framework also appears in the derandomisation programme [47.05.05], where approximate counting via random walks provides efficient algorithms for special cases.

## Full proof set [Master]

**Proposition 1 (#SAT is #P-complete).** *The function $\#\mathrm{SAT}(\phi) = |\{\text{satisfying assignments of } \phi\}|$ is #P-complete under parsimonious reductions.*

*Proof.* Membership: a nondeterministic polynomial-time machine on input $\phi$ with $n$ variables guesses an assignment $a \in \{0,1\}^n$ and evaluates $\phi(a)$. It accepts iff $\phi(a)$ is true. The number of accepting paths equals the number of satisfying assignments.

Completeness: let $f \in \#P$ via machine $M$ with $p(n)$ nondeterministic choices. By the Cook-Levin construction [47.02.02], there is a formula $\phi_{M,x}$ such that satisfying assignments of $\phi_{M,x}$ are in bijection with accepting paths of $M$ on $x$. The bijection is parsimonious (preserves counts). Hence $f(x) = \#\mathrm{SAT}(\phi_{M,x})$. $\square$

**Proposition 2 (permanent is in #P).** *The permanent of an $n \times n$ matrix $A$ with non-negative integer entries is in #P.*

*Proof.* A nondeterministic machine guesses a permutation $\sigma \in S_n$ and computes $\prod_{i=1}^n A_{i,\sigma(i)}$. It accepts with multiplicity equal to this product (achieved by branching). The total number of accepting paths is $\sum_\sigma \prod_i A_{i,\sigma(i)} = \mathrm{perm}(A)$. $\square$

**Proposition 3 (PP within P^#P).** *$\mathrm{PP} \subseteq \mathrm{P}^{\#P}$.*

*Proof.* Let $L \in \mathrm{PP}$ via machine $M$ using $p(n)$ random bits. The number $a(x)$ of accepting random strings is a #P function. Query the #P oracle for $a(x)$ and compare to $2^{p(|x|)-1}$. If $a(x) > 2^{p(|x|)-1}$, accept; else reject. This uses one #P query and polynomial post-processing. $\square$

**Proposition 4 (PH within BP . $\oplus$ . P).** *For every $k$, $\Sigma_k^{\mathrm{P}} \subseteq \mathrm{BP} \cdot \oplus \cdot \mathrm{P}$.*

*Proof sketch.* By induction on $k$. Base case $k = 1$: $\Sigma_1 = \mathrm{NP}$. Given a formula $\phi$, apply Valiant-Vazirani to obtain $\psi$ with the isolation property. Then $\phi \in \mathrm{SAT}$ iff with probability $\ge 1/n$ over the random hash function, $\psi$ has exactly one satisfying assignment (detected by parity). This gives $\mathrm{NP} \subseteq \mathrm{BP} \cdot \oplus \cdot \mathrm{P}$.

Inductive step: $\Sigma_k = \exists \forall \cdots$. Apply isolation to the outermost existential quantifier, converting it to BP and parity. The inner quantifiers are handled by the induction hypothesis. Amplification boosts the success probability at each level. $\square$

**Proposition 5 (BP . $\oplus$ . P within P^#P).** *$\mathrm{BP} \cdot \oplus \cdot \mathrm{P} \subseteq \mathrm{P}^{\#P}$.*

*Proof.* A language $L \in \mathrm{BP} \cdot \oplus \cdot \mathrm{P}$ has a polynomial-time predicate $R$ such that for $x \in L$, $\Pr_y[|\{z : R(x,y,z)\}| \text{ is odd}] \ge 2/3$, and for $x \notin L$ the probability is $\le 1/3$. The quantity $\Pr_y[\text{odd parity}]$ is computable from two #P queries: (a) count pairs $(y, z)$ with $R(x,y,z)$, and (b) count pairs $(y, z_1, z_2)$ with $R(x,y,z_1) \wedge R(x,y,z_2)$, which lets us extract the parity information. The comparison to the threshold $1/2$ is a polynomial-time post-processing step. $\square$

## Connections [Master]

- NP-completeness [47.02.02] provides the Cook-Levin reduction that underlies #P-completeness. The parsimonious version of Cook-Levin preserves witness counts, making #3SAT #P-complete. The entire #P theory is the counting extension of the NP-completeness framework.

- Probability spaces and Kolmogorov axioms [37.01.01] provide the measure-theoretic foundation for PP and the probabilistic analysis in Toda's theorem. The threshold behaviour of PP ("accept if probability exceeds 1/2") is a measure comparison.

- Interactive proofs [47.03.04] connect to #P through the LFKN protocol and the IP = PSPACE proof. The counting techniques developed for #P (especially the sum-check protocol) are central to the interactive proof machinery.

- FPRAS for counting [47.05.04] studies efficient approximation algorithms for #P-complete problems. The permanent of non-negative matrices admits an FPRAS, showing that approximate counting can be easier than exact counting.

## Historical & philosophical context [Master]

Leslie Valiant proved the #P-completeness of the permanent in 1979, in his paper "The Complexity of Computing the Permanent" published in *Theoretical Computer Science*. The result was striking because the permanent is a simple algebraic expression — just the determinant without signs — yet it captures the full difficulty of counting. The construction introduced the cycle-cover method for encoding Boolean constraints into matrix entries, a technique that has become standard in algebraic complexity [ref: TODO_REF Arora-Barak §17.2].

Seinosuke Toda proved his theorem in 1991, in "PP is Harder than the Polynomial-Time Hierarchy" (SIAM Journal on Computing). The result was unexpected: it showed that counting is strictly more powerful than any fixed level of quantifier alternation, giving the first tight connection between counting classes and the polynomial hierarchy. The proof introduced the use of the Valiant-Vazirani isolation lemma as a tool for converting quantifiers into parity constraints.

The development of FPRAS algorithms for counting, culminating in the Jerrum-Sinclair-Vigoda algorithm for the permanent (2004), showed that approximate counting is sometimes feasible even when exact counting is #P-complete. This opened the field of approximate counting, connecting complexity theory to practical algorithm design.

## Bibliography [Master]

```bibtex
@article{valiant1979complexity,
  author  = {Valiant, Leslie G.},
  title   = {The complexity of computing the permanent},
  journal = {Theoretical Computer Science},
  volume  = {8},
  number  = {2},
  year    = {1979},
  pages   = {189--201}
}

@article{toda1991pp,
  author  = {Toda, Seinosuke},
  title   = {{PP} is harder than the polynomial-time hierarchy},
  journal = {SIAM Journal on Computing},
  volume  = {20},
  number  = {5},
  year    = {1991},
  pages   = {865--877}
}

@article{valiant1986np,
  author  = {Valiant, Leslie G. and Vazirani, Vijay V.},
  title   = {{NP} is as easy as detecting unique solutions},
  journal = {Theoretical Computer Science},
  volume  = {47},
  year    = {1986},
  pages   = {85--93}
}

@article{jerrum2004polynomial,
  author  = {Jerrum, Mark and Sinclair, Alistair and Vigoda, Eric},
  title   = {A polynomial-time approximation algorithm for the permanent of a matrix with nonnegative entries},
  journal = {Journal of the ACM},
  volume  = {51},
  number  = {4},
  year    = {2004},
  pages   = {671--697}
}

@book{arora2009computational,
  author    = {Arora, Sanjeev and Barak, Boaz},
  title     = {Computational Complexity: A Modern Approach},
  publisher = {Cambridge University Press},
  year      = {2009}
}
```
