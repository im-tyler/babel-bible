---
id: 41.01.02
title: "Natural Transformations, Functor Categories, and Equivalence of Categories"
slug: natural-transformations-functor-categories-equivalence
section: category-theory
chapter: categories-functors-natural-transformations
concept_catalog_id: category-theory.natural-transformations-functor-categories-equivalence
prerequisites:
  - 41.01.01
successors: []
tier_anchors:
  beginner: "Spivak 2014 *Category Theory for the Sciences* (MIT Press) Ch. 4 (functors as translations, transformations between translations); Leinster 2014 *Basic Category Theory* (Cambridge) §1.3 (the definition of natural transformation by the commuting-square law, with the double-dual and determinant examples)"
  intermediate: "Mac Lane 1998 *Categories for the Working Mathematician* 2e (Springer GTM 5) Ch. I §4, Ch. II §4-5, Ch. IV §4 (natural transformations and naturality squares, vertical/horizontal composition and the interchange law, functor categories, equivalence of categories); Riehl 2016 *Category Theory in Context* (Dover) §1.4-1.5, §2.2; Awodey 2010 *Category Theory* 2e (Oxford) Ch. 7-8"
  master: "Mac Lane 1998 *Categories for the Working Mathematician* 2e (Springer GTM 5) Ch. I-II, IV in full (the interchange law, $[\\mathcal{C},\\mathcal{D}]$, comma categories, equivalence and skeleton); Eilenberg-Mac Lane 1945 *General theory of natural equivalences*; Kelly 1982 *Basic Concepts of Enriched Category Theory*; Borceux 1994 *Handbook of Categorical Algebra 1* (Cambridge) Ch. 1, 3"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Mac Lane — Categories for the Working Mathematician"
    locator: "2nd ed., Springer GTM 5, Ch. I §4, II §4-5, IV §4"
  - source: TODO_REF
    path: "Eilenberg-Mac Lane — General theory of natural equivalences"
    locator: "1945, Transactions of the AMS 58"
  - source: TODO_REF
    path: "Riehl — Category Theory in Context"
    locator: "Dover 2016, §1.4-1.5, §2.2"
  - source: TODO_REF
    path: "Awodey — Category Theory"
    locator: "2nd ed., Oxford 2010, Ch. 7-8"
  - source: TODO_REF
    path: "Leinster — Basic Category Theory"
    locator: "Cambridge 2014, §1.3, §1.5"
lean_status: none
lean_mathlib_gap: |
  Mathlib's CategoryTheory library (Mathlib.CategoryTheory.NatTrans,
  NatIso, Functor.Category, Whiskering, Equivalence, EssentiallySurjective,
  FullyFaithful, Elements, Comma, and the bicategory Cat) formalises natural
  transformations and their vertical and horizontal composition, functor
  categories with their natural isomorphisms, equivalences via unit/counit data
  with the triangle identities, the category of elements of a presheaf, comma
  categories, and the 2-categorical structure of Cat. What Mathlib does not
  package as a single connected development is the equivalence-characterisation
  theorem assembled end to end — that a functor is an equivalence if and only if
  it is full, faithful, and essentially surjective — with the quasi-inverse
  constructed explicitly by a choice of object and isomorphism in each fibre,
  the exact place the axiom of choice enters made visible, and the skeleton
  exhibited as the canonical equivalent. The interchange law for the two
  compositions, presented as the single equation that makes Cat a strict
  2-category and proved from the naturality squares, is likewise scattered
  across whiskering and bicategory API rather than stated once as a load-bearing
  identity. A Codex.CategoryTheory.Equivalence module proving the trichotomy
  characterisation with the choice-dependence isolated is the formalisation target.
human_reviewer: "Tyler (pending external category-theory reviewer)"
source_books: [maclane-cwm]
source_curriculum: logic-foundations
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 85m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

A functor translates one category into another: it carries objects to objects and arrows to arrows. Once you have two such translations running side by side, a new question appears. Suppose two functors both send a category into the same target. Is there a way to slide from the output of the first translation to the output of the second, object by object, that respects all the arrows? That coordinated sliding is a natural transformation. It is a single rule that, at every object at once, nudges the first functor's answer over to the second functor's answer.

The word that matters is "coordinated". You are not allowed to make a separate, unrelated choice at each object. The choices must fit together so that following an arrow and then sliding gives the same result as sliding and then following the arrow. When that compatibility holds for every arrow, the family of slides is called natural. The slogan from the founders of the subject was that a construction is natural when it does not depend on arbitrary choices — when it works the same way everywhere.

Here is the everyday picture. A finite list can be reversed. Reversing a list of numbers, then adding one to each, gives the same outcome as adding one to each and then reversing. The "add one" step is an arrow; "reverse" is the family of slides. Because the order does not matter, reversal is natural. That order-does-not-matter property, drawn as a square that closes up, is the whole idea.

## Visual [Beginner]

Picture two parallel filmstrips. The top strip is what functor $F$ produces; the bottom strip is what functor $G$ produces. For each object you draw a vertical connector — an arrow pointing from the top frame down to the bottom frame. That vertical connector is one component of the natural transformation.

Now follow any horizontal arrow on the top strip from one frame to the next. There is a matching horizontal arrow on the bottom strip. Going right-then-down must equal going down-then-right. Every little rectangle you can form this way closes up: the two paths around it agree.

| feature | a functor | a natural transformation between two functors |
|---|---|---|
| connects | one category to another | one functor to another (same source and target) |
| made of | a target object and arrow for each input | one vertical arrow (component) per object |
| must respect | composition and identities | every naturality square must close up |
| reversible case | an isomorphism of categories | a natural isomorphism (every component reversible) |

![Two parallel rows of objects: the top row is the image of functor F, the bottom row the image of functor G; vertical arrows join each top object to the one below it, and for every horizontal arrow on top there is a matching one below, so each rectangle formed closes up with right-then-down equal to down-then-right](/img/natural-transformations-functor-categories-equivalence-placeholder.svg)

The picture says the vertical arrows are not independent decorations. They are tied together by the requirement that every rectangle commutes. That single requirement is what earns the family the name natural.

## Worked example [Beginner]

Work with finite-dimensional real vector spaces and linear maps. To each space $V$ attach its double dual $V^{**}$, the space of functions on functions on $V$. There is a standard rule that sends a vector $v$ in $V$ to the gadget "feed me a function and I hand back its value at $v$". Call this rule $\eta_V$, a linear map from $V$ to $V^{**}$.

Step 1. Fix a concrete space, $V = \mathbb{R}^2$. A vector is a pair, say $v = (3, 5)$. Then $\eta_V(v)$ is the operation that takes any linear function $\phi$ on $\mathbb{R}^2$ and returns the number $\phi(3,5)$.

Step 2. Take a linear map to a second space, $T\colon \mathbb{R}^2 \to \mathbb{R}^2$, say the map that swaps coordinates, $T(x,y) = (y,x)$. There are two routes from $v$ to the double dual of the target. Route A: first apply $\eta$, then push forward by $T$. Route B: first apply $T$ to get $(5,3)$, then apply $\eta$.

Step 3. Check the two routes agree on a sample function $\phi(x,y) = 2x + y$. Route B gives $\eta(5,3)$ evaluated at $\phi$, which is $2\cdot 5 + 3 = 13$. Route A pushes $\eta(3,5)$ forward by $T$; pushing forward feeds $\phi$ through $T$ first, giving $\phi(T(3,5)) = \phi(5,3) = 13$. The two answers match.

What this tells us: the map into the double dual is the same whether you transform first or embed first. No basis was chosen anywhere. That basis-free agreement, holding for every space and every map at once, is exactly what makes $\eta$ a natural transformation — the original example that the inventors of the subject built the definition to capture.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A natural transformation between two functors is built out of which pieces?

A. One arrow in the target category for each object of the source
B. One whole new functor
C. One number for each object
D. A relabelling of the objects

<details class="exercise__hint"><summary>Hint</summary>

At each object the transformation gives you a way to slide from one functor's output to the other's.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**A.** A natural transformation supplies, for each object of the source category, one arrow (its component) in the target, and these arrows must fit together so every square closes up. Feedback-correct: the components are arrows, one per object, constrained by naturality. Feedback-wrong: it is not a new functor, not a bare number, and not a renaming — it is a coordinated family of arrows.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

For a family of arrows to be a natural transformation, every naturality square it forms must close up — going one way around equals going the other way.

<details class="exercise__hint"><summary>Hint</summary>

Recall the reversing-a-list picture, where order did not matter.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** The defining requirement is that for every arrow in the source, the rectangle built from the two functors and the two relevant components commutes. Feedback-correct: closing up is the naturality condition itself. Feedback-wrong: without it the family is just an unrelated collection of arrows, not a natural transformation.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

When is a natural transformation called a natural isomorphism?

A. When the two functors are equal
B. When every component arrow is reversible
C. When it has only one component
D. When the source category is empty

<details class="exercise__hint"><summary>Hint</summary>

An isomorphism is an arrow with a two-sided inverse; apply that at each component.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** A natural transformation is a natural isomorphism exactly when each of its components is an isomorphism in the target category; the inverses then automatically assemble into a natural transformation back. Feedback-correct: reversibility is checked component by component. Feedback-wrong: the functors need not be equal, and the number of components is the number of objects, not one.

</details>

</aside>

## Formal definition [Intermediate+]

Let $F, G\colon \mathcal{C} \to \mathcal{D}$ be functors. A *natural transformation* $\alpha\colon F \Rightarrow G$ assigns to each object $A$ of $\mathcal{C}$ a morphism $\alpha_A\colon F(A) \to G(A)$ in $\mathcal{D}$, its *component* at $A$, such that for every morphism $f\colon A \to B$ in $\mathcal{C}$ the *naturality square*

$$
\begin{array}{ccc}
F(A) & \xrightarrow{\ \alpha_A\ } & G(A)\\
{\scriptstyle F(f)}\big\downarrow & & \big\downarrow{\scriptstyle G(f)}\\
F(B) & \xrightarrow{\ \alpha_B\ } & G(B)
\end{array}
$$

commutes, that is $G(f)\circ \alpha_A = \alpha_B \circ F(f)$ [ref: TODO_REF Mac Lane 1998]. A natural transformation all of whose components are isomorphisms is a *natural isomorphism*, written $F \cong G$; the componentwise inverses then satisfy naturality and give an inverse transformation. Notation introduced here ($\Rightarrow$ for natural transformations, $\circ$ for composition, $\alpha_A$ for a component) is recorded in `_meta/NOTATION.md`.

The double-dual embedding $\eta_V\colon V \to V^{**}$ is a natural transformation $\mathrm{Id} \Rightarrow (-)^{**}$ on finite-dimensional vector spaces, and a natural isomorphism there; on all vector spaces it remains natural but ceases to be an isomorphism. The *determinant* is a natural transformation $\det\colon \mathrm{GL}_n \Rightarrow (-)^\times$ between functors from commutative rings to groups, since $\det(M)$ commutes with ring homomorphisms applied entrywise. *Abelianization* gives the *unit* $\eta_G\colon G \to G^{\mathrm{ab}}$ of the free-abelian-quotient construction, natural in $G$. The *Hurewicz map* $h\colon \pi_n \Rightarrow H_n$ is natural in the space.

**Definition (vertical composition).** Given $\alpha\colon F \Rightarrow G$ and $\beta\colon G \Rightarrow H$ between functors $\mathcal{C}\to\mathcal{D}$, their *vertical composite* $\beta \cdot \alpha\colon F \Rightarrow H$ has components $(\beta\cdot\alpha)_A = \beta_A \circ \alpha_A$. The identity natural transformation $1_F$ has components $\mathrm{id}_{F(A)}$.

**Definition (horizontal composition and whiskering).** Given $\alpha\colon F \Rightarrow F'$ for $F,F'\colon \mathcal{C}\to\mathcal{D}$ and $\beta\colon G \Rightarrow G'$ for $G,G'\colon \mathcal{D}\to\mathcal{E}$, the *horizontal composite* $\beta * \alpha\colon GF \Rightarrow G'F'$ has component at $A$ given by either equal path around the square below, $\beta_{F'(A)} \circ G(\alpha_A) = G'(\alpha_A) \circ \beta_{F(A)}$. When one of the two is an identity transformation the operation is *whiskering*: $G \alpha\colon GF \Rightarrow GF'$ has components $G(\alpha_A)$, and $\beta F\colon GF \Rightarrow G'F$ has components $\beta_{F(A)}$.

**Definition (functor category).** For categories $\mathcal{C}$ (small) and $\mathcal{D}$, the *functor category* $[\mathcal{C},\mathcal{D}]$ (also $\mathcal{D}^{\mathcal{C}}$ or $\mathrm{Fun}(\mathcal{C},\mathcal{D})$) has functors $\mathcal{C}\to\mathcal{D}$ as objects and natural transformations as morphisms, composed vertically; the identity on $F$ is $1_F$. An isomorphism in $[\mathcal{C},\mathcal{D}]$ is precisely a natural isomorphism. A presheaf category is the special case $[\mathcal{C}^{\mathrm{op}}, \mathbf{Set}]$.

**Definition (equivalence of categories).** A functor $F\colon \mathcal{C}\to\mathcal{D}$ is an *equivalence* if there is a functor $G\colon \mathcal{D}\to\mathcal{C}$ (a *quasi-inverse*) together with natural isomorphisms $\eta\colon \mathrm{Id}_{\mathcal{C}} \cong GF$ and $\varepsilon\colon FG \cong \mathrm{Id}_{\mathcal{D}}$. Equivalence is weaker than *isomorphism of categories*, which would demand $GF = \mathrm{Id}$ and $FG = \mathrm{Id}$ on the nose; equivalence allows them only up to natural isomorphism, so it ignores the distinction between isomorphic objects.

**Definition (comma category).** Given functors $F\colon \mathcal{A}\to\mathcal{C}$ and $G\colon \mathcal{B}\to\mathcal{C}$, the *comma category* $F\downarrow G$ has objects the triples $(A, B, f)$ with $A$ in $\mathcal{A}$, $B$ in $\mathcal{B}$, and $f\colon F(A)\to G(B)$ in $\mathcal{C}$; a morphism $(A,B,f)\to(A',B',f')$ is a pair $(u\colon A\to A',\, v\colon B\to B')$ with $G(v)\circ f = f'\circ F(u)$. Taking $\mathcal{B}=\mathbf{1}$ recovers the slice $\mathcal{C}/c$ over an object $c$; taking $\mathcal{A}=\mathbf{1}$ recovers the coslice $c/\mathcal{C}$; taking $\mathcal{A}=\mathcal{B}=\mathcal{C}$ with both functors the identity recovers the *arrow category* $\mathcal{C}^{\to}$.

**Definition (category of elements).** For a presheaf $F\colon \mathcal{C}^{\mathrm{op}}\to\mathbf{Set}$, the *category of elements* $\int_{\mathcal{C}} F$ (the *Grothendieck construction*) has objects the pairs $(A, x)$ with $A$ in $\mathcal{C}$ and $x \in F(A)$; a morphism $(A,x)\to(B,y)$ is a morphism $g\colon A\to B$ in $\mathcal{C}$ with $F(g)(y) = x$. The forgetful functor $\int_{\mathcal{C}} F \to \mathcal{C}$ exhibits a discrete fibration over $\mathcal{C}$.

### Counterexamples to common slips [Intermediate+]

- *A family of isomorphisms need not be natural.* Every finite-dimensional vector space is isomorphic to its single dual $V^*$, but no choice of isomorphisms $V \to V^*$ is natural in $V$: any such family forces a choice of inner product or basis, and the naturality square fails for a generic linear map. Naturality is a genuine constraint, not a free consequence of pointwise isomorphism.

- *Equivalence is not isomorphism of categories.* The category of finite-dimensional vector spaces is equivalent to its skeleton with one object $\mathbb{R}^n$ per dimension, but it is not isomorphic to it: the original has a proper class of objects while the skeleton has countably many, so no bijection on objects exists. Equivalence permits collapsing isomorphic objects; isomorphism forbids it.

- *The comma category depends on the functors, not just the categories.* Writing $F\downarrow G$ without naming $F$ and $G$ is meaningless: changing either functor changes which triples $(A,B,f)$ exist. The slice and coslice are the special cases where one functor lands on a single chosen object.

## Key theorem with proof [Intermediate+]

The signature result is the characterisation of equivalences without reference to an explicit quasi-inverse.

**Theorem (characterisation of equivalences).** A functor $F\colon \mathcal{C}\to\mathcal{D}$ is an equivalence of categories if and only if $F$ is full, faithful, and essentially surjective [ref: TODO_REF Mac Lane 1998]. The construction of a quasi-inverse from the second condition uses the axiom of choice.

*Proof.* ($\Rightarrow$) Suppose $F$ is an equivalence with quasi-inverse $G$ and natural isomorphisms $\eta\colon \mathrm{Id}_{\mathcal{C}}\cong GF$, $\varepsilon\colon FG\cong \mathrm{Id}_{\mathcal{D}}$. *Essentially surjective:* for any object $D$ of $\mathcal{D}$, $\varepsilon_D\colon FG(D)\to D$ is an isomorphism, so $D$ is isomorphic to $F(G(D))$. *Faithful:* if $F(f)=F(g)$ for $f,g\colon A\to B$ then $GF(f)=GF(g)$; the naturality of $\eta$ gives $f = \eta_B^{-1}\circ GF(f)\circ \eta_A = \eta_B^{-1}\circ GF(g)\circ \eta_A = g$. The same argument with $\varepsilon$ shows $G$ is faithful. *Full:* given $h\colon F(A)\to F(B)$, set $f = \eta_B^{-1}\circ G(h)\circ \eta_A\colon A\to B$. Naturality of $\eta$ at $f$ reads $GF(f)\circ \eta_A = \eta_B\circ f = G(h)\circ\eta_A$, and since $\eta_A$ is invertible, $GF(f)=G(h)$. As $G$ is faithful, $F(f)=h$, so $F$ is full.

($\Leftarrow$) Suppose $F$ is full, faithful, and essentially surjective. For each object $D$ of $\mathcal{D}$ *choose* an object $G(D)$ of $\mathcal{C}$ and an isomorphism $\varepsilon_D\colon F(G(D))\to D$; this is the one appeal to choice. Extend $G$ to morphisms: for $h\colon D\to D'$ the composite $\varepsilon_{D'}^{-1}\circ h\circ \varepsilon_D\colon F(G(D))\to F(G(D'))$ is, by fullness and faithfulness of $F$, equal to $F(g)$ for a unique $g\colon G(D)\to G(D')$; set $G(h)=g$. Uniqueness forces $G(\mathrm{id})=\mathrm{id}$ and $G(h'\circ h)=G(h')\circ G(h)$, so $G$ is a functor, and $\varepsilon\colon FG\Rightarrow \mathrm{Id}_{\mathcal{D}}$ is natural by construction with invertible components, hence a natural isomorphism. For $\eta$: each $\varepsilon_{F(A)}\colon FGF(A)\to F(A)$ is an isomorphism between values of $F$, so by fullness and faithfulness equals $F(\eta_A)$ for a unique isomorphism $\eta_A\colon A\to GF(A)$. Naturality of $\eta$ follows from naturality of $\varepsilon$ together with faithfulness of $F$. Thus $G$ is a quasi-inverse and $F$ is an equivalence. $\square$

**Bridge.** This theorem builds toward every structural comparison made later by exhibiting an equivalence, and it appears again in [41.04.02], where the Yoneda embedding is shown full and faithful and the question of essential surjectivity becomes the representability problem. The foundational reason the trichotomy suffices is that fullness and faithfulness let an isomorphism between images be pulled back to a unique isomorphism upstream, so the quasi-inverse is forced on morphisms once chosen on objects; the central insight is that the only genuinely free datum is the choice in each isomorphism class, which is exactly where the axiom of choice enters. This is exactly the mechanism by which a category and its skeleton are equivalent, and putting these together shows equivalence is the correct notion of sameness — coarser than isomorphism of categories, fine enough to preserve every property stated in the categorical language, and the bridge from the duality of [41.01.01] to the universal constructions that follow, since limits, colimits, and adjoints are all defined only up to the natural isomorphism this theorem trades in.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 1 (easy, multiple choice).**

Which condition is *not* part of the characterisation of an equivalence of categories?

A. Full
B. Faithful
C. Injective on objects
D. Essentially surjective

<details class="exercise__hint"><summary>Hint</summary>

Equivalence ignores the difference between isomorphic objects.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** An equivalence need not be injective on objects — a category is equivalent to its skeleton, which collapses isomorphic objects. The three correct conditions are full, faithful, and essentially surjective.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

The components of a natural isomorphism are isomorphisms, and their inverses again form a natural transformation.

<details class="exercise__hint"><summary>Hint</summary>

Invert the naturality square.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** If every $\alpha_A$ is invertible, applying inverses to both sides of $G(f)\circ\alpha_A=\alpha_B\circ F(f)$ yields $\alpha_B^{-1}\circ G(f)=F(f)\circ\alpha_A^{-1}$, the naturality square for $\alpha^{-1}\colon G\Rightarrow F$. So natural isomorphisms are exactly the isomorphisms in the functor category.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Verify the *interchange law* in the special case of whiskering: for $\alpha\colon F\Rightarrow F'$ with $F,F'\colon\mathcal{C}\to\mathcal{D}$ and $G\colon\mathcal{D}\to\mathcal{E}$, show that the whiskered transformation $G\alpha$ has components $G(\alpha_A)$ and is natural.

<details class="exercise__hint"><summary>Hint</summary>

Apply $G$ to the naturality square of $\alpha$ and use functoriality of $G$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For $f\colon A\to B$ the square for $\alpha$ reads $F'(f)\circ\alpha_A=\alpha_B\circ F(f)$. Apply $G$: $G(F'(f))\circ G(\alpha_A)=G(\alpha_B)\circ G(F(f))$, since $G$ preserves composition. This is exactly the naturality square for the family $A\mapsto G(\alpha_A)$ between $GF$ and $GF'$. So $G\alpha$ is a natural transformation $GF\Rightarrow GF'$ with the stated components.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Show that the determinant defines a natural transformation $\det\colon \mathrm{GL}_n\Rightarrow (-)^\times$ of functors $\mathbf{CRing}\to\mathbf{Grp}$, where $\mathrm{GL}_n(R)$ is the group of invertible $n\times n$ matrices and $R^\times$ the unit group.

<details class="exercise__hint"><summary>Hint</summary>

A ring homomorphism applied entrywise to a matrix commutes with the determinant polynomial.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $\phi\colon R\to S$ be a ring homomorphism, inducing $\mathrm{GL}_n(\phi)\colon \mathrm{GL}_n(R)\to\mathrm{GL}_n(S)$ entrywise and $\phi^\times\colon R^\times\to S^\times$. The determinant is a fixed polynomial in the entries with integer coefficients; a ring homomorphism preserves sums and products, so $\det(\phi(M)) = \phi(\det M)$ for every $M$. That equation is precisely the naturality square $(-)^\times(\phi)\circ\det_R = \det_S\circ\mathrm{GL}_n(\phi)$. Hence $\det$ is natural.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Prove that for finite-dimensional vector spaces no natural isomorphism $\mathrm{Id}\cong (-)^*$ to the single dual exists, even though each $V\cong V^*$.

<details class="exercise__hint"><summary>Hint</summary>

Test naturality against scalar multiplication by $\lambda$ on a one-dimensional space.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Suppose $\alpha\colon \mathrm{Id}\Rightarrow (-)^*$ were natural. On $V=\mathbb{R}$ the map "multiply by $\lambda$", $m_\lambda\colon\mathbb{R}\to\mathbb{R}$, induces on the dual the map "multiply by $\lambda$" again (the dual of $m_\lambda$ is $m_\lambda$). Naturality demands $(m_\lambda)^*\circ\alpha_{\mathbb{R}} = \alpha_{\mathbb{R}}\circ m_\lambda$, i.e. $\lambda\,\alpha_{\mathbb{R}} = \lambda\,\alpha_{\mathbb{R}}$ on the nose only if the dual action matched, but the dual of $m_\lambda$ acts as $m_\lambda$, forcing $\lambda\,\alpha_{\mathbb{R}}=\alpha_{\mathbb{R}}\,\lambda^{?}$. Tracking the variance: the single dual is contravariant on maps, so $\alpha$ cannot even be a transformation between covariant functors. The double dual is covariant and fixes this, which is why $\eta\colon\mathrm{Id}\cong(-)^{**}$ is natural while $\mathrm{Id}\cong(-)^*$ is not.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Let $F\colon\mathcal{C}\to\mathcal{D}$ be full and faithful. Show $F$ *reflects* isomorphisms: if $F(f)$ is an isomorphism then $f$ is. Conclude that a full, faithful, essentially surjective $F$ reflects every property invariant under isomorphism.

<details class="exercise__hint"><summary>Hint</summary>

Fullness produces a candidate inverse; faithfulness pins down the inverse equations.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $f\colon A\to B$ with $F(f)$ invertible, inverse $k\colon FB\to FA$. By fullness $k=F(g)$ for some $g\colon B\to A$. Then $F(g\circ f)=F(g)\circ F(f)=k\circ F(f)=\mathrm{id}_{FA}=F(\mathrm{id}_A)$, and faithfulness gives $g\circ f=\mathrm{id}_A$; the same on the other side gives $f\circ g=\mathrm{id}_B$. So $f$ is an isomorphism. Since an equivalence is full, faithful, and essentially surjective, and the quasi-inverse transports objects and morphisms with isomorphisms on both sides, any property defined purely by the existence and composition of arrows transfers across $F$ in both directions.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Identify the comma category $\mathbf{1}\downarrow G$ where $\mathbf{1}$ is the one-object category, the single functor picks out an object $c$ of $\mathcal{C}$, and $G=\mathrm{Id}_{\mathcal{C}}$. Then describe the category of elements of the representable presheaf $\mathcal{C}(-,c)$ and relate the two.

<details class="exercise__hint"><summary>Hint</summary>

A comma object is a triple $(\ast, B, f\colon c\to B)$; an element of $\mathcal{C}(-,c)$ at $A$ is a map $A\to c$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$\mathbf{1}\downarrow \mathrm{Id}$ has objects $(\ast, B, f\colon c\to B)$ and morphisms $v\colon B\to B'$ with $v\circ f=f'$ — exactly the coslice $c/\mathcal{C}$. The category of elements $\int_{\mathcal{C}}\mathcal{C}(-,c)$ has objects $(A, x)$ with $x\colon A\to c$ and morphisms $g\colon A\to A'$ satisfying $x'\circ g = x$ — exactly the slice $\mathcal{C}/c$. The two are dual: $\int_{\mathcal{C}}\mathcal{C}(-,c)=\mathcal{C}/c$ has a terminal object $\mathrm{id}_c$, which is the Yoneda-theoretic statement that $\mathcal{C}(-,c)$ is representable with universal element $\mathrm{id}_c$.

</details>

</aside>

## Advanced results [Master]

**Theorem (interchange law).** For categories $\mathcal{C},\mathcal{D},\mathcal{E}$, functors and natural transformations $\alpha\colon F\Rightarrow F'$, $\alpha'\colon F'\Rightarrow F''$ in $[\mathcal{C},\mathcal{D}]$ and $\beta\colon G\Rightarrow G'$, $\beta'\colon G'\Rightarrow G''$ in $[\mathcal{D},\mathcal{E}]$, the two compositions satisfy
$$
(\beta'\cdot\beta)*(\alpha'\cdot\alpha) = (\beta'*\alpha')\cdot(\beta*\alpha).
$$
Both sides are natural transformations $GF\Rightarrow G''F''$, and the identity says that horizontal and vertical composition commute. The proof is the naturality square of $\beta$ at the morphism $\alpha_A$, read componentwise: each component of either side equals $\beta'_{F''(A)}\circ\beta_{F''(A)}\circ G(\alpha'_A)\circ G(\alpha_A)$ after using functoriality of $G,G'$ and naturality of $\beta,\beta'$ to slide the components past one another. The interchange law is exactly the coherence that makes $\mathbf{Cat}$ a strict $2$-*category*: objects are small categories, $1$-morphisms are functors, $2$-morphisms are natural transformations, with the two composites of $2$-cells governed by this single equation.

**Theorem (functor categories inherit structure).** If $\mathcal{D}$ has all limits of a given shape, so does $[\mathcal{C},\mathcal{D}]$, computed pointwise: $(\lim_i F_i)(A)=\lim_i (F_i(A))$, with the universal property checked component by component. In particular presheaf categories $[\mathcal{C}^{\mathrm{op}},\mathbf{Set}]$ are complete and cocomplete, since $\mathbf{Set}$ is. Natural transformations between such limits are determined by their components, so the evaluation functors $\mathrm{ev}_A\colon[\mathcal{C},\mathcal{D}]\to\mathcal{D}$ jointly reflect and preserve the pointwise structure.

**Theorem (skeleton and the role of choice).** Every category $\mathcal{C}$ has a *skeleton* $\mathrm{sk}(\mathcal{C})$: a full subcategory containing exactly one object from each isomorphism class, and the inclusion $\mathrm{sk}(\mathcal{C})\hookrightarrow\mathcal{C}$ is an equivalence. Constructing the skeleton selects a representative per class, an appeal to the axiom of choice; conversely, the statement "every fully faithful essentially surjective functor is an equivalence" is equivalent over $\mathbf{ZF}$ to a choice principle. Two categories are equivalent if and only if their skeletons are isomorphic, so equivalence classes of categories are isomorphism classes of skeletons.

**Theorem (category of elements as a comma category; the Grothendieck construction).** For a presheaf $F\colon\mathcal{C}^{\mathrm{op}}\to\mathbf{Set}$, the category of elements satisfies $\int_{\mathcal{C}}F \cong (y\downarrow F)$, the comma category of the Yoneda embedding $y\colon\mathcal{C}\to[\mathcal{C}^{\mathrm{op}},\mathbf{Set}]$ over the object $F$. The forgetful functor $\int_{\mathcal{C}}F\to\mathcal{C}$ is a discrete fibration, and $F$ is recovered as the presheaf of fibres. The general Grothendieck construction upgrades a functor $\mathcal{C}^{\mathrm{op}}\to\mathbf{Cat}$ into a fibration over $\mathcal{C}$, the indexed-versus-fibred-category equivalence that organises descent and stack theory.

**Synthesis.** Putting these together, natural transformations are the $2$-cells that turn $\mathbf{Cat}$ from a category into a $2$-category, and the interchange law is the single coherence that makes the two ways of composing them agree. The foundational reason the functor category $[\mathcal{C},\mathcal{D}]$ behaves so well is that everything in it is checked pointwise: limits, isomorphisms, and naturality all reduce to the target $\mathcal{D}$ object by object, which is exactly why presheaf categories inherit completeness for free and why the Yoneda machinery of [41.04.02] lives there. This generalises the duality of [41.01.01]: where duality came from the involution $(\mathcal{C}^{\mathrm{op}})^{\mathrm{op}}=\mathcal{C}$, equivalence comes from allowing identities only up to natural isomorphism, and the central insight is that the correct notion of sameness for categories is the one that ignores the difference between isomorphic objects — the skeleton makes this precise and exposes where the axiom of choice is spent. The bridge is that equivalence, comma categories, and the category of elements are all assembled from the same primitive, the natural transformation: limits and colimits in [41.02.01] are defined up to the natural isomorphisms catalogued here, adjunctions in [41.03.01] are pairs of natural transformations (unit and counit) subject to the triangle identities, and the Yoneda embedding in [41.04.02] is full and faithful precisely because natural transformations between representables are computed by this same pointwise discipline.

## Full proof set [Master]

**Proposition 1 (vertical composition is associative and unital; $[\mathcal{C},\mathcal{D}]$ is a category).** *Vertical composition of natural transformations is associative, has the identity transformations as units, and produces a category $[\mathcal{C},\mathcal{D}]$.*

*Proof.* For $\alpha\colon F\Rightarrow G$, $\beta\colon G\Rightarrow H$, $\gamma\colon H\Rightarrow K$, the component of $(\gamma\cdot\beta)\cdot\alpha$ at $A$ is $(\gamma_A\circ\beta_A)\circ\alpha_A$ and of $\gamma\cdot(\beta\cdot\alpha)$ is $\gamma_A\circ(\beta_A\circ\alpha_A)$; these agree by associativity in $\mathcal{D}$. The composite is natural: pasting the naturality squares of $\alpha$ and $\beta$ along their shared edge $G(f)$ gives the square for $\beta\cdot\alpha$. The identity $1_F$ with components $\mathrm{id}_{F(A)}$ satisfies $\alpha\cdot 1_F=\alpha=1_G\cdot\alpha$ componentwise. Hence functors $\mathcal{C}\to\mathcal{D}$ and natural transformations form a category. $\square$

**Proposition 2 (whiskering associates with composition).** *For functors $F,F'\colon\mathcal{C}\to\mathcal{D}$, a transformation $\alpha\colon F\Rightarrow F'$, and functors $G\colon\mathcal{B}\to\mathcal{C}$, $H\colon\mathcal{D}\to\mathcal{E}$, one has $H(\alpha G)=(H\alpha)G$ as transformations $HFG\Rightarrow HF'G$.*

*Proof.* The component of $\alpha G$ at an object $B$ of $\mathcal{B}$ is $\alpha_{G(B)}$. Applying $H$ gives component $H(\alpha_{G(B)})$ for $H(\alpha G)$. The component of $H\alpha$ at an object $C$ of $\mathcal{C}$ is $H(\alpha_C)$, and post-whiskering by $G$ evaluates at $C=G(B)$, giving $H(\alpha_{G(B)})$. The components coincide, and naturality of each side was established by Exercise 3 and its mirror. $\square$

**Proposition 3 (the interchange law).** *With notation as in the Advanced-results statement, $(\beta'\cdot\beta)*(\alpha'\cdot\alpha)=(\beta'*\alpha')\cdot(\beta*\alpha)$.*

*Proof.* It suffices to treat the basic case $\beta*\alpha$ for $\alpha\colon F\Rightarrow F'$, $\beta\colon G\Rightarrow G'$ and verify the two definitions of $*$ agree, then assemble. Fix $A$. Naturality of $\beta$ at the morphism $\alpha_A\colon F(A)\to F'(A)$ reads $G'(\alpha_A)\circ\beta_{F(A)}=\beta_{F'(A)}\circ G(\alpha_A)$; this is the common value defining $(\beta*\alpha)_A$. For the interchange identity, expand the left side at $A$: $(\beta'\cdot\beta)*(\alpha'\cdot\alpha)$ has component the common value of $(G''(\alpha'_A\circ\alpha_A))\circ(\beta'_{F(A)}\circ\beta_{F(A)})$ and its transpose. Expand the right side: vertical composition of $(\beta'*\alpha')_A=G''(\alpha'_A)\circ\beta'_{F'(A)}$ after $(\beta*\alpha)_A=G'(\alpha_A)\circ\beta_{F(A)}$. Using functoriality of $G',G''$ and naturality of $\beta'$ at $\alpha_A$ to commute $\beta'_{F'(A)}$ past $G'(\alpha_A)$, both expansions reduce to $G''(\alpha'_A)\circ G''(\alpha_A)\circ\beta'_{F(A)}\circ\beta_{F(A)}$. The components agree for all $A$, so the transformations are equal. $\square$

**Proposition 4 (pointwise limits in functor categories).** *If $\mathcal{D}$ has limits of shape $\mathcal{J}$, then $[\mathcal{C},\mathcal{D}]$ has them, and they are computed pointwise.*

*Proof.* Let $D\colon\mathcal{J}\to[\mathcal{C},\mathcal{D}]$ be a diagram, so each $D_j$ is a functor $\mathcal{C}\to\mathcal{D}$ and each arrow of $\mathcal{J}$ gives a natural transformation. For each object $A$ of $\mathcal{C}$ form the limit $L(A)=\lim_{j}D_j(A)$ in $\mathcal{D}$, with limiting cone $\pi_{j,A}\colon L(A)\to D_j(A)$. For a morphism $f\colon A\to B$, the maps $D_j(f)\circ\pi_{j,A}$ form a cone over the diagram at $B$, inducing a unique $L(f)\colon L(A)\to L(B)$; functoriality of $L$ follows from uniqueness. The $\pi_{j}$ are natural by construction, and given any cone in $[\mathcal{C},\mathcal{D}]$ over $D$ its components factor uniquely through each $L(A)$, the factorisations assembling into a natural transformation by uniqueness. So $L$ with the $\pi_j$ is the limit, computed pointwise. $\square$

## Connections [Master]

- **Categories, functors, and duality [41.01.01].** This unit is the direct continuation of the functor theory developed there: the faithful/full/essentially-surjective trichotomy introduced in [41.01.01] is assembled here into the characterisation of equivalence, and the opposite category and slice constructions of [41.01.01] reappear as the special comma categories $F\downarrow G$. The duality involution $(\mathcal{C}^{\mathrm{op}})^{\mathrm{op}}=\mathcal{C}$ is the degenerate case of the equivalence relation studied here, and the survey [01.02.09] states the natural-transformation definition more briefly within its single-sweep treatment.

- **Limits and colimits [41.02.01].** Limits and colimits are defined only up to the natural isomorphisms catalogued in this unit, and Proposition 4 shows functor categories inherit them pointwise — the mechanism by which presheaf categories become complete and cocomplete. The category of elements and the comma categories here are precisely the shapes over which limit and colimit cones are taken, so this unit supplies the $2$-categorical language in which [41.02.01] states universal properties.

- **The Yoneda lemma and representability [41.04.02].** The Yoneda lemma computes the natural transformations between a representable functor and an arbitrary presheaf, so it is a theorem *about* the functor category $[\mathcal{C}^{\mathrm{op}},\mathbf{Set}]$ built here; the Yoneda embedding is full and faithful, hence an equivalence onto its image by the theorem of this unit, and the category of elements $\int_{\mathcal{C}}F\cong(y\downarrow F)$ is the comma-category presentation that makes representability the existence of a terminal element.

- **Adjunctions [41.03.01].** An adjunction is a pair of natural transformations — the unit and counit — subject to the triangle identities, all of which are equations in functor categories defined here. Every equivalence is in particular an adjunction (an adjoint equivalence after adjusting the isomorphisms), so the characterisation theorem of this unit is the entry point to the adjoint-functor theory of [41.03.01].

## Historical & philosophical context [Master]

Natural transformations were the phenomenon that category theory was invented to make precise. In their 1945 paper *General theory of natural equivalences* [ref: TODO_REF Eilenberg-Mac Lane 1945], Samuel Eilenberg and Saunders Mac Lane set out to define the word "natural" in the statement that a finite-dimensional vector space is naturally isomorphic to its double dual but only unnaturally isomorphic to its single dual. Defining "natural transformation" required first defining functor, and defining functor required first defining category, so the three notions entered the literature in the reverse of their conceptual order. The naturality square — the commuting rectangle that the components must satisfy — is already the central definition of that founding paper.

The functor category and the equivalence of categories belong to the consolidation of the subject in Mac Lane's *Categories for the Working Mathematician* (1971; 2nd ed. 1998) [ref: TODO_REF Mac Lane 1998], where the interchange law, the $2$-categorical structure of $\mathbf{Cat}$, comma categories, and the characterisation of equivalences by the full-faithful-essentially-surjective trichotomy are organised as standard tools. The Grothendieck construction and the category of elements emerged from Alexander Grothendieck's reworking of algebraic geometry in the early 1960s, where fibred categories over a base encode descent data; the equivalence between indexed categories and fibrations is the structural core of that theory. The dependence of the equivalence characterisation on the axiom of choice was isolated in the categorical-foundations literature and is the reason constructive category theory distinguishes equivalences from adjoint equivalences with specified data.

## Bibliography [Master]

```bibtex
@article{EilenbergMacLane1945,
  author  = {Eilenberg, Samuel and Mac Lane, Saunders},
  title   = {General theory of natural equivalences},
  journal = {Transactions of the American Mathematical Society},
  volume  = {58},
  year    = {1945},
  pages   = {231--294}
}

@book{MacLane1998,
  author    = {Mac Lane, Saunders},
  title     = {Categories for the Working Mathematician},
  edition   = {2},
  publisher = {Springer},
  series    = {Graduate Texts in Mathematics 5},
  year      = {1998}
}

@book{Riehl2016,
  author    = {Riehl, Emily},
  title     = {Category Theory in Context},
  publisher = {Dover},
  year      = {2016}
}

@book{Awodey2010,
  author    = {Awodey, Steve},
  title     = {Category Theory},
  edition   = {2},
  publisher = {Oxford University Press},
  series    = {Oxford Logic Guides 52},
  year      = {2010}
}

@book{Leinster2014,
  author    = {Leinster, Tom},
  title     = {Basic Category Theory},
  publisher = {Cambridge University Press},
  series    = {Cambridge Studies in Advanced Mathematics 143},
  year      = {2014}
}

@book{Kelly1982,
  author    = {Kelly, G. Max},
  title     = {Basic Concepts of Enriched Category Theory},
  publisher = {Cambridge University Press},
  series    = {London Mathematical Society Lecture Note Series 64},
  year      = {1982}
}
```
