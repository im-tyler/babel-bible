---
id: 41.02.02
title: "Constructing Limits: Products, Equalizers, Preservation, and Filtered Colimits"
slug: constructing-limits-preservation-filtered
section: category-theory
chapter: limits-colimits
concept_catalog_id: category-theory.constructing-limits-preservation-filtered
prerequisites:
  - 41.02.01  # limits and colimits as universal cones
successors: []
tier_anchors:
  beginner: "Riehl 2016 *Category Theory in Context* (Dover) §3.2-3.3 (building limits out of products and equalizers, filtered colimits as unions); Leinster 2014 *Basic Category Theory* (Cambridge) §5.1, §6.3 (the completeness theorem and the directed-colimit picture)"
  intermediate: "Mac Lane 1998 *Categories for the Working Mathematician* 2e (Springer GTM 5) Ch. V §2 (limits from products and equalizers, completeness), Ch. V §3-4 (preservation and creation of limits), Ch. IX §1-2 (filtered categories, filtered colimits commute with finite limits in Set); Riehl 2016 *Category Theory in Context* (Dover) §3.2-3.3, §3.8; Awodey 2010 *Category Theory* 2e (Oxford) §5.6"
  master: "Mac Lane 1998 *Categories for the Working Mathematician* 2e (Springer GTM 5) Ch. V, IX in full; Adámek-Rosický 1994 *Locally Presentable and Accessible Categories* (Cambridge LMS 189) Ch. 1 (finitely presentable objects, filtered colimits, accessible categories); Borceux 1994 *Handbook of Categorical Algebra 1* (Cambridge) §2.13, Ch. 6 (flat functors, filtered colimits, ind-objects); Gabriel-Ulmer 1971 *Lokal präsentierbare Kategorien* (Springer LNM 221)"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Mac Lane — Categories for the Working Mathematician"
    locator: "2nd ed., Springer GTM 5, Ch. V §2-4, Ch. IX §1-2"
  - source: TODO_REF
    path: "Riehl — Category Theory in Context"
    locator: "Dover 2016, §3.2-3.3, §3.8"
  - source: TODO_REF
    path: "Adámek-Rosický — Locally Presentable and Accessible Categories"
    locator: "Cambridge LMS 189, 1994, Ch. 1"
  - source: TODO_REF
    path: "Borceux — Handbook of Categorical Algebra 1"
    locator: "Cambridge 1994, §2.13, Ch. 6"
  - source: TODO_REF
    path: "Gabriel-Ulmer — Lokal präsentierbare Kategorien"
    locator: "Springer LNM 221, 1971"
lean_status: none
lean_mathlib_gap: |
  Mathlib's CategoryTheory.Limits package has the two ingredients separately:
  Limits.Constructions.LimitsFromProductsAndEqualizers builds an arbitrary
  limit as the equalizer of two maps between products, and
  Limits.Preserves / Limits.Creates formalise PreservesLimit, ReflectsLimit,
  and CreatesLimit together with the theorem that a right adjoint preserves
  limits (Adjunction.rightAdjointPreservesLimits) and that forgetful functors
  of algebraic structures create them. The filtered side exists too:
  CategoryTheory.Filtered defines IsFiltered and IsCofiltered, and
  Limits.FilteredColimitCommutesFiniteLimit proves that filtered colimits
  commute with finite limits in the category of types. What Mathlib does not
  package as one connected development is the bridge proved here: that the
  product-plus-equalizer construction, the preservation/creation/reflection
  trichotomy, and the filtered-colimit commutation are three faces of how a
  functor interacts with the cone calculus, with finitely presentable objects
  characterised by the hom-functor preserving filtered colimits
  (Mathlib has IsFiltered but no general FinitelyPresentable predicate tied to
  Hom-preservation of filtered colimits, the Gabriel-Ulmer locally-presentable
  spine, sifted colimits, or the ind/pro-object completions Ind(C), Pro(C)).
  A Codex.CategoryTheory.Limits.ConstructionAndPreservation module assembling
  the completeness theorem, the right-adjoint-preserves-limits proof from the
  cone-equalizer presentation, and the finite-limit/filtered-colimit
  interchange as a single development is the formalisation target.
human_reviewer: "Tyler (pending external category-theory reviewer)"
source_books: [maclane-cwm]
source_curriculum: logic-foundations
estimated_time:
  beginner: 20m
  intermediate: 48m
  master: 88m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Once you know that a limit is the best object hanging consistently over a pattern of objects and arrows, the next question is practical: how do you actually build one? It would be tedious to invent a fresh construction for every shape of pattern. Two simple ingredients suffice. If a category lets you form products (the best object over a bunch of objects with no arrows) and equalizers (the best object on which two parallel arrows agree), then you can build the limit of any pattern at all. You bundle every object of the pattern into one big product, then carve out the part where the pattern's own arrows are respected. That carving is an equalizer.

A second question is how limits behave when you carry them from one category to another by a translation. Some translations keep the best object best: they preserve limits. Others are even better behaved and build the limit upstairs out of the limit downstairs: they create limits. Knowing which translations do this lets you compute a complicated limit by handing it to a friendlier category.

The last theme is a special kind of pattern, a filtered one, where any two pieces can always be merged further along. Filtered patterns behave like growing unions, and their colimits are gentle: they get along with finite limits, so you can swap the order of the two operations.

## Visual [Beginner]

Think of building a limit in two stages. First, lay all the pattern's objects side by side and take their product — one apex with a leg to each. This apex is too generous: it ignores the pattern's internal arrows. Second, keep only the part of the apex where those arrows are honored. That filtering step is the equalizer.

| stage | what you form | what it does |
|---|---|---|
| 1. product | one apex over all objects at once | provides a leg to every object, ignores internal arrows |
| 2. equalizer | the sub-apex respecting the arrows | keeps only the consistent families |
| result | the limit of the whole pattern | best cone, built from two stock pieces |

A filtered pattern looks different. Picture a growing tower of rooms where any two rooms share a larger room further up. The colimit is the whole building you reach in the limit of growth — and because the tower keeps merging, a finite check made inside the building can already be made in some single large-enough room.

![Two-stage limit construction: on the left a wide product apex sends one leg to each object of a pattern, on the right an equalizer carves out the sub-apex on which the pattern's internal arrows are respected, yielding the limit; below, a filtered system drawn as a growing tower of rooms each pair sharing a larger room above, whose union is the filtered colimit](/img/constructing-limits-preservation-filtered-placeholder.svg)

The picture shows the whole strategy: complicated limits are never built from scratch. You always assemble them from products and equalizers, and you compute them in whichever category makes those two pieces easy.

## Worked example [Beginner]

Build the limit of a small pattern in the category of sets, using only a product and an equalizer. Take two sets $A = \{1, 2, 3\}$ and $B = \{1, 2\}$ with a single arrow between them: the function $f\colon A \to B$ that sends $1, 2 \mapsto 1$ and $3 \mapsto 2$. The pattern is "$A$ with $f$ landing in $B$".

Step 1. Form the product apex $A \times B$, all ordered pairs. It has $3 \times 2 = 6$ elements. Its two legs read off the first and second slots.

Step 2. The pattern's arrow $f$ imposes a condition: a consistent family $(a, b)$ must have $f(a) = b$. So compare two functions from $A \times B$ to $B$: one reads off the second slot, $b$; the other applies $f$ to the first slot, $f(a)$. The equalizer keeps the pairs where these agree.

Step 3. List the survivors: $(1,1)$ has $f(1) = 1$, agree; $(2,1)$ has $f(2) = 1$, agree; $(3,2)$ has $f(3) = 2$, agree. The pairs $(1,2), (2,2), (3,1)$ all fail. So the limit has three elements: $\{(1,1), (2,1), (3,2)\}$.

What this tells us: the limit is a copy of $A$, since each element of $A$ pairs with exactly one forced value in $B$. We never guessed this; the product-then-equalizer recipe produced it. Building over the six-element product and carving by the arrow's condition gave the right three-element answer.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

To build the limit of an arbitrary pattern, which two ingredients are enough?

A. Products and equalizers
B. Products and coproducts
C. Sums and differences
D. Just one big product

<details class="exercise__hint"><summary>Hint</summary>

First bundle the objects, then carve out the consistent part.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**A.** A category with all products and all equalizers has every limit: bundle the objects into a product, then equalize to enforce the pattern's arrows. Feedback-correct: products give the apex, equalizers enforce consistency. Feedback-wrong: coproducts and sums build colimits, not limits, and a bare product ignores the pattern's internal arrows.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

A translation between categories that keeps every best cone best is said to preserve limits.

<details class="exercise__hint"><summary>Hint</summary>

Preserving means the image of the limit is again the limit of the image.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** A functor preserves limits when it carries a limit cone to a limit cone of the image diagram. Feedback-correct: the best object stays best after translation. Feedback-wrong: many functors fail this, so it is a genuine property worth naming, not automatic.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What is special about a filtered pattern?

A. It has no arrows at all
B. Any two pieces can be merged further along
C. It is always finite
D. Every arrow is reversible

<details class="exercise__hint"><summary>Hint</summary>

Think of a growing tower where any two rooms share a larger room above.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** A filtered system is one in which any two objects map into a common third, and any two parallel arrows are merged by a further arrow. Feedback-correct: this upward-merging is exactly what makes the colimit behave like a growing union. Feedback-wrong: filtered systems usually have many arrows and are typically infinite; reversibility is not required.

</details>

</aside>

## Formal definition [Intermediate+]

Fix a category $\mathcal{C}$. A *product* of a family $(X_i)_{i\in I}$ is a limit of the discrete diagram on $I$: an object $\prod_i X_i$ with projections $\mathrm{pr}_i$ universal among cones [ref: TODO_REF Mac Lane 1998]. An *equalizer* of a parallel pair $f,g\colon A\rightrightarrows B$ is a limit of the parallel-pair shape: a map $e\colon E\to A$ with $f\circ e=g\circ e$, universal among such maps. A category *has products* if every set-indexed family has one, and *has equalizers* if every parallel pair has one.

**Definition (preservation, reflection, creation of limits).** Let $U\colon\mathcal{C}\to\mathcal{D}$ be a functor and $F\colon\mathcal{J}\to\mathcal{C}$ a diagram. $U$ *preserves* the limit of $F$ if, whenever $(L,\pi)$ is a limit cone over $F$, the image $(U L, U\pi)$ is a limit cone over $U F$. $U$ *reflects* limits if a cone $(L,\pi)$ over $F$ is a limit whenever its image $(U L, U\pi)$ is a limit over $U F$. $U$ *creates* the limit of $F$ if, given a limit cone $(M,\sigma)$ over $U F$ in $\mathcal{D}$, there is a cone over $F$ in $\mathcal{C}$ — unique up to unique isomorphism — whose image is $(M,\sigma)$, and every such cone is a limit. A functor preserving all small limits is *continuous*; dually, preserving all small colimits is *cocontinuous*.

**Definition (filtered category).** A category $\mathcal{J}$ is *filtered* if it is nonempty and: (i) for any two objects $j,k$ there is an object $\ell$ with morphisms $j\to\ell$ and $k\to\ell$; (ii) for any two parallel morphisms $u,v\colon j\rightrightarrows k$ there is a morphism $w\colon k\to\ell$ with $w\circ u=w\circ v$. A *filtered colimit* is a colimit of a diagram $F\colon\mathcal{J}\to\mathcal{C}$ with $\mathcal{J}$ filtered. A *directed colimit* is the special case where $\mathcal{J}$ is a directed poset (every pair has an upper bound); the names *direct limit* and $\varinjlim$ are standard. *Cofiltered* and the dual *cofiltered limit* (inverse limit, $\varprojlim$) reverse all arrows.

**Definition (finitely presentable and finitely generated objects).** In a category $\mathcal{C}$ with filtered colimits, an object $X$ is *finitely presentable* (or *compact*) if the representable functor $\mathcal{C}(X,-)\colon\mathcal{C}\to\mathbf{Set}$ preserves filtered colimits: every morphism $X\to\varinjlim_j F(j)$ factors through some $F(j)$, and two factorisations agree after a further map in $\mathcal{J}$. $X$ is *finitely generated* if $\mathcal{C}(X,-)$ preserves filtered colimits of diagrams with monomorphic transition maps. A *sifted* category is one for which colimits commute with finite products in $\mathbf{Set}$; filtered categories are sifted, and so is the simplex-like category indexing reflexive coequalizers.

### Counterexamples to common slips [Intermediate+]

- *Preservation is not automatic.* The inclusion $\mathbf{Ab}\hookrightarrow\mathbf{Grp}$ does not preserve the coproduct: the coproduct in $\mathbf{Ab}$ is the direct sum, in $\mathbf{Grp}$ the free product, and these differ. Forgetful functors often preserve limits while destroying colimits.
- *Filtered is strictly stronger than connected, and matters for the commutation.* A pushout shape (a span $\bullet\leftarrow\bullet\rightarrow\bullet$) is connected but not filtered, and pushout colimits do not commute with finite limits in $\mathbf{Set}$. The filtered condition (ii) on parallel arrows is exactly what is needed; dropping it breaks the interchange.
- *Finitely generated is weaker than finitely presentable.* For modules, finitely presentable means finitely generated with a finitely generated module of relations; over a non-Noetherian ring a finitely generated module can fail to be finitely presentable. The categorical hom-preservation definitions separate the two notions cleanly.

## Key theorem with proof [Intermediate+]

The signature result is the completeness theorem: products and equalizers suffice to construct every limit, with the limit exhibited as a single equalizer of two maps between products.

**Theorem (completeness from products and equalizers).** Let $\mathcal{C}$ have all small products and all equalizers, and let $F\colon\mathcal{J}\to\mathcal{C}$ be a diagram with $\mathcal{J}$ small. Then $\lim F$ exists and is the equalizer $E$ of the parallel pair
$$
s,t\colon \prod_{j\in\mathrm{Ob}\,\mathcal{J}} F(j) \;\rightrightarrows\; \prod_{(u\colon j\to k)\in\mathrm{Mor}\,\mathcal{J}} F(k),
$$
where the component of $s$ at $u\colon j\to k$ is $F(u)\circ\mathrm{pr}_j$ and the component of $t$ at $u$ is $\mathrm{pr}_k$. Dually, coproducts and coequalizers give cocompleteness [ref: TODO_REF Mac Lane 1998].

*Proof.* Write $P=\prod_{j}F(j)$ and $Q=\prod_{u}F(k)$, the second product indexed by all morphisms of $\mathcal{J}$. A morphism $h\colon C\to P$ is exactly a family $(\lambda_j)_{j}$ with $\lambda_j=\mathrm{pr}_j\circ h\colon C\to F(j)$, by the universal property of $P$. Composing $h$ with $s$ gives the family whose $u$-component is $F(u)\circ\lambda_j$; composing with $t$ gives the family whose $u$-component is $\lambda_k$. Thus $s\circ h=t\circ h$ holds if and only if $F(u)\circ\lambda_j=\lambda_k$ for every $u\colon j\to k$, which is precisely the cone condition on $(\lambda_j)$. So maps $C\to P$ that equalize $s,t$ correspond bijectively, and naturally in $C$, to cones over $F$ with apex $C$.

Now let $e\colon E\to P$ be the equalizer of $s,t$. By the universal property of $E$, a map $C\to E$ is the same as a map $C\to P$ equalizing $s,t$, hence the same as a cone over $F$ with apex $C$. Setting $\pi_j=\mathrm{pr}_j\circ e\colon E\to F(j)$ makes $(E,\pi)$ a cone (it equalizes $s,t$, so satisfies the cone condition), and the bijection just established says every cone factors through $(E,\pi)$ uniquely. Therefore $(E,\pi)$ is terminal in $\mathrm{Cone}(F)$, i.e. $E=\lim F$. The dual statement is the same argument read in $\mathcal{C}^{\mathrm{op}}$, turning products into coproducts and equalizers into coequalizers. $\square$

**Bridge.** This construction builds toward the entire machinery of preservation and creation, and it appears again in [41.03.01], where the limit functor is exhibited as right adjoint to the diagonal and the equalizer-of-products formula becomes the explicit description of that adjoint's value. The foundational reason the reduction works is that a cone is a morphism into a product subject to equalizing conditions, so the two-stage shape — bundle, then carve — is forced by the definition of a cone rather than chosen; this is exactly the structure that makes a functor preserving products and equalizers automatically continuous, since it preserves each stage separately. The construction is dual to the colimit case throughout, coproducts-and-coequalizers giving cocompleteness by the same proof in the opposite category. Putting these together, the bridge is that "complete" and "has products and equalizers" name the same categories, which is what lets the next sections reduce questions about all limits to questions about these two stock shapes — and lets right adjoints, shown to preserve both, preserve every limit at a stroke.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise 1 (easy, multiple choice).**

In the completeness theorem, the second product is indexed by which data of $\mathcal{J}$?

A. The objects of $\mathcal{J}$
B. The morphisms of $\mathcal{J}$
C. The composable pairs of $\mathcal{J}$
D. The identity morphisms only

<details class="exercise__hint"><summary>Hint</summary>

The two maps $s,t$ encode the cone condition, one equation per arrow.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B.** The target product $\prod_{u\colon j\to k}F(k)$ runs over all morphisms $u$ of $\mathcal{J}$, since the cone condition imposes one equation $F(u)\circ\lambda_j=\lambda_k$ per arrow. The source product runs over objects.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

A functor that preserves all small products and all equalizers preserves all small limits.

<details class="exercise__hint"><summary>Hint</summary>

Every limit is built as one equalizer of two maps between products.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Since each limit is the equalizer of a parallel pair between two products, a functor preserving both stages carries the construction to the corresponding construction in the target, hence preserves the limit. This is the standard route to proving a functor continuous.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Show that the forgetful functor $U\colon\mathbf{Grp}\to\mathbf{Set}$ creates all small limits.

<details class="exercise__hint"><summary>Hint</summary>

Given a limit of underlying sets, equip it with the unique group structure making the projections homomorphisms.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $F\colon\mathcal{J}\to\mathbf{Grp}$ and let $(M,\sigma)$ be the limit in $\mathbf{Set}$ of $U F$, realised as compatible families $M=\{(x_j)\mid F(u)(x_j)=x_k\}\subseteq\prod_j |F(j)|$. Define multiplication componentwise: $(x_j)(y_j)=(x_j y_j)$, which again lands in $M$ because each $F(u)$ is a homomorphism, and similarly for the identity and inverses. This is the unique group structure on $M$ making every projection $\sigma_j$ a homomorphism, and with it $(M,\sigma)$ is a cone in $\mathbf{Grp}$ that is terminal, since any compatible family of homomorphisms into the $F(j)$ assembles to a homomorphism into $M$. So $U$ lifts the limit uniquely and the lift is a limit: $U$ creates limits.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

In $\mathbf{Set}$, exhibit the filtered colimit of the system $\mathbb{Z}/2\to\mathbb{Z}/4\to\mathbb{Z}/8\to\cdots$ of cyclic groups under the doubling-compatible inclusions $\mathbb{Z}/2^n\hookrightarrow\mathbb{Z}/2^{n+1}$, $1\mapsto 2$, and identify the resulting group.

<details class="exercise__hint"><summary>Hint</summary>

A directed colimit of these inclusions is the union; track where $2^{-n}$-torsion lands.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The transition maps embed $\mathbb{Z}/2^n$ as the $2^n$-torsion subgroup of the next stage. The directed (hence filtered) colimit is the union of all the $2^n$-torsion, which is the Prüfer group $\mathbb{Z}[1/2]/\mathbb{Z}=\mathbb{Z}(2^\infty)$, the group of dyadic rationals modulo $1$. Each element of the colimit comes from a single stage, and equality of two elements is detected at a common later stage — the defining behaviour of a filtered colimit, where every finite amount of data already appears in one object of the system.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Show that in $\mathbf{Set}$ a filtered colimit of injections is again an injection of each stage into the colimit, and use this to argue that finite sets are finitely presentable.

<details class="exercise__hint"><summary>Hint</summary>

A map out of a finite set is a finite tuple of elements; each lands in some stage, and finitely many can be merged.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $F\colon\mathcal{J}\to\mathbf{Set}$ be filtered with colimit $\varinjlim F$, computed as $\bigsqcup_j F(j)$ modulo identifying $x\in F(j)$ with $F(u)(x)$ for $u\colon j\to k$. A map $f\colon S\to\varinjlim F$ from a finite set $S=\{s_1,\dots,s_n\}$ assigns each $s_i$ a class represented in some $F(j_i)$; by filteredness (i) the finitely many $j_i$ map to a common $\ell$, so all representatives live in $F(\ell)$, and $f$ factors through $F(\ell)$. If two maps $S\to F(\ell)$ become equal in the colimit, each of the $n$ equalities is witnessed by a later map, and by filteredness (i) and (ii) one $\ell\to\ell'$ witnesses all of them. Hence $\mathbf{Set}(S,-)$ preserves filtered colimits, so finite sets are finitely presentable.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Give a finite limit and a filtered colimit in $\mathbf{Set}$ whose order of evaluation can be exchanged, and verify the canonical comparison map is a bijection on a small instance.

<details class="exercise__hint"><summary>Hint</summary>

Take the equalizer (a finite limit) of two natural maps between two filtered colimits, and compute both orders.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Take the constant filtered system $F(j)=\mathbb{N}$ along $\mathbb{N}$ as a directed poset, with identity transitions, so $\varinjlim F=\mathbb{N}$; and the parallel pair $\mathrm{id},\mathrm{succ}\colon\mathbb{N}\rightrightarrows\mathbb{N}$. The equalizer of $\mathrm{id},\mathrm{succ}$ is empty (no $n$ with $n=n+1$), and the filtered colimit of the equalizers (each empty) is empty; conversely the equalizer of the colimit maps $\mathrm{id},\mathrm{succ}\colon\mathbb{N}\rightrightarrows\mathbb{N}$ is again empty. Both orders give $\varnothing$, and the canonical map $\varinjlim(\text{eq})\to\text{eq}(\varinjlim)$ is the identity on $\varnothing$, a bijection — illustrating that filtered colimits commute with the finite limit "equalizer" in $\mathbf{Set}$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove that filtered colimits commute with finite products in $\mathbf{Set}$: for a filtered $\mathcal{J}$ and functors $F,G\colon\mathcal{J}\to\mathbf{Set}$, the canonical map $\varinjlim_j (F(j)\times G(j)) \to (\varinjlim_j F(j))\times(\varinjlim_j G(j))$ is a bijection.

<details class="exercise__hint"><summary>Hint</summary>

Surjectivity merges the two indices via filteredness (i); injectivity merges two witnessing later maps via (i) and (ii).

</details>

<details class="exercise__answer"><summary>Answer</summary>

*Surjectivity.* An element of the target is a pair $([x],[y])$ with $x\in F(j)$, $y\in G(k)$. By filteredness (i) choose $\ell$ with $j\to\ell$, $k\to\ell$; transport $x,y$ to $F(\ell),G(\ell)$. Then $(x_\ell,y_\ell)\in F(\ell)\times G(\ell)$ maps to $([x],[y])$, so the map is onto.

*Injectivity.* Suppose $(x,y),(x',y')\in F(j)\times G(j)$ (after moving both to a common $j$ by (i)) have equal images, so $[x]=[x']$ and $[y]=[y']$ in the respective colimits. Each equality is witnessed by a later map: some $u\colon j\to m$ with $F(u)(x)=F(u)(x')$ and some $v\colon j\to n$ with $G(v)(y)=G(v)(y')$. By (i) take $j\to p$ through which both $m,n$ map, then by (ii) coequalize the two resulting composites $j\rightrightarrows p$ by a single $p\to q$; over $q$ both coordinates agree, so $(x,y)$ and $(x',y')$ have the same class in $\varinjlim(F\times G)$. Hence the map is injective, so bijective. $\square$

</details>

</aside>

## Advanced results [Master]

**Theorem (right adjoints preserve limits; the dual).** If $U\colon\mathcal{C}\to\mathcal{D}$ has a left adjoint $L\dashv U$, then $U$ preserves all limits that exist in $\mathcal{C}$. Dually, a left adjoint preserves all colimits. The proof is a hom-set computation: for a diagram $F\colon\mathcal{J}\to\mathcal{C}$ with limit $L$,
$$
\mathcal{D}(D, U\lim F)\cong\mathcal{C}(L D,\lim F)\cong\lim_j \mathcal{C}(L D, F(j))\cong\lim_j \mathcal{D}(D, U F(j))\cong\mathcal{D}(D,\lim_j U F(j)),
$$
natural in $D$, so $U\lim F$ represents the same functor as $\lim U F$ and the two agree. The middle isomorphism is the basic fact that $\mathcal{C}(C,-)$ sends limits in $\mathcal{C}$ to limits in $\mathbf{Set}$. The detailed adjunction development is [41.03.01], and the preservation theorem with its consequences for free–forgetful pairs is [41.03.02]; the present unit supplies the completeness construction those proofs invoke.

**Theorem (finitely presentable objects and filtered colimits).** In a category $\mathcal{C}$ with filtered colimits, an object $X$ is finitely presentable if and only if $\mathcal{C}(X,-)$ preserves filtered colimits. In $\mathbf{Set}$ the finitely presentable objects are the finite sets; in $R\text{-}\mathbf{Mod}$ they are the finitely presented modules; in $\mathbf{Grp}$ they are the finitely presented groups (finitely many generators and relations); in a variety of algebras they are the finitely presented algebras. Every object of any of these categories is a filtered colimit of finitely presentable ones, which is the defining property of a *locally finitely presentable* category in the sense of Gabriel–Ulmer [ref: TODO_REF Gabriel-Ulmer 1971]: cocomplete, with a set of finitely presentable generators, every object their filtered colimit.

**Theorem (filtered colimits commute with finite limits in $\mathbf{Set}$; sifted colimits with finite products).** For a filtered category $\mathcal{J}$ and a finite category $\mathcal{I}$, and any $H\colon\mathcal{I}\times\mathcal{J}\to\mathbf{Set}$, the canonical comparison
$$
\varinjlim_{j}\lim_{i} H(i,j)\;\longrightarrow\;\lim_{i}\varinjlim_{j} H(i,j)
$$
is a bijection. The same holds in any *locally finitely presentable* category and, more generally, in any category where filtered colimits are computed as in $\mathbf{Set}$ on a generating set. *Sifted* colimits are exactly those commuting with finite products in $\mathbf{Set}$; filtered colimits and reflexive coequalizers are the basic sifted shapes, and a functor preserving finite products and sifted colimits is determined by its restriction to finitely presentable objects.

**Theorem (interchange of limits and colimits; the canonical map).** For a bifunctor $H\colon\mathcal{I}\times\mathcal{J}\to\mathcal{C}$ with $\mathcal{C}$ (co)complete there is always a canonical comparison $\varinjlim_j\lim_i H \to \lim_i\varinjlim_j H$ from "colimit of limits" to "limit of colimits," built from the universal properties. This map is rarely invertible: limits commute with limits and colimits with colimits unconditionally, but a colimit commutes with a limit only under special hypotheses — the filtered/finite case above, the existence of a left adjoint to the limit functor, or distributivity conditions as in an $\infty$-pretopos. The failure of the comparison is what derived functors and spectral sequences measure.

**Synthesis.** Putting these together, the completeness construction is the hinge from which the rest of the chapter swings. The foundational reason products-and-equalizers suffice is that a cone is a map into a product cut down by an equalizer, and this is exactly the structure that makes preservation modular: a functor is continuous as soon as it preserves the two stock shapes, which is why right adjoints, shown to preserve both by the hom-set computation, preserve every limit at once. This generalises the duality already met for cones: coproducts-and-coequalizers give cocompleteness and left adjoints preserve colimits, the whole theory reflected across $\mathcal{C}\mapsto\mathcal{C}^{\mathrm{op}}$. The central insight is that filteredness is the precise condition under which a colimit stops fighting finite limits — any finite diagram of data already lives in one object of a filtered system — so finitely presentable objects, defined by hom-functors preserving filtered colimits, become the atoms from which locally presentable categories are assembled. The bridge to what follows is that this same hom-set reasoning is the content of the adjoint-functor theorems in [41.03.01] and the representability machinery of [41.04.01]: a limit exists, is preserved, or is created exactly when the relevant functor of points is representable, and the ind/pro-object completions $\mathrm{Ind}(\mathcal{C})$ and $\mathrm{Pro}(\mathcal{C})$ are the universal ways of freely adjoining filtered colimits and cofiltered limits, which is exactly how the calculus built here extends to the accessible and presentable categories that organise modern algebra and geometry.

## Full proof set [Master]

**Proposition 1 (the cone–equalizer correspondence is natural).** *With $P=\prod_j F(j)$, $Q=\prod_{u}F(k)$, and $s,t\colon P\rightrightarrows Q$ as in the completeness theorem, the assignment sending a map $h\colon C\to P$ with $s\circ h=t\circ h$ to the cone $(\mathrm{pr}_j\circ h)_j$ is a natural isomorphism $\mathrm{Eq}(s,t)(C)\cong\mathrm{Cone}(C,F)$ of functors $\mathcal{C}^{\mathrm{op}}\to\mathbf{Set}$.*

*Proof.* The universal property of $P$ gives a natural bijection $\mathcal{C}(C,P)\cong\prod_j\mathcal{C}(C,F(j))$, sending $h$ to $(\mathrm{pr}_j\circ h)$. Under it, the equation $s\circ h=t\circ h$ becomes, componentwise over each $u\colon j\to k$, the equation $F(u)\circ(\mathrm{pr}_j\circ h)=\mathrm{pr}_k\circ h$, since the $u$-component of $s$ is $F(u)\circ\mathrm{pr}_j$ and of $t$ is $\mathrm{pr}_k$. That is exactly the cone condition on the family $(\mathrm{pr}_j\circ h)$. Naturality in $C$ is precomposition: for $g\colon C'\to C$ both the bijection and the cone condition are stable under $h\mapsto h\circ g$, since $\mathrm{pr}_j\circ(h\circ g)=(\mathrm{pr}_j\circ h)\circ g$. So the restriction to equalizing maps is a natural isomorphism onto the cone functor. $\square$

**Proposition 2 (a functor preserving products and equalizers is continuous).** *If $U\colon\mathcal{C}\to\mathcal{D}$ preserves all small products and all equalizers, and $\mathcal{C}$ is complete, then $U$ preserves all small limits.*

*Proof.* Let $F\colon\mathcal{J}\to\mathcal{C}$ be small with limit $E=\mathrm{eq}(s,t)$, $e\colon E\to P$, built as in the theorem. Apply $U$. Because $U$ preserves the two products $P,Q$, the images $U P,U Q$ are the corresponding products in $\mathcal{D}$ for the diagrams $U F(j)$ and $U F(k)$, and $U s,U t$ are the two structure maps for $U F$ (their components are $U(F(u))\circ\mathrm{pr}_j$ and $\mathrm{pr}_k$, since $U$ preserves projections). Because $U$ preserves the equalizer, $U e\colon U E\to U P$ is the equalizer of $U s,U t$. By the completeness theorem applied in $\mathcal{D}$, that equalizer is $\lim U F$, with cone $\mathrm{pr}_j\circ U e=U(\mathrm{pr}_j\circ e)=U\pi_j$. Hence $(U E,U\pi)$ is a limit cone over $U F$, i.e. $U$ preserves $\lim F$. $\square$

**Proposition 3 (right adjoints preserve limits).** *If $L\dashv U$ with $L\colon\mathcal{D}\to\mathcal{C}$, $U\colon\mathcal{C}\to\mathcal{D}$, then $U$ preserves every limit existing in $\mathcal{C}$.*

*Proof.* Let $F\colon\mathcal{J}\to\mathcal{C}$ have limit $\lim F$ with cone $\pi$. For each object $D$ of $\mathcal{D}$ the adjunction gives a bijection $\mathcal{D}(D,U C)\cong\mathcal{C}(L D,C)$ natural in $C$. Apply it with $C=\lim F$ and use that $\mathcal{C}(L D,-)$ preserves limits (a representable sends limits to limits in $\mathbf{Set}$):
$$
\mathcal{D}(D,U\lim F)\cong\mathcal{C}(L D,\lim F)\cong\lim_j\mathcal{C}(L D,F(j))\cong\lim_j\mathcal{D}(D,U F(j)).
$$
The composite is natural in $D$, and the last set is $\mathcal{D}(D,\lim_j U F(j))$ when $\lim U F$ exists, or otherwise exhibits the functor $D\mapsto\lim_j\mathcal{D}(D,U F(j))$ as represented by $U\lim F$. Either way $U\lim F$ together with the cone $U\pi$ satisfies the universal property of $\lim U F$. Hence $U$ preserves the limit. $\square$

**Proposition 4 (filtered colimits commute with finite limits in $\mathbf{Set}$).** *Let $\mathcal{J}$ be filtered, $\mathcal{I}$ finite, and $H\colon\mathcal{I}\times\mathcal{J}\to\mathbf{Set}$. Then the canonical map $\theta\colon\varinjlim_j\lim_i H(i,j)\to\lim_i\varinjlim_j H(i,j)$ is a bijection.*

*Proof.* A finite limit in $\mathbf{Set}$ is the equalizer of two maps between finite products, so it suffices to treat finite products and equalizers; finite products are handled by Exercise 7 (and its $n$-fold iterate, valid since $\mathcal{I}$ is finite). For an equalizer, fix the parallel pair $f_j,g_j\colon A(j)\rightrightarrows B(j)$ natural in $j$, with $A=\varinjlim A(j)$, $B=\varinjlim B(j)$ and induced $f,g\colon A\rightrightarrows B$. *Surjectivity of $\theta$.* An element of $\mathrm{eq}(f,g)$ is a class $[a]$, $a\in A(j)$, with $f([a])=g([a])$, so $[f_j(a)]=[g_j(a)]$ in $B$; some $u\colon j\to k$ witnesses $B(u)(f_j(a))=B(u)(g_j(a))$, i.e. $f_k(A(u)(a))=g_k(A(u)(a))$ by naturality. Thus $A(u)(a)\in\mathrm{eq}(f_k,g_k)$ represents a class mapping to $[a]$, so $\theta$ is onto. *Injectivity.* If $a\in\mathrm{eq}(f_j,g_j)$ and $a'\in\mathrm{eq}(f_{j'},g_{j'})$ have $[a]=[a']$ in $A$, a single later map merges them inside $A$, and the merged element still equalizes $f,g$ stagewise; so the two classes in $\varinjlim\mathrm{eq}$ already coincide. Hence $\theta$ is bijective. $\square$

**Proposition 5 (finite sets are finitely presentable; the converse).** *In $\mathbf{Set}$, an object $X$ is finitely presentable if and only if $X$ is finite.*

*Proof.* If $X$ is finite, $\mathbf{Set}(X,-)$ preserves filtered colimits by Exercise 5: a map out of a finite set factors through a single stage and two such maps merge at a later stage. Conversely, suppose $X$ is finitely presentable. Write $X=\varinjlim_{S\subseteq X\ \text{finite}} S$ as the filtered colimit of its finite subsets along inclusions (the poset of finite subsets is directed). Finite presentability makes $\mathrm{id}_X\colon X\to\varinjlim S$ factor through some finite $S_0$, giving $r\colon X\to S_0$ with the inclusion $S_0\hookrightarrow X$ composed with $r$ equal to $\mathrm{id}_X$ after passing to a later stage; since the transition maps are injections this forces $X\subseteq S_0$, so $X$ is finite. $\square$

## Connections [Master]

- **Limits and colimits as universal cones [41.02.01].** This unit is the construction half of the definition–construction pair begun in [41.02.01]: that unit defines the universal cone and states the products-plus-equalizers reduction as an anchoring theorem, while the completeness proof, the explicit two-map presentation, and the cone–equalizer correspondence of Proposition 1 are carried out here. The inverse-limit and direct-limit examples of [41.02.01] are the cofiltered and filtered special cases whose general theory this unit develops.

- **Limits as adjoints to the diagonal and the adjoint-functor theorems [41.03.01].** The limit functor is right adjoint to the constant-diagram functor $\Delta$, and the equalizer-of-products formula proved here is the explicit value of that adjoint; conversely the adjoint-functor theorems of [41.03.01] use exactly the completeness construction of this unit, since a continuous functor between locally presentable categories has a left adjoint precisely when it preserves the limits built from products and equalizers.

- **Right adjoints preserve limits; free–forgetful pairs [41.03.02].** Proposition 3 is the headline preservation theorem, and its co-produced companion [41.03.02] develops the consequences for free–forgetful adjunctions — that forgetful functors of algebraic categories create limits while their left adjoints preserve colimits — together with the worked computation of why $\mathbf{Grp}$, $\mathbf{Ring}$, and $R\text{-}\mathbf{Mod}$ limits are computed on underlying sets. This unit supplies the completeness construction and the hom-set proof those results invoke.

- **Representability, ind-objects, and presentable categories [41.04.01].** Finitely presentable objects (defined here by hom-functors preserving filtered colimits) are the generators of locally presentable categories, and the ind-completion $\mathrm{Ind}(\mathcal{C})$ that freely adjoins filtered colimits is the representability construction developed in [41.04.01]; Proposition 5 and the filtered-colimit commutation are the technical core of that accessibility theory, with the direct-limit material of the algebra corpus [01.02.09] supplying the running module-theoretic examples.

## Historical & philosophical context [Master]

The reduction of arbitrary limits to products and equalizers appears in Mac Lane's *Categories for the Working Mathematician* (1971; 2nd ed. 1998) [ref: TODO_REF Mac Lane 1998] as the standard route to the completeness theorem, codifying a technique already implicit in the construction of inverse limits as submodules of products in the homological algebra of the 1950s. The preservation of limits by right adjoints is one of the first consequences Kan drew from his 1958 definition of adjoint functors, and it is the structural fact behind the older observation that the underlying set of a product of groups is the product of the underlying sets.

Filtered colimits and the finiteness conditions they characterise were given their general form by Pierre Gabriel and Friedrich Ulmer in *Lokal präsentierbare Kategorien* (1971) [ref: TODO_REF Gabriel-Ulmer 1971], where finitely presentable objects, locally presentable categories, and the equivalence between such categories and certain limit theories were established; the theory was extended to accessible categories by Michael Makkai and Robert Paré in 1989 and given a textbook treatment by Jiří Adámek and Jiří Rosický in 1994. The commutation of filtered colimits with finite limits in $\mathbf{Set}$ is the categorical form of the algebraists' fact that a directed union of structures inherits any finitary property checkable on finitely many elements, and it underlies the exactness of the direct-limit functor in module categories that Grothendieck's 1957 Tôhoku axiom AB5 isolates. The ind- and pro-object constructions $\mathrm{Ind}(\mathcal{C})$ and $\mathrm{Pro}(\mathcal{C})$ were introduced by Grothendieck and Verdier in SGA 4 to handle the formal completions needed in étale cohomology.

## Bibliography [Master]

```bibtex
@book{MacLane1998construct,
  author    = {Mac Lane, Saunders},
  title     = {Categories for the Working Mathematician},
  edition   = {2},
  publisher = {Springer},
  series    = {Graduate Texts in Mathematics 5},
  year      = {1998}
}

@book{Riehl2016construct,
  author    = {Riehl, Emily},
  title     = {Category Theory in Context},
  publisher = {Dover},
  year      = {2016}
}

@book{AdamekRosicky1994,
  author    = {Ad\'amek, Ji\v{r}\'i and Rosick\'y, Ji\v{r}\'i},
  title     = {Locally Presentable and Accessible Categories},
  publisher = {Cambridge University Press},
  series    = {London Mathematical Society Lecture Note Series 189},
  year      = {1994}
}

@book{Borceux1994construct,
  author    = {Borceux, Francis},
  title     = {Handbook of Categorical Algebra 1: Basic Category Theory},
  publisher = {Cambridge University Press},
  series    = {Encyclopedia of Mathematics and its Applications 50},
  year      = {1994}
}

@incollection{GabrielUlmer1971,
  author    = {Gabriel, Peter and Ulmer, Friedrich},
  title     = {Lokal pr\"asentierbare Kategorien},
  publisher = {Springer},
  series    = {Lecture Notes in Mathematics 221},
  year      = {1971}
}

@article{Kan1958construct,
  author  = {Kan, Daniel M.},
  title   = {Adjoint functors},
  journal = {Transactions of the American Mathematical Society},
  volume  = {87},
  year    = {1958},
  pages   = {294--329}
}

@book{MakkaiPare1989,
  author    = {Makkai, Michael and Par\'e, Robert},
  title     = {Accessible Categories: The Foundations of Categorical Model Theory},
  publisher = {American Mathematical Society},
  series    = {Contemporary Mathematics 104},
  year      = {1989}
}
```
