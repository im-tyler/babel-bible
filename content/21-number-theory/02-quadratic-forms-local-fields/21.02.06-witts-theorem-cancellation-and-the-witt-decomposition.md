---
id: 21.02.06
title: "Witt's theorem: cancellation and the Witt decomposition"
slug: witts-theorem-cancellation-and-the-witt-decomposition
section: number-theory
chapter: quadratic-forms-local-fields
concept_catalog_id: number-theory.witt-cancellation-witt-decomposition
prerequisites:
  - 01.01.15  # bilinear / quadratic form (diagonalisation, signature, Sylvester)
  - 01.01.01  # field
  - 21.02.01  # finite fields F_q, structure and squares
tiers_present: [beginner, intermediate, master]
successors: []
pending_prereqs: false
tier_anchors:
  beginner: "Serre 1973 *A Course in Arithmetic* (Springer GTM 7) Ch. IV §1 informal opening; Lam 2005 *Introduction to Quadratic Forms over Fields* (AMS GSM 67) Ch. I §1 informal opening"
  intermediate: "Serre 1973 *A Course in Arithmetic* Ch. IV §§1-2; Lam 2005 *Introduction to Quadratic Forms over Fields* Ch. I-II; Scharlau 1985 *Quadratic and Hermitian Forms* (Springer Grundlehren 270) Ch. 1"
  master: "Witt 1937 *J. reine angew. Math.* 176, 31-44 (originator: cancellation, the extension theorem, and the Witt ring); Serre 1973 *A Course in Arithmetic* Ch. IV §§1-2; Lam 2005 *Introduction to Quadratic Forms over Fields* (AMS GSM 67) Ch. I-II (modern textbook treatment of the Witt ring and Witt decomposition); Scharlau 1985 *Quadratic and Hermitian Forms* (Springer Grundlehren 270) Ch. 1-2; Milnor-Husemoller 1973 *Symmetric Bilinear Forms* (Springer Ergebnisse 73)"
lean_status: none
lean_mathlib_gap: |
  Mathlib has the category of quadratic and symmetric bilinear forms in
  `Mathlib.LinearAlgebra.QuadraticForm` together with diagonalisation over a
  field of characteristic not two, but the structural pillars of Witt theory
  are absent. There is no hyperbolic-plane constructor packaged as an object,
  no isotropic / anisotropic predicate on a form, no Witt extension theorem
  (that an isometry between subspaces extends to an isometry of the ambient
  space), and no Witt cancellation lemma derived from it. Consequently the
  Witt decomposition into an anisotropic kernel orthogonal to a hyperbolic
  part, the well-definedness of the Witt index, and the Witt ring $W(k)$ with
  its ring structure under orthogonal sum and tensor product are all
  unformalised. The computed examples $W(\mathbb{R}) \cong \mathbb{Z}$,
  $W(\mathbb{C}) \cong \mathbb{Z}/2$, and $W(\mathbb{F}_q)$ have no Lean
  statement. A formalisation would need to thread the extension theorem
  through reflections (the Cartan-Dieudonne generation of the orthogonal
  group) before any cancellation result becomes available.
human_reviewer: "Tyler (pending external number-theory reviewer)"
estimated_time:
  beginner: 16m
  intermediate: 42m
  master: 85m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
references:
  - source: TODO_REF
    path: "Witt, E. — Theorie der quadratischen Formen in beliebigen Körpern"
    locator: "*Journal für die reine und angewandte Mathematik* 176 (1937), 31-44. The originator paper. Introduces the extension theorem (an isometry of subspaces extends to the whole space), the cancellation theorem, the decomposition into an anisotropic kernel plus hyperbolic planes, and the ring of equivalence classes now called the Witt ring."
  - source: TODO_REF
    path: "Serre, J.-P. — A Course in Arithmetic"
    locator: "Springer Graduate Texts in Mathematics 7 (1973), translation of *Cours d'arithmétique* (PUF 1970). Ch. IV §1 (quadratic forms over a field, orthogonality, isotropy, the hyperbolic plane); §2 (Witt's theorem, Witt decomposition, the Witt group)."
  - source: TODO_REF
    path: "Lam, T. Y. — Introduction to Quadratic Forms over Fields"
    locator: "American Mathematical Society Graduate Studies in Mathematics 67 (2005). Ch. I (foundations, diagonalisation, hyperbolic spaces), Ch. II (Witt's chain-equivalence theorem, Witt cancellation, the Witt decomposition, the Witt ring $W(k)$ and Witt-Grothendieck ring $\\widehat{W}(k)$), with the computations of $W(\\mathbb{R})$, $W(\\mathbb{C})$, and $W(\\mathbb{F}_q)$."
  - source: TODO_REF
    path: "Scharlau, W. — Quadratic and Hermitian Forms"
    locator: "Springer Grundlehren der mathematischen Wissenschaften 270 (1985). Ch. 1-2 give a careful modern account of the orthogonal group, the Cartan-Dieudonne theorem, Witt's theorems, and the fundamental ideal filtration of $W(k)$."
  - source: TODO_REF
    path: "Milnor, J. and Husemoller, D. — Symmetric Bilinear Forms"
    locator: "Springer Ergebnisse der Mathematik 73 (1973). A concise account of symmetric bilinear forms over fields and rings, the Witt ring, and its connection to algebraic K-theory and the Milnor conjectures."
  - source: TODO_REF
    path: "Cartan, É. — Leçons sur la théorie des spineurs"
    locator: "Hermann, Paris (1938). Develops the structure of the orthogonal group via reflections; the Cartan-Dieudonne generation of $O(q)$ by reflections is the geometric engine behind Witt's extension theorem."
  - source: TODO_REF
    path: "Dieudonné, J. — Sur les groupes classiques"
    locator: "Hermann, Paris (1948). The general-field version of the theorem that every isometry of a non-degenerate quadratic space is a product of at most $\\dim V$ reflections, including the characteristic-two caveats."
---

## Intuition [Beginner]

A quadratic form is a homogeneous degree-two polynomial such as $3x^2 - 2y^2 + 5z^2$. Two forms are the *same* form in disguise — isometric — when a linear change of variables turns one into the other. The basic question of the whole subject is: how do we tell two forms apart, and what is the shortest list of numbers that pins a form down up to isometry?

Witt's theorem answers this with a single clean picture. Every form is built out of two kinds of pieces. The first kind is a form that never takes the value zero except at the origin — an *anisotropic* form, a genuinely rigid core. The second kind is a *hyperbolic plane*, the form $xy$ (equivalently $u^2 - v^2$), which is as far from rigid as possible: it has whole lines of inputs on which it vanishes.

Witt's discovery is that the rigid core and the count of hyperbolic planes are not a matter of how you happen to write the form. They are intrinsic. Two forms with the same core and the same number of hyperbolic pieces are isometric, and you may cancel a shared summand from both sides of an isometry. So "the anisotropic core" and "how many hyperbolic planes split off" are two well-defined measurements of any quadratic form.

## Visual [Beginner]

Picture a quadratic form as a stack of blocks. The bottom block is the anisotropic kernel: a solid, indivisible core that records the form's true shape. On top sit identical thin slabs, one for each hyperbolic plane, each slab being the split form $xy$ that vanishes along two crossing lines.

![A stacked-block schematic of the Witt decomposition: a solid anisotropic core at the base labelled the anisotropic kernel, with several identical hyperbolic-plane slabs stacked above it, illustrating that a quadratic form splits as an anisotropic part orthogonal to a sum of hyperbolic planes.](/img/witts-theorem-cancellation-and-the-witt-decomposition-placeholder.svg)

The number of slabs is the Witt index. Witt's theorem says the height of the solid core and the number of slabs do not depend on how you cut the stack apart: every way of separating core from slabs gives the same answer. Over the real numbers the core is recorded by a single integer, the signature, recovering the older Sylvester picture as the simple case.

## Worked example [Beginner]

Take the real form $f(x, y, z) = x^2 + y^2 - z^2$. Does a hyperbolic plane split off, and what is left over?

Look for a line on which $f$ vanishes. Setting $x = 0$ gives $y^2 - z^2 = (y - z)(y + z)$, which is zero when $y = z$. So the vector $(0, 1, 1)$ satisfies $f = 0$ without being the origin; the form is *isotropic*.

Whenever a non-degenerate form has such a zero vector, the subspace it spans pairs up with a partner vector to form a copy of the hyperbolic plane. Here the pair $(0, 1, 1)$ and $(0, 1, -1)$ span a plane on which $f$ restricts to a multiple of $u^2 - v^2$. What remains orthogonal to that plane is the line spanned by $(1, 0, 0)$, where $f = x^2$.

So $f \cong \langle 1 \rangle \perp \mathbb{H}$: an anisotropic core $\langle 1 \rangle$ of rank one, orthogonal to one hyperbolic plane. The Witt index is $1$. The core $x^2$ never vanishes off the origin, so it cannot be cut down further. This matches the signature count: $f$ has signature $(2, 1)$, and pairing one plus with one minus into a hyperbolic plane leaves a lone plus.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Which form is the hyperbolic plane?

A. $x^2 + y^2$
B. $x^2 - y^2$
C. $x^2 + y^2 + z^2$
D. $x^2$

<details class="exercise__hint"><summary>Hint</summary>

The hyperbolic plane is the rank-two form that vanishes on a line off the origin. Over a field of characteristic not two it can be written $u^2 - v^2$, the same as $xy$ after a change of variables.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. $x^2 - y^2$.** This factors as $(x - y)(x + y)$, so it vanishes whenever $x = y$ — a line off the origin. The change of variables $u = x + y$, $v = x - y$ turns it into $uv$, the split binary form. The form $x^2 + y^2$ is anisotropic over $\mathbb{R}$ (positive-definite), and $x^2$ has rank one.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: over a field of characteristic not two, a non-degenerate form that takes the value zero on some non-zero vector must contain a hyperbolic plane.

<details class="exercise__hint"><summary>Hint</summary>

A zero vector of a non-degenerate form is never orthogonal to the whole space, so it has a partner that completes it to a split plane.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** If $v \neq 0$ has $q(v) = 0$ and the form is non-degenerate, there is a vector $w$ with the associated bilinear pairing $b(v, w) \neq 0$. The plane spanned by $v$ and $w$ then carries the split form $\mathbb{H}$. This is the basic isotropic-vector-to-hyperbolic-plane step.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

What is the Witt index of the real form $x^2 + y^2 - z^2 - w^2$?

<details class="exercise__hint"><summary>Hint</summary>

The Witt index counts hyperbolic planes that split off. Over $\mathbb{R}$ it equals the smaller of the count of positive and negative signs in the signature.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**2.** The signature is $(2, 2)$: two positive coefficients and two negative. Each positive sign pairs with a negative sign to make a hyperbolic plane, so two hyperbolic planes split off and the anisotropic core is empty. The Witt index is $\min(2, 2) = 2$. Rubric: full credit for $2$.

</details>

</aside>

## Formal definition [Intermediate+]

Throughout, $k$ is a field of characteristic different from two, and $(V, q)$ is a finite-dimensional quadratic space: $V$ is a $k$-vector space and $q : V \to k$ is a quadratic form, with associated symmetric bilinear form $b(x, y) = \tfrac{1}{2}\big(q(x + y) - q(x) - q(y)\big)$, so that $q(x) = b(x, x)$. The space is **non-degenerate** when $b(x, y) = 0$ for all $y$ forces $x = 0$.

An **isometry** between quadratic spaces $(V, q)$ and $(V', q')$ is a linear isomorphism $\sigma : V \to V'$ with $q'(\sigma x) = q(x)$ for all $x \in V$; we write $q \cong q'$ when one exists. The isometries of $(V, q)$ to itself form the **orthogonal group** $O(q) = O(V, q)$. Two subspaces are **orthogonal**, written $\perp$, when $b$ pairs them to zero; an orthogonal direct sum of quadratic spaces is denoted $q_1 \perp q_2$, and corresponds to a block-diagonal Gram matrix. Diagonalisation over $k$ gives $q \cong \langle a_1 \rangle \perp \cdots \perp \langle a_n \rangle$ with $a_i \in k^\times$, abbreviated $\langle a_1, \ldots, a_n \rangle$.

A non-zero vector $v$ is **isotropic** when $q(v) = 0$, and the space $(V, q)$ is **isotropic** when it contains an isotropic vector; otherwise $(V, q)$ is **anisotropic**. The **hyperbolic plane** $\mathbb{H}$ is the two-dimensional non-degenerate space with form $q(x, y) = xy$, isometric to $\langle 1, -1 \rangle$ over a field of characteristic not two. A space isometric to an orthogonal sum $\mathbb{H} \perp \cdots \perp \mathbb{H}$ of $m$ copies, written $\mathbb{H}^{\perp m}$, is **hyperbolic** (also called **split**) of dimension $2m$.

The **Witt index** of $(V, q)$, written $\operatorname{ind}(q)$, is the dimension of a maximal totally isotropic subspace — a subspace $W \subseteq V$ on which $q$ vanishes identically. The **discriminant** $d(q) = a_1 \cdots a_n \in k^\times / (k^\times)^2$ and, over $\mathbb{R}$, the **signature** are isometry invariants carried over from [01.01.15].

### Counterexamples to common slips

- Characteristic two is excluded. There the formula $b(x, y) = \tfrac{1}{2}(\cdots)$ has no sense, the quadratic form and its polarisation decouple, and the entire diagonalisation-and-cancellation package needs a separate treatment (Arf invariant in place of discriminant).
- Non-degeneracy is required for cancellation. The degenerate form $\langle 0 \rangle$ satisfies $\langle 0 \rangle \perp \langle 1 \rangle \cong \langle 0 \rangle \perp \langle 1 \rangle$ in many spurious ways once a radical is present, and cancellation can fail. One always splits off the radical first and works with the non-degenerate quotient.
- "Isotropic" is not "degenerate". The hyperbolic plane $\mathbb{H}$ is isotropic (it has zero vectors) yet non-degenerate (its Gram determinant is $-1 \neq 0$). Isotropy is about zero values of $q$; degeneracy is about the radical of $b$.

## Key theorem with proof [Intermediate+]

**Theorem (Witt's extension and cancellation theorems; Witt 1937).** *Let $(V, q)$ be a non-degenerate quadratic space over a field $k$ of characteristic not two. (Extension) Every isometry $\tau : U \to U'$ between subspaces $U, U' \subseteq V$ extends to an isometry $\widetilde{\tau} \in O(q)$ of the whole space. (Cancellation) If $q \perp q_1 \cong q \perp q_2$ for non-degenerate forms, then $q_1 \cong q_2$.*

**Proof.** We first prove extension, then derive cancellation. The engine is the reflection. For an anisotropic vector $v$ (so $q(v) \neq 0$), the **reflection** $\tau_v(x) = x - \dfrac{2 b(x, v)}{q(v)} \, v$ lies in $O(q)$, fixes the hyperplane $v^\perp$, and sends $v \mapsto -v$.

The extension theorem is proved by induction on $\dim U$. For $\dim U = 1$, write $U = k u$ and $U' = k u'$ with $q(u) = q(u') = a \neq 0$ (the degenerate case $a = 0$ is folded in below). Consider $q(u - u') + q(u + u') = 2 q(u) + 2 q(u') = 4 a \neq 0$, so at least one of $u - u'$ and $u + u'$ is anisotropic. If $u - u'$ is anisotropic, the reflection $\tau_{u - u'}$ swaps $u$ and $u'$, sending $u \mapsto u'$; if instead $u + u'$ is anisotropic, then $\tau_{u + u'}$ sends $u \mapsto -u'$, and composing with $\tau_{u'}$ sends $u \mapsto u'$. Either way a product of at most two reflections carries $u$ to $u'$, extending $\tau$.

For the inductive step with $\dim U \geq 2$, choose a decomposition $U = U_0 \perp k u$ with $q(u) \neq 0$ (a diagonalising basis of $U$ supplies one once we handle the isotropic case by first splitting off hyperbolic planes from $U$ and matching them across, which the rank-one case and non-degeneracy permit). Apply the rank-one case to extend $\tau|_{k u}$ to an isometry $\sigma \in O(q)$ with $\sigma(u) = \tau(u)$. Then $\sigma^{-1} \tau$ maps $U_0$ into $u^\perp$ and fixes $u$; by induction inside the non-degenerate space $(\tau u)^\perp$ it extends, and composing the extensions gives $\widetilde{\tau}$. This proves extension.

Cancellation follows. Suppose $\varphi : q \perp q_1 \to q \perp q_2$ is an isometry, where the first copy of $q$ sits on a subspace $U$ and the target copy on a subspace $U'$, both isometric to $(V_q, q)$ via the inclusion. Let $\tau = (\text{incl}_{U'})^{-1} \circ \varphi|_U : U \to U'$ be the induced isometry of the two copies of $q$. By extension, $\tau$ lifts to $\widetilde{\tau} \in O(q \perp q_2)$ of the ambient target. Then $\widetilde{\tau}^{-1} \circ \varphi$ is an isometry carrying $U$ identically onto the standard copy of $q$, hence carrying its orthogonal complement $q_1$ isometrically onto the orthogonal complement $q_2$. Therefore $q_1 \cong q_2$. $\square$

**Bridge.** Witt cancellation builds toward the entire local-global theory and is the foundational reason the Hasse-Minkowski classification can speak of "the anisotropic part" of a rational form at all. The central insight is that orthogonal summands may be cancelled, which is exactly the statement that the monoid of isometry classes under $\perp$ has cancellation and so embeds in a group; this group is the Witt-Grothendieck group, and quotienting by hyperbolic planes is the bridge to the Witt ring. The extension theorem generalises the Sylvester rigidity of real forms to an arbitrary field, and is dual to the Cartan-Dieudonne fact that reflections generate $O(q)$. Putting these together, the well-definedness of the Witt index and the anisotropic kernel — which [21.02.08] uses freely when it writes $V = V_{\mathrm{an}} \perp \mathbb{H}^{\perp m}$ — appears again in the proof that the Hasse-Witt invariant is an isometry invariant, and this is exactly the cancellation step that lets the local classification reduce every form to its anisotropic core. The bridge is the recognition that "two measurements of a form are well-defined" is a cancellation statement in disguise.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 1 (easy, symbolic).**

Write out the reflection $\tau_v$ in the standard form $\langle 1, 1 \rangle$ over $\mathbb{R}$ for $v = (1, 0)$, and verify it is an isometry.

<details class="exercise__hint"><summary>Hint</summary>

Use $\tau_v(x) = x - \frac{2 b(x, v)}{q(v)} v$ with $q(v) = 1$ and $b((x_1, x_2), (1, 0)) = x_1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\tau_v(x_1, x_2) = (-x_1, x_2)$.** Here $q(v) = 1$ and $b(x, v) = x_1$, so $\tau_v(x) = (x_1, x_2) - 2 x_1 (1, 0) = (-x_1, x_2)$. Then $q(\tau_v x) = (-x_1)^2 + x_2^2 = x_1^2 + x_2^2 = q(x)$, so $\tau_v$ preserves the form and is an isometry; it is the reflection across the $x_2$-axis. Rubric: full credit for the map and the preservation check.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 2 (medium, symbolic).**

Show that $\langle a, -a \rangle \cong \mathbb{H}$ for any $a \in k^\times$.

<details class="exercise__hint"><summary>Hint</summary>

Exhibit an isotropic vector and use non-degeneracy, or rescale a diagonal basis to reach $\langle 1, -1 \rangle$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The form $\langle a, -a \rangle$ is $q(x, y) = a x^2 - a y^2 = a(x - y)(x + y)$, which vanishes on the non-zero vector $(1, 1)$, so the space is isotropic and non-degenerate (Gram determinant $-a^2 \neq 0$). A two-dimensional non-degenerate isotropic space is a hyperbolic plane: scaling the diagonal basis by $a^{-1/2}$ where possible, or directly setting $u = x + y$, $v = x - y$ gives $q = a u v$, isometric to $\mathbb{H}$ after absorbing $a$ into one coordinate. Hence $\langle a, -a \rangle \cong \mathbb{H}$. Rubric: full credit for an isotropic vector plus the rank-two non-degenerate identification.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 3 (medium, numeric).**

Decompose the real form $\langle 1, 1, -1, -1, 1 \rangle$ into its anisotropic kernel and hyperbolic part, giving the Witt index.

<details class="exercise__hint"><summary>Hint</summary>

Over $\mathbb{R}$, pair each $+1$ with a $-1$ to form $\mathbb{H} \cong \langle 1, -1 \rangle$. The leftover diagonal entries form the anisotropic core.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$\langle 1 \rangle \perp \mathbb{H}^{\perp 2}$, Witt index $2$.** The signature is $(3, 2)$. Pairing two $+1$'s with the two $-1$'s gives $\mathbb{H}^{\perp 2}$, leaving the single remaining $+1$ as the anisotropic kernel $\langle 1 \rangle$. So $\operatorname{ind} = \min(3, 2) = 2$ and $q_{\mathrm{an}} = \langle 1 \rangle$. Rubric: full credit for the decomposition and index $2$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 4 (medium, short-answer).**

State the Cartan-Dieudonne theorem and explain its relation to Witt's extension theorem.

<details class="exercise__hint"><summary>Hint</summary>

Cartan-Dieudonne is about generating the orthogonal group; the extension proof builds the extending isometry out of the same generators.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**Cartan-Dieudonne theorem.** Over a field of characteristic not two, every isometry of an $n$-dimensional non-degenerate quadratic space is a product of at most $n$ reflections $\tau_v$ in anisotropic vectors. **Relation.** Witt's extension theorem builds the ambient isometry extending a subspace isometry as a product of reflections, one rank at a time, so it is a constructive companion to Cartan-Dieudonne: the extension lives inside the group that Cartan-Dieudonne says is reflection-generated. The rank-one step of the extension proof is exactly the observation that a single anisotropic vector can be moved to another of the same length by one or two reflections. Rubric: full credit for the at-most-$n$-reflections statement and the reflection-generation link.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 5 (hard, short-answer).**

Prove that the Witt index of a non-degenerate form is well-defined: any two maximal totally isotropic subspaces have the same dimension.

<details class="exercise__hint"><summary>Hint</summary>

Use Witt's extension theorem applied to an isometry between the two isotropic subspaces, or use cancellation on the hyperbolic parts they generate.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $W_1, W_2$ be maximal totally isotropic subspaces, with $\dim W_1 = m_1 \leq m_2 = \dim W_2$. A totally isotropic subspace of dimension $m$ inside a non-degenerate space generates a hyperbolic subspace $\mathbb{H}^{\perp m}$ (each basis vector pairs with a dual partner), so $q \cong \mathbb{H}^{\perp m_i} \perp q_i'$ with $q_i'$ non-degenerate for $i = 1, 2$. Choose a totally isotropic subspace $W_1' \subseteq W_2$ of dimension $m_1$; the inclusion-induced isometry $W_1 \to W_1'$ extends to $O(q)$ by Witt's extension theorem, carrying the hyperbolic envelope of $W_1$ onto that of $W_1'$. By Witt cancellation on the $\mathbb{H}^{\perp m_1}$ summand, the complements are isometric, so $q_1' \cong \mathbb{H}^{\perp(m_2 - m_1)} \perp q_2'$. If $m_2 > m_1$ then $q_1'$ contains a hyperbolic plane, contradicting the maximality of $W_1$ (its isotropic dimension could be raised). Hence $m_1 = m_2$. Rubric: full credit for the extension-plus-cancellation argument forcing equal dimension.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 6 (hard, symbolic).**

Compute the Witt ring $W(\mathbb{F}_5)$, identifying it as an explicit ring of order four.

<details class="exercise__hint"><summary>Hint</summary>

For $\mathbb{F}_q$ with $q \equiv 1 \pmod 4$, every binary form is isotropic, the anisotropic forms have rank at most $1$, and there are two square classes. Track rank modulo $2$ and discriminant.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$W(\mathbb{F}_5) \cong \mathbb{Z}/2[\mathbb{F}_5^\times/(\mathbb{F}_5^\times)^2] \cong \mathbb{Z}/2[\varepsilon]/(\varepsilon^2 - 1)$, order four.** In $\mathbb{F}_5$ the squares are $\{1, 4\}$, so $\mathbb{F}_5^\times/(\mathbb{F}_5^\times)^2 = \{1, u\}$ with $u = 2$ a non-square. Every binary form over a finite field is isotropic (Chevalley-Warning, or the pigeonhole on values), hence universal, so anisotropic forms have rank $0$ or $1$: the classes are $0$, $\langle 1 \rangle$, $\langle u \rangle$, and $\langle 1, u \rangle$. Since $q \equiv 1 \pmod 4$ we have $-1$ a square, so $\langle 1, 1 \rangle$ is isotropic ($= \mathbb{H} = 0$ in $W$); the additive group is $(\mathbb{Z}/2)^2$ and the ring is $\mathbb{Z}/2[\varepsilon]/(\varepsilon^2 - 1)$ with $\varepsilon = \langle u \rangle$. Rubric: full credit for order four and the ring identification with the two square classes.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Explain how Witt cancellation recovers Sylvester's law of inertia over $\mathbb{R}$.

<details class="exercise__hint"><summary>Hint</summary>

Sylvester says the signature $(p, q)$ is an invariant. Phrase it as cancellation of shared $\langle 1 \rangle$ and $\langle -1 \rangle$ summands.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Over $\mathbb{R}$ every non-degenerate form diagonalises to $\langle 1 \rangle^{\perp p} \perp \langle -1 \rangle^{\perp q}$. Suppose two such presentations of the same form gave $(p, q)$ and $(p', q')$ with $p + q = p' + q' = n$. Witt cancellation lets us cancel the common $\langle 1 \rangle$'s and $\langle -1 \rangle$'s: if $p < p'$ then after cancelling $p$ positive and the matched negative summands we would obtain an isometry between a positive-definite form and an indefinite one, impossible since one is anisotropic-positive and the other has an isotropic vector. So $p = p'$ and $q = q'$: the signature is well-defined. This is precisely Sylvester's law of inertia, now a corollary of cancellation rather than a separate eigenvalue argument. Rubric: full credit for casting Sylvester as a cancellation of signed summands.

</details>

</aside>

## Advanced results [Master]

**Theorem (Witt decomposition; Witt 1937).** *Every non-degenerate quadratic space $(V, q)$ over a field $k$ of characteristic not two decomposes as*
$$
V \;\cong\; V_{\mathrm{an}} \;\perp\; \mathbb{H}^{\perp m},
$$
*where $V_{\mathrm{an}}$ is anisotropic and $m = \operatorname{ind}(q)$ is the Witt index. The anisotropic kernel $V_{\mathrm{an}}$ is determined up to isometry, and $m$ is determined, by $(V, q)$.*

The existence half is a descending induction: while the form is isotropic it contains a hyperbolic plane (an isotropic vector plus a non-degenerate partner), which splits off as an orthogonal summand, lowering the dimension by two; the process halts at an anisotropic remainder. The uniqueness half is exactly Witt cancellation: two decompositions $V_{\mathrm{an}} \perp \mathbb{H}^{\perp m} \cong V_{\mathrm{an}}' \perp \mathbb{H}^{\perp m'}$ with $m \leq m'$ cancel the $\mathbb{H}^{\perp m}$ to give $V_{\mathrm{an}} \cong V_{\mathrm{an}}' \perp \mathbb{H}^{\perp(m' - m)}$, and anisotropy of the left side forces $m = m'$ and $V_{\mathrm{an}} \cong V_{\mathrm{an}}'$.

**Theorem (the Witt ring $W(k)$; Witt 1937).** *The set of isometry classes of anisotropic forms over $k$, equivalently the set of equivalence classes of non-degenerate forms modulo hyperbolic planes, carries a commutative ring structure with addition induced by orthogonal sum $\perp$ and multiplication induced by tensor product $\otimes$, in which the class of every hyperbolic form is the additive zero. This is the Witt ring $W(k)$.*

Two non-degenerate forms are **Witt-equivalent** when their anisotropic kernels are isometric, equivalently when they become isometric after adding hyperbolic planes to each. The orthogonal sum is well-defined on classes because cancellation makes $\perp$ a cancellative, hence group-completable, operation; the additive inverse of $\langle a_1, \ldots, a_n \rangle$ is $\langle -a_1, \ldots, -a_n \rangle$, since their sum $\langle a_1, -a_1, \ldots \rangle$ is a sum of hyperbolic planes. The tensor product of forms, with $\langle a \rangle \otimes \langle b \rangle = \langle ab \rangle$, descends to a ring multiplication because $\mathbb{H} \otimes q$ is hyperbolic for every $q$. The augmentation $\dim \bmod 2 : W(k) \to \mathbb{Z}/2$ has kernel the **fundamental ideal** $I(k)$, whose powers $I^n$ filter $W(k)$ and connect, by the Milnor conjectures (Voevodsky), to Galois cohomology and Milnor K-theory.

**Theorem (computation of the basic Witt rings).** *The Witt rings of the real, complex, and finite fields are:*
$$
W(\mathbb{R}) \cong \mathbb{Z}, \qquad W(\mathbb{C}) \cong \mathbb{Z}/2, \qquad
W(\mathbb{F}_q) \cong \begin{cases} \mathbb{Z}/2[\mathbb{F}_q^\times/(\mathbb{F}_q^\times)^2] & q \equiv 1 \!\!\pmod 4, \\ \mathbb{Z}/4 & q \equiv 3 \!\!\pmod 4. \end{cases}
$$

For $\mathbb{R}$ the only anisotropic forms are the definite ones $\langle 1 \rangle^{\perp p}$ and $\langle -1 \rangle^{\perp q}$, and the Witt class is recorded by the **signature** $p - q \in \mathbb{Z}$; the signature is the ring isomorphism $W(\mathbb{R}) \xrightarrow{\sim} \mathbb{Z}$, so the Witt ring over $\mathbb{R}$ is exactly Sylvester's invariant promoted to a ring. For $\mathbb{C}$ every form is determined by rank alone (every element is a square), so $\langle 1, 1 \rangle$ is already a hyperbolic plane and $W(\mathbb{C}) = \{0, \langle 1 \rangle\} \cong \mathbb{Z}/2$. For finite fields the two square classes and the universality of binary forms give a ring of order four, of one of the two listed shapes according to whether $-1$ is a square.

**Synthesis.** The central insight of Witt's theory is that two measurements of a quadratic form — its anisotropic kernel and its Witt index — are well-defined, and the foundational reason is cancellation, which is exactly the statement that orthogonal sum is a cancellative operation on isometry classes. Putting these together, the Witt decomposition $V \cong V_{\mathrm{an}} \perp \mathbb{H}^{\perp m}$ generalises Sylvester's law of inertia from $\mathbb{R}$ to an arbitrary field, and is dual to the Cartan-Dieudonne generation of the orthogonal group by reflections: the existence of the decomposition splits hyperbolic planes off geometrically, while uniqueness is the algebraic cancellation that the reflection calculus supplies. This is exactly the structure that [21.02.08] presupposes when it writes a rational form as an anisotropic core plus hyperbolic planes and classifies it by rank, discriminant, signature, and the Hasse-Witt invariant; the bridge is that each of those invariants is constant on Witt classes, so they are functions on $W(k)$, and the local-global theory becomes a statement about the family of localisation maps $W(\mathbb{Q}) \to \prod_v W(\mathbb{Q}_v)$. This pattern recurs in algebraic K-theory through the Milnor conjectures relating the filtration $I^n / I^{n+1}$ to Galois cohomology $H^n(k, \mu_2)$, and the central insight that a form is determined by an anisotropic core appears again in the theory of quadratic forms over schemes and the Grothendieck-Witt spectrum.

## Full proof set [Master]

**Proposition 1 (isotropic vector yields a hyperbolic plane).** *Let $(V, q)$ be non-degenerate over $k$ of characteristic not two, and let $v \in V$ be a non-zero isotropic vector. Then $v$ lies in a subspace isometric to $\mathbb{H}$.*

*Proof.* Since $(V, q)$ is non-degenerate and $v \neq 0$, the linear functional $b(v, -)$ is non-zero, so there is $w_0$ with $b(v, w_0) = 1$ after scaling. Set $w = w_0 - \tfrac{1}{2} q(w_0) v$. Then $q(w) = q(w_0) - q(w_0)\, b(v, w_0) + \tfrac{1}{4} q(w_0)^2 q(v) = q(w_0) - q(w_0) + 0 = 0$, using $q(v) = 0$ and $b(v, w_0) = 1$, so $w$ is isotropic as well, and $b(v, w) = b(v, w_0) = 1$. The plane $P = k v \oplus k w$ has Gram matrix $\begin{psmallmatrix} 0 & 1 \\ 1 & 0 \end{psmallmatrix}$ with determinant $-1 \neq 0$, so $P$ is non-degenerate with two isotropic basis vectors paired to $1$; this is precisely the hyperbolic plane $\mathbb{H}$. $\square$

**Proposition 2 (reflections are isometries fixing a hyperplane).** *For anisotropic $v$, the map $\tau_v(x) = x - \dfrac{2 b(x, v)}{q(v)} v$ lies in $O(q)$, fixes $v^\perp$ pointwise, sends $v \mapsto -v$, and satisfies $\tau_v^2 = \mathrm{id}$.*

*Proof.* Write $c = \tfrac{2 b(x, v)}{q(v)}$. Then
$$
q(\tau_v x) = q(x) - 2 c\, b(x, v) + c^2 q(v) = q(x) - 2 c\, b(x, v) + \frac{4 b(x, v)^2}{q(v)} = q(x),
$$
since $c^2 q(v) = \frac{4 b(x,v)^2}{q(v)^2} q(v) = \frac{4 b(x,v)^2}{q(v)}$ cancels the middle term $2 c\, b(x, v) = \frac{4 b(x,v)^2}{q(v)}$. So $\tau_v$ preserves $q$. If $b(x, v) = 0$ then $\tau_v(x) = x$, so $v^\perp$ is fixed; and $\tau_v(v) = v - \frac{2 q(v)}{q(v)} v = -v$. A direct substitution gives $\tau_v^2 = \mathrm{id}$. $\square$

**Proposition 3 (Witt cancellation in the rank-one case).** *If $\langle a \rangle \perp q_1 \cong \langle a \rangle \perp q_2$ with $a \in k^\times$ and $q_1, q_2$ non-degenerate, then $q_1 \cong q_2$.*

*Proof.* Let $\varphi$ be the isometry, and let $u_1, u_2$ be the vectors spanning the two copies of $\langle a \rangle$ in source and target, so $q(u_1) = q(u_2) = a$ and $\varphi(u_1) =: u_1'$ has $q(u_1') = a$. By the rank-one extension argument, one of $u_1' - u_2$ and $u_1' + u_2$ is anisotropic, and a product $\rho$ of at most two reflections in the target sends $u_1' \mapsto u_2$. Then $\rho \circ \varphi$ is an isometry of the ambient spaces sending the source copy of $\langle a \rangle$ exactly onto the target copy spanned by $u_2$. An isometry matching the $\langle a \rangle$ summands carries orthogonal complements to orthogonal complements, so it restricts to an isometry $q_1 \cong q_2$. $\square$

**Proposition 4 (Witt decomposition is unique).** *If $V_{\mathrm{an}} \perp \mathbb{H}^{\perp m} \cong V_{\mathrm{an}}' \perp \mathbb{H}^{\perp m'}$ with both kernels anisotropic, then $m = m'$ and $V_{\mathrm{an}} \cong V_{\mathrm{an}}'$.*

*Proof.* Without loss of generality $m \leq m'$. Iterating Witt cancellation on the $m$ shared hyperbolic planes (each a $\langle 1, -1 \rangle$, cancelled one anisotropic summand at a time via Proposition 3) yields $V_{\mathrm{an}} \cong V_{\mathrm{an}}' \perp \mathbb{H}^{\perp(m' - m)}$. If $m' > m$, the right-hand side contains the isotropic vectors of a hyperbolic plane, so it is isotropic; but the left-hand side $V_{\mathrm{an}}$ is anisotropic, a contradiction. Hence $m' = m$ and the leftover hyperbolic part is empty, giving $V_{\mathrm{an}} \cong V_{\mathrm{an}}'$. $\square$

**Proposition 5 (additive inverses in $W(k)$).** *In the Witt ring, $-[\langle a_1, \ldots, a_n \rangle] = [\langle -a_1, \ldots, -a_n \rangle]$.*

*Proof.* It suffices to show $\langle a \rangle \perp \langle -a \rangle$ is Witt-zero for each $a \in k^\times$, since orthogonal sum is additive on classes. The form $\langle a, -a \rangle$ is $a x^2 - a y^2$, which vanishes at $(1, 1) \neq 0$ and is non-degenerate, so by Proposition 1 it is a hyperbolic plane, hence zero in $W(k)$. Summing over the coordinates, $\langle a_1, \ldots, a_n \rangle \perp \langle -a_1, \ldots, -a_n \rangle \cong \mathbb{H}^{\perp n}$ is Witt-zero, so the second class is the additive inverse of the first. $\square$

**Proposition 6 ($W(\mathbb{R}) \cong \mathbb{Z}$ via signature).** *The signature map $\langle 1 \rangle^{\perp p} \perp \langle -1 \rangle^{\perp q} \mapsto p - q$ is a well-defined ring isomorphism $W(\mathbb{R}) \to \mathbb{Z}$.*

*Proof.* Over $\mathbb{R}$ every element of $\mathbb{R}^\times$ is $\pm$ a square, so every form diagonalises to $\langle 1 \rangle^{\perp p} \perp \langle -1 \rangle^{\perp q}$, and $\langle 1, -1 \rangle = \mathbb{H}$ is Witt-zero, so the class depends only on $p - q$; the anisotropic kernel is $\langle 1 \rangle^{\perp(p - q)}$ when $p \geq q$ and $\langle -1 \rangle^{\perp(q - p)}$ otherwise. The map is additive since signatures add under $\perp$, and multiplicative since $\langle \pm 1 \rangle \otimes \langle \pm 1 \rangle = \langle (\pm 1)(\pm 1) \rangle$ multiplies signs, matching multiplication in $\mathbb{Z}$. It is surjective ($p - q$ ranges over all of $\mathbb{Z}$) and injective (equal signatures give isometric anisotropic kernels by Witt's uniqueness), hence a ring isomorphism. $\square$

## Connections [Master]

- **Bilinear and quadratic forms [01.01.15].** This unit is the arithmetic continuation of the foundational linear-algebra treatment, which stops at diagonalisation, discriminant, signature, and Sylvester's law of inertia over $\mathbb{R}$. Witt's theory promotes those invariants to an arbitrary field of characteristic not two: Sylvester's law becomes the special case $W(\mathbb{R}) \cong \mathbb{Z}$ of the Witt-ring computation, and the signature reappears as the ring isomorphism. The cancellation theorem is the general-field reason the signature is well-defined, replacing the eigenvalue continuity argument used over $\mathbb{R}$.

- **Hilbert symbol and the product formula [21.02.05].** The Hilbert symbol $(a, b)_v$ records whether the ternary form $\langle 1, -a, -b \rangle$ is isotropic over $\mathbb{Q}_v$, which by the Witt decomposition is the question of whether that form has a hyperbolic plane splitting off. The Hasse-Witt invariant $\varepsilon_v(f) = \prod_{i < j}(a_i, a_j)_v$ is constant on Witt classes precisely because cancellation makes it well-defined, so the symbol calculus of [21.02.05] is a calculation inside the local Witt rings $W(\mathbb{Q}_v)$.

- **Hasse-Minkowski theorem [21.02.08].** Hasse-Minkowski writes every rational form as an anisotropic kernel orthogonal to hyperbolic planes and classifies it by rank, discriminant, signature, and Hasse-Witt invariant — every step of which presupposes the Witt decomposition and cancellation proved here. The local-global classification is the injectivity of $W(\mathbb{Q}) \to \prod_v W(\mathbb{Q}_v)$, a statement about the Witt rings of $\mathbb{Q}$ and its completions; this unit supplies the rings, and [21.02.08] supplies the reciprocity constraint cutting out the image.

- **Theta series of quadratic forms [21.04.04].** The representation numbers $r_Q(n)$ counted by a theta series depend only on the isometry class of the positive-definite integral form $Q$, and the Witt-theoretic invariants (rank, discriminant, and the anisotropic kernel of $Q \otimes \mathbb{Q}_v$ at each place) are the genus data that organise forms with the same theta behaviour. The co-produced lateral unit on theta series builds on the structure theory established here, using the Witt decomposition over $\mathbb{Q}_p$ to read off local representability.

## Historical & philosophical context [Master]

Ernst Witt's 1937 paper *Theorie der quadratischen Formen in beliebigen Körpern* (*J. reine angew. Math.* 176, 31-44) [ref: TODO_REF Witt 1937] reshaped a subject that had been tied to the real and rational fields into a structural theory over any field. Before Witt, the classification of quadratic forms proceeded field by field: Sylvester's 1852 law of inertia handled $\mathbb{R}$, and the Minkowski-Hasse local-global apparatus handled $\mathbb{Q}$, but each rested on field-specific computations. Witt's insight was to isolate the purely formal content — that orthogonal sum admits cancellation, that an isometry of subspaces extends to the whole space, and that every form decomposes into an anisotropic core and a controlled number of hyperbolic planes — and to show that this content holds over an arbitrary field of characteristic not two. The resulting **Witt ring** $W(k)$ turned the classification problem into the study of a single commutative ring functorial in the field, an early and influential instance of the structural turn that would define twentieth-century algebra.

The geometric engine, the generation of the orthogonal group by reflections, traces to Cartan's 1938 *Leçons sur la théorie des spineurs* [ref: TODO_REF Cartan 1938] and was put in general-field form by Dieudonne's 1948 *Sur les groupes classiques* [ref: TODO_REF Dieudonné 1948], giving the Cartan-Dieudonne theorem that every isometry is a product of at most $\dim V$ reflections. Serre's 1973 *A Course in Arithmetic* (Ch. IV) made Witt's package the standard prelude to the Hasse-Minkowski theorem, and Milnor and Husemoller's 1973 *Symmetric Bilinear Forms* connected the fundamental-ideal filtration $I^n(k)$ of the Witt ring to algebraic K-theory and Galois cohomology, a thread that culminated in Voevodsky's proof of the Milnor conjecture. Philosophically, Witt's theorem is a paradigm of how isolating the right invariants — here the anisotropic kernel and the Witt index — converts a tangle of case-by-case computations into a clean statement of well-definedness, and how a cancellation law silently underwrites the very possibility of speaking about "the" core of a structure.

## Bibliography [Master]

```bibtex
@article{Witt1937,
  author  = {Witt, Ernst},
  title   = {Theorie der quadratischen Formen in beliebigen K{\"o}rpern},
  journal = {Journal f{\"u}r die reine und angewandte Mathematik},
  volume  = {176},
  year    = {1937},
  pages   = {31--44}
}

@book{Serre1973,
  author    = {Serre, Jean-Pierre},
  title     = {A Course in Arithmetic},
  publisher = {Springer-Verlag},
  series    = {Graduate Texts in Mathematics},
  volume    = {7},
  year      = {1973},
  note      = {English translation of \emph{Cours d'arithm{\'e}tique}, Presses Universitaires de France, 1970}
}

@book{Lam2005,
  author    = {Lam, T. Y.},
  title     = {Introduction to Quadratic Forms over Fields},
  publisher = {American Mathematical Society},
  series    = {Graduate Studies in Mathematics},
  volume    = {67},
  year      = {2005}
}

@book{Scharlau1985,
  author    = {Scharlau, Winfried},
  title     = {Quadratic and Hermitian Forms},
  publisher = {Springer-Verlag},
  series    = {Grundlehren der mathematischen Wissenschaften},
  volume    = {270},
  year      = {1985}
}

@book{MilnorHusemoller1973,
  author    = {Milnor, John and Husemoller, Dale},
  title     = {Symmetric Bilinear Forms},
  publisher = {Springer-Verlag},
  series    = {Ergebnisse der Mathematik und ihrer Grenzgebiete},
  volume    = {73},
  year      = {1973}
}

@book{Cartan1938,
  author    = {Cartan, {\'E}lie},
  title     = {Le{\c c}ons sur la th{\'e}orie des spineurs},
  publisher = {Hermann},
  address   = {Paris},
  year      = {1938}
}

@book{Dieudonne1948,
  author    = {Dieudonn{\'e}, Jean},
  title     = {Sur les groupes classiques},
  publisher = {Hermann},
  address   = {Paris},
  year      = {1948}
}
```
