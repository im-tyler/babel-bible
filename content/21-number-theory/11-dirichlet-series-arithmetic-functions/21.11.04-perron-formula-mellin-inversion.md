---
id: 21.11.04
title: "Perron's Formula and Mellin Inversion"
slug: perron-formula-mellin-inversion
section: number-theory
chapter: dirichlet-series-arithmetic-functions
concept_catalog_id: number-theory.perron-formula-mellin-inversion
prerequisites:
  - 06.01.03  # Residue theorem (contour integration engine)
  - 06.01.16  # Riemann zeta function (the model Dirichlet series)
  - 21.03.01  # Riemann zeta function (number-theoretic anchor)
successors: []
tier_anchors:
  beginner: "Derbyshire 2003 *Prime Obsession* Ch. 19-21 (informal account of summing primes by contour integration); Stewart *Calculus* early-transcendentals on step functions and partial sums"
  intermediate: "Davenport 2000 *Multiplicative Number Theory* (3rd ed., GTM 74) §11 (Perron's formula and the truncated form with error term); Montgomery-Vaughan 2007 *Multiplicative Number Theory I: Classical Theory* (Cambridge Studies 97) §5.1 (Perron, Mellin inversion, the discontinuous integral)"
  master: "Perron 1908 *J. reine angew. Math.* 134, 95 (the original formula); Riemann 1859 *Monatsber. Berliner Akad.* (the contour-integral programme); Davenport 2000 *Multiplicative Number Theory* §11-12; Montgomery-Vaughan 2007 *Multiplicative Number Theory I* §5; Titchmarsh 1986 *The Theory of the Riemann Zeta-Function* (2nd ed., rev. Heath-Brown, Oxford) §3; Tenenbaum 2015 *Introduction to Analytic and Probabilistic Number Theory* (3rd ed., AMS) §II.2"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Perron, O. — Zur Theorie der Dirichletschen Reihen"
    locator: "*Journal für die reine und angewandte Mathematik* 134 (1908), 95-143. The original derivation of the formula expressing the partial-sum function of a Dirichlet series as a vertical contour integral of the series against $x^s/s$."
  - source: TODO_REF
    path: "Davenport, H. — Multiplicative Number Theory"
    locator: "Springer Graduate Texts in Mathematics 74, 3rd edition revised by H. L. Montgomery (2000). §11 (Perron's formula, the truncated form with explicit error term, application to the prime number theorem); §12 (the contour proof of PNT, moving the line of integration past the pole)."
  - source: TODO_REF
    path: "Montgomery, H. L. & Vaughan, R. C. — Multiplicative Number Theory I: Classical Theory"
    locator: "Cambridge Studies in Advanced Mathematics 97 (2007). §5.1 (Perron's formula via the discontinuous integral, Mellin inversion, the effective truncated form with the standard $O$-term)."
  - source: TODO_REF
    path: "Titchmarsh, E. C. — The Theory of the Riemann Zeta-Function"
    locator: "Oxford University Press, 2nd edition revised by D. R. Heath-Brown (1986). §3 (the function $\\psi(x)$, the explicit formula, and the truncated Perron formula as the analytic engine of the prime number theorem with error term)."
  - source: TODO_REF
    path: "Tenenbaum, G. — Introduction to Analytic and Probabilistic Number Theory"
    locator: "American Mathematical Society Graduate Studies in Mathematics 163, 3rd edition (2015). §II.2 (the Mellin transform, Perron's formula, and the effective truncation, with sharp error bounds)."
  - source: TODO_REF
    path: "Mellin, H. — Abriss einer einheitlichen Theorie der Gamma- und der hypergeometrischen Funktionen"
    locator: "*Mathematische Annalen* 68 (1910), 305-337. The systematic theory of the Mellin transform and its inversion, the multiplicative-Fourier underpinning of Perron's formula."
lean_status: none
lean_mathlib_gap: |
  Mathlib does not contain Perron's formula or the Mellin-inversion machinery on
  which it rests. Specifically missing: (1) the discontinuous integral lemma
  $\frac{1}{2\pi i}\int_{c-i\infty}^{c+i\infty} y^s\,\frac{ds}{s} = 1, \tfrac12, 0$
  for $y > 1, = 1, < 1$ respectively, as a theorem about the convergence of the
  principal-value vertical contour integral; (2) the truncated form of this
  lemma with its explicit $O(y^c/(T|\log y|))$ error term, which is the version
  actually used in analytic number theory; (3) the Mellin transform of a locally
  integrable function on $\mathbb{R}_{>0}$ and its inversion theorem, the
  multiplicative analogue of the Fourier inversion theorem that Mathlib has only
  in additive form; (4) Perron's formula itself, $\sum_{n\le x}' a_n =
  \frac{1}{2\pi i}\int_{c-iT}^{c+iT} A(s)\,\frac{x^s}{s}\,ds + O(\cdots)$,
  relating the summatory function of a Dirichlet series to a contour integral of
  the series. Each is a substantial development requiring the contour-integration
  API, principal-value-integral convergence over infinite vertical lines, and the
  interchange of an absolutely convergent Dirichlet series with the contour
  integral. None is present in Mathlib as of 2026.
human_reviewer: "analytic number theory — multiplicative-NT specialist (contour-integral methods)"
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 80m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
source_books: [davenport-multiplicative]
source_curriculum: analytic-nt
---

## Intuition [Beginner]

Suppose you have packaged a sequence of numbers $a_1, a_2, a_3, \ldots$ into a single function by forming the sum $A(s) = a_1 + a_2/2^s + a_3/3^s + \ldots$. This packaging is called a Dirichlet series, and the Riemann zeta function is the case where every $a_n$ equals one. The packaged function $A(s)$ is smooth and analytic, and the tools of calculus apply to it. The original numbers are hidden inside.

Perron's formula is the recipe for getting the original numbers back out, and in particular for recovering the running total $a_1 + a_2 + \ldots$ up to some cutoff $x$. The recipe is a contour integral: you integrate $A(s)$ multiplied by a simple steering factor $x^s/s$ along a vertical line in the complex plane, and the answer is exactly the partial sum you wanted. The steering factor acts like a switch. For each integer $n$, it turns the term $a_n$ fully on when $n$ is below the cutoff and fully off when $n$ is above it.

Why does this matter? Questions about the original numbers — how the primes pile up, how divisors are distributed — are hard to attack directly. Once the running total is written as a contour integral, you can slide the contour around, pick up contributions from the poles of $A(s)$, and read off the answer. This is the engine behind the proof that the primes thin out like $x/\ln x$.

## Visual [Beginner]

A vertical line in the complex plane at horizontal position $c$ (to the right of where the series stops converging). Along this line the integration runs from bottom to top. The integrand carries the factor $x^s/s$, which behaves like an on-off switch keyed to the cutoff $x$.

![A vertical contour line at real part c in the complex plane, with the on-off switching behaviour of the steering factor indicated for n below and above the cutoff x.](/img/perron-formula-mellin-inversion-placeholder.svg)

```
  imaginary
    axis
     ^
     |        contour runs upward
     |        along the vertical line Re(s) = c
     |              |
     |              |   integrand: A(s) * x^s / s
-----+--------------|----------------> real axis
     |              |
     |             Re(s) = c
     |
   switch value of (1/2 pi i) * integral of y^s/s :
        y = n/x < 1  (n below cutoff x)  ->  1   (term kept)
        y = n/x > 1  (n above cutoff x)  ->  0   (term dropped)
        y = 1        (n exactly at x)    ->  1/2 (half weight)
```

The table inside the diagram is the heart of the matter: the contour integral of $y^s/s$ outputs $1$, $\tfrac12$, or $0$ depending on whether $y$ is below, at, or above $1$. Setting $y = n/x$ turns this into the switch that keeps exactly the terms with $n \le x$.

## Worked example [Beginner]

Take the simplest Dirichlet series, where $a_1 = a_2 = a_3 = \ldots = 1$, so $A(s)$ is the Riemann zeta function. The running total up to a cutoff $x$ is just the count of integers from $1$ to $x$. Let us check the switch directly with small numbers instead of integrating.

Step 1. The switching rule says: for a number $y$ bigger than $1$ the switch outputs $0$, for $y$ smaller than $1$ it outputs $1$, and at $y = 1$ it outputs $1/2$. Put the cutoff at $x = 3.5$.

Step 2. Test each integer $n$ by computing $y = n/x$ and reading the switch. For $n = 1$: $y = 1/3.5 = 0.286 < 1$, switch outputs $1$. For $n = 2$: $y = 0.571 < 1$, outputs $1$. For $n = 3$: $y = 0.857 < 1$, outputs $1$. For $n = 4$: $y = 1.143 > 1$, outputs $0$. For $n = 5$ and beyond: $y > 1$, outputs $0$.

Step 3. Add the kept terms: $1 + 1 + 1 = 3$. The count of integers up to $3.5$ is indeed $3$ (namely $1, 2, 3$).

What this tells us: the steering factor $x^s/s$, integrated along the vertical line, is exactly a device that keeps the terms with $n \le x$ and discards the rest. The contour integral does the bookkeeping of a partial sum automatically.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

The contour integral of the steering factor $y^s/s$ along the full vertical line outputs which value when $y$ is greater than $1$?

A. $1$

B. $1/2$

C. $0$

D. $y$

<details class="exercise__hint"><summary>Hint</summary>

Think of the integral as a switch: it keeps a term when the term is below the cutoff and drops it when above. A value $y > 1$ corresponds to an index $n$ that sits above the cutoff $x$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**A.** For $y > 1$ the discontinuous integral outputs $1$. Feedback-correct: with $y = n/x$, the case $y > 1$ means $n > x$ in some conventions, but in the standard normalisation the switch is set so that $y > 1$ returns $1$ and $y < 1$ returns $0$. The convention used in this unit puts $y = x/n$, so $y > 1$ means $n < x$ (term kept). Feedback-wrong: $0$ is the output for $y < 1$, and $1/2$ is the boundary value at $y = 1$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

Perron's formula expresses the running total of a Dirichlet series as a contour integral.

<details class="exercise__hint"><summary>Hint</summary>

Recall what the steering factor $x^s/s$ does when integrated against the packaged function $A(s)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** Perron's formula writes the partial sum of the first $a_n$ up to the cutoff as the integral of $A(s)\,x^s/s$ along a vertical line, where $A(s)$ is the Dirichlet series with coefficients $a_n$. The factor $x^s/s$ acts as a switch that keeps exactly the terms with index at most $x$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

Using the on-off switch with $x = 5.5$ and all coefficients equal to $1$, how many terms are kept (that is, what is the running total)?

<details class="exercise__hint"><summary>Hint</summary>

Count the integers $n$ with $n$ below the cutoff $5.5$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$5$.** The integers below $5.5$ are $1, 2, 3, 4, 5$, so the switch keeps five terms and the running total is $5$. With all coefficients equal to $1$, the partial sum is simply the count of integers up to the cutoff.

</details>

</aside>

## Formal definition [Intermediate+]

Let $A(s) = \sum_{n=1}^{\infty} a_n n^{-s}$ be a Dirichlet series with coefficients $a_n \in \mathbb{C}$, absolutely convergent for $\operatorname{Re}(s) > \sigma_a$ (the abscissa of absolute convergence). Write $\sigma = \operatorname{Re}(s)$ throughout, and fix a real number $c > \sigma_a$. The notation $\sum_{n \le x}'$ denotes the partial sum in which the term $n = x$ (when $x$ is an integer) is counted with weight $\tfrac12$. [ref: TODO_REF Montgomery-Vaughan §5.1]

**Definition (Mellin transform).** For a function $f \colon \mathbb{R}_{>0} \to \mathbb{C}$ that is locally integrable and suitably decaying, the **Mellin transform** is

$$
\widetilde{f}(s) := \int_0^{\infty} f(x)\, x^{s-1}\, dx,
$$

defined on the vertical strip where the integral converges absolutely. The Mellin transform is the multiplicative analogue of the Fourier transform: the substitution $x = e^{-u}$ converts it into the bilateral Laplace transform of $f(e^{-u})$, and hence into a Fourier transform after a further rotation. Its inversion theorem reads

$$
f(x) = \frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} \widetilde{f}(s)\, x^{-s}\, ds
$$

at points of continuity of $f$, for any $c$ inside the strip of convergence.

**Definition (discontinuous integral).** For $y > 0$, $c > 0$, define the principal-value vertical integral

$$
I(y, c) := \frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} \frac{y^s}{s}\, ds
\;=\; \lim_{T \to \infty} \frac{1}{2\pi i} \int_{c-iT}^{c+iT} \frac{y^s}{s}\, ds.
$$

The **discontinuous-integral lemma** states

$$
I(y, c) = \begin{cases} 1 & y > 1, \\ \tfrac12 & y = 1, \\ 0 & 0 < y < 1. \end{cases}
$$

The discontinuity across $y = 1$ is the feature that makes the integral a switch.

**Definition (Perron's formula).** With $A(s)$ and $c > \sigma_a$ as above, and $x > 0$ not necessarily an integer,

$$
{\sum_{n \le x}}' a_n = \frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} A(s)\, \frac{x^s}{s}\, ds.
$$

This is the **(full) Perron formula**: substituting $A(s) = \sum_n a_n n^{-s}$ and integrating term by term applies $I(x/n, c)$ to each coefficient, selecting precisely those $n \le x$.

### Counterexamples to common slips

- *The contour line must lie strictly to the right of $\sigma_a$.* Choosing $c \le \sigma_a$ destroys the term-by-term interchange because the Dirichlet series no longer converges absolutely on the line of integration. The standard practice is $c = \sigma_a + 1/\ln x$, which keeps $x^c = x^{\sigma_a} \cdot e$ controlled while staying in the convergence region.
- *The principal value is essential.* The integrand $y^s/s$ is not absolutely integrable on the vertical line — $|y^s/s| = y^c/|s|$ decays only like $1/|t|$, and $\int dt/|t|$ diverges. Convergence of $I(y,c)$ holds only as a symmetric limit $T \to \infty$; dropping the symmetry breaks it.
- *The boundary weight is $\tfrac12$, not $1$ or $0$.* When $x$ is an integer, the term $n = x$ contributes $I(1, c) = \tfrac12$. Writing $\sum_{n \le x}$ (full weight) instead of $\sum_{n \le x}'$ introduces an error of $\tfrac12 a_x$ that matters in sharp asymptotic work.

## Key theorem with proof [Intermediate+]

The signature result is the truncated Perron formula — the effective version that replaces the infinite contour by a finite segment of length $2T$ and bounds the resulting error. This is the form used in every contour proof of the prime number theorem, because an infinite contour cannot be estimated, while a finite one can. [ref: TODO_REF Davenport §11]

**Theorem (truncated Perron formula).** *Let $A(s) = \sum_{n=1}^{\infty} a_n n^{-s}$ be absolutely convergent for $\operatorname{Re}(s) > \sigma_a$. Let $c > \max(0, \sigma_a)$, $x \ge 1$, and $T \ge 1$. Then*

$$
{\sum_{n \le x}}' a_n = \frac{1}{2\pi i} \int_{c-iT}^{c+iT} A(s)\, \frac{x^s}{s}\, ds + R(x, T),
$$

*where the error term satisfies*

$$
|R(x, T)| \;\le\; \sum_{n=1}^{\infty} |a_n| \left(\frac{x}{n}\right)^{c} \min\!\left(1, \frac{1}{T\,|\ln(x/n)|}\right).
$$

*Proof.* The proof rests on the truncated discontinuous-integral lemma: for $y > 0$, $y \ne 1$, $c > 0$, $T \ge 1$,

$$
\left| \frac{1}{2\pi i} \int_{c-iT}^{c+iT} \frac{y^s}{s}\, ds - \mathbf{1}_{\{y > 1\}} \right| \;\le\; y^{c} \min\!\left(1, \frac{1}{T\,|\ln y|}\right),
$$

where $\mathbf{1}_{\{y>1\}}$ is $1$ for $y > 1$ and $0$ for $y < 1$.

Establish this lemma first. Suppose $y > 1$. Consider the rectangle with vertical side $[c-iT, c+iT]$ closed to the left by a far vertical line $\operatorname{Re}(s) = -U$ with $U \to \infty$. The integrand $y^s/s$ has a single simple pole inside, at $s = 0$, with residue $y^0 = 1$. By the residue theorem [21.03.01] the closed contour integral equals $1$. The contributions of the two horizontal sides $\operatorname{Im}(s) = \pm T$ are bounded by integrating $|y^s/s| = y^{\sigma}/|s|$ across $\sigma \in [-U, c]$; since $|s| \ge T$ on these sides and $\int_{-\infty}^{c} y^{\sigma}\, d\sigma = y^c/\ln y$ converges for $y > 1$, each horizontal side contributes at most $y^c/(2\pi T \ln y)$. The left side vanishes as $U \to \infty$ because $y^{-U} \to 0$. Hence the vertical segment differs from $1$ by at most $y^c/(\pi T \ln y)$, and the cruder uniform bound $y^c$ holds by estimating the segment directly. Taking the smaller of the two bounds gives the stated $\min$.

For $0 < y < 1$ the same argument closes the rectangle to the *right*, where the integrand has no pole, so the residue is $0$ and the same horizontal-side estimates apply with $|\ln y|$ in place of $\ln y$. This proves the truncated lemma.

Now apply it term by term. Because $A(s)$ converges absolutely on the line $\operatorname{Re}(s) = c$, the series and the finite-length integral may be interchanged:

$$
\frac{1}{2\pi i} \int_{c-iT}^{c+iT} A(s)\, \frac{x^s}{s}\, ds
= \sum_{n=1}^{\infty} a_n \cdot \frac{1}{2\pi i} \int_{c-iT}^{c+iT} \frac{(x/n)^s}{s}\, ds.
$$

Replace each inner integral by $\mathbf{1}_{\{x/n > 1\}} = \mathbf{1}_{\{n < x\}}$ plus its error, controlled by the truncated lemma with $y = x/n$. The main terms sum to $\sum_{n < x} a_n$, which agrees with $\sum'_{n \le x} a_n$ up to the half-weight boundary term absorbed into the convention. The accumulated error is bounded by $\sum_n |a_n| (x/n)^c \min(1, 1/(T|\ln(x/n)|))$, exactly $R(x, T)$. $\square$

**Bridge.** The truncated formula builds toward the contour proof of the prime number theorem, where this is exactly the step that converts $\psi(x) = \sum_{n \le x} \Lambda(n)$ into a contour integral of $-\zeta'(s)/\zeta(s) \cdot x^s/s$, and the asymptotic $\psi(x) \sim x$ appears again in [21.03.01] as the residue of the pole at $s = 1$. The foundational reason the method works is that the discontinuous integral is a switch, and the central insight is that sliding the contour past a pole reads off a main term while the shifted line gives a small remainder — putting these together, the location of the zeros of $\zeta(s)$ controls the size of the error, which generalises to every $L$-function whose logarithmic derivative one wishes to sum. The bridge is the Mellin transform: Perron's formula is Mellin inversion applied to the summatory function, and this is dual to the Dirichlet-series packaging that produced $A(s)$ in the first place.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

For the constant Dirichlet series $A(s) = \zeta(s)$ (all $a_n = 1$), evaluate $\sum'_{n \le x} a_n$ at $x = 7$ (an integer), using the half-weight convention for the boundary term.

<details class="exercise__hint"><summary>Hint</summary>

The integers strictly below $7$ are counted with full weight; the term $n = 7$ is counted with weight $1/2$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$6.5$.** The terms $n = 1, \ldots, 6$ contribute $1$ each (total $6$), and the boundary term $n = 7$ contributes $\tfrac12$. Hence $\sum'_{n \le 7} 1 = 6 + \tfrac12 = 6.5$. The full-weight sum $\sum_{n \le 7} 1 = 7$ differs by exactly $\tfrac12 a_7 = \tfrac12$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise 2 (easy, true-false).**

The integrand $y^s/s$ is absolutely integrable along the vertical line $\operatorname{Re}(s) = c$, so the discontinuous integral converges in the ordinary (not principal-value) sense.

<details class="exercise__hint"><summary>Hint</summary>

Estimate $|y^s/s|$ on the line and compare with $\int dt/|t|$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** On the line $s = c + it$ one has $|y^s/s| = y^c/\sqrt{c^2 + t^2}$, which decays only like $1/|t|$. Since $\int^{\infty} dt/|t|$ diverges, the integrand is not absolutely integrable, and the integral exists only as the symmetric principal value $\lim_{T \to \infty} \int_{c-iT}^{c+iT}$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Prove the discontinuous-integral lemma for $0 < y < 1$: show that $\frac{1}{2\pi i}\int_{c-i\infty}^{c+i\infty} y^s/s\, ds = 0$ for $c > 0$.

<details class="exercise__hint"><summary>Hint</summary>

Close the contour to the right, where $y^{\sigma} \to 0$ as $\sigma \to +\infty$ because $y < 1$. There is no pole in the right half-plane $\operatorname{Re}(s) > c > 0$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Close the vertical segment $[c-iT, c+iT]$ by a large rectangle extending to $\operatorname{Re}(s) = U > c$. The only pole of $y^s/s$ is at $s = 0$, which lies to the *left* of the line $\operatorname{Re}(s) = c > 0$ and hence outside the rectangle; by the residue theorem the closed integral is $0$. On the right side $\operatorname{Re}(s) = U$, $|y^s/s| \le y^U/U \to 0$ as $U \to \infty$ since $y < 1$. The horizontal sides at $\operatorname{Im}(s) = \pm T$ contribute at most $\int_c^U y^{\sigma}\, d\sigma / T \le y^c/(T |\ln y|)$, which tends to $0$ as $T \to \infty$. Therefore the vertical-line integral equals $0$. $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Show that the Mellin transform of $f(x) = e^{-x}$ is $\widetilde{f}(s) = \Gamma(s)$ for $\operatorname{Re}(s) > 0$, and interpret Mellin inversion in this case.

<details class="exercise__hint"><summary>Hint</summary>

Substitute directly into $\int_0^{\infty} f(x) x^{s-1}\,dx$ and recognise the Gamma integral.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By definition, $\widetilde{f}(s) = \int_0^{\infty} e^{-x} x^{s-1}\, dx = \Gamma(s)$, convergent for $\operatorname{Re}(s) > 0$. Mellin inversion then asserts

$$
e^{-x} = \frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} \Gamma(s)\, x^{-s}\, ds
$$

for any $c > 0$. This is the Cahen-Mellin integral, the contour representation of the exponential that underlies the Mellin-Barnes representation of special functions: moving the contour left past the poles of $\Gamma(s)$ at $s = 0, -1, -2, \ldots$ and summing residues $(-1)^k/k!$ recovers the Taylor series $e^{-x} = \sum_k (-x)^k/k!$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 5 (medium, symbolic).**

Choosing $c = 1 + 1/\ln x$ in the truncated Perron formula for $A(s) = \zeta(s)$, show that the factor $x^c$ is bounded by $e \cdot x$.

<details class="exercise__hint"><summary>Hint</summary>

Write $x^c = x \cdot x^{1/\ln x}$ and simplify $x^{1/\ln x}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $x^c = x^{1 + 1/\ln x} = x \cdot x^{1/\ln x}$. Now $x^{1/\ln x} = \exp\!\big(\tfrac{1}{\ln x}\ln x\big) = \exp(1) = e$. Hence $x^c = e\, x$, bounded independently of $x$ by the constant factor $e$. This is precisely why the choice $c = 1 + 1/\ln x$ is standard: it pushes the line just past the abscissa of absolute convergence $\sigma_a = 1$ of $\zeta$, while keeping the size factor $x^c$ at the optimal scale $O(x)$ rather than $O(x^{1+\epsilon})$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 6 (medium, multiple choice).**

In the contour proof of the prime number theorem, which Dirichlet series is fed into Perron's formula to produce the Chebyshev function $\psi(x) = \sum_{n \le x} \Lambda(n)$?

A. $\zeta(s)$

B. $1/\zeta(s)$

C. $-\zeta'(s)/\zeta(s)$

D. $\zeta(s)^2$

<details class="exercise__hint"><summary>Hint</summary>

The von Mangoldt function $\Lambda(n)$ is the coefficient of the Dirichlet series whose value is the logarithmic derivative of $\zeta$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** The identity $-\zeta'(s)/\zeta(s) = \sum_{n=1}^{\infty} \Lambda(n) n^{-s}$ for $\operatorname{Re}(s) > 1$ packages the von Mangoldt function as a Dirichlet series. Feeding $A(s) = -\zeta'(s)/\zeta(s)$ into Perron's formula gives $\psi(x)$ as a contour integral; the pole of $A(s)$ at $s = 1$ (from the zero of $\zeta$'s reciprocal vanishing, i.e. the simple pole of $\zeta$) contributes the main term $x$, yielding $\psi(x) \sim x$. Option A produces a counting function, not the weighted prime sum; B is $\sum \mu(n) n^{-s}$; D is $\sum d(n) n^{-s}$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Using the truncated Perron formula for $A(s) = \zeta(s)$ with $a_n = 1$, derive the classical estimate $\sum_{n \le x} 1 = x + O\big(x^{1+\epsilon}/T\big) + (\text{contour integral})$, identifying which terms of the error sum dominate.

<details class="exercise__hint"><summary>Hint</summary>

Split the error sum $\sum_n (x/n)^c \min(1, 1/(T|\ln(x/n)|))$ into the ranges $n \le x/2$, $x/2 < n < 2x$, and $n \ge 2x$. The middle range, where $|\ln(x/n)|$ is small, is the delicate one.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Take $c = 1 + 1/\ln x$, so $x^c \asymp x$. The error sum is $E = \sum_{n=1}^{\infty} (x/n)^c \min(1, 1/(T|\ln(x/n)|))$.

For $n \le x/2$ or $n \ge 2x$, $|\ln(x/n)| \ge \ln 2$, so each term is $\le (x/n)^c/(T \ln 2)$. Summing $(x/n)^c$ over $n \ge 1$ gives $x^c \zeta(c) \asymp x \cdot \ln x$ (since $\zeta(c) \sim 1/(c-1) = \ln x$ near $c = 1$). This range contributes $O(x \ln x / T)$.

For the near-diagonal range $x/2 < n < 2x$ with $n \ne x$, write $n = x + r$ with $|r| < x$. Then $|\ln(x/n)| = |\ln(1 + r/x)| \asymp |r|/x$, so the term is $\asymp \min(1, x/(T|r|))$. Summing over integer $r$ with $1 \le |r| < x$: the terms with $|r| \le x/T$ contribute $O(x/T \cdot 1) = O(x/T)$ each capped at $1$, totalling $O(x/T)$; the terms with $|r| > x/T$ contribute $\sum x/(T|r|) = O((x/T)\ln T)$. The integer nearest $x$ (if $x$ is close to an integer) gives an extra $O(1)$ from the $\min(1, \cdot)$ cap.

Collecting, $E = O\big(x \ln x / T\big)$, which for any fixed $\epsilon > 0$ is $O(x^{1+\epsilon}/T)$ once $T$ is a fixed power of $x$. Choosing $T = x$ gives error $O(x^{\epsilon})$, recovering the elementary $\sum_{n \le x} 1 = \lfloor x \rfloor = x + O(1)$ up to the contour main term $x$ (the residue at $s = 1$). $\square$

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Derive Perron's formula from Mellin inversion. Specifically, identify the summatory function $\sum'_{n \le x} a_n$ as the inverse Mellin transform of $A(s)/s$ and explain the role of the $1/s$ factor.

<details class="exercise__hint"><summary>Hint</summary>

The Mellin transform of the indicator $\mathbf{1}_{[0,1]}(t)$, suitably scaled, is $1/s$. Convolution in the multiplicative group $\mathbb{R}_{>0}$ becomes multiplication of Mellin transforms.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Let $S(x) = \sum'_{n \le x} a_n$, the right-continuous step function jumping by $a_n$ at each integer $n$. Its Mellin transform, computed via integration by parts (or directly on the jumps), is

$$
\int_0^{\infty} S(x)\, x^{-s-1}\, dx \cdot s = \sum_{n=1}^{\infty} a_n \int_n^{\infty} x^{-s-1}\, dx \cdot s = \sum_{n=1}^{\infty} a_n\, n^{-s} = A(s),
$$

so the Mellin transform of $S$ is $A(s)/s$, valid for $\operatorname{Re}(s) > \sigma_a$. The factor $1/s$ arises because $\int_n^{\infty} x^{-s-1}\, dx = n^{-s}/s$: it is the Mellin transform of the tail-indicator, i.e. integrating the coefficient mass from $n$ to infinity. Applying the Mellin inversion theorem,

$$
S(x) = \frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} \frac{A(s)}{s}\, x^{s}\, ds,
$$

which is exactly Perron's formula. The $1/s$ is thus the signature of the *summatory* (rather than pointwise-coefficient) inversion: it converts the Dirichlet series of the coefficients into the Mellin transform of their running total. $\square$

</details>

</aside>

## Advanced results [Master]

**Effective truncation with smoothing.** The sharp error in the truncated formula can be reduced by integrating against a smooth weight rather than the hard cutoff. Replacing $x^s/s$ by $x^s \cdot \widetilde{w}(s)$ for a Mellin pair $(w, \widetilde{w})$ with $w$ smooth and compactly supported produces a *smoothed* summatory function $\sum_n a_n w(n/x)$ whose contour integral converges absolutely, so no principal value is needed and the error decays faster than any power of $T$. The price is that one recovers $\sum_n a_n w(n/x)$ rather than the sharp partial sum; the difference is controlled by the support width of $w$. This smoothed Perron formula is the modern workhorse in sieve theory and in the study of moments of $L$-functions [ref: TODO_REF Tenenbaum §II.2].

**The explicit formula as a Perron computation.** Feeding $A(s) = -\zeta'(s)/\zeta(s)$ into the full Perron formula and moving the contour leftward past the pole at $s = 1$ and past every nontrivial zero $\rho$ of $\zeta$ produces the Riemann-von Mangoldt explicit formula

$$
\psi(x) = x - \sum_{\rho} \frac{x^{\rho}}{\rho} - \frac{\zeta'(0)}{\zeta(0)} - \tfrac12 \ln(1 - x^{-2}),
$$

valid for non-integer $x > 1$. Each term is a residue: $x$ is the residue at the pole $s = 1$ of $\zeta$, the sum over $\rho$ collects residues at the zeros, and the final terms come from the pole at $s = 0$ and the negative-even zeros [ref: TODO_REF Titchmarsh §3]. The convergence of $\sum_{\rho} x^{\rho}/\rho$ is conditional and must be read symmetrically in $|\operatorname{Im}\rho| \le T$, the same principal-value structure inherited from the discontinuous integral.

**Abscissa shifting and the zero-free region.** The size of $\psi(x) - x$ is governed by how far left the contour can be pushed without crossing a zero. If $\zeta(s) \ne 0$ for $\sigma \ge 1 - c_0/\ln(|t|+2)$, the contour can be moved to $\sigma = 1 - c_0/\ln T$, on which $|x^s/s| \le x^{1 - c_0/\ln T}/|s|$. Balancing the truncation error $x \ln x / T$ against the shifted-line contribution $x \exp(-c_0 \ln x / \ln T)$ and optimising $T$ yields the classical prime number theorem error $\psi(x) = x + O\big(x \exp(-c\sqrt{\ln x})\big)$. The entire mechanism is Perron's formula plus the residue theorem [21.03.01]; the analytic input is the zero-free region.

**Mellin-Barnes and the analytic continuation of integrals.** The discontinuous integral is the simplest Mellin-Barnes integral. The general Mellin-Barnes representation writes a product of Gamma factors $\frac{1}{2\pi i}\int \prod_j \Gamma(a_j + s) \prod_k \Gamma(b_k - s)\, x^{-s}\, ds$ and evaluates it by residue summation, producing hypergeometric functions. Perron's formula is the degenerate case where the only Gamma factor is the pole $1/s = \Gamma(s)/\Gamma(s+1)$ at $s = 0$. This places Perron's formula inside the broader theory of Mellin-Barnes integrals that compute Feynman amplitudes and special-function identities.

**Synthesis.** The truncated Perron formula is the foundational reason analytic number theory can pass from a Dirichlet series back to its coefficients, and the central insight is that the discontinuous integral is a switch whose only singularity is the simple pole at $s = 0$. This is exactly the structure that generalises: each pole one crosses while sliding the contour contributes a residue, so the main term $x$ for $\psi(x)$ is the residue at $\zeta$'s pole, the oscillating corrections are residues at the zeros, and putting these together yields the explicit formula. The bridge is Mellin inversion — Perron's formula is the inverse Mellin transform of $A(s)/s$ — and the method is dual to the Dirichlet-series packaging that built $A(s)$. The same mechanism appears again in [21.03.01] through the zero-free region that bounds the error, and it generalises to every $L$-function whose summatory coefficients one wishes to extract, making Perron's formula the universal engine that converts analytic facts about $A(s)$ into arithmetic facts about $\sum_{n \le x} a_n$.

## Full proof set [Master]

**Proposition (full discontinuous-integral lemma).** *For $c > 0$ and $y > 0$,*

$$
\frac{1}{2\pi i} \int_{c-i\infty}^{c+i\infty} \frac{y^s}{s}\, ds = \begin{cases} 1 & y > 1, \\ \tfrac12 & y = 1, \\ 0 & 0 < y < 1, \end{cases}
$$

*the integral understood as a symmetric principal value.*

*Proof.* For $y > 1$ write $y^s = e^{s \ln y}$ with $\ln y > 0$. On a left-closing rectangle with corners $c \pm iT$ and $-U \pm iT$, the integrand $y^s/s$ has its unique pole at $s = 0$ inside, with residue $\lim_{s \to 0} s \cdot y^s/s = y^0 = 1$. By the residue theorem [06.01.03] the closed contour integral equals $1$. On the horizontal sides $\operatorname{Im}(s) = \pm T$, $|y^s/s| \le y^{\sigma}/T$ and $\int_{-U}^{c} y^{\sigma}\, d\sigma \le y^c/\ln y$, so each contributes $O(y^c/(T \ln y)) \to 0$ as $T \to \infty$. On the left side $\operatorname{Re}(s) = -U$, $|y^s| = y^{-U} \to 0$ as $U \to \infty$, killing that contribution. Hence the vertical integral equals the residue, namely $1$.

For $0 < y < 1$, $\ln y < 0$, so $y^{\sigma}$ decays as $\sigma \to +\infty$; close the rectangle to the right with corners $c \pm iT$, $U \pm iT$. No pole lies inside (the pole $s = 0$ is to the left of $\operatorname{Re}(s) = c > 0$), so the closed integral is $0$, and the same horizontal-side estimates with $|\ln y|$ give the vertical integral the value $0$.

For $y = 1$ the integrand is $1/s$. On the line $s = c + it$, $\frac{1}{2\pi i}\int_{-T}^{T} \frac{1}{c+it}\, i\, dt = \frac{1}{2\pi}\int_{-T}^{T} \frac{dt}{c+it}$. Splitting the real and imaginary parts, the imaginary part is odd in $t$ and cancels, while the real part is $\frac{1}{2\pi}\int_{-T}^{T} \frac{c}{c^2+t^2}\, dt = \frac{1}{2\pi} \cdot 2 \arctan(T/c) \to \frac{1}{2\pi}\cdot \pi = \tfrac12$ as $T \to \infty$. $\square$

**Proposition (term-by-term interchange is valid).** *If $A(s) = \sum_n a_n n^{-s}$ converges absolutely on $\operatorname{Re}(s) = c$, then for each fixed $T$,*

$$
\frac{1}{2\pi i}\int_{c-iT}^{c+iT} A(s)\frac{x^s}{s}\, ds = \sum_{n=1}^{\infty} a_n \cdot \frac{1}{2\pi i}\int_{c-iT}^{c+iT}\frac{(x/n)^s}{s}\, ds.
$$

*Proof.* On the compact segment $s = c + it$, $t \in [-T, T]$, the partial sums $\sum_{n \le N} a_n n^{-s}$ converge uniformly to $A(s)$ because $\big|\sum_{n > N} a_n n^{-s}\big| \le \sum_{n > N} |a_n| n^{-c} \to 0$ independently of $t$. The factor $x^s/s$ is bounded on the segment (it is continuous on a compact set avoiding $s = 0$ when $c > 0$). Uniform convergence of the integrand on a finite-length contour permits interchange of the sum and the integral by the uniform-limit theorem for integrals. $\square$

## Connections [Master]

- **Residue theorem [06.01.03].** Perron's formula is evaluated by exactly the contour-closing argument the residue theorem provides: every value of the discontinuous integral is a residue at $s = 0$, and the prime number theorem main term $x$ is the residue at the pole $s = 1$ of $\zeta$. The residue theorem is the computational engine that turns a vertical contour integral into a sum over poles.

- **Riemann zeta function [06.01.16].** The zeta function is the model Dirichlet series to which Perron's formula is applied, and its Euler product, pole at $s = 1$, and zeros are exactly the analytic data that the contour shift converts into the asymptotics of $\sum_{n \le x} a_n$. Perron's formula is the bridge that carries the analytic structure of $\zeta$ over to the counting of integers and primes.

- **Riemann zeta function, number-theoretic anchor [21.03.01].** The contour proof of the prime number theorem feeds $-\zeta'(s)/\zeta(s)$ into the truncated Perron formula; the zero-free region of $\zeta$ established there is precisely the input that bounds the Perron error term and yields $\psi(x) = x + O(x e^{-c\sqrt{\ln x}})$. The explicit formula is a Perron computation read as a sum of residues at the zeros.

## Historical & philosophical context [Master]

Oskar Perron published the formula bearing his name in 1908 in *Crelle's Journal* [ref: TODO_REF Perron 1908], in a paper on the general theory of Dirichlet series. The contour-integral idea was older: Riemann's 1859 memoir had already represented prime-counting functions by contour integrals of $\zeta$, and the formula is in this sense a precise and general statement of a method Riemann had used in a special case. Perron's contribution was to state the inversion as a theorem about an arbitrary Dirichlet series and to isolate the discontinuous integral as its analytic core. The systematic theory of the Mellin transform on which the inversion rests was developed by Hjalmar Mellin in the same period, culminating in his 1910 *Mathematische Annalen* synthesis [ref: TODO_REF Mellin 1910], which placed the Gamma function, hypergeometric functions, and the inversion integral in a single framework.

The formula became the standard engine of multiplicative number theory through its treatment in Landau's 1909 *Handbuch* and, in the modern canon, in Davenport's *Multiplicative Number Theory* and Montgomery-Vaughan's text, where the truncated form with explicit error term is the version used to prove the prime number theorem with error. The discontinuous integral itself dates to Cahen's 1894 thesis on Dirichlet series; Cahen's evaluation contained a gap that Perron and later Landau repaired. The truncated form, with its $\min(1, 1/(T|\ln(x/n)|))$ error, is the practical instrument: an exact infinite contour integral admits no asymptotic estimate, while a finite segment of length $2T$ can be balanced against the analytic continuation of the series past its abscissa of convergence.

## Bibliography [Master]

```bibtex
@article{Perron1908,
  author = {Perron, Oskar},
  title = {Zur Theorie der Dirichletschen Reihen},
  journal = {Journal f\"ur die reine und angewandte Mathematik},
  volume = {134},
  year = {1908},
  pages = {95--143},
  note = {Original statement of Perron's formula for Dirichlet series}
}

@article{Mellin1910,
  author = {Mellin, Hjalmar},
  title = {Abriss einer einheitlichen Theorie der Gamma- und der hypergeometrischen Funktionen},
  journal = {Mathematische Annalen},
  volume = {68},
  year = {1910},
  pages = {305--337},
  note = {Systematic theory of the Mellin transform and its inversion}
}

@book{Davenport2000,
  author = {Davenport, Harold},
  title = {Multiplicative Number Theory},
  edition = {3},
  publisher = {Springer},
  series = {Graduate Texts in Mathematics},
  volume = {74},
  year = {2000},
  note = {Revised by H. L. Montgomery. \S11-12: Perron's formula and the contour proof of PNT}
}

@book{MontgomeryVaughan2007,
  author = {Montgomery, Hugh L. and Vaughan, Robert C.},
  title = {Multiplicative Number Theory I: Classical Theory},
  publisher = {Cambridge University Press},
  series = {Cambridge Studies in Advanced Mathematics},
  volume = {97},
  year = {2007},
  note = {\S5.1: Perron's formula, Mellin inversion, the discontinuous integral}
}

@book{Titchmarsh1986,
  author = {Titchmarsh, E. C.},
  title = {The Theory of the Riemann Zeta-Function},
  edition = {2},
  publisher = {Oxford University Press},
  year = {1986},
  note = {Revised by D. R. Heath-Brown. \S3: explicit formula, truncated Perron}
}

@book{Tenenbaum2015,
  author = {Tenenbaum, G\'erald},
  title = {Introduction to Analytic and Probabilistic Number Theory},
  edition = {3},
  publisher = {American Mathematical Society},
  series = {Graduate Studies in Mathematics},
  volume = {163},
  year = {2015},
  note = {\S II.2: Mellin transform, Perron's formula, effective truncation}
}
```
