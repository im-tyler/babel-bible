---
id: 02.15.02
title: "The Itô integral and Itô's formula"
slug: the-ito-integral-and-itos-formula
section: analysis
chapter: stochastic-analysis
concept_catalog_id: analysis.ito-calculus
prerequisites:
  - 02.07.04  # Lebesgue integral, monotone convergence
  - 02.07.06  # Lp spaces, Hölder, Minkowski, completeness
successors: []
tier_anchors:
  beginner: "Øksendal — Stochastic Differential Equations Ch. 3 (informal Itô integral); Evans — An Introduction to Stochastic Differential Equations Ch. 4"
  intermediate: "Øksendal — Stochastic Differential Equations (6th ed.) Ch. 3-4; Evans — SDE Ch. 4-5 (Itô isometry, Itô's formula)"
  master: "Karatzas–Shreve — Brownian Motion and Stochastic Calculus Ch. 3; Revuz–Yor — Continuous Martingales and Brownian Motion Ch. IV; Protter — Stochastic Integration and Differential Equations Ch. II"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Karatzas, Shreve — Brownian Motion and Stochastic Calculus (Springer GTM 113, 2nd ed., 1991)"
    locator: "Ch. 3 §3.1-3.3 (construction of the Itô integral, Itô isometry), §3.3 (Itô's formula), §3.3.D (multidimensional rule)"
  - source: TODO_REF
    path: "Øksendal — Stochastic Differential Equations: An Introduction with Applications (Springer, 6th ed., 2003)"
    locator: "Ch. 3 (Itô integral, isometry), Ch. 4 (Itô formula, geometric Brownian motion, exponential martingale)"
  - source: TODO_REF
    path: "Revuz, Yor — Continuous Martingales and Brownian Motion (Springer Grundlehren 293, 3rd ed., 1999)"
    locator: "Ch. IV (stochastic integration, quadratic variation, Itô's formula), §IV.4 (Burkholder-Davis-Gundy inequalities)"
  - source: TODO_REF
    path: "Itô — Stochastic Integral (Proc. Imperial Academy Tokyo 20, 1944)"
    locator: "pp. 519-524; the original definition of the stochastic integral and the change-of-variables correction term"
lean_status: none
lean_mathlib_gap: |
  Mathlib has measure theory, the Lebesgue and Bochner integrals, $L^p$ spaces,
  conditional expectation, and a developing martingale library (discrete-time
  martingales, optional stopping, Doob's maximal inequality), but it does not
  yet contain a continuous-time stochastic integral. Specifically there is no
  `IsItoIntegral` or `stochasticIntegral` against a continuous semimartingale,
  no Itô isometry packaging the $L^2$ extension from simple predictable
  integrands, no quadratic-variation process `[X]` or covariation `[X, Y]` for
  continuous local martingales, and no Itô change-of-variables lemma
  `f(B_t) = f(B_0) + \int f'(B_s)\,dB_s + \tfrac12 \int f''(B_s)\,ds`. The
  predictable $\sigma$-algebra and progressive measurability are partly present
  through the filtration API, so the simple-integrand stage is reachable, but
  the completion argument, the continuous-martingale property of the integral,
  and the Burkholder-Davis-Gundy inequalities are all formalisation targets.
human_reviewer: "Tyler (pending external probability reviewer)"
estimated_time:
  beginner: 18m
  intermediate: 45m
  master: 85m
status: shipped
produced_by: claude-opus-4-8 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Ordinary integration adds up a quantity that changes along a smooth path: how far you travel is your speed summed over time. Stochastic integration asks the same kind of question, but the path you are summing against is a jittery random walk — the trajectory of a particle bumped around by countless tiny collisions. You hold some amount of a risky asset, the asset price jiggles randomly, and you want the total gain. The amount you hold can itself change as you watch the price move.

The catch is that the random path is far too rough to handle the usual way. Over any time interval, however short, the path wiggles up and down without settling, and its total back-and-forth length is infinite. So we cannot treat the random increments as ordinary small steps and add them with the familiar rules.

The fix is a rule about timing: you must decide how much to hold *before* you see the next random jump, never after. That single honesty condition — no peeking ahead — is what makes the whole construction work and what separates this integral from every integral that came before it.

## Visual [Beginner]

Picture a jagged random path climbing and falling across the page from left to right: the running position of a particle in random motion. Below it, a second staircase shows how much of the asset you are holding, flat over each short time block and then jumping to a new flat level at the start of the next block.

![A jagged random path across time, with a step-function holding strategy beneath it that stays flat over each time block and jumps only at the left edge of the next block, before the path's next move is revealed.](/img/the-ito-integral-and-itos-formula-placeholder.svg)

Each block contributes one term: your fixed holding over that block, multiplied by the net change in the random path across that block. Because your holding for a block is locked in at its left edge — before the path moves through the block — you never get to use future information. Summing these block contributions gives the total gain. As the blocks shrink toward zero width, the staircase sums settle onto a single number: the stochastic integral.

## Worked example [Beginner]

Take the simplest holding: hold exactly one unit at all times, from time $0$ to time $T$. The random path starts at $0$. Split $[0, T]$ into blocks at times $0 = t_0 < t_1 < \cdots < t_n = T$. Over block $k$ the path changes by $B(t_{k+1}) - B(t_k)$, and your holding is $1$, so block $k$ contributes that change. Adding all blocks gives $B(t_1) - B(t_0) + B(t_2) - B(t_1) + \cdots = B(T) - B(0) = B(T)$. The total gain from holding one unit is just the net displacement $B(T)$, exactly as you would hope.

Now hold the *current path value* instead: at the left edge of block $k$ you hold $B(t_k)$. Block $k$ then contributes $B(t_k)\,(B(t_{k+1}) - B(t_k))$. A short algebra rearrangement of the telescoping total gives one half of $B(T)^2$ minus one half of the added-up squared changes across all the blocks. The squared changes do **not** vanish as blocks shrink: their running total settles to $T$. So the total gain is $\tfrac12 B(T)^2 - \tfrac12 T$, not the $\tfrac12 B(T)^2$ that ordinary calculus would predict. That stubborn extra $-\tfrac12 T$ is the signature of the whole subject: the random path is so rough that its squared wiggles add up to real time.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

Why must the holding over each time block be fixed *before* the random path moves through that block?

A. To make the arithmetic shorter  
B. So the strategy never uses information from the future  
C. Because the path is smooth  
D. To force the total gain to be zero

<details class="exercise__hint"><summary>Hint</summary>

Think about what would go wrong if you could choose how much to hold after already seeing the jump.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. So the strategy never uses information from the future.** Locking the holding in at the start of each block enforces the no-peeking rule. If you could set your holding after seeing the jump, you could always bet on a rise you have already observed, and the integral would no longer model an honest trading or prediction strategy.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

True or false: holding the current path value and integrating gives $\tfrac12 B(T)^2$, exactly as ordinary calculus would predict for integrating $x$ against $dx$.

<details class="exercise__hint"><summary>Hint</summary>

Recall the stubborn extra term from the worked example, coming from the squared changes.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**False.** The answer is $\tfrac12 B(T)^2 - \tfrac12 T$. The squared changes of the random path add up to $T$ rather than vanishing, producing the correction $-\tfrac12 T$. This is the central difference between stochastic and ordinary integration.

</details>

</aside>

## Formal definition [Intermediate+]

Fix a probability space $(\Omega, \mathcal{F}, \mathbb{P})$ with a filtration $(\mathcal{F}_t)_{t \ge 0}$ satisfying the usual conditions, and let $(B_t)_{t \ge 0}$ be a one-dimensional Brownian motion adapted to this filtration, with increments $B_t - B_s$ independent of $\mathcal{F}_s$ for $s < t$ (the process constructed in [02.15.01]). An integrand $(H_t)$ is **simple predictable** when there exist deterministic times $0 = t_0 < t_1 < \cdots < t_n = T$ and bounded $\mathcal{F}_{t_k}$-measurable random variables $\xi_k$ with
$$
H_t(\omega) = \sum_{k=0}^{n-1} \xi_k(\omega)\, \mathbf{1}_{(t_k, t_{k+1}]}(t).
$$
The measurability of $\xi_k$ with respect to $\mathcal{F}_{t_k}$ is the non-anticipation requirement. For such $H$ the **Itô integral** is defined pathwise by the finite sum
$$
\int_0^T H_s\, dB_s := \sum_{k=0}^{n-1} \xi_k\,(B_{t_{k+1}} - B_{t_k}),
$$
evaluating each integrand at the *left* endpoint of its block. The left-endpoint choice is the convention that makes the construction a martingale; the right-endpoint or midpoint choices give different integrals, the latter being the Stratonovich integral of [02.15.05].

The defining estimate is the **Itô isometry**: for simple predictable $H$ with $\mathbb{E}\!\int_0^T H_s^2\, ds < \infty$,
$$
\mathbb{E}\!\left[\left(\int_0^T H_s\, dB_s\right)^{\!2}\right] = \mathbb{E}\!\int_0^T H_s^2\, ds.
$$
This identity says the map $H \mapsto \int_0^T H\, dB$ sends the inner-product space of simple integrands isometrically into $L^2(\Omega)$. The class $\mathcal{L}^2_T$ of progressively measurable integrands with $\mathbb{E}\!\int_0^T H_s^2\, ds < \infty$ is the completion of the simple integrands under the norm $\|H\| = (\mathbb{E}\!\int_0^T H_s^2\, ds)^{1/2}$; the isometry extends the integral uniquely and continuously to all of $\mathcal{L}^2_T$, since a complete target space ($L^2(\Omega)$, by completeness from [02.07.06]) receives the limit of any Cauchy sequence of integrals.

The **quadratic variation** of a continuous process $(X_t)$ is the limit in probability
$$
[X]_t = \lim_{\|\Pi\| \to 0} \sum_{k} (X_{t_{k+1}} - X_{t_k})^2
$$
over partitions $\Pi$ of $[0, t]$ with mesh $\|\Pi\| \to 0$; for Brownian motion $[B]_t = t$. The **covariation** of two processes is the polarisation $[X, Y]_t = \tfrac12([X+Y]_t - [X]_t - [Y]_t)$, so $[B]_t = [B, B]_t$. The heuristic $dB_t\, dB_t = dt$, $dB_t\, dt = 0$, $dt\, dt = 0$ encodes these facts and drives every Itô computation below.

### Counterexamples to common slips

- Evaluating the integrand at the right endpoint $\xi_k = B_{t_{k+1}}$ instead of the left destroys the martingale property and shifts the answer by the quadratic variation; this is not the Itô integral.
- The quadratic variation $[B]_t = t$ is not zero. For an ordinary differentiable path the analogous limit *is* zero, so importing the smooth-path intuition that squared increments are negligible is the standard error.
- The Itô isometry requires $H$ to be non-anticipating. For an integrand that peeks ahead, $\mathbb{E}[\xi_k (B_{t_{k+1}} - B_{t_k})]$ need not vanish and the isometry fails.

## Key theorem with proof [Intermediate+]

**Theorem (Itô's formula, one-dimensional).** *Let $f \in C^2(\mathbb{R})$ and let $B$ be Brownian motion. Then for every $t \ge 0$, almost surely,*
$$
f(B_t) = f(B_0) + \int_0^t f'(B_s)\, dB_s + \frac{1}{2}\int_0^t f''(B_s)\, ds.
$$

**Proof.** Fix $t$ and a partition $0 = t_0 < \cdots < t_n = t$ with mesh tending to zero. A second-order Taylor expansion of $f$ between consecutive points gives, for each $k$,
$$
f(B_{t_{k+1}}) - f(B_{t_k}) = f'(B_{t_k})\,\Delta_k B + \tfrac12 f''(B_{t_k})\,(\Delta_k B)^2 + R_k,
$$
where $\Delta_k B = B_{t_{k+1}} - B_{t_k}$ and the remainder satisfies $|R_k| \le \tfrac12\, \omega(|\Delta_k B|)\,(\Delta_k B)^2$ with $\omega$ the modulus of continuity of $f''$ on the (almost surely bounded) range of $B$ over $[0,t]$. Summing over $k$ telescopes the left side to $f(B_t) - f(B_0)$.

The first sum $\sum_k f'(B_{t_k})\,\Delta_k B$ is the Itô-integral approximation of $\int_0^t f'(B_s)\, dB_s$; since $f'(B)$ is continuous and adapted, hence in $\mathcal{L}^2$ on $[0,t]$, this converges in $L^2(\mathbb{P})$ to that integral as the mesh shrinks.

For the second sum, replace $(\Delta_k B)^2$ by $\Delta_k t = t_{k+1} - t_k$ and control the difference. Write
$$
\sum_k f''(B_{t_k})(\Delta_k B)^2 = \sum_k f''(B_{t_k})\,\Delta_k t + \sum_k f''(B_{t_k})\big((\Delta_k B)^2 - \Delta_k t\big).
$$
The first piece is a Riemann sum converging almost surely to $\int_0^t f''(B_s)\, ds$ by continuity of $s \mapsto f''(B_s)$. For the second piece, the terms $(\Delta_k B)^2 - \Delta_k t$ are conditionally centred — $\mathbb{E}[(\Delta_k B)^2 - \Delta_k t \mid \mathcal{F}_{t_k}] = 0$ — and conditionally uncorrelated across blocks, so the second moment of the sum is $\sum_k \mathbb{E}[f''(B_{t_k})^2]\,\mathbb{E}[((\Delta_k B)^2 - \Delta_k t)^2] = \sum_k \mathbb{E}[f''(B_{t_k})^2]\cdot 2(\Delta_k t)^2$, using $\mathbb{E}[(\Delta_k B)^2 - \Delta_k t)^2] = 2(\Delta_k t)^2$ for a Gaussian increment. This is bounded by $2\,\|f''\|_{\infty,[0,t]}^2 \cdot \|\Pi\|\cdot t \to 0$. Hence the second piece vanishes in $L^2$.

Finally $\sum_k R_k \to 0$ because $\omega(|\Delta_k B|) \to 0$ uniformly (the path is uniformly continuous on $[0,t]$) while $\sum_k (\Delta_k B)^2 \to t$ stays bounded. Collecting the limits gives the stated identity, with the factor $\tfrac12$ on the second integral coming directly from the second-order Taylor coefficient. $\square$

**Bridge.** Itô's formula builds toward the entire theory of stochastic differential equations and their links to partial differential equations, and the same correction term appears again in the multidimensional and time-dependent versions below. The foundational reason for the extra $\tfrac12 \int f''\, ds$ is that quadratic variation does not vanish: $[B]_t = t$ forces the second-order Taylor term to survive in the limit, which is exactly the heuristic $dB^2 = dt$ made into a theorem. This is exactly the mechanism by which the worked example produced $\int_0^t B\, dB = \tfrac12 B_t^2 - \tfrac12 t$ rather than $\tfrac12 B_t^2$, and it generalises the ordinary chain rule, to which it reduces whenever the integrating path has zero quadratic variation. Putting these together, the bridge is that Itô's formula converts the analytic operator $\tfrac12 f''$ into the generator of Brownian motion, so that harmonic and heat-equation theory transfer wholesale to the probabilistic setting — the central insight that organises stochastic analysis and reappears in [02.15.03] as the link between SDEs and second-order PDEs.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 1 (easy, symbolic).**

Apply Itô's formula to $f(x) = x^2$ to evaluate $\int_0^t B_s\, dB_s$.

<details class="exercise__hint"><summary>Hint</summary>

Here $f'(x) = 2x$ and $f''(x) = 2$. Write out the formula and solve for the stochastic integral.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Itô's formula gives $B_t^2 = 0 + \int_0^t 2B_s\, dB_s + \tfrac12 \int_0^t 2\, ds = 2\int_0^t B_s\, dB_s + t$. Solving, $\int_0^t B_s\, dB_s = \tfrac12 (B_t^2 - t)$. Rubric: full credit for correct $f', f''$ substitution and the algebraic isolation matching the worked example.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="symbolic" markdown="1">

**Exercise 2 (easy, symbolic).**

Compute $\mathbb{E}\!\left[\left(\int_0^t B_s\, dB_s\right)^2\right]$ using the Itô isometry.

<details class="exercise__hint"><summary>Hint</summary>

The integrand is $H_s = B_s$, so the isometry gives $\mathbb{E}\int_0^t B_s^2\, ds$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By the isometry, $\mathbb{E}[(\int_0^t B_s\, dB_s)^2] = \mathbb{E}\int_0^t B_s^2\, ds = \int_0^t \mathbb{E}[B_s^2]\, ds = \int_0^t s\, ds = t^2/2$. As a check, $\tfrac12(B_t^2 - t)$ has second moment $\tfrac14(\mathbb{E}[B_t^4] - 2t\,\mathbb{E}[B_t^2] + t^2) = \tfrac14(3t^2 - 2t^2 + t^2) = t^2/2$. Rubric: full credit for the isometry application and the Fubini evaluation.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 3 (medium, symbolic).**

Let $X_t = \exp(\sigma B_t - \tfrac12 \sigma^2 t)$. Use Itô's formula on $f(t, x) = \exp(\sigma x - \tfrac12 \sigma^2 t)$ to show $dX_t = \sigma X_t\, dB_t$, and conclude $X$ is a martingale.

<details class="exercise__hint"><summary>Hint</summary>

Compute $\partial_t f$, $\partial_x f$, $\partial_{xx} f$ and assemble $df = \partial_t f\, dt + \partial_x f\, dB + \tfrac12 \partial_{xx} f\, dt$. The $dt$ terms should cancel.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Here $\partial_t f = -\tfrac12 \sigma^2 f$, $\partial_x f = \sigma f$, $\partial_{xx} f = \sigma^2 f$. The time-dependent Itô formula gives $dX = (-\tfrac12 \sigma^2 f)\, dt + \sigma f\, dB + \tfrac12 \sigma^2 f\, dt = \sigma f\, dB = \sigma X\, dB$. The drift cancels exactly, so $X$ has no $dt$ part and is a stochastic integral against $B$, hence a local martingale; the integrand $\sigma X$ is square-integrable on bounded intervals, so $X$ is a genuine martingale. Rubric: full credit for the three derivatives, the drift cancellation, and the martingale conclusion.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 4 (medium, symbolic).**

Geometric Brownian motion solves $dS_t = \mu S_t\, dt + \sigma S_t\, dB_t$ with $S_0 > 0$. Apply Itô's formula to $\log S_t$ and solve for $S_t$ explicitly.

<details class="exercise__hint"><summary>Hint</summary>

With $g(x) = \log x$, $g'(x) = 1/x$, $g''(x) = -1/x^2$. Use $d\log S = g'(S)\, dS + \tfrac12 g''(S)\, d[S]$ and $d[S]_t = \sigma^2 S_t^2\, dt$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$d\log S_t = \tfrac1{S_t}\, dS_t - \tfrac1{2 S_t^2}\, d[S]_t = (\mu\, dt + \sigma\, dB_t) - \tfrac12 \sigma^2\, dt = (\mu - \tfrac12 \sigma^2)\, dt + \sigma\, dB_t$. Integrating, $\log S_t = \log S_0 + (\mu - \tfrac12\sigma^2)t + \sigma B_t$, so $S_t = S_0 \exp((\mu - \tfrac12 \sigma^2)t + \sigma B_t)$. The $-\tfrac12 \sigma^2 t$ correction in the exponent is the Itô term. Rubric: full credit for the log transform, the quadratic-variation term, and the explicit exponential.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 5 (medium, short-answer).**

Show that $M_t = B_t^3 - 3\int_0^t B_s\, ds$ is a martingale by exhibiting it as a stochastic integral against $B$.

<details class="exercise__hint"><summary>Hint</summary>

Apply Itô's formula to $f(x) = x^3$ and rearrange so that the $ds$ integral is moved to the left side.

</details>

<details class="exercise__answer"><summary>Answer</summary>

With $f(x) = x^3$, $f' = 3x^2$, $f'' = 6x$: $B_t^3 = \int_0^t 3B_s^2\, dB_s + \tfrac12 \int_0^t 6 B_s\, ds = \int_0^t 3 B_s^2\, dB_s + 3\int_0^t B_s\, ds$. Hence $M_t = B_t^3 - 3\int_0^t B_s\, ds = \int_0^t 3 B_s^2\, dB_s$, a stochastic integral with square-integrable integrand on bounded intervals, therefore a martingale. Rubric: full credit for the Itô expansion and the identification of $M$ as a pure Itô integral.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 6 (hard, short-answer).**

Prove the integration-by-parts (product) rule $d(X_t Y_t) = X_t\, dY_t + Y_t\, dX_t + d[X, Y]_t$ for two Itô processes, and specialise to $X = Y = B$ to recover $\int_0^t B\, dB = \tfrac12(B_t^2 - t)$.

<details class="exercise__hint"><summary>Hint</summary>

Apply the two-dimensional Itô formula to $f(x, y) = xy$, whose only nonzero second derivative is $\partial_{xy} f = 1$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

For $f(x,y) = xy$ the multidimensional Itô formula gives $d(X Y) = \partial_x f\, dX + \partial_y f\, dY + \tfrac12(\partial_{xx} f\, d[X] + 2\partial_{xy} f\, d[X,Y] + \partial_{yy} f\, d[Y])$. Since $\partial_x f = y$, $\partial_y f = x$, $\partial_{xx} f = \partial_{yy} f = 0$, $\partial_{xy} f = 1$, this is $d(XY) = Y\, dX + X\, dY + d[X,Y]$. With $X = Y = B$, $[B,B]_t = t$, so $d(B^2) = 2B\, dB + dt$; integrating, $B_t^2 = 2\int_0^t B\, dB + t$, giving $\int_0^t B\, dB = \tfrac12(B_t^2 - t)$. Rubric: full credit for the product-rule derivation via $f = xy$ and the specialisation reproducing the worked example.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 7 (hard, short-answer).**

Let $u(t, x)$ solve the backward heat equation $\partial_t u + \tfrac12 \partial_{xx} u = 0$ on $[0, T] \times \mathbb{R}$. Show that $u(t, B_t)$ is a martingale, and deduce the Feynman-Kac representation $u(0, x) = \mathbb{E}[u(T, B_T) \mid B_0 = x]$.

<details class="exercise__hint"><summary>Hint</summary>

Apply the time-dependent Itô formula to $u(t, B_t)$ and use the PDE to cancel all $dt$ terms.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By the time-dependent Itô formula, $du(t, B_t) = (\partial_t u + \tfrac12 \partial_{xx} u)\, dt + \partial_x u\, dB_t$. The PDE makes the $dt$ coefficient vanish, leaving $du(t, B_t) = \partial_x u(t, B_t)\, dB_t$, a stochastic integral and hence (under integrability of $\partial_x u(t, B_t)$) a martingale. The martingale property between times $0$ and $T$ gives $u(0, x) = \mathbb{E}[u(0, B_0)] = \mathbb{E}[u(T, B_T) \mid B_0 = x]$. Rubric: full credit for the drift cancellation via the PDE and the martingale-to-expectation step. This is the prototype of the SDE-PDE correspondence.

</details>

</aside>

## Advanced results [Master]

The Itô integral against Brownian motion is a continuous $L^2$-bounded martingale: for $H \in \mathcal{L}^2_T$ the process $I_t(H) = \int_0^t H_s\, dB_s$, $0 \le t \le T$, admits a continuous modification, is a martingale with $\mathbb{E}[I_t(H)] = 0$, and has quadratic variation $[I(H)]_t = \int_0^t H_s^2\, ds$. The isometry is the case $t = T$ of $\mathbb{E}[I_t(H)^2] = \mathbb{E}\!\int_0^t H_s^2\, ds$, itself the statement that $I_t(H)^2 - \int_0^t H_s^2\, ds$ is a martingale. This identification of the integral's quadratic variation with the time-integral of the squared integrand is the foundational reason the whole calculus closes on itself: differentiating an Itô integral returns its integrand, and squaring returns the $ds$-clock that drives every correction term.

Itô's formula extends to continuous semimartingales. For an Itô process $dX_t = b_t\, dt + \sigma_t\, dB_t$ with $b, \sigma$ progressively measurable and locally square-integrable, and $f \in C^{1,2}([0,\infty) \times \mathbb{R})$,
$$
df(t, X_t) = \partial_t f\, dt + \partial_x f\, dX_t + \tfrac12 \partial_{xx} f\, d[X]_t, \qquad d[X]_t = \sigma_t^2\, dt.
$$
The multidimensional version, for $X_t \in \mathbb{R}^n$ driven by an $m$-dimensional Brownian motion through $dX_t = b_t\, dt + \sigma_t\, dB_t$ with $\sigma_t \in \mathbb{R}^{n \times m}$, reads
$$
df(t, X_t) = \partial_t f\, dt + \nabla f \cdot dX_t + \tfrac12 \operatorname{tr}\!\big(\sigma_t \sigma_t^{\mathsf T} D^2 f\big)\, dt,
$$
with $D^2 f$ the Hessian. The trace term is the contraction of the Hessian against the diffusion matrix $a_t = \sigma_t \sigma_t^{\mathsf T}$; it is the multidimensional avatar of the $\tfrac12 f''$ correction and identifies $\tfrac12 \operatorname{tr}(a\, D^2 \cdot) + b\cdot\nabla$ as the generator of the diffusion $X$.

The exponential martingale is the universal example. For an adapted $\theta$ with $\mathbb{E}\!\int_0^T \theta_s^2\, ds < \infty$, the Doléans-Dade exponential
$$
\mathcal{E}(\theta)_t = \exp\!\Big(\int_0^t \theta_s\, dB_s - \tfrac12 \int_0^t \theta_s^2\, ds\Big)
$$
satisfies $d\mathcal{E} = \theta\, \mathcal{E}\, dB$, so it is a local martingale; under Novikov's condition $\mathbb{E}[\exp(\tfrac12 \int_0^T \theta_s^2\, ds)] < \infty$ it is a true martingale, and it is the density process of the Girsanov change of measure that removes drift. The $-\tfrac12 \int \theta^2\, ds$ in the exponent is precisely the Itô correction, the same term that appeared in geometric Brownian motion.

The **Burkholder-Davis-Gundy inequalities** control the running maximum of a continuous local martingale $M$ with $M_0 = 0$ by its quadratic variation: for every $p > 0$ there are universal constants $0 < c_p \le C_p < \infty$ with
$$
c_p\, \mathbb{E}\big[[M]_T^{\,p/2}\big] \le \mathbb{E}\Big[\sup_{0 \le t \le T} |M_t|^p\Big] \le C_p\, \mathbb{E}\big[[M]_T^{\,p/2}\big].
$$
For $p = 2$ the upper bound is Doob's $L^2$ inequality combined with the isometry; the general case is the quantitative backbone of $L^p$ estimates for stochastic integrals and of existence-uniqueness theory for SDEs.

**Synthesis.** The foundational reason Itô calculus exists as a closed system is the identity $[I(H)]_t = \int_0^t H_s^2\, ds$: it says the quadratic variation of a stochastic integral is the time-integral of its squared integrand, and this is exactly the bookkeeping that turns the heuristic $dB^2 = dt$ into operative calculus. Putting these together, the one-dimensional correction $\tfrac12 f''$, the multidimensional trace term $\tfrac12 \operatorname{tr}(a D^2 f)$, and the exponential-martingale drift $-\tfrac12 \int \theta^2$ are one phenomenon wearing three costumes — each is the second-order Taylor remainder that survives because quadratic variation does not vanish. This is exactly the central insight that the generator of a diffusion is a second-order operator, which generalises the ordinary chain rule and is dual to the forward Kolmogorov (Fokker-Planck) evolution of the law of $X$. The Burkholder-Davis-Gundy inequalities then make the quadratic variation the universal yardstick: control of $[M]$ controls every $L^p$ norm of the path, and this is the bridge from the algebra of the differential rules to the analysis of existence, uniqueness, and convergence for stochastic differential equations in [02.15.03].

## Full proof set [Master]

The one-dimensional Itô formula and its partition argument are proved in full in the Key theorem section. The remaining Master claims are recorded here.

**Proposition (Itô isometry and the $L^2$ extension).** *For simple predictable $H$, $\mathbb{E}[(\int_0^T H\, dB)^2] = \mathbb{E}\int_0^T H^2\, ds$. Consequently the integral extends uniquely to a linear isometry $\mathcal{L}^2_T \to L^2(\Omega)$.*

*Proof.* Write $\int_0^T H\, dB = \sum_k \xi_k\, \Delta_k B$ with $\Delta_k B = B_{t_{k+1}} - B_{t_k}$. Expanding the square,
$$
\mathbb{E}\Big[\Big(\sum_k \xi_k \Delta_k B\Big)^2\Big] = \sum_{j,k} \mathbb{E}[\xi_j \xi_k\, \Delta_j B\, \Delta_k B].
$$
For $j < k$, condition on $\mathcal{F}_{t_k}$: the factor $\xi_j \xi_k \Delta_j B$ is $\mathcal{F}_{t_k}$-measurable and $\Delta_k B$ is independent of $\mathcal{F}_{t_k}$ with mean zero, so $\mathbb{E}[\xi_j \xi_k \Delta_j B\, \Delta_k B] = \mathbb{E}[\xi_j \xi_k \Delta_j B]\,\mathbb{E}[\Delta_k B] = 0$; the same holds for $j > k$ by symmetry. The diagonal terms give $\mathbb{E}[\xi_k^2 (\Delta_k B)^2] = \mathbb{E}[\xi_k^2]\,\mathbb{E}[(\Delta_k B)^2] = \mathbb{E}[\xi_k^2]\,\Delta_k t$, again by independence of the increment from $\mathcal{F}_{t_k}$. Summing, $\sum_k \mathbb{E}[\xi_k^2]\,\Delta_k t = \mathbb{E}\int_0^T H_s^2\, ds$. The isometry holds. Since the simple integrands are dense in $\mathcal{L}^2_T$ and $L^2(\Omega)$ is complete by [02.07.06], the isometric map sends Cauchy sequences to Cauchy sequences and extends uniquely and continuously to the completion. $\square$

**Proposition (the integral is a continuous martingale).** *For $H \in \mathcal{L}^2_T$ the process $I_t(H) = \int_0^t H\, dB$ is a martingale with respect to $(\mathcal{F}_t)$ and admits a continuous modification.*

*Proof.* For simple $H$ and $s < t$, the increment $I_t(H) - I_s(H)$ is a sum of terms $\xi_k \Delta_k B$ over blocks past $s$; each has $\mathbb{E}[\xi_k \Delta_k B \mid \mathcal{F}_s] = \mathbb{E}[\xi_k\, \mathbb{E}[\Delta_k B \mid \mathcal{F}_{t_k}] \mid \mathcal{F}_s] = 0$ by the tower property and mean-zero independent increments, so $\mathbb{E}[I_t(H) \mid \mathcal{F}_s] = I_s(H)$. The martingale property passes to the $L^2$ limit because conditional expectation is an $L^2$-contraction. Continuity of the simple-integrand process is plain (it is piecewise a multiple of continuous $B$); for general $H$, take simple $H^{(n)} \to H$ in $\mathcal{L}^2$, and Doob's maximal inequality gives $\mathbb{E}[\sup_{t \le T} |I_t(H^{(n)}) - I_t(H^{(m)})|^2] \le 4\,\mathbb{E}\int_0^T (H^{(n)} - H^{(m)})^2\, ds \to 0$, so a subsequence converges uniformly almost surely, and the uniform limit of continuous paths is continuous. $\square$

**Proposition (quadratic variation of the integral).** *For $H \in \mathcal{L}^2_T$, $I_t(H)^2 - \int_0^t H_s^2\, ds$ is a martingale; equivalently $[I(H)]_t = \int_0^t H_s^2\, ds$.*

*Proof.* It suffices to show $\mathbb{E}[(I_t - I_s)^2 \mid \mathcal{F}_s] = \mathbb{E}[\int_s^t H^2\, du \mid \mathcal{F}_s]$ for $s < t$, since then $I_t^2 - \int_0^t H^2 = I_s^2 - \int_0^s H^2 + (I_t - I_s)^2 - \int_s^t H^2 + 2 I_s(I_t - I_s)$ has $\mathcal{F}_s$-conditional expectation $I_s^2 - \int_0^s H^2$, the martingale identity (the cross term vanishes by the martingale property of $I$). The displayed equality is the Itô isometry applied to the integral of $H\,\mathbf{1}_{(s,t]}$ against $B$ conditioned on $\mathcal{F}_s$, which holds blockwise for simple integrands by the diagonal computation of the first proposition and extends by the $L^2$-limit. By the characterisation of quadratic variation as the unique continuous increasing process $A$ with $I^2 - A$ a martingale, $[I(H)]_t = \int_0^t H_s^2\, ds$. $\square$

**Proposition (multidimensional Itô formula).** *For $X_t \in \mathbb{R}^n$ with $dX_t = b_t\, dt + \sigma_t\, dB_t$, $B$ an $m$-dimensional Brownian motion, and $f \in C^{1,2}$, $df(t, X_t) = \partial_t f\, dt + \nabla f \cdot dX_t + \tfrac12 \operatorname{tr}(\sigma_t \sigma_t^{\mathsf T} D^2 f)\, dt$.*

*Proof.* The one-dimensional partition argument generalises componentwise. The second-order Taylor expansion of $f$ now carries cross terms $\tfrac12 \partial_{x_i x_j} f\,\Delta X^i\, \Delta X^j$. The covariation of the components is $d[X^i, X^j]_t = (\sigma_t \sigma_t^{\mathsf T})_{ij}\, dt$, because $d[B^a, B^b] = \delta_{ab}\, dt$ for independent Brownian coordinates and $dX^i = b^i\, dt + \sum_a \sigma^{ia}\, dB^a$, so $d[X^i, X^j] = \sum_{a,b} \sigma^{ia}\sigma^{jb}\, d[B^a, B^b] = \sum_a \sigma^{ia}\sigma^{ja}\, dt = (\sigma\sigma^{\mathsf T})_{ij}\, dt$; products involving $dt$ contribute nothing. Summing the Taylor terms over a shrinking partition, the first-order part assembles to $\nabla f \cdot dX$, the diagonal-and-cross second-order part to $\tfrac12 \sum_{i,j} \partial_{x_i x_j} f\, (\sigma\sigma^{\mathsf T})_{ij}\, dt = \tfrac12 \operatorname{tr}(\sigma\sigma^{\mathsf T} D^2 f)\, dt$, and the explicit time-dependence to $\partial_t f\, dt$. The remainder estimate from the one-dimensional case applies coordinatewise. $\square$

## Connections [Master]

Brownian motion [02.15.01] is the integrator on which this entire construction rests. The independence of increments and the Gaussian scaling $\mathbb{E}[(\Delta B)^2] = \Delta t$ established there are exactly what make the Itô isometry hold and what give $[B]_t = t$; without the non-anticipating filtration and the mean-zero independent increments of that unit, neither the martingale property of the integral nor the surviving $\tfrac12 f''$ correction term would be available.

The Lebesgue integral and monotone convergence [02.07.04] supply the integration theory used pathwise and in expectation throughout: the $ds$-integrals $\int_0^t f''(B_s)\, ds$ are ordinary Lebesgue integrals along each path, and the interchange of limit and expectation in the isometry extension and in the martingale-convergence steps rests on the convergence theorems proved there.

$L^p$ spaces and completeness [02.07.06] provide the target space for the central extension. The Itô integral is defined as the unique continuous extension of an isometry from simple integrands into $L^2(\Omega)$, and that extension exists precisely because $L^2(\Omega)$ is complete; the Burkholder-Davis-Gundy inequalities then live in the $L^p(\Omega)$ scale built in that unit.

Stochastic differential equations [02.15.03] are the immediate downstream consumer. Itô's formula is the change-of-variables rule that lets one solve SDEs in closed form (geometric Brownian motion, the exponential martingale) and is the analytic engine behind the Feynman-Kac and Kolmogorov correspondences that tie diffusion processes to second-order parabolic PDEs; the existence-uniqueness theory there runs on the isometry and the Burkholder-Davis-Gundy bounds proved here.

The Stratonovich integral [02.15.05] is the alternative construction that this unit pointedly does not use. Evaluating the integrand at the midpoint rather than the left endpoint restores the ordinary chain rule at the cost of the martingale property; the difference between the two integrals is exactly $\tfrac12 \int d[H, B]$, the same quadratic-covariation term that produces the Itô correction, so the contrast between the two conventions is a direct corollary of the calculus developed here.

## Historical & philosophical context [Master]

The stochastic integral was introduced by Kiyosi Itô in 1944 (*Stochastic Integral*, Proc. Imperial Academy Tokyo 20, 519–524) [ref: TODO_REF Itô 1944], building on Norbert Wiener's 1923 rigorous construction of Brownian motion and on Paul Lévy's structural study of its paths. Itô's decisive move was to define the integral for non-anticipating integrands and to prove the change-of-variables formula with its second-order correction term, the result now universally called Itô's formula; Wolfgang Doeblin had independently arrived at closely related ideas in a sealed note deposited with the Académie des Sciences in 1940 and opened only in 2000, so the lemma is sometimes called the Itô-Doeblin formula. The modern measure-theoretic treatment via the $L^2$ isometry and the martingale property is the synthesis of Joseph Doob's martingale theory with Itô's construction; the textbook accounts of Karatzas and Shreve and of Revuz and Yor [ref: TODO_REF Revuz 1999] codify the semimartingale generality, and Philip Protter's functional-analytic development takes the integral itself as the primitive object.

The conceptual content is that a calculus can be built on a path of infinite total variation provided one fixes the order in which information is revealed. The quadratic variation, which vanishes for every classically differentiable path, becomes the carrier of the new structure: it is the clock against which the second-order term is measured, and the choice of evaluation point (left endpoint for Itô, midpoint for Stratonovich) is the choice of which symmetry to preserve — the martingale property or the ordinary chain rule. Itô's framework gave probability theory its own differential calculus, and through the Feynman-Kac correspondence it supplied a probabilistic representation for solutions of second-order parabolic partial differential equations, a bridge that has organised diffusion theory ever since (Itô 1951, *On Stochastic Differential Equations*, Memoirs Amer. Math. Soc. 4).

## Bibliography [Master]

```bibtex
@article{ito1944,
  author  = {It\^o, Kiyosi},
  title   = {Stochastic Integral},
  journal = {Proceedings of the Imperial Academy (Tokyo)},
  volume  = {20},
  number  = {8},
  pages   = {519--524},
  year    = {1944}
}

@book{karatzas1991,
  author    = {Karatzas, Ioannis and Shreve, Steven E.},
  title     = {Brownian Motion and Stochastic Calculus},
  series    = {Graduate Texts in Mathematics},
  volume    = {113},
  edition   = {2nd},
  publisher = {Springer-Verlag, New York},
  year      = {1991}
}

@book{revuzyor1999,
  author    = {Revuz, Daniel and Yor, Marc},
  title     = {Continuous Martingales and Brownian Motion},
  series    = {Grundlehren der mathematischen Wissenschaften},
  volume    = {293},
  edition   = {3rd},
  publisher = {Springer-Verlag, Berlin},
  year      = {1999}
}

@book{oksendal2003,
  author    = {{\O}ksendal, Bernt},
  title     = {Stochastic Differential Equations: An Introduction with Applications},
  edition   = {6th},
  publisher = {Springer-Verlag, Berlin},
  year      = {2003}
}

@book{protter2005,
  author    = {Protter, Philip E.},
  title     = {Stochastic Integration and Differential Equations},
  series    = {Stochastic Modelling and Applied Probability},
  volume    = {21},
  edition   = {2nd},
  publisher = {Springer-Verlag, Berlin},
  year      = {2005}
}

@article{ito1951sde,
  author  = {It\^o, Kiyosi},
  title   = {On Stochastic Differential Equations},
  journal = {Memoirs of the American Mathematical Society},
  volume  = {4},
  pages   = {1--51},
  year    = {1951}
}
```
