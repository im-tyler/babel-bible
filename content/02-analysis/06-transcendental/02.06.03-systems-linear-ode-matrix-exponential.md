---
id: 02.06.03
title: "Systems of linear ODEs and the matrix exponential"
slug: systems-linear-ode-matrix-exponential
section: analysis
chapter: transcendental
concept_catalog_id: analysis.systems-ode-matrix-exponential
prerequisites:
  - 02.06.02
successors: []
tier_anchors:
  beginner: "3Blue1Brown essence of linear algebra + ODE visualisation; coupled-tank analogy"
  intermediate: "Boyce-Diprima Elementary Differential Equations Ch. 7-8; Apostol Calculus Vol. 2 Ch. 6"
  master: "Sylvester 1883 Comptes Rendus 94; Peano 1888 Integration par series; Coddington-Levinson Theory of Ordinary Differential Equations Ch. 3"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Sylvester 1883 — On the Equation to the Secular Inequalities in the Planetary Theory"
    locator: "Comptes Rendus 94, originator of the matrix exponential concept"
  - source: TODO_REF
    path: "Peano 1888 — Integration par series des equations differentielles lineaires"
    locator: "Math. Ann. 32, originator of the series definition of the matrix exponential for solving ODE systems"
  - source: TODO_REF
    path: "Boyce and Diprima — Elementary Differential Equations and Boundary Value Problems"
    locator: "Ch. 7-8, systems of first-order equations and the matrix exponential"
  - source: TODO_REF
    path: "Coddington and Levinson — Theory of Ordinary Differential Equations"
    locator: "Ch. 3, linear systems"
lean_status: none
lean_mathlib_gap: |
  Mathlib provides the matrix exponential via `Analysis.SpecialFunctions.Matrix.Complex` and
  `Analysis.ODE.Linear` with basic existence-uniqueness results for linear systems. The
  `Matrix.Exponential` namespace contains the power-series definition and proofs of basic
  properties like $\exp(0) = I$ and $\exp(A + B) = \exp(A)\exp(B)$ when $AB = BA$.
  What Mathlib does not consolidate in a pedagogically ordered treatment is: the proof
  that $\mathbf{x}(t) = e^{At}\mathbf{x}(0)$ solves $\mathbf{x}' = A\mathbf{x}$ via
  term-by-term differentiation of the power series; the computation of $e^{At}$ via
  diagonalisation $A = PDP^{-1}$ giving $e^{At} = Pe^{Dt}P^{-1}$; the Jordan canonical
  form approach for non-diagonalisable matrices; the fundamental matrix and its
  Wronskian-determinant identity $\det \Phi(t) = e^{\mathrm{tr}(A)t}\det\Phi(0)$
  (Liouville's formula); Lyapunov stability criteria; and the controllability and
  observability matrices from control theory.
human_reviewer: "TBD"
estimated_time:
  beginner: 15m
  intermediate: 35m
  master: 70m
status: shipped
produced_by: glm-5.1 (autonomous production driver)
reviewed_by: []
---

## Intuition [Beginner]

Imagine two water tanks connected by a pipe. Water flows from tank $1$ to tank $2$ at one rate, and from tank $2$ back to tank $1$ at a different rate. The water level in each tank changes over time, but the two levels are coupled: what happens in tank $1$ affects tank $2$ and vice versa.

A *system* of ODEs captures exactly this situation. Instead of one unknown function $y(t)$, there are several — say $x_1(t)$ and $x_2(t)$ — and each derivative depends on both unknowns. Writing the unknowns as a column vector $\mathbf{x}$ and the coupling coefficients as a matrix $A$, the entire system collapses to one compact equation: $\mathbf{x}' = A\mathbf{x}$.

The solution is the *matrix exponential* $e^{A t}$, which acts like the ordinary exponential $e^{at}$ but works for matrices. It turns a coupled system into a single formula: $\mathbf{x}(t) = e^{A t}\mathbf{x}(0)$. Why does this concept exist? Because most real-world systems have multiple interacting components, and the matrix exponential gives one unified way to solve them all.

## Visual [Beginner]

The picture shows two curves in the $(x_1, x_2)$-plane, starting from different initial points. Both spiral inward toward the origin, because the matrix $A$ has eigenvalues with negative real parts. The spiral shape comes from the complex eigenvalues, and the inward drift comes from the negative real parts.

![A phase portrait in the x1-x2 plane showing two solution trajectories spiralling inward toward the origin. The spirals come from complex eigenvalues of the matrix A, and the inward drift comes from eigenvalues with negative real parts.](/img/systems-linear-ode-matrix-exponential-placeholder.svg)

Each trajectory is the path traced by the tip of the vector $\mathbf{x}(t) = e^{At}\mathbf{x}(0)$ as time $t$ increases from $0$.

## Worked example [Beginner]

Solve the system $x_1' = -x_1 + 2x_2$, $x_2' = -2x_1 - x_2$ with initial conditions $x_1(0) = 1$, $x_2(0) = 0$.

Step 1. Write as a matrix equation: $\mathbf{x}' = A\mathbf{x}$ where $A = \begin{pmatrix} -1 & 2 \\ -2 & -1 \end{pmatrix}$.

Step 2. Find the eigenvalues of $A$: $\det(A - rI) = (r+1)^2 + 4 = 0$, so $r = -1 \pm 2i$.

Step 3. The real parts are $-1$ (decay), and the imaginary parts are $\pm 2$ (oscillation). The solution is $x_1(t) = e^{-t}\cos(2t)$, $x_2(t) = -e^{-t}\sin(2t)$.

What this tells us: the two water tanks exchange fluid in an oscillating pattern that decays over time. The matrix exponential captures both the oscillation (from the imaginary part of the eigenvalues) and the decay (from the negative real part).

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

A system of two first-order ODEs $x_1' = ax_1 + bx_2$, $x_2' = cx_1 + dx_2$ can be written as $\mathbf{x}' = A\mathbf{x}$. What are the dimensions of the matrix $A$?

A. $1 \times 2$
B. $2 \times 1$
C. $2 \times 2$
D. $1 \times 1$

<details class="exercise__hint"><summary>Hint</summary>

The matrix $A$ maps the two-component vector $(x_1, x_2)$ to the two-component vector $(x_1', x_2')$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**C.** A system of $2$ equations in $2$ unknowns requires a $2 \times 2$ matrix. An $n$-equation system requires an $n \times n$ matrix.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="true-false" markdown="1">

**Exercise (easy, true-false).**

If all eigenvalues of $A$ have negative real parts, then every solution of $\mathbf{x}' = A\mathbf{x}$ decays to zero as $t$ increases.

<details class="exercise__hint"><summary>Hint</summary>

Each eigenvalue $r = \alpha + i\beta$ contributes a factor $e^{\alpha t}$. If $\alpha < 0$, this factor decays.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**True.** The solution $\mathbf{x}(t) = e^{At}\mathbf{x}(0)$ involves factors of $e^{\alpha t}$ where $\alpha$ is the real part of each eigenvalue. When every $\alpha < 0$, every exponential factor decays, and the solution approaches the zero vector.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise (easy, numeric).**

A $3 \times 3$ matrix $A$ has eigenvalues $-2$, $-3$, and $-5$. All solutions of $\mathbf{x}' = A\mathbf{x}$ decay. Which eigenvalue controls the slowest rate of decay? State the answer as a single number.

<details class="exercise__hint"><summary>Hint</summary>

The slowest-decaying term has the eigenvalue closest to zero.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**$-2$.** The factor $e^{-2t}$ decays more slowly than $e^{-3t}$ or $e^{-5t}$. The eigenvalue with real part closest to zero dominates the long-term behaviour.

</details>

</aside>

## Formal definition [Intermediate+]

A *first-order linear homogeneous system* of ODEs with constant coefficients is

$$\mathbf{x}'(t) = A\mathbf{x}(t),$$

where $\mathbf{x} \colon \mathbb{R} \to \mathbb{R}^n$ (or $\mathbb{C}^n$) and $A$ is an $n \times n$ constant matrix.

The *matrix exponential* is defined by the convergent power series

$$e^{A t} = \sum_{k=0}^{\infty} \frac{(At)^k}{k!} = I + At + \frac{A^2 t^2}{2!} + \frac{A^3 t^3}{3!} + \cdots,$$

which converges for every $n \times n$ matrix $A$ and every $t \in \mathbb{R}$ (because $\|At\|^k / k! \leq (\|A\| |t|)^k / k!$ and the scalar exponential series converges).

**Fundamental matrix.** An $n \times n$ matrix function $\Phi(t)$ is a *fundamental matrix* for $\mathbf{x}' = A\mathbf{x}$ if each column of $\Phi$ is a solution and $\Phi(t)$ is invertible for all $t$. The matrix exponential $e^{At}$ is the unique fundamental matrix satisfying $\Phi(0) = I$.

### Counterexamples to common slips

- *$e^{A+B} \neq e^A e^B$ in general.* This identity holds only when $AB = BA$. For non-commuting matrices, the correct formula is the Baker-Campbell-Hausdorff expansion.

- *$\mathbf{x}' = A(t)\mathbf{x}$ with variable $A(t)$ does not have solution $e^{\int A(t)dt}$.* The matrix exponential formula applies only to constant-coefficient systems. Variable-coefficient systems require the Magnus expansion or other techniques.

- *$e^{At}$ is not computed by exponentiating each entry of $At$.* The matrix exponential is defined by the power series, not entrywise exponentiation. For diagonal matrices, $e^{At}$ does equal the diagonal matrix of exponentials, but this fails for general matrices.

## Key theorem with proof [Intermediate+]

**Theorem (Matrix exponential solution).** The unique solution of the initial value problem $\mathbf{x}' = A\mathbf{x}$, $\mathbf{x}(0) = \mathbf{x}_0$ is

$$\mathbf{x}(t) = e^{At}\mathbf{x}_0.$$

Moreover, $e^{At}$ satisfies:

1. $\frac{d}{dt}e^{At} = Ae^{At} = e^{At}A$.
2. $e^{A \cdot 0} = I$.
3. $e^{A(t+s)} = e^{At}e^{As}$ for all $t, s \in \mathbb{R}$.

*Proof of (1).* Differentiate the power series term by term:

$$\frac{d}{dt}e^{At} = \frac{d}{dt}\sum_{k=0}^{\infty}\frac{A^k t^k}{k!} = \sum_{k=1}^{\infty}\frac{A^k \cdot k t^{k-1}}{k!} = \sum_{k=1}^{\infty}\frac{A^k t^{k-1}}{(k-1)!}.$$

Re-indexing $j = k - 1$:

$$\frac{d}{dt}e^{At} = A\sum_{j=0}^{\infty}\frac{A^j t^j}{j!} = Ae^{At}.$$

Since $A^k = A \cdot A^{k-1} = A^{k-1} \cdot A$, the same computation gives $e^{At}A$. Term-by-term differentiation is justified because the series converges uniformly on every bounded interval (the derivative series has the same radius of convergence).

*Proof of (2).* At $t = 0$: $e^{A \cdot 0} = \sum_{k=0}^{\infty} A^k \cdot 0^k / k! = I + 0 + 0 + \cdots = I$.

*Proof of (3).* Fix $s$ and let $F(t) = e^{A(t+s)}$. Then $F'(t) = Ae^{A(t+s)} = AF(t)$ by property (1), and $F(0) = e^{As}$. The function $G(t) = e^{At}e^{As}$ satisfies $G'(t) = Ae^{At}e^{As} = AG(t)$ and $G(0) = e^{As}$. By uniqueness of solutions to $\mathbf{y}' = A\mathbf{y}$ with given initial condition, $F(t) = G(t)$.

*Proof of the solution formula.* The function $\mathbf{x}(t) = e^{At}\mathbf{x}_0$ satisfies $\mathbf{x}'(t) = Ae^{At}\mathbf{x}_0 = A\mathbf{x}(t)$ by property (1), and $\mathbf{x}(0) = e^{A \cdot 0}\mathbf{x}_0 = I\mathbf{x}_0 = \mathbf{x}_0$ by property (2). Uniqueness follows from the existence-uniqueness theorem for linear ODE systems. $\square$

**Bridge.** The foundational reason the matrix exponential solves the system is that the power series $\sum (At)^k/k!$ differentiates to $A$ times itself, and this is exactly the matrix analogue of the scalar identity $(d/dt)e^{at} = ae^{at}$. The central insight is that $e^{At}$ plays the role of the scalar exponential $e^{at}$, but in $n$ dimensions simultaneously. This result builds toward Lyapunov stability theory where the eigenvalues of $A$ determine whether $e^{At}$ decays, and appears again in [02.06.02] via the companion-matrix reduction where a scalar nth-order ODE becomes a system whose solution is $e^{At}$. Putting these together, the diagonalisation identity $e^{At} = Pe^{Dt}P^{-1}$ (where $D$ is diagonal with the eigenvalues) is the bridge that connects the abstract power series to concrete computations: the eigenvalues of $A$ control the behaviour of every solution.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 1 (easy, numeric).**

Compute $e^{At}$ for $A = \begin{pmatrix} 2 & 0 \\ 0 & -3 \end{pmatrix}$. Express each entry as a function of $t$.

<details class="exercise__hint"><summary>Hint</summary>

A diagonal matrix has $e^{Dt} = \mathrm{diag}(e^{d_1 t}, e^{d_2 t})$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$e^{At} = \begin{pmatrix} e^{2t} & 0 \\ 0 & e^{-3t} \end{pmatrix}$.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="numeric" markdown="1">

**Exercise 2 (easy, numeric).**

For $A = \begin{pmatrix} 0 & 1 \\ 0 & 0 \end{pmatrix}$, compute $A^2$. Hence write $e^{At}$ as a finite sum.

<details class="exercise__hint"><summary>Hint</summary>

Compute $A^2$: the nilpotent matrix satisfies $A^2 = 0$, so all higher powers vanish.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$A^2 = \begin{pmatrix} 0 & 0 \\ 0 & 0 \end{pmatrix}$. So $e^{At} = I + At = \begin{pmatrix} 1 & t \\ 0 & 1 \end{pmatrix}$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="multiple-choice" markdown="1">

**Exercise 3 (medium, multiple choice).**

Let $A = \begin{pmatrix} 3 & 0 \\ 0 & 3 \end{pmatrix}$. Then $e^{At}$ equals:

A. $\begin{pmatrix} e^{3t} & 0 \\ 0 & e^{3t} \end{pmatrix}$
B. $\begin{pmatrix} e^{3t} & 3t \\ 0 & e^{3t} \end{pmatrix}$
C. $e^{3t} I$
D. Both A and C

<details class="exercise__hint"><summary>Hint</summary>

$A = 3I$, so $A^k = 3^k I$ and $e^{At} = \sum (3t)^k/k! \cdot I = e^{3t} I$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**D.** Since $A = 3I$, every power $A^k = 3^k I$, and $e^{At} = (\sum (3t)^k/k!)I = e^{3t}I$. This is both the scalar multiple of the identity in option C and the diagonal matrix in option A.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 4 (medium, numeric).**

Solve $\mathbf{x}' = A\mathbf{x}$ with $A = \begin{pmatrix} 0 & 1 \\ -4 & 0 \end{pmatrix}$ and $\mathbf{x}(0) = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$. Find the eigenvalues of $A$ and write the solution components $x_1(t)$ and $x_2(t)$.

<details class="exercise__hint"><summary>Hint</summary>

Eigenvalues: $\det(A - rI) = r^2 + 4 = 0$, so $r = \pm 2i$. The solution involves $\cos(2t)$ and $\sin(2t)$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Eigenvalues $r = \pm 2i$. With eigenvector $\begin{pmatrix} 1 \\ 2i \end{pmatrix}$ for $r = 2i$, the real solutions are $\begin{pmatrix} \cos 2t \\ -2\sin 2t \end{pmatrix}$ and $\begin{pmatrix} \sin 2t \\ 2\cos 2t \end{pmatrix}$. From $\mathbf{x}(0) = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$: $x_1(t) = \cos(2t)$, $x_2(t) = -2\sin(2t)$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="numeric" markdown="1">

**Exercise 5 (medium, numeric).**

For $A = \begin{pmatrix} 1 & 2 \\ 0 & 3 \end{pmatrix}$, diagonalise $A$ as $A = PDP^{-1}$ and use this to compute $e^{A \cdot 1}$ (the matrix exponential at $t = 1$). Round each entry to three decimal places.

<details class="exercise__hint"><summary>Hint</summary>

Eigenvalues $1$ and $3$ (upper triangular). Eigenvectors: for $r = 1$, $(1, 0)^T$; for $r = 3$, $(1, 1)^T$. So $P = \begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}$, $D = \begin{pmatrix} 1 & 0 \\ 0 & 3 \end{pmatrix}$, $P^{-1} = \begin{pmatrix} 1 & -1 \\ 0 & 1 \end{pmatrix}$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$e^{A} = Pe^{D}P^{-1} = \begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}\begin{pmatrix} e & 0 \\ 0 & e^3 \end{pmatrix}\begin{pmatrix} 1 & -1 \\ 0 & 1 \end{pmatrix}$

$= \begin{pmatrix} e & e^3 \\ 0 & e^3 \end{pmatrix}\begin{pmatrix} 1 & -1 \\ 0 & 1 \end{pmatrix} = \begin{pmatrix} e & e^3 - e \\ 0 & e^3 \end{pmatrix}$

Numerically: $e \approx 2.718$, $e^3 \approx 20.086$, so $e^{A} \approx \begin{pmatrix} 2.718 & 17.368 \\ 0 & 20.086 \end{pmatrix}$.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="symbolic" markdown="1">

**Exercise 6 (medium, symbolic).**

Prove that $\det(e^{A}) = e^{\mathrm{tr}(A)}$ for any $n \times n$ matrix $A$.

<details class="exercise__hint"><summary>Hint</summary>

Use the identity $\det(AB) = \det(A)\det(B)$ and the fact that $\mathrm{tr}(A) = \sum \lambda_i$ where $\lambda_i$ are the eigenvalues (counted with multiplicity).

</details>

<details class="exercise__answer"><summary>Answer</summary>

For diagonalisable $A = PDP^{-1}$: $e^A = Pe^DP^{-1}$, so $\det(e^A) = \det(P)\det(e^D)\det(P^{-1}) = \det(e^D)$. Since $D = \mathrm{diag}(\lambda_1, \ldots, \lambda_n)$, $e^D = \mathrm{diag}(e^{\lambda_1}, \ldots, e^{\lambda_n})$ and $\det(e^D) = \prod e^{\lambda_i} = e^{\sum \lambda_i} = e^{\mathrm{tr}(A)}$.

For non-diagonalisable $A$: approximate $A$ by a sequence of diagonalisable matrices $A_k \to A$. The identity holds for each $A_k$, and both sides are continuous in $A$ (the determinant is a polynomial in entries of $e^A$, and $e^A$ is continuous in $A$), so it holds in the limit.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 7 (hard, symbolic).**

Prove Liouville's formula: if $\Phi(t)$ is a fundamental matrix for $\mathbf{x}' = A\mathbf{x}$, then $\det\Phi(t) = \det\Phi(0) \cdot e^{\mathrm{tr}(A)t}$.

<details class="exercise__hint"><summary>Hint</summary>

Differentiate $\det\Phi(t)$ using Jacobi's formula: $d/dt \det\Phi = \mathrm{tr}(\mathrm{adj}(\Phi) \cdot \Phi') = \mathrm{tr}(\Phi^{-1}\Phi' \cdot \Phi) \cdot (\det\Phi)/\det\Phi$... actually, use $d/dt \det\Phi = \det\Phi \cdot \mathrm{tr}(\Phi^{-1}\Phi')$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

By Jacobi's formula, $(d/dt)\det\Phi(t) = \det\Phi(t) \cdot \mathrm{tr}(\Phi(t)^{-1}\Phi'(t))$. Since $\Phi'(t) = A\Phi(t)$, we get $\Phi(t)^{-1}\Phi'(t) = \Phi(t)^{-1}A\Phi(t)$. The trace is invariant under similarity: $\mathrm{tr}(\Phi^{-1}A\Phi) = \mathrm{tr}(A)$. So $(d/dt)\det\Phi = \mathrm{tr}(A) \cdot \det\Phi$. This scalar ODE has solution $\det\Phi(t) = \det\Phi(0) \cdot e^{\mathrm{tr}(A)t}$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="symbolic" markdown="1">

**Exercise 8 (hard, symbolic).**

Let $A$ have a single eigenvalue $\lambda$ with algebraic multiplicity $n$ but geometric multiplicity $1$. Show that $e^{At} = e^{\lambda t}\sum_{k=0}^{n-1}\frac{(A - \lambda I)^k t^k}{k!}$.

<details class="exercise__hint"><summary>Hint</summary>

Write $A = \lambda I + N$ where $N = A - \lambda I$ is nilpotent with $N^n = 0$. Use the fact that $\lambda I$ commutes with every matrix.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Write $A = \lambda I + N$ where $N = A - \lambda I$. Since $\lambda I$ commutes with $N$, the identity $e^{A} = e^{\lambda I}e^{N}$ holds (even though $e^{A+B} = e^A e^B$ requires commutativity, it is satisfied here). Now $e^{\lambda I} = e^{\lambda}I$ (scalar times identity) and $e^{Nt} = \sum_{k=0}^{n-1} N^k t^k / k!$ because $N^n = 0$ and all higher powers vanish. Combining: $e^{At} = e^{\lambda t}I \cdot \sum_{k=0}^{n-1} N^k t^k / k! = e^{\lambda t}\sum_{k=0}^{n-1}(A - \lambda I)^k t^k / k!$.

</details>

</aside>

## Advanced results [Master]

**Theorem 1 (Diagonalisation formula).** If $A = PDP^{-1}$ with $D = \mathrm{diag}(\lambda_1, \ldots, \lambda_n)$, then $e^{At} = P\,\mathrm{diag}(e^{\lambda_1 t}, \ldots, e^{\lambda_n t})P^{-1}$. The columns of $P$ are eigenvectors of $A$, and each eigenvalue $\lambda_j = \alpha_j + i\beta_j$ contributes a factor $e^{\alpha_j t}(\cos\beta_j t + i\sin\beta_j t)$ to the solution.

**Theorem 2 (Jordan canonical form and the matrix exponential).** For any matrix $A$, there exists an invertible $P$ such that $A = PJP^{-1}$ where $J$ is the Jordan form: a block-diagonal matrix of Jordan blocks $J_i(\lambda) = \lambda I + N_i$ where $N_i$ is nilpotent. Then $e^{At} = Pe^{Jt}P^{-1}$ and each Jordan block contributes $e^{\lambda t}\sum_{k=0}^{m-1}N^k t^k/k!$ where $m$ is the block size. This handles non-diagonalisable matrices.

**Theorem 3 (Lyapunov stability theorem).** The equilibrium $\mathbf{x} = 0$ of $\mathbf{x}' = A\mathbf{x}$ is:

- *Asymptotically stable* if and only if every eigenvalue of $A$ has negative real part.
- *Stable* (but not asymptotically) if and only if every eigenvalue has non-positive real part and every eigenvalue with zero real part is semisimple (its algebraic and geometric multiplicities coincide).
- *Unstable* otherwise.

The Lyapunov equation $A^TP + PA = -Q$ (for any positive-definite $Q$) has a unique positive-definite solution $P$ if and only if $A$ is asymptotically stable.

**Theorem 4 (Controllability and the Kalman rank condition).** The system $\mathbf{x}' = A\mathbf{x} + B\mathbf{u}$ is *controllable* (any initial state can be driven to any target state in finite time by a suitable input $\mathbf{u}$) if and only if the controllability matrix $\mathcal{C} = [B \mid AB \mid A^2B \mid \cdots \mid A^{n-1}B]$ has full row rank $n$. This is the Kalman rank condition.

**Theorem 5 (Putzer's algorithm).** The matrix exponential can be computed without finding eigenvectors, using only the eigenvalues $\lambda_1, \ldots, \lambda_n$ and the powers of $A$. Define $P_0 = I$, $P_k = (A - \lambda_k I)P_{k-1}$, and solve the cascade of first-order linear ODEs $r_1' = \lambda_1 r_1$, $r_k' = \lambda_k r_k + r_{k-1}$ for $k \geq 2$, with $r_1(0) = 1$, $r_k(0) = 0$ for $k \geq 2$. Then $e^{At} = \sum_{k=1}^n r_k(t)P_{k-1}$.

**Theorem 6 (Non-homogeneous systems and variation of constants).** The solution of $\mathbf{x}' = A\mathbf{x} + \mathbf{f}(t)$ with $\mathbf{x}(t_0) = \mathbf{x}_0$ is

$$\mathbf{x}(t) = e^{A(t-t_0)}\mathbf{x}_0 + \int_{t_0}^{t} e^{A(t-s)}\mathbf{f}(s)\,ds.$$

This is the *variation of constants* (or *Duhamel*) formula.

**Synthesis.** The foundational reason the matrix exponential works is that the power series $\sum (At)^k/k!$ converges for every matrix and differentiates to $A$ times itself, and this is exactly the content that makes $\mathbf{x}(t) = e^{At}\mathbf{x}(0)$ solve the system. The central insight is that the eigenvalues of $A$ control the qualitative behaviour: decay, growth, oscillation, or a mix. Putting these together with the Jordan form, even non-diagonalisable matrices are handled by the nilpotent correction $e^{\lambda t}\sum N^k t^k/k!$. The bridge is between the abstract power series and the concrete eigenvalue decomposition $A = PDP^{-1}$ that reduces $e^{At}$ to scalar exponentials. This identification appears again in the companion matrix reduction of [02.06.02] where scalar nth-order equations become systems, the pattern generalises to the non-homogeneous Duhamel formula, and builds toward Lyapunov stability and controllability where the eigenvalue locations determine whether the system can be stabilised or controlled.

## Full proof set [Master]

**Proposition 1 (The power series for $e^{At}$ converges).** For any $n \times n$ matrix $A$ and any $t \in \mathbb{R}$, the series $\sum_{k=0}^{\infty}(At)^k/k!$ converges.

*Proof.* Let $\|\cdot\|$ be any submultiplicative matrix norm (e.g., the operator norm). Then $\|(At)^k/k!\| \leq (\|A\| \cdot |t|)^k / k!$. The series $\sum (\|A\||t|)^k/k! = e^{\|A\||t|}$ converges. By the Weierstrass M-test, the matrix series converges absolutely, hence converges. $\square$

**Proposition 2 ($e^{A+B} = e^A e^B$ when $AB = BA$).** If $AB = BA$, then $e^{A+B} = e^A e^B$.

*Proof.* When $AB = BA$, the binomial theorem applies to matrices: $(A + B)^k = \sum_{j=0}^{k}\binom{k}{j}A^j B^{k-j}$. So

$$e^{A+B} = \sum_{k=0}^{\infty}\frac{1}{k!}\sum_{j=0}^{k}\binom{k}{j}A^j B^{k-j} = \sum_{k=0}^{\infty}\sum_{j=0}^{k}\frac{A^j}{j!}\frac{B^{k-j}}{(k-j)!}.$$

This double sum equals $(\sum_j A^j/j!)(\sum_m B^m/m!) = e^A e^B$ by the Cauchy product formula, since both series converge absolutely. $\square$

## Connections [Master]

- **n-th-order linear ODE with constant coefficients [02.06.02].** The companion matrix reduction transforms every scalar nth-order ODE into a first-order system $\mathbf{x}' = A\mathbf{x}$, and the eigenvalues of the companion matrix are exactly the roots of the characteristic polynomial. The matrix exponential $e^{At}$ generalises the scalar exponential solutions $e^{rt}$ to the coupled setting, recovering the scalar theory when $n = 1$.

- **First-order linear and separable ODEs [02.08.01].** The existence-uniqueness theorem for first-order linear ODE systems provides the theoretical foundation for the solution formula $\mathbf{x}(t) = e^{At}\mathbf{x}(0)$. The scalar integrating-factor method from that unit is the $1 \times 1$ case of the matrix exponential formula.

- **Second-order linear ODE with constant coefficients [02.08.02].** A second-order equation $y'' + by' + cy = 0$ becomes a $2 \times 2$ system via the companion matrix $A = \begin{pmatrix} 0 & 1 \\ -c & -b \end{pmatrix}$. The eigenvalues of $A$ are the roots of $r^2 + br + c = 0$, and $e^{At}$ produces the same oscillatory, exponential, or critically-damped solutions as the scalar characteristic-polynomial method.

## Historical & philosophical context [Master]

Sylvester 1883, in *On the Equation to the Secular Inequalities in the Planetary Theory* [ref: TODO_REF Sylvester1883], introduced the matrix exponential in the context of solving systems of linear differential equations arising from celestial mechanics. His formulation recognised that the power series $\sum (At)^k/k!$ generalises the scalar exponential to matrices and satisfies the key differential-equation property.

Peano 1888, in *Integration par series des equations differentielles lineaires* [ref: TODO_REF Peano1888], gave the rigorous power-series definition of the matrix exponential and proved the fundamental properties: term-by-term differentiation yields $d/dt\, e^{At} = Ae^{At}$, and the composition $e^{A(t+s)} = e^{At}e^{As}$. Peano's treatment was the first to establish the matrix exponential as the canonical solution operator for linear ODE systems with constant coefficients. The modern synthesis — diagonalisation, Jordan form, stability theory, and the Kalman controllability criterion — was developed through the mid-twentieth century, with the Jordan-form computation of $e^{At}$ appearing in Gantmacher's *Theory of Matrices* (1959) and the controllability theory in Kalman's foundational papers of 1960-1963.

## Bibliography [Master]

```bibtex
@article{Sylvester1883,
  author = {Sylvester, James Joseph},
  title = {Sur les quantites formant un groupe de nonnes analogues aux quaternions de Hamilton},
  journal = {Comptes Rendus de l'Academie des Sciences, Paris},
  volume = {94},
  year = {1883},
  pages = {1336--1340},
}

@article{Peano1888,
  author = {Peano, Giuseppe},
  title = {Integration par series des equations differentielles lineaires},
  journal = {Mathematische Annalen},
  volume = {32},
  year = {1888},
  pages = {450--456},
}

@book{BoyceDiPrima2012,
  author = {Boyce, William E. and DiPrima, Richard C.},
  title = {Elementary Differential Equations and Boundary Value Problems},
  publisher = {Wiley},
  year = {2012},
  edition = {10th},
}

@book{CoddingtonLevinson1955,
  author = {Coddington, Earl A. and Levinson, Norman},
  title = {Theory of Ordinary Differential Equations},
  publisher = {McGraw-Hill},
  year = {1955},
}

@book{Gantmacher1959,
  author = {Gantmacher, Felix R.},
  title = {The Theory of Matrices},
  publisher = {Chelsea},
  year = {1959},
}
```
