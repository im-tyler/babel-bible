---
id: 02.01.06
title: Quotient and identification topology
slug: quotient-topology
section: analysis
chapter: topology
concept_catalog_id: topology.quotient-topology
prerequisites:
  - 02.01.01  # topological space
  - 02.01.02  # continuous map
successors:
  - 03.12.01  # homotopy and homotopy group
  - 03.12.02  # covering space
tier_anchors:
  beginner: "Brown — *Topology and Groupoids* §4 (informal universal-property treatment); Hatcher §0"
  intermediate: "Brown §4; Hatcher §0; Munkres *Topology* §22"
  master: "Brown §4; tom Dieck §1.5; Strickland *Topology and Groupoids* class notes"
tiers_present: [beginner, intermediate, master]
pending_prereqs: false
references:
  - source: TODO_REF
    path: "Brown — Topology and Groupoids"
    locator: "§4 identification topology"
  - source: TODO_REF
    path: "Hatcher — Algebraic Topology"
    locator: "§0 prelim constructions"
  - source: TODO_REF
    path: "Munkres — Topology"
    locator: "§22 quotient topology"
lean_status: none
lean_mathlib_gap: |
  Mathlib has Quotient and the topological quotient via TopologicalSpace.coinduced;
  what's missing is a clean packaging of the universal property as a categorical
  colimit, the standard adjunction-space pushout, and proven mapping-cylinder /
  mapping-cone constructions with the deformation-retract lemmas for cofibrations
  in the spirit of Brown §4. The Lean layer would need: (1) the universal-property
  lemma in pushout form, (2) explicit constructions of cone/suspension/mapping
  cone with their universal properties verified, (3) the cellular-pushout square
  used to build CW-complexes inductively.
human_reviewer: "TBD"
estimated_time:
  beginner: 12m
  intermediate: 35m
  master: 60m
status: shipped
produced_by: claude-opus-4-7 (gap-driven from Brown audit 2026-05-07)
reviewed_by: []
---

## Intuition [Beginner]

A **quotient topology** is what you get when you collapse parts of a space according to an equivalence relation: glue points together, and ask which subsets count as open in the result. The rule is the natural one — a set in the quotient is open exactly when its preimage in the original space is open.

This single construction packages a huge family of basic operations:

- **Cone $CX$** — collapse one end of $X \times [0,1]$ to a point. Every space embeds in its cone, and the cone is contractible.
- **Suspension $SX$** — collapse both ends. The suspension of $S^n$ is $S^{n+1}$.
- **Mapping cylinder $M_f$** of $f: X \to Y$ — glue one end of $X \times [0,1]$ to $Y$ via $f$. $M_f$ deformation-retracts onto $Y$ and replaces $f$ by a cofibration.
- **Mapping cone $C_f$** — quotient $M_f$ by collapsing the other end. The reduced version is $\mathrm{cofib}(f)$ in homotopy theory.
- **Adjunction space $X \cup_f Y$** — glue $X$ to $Y$ along a map $f: A \to Y$ from a subspace $A \subseteq X$. CW skeleta are built by iterated adjunction.

Brown's textbook treats all of these uniformly through the universal property of the quotient: a continuous map out of a quotient space is the same data as a continuous map out of the original space that is constant on equivalence classes.

## Visual [Beginner]

A square gets its top edge collapsed to a single point: the result is a triangle, and the topology on the triangle is the one in which open sets are exactly the images of open sets in the square that respect the collapse.

![A schematic placeholder diagram for quotient topology.](/img/quotient-topology-placeholder.svg)

The same picture, with both top and bottom edges collapsed, is the suspension of the horizontal interval.

## Worked example [Beginner]

Take the closed interval $X = [0, 1]$ and identify $0 \sim 1$. The quotient $X/{\sim}$ is the circle $S^1$.

To see this is the *correct* topology on the circle, check the universal property: a continuous map $S^1 \to Y$ is the same data as a continuous map $f: [0, 1] \to Y$ with $f(0) = f(1)$. Trace through this with $Y = \mathbb{R}^2$ and $f(t) = (\cos 2\pi t, \sin 2\pi t)$ — the closed-curve condition $f(0) = f(1)$ is exactly the constraint that says you've drawn a loop, and the universal property turns it into a continuous map of the circle.

## Check your understanding [Beginner]

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

In the quotient topology on $X/{\sim}$, when is a set $U \subseteq X/{\sim}$ open?

A. When some preimage in $X$ is open  
B. When the preimage $q^{-1}(U)$ under the quotient map is open in $X$  
C. When $U$ is closed in $X$  
D. When $U$ has finitely many points

<details class="exercise__hint"><summary>Hint</summary>

The quotient topology is defined to make the projection map continuous in the most permissive way.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. When the preimage $q^{-1}(U)$ under the quotient map is open in $X$.**

This is the *largest* topology on $X/{\sim}$ that makes the quotient map continuous.

</details>

</aside>

<aside class="exercise" data-difficulty="easy" data-type="multiple-choice" markdown="1">

**Exercise (easy, multiple choice).**

What is the quotient of the closed disc $D^2$ where the entire boundary circle is collapsed to a single point?

A. The disc itself  
B. The 2-sphere $S^2$  
C. A torus  
D. A Möbius band

<details class="exercise__hint"><summary>Hint</summary>

Imagine pulling the boundary together like a drawstring bag.

</details>

<details class="exercise__answer"><summary>Answer</summary>

**B. The 2-sphere $S^2$.**

The disc with boundary collapsed is the 2-sphere; this is one cell in the standard CW structure on $S^2$.

</details>

</aside>

## Formal definition [Intermediate+]

Let $X$ be a topological space and $\sim$ an equivalence relation on $X$. Let $q: X \to X/{\sim}$ be the projection map. The **quotient topology** on $X/{\sim}$ is the collection of subsets $U \subseteq X/{\sim}$ such that $q^{-1}(U)$ is open in $X$.

Equivalent reformulation in terms of an arbitrary surjection: given any surjection $q: X \to Y$ of sets with $X$ a topological space, the **identification topology** on $Y$ is $\{U \subseteq Y : q^{-1}(U) \text{ open in } X\}$. The pair $(Y, \text{identification topology})$ is called the *identification space* and $q$ is then automatically a quotient map.

**Universal property.** *For any topological space $Z$ and continuous map $f: X \to Z$ that is constant on equivalence classes (equivalently, factors set-theoretically through $q$), there is a unique continuous map $\tilde f: X/{\sim} \to Z$ with $\tilde f \circ q = f$.*

This is the defining property: quotient spaces *are* the spaces representing the functor $f \mapsto \{f : X \to Z \text{ continuous, factoring through } q\}$.

A quotient map $q$ need not be open or closed in general; what's preserved is the universal property and continuity in the "minimal" direction.

## Key theorem with proof [Intermediate+]

**Theorem (universal property of the quotient topology).** *Let $q: X \to Y$ be a surjection equipped with the identification topology. For any topological space $Z$ and any function $f: Y \to Z$, the composition $f \circ q: X \to Z$ is continuous if and only if $f$ is continuous.*

**Proof.** *($\Rightarrow$).* Suppose $f \circ q$ is continuous and let $V \subseteq Z$ be open. Then $(f \circ q)^{-1}(V) = q^{-1}(f^{-1}(V))$ is open in $X$. By definition of the identification topology, $f^{-1}(V)$ is open in $Y$. Hence $f$ is continuous.

*($\Leftarrow$).* If $f$ is continuous, $f \circ q$ is a composition of continuous maps and is continuous. $\square$

**Corollary.** *Two surjections $q_1: X \to Y$ and $q_2: X \to Y$ inducing the same partition of $X$ give the same identification topology on $Y$.*

**Bridge.** This universal-property packaging builds toward [03.12.01] (homotopy and homotopy group), where every standard construction — cone, suspension, mapping cone, mapping cylinder, smash product, adjunction — is exactly a quotient and the universal property is the foundational reason continuous-map computations in algebraic topology lift cleanly to homotopy classes. The same construction appears again in [03.12.02] (covering space), where the orbit space $\tilde X / G$ of a properly discontinuous action of a discrete group is the basic source of covering spaces. Putting these together, identification topology is the bridge between point-set topology and the world of CW complexes — every CW complex is built by iterated quotients of disjoint unions of discs, and the universal property generalises to the cellular pushout that defines a CW structure.

## Exercises [Intermediate+]

<aside class="exercise" data-difficulty="easy" data-type="short-answer" markdown="1">

**Exercise 1 (easy, short-answer).**

Let $X = \mathbb{R}$ and define $\sim$ by $x \sim y$ iff $x - y \in \mathbb{Z}$. Identify $X/{\sim}$ as a topological space.

<details class="exercise__hint"><summary>Hint</summary>

Look at a fundamental domain $[0, 1)$ and check what the topology gives near the boundary.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$X/{\sim} \cong S^1$, the circle. The quotient map $\mathbb{R} \to S^1$ is $t \mapsto (\cos 2\pi t, \sin 2\pi t)$, and the identification topology on $S^1$ matches the subspace topology from $\mathbb{R}^2$. Rubric: full credit for the homeomorphism plus checking the topology near 0.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 2 (medium, short-answer).**

Show that the suspension $S X = X \times [0, 1] / \bigl((x, 0) \sim (x', 0), (x, 1) \sim (x', 1)\bigr)$ of $S^n$ is homeomorphic to $S^{n+1}$.

<details class="exercise__hint"><summary>Hint</summary>

Think of $S^{n+1}$ as the union of upper and lower hemispheres glued along the equator, with each hemisphere a cone on $S^n$.

</details>

<details class="exercise__answer"><summary>Answer</summary>

$S^{n+1} = D^{n+1}_+ \cup_{S^n} D^{n+1}_-$ where $D^{n+1}_\pm$ are upper and lower hemispheres. Each $D^{n+1}_\pm \cong CS^n$ (cone on $S^n$ via the central projection from the pole). Gluing two cones on $S^n$ along their bases gives exactly $SS^n$. Apply the universal property to define the homeomorphism. Rubric: full credit for the cone-pair-gluing identification.

</details>

</aside>

<aside class="exercise" data-difficulty="medium" data-type="short-answer" markdown="1">

**Exercise 3 (medium, short-answer).**

Let $f: X \to Y$ be a continuous map. The mapping cylinder is $M_f = (X \times [0, 1]) \sqcup Y / \bigl((x, 1) \sim f(x)\bigr)$. Show that $M_f$ deformation-retracts onto $Y$.

<details class="exercise__hint"><summary>Hint</summary>

Slide $X \times [0, 1]$ along the second factor.

</details>

<details class="exercise__answer"><summary>Answer</summary>

Define $H: M_f \times [0, 1] \to M_f$ by $H((x, s), t) = (x, s + t(1 - s))$ on $X \times [0, 1)$ and the identity on $Y$. At $t = 0$ this is the identity on $M_f$; at $t = 1$ it sends every $(x, s)$ to $(x, 1) \sim f(x) \in Y$. Continuity uses the universal property of the quotient. Rubric: full credit for the homotopy $H$ and the verification at $t = 0, 1$.

</details>

</aside>

<aside class="exercise" data-difficulty="hard" data-type="short-answer" markdown="1">

**Exercise 4 (hard, short-answer).**

Define the **adjunction space** $Y \cup_f X$ for a map $f: A \to Y$ where $A \subseteq X$ is a subspace: it is $(X \sqcup Y) / (a \sim f(a) \text{ for } a \in A)$. Prove the universal property: for any space $Z$ and pair $(g: X \to Z, h: Y \to Z)$ with $g|_A = h \circ f$, there is a unique continuous map $Y \cup_f X \to Z$ extending both.

<details class="exercise__hint"><summary>Hint</summary>

Apply the universal property of the disjoint-union and quotient topologies in succession.

</details>

<details class="exercise__answer"><summary>Answer</summary>

The disjoint-union universal property gives a continuous $g \sqcup h: X \sqcup Y \to Z$. Compatibility $g|_A = h \circ f$ ensures it factors through the equivalence relation, hence through the quotient by the universal property of the quotient topology. Uniqueness is immediate from the surjectivity of the quotient map. Rubric: full credit for the two-stage application of universal properties.

</details>

</aside>

## Advanced results [Master]

The identification topology is the unique topology on $Y$ making $q: X \to Y$ continuous and satisfying the universal property — and the universal property is what packages a long list of constructions in algebraic topology.

**Cellular constructions.** A *CW complex* $X$ is built by iterated adjunction: $X^{(n)} = X^{(n-1)} \cup_{\partial} \bigsqcup_\alpha D^n_\alpha$ along the attaching maps $\partial D^n_\alpha \to X^{(n-1)}$. Each step is an adjunction space; each cell-attachment changes the homotopy type in a controlled way, and the universal property of the quotient gives a clean cellular pushout in $\mathbf{Top}$:

$$
\begin{array}{ccc}
\bigsqcup_\alpha S^{n-1} & \to & X^{(n-1)} \\
\downarrow & & \downarrow \\
\bigsqcup_\alpha D^n & \to & X^{(n)}
\end{array}
$$

The corresponding statement after passing to fundamental group / fundamental groupoid is exactly the cellular form of Seifert-van Kampen [03.12.09] — the pushout of groupoids matches the pushout of spaces.

**Smash and wedge products.** The wedge sum $X \vee Y = (X \sqcup Y) / (x_0 \sim y_0)$ identifies basepoints. The smash product is

$$
X \wedge Y = (X \times Y) / (X \vee Y)
$$

where $X \vee Y \subseteq X \times Y$ embeds via the basepoints. Smash gives the symmetric monoidal structure on pointed spaces underlying spectra and stable homotopy [03.12.04, 03.12.06].

**Mapping cone and cofibre sequence.** The reduced mapping cone $C_f = (CX) \cup_f Y / (\mathrm{cone\ apex})$ fits into the canonical cofibre sequence

$$
X \xrightarrow{f} Y \to C_f \to \Sigma X \to \Sigma Y \to \cdots
$$

This is the long exact sequence in (co)homology after applying any cohomology theory; it is the homotopy-theoretic analogue of an exact triangle.

**Quotient by group action.** Given a group $G$ acting continuously on $X$, the orbit space $X/G$ carries the identification topology. The action is *properly discontinuous* if every $x \in X$ has a neighbourhood $U$ with $gU \cap U = \emptyset$ for all $g \neq e$; in that case $X \to X/G$ is a covering map and the action is the most basic source of covering spaces [03.12.02].

**Synthesis.** Quotient topology generalises the elementary "glue some points together" intuition to a categorical statement: it is *exactly* the colimit of the equivalence relation in $\mathbf{Top}$, and the universal property is the foundational reason it can be reasoned about without coordinates. Read in the opposite direction, the identification topology is dual to the subspace topology — where subspace is the limit ($\lim$) along an inclusion, quotient is the colimit ($\mathrm{colim}$) along a surjection. The central insight that organises the entire CW-complex programme is exactly this: building a complicated space by iterated quotient operations identifies algebra with geometry — generators correspond to cells, relations to attaching maps, and the cellular chain complex is exactly the algebraic shadow of the cellular pushout.

## Full proof set [Master]

**Proposition.** *The identification topology is the largest topology on $Y$ for which $q: X \to Y$ is continuous.*

*Proof.* Suppose $\tau$ is any topology on $Y$ making $q$ continuous. For $U \in \tau$, $q^{-1}(U)$ is open in $X$, so $U$ is open in the identification topology. Hence $\tau$ is contained in the identification topology. $\square$

**Proposition.** *Let $f: X \to Y$ be a quotient map (surjection equipped with the identification topology). Composition with $f$ defines a bijection between continuous maps $g: Y \to Z$ and continuous maps $\tilde g: X \to Z$ that are constant on $f$-fibres.*

*Proof.* The forward direction sends $g$ to $g \circ f$, which is continuous (composition) and constant on $f$-fibres ($f$ is surjective, so $f$-fibres are non-empty). The inverse sends $\tilde g$ to its set-theoretic factorisation $g$ through $f$; continuity of $g$ follows from the universal property. The two maps are inverse on the nose. $\square$

**Proposition.** *A quotient of a Hausdorff space need not be Hausdorff.*

*Proof.* Take $X = \mathbb{R}$ with the relation $x \sim y$ iff $x = y$ or $\{x, y\} = \{0, 1\}$ (identify only $0$ with $1$). Then $X/{\sim}$ is Hausdorff (the equivalence classes are closed). But take $X = \mathbb{R} \sqcup \mathbb{R}$ with the relation that identifies the two zero points and only those: the quotient is the "line with two origins", a non-Hausdorff manifold-like object. The two zero points cannot be separated. $\square$

**Theorem (compactness preservation).** *If $X$ is compact and $q: X \to Y$ is a surjective quotient map, then $Y$ is compact.*

*Proof.* Let $\{U_\alpha\}$ be an open cover of $Y$. By definition of the identification topology, $\{q^{-1}(U_\alpha)\}$ is an open cover of $X$. By compactness, finitely many $q^{-1}(U_{\alpha_1}), \ldots, q^{-1}(U_{\alpha_n})$ cover $X$. Then $U_{\alpha_1}, \ldots, U_{\alpha_n}$ cover $Y = q(X)$. $\square$

## Connections [Master]

- **Topological space [02.01.01].** The identification topology is the standard categorical mechanism for building new topological spaces by gluing — the colimit construction in $\mathbf{Top}$.

- **Continuous map [02.01.02].** The universal property is the cleanest form of the statement that continuity passes through quotients: a function out of a quotient is continuous iff its lift to the source is.

- **Homotopy and homotopy group [03.12.01].** Every basic construction in algebraic topology — cone, suspension, mapping cone, mapping cylinder, wedge, smash — is a quotient or adjunction space. Homotopy invariance of these constructions follows from the universal property.

- **Covering space [03.12.02].** Orbit spaces of properly discontinuous group actions are quotient spaces; the quotient map is a covering map. This is the basic source of nontrivial covering spaces.

- **Seifert-van Kampen theorem [03.12.09].** The cellular pushout that defines a CW complex matches the groupoid pushout that computes its fundamental groupoid. Adjunction spaces and the groupoid Seifert-van Kampen are the same construction read at the level of spaces and at the level of groupoids.

## Historical & philosophical context [Master]

The quotient topology has been part of the language of point-set topology since Hausdorff's 1914 *Grundzüge der Mengenlehre* [ref: TODO_REF Hausdorff 1914]. Its central role in algebraic topology — through CW complexes, adjunction spaces, mapping cones, and smash products — was developed during the mid-20th-century algebraic-topology revolution, especially in work by J. H. C. Whitehead (*Combinatorial homotopy*, 1949) [ref: TODO_REF Whitehead 1949]. The CW-complex framework Whitehead introduced makes the iterated-quotient construction the basic mechanism for building all spaces of homotopy-theoretic interest.

Ronald Brown's *Topology and Groupoids* (1968 / 2006) [ref: TODO_REF Brown 2006] is one of the few textbooks to put the universal property of the identification topology *first* and treat all the standard constructions through it uniformly. Brown's framing is what makes the analogy between cellular pushouts of spaces and pushouts of fundamental groupoids transparent, and it sets up the higher-categorical generalisations of the same construction (filtered spaces, crossed complexes, ∞-groupoids).

In contemporary foundations, the universal property of the quotient topology is the prototype of a *higher inductive type* in homotopy type theory: the type-theoretic construction of the suspension $\Sigma X$ as a higher inductive type with two point-constructors and a path-constructor for each $x \in X$ is exactly the universal property of $\Sigma X$ as a quotient of $X \times [0,1]$.

## Bibliography [Master]

```bibtex
@book{Hausdorff1914Grundzuge,
  author    = {Hausdorff, Felix},
  title     = {Grundz{\"u}ge der Mengenlehre},
  publisher = {Veit \& Comp.},
  year      = {1914}
}

@article{Whitehead1949Combinatorial,
  author  = {Whitehead, J. H. C.},
  title   = {Combinatorial homotopy. {I}},
  journal = {Bull. Amer. Math. Soc.},
  volume  = {55},
  year    = {1949},
  pages   = {213--245}
}

@book{BrownTopologyAndGroupoids,
  author    = {Brown, Ronald},
  title     = {Topology and Groupoids},
  publisher = {BookSurge},
  year      = {2006}
}

@book{HatcherAlgebraicTopology,
  author    = {Hatcher, Allen},
  title     = {Algebraic Topology},
  publisher = {Cambridge University Press},
  year      = {2002}
}

@book{MunkresTopology,
  author    = {Munkres, James R.},
  title     = {Topology},
  publisher = {Prentice Hall},
  year      = {2000},
  edition   = {2nd}
}
```
