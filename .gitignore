# OS
.DS_Store
._*
nul

# Editors
.vscode/
.idea/
*.swp

# Node / build
node_modules/
.pnpm-store/
.turbo/
dist/
build/
.next/
.astro/
out/
.cache/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Env / secrets
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Local CC settings — only ignore the per-machine settings file.
# Slash commands and shared agents live under .claude/ and SHOULD be tracked.
.claude/settings.local.json

# LEAN — build artifacts (6.9G of cache+deps, regenerable via lake)
.lake/
lean-packages/
lake-packages/

# REFERENCE — vendored upstream textbooks/papers (1G, not your work)
reference/

# --- transient agent/session context: never publish ---
SESSION_NOTES.md
*_NOTES.md
HANDOFF*.md
CONTINUATION_PROMPT.md
*_NEXT_SESSION.md
.opencode/
